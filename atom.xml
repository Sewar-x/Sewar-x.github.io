<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en-US">
  <id>https://sewar-x.github.io/</id>
  <title>Sewar Blog</title>
  <subtitle>Hello! WelCome to Sewar Blog</subtitle>
  <updated>2021-07-19T12:57:48.402Z</updated>
  <generator>@mr-hope/vuepress-plugin-feed</generator>
  <link rel="self" href="https://sewar-x.github.io/atom.xml"/>
  <link rel="alternate" href="https://sewar-x.github.io/"/>
  <entry>
    <title type="html">Vue Vs React</title>
    <id>https://sewar-x.github.io/Frame-contrast/</id>
    <link href="https://sewar-x.github.io/Frame-contrast/"/>
    <updated>2021-07-12T17:26:17.000Z</updated>
    <content type="html"><![CDATA[<h2 id="vue-vs-react"> Vue Vs React</h2>
<h3 id="总览"> 总览</h3>
<p><img src="./../images/Vue-vs-React.png" alt="" /></p>
<p><strong>参考资料</strong></p>
<p><a href="https://blog.csdn.net/xgangzai/article/details/115301290" target="_blank" rel="noopener noreferrer">谈谈Vue和React的区别？</a></p>
<h4 id="区别"> 区别</h4>
<h5 id="生命周期"> 生命周期</h5>
<p>React:</p>
<ul>
<li>组件的生命周期可分成三个状态：
<ul>
<li>Mounting：已插入真实 DOM</li>
<li>Updating：正在被重新渲染</li>
<li>Unmounting：已移出真实 DOM</li>
</ul>
</li>
<li>生命周期的方法有：
<ul>
<li><strong>componentWillMount</strong> 在渲染前调用,在客户端也在服务端。</li>
<li><strong>componentDidMount</strong> : 在第一次渲染后调用，只在客户端。之后组件已经生成了对应的DOM结构，可以通过this.getDOMNode()来进行访问。 如果你想和其他JavaScript框架一起使用，可以在这个方法中调用setTimeout, setInterval或者发送AJAX请求等操作(防止异步操作阻塞UI)。</li>
<li><strong>componentWillReceiveProps</strong> 在组件接收到一个新的 prop (更新后)时被调用。这个方法在初始化render时不会被调用。</li>
<li><strong>shouldComponentUpdate</strong> 返回一个布尔值。在组件接收到新的props或者state时被调用。在初始化时或者使用forceUpdate时不被调用。
可以在你确认不需要更新组件时使用。</li>
<li><strong>componentWillUpdate</strong>在组件接收到新的props或者state但还没有render时被调用。在初始化时不会被调用。</li>
<li><strong>componentDidUpdate</strong> 在组件完成更新后立即调用。在初始化时不会被调用。</li>
<li><strong>componentWillUnmount</strong>在组件从 DOM 中移除之前立刻被调用。</li>
</ul>
</li>
</ul>
<p>这些方法的详细说明，可以参考<a href="http://facebook.github.io/react/docs/component-specs.html#lifecycle-methods" target="_blank" rel="noopener noreferrer">官方文档</a>。</p>
<p><img src="./../images/react生命周期.png" alt="image-20210712094013630" /></p>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-07-12T03:10:17.000Z</published>
  </entry>
  <entry>
    <title type="html">前端工程化</title>
    <id>https://sewar-x.github.io/Front-end-Engineering/</id>
    <link href="https://sewar-x.github.io/Front-end-Engineering/"/>
    <updated>2021-07-01T16:57:35.000Z</updated>
    <content type="html"><![CDATA[<h1 id="前端工程化"> 前端工程化</h1>
<ul>
<li>
<p>概念：根据实际业务的特点，将前端开发的<strong>整个流程</strong> <strong>规范化、标准化</strong>，从创建项目——编码——预览测试——提交——部署，都遵循一定的标准和规范，通过不同的步骤采用不同的<strong>工具</strong>提高开发效率，减低成本的一种手段。</p>
</li>
<li>
<p>前端工程化涉及内容：</p>
<p><img src="./../images/前端工程化.png" alt="前端工程化" /></p>
</li>
<li>
<p>前端工程化相关工具</p>
<p><img src="./../images/前端工程化相关工具.png" alt="" /></p>
</li>
</ul>
<h2 id="脚手架"> 脚手架</h2>
<ul>
<li>
<p>概念：脚手架本质是开发人员的一个操作系统的客户端工具。它通过命令行执行。</p>
</li>
<li>
<p>脚手架作用：创建项目基本结构 提供项目规范和约定。</p>
<ul>
<li>
<p>相同的<strong>组织结构</strong></p>
</li>
<li>
<p>相同的<strong>开发范式</strong></p>
</li>
<li>
<p>相同的<strong>模块依赖</strong></p>
</li>
<li>
<p>相同的<strong>工具配置</strong></p>
</li>
<li>
<p>相同的<strong>代码基础</strong></p>
<p><img src="./../images/脚手架生成.png" alt="" /></p>
</li>
</ul>
</li>
<li>
<p>为什么需要脚手架？</p>
<ul>
<li>脚手架是实现前端自动化的关键工具。</li>
<li>开发脚手架的核心目标是：提升前端研发效能。</li>
<li>价值：
<ul>
<li>自动化：项目重复代码拷贝 / git操作 / 发布上线操作，实现自动化操作，减少人工操作风险</li>
<li>标准化：项目创建/git flow/发布流程/回滚流程，实现多个团队各个项目的一系列技术流程、技术操作标准化</li>
<li>数据化：研发过程系统化，数据化，使得研发过程可量化。比如新建项目的时间，我们可以通过脚手架工具实现其可统计为时间指标，有了指标就能够进一步为我们后续的脚手架迭代带来方向和目标。</li>
</ul>
</li>
</ul>
</li>
<li>
<p>脚手架分类：</p>
<ul>
<li>
<p>通用脚手架工具：</p>
<ul>
<li><strong>Yeoman</strong> 通用项目脚手架 灵活易拓展</li>
<li><strong>Plop</strong> 创建特定类型的文件/模块</li>
</ul>
</li>
<li>
<p>专用脚手架工具：</p>
<ul>
<li>vue 脚手架工具： <a href="https://cli.vuejs.org/zh/" target="_blank" rel="noopener noreferrer">vue-cli</a></li>
</ul>
<p><img src="./../images/脚手架分类.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
<h3 id="yeoman"> <a href="https://yeoman.io/learning/index.html" target="_blank" rel="noopener noreferrer">Yeoman</a></h3>
<ul>
<li>
<p>概念：</p>
<ul>
<li>
<p>Yeoman 是一个通用的脚手架系统，允许创建任何类型的应用程序。它允许快速开始新项目并简化现有项目的维护。</p>
</li>
<li>
<p>Yeoman 是语言不可知论者。它可以生成任何语言的项目（Web、Java、Python、C#等）</p>
</li>
<li>
<p>Yeoman 本身不做任何决定。每个决定都是由<em>生成器</em>做出的，<em>生成器</em>基本上是 Yeoman 环境中的插件。有<a href="https://yeoman.io/generators/" target="_blank" rel="noopener noreferrer">很多公开可用的生成器，</a>并且很容易<a href="https://yeoman.io/authoring/" target="_blank" rel="noopener noreferrer">创建一个新的</a><a href="https://yeoman.io/generators/" target="_blank" rel="noopener noreferrer">生成器</a>来匹配任何工作流程。Yeoman 始终是满足您脚手架需求的正确选择。</p>
<p>以下是一些常见的用例：</p>
<ul>
<li>快速创建新项目</li>
<li>创建项目的新部分，例如带有单元测试的新控制器</li>
<li>创建模块或包</li>
<li>引导新服务</li>
<li>执行标准、最佳实践和风格指南</li>
<li>通过让用户开始使用示例应用程序来推广新项目</li>
</ul>
</li>
</ul>
</li>
<li>
<p>yo 是 Yeoman 的命令行管理工具，可以在命令行运行 yeoman 的命令。</p>
</li>
<li>
<p>generator 生成器则是 Yeoman 中具体的脚手架。针对不同的项目，例如网站、APP、小程序等，有不同的脚手架生成器。</p>
<p><img src="./../images/脚手架yo流程.png" alt="" /></p>
</li>
<li>
<p>Yeoman 使用说明及使用步骤：具体使用查看<a href="https://yeoman.io/learning/index.html" target="_blank" rel="noopener noreferrer">Yeoman 文档</a></p>
<ul>
<li>全局安装 yo：<code>npm install -g yo</code></li>
<li>安装 generator：不同的 generator 可以生成不同的项目。</li>
</ul>
<div><pre><code>npm install -g generator-webapp  ||  npm install -g generator-node 
</code></pre>
<div><span>1</span><br></div></div><ul>
<li>通过 yo 运行 generator：<code>yo webapp || yo node</code></li>
<li>启动应用：<code>npm run start</code></li>
</ul>
</li>
<li>
<p>使用步骤：</p>
<p>1：明确你的需求;</p>
<p>2：找到合适的 Generator;</p>
<p>3：全局范围安装找到的 Generator;</p>
<p>4：通过 Yo 运行对应的 Generator;</p>
<p>5：通过命令行交互填写选项;</p>
<p>6：生成你所需要的项目结构;</p>
</li>
<li>
<p>具体构建流程查看官方文档<a href="https://yeoman.io/learning/index.html" target="_blank" rel="noopener noreferrer">Yeoman 文档</a></p>
</li>
</ul>
<h3 id="构建脚手架"> 构建脚手架</h3>
<ul>
<li>
<p>创建脚手架流程</p>
<p><img src="./../images/脚手架流程.png" alt="" /></p>
</li>
<li>
<p>构建脚手架方式：</p>
<ul>
<li>使用 Yeoman</li>
<li>使用 Node 自定义：<a href="https://zhuanlan.zhihu.com/p/37929333" target="_blank" rel="noopener noreferrer">使用 Node.js 开发简单的脚手架工具</a></li>
</ul>
</li>
</ul>
<h3 id="vue-cli"> Vue-cli</h3>
<h4 id="基础"> 基础</h4>
<ul>
<li>构建的 vue-cli 工程都到了哪些技术，它们的作用分别是什么？
<ul>
<li><code>vue.js</code>：<code>vue-cli</code> 工程的核心。</li>
<li><code>vue-router</code>：vue官方推荐使用的路由框架。</li>
<li><code>vuex</code>：专为 Vue.js 应用项目开发的状态管理器，主要用于维护vue组件间共用的一些 变量 和 方法。</li>
<li><code>axios</code>（ 或者 fetch 、ajax ）：用于发起 GET 、或 POST 等 http请求，基于 Promise 设计。</li>
<li><code>vux</code> 等组件库：一个专为 vue 设计的移动端UI组件库。</li>
<li><code>emit.js</code> 文件，用于 vue 事件机制的管理。</li>
<li><code>webpack</code>：模块加载和 vue-cli 工程打包器。</li>
</ul>
</li>
<li>vue-cli 工程常用的 npm 命令有哪些？
<ul>
<li>下载 node_modules 资源包的命令：<code>npm install</code></li>
<li>启动 vue-cli 开发环境的 npm命令：<code>npm run dev</code></li>
<li>vue-cli 生成 生产环境部署资源 的 npm命令：<code>npm run build</code></li>
<li>用于查看 vue-cli 生产环境部署资源文件大小的 npm命令：<code>npm run build --report</code></li>
</ul>
</li>
<li>自定义脚本命令： 在 script 添加命令，使用 <code>npm run 命令</code> 执行命令。</li>
<li>vue-cli目录解析：
<ul>
<li>build 文件夹：用于存放 webpack 相关配置和脚本。开发中仅 偶尔使用 到此文件夹下 webpack.base.conf.js 用于配置 less、sass等css预编译库，或者配置一下 UI 库。</li>
<li>config 文件夹：主要存放配置文件，用于区分开发环境、线上环境的不同。 常用到此文件夹下 config.js 配置开发环境的 端口号、是否开启热加载 或者 设置生产环境的静态资源相对路径、是否开启gzip压缩、npm run build 命令打包生成静态资源的名称和路径等。</li>
<li>dist 文件夹：默认 npm run build 命令打包生成的静态资源文件，用于生产部署。</li>
<li>node_modules：存放npm命令下载的开发环境和生产环境的依赖包。</li>
<li>src: 存放项目源码及需要引用的资源文件。</li>
<li>src/assets：存放项目中需要用到的资源文件，css、js、images等。</li>
<li>src/componets：存放vue开发中一些公共组件：header.vue、footer.vue等。</li>
<li>src/emit：自己配置的vue集中式事件管理机制。</li>
<li>src/router：vue-router vue路由的配置文件。</li>
<li>src/service：自己配置的vue请求后台接口方法。</li>
<li>src/page：存在vue页面组件的文件夹。</li>
<li>src/util：存放vue开发过程中一些公共的.js方法。</li>
<li>src/vuex：存放 vuex 为vue专门开发的状态管理器。</li>
<li>src/app.vue：使用标签<code>&lt;route-view&gt;&lt;/router-view&gt;</code>渲染整个工程的.vue组件。</li>
<li>src/main.js：vue-cli工程的入口文件。</li>
<li>index.html：设置项目的一些meta头信息和提供<code>&lt;div id=&quot;app&quot;&gt;&lt;/div&gt;</code>用于挂载 vue 节点。</li>
<li>package.json：用于 node_modules资源部 和 启动、打包项目的 npm 命令管理。</li>
</ul>
</li>
</ul>
<h4 id="原理"> 原理</h4>
<ul>
<li>执行过程：
<ul>
<li>终端输入<code>vue create vue-test-app</code></li>
<li>终端解析出<code>vue</code>主命令</li>
<li>终端在环境变量中找到<code>vue</code>命令</li>
<li>终端根据<code>vue</code>命令链接到实际可执行文件vue.js</li>
<li>终端利用node执行vue.js</li>
<li>vue.js解析command和option</li>
<li>vue.js执行command</li>
<li>执行完毕，退出执行</li>
</ul>
</li>
</ul>
<p><strong>参考资料</strong></p>
<p><a href="https://cli.vuejs.org/zh/guide/" target="_blank" rel="noopener noreferrer">vue-cli</a></p>
<p><a href="https://juejin.cn/post/6844903833160646663#heading-22" target="_blank" rel="noopener noreferrer">自己搭建vue开发环境</a></p>
<p><a href="https://cloud.tencent.com/developer/article/1781202#:~:text=Vue%20CLI%20%E6%98%AF%E4%B8%80%E4%B8%AA%E5%9F%BA%E4%BA%8E%20Vue.js%20%E8%BF%9B%E8%A1%8C%E5%BF%AB%E9%80%9F%E5%BC%80%E5%8F%91%E7%9A%84%E5%AE%8C%E6%95%B4%E7%B3%BB%E7%BB%9F%EF%BC%8C%E6%8F%90%E4%BE%9B%E4%BA%86%E7%BB%88%E7%AB%AF%E5%91%BD%E4%BB%A4%E8%A1%8C%E5%B7%A5%E5%85%B7%E3%80%81%E9%9B%B6%E9%85%8D%E7%BD%AE%E8%84%9A%E6%89%8B%E6%9E%B6%E3%80%81%E6%8F%92%E4%BB%B6%E4%BD%93%E7%B3%BB%E3%80%81%E5%9B%BE%E5%BD%A2%E5%8C%96%E7%AE%A1%E7%90%86%E7%95%8C%E9%9D%A2%E7%AD%89%E3%80%82,%E6%9C%AC%E6%96%87%E6%9A%82%E4%B8%94%E5%8F%AA%E5%88%86%E6%9E%90%20%E9%A1%B9%E7%9B%AE%E5%88%9D%E5%A7%8B%E5%8C%96%20%E9%83%A8%E5%88%86%EF%BC%8C%E4%B9%9F%E5%B0%B1%E6%98%AF%E7%BB%88%E7%AB%AF%E5%91%BD%E4%BB%A4%E8%A1%8C%E5%B7%A5%E5%85%B7%E7%9A%84%E5%AE%9E%E7%8E%B0%E3%80%82%200.%20%E7%94%A8%E6%B3%95" target="_blank" rel="noopener noreferrer">剖析 Vue CLI 实现原理</a></p>
<h3 id="参考资料"> 参考资料</h3>
<p><a href="https://yeoman.io/learning/index.html" target="_blank" rel="noopener noreferrer">Yeoman 文档</a></p>
<p><a href="https://mp.weixin.qq.com/s/kAHipuplQpe0XbnI7e8rnA" target="_blank" rel="noopener noreferrer">如何快速开发一个自己的项目脚手架？</a></p>
<p><a href="https://www.bilibili.com/video/BV1gV41177Bh?p=7" target="_blank" rel="noopener noreferrer">前端工程化体系建设从理念到实战</a></p>
<h2 id="构建工具"> 构建工具</h2>
<h3 id="npm-script"> npm script</h3>
<ul>
<li>
<p>npm 允许在<code>package.json</code>文件里面，使用<code>scripts</code>字段定义脚本命令。</p>
<blockquote>
<div><pre><code><span>{</span>
  <span>// ...</span>
  <span>"scripts"</span><span>:</span> <span>{</span>
   	<span>"build"</span><span>:</span> <span>"node build.js"</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div></blockquote>
</li>
<li>
<p>npm script 任务：</p>
<p><img src="./../images/npmscirpt.png" alt="" /></p>
</li>
<li>
<p>原理：</p>
<ul>
<li>
<p>每当执行<code>npm run</code>，就会自动新建一个 Shell，在这个 Shell 里面执行指定的脚本命令。因此，只要是 Shell（一般是 Bash）可以运行的命令，就可以写在 npm 脚本里面。</p>
<p>比较特别的是，<code>npm run</code>新建的这个 Shell，会将当前目录的<code>node_modules/.bin</code>子目录加入<code>PATH</code>变量，执行结束后，再将<code>PATH</code>变量恢复原样。</p>
<p>这意味着，当前目录的<code>node_modules/.bin</code>子目录里面的所有脚本，都可以直接用脚本名调用，而不必加上路径。比如，当前项目的依赖里面有 Mocha，只要直接写<code>mocha test</code>就可以了。</p>
<blockquote>
<div><pre><code><span>"test"</span><span>:</span> <span>"mocha test"</span>
</code></pre>
<div><span>1</span><br></div></div></blockquote>
<p>而不用写成下面这样。</p>
<blockquote>
<div><pre><code><span>"test"</span><span>:</span> <span>"./node_modules/.bin/mocha test"</span>
</code></pre>
<div><span>1</span><br></div></div></blockquote>
<p>由于 npm 脚本的唯一要求就是可以在 Shell 执行，因此它不一定是 Node 脚本，任何可执行文件都可以写在里面。</p>
<p>npm 脚本的退出码，也遵守 Shell 脚本规则。如果退出码不是<code>0</code>，npm 就认为这个脚本执行失败</p>
</li>
</ul>
</li>
<li>
<p>应用：</p>
<ul>
<li>
<p>在项目，可以添加多个命令脚本，执行 <code>npm run 命令</code> 的方式执行不同的脚本</p>
<p><img src="./../images/npmscript2.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
<h3 id="参考资料-2"> 参考资料</h3>
<p><a href="http://www.ruanyifeng.com/blog/2016/10/npm_scripts.html" target="_blank" rel="noopener noreferrer">npm scripts 使用指南</a></p>
<h2 id="模块化打包工具"> 模块化打包工具</h2>
<h3 id="webpack"> WebPack</h3>
<h2 id="标准化规范和工具"> 标准化规范和工具</h2>
<h3 id="代码规范"> 代码规范</h3>
<h4 id="javascript-代码规范"> JavaScript 代码规范</h4>
<p>在平常项目开发中，我们遵守一些基本规范，比如说：</p>
<ul>
<li>一个函数作用域中所有的变量声明应该尽量提到函数首部。</li>
<li>用一个var声明，不允许出现两个连续的var声明，声明时如果变量没有值，应该给该变量赋值对应类型的初始值，便于他人阅读代码时，能够一目了然的知道变量对应的类型值。</li>
<li>代码中出现地址、时间等字符串时需要使用常量代替。</li>
<li>在进行比较的时候吧，尽量使用 '===', '!==' 代替 '==', '!='。</li>
<li>不要在内置对象的原型上添加方法，如 Array,Date。</li>
<li>switch 语句必须带有 default 分支。</li>
<li>for 循环必须使用大括号。</li>
<li>if 语句必须使用大括号。</li>
</ul>
<h2 id="自动部署和工具"> 自动部署和工具</h2>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-26T11:10:03.000Z</published>
  </entry>
  <entry>
    <title type="html">Home</title>
    <id>https://sewar-x.github.io/</id>
    <link href="https://sewar-x.github.io/"/>
    <updated>2021-07-09T13:58:11.000Z</updated>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-25T19:47:29.000Z</published>
  </entry>
  <entry>
    <title type="html">WEB 安全</title>
    <id>https://sewar-x.github.io/Web-Security/</id>
    <link href="https://sewar-x.github.io/Web-Security/"/>
    <updated>2021-06-29T22:43:23.000Z</updated>
    <content type="html"><![CDATA[<h1 id="web-安全"> WEB 安全</h1>
<h2 id="总览"> 总览</h2>
<p><img src="./../images/web-anqun.png" alt="" /></p>
<p><img src="./../images/web-anquan2.png" alt="" /></p>
<h2 id="同源策略"> 同源策略</h2>
<p><img src="./../images/tongyuncelve.png" alt="" /></p>
<h2 id="跨域"> 跨域</h2>
<p><img src="./../images/kuayu.png" alt="" /></p>
<h3 id="跨域解决方案"> 跨域解决方案</h3>
<p><img src="./../images/kuayu-fangan.png" alt="" /></p>
<p><img src="./../images/kuayu-fangan1.png" alt="" /></p>
<p><img src="./../images/kuayu-fangan2.png" alt="" /></p>
<p><img src="./../images/kuayu-fangan3.png" alt="" /></p>
<p><img src="./../images/kuayu-fangan4.png" alt="" /></p>
<p><img src="./../images/kuayu-fangan5.png" alt="" /></p>
<p><img src="./../images/kuayu-fangan6.png" alt="" /></p>
<ul>
<li>参考资料：<a href="https://www.ruanyifeng.com/blog/2016/04/same-origin-policy.html" target="_blank" rel="noopener noreferrer">浏览器同源政策及其规避方法</a></li>
</ul>
<h2 id="xss-跨站脚本攻击"> XSS(跨站脚本攻击)</h2>
<p><img src="./../images/xss1.png" alt="" /></p>
<p><img src="./../images/xss2.png" alt="" /></p>
<p><img src="./../images/xss3.png" alt="" /></p>
<h2 id="csp-内容安全策略"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/CSP" target="_blank" rel="noopener noreferrer">CSP</a>(内容安全策略)</h2>
<ul>
<li>CSP指的是内容安全策略，它的本质是建立一个白名单，告诉浏览器哪些外部资源可以加载和执行。我们只需要配置规则，如何拦截由浏览器自己来实现。</li>
<li>通常有两种方式来开启CSP:
<ul>
<li>一种是设置HTTP首部中的 <code>Content-Security-Policy</code></li>
<li>一种是设置meta标签的方式 <code>&lt;metahttp-equiv=&quot;Content-Security-Policy&quot;&gt;</code></li>
</ul>
</li>
</ul>
<h2 id="csrf-跨站请求伪造"> CSRF(跨站请求伪造)</h2>
<p><img src="./../images/scrf.png" alt="" /></p>
<h2 id="点击劫持"> 点击劫持</h2>
<p><strong>概念：</strong></p>
<ul>
<li>点击劫持是一种视觉欺骗的攻击手段，攻击者将需要攻击的网站通过  <code>iframe</code> 嵌套的方式嵌入自己的网页中，并将 <code>iframe</code> 设置为透明，在页面中透出一个按钮诱导用户点击。</li>
</ul>
<p><strong>如何预防？</strong></p>
<ul>
<li>在http相应头中设置<code>X-FRAME-OPTIONS</code>来防御用<code>iframe</code>嵌套的点击劫持攻击。通过不同的值，可以规定页面在特定的一些情况才能作为iframe来使用。</li>
</ul>
<p><strong>参考资料：</strong></p>
<p><a href="https://www.jianshu.com/p/251704d8ff18" target="_blank" rel="noopener noreferrer">《web安全之--点击劫持攻击与防御技术简介》</a></p>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-29T22:43:23.000Z</published>
  </entry>
  <entry>
    <title type="html">数据结构/算法</title>
    <id>https://sewar-x.github.io/algorithms/</id>
    <link href="https://sewar-x.github.io/algorithms/"/>
    <updated>2021-07-15T12:37:23.000Z</updated>
    <content type="html"><![CDATA[<h1 id="数据结构-算法"> 数据结构/算法</h1>
<h2 id="基础概念"> 基础概念</h2>
<h3 id="数据结构"> 数据结构</h3>
<p><img src="./../images/数据结构概念.png" alt="image-20210707154220796" /></p>
<ul>
<li>根据数据结构，可以分为线性表、字符串、树、图。</li>
</ul>
<h3 id="线性表"> 线性表</h3>
<p><img src="./../images/线性表.png" alt="image-20210707155311589" /></p>
<h3 id="字符串"> 字符串</h3>
<p><img src="./../images/字符串.png" alt="image-20210707155417730" /></p>
<h3 id="树结构"> 树结构</h3>
<p><img src="./../images/树.png" alt="image-20210707155457783" /></p>
<h3 id="图结构"> 图结构</h3>
<p><img src="./../images/图.png" alt="image-20210707155545815" /></p>
<h3 id="时间复杂度"> 时间复杂度</h3>
<ul>
<li>
<p>一个算法的时间复杂度反映了程序运行从开始到结束所需要的时间。</p>
</li>
<li>
<p>把算法中基本操作重复执行的次数（频度）作为算法的时间复杂度。</p>
<ul>
<li>没有循环语句，记作<code>O(1)</code>，也称为常数阶。</li>
<li>只有一重循环，则算法的基本操作的执行频度与问题规模n呈线性增大关系，记作<code>O（n）</code>，也叫线性阶。</li>
</ul>
</li>
<li>
<p>常见的时间复杂度有：</p>
<ul>
<li><code>O(1)</code>: Constant Complexity: Constant 常数复杂度</li>
<li><code>O(log n)</code>: Logarithmic Complexity: 对数复杂度</li>
<li><code>O(n)</code>: Linear Complexity: 线性时间复杂度</li>
<li><code>O(n^2)</code>: N square Complexity 平⽅方</li>
<li><code>O(n^3)</code>: N square Complexity ⽴立⽅方</li>
<li><code>O(2^n)</code>: Exponential Growth 指数</li>
<li><code>O(n!)</code>: Factorial 阶乘</li>
</ul>
</li>
</ul>
<p><img src="./../images/时间复杂度.png" alt="img" /></p>
<h3 id="空间复杂度"> 空间复杂度</h3>
<ul>
<li>
<p>一个程序的空间复杂度是指运行完一个程序所需内存的大小。</p>
</li>
<li>
<p>利用程序的空间复杂度，可以对程序的运行所需要的内存多少有个预先估计。</p>
</li>
<li>
<p>一个程序执行时除了需要存储空间和存储本身所使用的指令、常数、变量和输入数据外，还需要一些对数据进行操作的工作单元和存储一些为现实计算所需信息的辅助空间。</p>
</li>
</ul>
<h2 id="数据结构-2"> 数据结构</h2>
<h3 id="数组"> 数组</h3>
<h4 id="数组操作"> 数组操作</h4>
<h5 id="数组插入数据"> 数组插入数据</h5>
<ul>
<li>
<p>题目：在线性表的第i个元素中插入元素e</p>
</li>
<li>
<p>思路：</p>
<ul>
<li>方法一：将所有第i个元素数据后面元素后移一位，将元素e插入第i个位置</li>
<li>方法二：使用数组的splice(index,howmany,item1,.....,itemX)方法实现</li>
</ul>
</li>
<li>
<p>分析：时间复杂度O(n)</p>
</li>
<li>
<p>实现：</p>
<div><pre><code><span>/**
 * 
 * @param { number } i 插入位置
 * @param { ElementType } e 插入元素
 * @param { ElementList } list 元素列表
 */</span>
<span>function</span> <span>ListInsert</span><span>(</span><span>i<span>,</span> e<span>,</span> list</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span>i <span>></span> list<span>.</span>length <span>||</span> i <span>&lt;=</span> <span>0</span><span>)</span> <span>{</span>
    <span>return</span> list
  <span>}</span>
  list<span>.</span>length <span>=</span> list<span>.</span>length <span>+</span> <span>1</span> <span>//数组增加一个位子</span>
  <span>for</span> <span>(</span><span>let</span> j <span>=</span> list<span>.</span>length <span>-</span> <span>2</span><span>;</span> j <span>>=</span> list<span>.</span>length <span>-</span> i <span>-</span> <span>1</span><span>;</span> j<span>--</span><span>)</span> <span>{</span>
    list<span>[</span>j <span>+</span> <span>1</span><span>]</span> <span>=</span> list<span>[</span>j<span>]</span>
  <span>}</span>
  list<span>[</span>i <span>-</span> <span>1</span><span>]</span> <span>=</span> e
  <span>return</span> list
<span>}</span>
<span>//----------------测试---------------------</span>
<span>let</span> a <span>=</span> <span>[</span><span>9</span><span>,</span> <span>8</span><span>,</span> <span>7</span><span>,</span> <span>6</span><span>,</span> <span>5</span><span>,</span> <span>4</span><span>,</span> <span>3</span><span>,</span> <span>2</span><span>,</span> <span>1</span><span>]</span>
<span>let</span> b <span>=</span> <span>ListInsert</span><span>(</span><span>3</span><span>,</span> <span>10</span><span>,</span> a<span>)</span>
console<span>.</span><span>log</span><span>(</span>b<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br></div></div></li>
</ul>
<h5 id="数组删除数据"> 数组删除数据</h5>
<ul>
<li>
<p>题目：在线性表的删除第i个元素</p>
</li>
<li>
<p>思路：</p>
<ul>
<li>方法一：将所有第i个元素数据后面元素前移一位</li>
<li>方法二：使用数组的splice(index,howmany,item1,.....,itemX)方法实现</li>
</ul>
</li>
<li>
<p>分析：时间复杂度O(n)</p>
</li>
<li>
<p>实现：</p>
<div><pre><code><span>function</span> <span>ListDelete</span><span>(</span><span>i<span>,</span> list</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span>i <span>&lt;=</span> <span>0</span> <span>||</span> i <span>></span> list<span>.</span>length<span>)</span> <span>{</span>
    <span>return</span> list
  <span>}</span>
  <span>for</span> <span>(</span><span>let</span> j <span>=</span> i <span>-</span> <span>1</span><span>;</span> j <span>&lt;</span> list<span>.</span>length<span>;</span> j<span>++</span><span>)</span> <span>{</span>
    list<span>[</span>j<span>]</span> <span>=</span> list<span>[</span>j <span>+</span> <span>1</span><span>]</span>
  <span>}</span>
  list<span>.</span>length <span>=</span> list<span>.</span>length <span>-</span> <span>1</span>
  <span>return</span> list
<span>}</span>
<span>//------------------测试--------------------</span>
<span>let</span> a <span>=</span> <span>[</span><span>1</span><span>,</span> <span>2</span><span>,</span> <span>3</span><span>,</span> <span>4</span><span>,</span> <span>5</span><span>,</span> <span>6</span><span>,</span> <span>6</span><span>,</span> <span>7</span><span>,</span> <span>8</span><span>,</span> <span>9</span><span>]</span>
<span>let</span> b <span>=</span> <span>ListDelete</span><span>(</span><span>5</span><span>,</span> a<span>)</span>
console<span>.</span><span>log</span><span>(</span>b<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div></li>
</ul>
<h4 id="数组去重"> 数组去重</h4>
<h5 id="使用双层循环"> 使用双层循环</h5>
<div><pre><code><span>/**
 * 思想：循环遍历数组，循环查找当前数组重复数字
 * 分析：时间复杂度 O(n^2)，空间复杂度 O(1)
 * @param {Array} arr 包含重复元素数组
 * @return array
 */</span>
<span>function</span> <span>unique</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> arr<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
    <span>for</span> <span>(</span><span>let</span> j <span>=</span> i <span>+</span> <span>1</span><span>;</span> j <span>&lt;</span> arr<span>.</span>length<span>;</span> j<span>++</span><span>)</span> <span>{</span>
      <span>if</span> <span>(</span>arr<span>[</span>i<span>]</span> <span>===</span> arr<span>[</span>j<span>]</span><span>)</span> <span>{</span>
        arr<span>.</span><span>splice</span><span>(</span>j<span>,</span> <span>1</span><span>)</span>
        i<span>--</span> <span>//删除后后移下标</span>
      <span>}</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div><h5 id="使用array-indexof"> 使用Array.indexOf()</h5>
<div><pre><code><span>//indexOf()方法：返回数组中某个元素首次出现的位置</span>
<span>/**
 * 思想：使用indeof()方法判断，遍历数组，当当前数据下标不等于它首次出现位置的下标时，认为重复数据，去除
 * 分析：时间复杂度O(n)，空间复杂度O(1)
 * @param {Array} arr 包含重复元素数组
 * @return array
 */</span>
<span>function</span> <span>unique1</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> arr<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>arr<span>.</span><span>indexOf</span><span>(</span>arr<span>[</span>i<span>]</span><span>)</span> <span>!=</span> i<span>)</span> <span>{</span>
      arr<span>.</span><span>splice</span><span>(</span>i<span>,</span> <span>1</span><span>)</span>
      i<span>--</span>
    <span>}</span>
  <span>}</span>
  <span>return</span> arr
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div><div><pre><code><span>/**
 * 思想: 使用indexOf方法：遍历数组，判断当前数据下标是否为该数据首次出现下标，如果是，则加入另一个数组；该数组为去重后的数组
 * 分析：时间复杂度O(n)，空间复杂度O(n)
 * @param {Array} arr 包含重复元素数组
 * @return uArray
 */</span>
<span>function</span> <span>unique2</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>let</span> uArr <span>=</span> <span>[</span><span>]</span>
  <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> arr<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>arr<span>.</span><span>indexOf</span><span>(</span>arr<span>[</span>i<span>]</span><span>)</span> <span>===</span> i<span>)</span> <span>{</span> <span>//为数据首次出现下标</span>
      uArr<span>.</span><span>push</span><span>(</span>arr<span>[</span>i<span>]</span><span>)</span>
    <span>}</span>
  <span>}</span>
  <span>return</span> uArr
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div><div><pre><code><span>/**
 * 思想：使用indexOf方法：创建一个数组，该数组为去重数据后的数组；遍历数组，判断去重数据后的数组中是否存在该数据，不存在则添加，否则跳过
 * 分析：时间复杂度O(n)，空间复杂度O(n)
 * @param {Array} arr 包含重复元素数组
 * @return uArray
 */</span>
<span>function</span> <span>unique3</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>let</span> uArr <span>=</span> <span>[</span><span>]</span>
  <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> arr<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>uArr<span>.</span><span>indexOf</span><span>(</span>arr<span>[</span>i<span>]</span> <span>==</span> <span>-</span><span>1</span><span>)</span><span>)</span> <span>{</span> <span>//判断数据是否存在</span>
      uArr<span>.</span><span>push</span><span>(</span>arr<span>[</span>i<span>]</span><span>)</span>
    <span>}</span>
  <span>}</span>
  <span>return</span> uArr
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div><h5 id="使用array-filter"> 使用Array.filter()</h5>
<div><pre><code><span>//filter():遍历并检测数组，返回一个新的数组，新数组中的元素是通过检查指定数组中符合条件的所有元素</span>
<span>/**
 * 思想：使用filter过滤出数组中元素首次出现的下标与当前遍历的下标值相同的元素
 * @param {Array} arr 包含重复元素数组
 * @return array
 */</span>
<span>function</span> <span>unique</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>return</span> arr<span>.</span><span>filter</span><span>(</span><span>(</span><span>current<span>,</span> index</span><span>)</span> <span>=></span> <span>{</span>
    arr<span>.</span><span>indexOf</span><span>(</span>current<span>)</span> <span>===</span> index <span>//返回下标与数组首次出现下标位置相同数据</span>
  <span>}</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><div><pre><code><span>function</span> <span>unique</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>return</span> arr<span>.</span><span>filter</span><span>(</span><span>(</span><span>current<span>,</span> index</span><span>)</span> <span>=></span> <span>{</span>
    arr<span>.</span><span>indexOf</span><span>(</span>current<span>)</span> <span>===</span> arr<span>.</span><span>lastIndexOf</span><span>(</span>current<span>)</span> <span>//返回首次出现下标和最后一次出现下标相同数据</span>
  <span>}</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><div><pre><code><span>//hasOwnProperty()方法返回一个布尔值，该布尔值指示对象是否具有指定的属性作为其自身的属性（而不是继承它）</span>
<span>/**
 * 思想：开辟一个外部存储空间用于标示元素是否出现过，过滤出外部存储空间中没有出现过的元素
 * @param {Array} arr 包含重复元素数组
 */</span>
<span>function</span> <span>unique</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>let</span> container <span>=</span> <span>{</span><span>}</span>
  <span>return</span> arr<span>.</span><span>filter</span><span>(</span><span>(</span><span>current<span>,</span> index</span><span>)</span> <span>=></span> <span>{</span>
    container<span>.</span><span>hasOwnProperty</span><span>(</span>current<span>)</span> <span>?</span> <span>false</span> <span>:</span> <span>(</span>container<span>[</span>currnet<span>]</span> <span>=</span> <span>true</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><h5 id="使用array-includes"> 使用Array.includes()</h5>
<div><pre><code><span>/**
 * includes方法判断一个数组是否包含一个指定元素，是返回true，否则返回false
 * 思想：新建去重数组，遍历原数组判断去重数组是否存在数据，不存在则添加进去重数组
 * @param {Array} arr 包含重复元素数组
 * @return array
 */</span>
<span>function</span> <span>unique</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>let</span> uArr <span>=</span> <span>[</span><span>]</span>
  <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> arr<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span><span>!</span>uArr<span>.</span><span>includes</span><span>(</span>arr<span>[</span>i<span>]</span><span>)</span><span>)</span> <span>{</span> <span>//去重数据总不包含</span>
      uArr<span>.</span><span>push</span><span>(</span>arr<span>[</span>i<span>]</span><span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div><h5 id="使用set"> 使用Set()</h5>
<div><pre><code><span>/**
 * 思想：创建数组的Set集合，将Set结合转化为数组
 * @param {Array} arr 包含重复元素数组
 * @return array
 */</span>
<span>function</span> <span>unique</span><span>(</span><span>params</span><span>)</span> <span>{</span>
  <span>return</span> Array<span>.</span><span>from</span><span>(</span><span>new</span> <span>Set</span><span>(</span>arr<span>)</span><span>)</span>
<span>}</span>

</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div><h5 id="排序后去重"> 排序后去重</h5>
<div><pre><code><span>/**
 * 思想：数组排序后比较相邻数组是否相同，不相同时添加进入去重后的数组
 * 分析：时间复杂度O（n），空间复杂度O（n）
 * @param {Array} arr 包含重复元素数组
 * @return array
 */</span>
<span>function</span> <span>unique</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>let</span> uArr <span>=</span> <span>[</span><span>]</span>
  arr<span>.</span><span>sort</span><span>(</span><span>)</span><span>//将数组排序，使相同值的数据相邻</span>
  uArr<span>.</span><span>push</span><span>(</span>arr<span>[</span><span>0</span><span>]</span><span>)</span><span>//保存排序后第一个数据</span>
  <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>1</span><span>;</span> i <span>&lt;</span> arr<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span><span>//从数组第二个数据开始遍历，判断是否与前一个数据相同，不相同则保存</span>
    <span>if</span> <span>(</span>arr<span>[</span>i<span>]</span> <span>!=</span> arr<span>[</span>i <span>-</span> <span>1</span><span>]</span><span>)</span> <span>{</span>
      uArr<span>.</span><span>push</span><span>(</span>arr<span>[</span>i<span>]</span><span>)</span>
    <span>}</span>
  <span>}</span>
  <span>return</span> uArr
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br></div></div><h4 id="类数组转化为数组"> 类数组转化为数组</h4>
<ul>
<li>概念：
<ul>
<li>类数组是具有<strong>length</strong>属性，但不具有数组原型上的方法</li>
<li>常见的类数组有<strong>arguments</strong>、DOM操作方法返回的结果，比如 <code>document.getElementsByTagName()</code></li>
</ul>
</li>
</ul>
<h5 id="使用array-from"> 使用<a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/from" target="_blank" rel="noopener noreferrer">Array.from</a></h5>
<div><pre><code>Array<span>.</span><span>from</span><span>(</span>document<span>.</span><span>querySelectorAll</span><span>(</span><span>'div'</span><span>)</span><span>)</span>
</code></pre>
<div><span>1</span><br></div></div><h5 id="array-prototype-slice-call"> Array.prototype.slice.call()</h5>
<ul>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/slice" target="_blank" rel="noopener noreferrer">slice()</a>方法可以用来将一个类数组（Array-like）对象/集合转换成一个新数组。你只需将该方法绑定到这个对象上</li>
</ul>
<div><pre><code><span>Array</span><span>.</span>prototype<span>.</span><span>slice</span><span>.</span><span>call</span><span>(</span>arguments<span>)</span>
</code></pre>
<div><span>1</span><br></div></div><h5 id="扩展运算符"> 扩展运算符</h5>
<div><pre><code><span>[</span><span>...</span>arguments<span>]</span>
</code></pre>
<div><span>1</span><br></div></div><h5 id="利用concat"> 利用<a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/concat" target="_blank" rel="noopener noreferrer">concat()</a></h5>
<div><pre><code><span>Array</span><span>.</span>prototype<span>.</span><span>concat</span><span>.</span><span>apply</span><span>(</span><span>[</span><span>]</span><span>,</span> arguments<span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br></div></div><h4 id="js-数组-api-实现"> JS 数组 API 实现</h4>
<h5 id="map"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/map" target="_blank" rel="noopener noreferrer">map()</a></h5>
<p>语法</p>
<ul>
<li>
<div><pre><code><span>var</span> new_array <span>=</span> arr<span>.</span><span>map</span><span>(</span><span>function</span> <span>callback</span><span>(</span><span>currentValue<span>[</span><span>,</span> index<span>[</span><span>,</span> array<span>]</span><span>]</span></span><span>)</span> <span>{</span>
 <span>// Return element for new_array </span>
<span>}</span><span>[</span><span>,</span> thisArg<span>]</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
</ul>
<p>注意</p>
<ul>
<li><code>map</code>生成一个新数组</li>
<li>如果 <code>thisArg</code> 参数提供给<code>map</code>，则会被用作回调函数的<code>this</code>值。否则<a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/undefined" target="_blank" rel="noopener noreferrer"><code>undefined</code></a>会被用作回调函数的<code>this</code>值</li>
<li><code>map</code>不修改调用它的原数组本身</li>
</ul>
<p>实现</p>
<ul>
<li>遍历数组实现</li>
</ul>
<div><pre><code><span>/**
 * array.map(function(currentValue,index,arr), thisValue)方法介绍：
 * 作用：按照原始数组元素顺序依次调用处理函数处理元素
 * @param {Function} fn 元素处理函数
 * @param {Object}  context 可选。对象作为该执行回调时使用，传递给函数，用作 "this" 的值。如果省略了 thisValue，或者传入 null、undefined，那么回调函数的 this 为全局对象
 * @return array 返回一个新数组，数组中的元素为原始数组元素调用函数处理后的值
 */</span>
<span>Array</span><span>.</span>prototype<span>.</span><span>myMap</span> <span>=</span> <span>function</span> <span>(</span><span>fn<span>,</span> context <span>=</span> window</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>typeof</span> fn <span>!==</span> <span>"function"</span><span>)</span> <span>{</span>
    <span>throw</span> <span>new</span> <span>TypeError</span><span>(</span><span><span>`</span><span><span>${</span>fn<span>}</span></span><span> is not a function</span><span>`</span></span><span>)</span>
  <span>}</span>
  <span>let</span> result <span>=</span> <span>[</span><span>]</span> 						<span>//返回的新数组</span>
  <span>let</span> arr <span>=</span> <span>this</span> 						<span>//this指向方法调用者，一般方法调用者为数组，所以这里this就是调用该遍历方法的数组</span>
  context <span>=</span> context <span>||</span> window 			<span>//省略context或则context为null、undefined的时候默认使用全局对象</span>
  <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> arr<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
    <span>//往结果数组中添加在context作用域上调用的fn函数的结果,参数为map处理函数的参数：currentValue,index,arr</span>
    result<span>.</span><span>push</span><span>(</span><span>fn</span><span>.</span><span>call</span><span>(</span>context<span>,</span> arr<span>[</span>i<span>]</span><span>,</span> i<span>,</span> arr<span>)</span><span>)</span>
  <span>}</span>
  <span>return</span> result
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br></div></div><ul>
<li>reduce模拟实现</li>
</ul>
<div><pre><code><span>Array</span><span>.</span>prototype<span>.</span><span>myMap</span> <span>=</span> <span>function</span> <span>(</span><span>fn<span>,</span> context <span>=</span> window</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>typeof</span> fn <span>!==</span> <span>"function"</span><span>)</span> <span>{</span>
    <span>throw</span> <span>new</span> <span>TypeError</span><span>(</span><span><span>`</span><span><span>${</span>fn<span>}</span></span><span> is not a function</span><span>`</span></span><span>)</span>
  <span>}</span>
  context <span>=</span> context <span>||</span> window
  <span>//this指向方法调用者,一般该方法调用者为被遍历是数组，所以this指向被遍历的数组</span>
  <span>this</span><span>.</span><span>reduce</span><span>(</span><span>(</span><span>result<span>,</span> current<span>,</span> index<span>,</span> arr</span><span>)</span> <span>=></span> <span>{</span>
    result<span>.</span><span>push</span><span>(</span><span>fn</span><span>.</span><span>call</span><span>(</span>context<span>,</span> current<span>,</span> index<span>,</span> arr<span>)</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div><h5 id="foreach"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/forEach" target="_blank" rel="noopener noreferrer">forEach()</a></h5>
<p>语法</p>
<div><pre><code>arr<span>.</span><span>forEach</span><span>(</span><span>callback</span><span>(</span>currentValue <span>[</span><span>,</span> index <span>[</span><span>,</span> array<span>]</span><span>]</span><span>)</span><span>[</span><span>,</span> thisArg<span>]</span><span>)</span>
</code></pre>
<div><span>1</span><br></div></div><p>注意</p>
<ul>
<li>如果省略了 <code>thisArg</code> 参数，或者其值为 <code>null</code> 或 <code>undefined</code>，<code>this</code> 则指向全局对象</li>
<li><code>forEach()</code> 遍历的范围在第一次调用 <code>callback</code> 前就会确定
<ul>
<li>调用 <code>forEach</code> 后添加到数组中的项不会被 <code>callback</code> 访问到</li>
<li>如果已经存在的值被改变，则传递给 <code>callback</code> 的值是 <code>forEach()</code> 遍历到他们那一刻的值</li>
<li>已删除的项不会被遍历到。如果已访问的元素在迭代时被删除了（例如使用 <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/shift" target="_blank" rel="noopener noreferrer"><code>shift()</code></a>），之后的一个元素将被跳过</li>
</ul>
</li>
<li>除了抛出异常以外，没有办法中止或跳出 <code>forEach()</code> 循环</li>
</ul>
<p>实现</p>
<div><pre><code><span>Array</span><span>.</span>prototype<span>.</span><span>forEach</span> <span>=</span> <span>function</span><span>(</span><span>callback<span>,</span> thisArg</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>this</span> <span>==</span> <span>null</span><span>)</span> <span>{</span>
    <span>throw</span> <span>new</span> <span>TypeError</span><span>(</span><span>'this is null or not defined'</span><span>)</span><span>;</span>
  <span>}</span>
  <span>if</span> <span>(</span><span>typeof</span> callback <span>!==</span> <span>"function"</span><span>)</span> <span>{</span>
    <span>throw</span> <span>new</span> <span>TypeError</span><span>(</span>callback <span>+</span> <span>' is not a function'</span><span>)</span><span>;</span>
  <span>}</span>
  <span>const</span> <span>O</span> <span>=</span> <span>Object</span><span>(</span><span>this</span><span>)</span><span>;</span>
  <span>const</span> len <span>=</span> <span>O</span><span>.</span>length <span>>>></span> <span>0</span><span>;</span>
  <span>let</span> k <span>=</span> <span>0</span><span>;</span>
  <span>while</span> <span>(</span>k <span>&lt;</span> len<span>)</span> <span>{</span>
    <span>if</span> <span>(</span>k <span>in</span> <span>O</span><span>)</span> <span>{</span>
      <span>callback</span><span>.</span><span>call</span><span>(</span>thisArg<span>,</span> <span>O</span><span>[</span>k<span>]</span><span>,</span> k<span>,</span> <span>O</span><span>)</span><span>;</span>
    <span>}</span>
    k<span>++</span><span>;</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br></div></div><h5 id="reduce"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/Reduce" target="_blank" rel="noopener noreferrer">reduce()</a></h5>
<p>语法</p>
<ul>
<li>
<div><pre><code>arr<span>.</span><span>reduce</span><span>(</span><span>callback</span><span>(</span>accumulator<span>,</span> currentValue<span>[</span><span>,</span> index<span>[</span><span>,</span> array<span>]</span><span>]</span><span>)</span><span>[</span><span>,</span> initialValue<span>]</span><span>)</span>
</code></pre>
<div><span>1</span><br></div></div></li>
</ul>
<p>注意</p>
<ul>
<li>回调函数第一次执行时，<code>accumulator</code> 和<code>currentValue</code>的取值有两种情况：
<ul>
<li>如果调用<code>reduce()</code>时提供了<code>initialValue</code>，<code>accumulator</code>取值为<code>initialValue</code>，<code>currentValue</code>取数组中的第一个值；</li>
<li>如果没有提供 <code>initialValue</code>，那么<code>accumulator</code>取数组中的第一个值，<code>currentValue</code>取数组中的第二个值。</li>
</ul>
</li>
</ul>
<p>实现</p>
<div><pre><code><span>Array</span><span>.</span>prototype<span>.</span><span>reduce</span> <span>=</span> <span>function</span> <span>(</span><span>reducer<span>,</span>initVal</span><span>)</span> <span>{</span>
    <span>let</span> start<span>,</span>accumulator
    <span>if</span><span>(</span>initVal<span>)</span> <span>{</span>
        start <span>=</span> <span>0</span>
        accumulator <span>=</span> initVal
    <span>}</span><span>else</span> <span>{</span>
        start <span>=</span> <span>1</span>
        accumulator <span>=</span> <span>this</span><span>[</span><span>0</span><span>]</span>
    <span>}</span>
    <span>for</span><span>(</span><span>let</span> i<span>=</span>start<span>;</span>i<span>&lt;</span><span>this</span><span>.</span>length<span>;</span>i<span>++</span><span>)</span><span>{</span>
        initVal <span>=</span><span>reducer</span><span>(</span>accumulator<span>,</span><span>this</span><span>[</span>i<span>]</span><span>,</span>i<span>,</span><span>this</span><span>)</span><span>;</span>
    <span>}</span>
    <span>return</span> initVal
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div><h5 id="filter"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/filter" target="_blank" rel="noopener noreferrer">filter()</a></h5>
<p>语法</p>
<ul>
<li>
<div><pre><code><span>var</span> newArray <span>=</span> arr<span>.</span><span>filter</span><span>(</span><span>callback</span><span>(</span>element<span>[</span><span>,</span> index<span>[</span><span>,</span> array<span>]</span><span>]</span><span>)</span><span>[</span><span>,</span> thisArg<span>]</span><span>)</span>
</code></pre>
<div><span>1</span><br></div></div></li>
</ul>
<p>注意：</p>
<ul>
<li><code>filter</code> 不会改变原数组，它返回过滤后的新数组</li>
<li>如果为 <code>filter</code> 提供一个 <code>thisArg</code> 参数，则它会被作为 <code>callback</code> 被调用时的 <code>this</code> 值。否则，<code>callback</code> 的 <code>this</code> 值在非严格模式下将是全局对象，严格模式下为 <code>undefined</code></li>
</ul>
<p>实现</p>
<ul>
<li>遍历数组实现</li>
</ul>
<div><pre><code><span>Array</span><span>.</span>prototype<span>.</span><span>myFilter</span> <span>=</span> <span>function</span> <span>(</span><span>fn<span>,</span> context <span>=</span> window</span><span>)</span> <span>{</span>
      <span>if</span> <span>(</span><span>typeof</span> fn <span>!==</span> <span>"function"</span><span>)</span> <span>{</span>
        <span>throw</span> <span>new</span> <span>TypeError</span><span>(</span><span><span>`</span><span><span>${</span>fn<span>}</span></span><span> is not a function</span><span>`</span></span><span>)</span>
      <span>}</span>
      <span>let</span> result <span>=</span> <span>[</span><span>]</span> <span>//返回的新数组</span>
      <span>let</span> arr <span>=</span> <span>this</span> <span>//this指向方法调用者，一般方法调用者为数组，所以这里this就是调用该遍历方法的数组</span>
      context <span>=</span> context <span>||</span> window <span>//省略context或则context为null、undefined的时候默认使用全局对象</span>
      <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> arr<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
        <span>if</span> <span>(</span><span>fn</span><span>.</span><span>call</span><span>(</span>context<span>,</span> arr<span>[</span>i<span>]</span><span>,</span> i<span>,</span> arr<span>)</span><span>)</span> <span>//调用fn执行，若结果返回为true，将数据放入结果数组</span>
          <span>//往结果数组中添加在context作用域上调用的fn函数的结果,参数为map处理函数的参数：currentValue,index,arr</span>
          result<span>.</span><span>push</span><span>(</span>arr<span>[</span>i<span>]</span><span>)</span>
      <span>}</span>
 	<span>return</span> result
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div><ul>
<li>使用reduce实现</li>
</ul>
<div><pre><code><span>Array</span><span>.</span>prototype<span>.</span><span>myFilter</span> <span>=</span> <span>function</span> <span>(</span><span>fn<span>,</span> context <span>=</span> window</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>typeof</span> fn <span>!==</span> <span>"function"</span><span>)</span> <span>{</span>
    <span>throw</span> <span>new</span> <span>TypeError</span><span>(</span><span><span>`</span><span><span>${</span>fn<span>}</span></span><span> is not a function</span><span>`</span></span><span>)</span>
  <span>}</span>
  context <span>=</span> context <span>||</span> window
  <span>return</span> <span>this</span><span>.</span><span>reduce</span><span>(</span><span>(</span><span>result<span>,</span> current<span>,</span> index<span>,</span> arr</span><span>)</span> <span>=></span> <span>{</span>
    <span>if</span> <span>(</span><span>fn</span><span>.</span><span>call</span><span>(</span>context<span>,</span> current<span>,</span> index<span>,</span> arr<span>)</span><span>)</span> <span>{</span>
      result<span>.</span><span>push</span><span>(</span>current<span>)</span>
    <span>}</span>
  <span>}</span><span>,</span> <span>[</span><span>]</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><h5 id="some"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/some" target="_blank" rel="noopener noreferrer">some()</a></h5>
<p>语法</p>
<ul>
<li>
<div><pre><code>arr<span>.</span><span>some</span><span>(</span><span>callback</span><span>(</span>element<span>[</span><span>,</span> index<span>[</span><span>,</span> array<span>]</span><span>]</span><span>)</span><span>[</span><span>,</span> thisArg<span>]</span><span>)</span>
</code></pre>
<div><span>1</span><br></div></div></li>
</ul>
<p>注意</p>
<ul>
<li><code>some()</code> 被调用时不会改变数组</li>
<li>如果一个<code>thisArg</code>参数提供给some()，它将被用作调用的 <code>callback</code>的 <code>this</code> 值。否则， 它的 <code>this</code> value将是 <code>undefined</code></li>
</ul>
<p>实现</p>
<div><pre><code><span>Array</span><span>.</span>prototype<span>.</span><span>some</span><span>=</span><span>function</span> <span>(</span><span>fn<span>,</span> context <span>=</span> window</span><span>)</span> <span>{</span>
    <span>let</span> arr <span>=</span> <span>this</span>
    context <span>=</span> context <span>||</span> window
    <span>for</span><span>(</span><span>let</span> i<span>=</span><span>0</span><span>;</span>i<span>&lt;</span>arr<span>.</span>length<span>;</span>i<span>++</span><span>)</span><span>{</span>
        <span>if</span><span>(</span><span>fn</span><span>.</span><span>call</span><span>(</span>context<span>,</span><span>this</span><span>[</span>i<span>]</span><span>,</span>i<span>,</span>arr<span>)</span><span>)</span> <span>{</span>
            <span>return</span> <span>true</span>
        <span>}</span>
    <span>}</span>
    <span>return</span> <span>false</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div><h5 id="every"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/every" target="_blank" rel="noopener noreferrer">every()</a></h5>
<p>语法</p>
<ul>
<li>
<div><pre><code>arr<span>.</span><span>every</span><span>(</span><span>callback</span><span>(</span>element<span>[</span><span>,</span> index<span>[</span><span>,</span> array<span>]</span><span>]</span><span>)</span><span>[</span><span>,</span> thisArg<span>]</span><span>)</span>
</code></pre>
<div><span>1</span><br></div></div></li>
</ul>
<p>注意</p>
<ul>
<li><code>every</code> 不会改变原数组</li>
<li>如果省略 thisArg 参数，则 <code>callback</code> 被调用时的 <code>this</code> 值，在非严格模式下为全局对象，在严格模式下传入 <code>undefined</code></li>
</ul>
<p>实现</p>
<div><pre><code><span>Array</span><span>.</span>prototype<span>.</span><span>every</span><span>=</span><span>function</span> <span>(</span><span>fn<span>,</span>context <span>=</span> window</span><span>)</span> <span>{</span>
    <span>let</span> arr <span>=</span> <span>this</span>
    context  <span>=</span> context <span>||</span> window
    <span>for</span><span>(</span><span>let</span> i<span>=</span><span>0</span><span>;</span>i<span>&lt;</span>arr<span>.</span>length<span>;</span>i<span>++</span><span>)</span><span>{</span>
        <span>if</span><span>(</span><span>!</span><span>fn</span><span>.</span><span>call</span><span>(</span>context<span>,</span><span>this</span><span>[</span>i<span>]</span><span>,</span>i<span>,</span>arr<span>)</span><span>)</span> <span>{</span>
            <span>return</span> <span>false</span>
        <span>}</span>
    <span>}</span>
    <span>return</span> <span>true</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div><h5 id="flat"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/flat" target="_blank" rel="noopener noreferrer">flat()</a></h5>
<ul>
<li>数组扁平化：指将多维数组转化成一维数组</li>
</ul>
<p>语法</p>
<div><pre><code><span>var</span> newArray <span>=</span> arr<span>.</span><span>flat</span><span>(</span><span>[</span>depth<span>]</span><span>)</span>
</code></pre>
<div><span>1</span><br></div></div><p>实现</p>
<ul>
<li>遍历扁平化</li>
</ul>
<div><pre><code><span>/**
 * 使用扩展运算符和some方法
 * some方法：用于检测数组中的元素是否满足指定条件（函数提供）；会依次执行数组的每个元素：
 * 如果有一个元素满足条件，则表达式返回true , 剩余的元素不会再执行检测。如果没有满足条件的元素，则返回false
 * 思想：some检测数组元素中是否含有数组，如果是则扩展这一层数组，循环知道数组中没有数组
 * @param {Array} arr 多维数组
 * @return faltArr 扁平化后的数组
 */</span>
<span>function</span> <span>flatten</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>while</span> <span>(</span>arr<span>.</span><span>some</span><span>(</span>Array<span>.</span>isArray<span>)</span><span>)</span> <span>{</span><span>//检测数组中元素是否包含数组</span>
    arr <span>=</span> <span>[</span><span>]</span><span>.</span><span>concat</span><span>(</span><span>...</span>arr<span>)</span> <span>//扩展这一层数组</span>
  <span>}</span>
  <span>return</span> arr
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div><ul>
<li>递归扁平化</li>
</ul>
<div><pre><code><span>// 使用 reduce、concat 和递归展开无限多层嵌套的数组</span>
<span>var</span> arr1 <span>=</span> <span>[</span><span>1</span><span>,</span><span>2</span><span>,</span><span>3</span><span>,</span><span>[</span><span>1</span><span>,</span><span>2</span><span>,</span><span>3</span><span>,</span><span>4</span><span>,</span> <span>[</span><span>2</span><span>,</span><span>3</span><span>,</span><span>4</span><span>]</span><span>]</span><span>]</span><span>;</span>

<span>function</span> <span>flatten</span><span>(</span><span>arr<span>,</span> d <span>=</span> <span>1</span></span><span>)</span> <span>{</span>
   <span>return</span> d <span>></span> <span>0</span> <span>?</span> arr<span>.</span><span>reduce</span><span>(</span><span>(</span><span>acc<span>,</span> val</span><span>)</span> <span>=></span> acc<span>.</span><span>concat</span><span>(</span>Array<span>.</span><span>isArray</span><span>(</span>val<span>)</span> <span>?</span> <span>flatten</span><span>(</span>val<span>,</span> d <span>-</span> <span>1</span><span>)</span> <span>:</span> val<span>)</span><span>,</span> <span>[</span><span>]</span><span>)</span>
                <span>:</span> arr<span>.</span><span>slice</span><span>(</span><span>)</span><span>;</span>
<span>}</span><span>;</span>

<span>flatten</span><span>(</span>arr1<span>,</span> <span>Infinity</span><span>)</span><span>;</span>
<span>// [1, 2, 3, 1, 2, 3, 4, 2, 3, 4]</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div><div><pre><code><span>/**
 * 遍历数组元素，判断元素是否为数组，如果是，则递归扁平并合并到新数组，否则放入新数组
 * @param {Array} arr 多维数组
 * @return 扁平后的一维数组
 */</span>
<span>function</span> <span>flatten</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>let</span> flatArr <span>=</span> <span>[</span><span>]</span>
  <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> arr<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>arr<span>[</span>i<span>]</span><span>)</span><span>)</span> <span>{</span>
      flatArr <span>=</span> flatArr<span>.</span><span>concat</span><span>(</span><span>flatten</span><span>(</span>arr<span>[</span>i<span>]</span><span>)</span><span>)</span>
    <span>}</span> <span>else</span> <span>{</span>
      flatArr<span>.</span><span>push</span><span>(</span>arr<span>[</span>i<span>]</span><span>)</span>
    <span>}</span>
  <span>}</span>
  <span>return</span> flatArr
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div><h4 id="算法题"> 算法题</h4>
<h5 id="合并有序数组"> 合并有序数组</h5>
<p>题目：</p>
<blockquote>
<p>已知线性表LA和LB的数据元素按值非递减有序排列，现要求合并LA和LB成一个新的线性表LC，且LC也非递减有序</p>
<p>例如：LA=（3，5，8，11）; LB=（2，6，8，9，11，15，20）</p>
<p>则合并的Lc=(2，3，5，6，8，8，9，11，11，15，20)</p>
</blockquote>
<p>思路：假设LA中元素为ai,LB中元素为bi,LC中元素为ci,则当ai&lt;=bi时，ci=ai;当ai&gt;bi时，ci=bi</p>
<p>实现：</p>
<div><pre><code><span>/**
 * 合并两个数组
 * @param {Array} La 待合并数组
 * @param {Array} Lb 待合并数组
 * @return {Array} Lc 待合后数组
 */</span>
<span>function</span> <span>mergeList</span><span>(</span><span>La<span>,</span> Lb</span><span>)</span> <span>{</span>
  <span>let</span> i <span>=</span> j <span>=</span> <span>0</span><span>;</span>
  <span>let</span> Lc <span>=</span> <span>[</span><span>]</span>
  <span>while</span> <span>(</span>i <span>&lt;</span> La<span>.</span>length <span>||</span> j <span>&lt;</span> Lb<span>.</span>length<span>)</span> <span>{</span>
    <span>if</span> <span>(</span>La<span>[</span>i<span>]</span> <span>&lt;=</span> Lb<span>[</span>j<span>]</span><span>)</span> <span>{</span>
      Lc<span>.</span><span>push</span><span>(</span>La<span>[</span>i<span>]</span><span>)</span>
      i<span>++</span>
    <span>}</span> <span>else</span> <span>{</span>
      Lc<span>.</span><span>push</span><span>(</span>Lb<span>[</span>j<span>]</span><span>)</span>
      j<span>++</span>
    <span>}</span>
  <span>}</span>
  <span>while</span> <span>(</span>i <span>&lt;</span> La<span>.</span>length<span>)</span> <span>{</span>
    Lc<span>.</span><span>push</span><span>(</span>La<span>[</span>i<span>]</span><span>)</span>
    i<span>++</span>
  <span>}</span>
  <span>while</span> <span>(</span>j <span>&lt;</span> Lb<span>.</span>length<span>)</span> <span>{</span>
    Lc<span>.</span><span>push</span><span>(</span>Lb<span>[</span>j<span>]</span><span>)</span>
    j<span>++</span>
  <span>}</span>
  <span>return</span> Lc
<span>}</span>

<span>//-------------------------------测试---------------------</span>
<span>let</span> La <span>=</span> <span>[</span><span>3</span><span>,</span> <span>5</span><span>,</span> <span>8</span><span>,</span> <span>11</span><span>]</span>
<span>let</span> Lb <span>=</span> <span>[</span><span>2</span><span>,</span> <span>6</span><span>,</span> <span>8</span><span>,</span> <span>9</span><span>,</span> <span>11</span><span>,</span> <span>15</span><span>,</span> <span>20</span><span>]</span>
<span>let</span> Lc <span>=</span> <span>mergeList</span><span>(</span>La<span>,</span> Lb<span>)</span>
console<span>.</span><span>log</span><span>(</span>Lc<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br></div></div><hr>
<h3 id="队列"> 队列</h3>
<h3 id="栈"> 栈</h3>
<h3 id="链表"> 链表</h3>
<h3 id="树"> <a href="./tree.html">树</a></h3>
<h3 id="图"> 图</h3>
<h2 id="算法"> 算法</h2>
<h3 id="查找"> 查找</h3>
<p><img src="./../images/分类.png" alt="" /></p>
<h4 id="折半查找"> 折半查找</h4>
<ul>
<li>
<p>适用范围： 有序表</p>
</li>
<li>
<p>思想：</p>
<ul>
<li>在有序表中，首先选取有序表的中间值 mid</li>
<li>比较中间值 mid 和 目标值 target 大小
<ul>
<li>mid &lt; target: 递归查找左子表</li>
<li>mid &gt; target:  递归查找右子表</li>
</ul>
</li>
</ul>
</li>
<li>
<p>示例</p>
<ul>
<li>对静态查找表<code>{5,13,19,21,37,56,64,75,80,88,92}</code>采用折半查找算法查找关键字为 21 的过程为：</li>
<li><img src="./../images/2-1G016102104D7.png" alt="img" />
<a href="http://data.biancheng.net/view/200.html" target="_blank" rel="noopener noreferrer">图</a> 1 折半查找的过程（a）</li>
<li>如上图 1 所示，指针 low 和 high 分别指向查找表的第一个关键字和最后一个关键字，指针 mid 指向处于 low 和 high 指针中间位置的关键字。在查找的过程中每次都同 mid 指向的关键字进行比较，由于整个表中的数据是有序的，因此在比较之后就可以知道要查找的关键字的大致位置。</li>
<li>例如在查找关键字 21 时，首先同 56 作比较，由于<code>21 &lt; 56</code>，而且这个查找表是按照升序进行排序的，所以可以判定如果静态查找表中有 21 这个关键字，就一定存在于 low 和 mid 指向的区域中间。</li>
<li>因此，再次遍历时需要更新 high 指针和 mid 指针的位置，令 high 指针移动到 mid 指针的左侧一个位置上，同时令 mid 重新指向 low 指针和 high 指针的中间位置。如图 2 所示：</li>
<li><img src="./../images/2-1G016102250N2.png" alt="img" />
图 2 折半查找的过程（b）</li>
<li>同样，用 21 同 mid 指针指向的 19 作比较，<code>19 &lt; 21</code>，所以可以判定 21 如果存在，肯定处于 mid 和 high 指向的区域中。所以令 low 指向 mid 右侧一个位置上，同时更新 mid 的位置。</li>
<li><img src="./../images/2-1G01610231cS.png" alt="img" />
<ul>
<li>图 3 折半查找的过程（3）</li>
</ul>
</li>
<li>当第三次做判断时，发现 mid 就是关键字 21 ，查找结束。</li>
</ul>
</li>
<li>
<p>注意：在做查找的过程中，如果 low 指针和 high 指针的中间位置在计算时位于两个关键字中间，即求得 mid 的位置不是整数，需要统一做取整操作。</p>
</li>
<li>
<p>实现：</p>
<div><pre><code><span>function</span> <span>BinarySearch</span><span>(</span><span>array<span>,</span>target<span>,</span>left<span>,</span> right</span><span>)</span><span>{</span>
	<span>let</span>  mid  
	<span>let</span>  left <span>=</span> <span>0</span>
	<span>let</span>  right <span>=</span> array<span>.</span>length <span>-</span> <span>1</span>
	<span>while</span><span>(</span>left  <span>&lt;=</span> right<span>)</span> <span>{</span>
		mid <span>=</span> <span>(</span>left <span>+</span> right<span>)</span><span>/</span><span>2</span>
		<span>if</span><span>(</span>array<span>[</span>mid<span>]</span> <span>===</span> array<span>[</span>target<span>]</span><span>)</span><span>{</span>
			<span>return</span> array<span>[</span>mid<span>]</span>
		<span>}</span><span>else</span> <span>if</span><span>(</span>array<span>[</span>mid<span>]</span> <span>></span> array<span>[</span>target<span>]</span><span>)</span> <span>{</span>
			right <span>=</span> mid <span>-</span> <span>1</span> 
        <span>}</span><span>else</span> <span>{</span>
        	left <span>=</span> mid <span>+</span> <span>1</span> 
        <span>}</span>
	<span>}</span>
	<span>return</span> <span>false</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div></li>
</ul>
<h4 id="二叉排序树-二叉查找树"> 二叉排序树（二叉查找树）</h4>
<h4 id="平衡二叉树-avl树"> 平衡二叉树（AVL树）</h4>
<h4 id="哈希查找"> 哈希查找</h4>
<h4 id="javascript-中查找方法"> JavaScript 中查找方法</h4>
<h5 id="数组中查找方法"> 数组中查找方法</h5>
<h6 id="find"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/find" target="_blank" rel="noopener noreferrer">find()</a></h6>
<ul>
<li>
<p>实现</p>
<ul>
<li>
<div><pre><code><span>Array</span><span>.</span>prototype<span>.</span><span>find</span> <span>=</span> <span>function</span><span>(</span><span>fn</span><span>)</span> <span>{</span>
    <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> <span>this</span><span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
        <span>if</span> <span>(</span><span>fn</span><span>(</span><span>this</span><span>[</span>i<span>]</span><span>)</span><span>)</span> 
        	<span>return</span> <span>this</span><span>[</span>i<span>]</span>
    <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div></li>
</ul>
</li>
</ul>
<h6 id="findindex"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/findIndex" target="_blank" rel="noopener noreferrer">findIndex()</a></h6>
<ul>
<li>
<p>实现</p>
<ul>
<li>
<div><pre><code><span>Array</span><span>.</span>prototype<span>.</span><span>find</span> <span>=</span> <span>function</span><span>(</span><span>fn</span><span>)</span> <span>{</span>
    <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> <span>this</span><span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
        <span>if</span> <span>(</span><span>fn</span><span>(</span><span>this</span><span>[</span>i<span>]</span><span>)</span><span>)</span> 
        	<span>return</span> i
    <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div></li>
</ul>
</li>
</ul>
<h6 id="indexof"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/indexOf" target="_blank" rel="noopener noreferrer">indexOf()</a></h6>
<ul>
<li>
<p>实现</p>
<ul>
<li>
<div><pre><code><span>Array</span><span>.</span>prototype<span>.</span><span>indexOf</span> <span>=</span> <span>function</span><span>(</span><span>value<span>,</span>start</span><span>)</span> <span>{</span>
	<span>if</span><span>(</span><span>this</span> <span>===</span> <span>null</span> <span>||</span> <span>!</span>value<span>)</span>								<span>// 不允许null调用或则查找值为空</span>
		<span>return</span> <span>-</span><span>1</span>
	<span>let</span> i <span>=</span> <span>0</span><span>,</span>length <span>=</span> <span>this</span><span>.</span>length
	<span>if</span><span>(</span>start <span>&amp;&amp;</span> <span>typeof</span> start <span>===</span> <span>'number'</span><span>)</span> <span>{</span>				<span>// 处理第二个参数</span>
		<span>if</span><span>(</span>start <span>></span> length<span>)</span> <span>return</span> <span>-</span><span>1</span>
		i <span>=</span> <span>(</span>start <span>&lt;</span> <span>0</span> <span>?</span> Math<span>.</span><span>max</span><span>(</span><span>0</span><span>,</span>length<span>+</span>start<span>)</span><span>:</span>start<span>)</span><span>;</span>
	<span>}</span>
    <span>for</span><span>(</span><span>;</span>i<span>&lt;</span>length<span>;</span>i<span>++</span><span>)</span><span>{</span>										<span>//	从第二个参数位置起从前往后查找</span>
       <span>if</span><span>(</span><span>this</span><span>[</span>i<span>]</span><span>===</span>value<span>)</span><span>{</span>
           <span>return</span> i<span>;</span>
       <span>}</span>
   	<span>}</span>
   	<span>return</span> <span>-</span><span>1</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div></li>
<li>
<p>注意</p>
<ul>
<li><code>indexOf</code> 使用<a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Operators/Comparison_Operators#Using_the_Equality_Operators" target="_blank" rel="noopener noreferrer">strict equality</a> (无论是 ===, 还是 triple-equals操作符都基于同样的方法)进行判断 <code>searchElement与</code>数组中包含的元素之间的关系</li>
<li>第二个参数 start (开始查找的位置) 规则:
<ul>
<li>如果该索引值大于或等于数组长度，意味着不会在数组里查找，返回-1。</li>
<li>如果参数中提供的索引值是一个负值，则将其作为数组末尾的一个抵消，即-1表示从最后一个元素开始查找，-2表示从倒数第二个元素开始查找 ，以此类推。</li>
<li>注意：如果参数中提供的索引值是一个负值，并不改变其查找顺序，查找顺序仍然是从前向后查询数组。</li>
<li>如果抵消后的索引值仍小于0，则整个数组都将会被查询。其默认值为0.</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h6 id="lastindexof"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/lastIndexOf" target="_blank" rel="noopener noreferrer">lastIndexOf()</a></h6>
<ul>
<li>实现</li>
</ul>
<div><pre><code>    <span>Array</span><span>.</span>prototype<span>.</span><span>lastIndexOf</span> <span>=</span> <span>function</span><span>(</span><span>value<span>,</span>start</span><span>)</span> <span>{</span>
    	<span>if</span><span>(</span><span>this</span> <span>===</span> <span>null</span> <span>||</span> <span>!</span>value<span>)</span>	
    		<span>return</span> <span>-</span><span>1</span>
    	<span>let</span> i<span>=</span><span>this</span><span>.</span>length<span>-</span><span>1</span>
    	<span>if</span><span>(</span>start <span>&amp;&amp;</span> <span>typeof</span> start <span>===</span> <span>'number'</span><span>)</span> <span>{</span>
    		<span>if</span><span>(</span>start <span>&lt;</span> <span>0</span><span>)</span> <span>{</span>
    			<span>if</span><span>(</span>Math<span>.</span><span>abs</span><span>(</span>start<span>)</span> <span>></span> i<span>)</span> <span>{</span>
    				<span>return</span> <span>-</span><span>1</span>
    			<span>}</span><span>else</span> <span>{</span>
    				i <span>=</span> i<span>+</span>start
    			<span>}</span>
    		<span>}</span><span>else</span> <span>{</span>
    			i <span>=</span> i<span>-</span>start
    		<span>}</span>
    	<span>}</span>
    	<span>for</span><span>(</span><span>;</span>i<span>>=</span><span>0</span><span>;</span>i<span>--</span><span>)</span><span>{</span>										<span>//	从第二个参数位置起从后往前查找</span>
           <span>if</span><span>(</span><span>this</span><span>[</span>i<span>]</span><span>===</span>value<span>)</span><span>{</span>
               <span>return</span> i<span>;</span>
           <span>}</span>
       	<span>}</span>
       	<span>return</span> <span>-</span><span>1</span>
    <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br></div></div><h6 id="includes"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/includes" target="_blank" rel="noopener noreferrer">includes()</a></h6>
<hr>
<h3 id="排序"> 排序</h3>
<h4 id="排序算法分类"> 排序算法分类</h4>
<p><img src="./../images/排序.png" alt="image-20210707160132577" /></p>
<ul>
<li>按照存储类型分类：
<ul>
<li>内排序</li>
<li>外排序</li>
</ul>
</li>
<li>按照是否比较分类
<ul>
<li>比较类排序：通过比较来决定元素间的相对次序，由于其时间复杂度不能突破O(nlogn)，因此也称为非线性时间比较类排序。</li>
<li>非比较类排序：不通过比较来决定元素间的相对次序，它可以突破基于比较排序的时间下界，以线性时间运行，因此也称为线性时间非比较类排序。</li>
</ul>
</li>
</ul>
<h4 id="内排序分类"> 内排序分类</h4>
<ul>
<li>插入排序</li>
<li>快速排序</li>
<li>选择排序</li>
<li>归并排序</li>
<li>基排序</li>
</ul>
<h4 id="相关概念"> 相关概念</h4>
<ul>
<li><strong>稳定</strong>：如果a原本在b前面，而a=b，排序之后a仍然在b的前面。</li>
<li><strong>不稳定</strong>：如果a原本在b的前面，而a=b，排序之后 a 可能会出现在 b 的后面。</li>
<li><strong>时间复杂度</strong>：对排序数据的总的操作次数。反映当n变化时，操作次数呈现什么规律。</li>
<li>**空间复杂度：**是指算法在计算机</li>
</ul>
<p>内执行时所需存储空间的度量，它也是数据规模n的函数</p>
<h4 id="插入排序"> 插入排序</h4>
<p><img src="./../images/插入排序.png" alt="" /></p>
<h5 id="直接插入排序"> 直接插入排序</h5>
<p>思想：将左侧序列看成一个有序序列，每次将一个数字插入该有序序列，插入时，从有序序列最右侧开始比较，若比较的数较大，后移一位</p>
<p><img src="./../images/插入排序.gif" alt="" /></p>
<p>实现：从小到大排序插入排序</p>
<div><pre><code><span>function</span> <span>InsertSort</span><span>(</span><span>array</span><span>)</span> <span>{</span>  
	<span>for</span><span>(</span><span>let</span> i<span>=</span><span>1</span><span>;</span> i<span>&lt;</span>array<span>.</span>leng<span>;</span> i<span>++</span><span>)</span> <span>{</span> 			<span>//从第二位开始排序  </span>
 		<span>let</span> target <span>=</span> array<span>[</span>i<span>]</span> <span>//选择待排序目标值</span>
		<span>for</span><span>(</span><span>let</span> j<span>=</span>i<span>-</span><span>1</span><span>;</span> j<span>&lt;</span>array<span>.</span>leng<span>;</span> j<span>++</span><span>)</span> <span>{</span> 	<span>//往前（有序列表）中查找目标值的位置</span>
			<span>if</span><span>(</span>array<span>[</span>target<span>]</span> <span>&lt;</span> array<span>[</span>j<span>]</span><span>)</span> <span>{</span> 		<span>//如果大于目标，和目标值交换位置，目标值往前移</span>
				<span>[</span> array<span>[</span>target<span>]</span><span>,</span> array<span>[</span>j<span>]</span> <span>]</span> <span>=</span> <span>[</span> array<span>[</span>j<span>]</span><span>,</span> arrayp<span>[</span>target<span>]</span> <span>]</span>  
				target <span>=</span> j  					<span>//记录新的目标值的位置</span>
			<span>}</span><span>else</span> <span>{</span>  							<span>//否则，找到目标值插入的位置</span>
				<span>break</span>
			<span>}</span>
		<span>}</span>
	<span>}</span>
	<span>return</span> array
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div><p>复杂度</p>
<ul>
<li>时间复杂度 O(n2)</li>
<li>空间复杂度 O(1)</li>
</ul>
<p>稳定性：稳定</p>
<h5 id="折半插入排序"> 折半插入排序</h5>
<p>思想：由于插入排序基本操作是在一张有序表中进行查找插入目标值的位置，则该“查找”过程可以使用折半查找实现</p>
<p>实现</p>
<div><pre><code><span>function</span> <span>BianryInsertionSort</span><span>(</span><span>array</span><span>)</span> <span>{</span>			<span>//从小到大折半插入排序</span>
	<span>for</span><span>(</span><span>let</span> i<span>=</span><span>1</span><span>;</span> i<span>&lt;</span>array<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>			<span>//从第二位往前开始往前查找插入位置</span>
		<span>let</span> target<span>=</span>i<span>,</span> low<span>=</span><span>0</span><span>,</span> hight<span>=</span>i<span>-</span><span>1</span>			<span>//折半查找的头尾为有序表的头尾</span>
		<span>while</span><span>(</span>low<span>&lt;=</span>hight<span>)</span> <span>{</span>						<span>//折半查找插入位置</span>
			<span>let</span> mid <span>=</span> <span>(</span>low <span>+</span> hight<span>)</span><span>/</span><span>2</span>
			<span>if</span><span>(</span>array<span>[</span>target<span>]</span> <span>&lt;</span> array<span>[</span>mid<span>]</span><span>)</span> <span>{</span>
				hight <span>=</span> mid <span>-</span> <span>1</span>
			<span>}</span><span>else</span> <span>{</span>
				low  <span>=</span> mid <span>+</span> <span>1</span>
			<span>}</span>
		<span>}</span>										<span>//查找到插入位置为 hight+1 或者 low</span>
		<span>let</span> temp <span>=</span>  array<span>[</span>target<span>]</span>				<span>//记录目标值</span>
		<span>for</span><span>(</span><span>let</span> j<span>=</span>i<span>-</span><span>1</span><span>;</span> j<span>>=</span>hight<span>+</span><span>1</span><span>;</span> j<span>--</span><span>)</span> 		<span>//将 hight+1 之后元素后移一位 </span>
			array<span>[</span>j<span>+</span><span>1</span><span>]</span> <span>=</span> array<span>[</span>j<span>]</span>
		array<span>[</span>hight<span>+</span><span>1</span><span>]</span> <span>=</span> temp					<span>// 将目标元素插入 hight+1 位置</span>
	<span>}</span>
	<span>return</span> array
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br></div></div><p>复杂度</p>
<ul>
<li>时间复杂度O(n2) ： 折半查找仅仅减少关键字的比较次数，而记录移动次不变</li>
<li>空间复杂度 O(1)</li>
</ul>
<p>稳定性：稳定</p>
<h5 id="_2-路插入排序"> 2-路插入排序</h5>
<p>思想</p>
<ul>
<li>
<p>2-路插入排序算法是在折半插入排序的基础上对其进行改进，减少其在排序过程中移动记录的次数从而提高效率。</p>
</li>
<li>
<p>具体实现思路为：另外设置一个同存储记录的数组大小相同的数组 d，将数组d 看作头尾相接的环状结构；将无序表中第一个记录添加进 d[0] 的位置上，然后从无序表中第二个记录开始，同 d[0] 作比较：如果该值比 d[0] 大，则添加到其右侧；反之添加到其左侧。</p>
</li>
</ul>
<p>示例</p>
<ul>
<li>使用 2-路插入排序算法对无序表<code>{3,1,7,5,2,4,9,6}</code>排序的过程如下：
<ul>
<li>
<p>将记录 3 添加到数组 d 中：<img src="./../images/2-1G031103H0442.png" alt="img" /></p>
</li>
<li>
<p>然后将 1 插入到数组 d 中，如下所示：<img src="./../images/2-1G031103J55K.png" alt="img" /></p>
</li>
<li>
<p>将记录 7 插入到数组 d 中，如下图所示：<img src="./../images/2-1G031103S5252.png" alt="img" /></p>
</li>
<li>
<p>将记录 5 插入到数组 d 中，由于其比 7小，但是比 3 大，所以需要移动 7 的位置，然后将 5 插入，如下图所示：<img src="./../images/2-1G031103Z0Q7.png" alt="img" /></p>
</li>
<li>
<p>将记录 2 插入到数组 d 中，由于比 1大，比 3 小，所以需要移动 3、7、5 的位置，然后将 2 插入，如下图所示：<img src="./../images/2-1G03110391VW.png" alt="img" /></p>
</li>
<li>
<p>将记录 4 插入到数组 d 中，需要移动 5 和 7 的位置，如下图所示：<img src="./../images/2-1G031103942559.png" alt="img" /></p>
</li>
<li>
<p>将记录 9 插入到数组 d 中，如下图所示：<img src="./../images/2-1G031104001P7.png" alt="img" /></p>
</li>
<li>
<p>将记录 6 插入到数组 d 中，如下图所示：<img src="./../images/2-1G031104022292.png" alt="img" /></p>
</li>
<li>
<p>最终存储在原数组时，从 d[7] 开始依次存储。</p>
</li>
</ul>
</li>
</ul>
<p>实现</p>
<div><pre><code><span>function</span> <span>2</span><span>-</span><span>InserSort</span><span>(</span><span>array</span><span>)</span> <span>{</span> 
	<span>let</span> temp<span>=</span><span>[</span><span>]</span> 											<span>//创建环形数组</span>
	<span>let</span> j <span>=</span> <span>0</span>
	<span>let</span> min<span>=</span>max<span>=</span><span>0</span>											<span>//记录数组中最大和最小值</span>
	temp<span>[</span><span>0</span><span>]</span><span>=</span>array<span>[</span><span>0</span><span>]</span>										<span>//将最大和最小值放入环形数组</span>
	<span>for</span><span>(</span><span>let</span> i<span>=</span><span>1</span><span>;</span> i<span>&lt;</span>array<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>						<span>//循环将元素插入合适位置</span>
		<span>if</span><span>(</span>array<span>[</span>i<span>]</span> <span>&lt;</span> temp<span>[</span>min<span>]</span><span>)</span> <span>{</span>							<span>//待插入元素比最小元素小</span>
			min  <span>=</span> <span>(</span>min <span>-</span> <span>1</span> <span>+</span> n<span>)</span> <span>%</span> n						<span>//记录最小元素位置为当前最小元素位置后一位</span>
			temp<span>[</span>min<span>]</span> <span>=</span> array<span>[</span>i<span>]</span>							
		<span>}</span><span>else</span> <span>if</span><span>(</span>array<span>[</span>i<span>]</span> <span>></span> temp<span>[</span>max<span>]</span><span>)</span> <span>{</span>					<span>//待插入元素比最大元素大</span>
			max <span>=</span> <span>(</span>max <span>+</span> <span>1</span> <span>+</span> n<span>)</span> <span>%</span> n							<span>//记录最大元素位置为当前最大元素前一位</span>
			temp<span>[</span>max<span>]</span> <span>=</span> array<span>[</span>i<span>]</span>
		<span>}</span> <span>else</span> <span>{</span>											<span>//待插入元素位于最大和最小元素之间</span>
			j <span>=</span> <span>(</span>max <span>-</span><span>1</span> <span>+</span> n<span>)</span> <span>%</span> n							<span>//记录最大元素前一个位置</span>
			<span>while</span><span>(</span>temp<span>[</span> <span>(</span>k <span>-</span> <span>1</span> <span>+</span> n<span>)</span> <span>%</span> n<span>]</span> <span>></span> array<span>[</span>i<span>]</span><span>)</span> <span>{</span>		<span>//从最大元素往前查找待插入元素位置</span>
				temp<span>[</span> <span>(</span>k <span>+</span> n<span>)</span><span>%</span>n<span>]</span> <span>=</span> temp<span>[</span> <span>(</span>k <span>-</span> <span>1</span> <span>+</span> n<span>)</span><span>%</span>n<span>]</span>
				j <span>=</span> <span>(</span>k <span>-</span> <span>1</span> <span>+</span> n<span>)</span> <span>%</span> n
			<span>}</span>												<span>//找到待插入元素位置为 (k-1+n)%n</span>
			temp<span>[</span> <span>(</span>k <span>-</span> <span>1</span> <span>+</span> n<span>)</span><span>%</span>n<span>]</span> <span>=</span> array<span>[</span>i<span>]</span>					<span>//插入元素</span>
			max <span>=</span> <span>(</span>max <span>+</span> <span>1</span> <span>+</span> n<span>)</span> <span>%</span> n							<span>//最大元素位置前移一位</span>
		<span>}</span>
	<span>}</span>
	<span>return</span> temp
	
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br></div></div><p>复杂度</p>
<ul>
<li>
<p>时间复杂度 O(n2)：2-路插入排序相比于折半插入排序，只是减少了移动记录的次数，没有根本上避免</p>
</li>
<li>
<p>空间复杂度 O(n)</p>
</li>
</ul>
<p>稳定性：</p>
<h5 id="表插入排序"> <a href="http://data.biancheng.net/view/68.html" target="_blank" rel="noopener noreferrer">表插入排序</a></h5>
<h5 id="希尔排序"> 希尔排序</h5>
<ul>
<li>希尔排序（缩小增量排序）属于插入排序</li>
</ul>
<p>思想</p>
<ul>
<li>先将整体待排序记录分割成若干个子序列分别进行直接插入排序</li>
<li>选择一个增量序列 t1,t2,t3....tk,其中t1&gt;t2&gt;...，tk=1；</li>
<li>按增量序列个数k，对序列进行k 趟排序；</li>
<li>每趟排序，根据对应的增量ti，将待排序列分割成若干长度为m 的子序列，分别对各子表进行直接插入排序</li>
<li>仅增量因子为1 时，整个序列作为一个表来处理，表长度即为整个序列的长度\</li>
</ul>
<p>示例</p>
<ul>
<li>示例1：</li>
</ul>
<p><img src="./../images/希尔排序.jpg" alt="" /></p>
<ul>
<li>
<p>示例2：</p>
<p><img src="./../images/希尔排序.gif" alt="" /></p>
</li>
</ul>
<p>实现</p>
<div><pre><code><span>//一趟希尔排序</span>
<span>function</span> <span>ShellInsert</span><span>(</span><span>array<span>,</span>gap</span><span>)</span> <span>{</span>								<span>//数组分为 gap+1 ... array.length 组</span>
	<span>for</span><span>(</span><span>let</span> i<span>=</span>gap<span>+</span><span>1</span><span>;</span> i<span>&lt;</span>array<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>						<span>//从增量后一位开始作为插入值</span>
		<span>let</span> target <span>=</span> array<span>[</span>i<span>]</span>								
		<span>for</span><span>(</span>j<span>=</span>i<span>-</span>gap<span>;</span> array<span>[</span>j<span>]</span><span>></span>array<span>[</span>target<span>]</span> <span>&amp;&amp;</span> j<span>></span><span>0</span><span>;</span> j<span>-=</span>gap<span>)</span> <span>{</span>	<span>//按组插入排序</span>
			array<span>[</span>j<span>+</span>gap<span>]</span> <span>=</span> array<span>[</span>j<span>]</span>								<span>//记录后移，查找插入位置</span>
		<span>}</span>
		array<span>[</span>j<span>]</span> <span>=</span> target 										<span>//目标插入相应位置</span>
	<span>}</span>
<span>}</span>

<span>//按照增量 gap 进行排序</span>
<span>function</span> <span>ShellSort</span><span>(</span><span>array</span><span>)</span> <span>{</span>
	<span>for</span><span>(</span><span>let</span> gap<span>=</span>Math<span>.</span><span>floor</span><span>(</span>array<span>.</span>length<span>/</span><span>2</span><span>)</span><span>;</span> gap<span>></span><span>0</span><span>;</span> gap<span>=</span>Math<span>.</span><span>floor</span><span>(</span>gap<span>/</span><span>2</span><span>)</span><span>)</span> <span>{</span> <span>//gap 趟希尔排序</span>
		<span>ShellInsert</span><span>(</span>array<span>,</span>gap<span>)</span>
	<span>}</span>
	<span>return</span> array
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br></div></div><p>复杂度</p>
<ul>
<li>时间复杂度 O(nlogn)</li>
<li>空间复杂度</li>
</ul>
<p>稳定性：</p>
<h4 id="快速排序"> 快速排序</h4>
<p><img src="./../images/快速排序.png" alt="" /></p>
<h5 id="冒泡排序"> 冒泡排序</h5>
<p>思想</p>
<ul>
<li>一趟冒泡排序：先将第一个记录和第二个记录比较,若为逆序,则交换;继续比较第二和第三个关键字,依次类推,直至到第n-1个记录;
结果是将最大/最小的记录冒泡到最后一位</li>
<li>n个记录一共执行 n-1 趟冒泡排序</li>
</ul>
<p>示例</p>
<p><img src="./../images/冒泡排序.gif" alt="" /></p>
<p>实现</p>
<ul>
<li>基本实现</li>
</ul>
<div><pre><code><span>function</span> <span>BubbleSort</span><span>(</span><span>array</span><span>)</span> <span>{</span>
	<span>for</span><span>(</span><span>let</span> i<span>=</span>array<span>.</span>length<span>-</span><span>1</span><span>;</span>  i<span>></span><span>0</span><span>;</span> i<span>--</span><span>)</span> <span>{</span>							<span>// 进行 n-1 趟冒泡排序</span>
		<span>for</span><span>(</span><span>let</span> j<span>=</span><span>0</span><span>;</span> j<span>&lt;</span>i<span>;</span> j<span>++</span><span>)</span> <span>{</span>									<span>//	一趟冒泡排序</span>
			<span>if</span><span>(</span>array<span>[</span>j<span>]</span> <span>&lt;</span> array<span>[</span>j<span>+</span><span>1</span><span>]</span><span>)</span> <span>{</span>								<span>//	逆序，转换位置</span>
				<span>[</span> array<span>[</span>j<span>+</span><span>1</span><span>]</span><span>,</span> array<span>[</span>j<span>]</span> <span>]</span> <span>=</span> <span>[</span> array<span>[</span>j<span>]</span><span>,</span> array<span>[</span>j<span>+</span><span>1</span><span>]</span> <span>]</span>
			<span>}</span>
		<span>}</span>
	<span>}</span>
	<span>return</span> array
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div><ul>
<li>优化：当一次循环没有发生冒泡，说明已经排序完成，停止循环</li>
</ul>
<div><pre><code><span>function</span> <span>BubbleSort</span><span>(</span><span>array</span><span>)</span> <span>{</span>
	<span>for</span><span>(</span><span>let</span> i<span>=</span>array<span>.</span>length<span>;</span> i<span>></span><span>0</span><span>;</span> i<span>--</span><span>)</span> <span>{</span>
		<span>let</span> complete<span>=</span><span>true</span>												<span>//假设完成一趟排序</span>
		<span>for</span><span>(</span><span>let</span> j<span>=</span><span>0</span><span>;</span> j<span>&lt;</span>i<span>;</span> j<span>++</span><span>)</span> <span>{</span>
			<span>if</span><span>(</span>array<span>[</span>j<span>]</span> <span>&lt;</span> array<span>[</span>j<span>+</span><span>1</span><span>]</span><span>)</span> <span>{</span>
				<span>[</span> array<span>[</span>j<span>+</span><span>1</span><span>]</span><span>,</span> array<span>[</span>j<span>]</span> <span>]</span> <span>=</span> <span>[</span> array<span>[</span>j<span>]</span><span>,</span> array<span>[</span>j<span>+</span><span>1</span><span>]</span> <span>]</span>		<span>//发生冒泡，表示该趟未排序</span>
				complete<span>=</span><span>false</span>											
			<span>}</span>
			<span>if</span><span>(</span>complete<span>)</span> 												<span>//未发生过冒泡，表示该趟有序</span>
				<span>break</span>
		<span>}</span>
	<span>}</span>
	<span>return</span> array
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div><p>复杂度</p>
<ul>
<li>时间复杂度 O(n2)</li>
<li>空间复杂度O(1)</li>
</ul>
<p>稳定性</p>
<ul>
<li>稳定</li>
</ul>
<h5 id="快速排序-2"> 快速排序</h5>
<p>思想</p>
<ul>
<li>通过一趟排序将要排序的数据分割成独立的两部分，其中一部分的所有数据比另一部分的所有数据要小，再按这种方法对这两部分数据分别进行快速排序，整个排序过程可以递归进行，使整个数据变成有序序列</li>
<li>步骤：
<ul>
<li>选择一个基准元素<code>target</code>（一般选择第一个数）</li>
<li>将比<code>target</code>小的元素移动到数组左边，比<code>target</code>大的元素移动到数组右边</li>
<li>分别对<code>target</code>左侧和右侧的元素进行快速排序</li>
</ul>
</li>
<li>快速排序也利用了分治的思想（将问题分解成一些小问题递归求解）</li>
</ul>
<p>示例</p>
<ul>
<li>
<p>下面是对序列<code>6、1、2、7、9、3、4、5、10、8</code>排序的过程：</p>
<p><img src="./../images/快速排序.jpg" alt="" /></p>
<p><img src="./../images/快速排序2.gif" alt="" /></p>
</li>
</ul>
<p>实现</p>
<ul>
<li>递归实现1：
<ul>
<li>单独开辟两个存储空间<code>left</code>和<code>right</code>来存储每次递归比<code>target</code>小和大的序列</li>
<li>每次递归直接返回<code>left、target、right</code>拼接后的数组</li>
<li>浪费大量存储空间，写法简单</li>
</ul>
</li>
</ul>
<div><pre><code><span>function</span> <span>quickSort</span><span>(</span><span>array</span><span>)</span> <span>{</span>
      <span>if</span> <span>(</span>array<span>.</span>length <span>&lt;</span> <span>2</span><span>)</span> <span>{</span>							<span>//	个数小于2时递归结束</span>
        <span>return</span> array<span>;</span>
      <span>}</span>
      <span>const</span> target <span>=</span> array<span>[</span><span>0</span><span>]</span>							<span>//	以每次分割前第一个数组作为分割点</span>
      <span>const</span> left <span>=</span> <span>[</span><span>]</span>
      <span>const</span> right <span>=</span> <span>[</span><span>]</span>
      <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>1</span><span>;</span> i <span>&lt;</span> array<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>			<span>// 遍历数组分割数组</span>
        <span>if</span> <span>(</span>array<span>[</span>i<span>]</span> <span>&lt;</span> target<span>)</span> <span>{</span>
          left<span>.</span><span>push</span><span>(</span>array<span>[</span>i<span>]</span><span>)</span>							<span>// 小于分割点的数组</span>
        <span>}</span> <span>else</span> <span>{</span>
          right<span>.</span><span>push</span><span>(</span>array<span>[</span>i<span>]</span><span>)</span>							<span>// 大于分割点的数组</span>
        <span>}</span>
      <span>}</span>
      <span>return</span> <span>quickSort</span><span>(</span>left<span>)</span><span>.</span><span>concat</span><span>(</span><span>[</span>target<span>]</span><span>,</span> <span>quickSort</span><span>(</span>right<span>)</span><span>)</span>	<span>// 递归分割左右两边数组</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div><ul>
<li>
<p>递归实现2：</p>
<ul>
<li>附设两个指针 <code>l</code> 和 <code>r</code>，它们的初值分别为 数组第一个 和 最后一个值，设枢轴记录的关键字为 target</li>
<li>首先从 <code>l</code> 所指位置起向前搜索找到第一个关键字小于 target 的记录和 target 记录互相交换</li>
<li>然后从 <code>l</code> 所指位置起向后搜索，找到第一个关键字大于 target 的记录和 target 记录互相交换</li>
<li>重复这两步直至 <code>l</code> = <code>r</code>为止, 此时左侧的值全部小于<code>target</code>，右侧的值全部小于<code>target</code>，将<code>target</code>放到该位置</li>
</ul>
<div><pre><code>   <span>function</span> <span>quickSort</span><span>(</span><span>array<span>,</span> start<span>,</span> end</span><span>)</span> <span>{</span>
      <span>if</span> <span>(</span>end <span>-</span> start <span>&lt;</span> <span>1</span><span>)</span> <span>{</span> 					<span>// 递归结束条件</span>
        <span>return</span>
      <span>}</span>
      <span>const</span> target <span>=</span> array<span>[</span>start<span>]</span>				
      <span>let</span> l <span>=</span> start								<span>// 起始 start 为 0</span>
      <span>let</span> r <span>=</span> end								<span>// 起始 end 为 0</span>
      
      <span>while</span> <span>(</span>l <span>&lt;</span> r<span>)</span> <span>{</span>							<span>// l往前搜索，r往后搜索；当 l=r 表明数组遍历完成</span>
        <span>while</span> <span>(</span>l <span>&lt;</span> r <span>&amp;&amp;</span> array<span>[</span>r<span>]</span> <span>>=</span> target<span>)</span> <span>{</span>	<span>// 从右侧往左侧查找小于 target 的值</span>
          r<span>--</span>						
        <span>}</span>
        array<span>[</span>l<span>]</span> <span>=</span> array<span>[</span>r<span>]</span>					 	<span>// 将右侧小于 target 的值和 target 值交换</span>
        <span>while</span> <span>(</span>l <span>&lt;</span> r <span>&amp;&amp;</span> array<span>[</span>l<span>]</span> <span>&lt;</span> target<span>)</span> <span>{</span>	<span>//从左侧往右侧查找大于 target 的值</span>
          l<span>++</span>
        <span>}</span>
         array<span>[</span>r<span>]</span> <span>=</span> array<span>[</span>l<span>]</span>					  <span>// 将左侧大于 target 的值和 target 值交换</span>
      <span>}</span>
      array<span>[</span>l<span>]</span> <span>=</span> target								<span>// 当 l=r 时的位置为 target 的位置,完成一趟快排</span>
      <span>quickSort</span><span>(</span>array<span>,</span> start<span>,</span> l <span>-</span> <span>1</span><span>)</span>				<span>// 快速排序左侧数据</span>
      <span>quickSort</span><span>(</span>array<span>,</span> l <span>+</span> <span>1</span><span>,</span> end<span>)</span>					<span>// 快速排序右侧数据</span>
      <span>return</span> array
    <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br></div></div></li>
</ul>
<p>复杂度</p>
<ul>
<li>
<p>时间复杂度：平均<code>O(nlogn)</code>，最坏<code>O(n2)</code>，实际上大多数情况下小于<code>O(nlogn)</code></p>
</li>
<li>
<p>空间复杂度:<code>O(logn)</code>（递归调用消耗）</p>
</li>
</ul>
<p>稳定性</p>
<ul>
<li>不稳定</li>
</ul>
<h4 id="选择排序"> 选择排序</h4>
<p><img src="./../images/选择排序.png" alt="" /></p>
<h5 id="简单选择排序"> 简单选择排序</h5>
<p>思想</p>
<ul>
<li>一趟选择：从数组中 n-i+1 ( n 为数组长度 )个记录中选择一个最小/最大的数与第 i 个数据交换位置</li>
<li>长度为 n 数组循环选择 n-1 趟</li>
</ul>
<p>示例</p>
<p><img src="./../images/选择排序.gif" alt="" /></p>
<p>实现</p>
<div><pre><code><span>function</span> <span>SelectSort</span><span>(</span><span>array</span><span>)</span> <span>{</span>								<span>// 从小到大选择排序</span>
	<span>for</span><span>(</span><span>let</span> i<span>=</span><span>0</span><span>;</span> i<span>&lt;</span>array<span>.</span>length<span>-</span><span>1</span><span>;</span> i<span>++</span><span>)</span> <span>{</span>					<span>// n-1 趟循环选择</span>
		<span>let</span> min<span>=</span>i											<span>// 选择最小值下标</span>
		<span>for</span><span>(</span><span>let</span> j<span>=</span>i<span>+</span><span>1</span><span>;</span> j<span>&lt;</span>array<span>.</span>length<span>;</span> j<span>++</span><span>)</span> <span>{</span>				<span>// 遍历查找最小值下标</span>
			<span>if</span><span>(</span>array<span>[</span>j<span>]</span> <span>&lt;</span> array<span>[</span>min<span>]</span><span>)</span> <span>{</span>
				min <span>=</span> j										<span>//	记录最小值下标</span>
			<span>}</span>
		<span>}</span>
		<span>[</span> array<span>[</span>min<span>]</span><span>,</span> array<span>[</span>j<span>]</span> <span>]</span> <span>=</span>  <span>[</span> array<span>[</span>j<span>]</span><span>,</span> array<span>[</span>min<span>]</span> <span>]</span> <span>// 交换最小值</span>
	<span>}</span>
	<span>return</span> array
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div><p>复杂度</p>
<ul>
<li>时间复杂度 O(n2)</li>
<li>空间复杂度 O(1)</li>
</ul>
<p>稳定性</p>
<ul>
<li>不稳定</li>
</ul>
<h5 id="树形选择排序"> 树形选择排序</h5>
<p>思想</p>
<ul>
<li>树形选择排序（又称“锦标赛排序”），是一种按照锦标赛的思想进行选择排序的方法</li>
<li>即所有记录采取两两分组，筛选出较小（较大）的值；</li>
<li>然后从筛选出的较小（较大）值中再两两分组选出更小（更大）值，依次类推，直到最后选出一个最小（最大）值。</li>
<li>同样可以采用此方式筛选出次小（次大）值等</li>
<li>整个排序的过程，可以用一棵具有 n 个叶子结点的完全二叉树表示</li>
</ul>
<p>示例</p>
<ul>
<li>
<p>对无序表<code>{49，38，65，97，76，13，27，49}</code>采用树形选择的方式排序，过程如下：</p>
</li>
<li>
<p>首先将无序表中的记录采用两两分组，筛选出各组中的较小值（如图1 中的（a）过程）；然后将筛选出的较小值两两分组，筛选出更小的值，以此类推（如图 1 中的（b）（c）过程），最终整棵树的根结点中的关键字即为最小关键字：</p>
<p><img src="./../images/2-1G10G11310U9.png" alt="img" />
图 1 树形选择排序（一）</p>
</li>
<li>
<p>筛选出关键字 13 之后，继续重复此方式找到剩余记录中的最小值，此时由于关键字 13 已经筛选完成，需要将关键字 13 改为“最大值”，继续重复此过程，如图 2 所示：</p>
<p><img src="./../images/2-1G10G11341338.png" alt="img" />
图 2 树形选择排序（二）</p>
</li>
<li>
<p>通过不断地重复此过程，可依次筛选出从小到大的所有关键字</p>
</li>
</ul>
<p>复杂度</p>
<ul>
<li>时间复杂度 O(nlogn)</li>
<li>空间复杂度、</li>
</ul>
<h5 id="堆排序"> 堆排序</h5>
<p>相关概念</p>
<ul>
<li>
<p>堆的含义：在含有 n 个元素的序列中，如果序列中的元素满足下面其中一种关系时，此序列可以称之为堆。</p>
<ul>
<li>ki ≤ k2i 且 ki ≤ k2i+1（在 n 个记录的范围内，第 i 个关键字的值小于第 2<em>i 个关键字，同时也小于第 2</em>i+1 个关键字）</li>
<li>ki ≥ k2i 且 ki ≥ k2i+1（在 n 个记录的范围内，第 i 个关键字的值大于第 2<em>i 个关键字，同时也大于第 2</em>i+1 个关键字）</li>
</ul>
</li>
<li>
<p>对于堆的定义也可以使用完全二叉树来解释，因为在完全二叉树中第 i 个结点的左孩子恰好是第 2i 个结点，右孩子恰好是 2i+1 个结点。如果该序列可以被称为堆，则使用该序列构建的完全二叉树中，每个根结点的值都必须不小于（或者不大于）左右孩子结点的值。</p>
</li>
<li>
<p>以无序表<code>{49，38，65，97，76，13，27，49}</code>来讲，其对应的堆用完全二叉树来表示为：</p>
<p><img src="./../images/2-1G10G11P3256.png" alt="img" />
图 3 无序表对应的堆</p>
</li>
</ul>
<blockquote>
<p>提示：堆用完全二叉树表示时，其表示方法不唯一，但是可以确定的是树的根结点要么是无序表中的最小值，要么是最大值。</p>
</blockquote>
<ul>
<li>堆排序: 通过将无序表转化为堆，可以直接找到表中最大值或者最小值，然后将其提取出来，令剩余的记录再重建一个堆，取出次大值或者次小值，如此反复执行就可以得到一个有序序列，此过程为堆排序。</li>
</ul>
<p>示例</p>
<ol>
<li>
<p>将无序序列转化为一个堆</p>
<ul>
<li>
<p>假设无序表<code>{49，38，65，97，76，13，27，49}</code>初步建立的完全二叉树，如下图所示：</p>
</li>
<li>
<p><img src="./../images/2-1G10G12041N3.png" alt="img" /></p>
</li>
<li>
<p>在对上图做筛选工作时，规律是从底层结点开始，一直筛选到根结点。</p>
</li>
<li>
<p>对于具有 n 个结点的完全二叉树，筛选工作开始的结点为第 ⌊n/2⌋个结点（此结点后序都是叶子结点，无需筛选）</p>
</li>
<li>
<p>所以，对于有 9 个结点的完全二叉树，筛选工作从第 4 个结点 97 开始，由于 97 &gt; 49 ,所以需要相互交换，交换后如下图所示：</p>
</li>
<li>
<p><img src="./../images/2-1G10G1212b35.png" alt="img" /></p>
</li>
<li>
<p>然后再筛选第 3 个结点 65 ，由于 65 比左右孩子结点都大，则选择一个最小的同 65 进行交换，交换后的结果为：</p>
</li>
<li>
<p><img src="./../images/2-1G10G1214J09.png" alt="img" /></p>
</li>
<li>
<p>然后筛选第 2 个结点，由于其符合要求，所以不用筛选；最后筛选根结点 49 ，同 13 进行交换，交换后的结果为：</p>
</li>
<li>
<p><img src="./../images/2-1G10G12204617.png" alt="img" /></p>
</li>
<li>
<p>交换后，发现破坏了其右子树堆的结构，所以还需要调整，最终调整后的结果为：</p>
</li>
<li>
<p><img src="./../images/2-1G10G12221A8.png" alt="img" /></p>
</li>
</ul>
</li>
<li>
<p>在输出堆顶元素之后（完全二叉树的树根结点），调整剩余元素构建一个新的堆</p>
<ul>
<li>图 3 所示为一个完全二叉树，若去除堆顶元素，即删除二叉树的树根结点，此时用二叉树中最后一个结点 97 代替,如图所示：</li>
<li><img src="./../images/2-1G10G1192WU.png" alt="img" /></li>
<li>此时由于结点 97 比左右孩子结点的值都大，破坏了堆的结构，所以需要进行调整：首先以 堆顶元素 97 同左右子树比较，同值最小的结点交换位置，即 27 和 97 交换位置：</li>
<li><img src="./../images/2-1G10G1194A41.png" alt="img" /></li>
<li>由于替代之后破坏了根结点右子树的堆结构，所以需要进行和上述一样的调整，即令 97 同 49 进行交换位置：</li>
<li><img src="./../images/2-1G10G12003264.png" alt="img" /></li>
<li>通过上述的调整，之前被破坏的堆结构又重新建立。从根结点到叶子结点的整个调整的过程，被称为“筛选“</li>
</ul>
</li>
<li>
<p>动态图演示(创建大顶堆，从小到大排序)</p>
<ul>
<li><img src="./../images/堆排序.gif" alt="" /></li>
</ul>
</li>
</ol>
<p>实现</p>
<ul>
<li>递归实现</li>
</ul>
<div><pre><code><span>//从小到大堆排序</span>
<span>function</span> <span>heapSort</span><span>(</span><span>array</span><span>)</span> <span>{</span>
	<span>createMaxHeap</span><span>(</span>array<span>)</span>									<span>//	创建一个大顶堆</span>
	<span>for</span><span>(</span><span>let</span> i<span>=</span>array<span>.</span>length<span>-</span><span>1</span><span>;</span> i<span>></span><span>0</span><span>;</span> i<span>--</span><span>)</span> <span>{</span>					<span>//  遍历调整堆</span>
		<span>[</span> array<span>[</span>i<span>]</span><span>,</span> array<span>[</span><span>0</span><span>]</span> <span>]</span>  <span>=</span> <span>[</span> array<span>[</span><span>0</span><span>]</span><span>,</span> array<span>[</span>i<span>]</span> <span>]</span>	<span>// 	输出最大元素</span>
		<span>adjustHeap</span><span>(</span>array<span>,</span><span>0</span><span>,</span>i<span>)</span>								<span>//	调整剩余堆，第一个元素调整到对应位置</span>
	<span>}</span>
	<span>return</span> array
<span>}</span>

<span>//创建大顶堆</span>
<span>function</span> <span>createHeap</span><span>(</span><span>array</span><span>)</span> <span>{</span>
	<span>let</span> start <span>=</span> array<span>.</span>length<span>/</span><span>2</span>								<span>// 数组中间值作为初始堆顶元素					</span>
	<span>for</span><span>(</span><span>let</span> i<span>=</span>start<span>;</span> i<span>>=</span><span>0</span><span>;</span> i<span>--</span><span>)</span> <span>{</span>							<span>// 调整堆的过程就是创建大顶堆的过程</span>
		<span>adjustHeap</span><span>(</span>array<span>,</span> i<span>,</span> array<span>.</span>length<span>)</span>								<span>// 调整第i个元素为大顶堆</span>
	<span>}</span>
<span>}</span>

<span>//将 target 元素进行下沉，孩子节点有比 target 元素大的就下沉</span>
<span>function</span> <span>adjustHeap</span><span>(</span><span>array<span>,</span> target<span>,</span>len</span><span>)</span> <span>{</span>
	<span>let</span> left <span>=</span> <span>2</span> <span>*</span> targer <span>+</span> <span>1</span><span>,</span>								<span>// target 左子节点 </span>
		rigth <span>=</span> <span>2</span> <span>*</span> target <span>+</span> <span>2</span><span>,</span>								<span>// targer 右子节点	</span>
		max <span>=</span> target										<span>// 假设当前堆顶 target 为最大元素</span>
	<span>if</span><span>(</span>left <span>&lt;</span> len <span>&amp;&amp;</span> array<span>[</span>left<span>]</span> <span>></span> array<span>[</span>max<span>]</span><span>)</span> <span>{</span>			<span>// 左子节比堆顶值大，设定左子节点为最大值</span>
		max <span>=</span> left
	<span>}</span>
	<span>if</span><span>(</span>right <span>&lt;</span> len <span>&amp;&amp;</span> array<span>[</span>right<span>]</span> <span>></span> array<span>[</span>max<span>]</span><span>)</span> <span>{</span>			<span>// 右子节点比堆顶值大，设定右子节点为最大值</span>
		max <span>=</span> right
	<span>}</span> 
	<span>if</span><span>(</span>max <span>!=</span> i<span>)</span> <span>{</span>											<span>//	最大值不是堆顶</span>
		<span>[</span> array<span>[</span>max<span>]</span><span>,</span> array<span>[</span>i<span>]</span> <span>]</span> <span>=</span> <span>[</span> array<span>[</span>i<span>]</span><span>,</span> array<span>[</span>max<span>]</span> <span>]</span> <span>// 交换堆顶和最大值</span>
		<span>adjustHeap</span><span>(</span>array<span>,</span> max<span>,</span>len<span>)</span>
	<span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br></div></div><ul>
<li>非递归实现</li>
</ul>
<div><pre><code><span>//从小到大堆排序</span>
<span>function</span> <span>heapSort</span><span>(</span><span>array</span><span>)</span> <span>{</span>
	<span>createMaxHeap</span><span>(</span>array<span>)</span>									<span>//	创建一个大顶堆</span>
	<span>for</span><span>(</span><span>let</span> i<span>=</span>array<span>.</span>length<span>-</span><span>1</span><span>;</span> i<span>></span><span>0</span><span>;</span> i<span>--</span><span>)</span> <span>{</span>					<span>//  遍历调整堆</span>
		<span>[</span> array<span>[</span>i<span>]</span><span>,</span> array<span>[</span><span>0</span><span>]</span> <span>]</span>  <span>=</span> <span>[</span> array<span>[</span><span>0</span><span>]</span><span>,</span> array<span>[</span>i<span>]</span> <span>]</span>	<span>// 	输出最大元素</span>
		<span>adjustHeap</span><span>(</span>array<span>,</span><span>0</span><span>,</span>i<span>)</span>								<span>//	调整剩余堆，第一个元素调整到对应位置</span>
	<span>}</span>
	<span>return</span> array
<span>}</span>

<span>//创建大顶堆</span>
<span>function</span> <span>createHeap</span><span>(</span><span>array</span><span>)</span> <span>{</span>
	<span>let</span> start <span>=</span> array<span>.</span>length<span>/</span><span>2</span>								<span>// 数组中间值作为初始堆顶元素					</span>
	<span>for</span><span>(</span><span>let</span> i<span>=</span>start<span>;</span> i<span>>=</span><span>0</span><span>;</span> i<span>--</span><span>)</span> <span>{</span>							<span>// 调整堆的过程就是创建大顶堆的过程</span>
		<span>adjustHeap</span><span>(</span>array<span>,</span> i<span>,</span> array<span>.</span>length<span>)</span>					<span>// 调整第i个元素为大顶堆</span>
	<span>}</span>
<span>}</span>

<span>// 将第target个元素进行下沉，孩子节点有比他大的就下沉</span>
<span>// 已知 array[target ... len] 中记录的关键字除了 array[target] 之外均满足堆的定义，本函数调整 array[target]</span>
<span>// 的关键字，使 array[target ... len]成为一个大顶堆</span>
<span>function</span> <span>adjustHeap</span><span>(</span><span>array<span>,</span> target<span>,</span> len</span><span>)</span> <span>{</span>
      <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>2</span> <span>*</span> target <span>+</span> <span>1</span><span>;</span> i <span>&lt;</span> len<span>;</span> i <span>=</span> <span>2</span> <span>*</span> i <span>+</span> <span>1</span><span>)</span> <span>{</span> <span>//沿着key较大的孩子节点向下筛选</span>
        <span>if</span> <span>(</span>i <span>+</span> <span>1</span> <span>&lt;</span> len <span>&amp;&amp;</span> array<span>[</span>i <span>+</span> <span>1</span><span>]</span> <span>></span> array<span>[</span>i<span>]</span><span>)</span> <span>{</span>    	 <span>// 找到孩子节点中最大的</span>
          i <span>=</span> i <span>+</span> <span>1</span><span>;</span>
        <span>}</span>
        <span>if</span> <span>(</span>array<span>[</span>i<span>]</span> <span>></span> array<span>[</span>target<span>]</span><span>)</span> <span>{</span>						 <span>// 当子节点大于目标节点，交换位置并将子节点作为新目标</span>
          <span>[</span>array<span>[</span>i<span>]</span><span>,</span> array<span>[</span>target<span>]</span><span>]</span> <span>=</span> <span>[</span>array<span>[</span>target<span>]</span><span>,</span> array<span>[</span>i<span>]</span><span>]</span>
          target <span>=</span> i<span>;</span>
        <span>}</span> <span>else</span> <span>{</span>											<span>//	否则推出本次查找</span>
          <span>break</span><span>;</span>
        <span>}</span>
      <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br></div></div><p>复杂度</p>
<ul>
<li>时间复杂度  O(nlogn)</li>
<li>空间复杂度  O(1)</li>
</ul>
<p>稳定性</p>
<ul>
<li>不稳定</li>
</ul>
<h4 id="归并排序"> 归并排序</h4>
<p>思想</p>
<ul>
<li>归并是采用分治法（<code>Divide and Conquer</code>）的一个非常典型的应用。
（分治法将问题分成一些小的问题然后递归求解，而治的阶段则将分的阶段得到的各答案&quot;修补&quot;在一起，即分而治之)
<ul>
<li>将已有序的子序列合并，得到完全有序的序列</li>
<li>即先使每个子序列有序，再使子序列段间有序</li>
<li>若将两个有序表合并成一个有序表，称为二路归并</li>
</ul>
</li>
<li>步骤：
<ul>
<li>分割：
<ul>
<li>将数组从中点进行分割，分为左、右两个数组</li>
<li>递归分割左、右数组，直到数组长度小于<code>2</code></li>
</ul>
</li>
<li>合并
<ul>
<li>如果需要合并，那么左右两数组已经有序了</li>
<li>创建一个临时存储数组<code>temp</code>，比较两数组第一个元素，将较小的元素加入临时数组</li>
<li>若左右数组有一个为空，那么此时另一个数组一定大于<code>temp</code>中的所有元素，直接将其所有元素加入<code>temp</code></li>
</ul>
</li>
</ul>
</li>
</ul>
<p>示例</p>
<ul>
<li>假设数组[8,4,5,7,1,3,6,2]</li>
</ul>
<p><img src="./../images/归并排序.png" alt="" /></p>
<ul>
<li>合并过程：
<ul>
<li>如上图中的最后一次合并，要将[4,5,7,8]和[1,2,3,6]两个已经有序的子序列，合并为最终序列[1,2,3,4,5,6,7,8]，来看下实现步骤</li>
<li><img src="./../images/归并排序-合并.png" alt="" /></li>
</ul>
</li>
</ul>
<p>实现</p>
<ul>
<li>
<p>实现1：</p>
<p>分割数组时直接将数组分割为两个数组，合并时直接合并数组。</p>
<p>优点：思路简单，写法简单</p>
<p>缺点：空间复杂度略高，需要复制多个数组</p>
</li>
</ul>
<div><pre><code><span>function</span> <span>mrgeSort</span><span>(</span><span>array</span><span>)</span> <span>{</span>
	<span>if</span><span>(</span>array<span>.</span>length <span>&lt;</span> <span>2</span><span>)</span> 							<span>//	数组个数小于2不能再分割，返回数组</span>
		<span>return</span>	array 
	<span>let</span> mid <span>=</span> Math<span>.</span><span>floor</span><span>(</span>array<span>.</span>length<span>/</span><span>2</span><span>)</span>			<span>//	以数组中间数据作为分割点</span>
	<span>let</span> left <span>=</span> array<span>.</span><span>slice</span><span>(</span><span>0</span><span>,</span> mid<span>)</span>					<span>//	分割点左侧数组</span>
	<span>let</span> right <span>=</span> array<span>.</span><span>slice</span><span>(</span>mid<span>,</span> array<span>.</span>length<span>)</span>		<span>//	分割点右侧数组</span>
	<span>return</span> <span>mrge</span><span>(</span><span>mergeSort</span><span>(</span>left<span>)</span><span>,</span> <span>mergeSort</span><span>(</span>right<span>)</span><span>)</span>	<span>//	递归分割数组后递归合并数组</span>
<span>}</span>
<span>//从小到大合并数据</span>
<span>function</span> <span>merge</span><span>(</span><span>left<span>,</span>right</span><span>)</span> <span>{</span>						
	<span>let</span> temp <span>=</span> <span>[</span><span>]</span>									<span>//	设定空数组保存合并数组</span>
	<span>while</span><span>(</span>left<span>.</span>length <span>&amp;&amp;</span> right<span>.</span>length <span>)</span> <span>{</span>			<span>//	左右两侧有数组都不为空表示未合并完成</span>
		<span>if</span><span>(</span>left<span>[</span><span>0</span><span>]</span><span>&lt;</span>right<span>[</span><span>0</span><span>]</span><span>)</span> <span>{</span>						<span>//	比较数组第一个数据大小</span>
			temp<span>.</span><span>push</span><span>(</span> left<span>.</span><span>shift</span><span>(</span><span>)</span> <span>)</span>				<span>//	移出较小数据放入新数组</span>
		<span>}</span><span>else</span> <span>{</span>
			temp<span>.</span><span>push</span><span>(</span> right<span>.</span><span>shift</span><span>(</span><span>)</span> <span>)</span>
		<span>}</span>
	<span>}</span>
	<span>while</span><span>(</span>left<span>.</span>length<span>)</span> <span>{</span>							<span>//	未合并完成，剩余数据放入新数组</span>
		temp<span>.</span><span>push</span><span>(</span> left<span>.</span><span>shift</span><span>(</span><span>)</span> <span>)</span>
	<span>}</span>
	<span>while</span><span>(</span>right<span>.</span>length<span>)</span> <span>{</span>							<span>//	未合并完成，剩余数据放入新数组</span>
		temp<span>.</span><span>push</span><span>(</span> right<span>.</span><span>shift</span><span>(</span><span>)</span> <span>)</span>
	<span>}</span>
	<span>return</span> temp
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br></div></div><ul>
<li>
<p>实现2：</p>
<p>记录数组的索引，使用<code>left、right</code>两个索引来限定当前分割的数组。</p>
<p>优点：空间复杂度低，只需一个<code>temp</code>存储空间，不需要拷贝数组</p>
<p>缺点：写法复杂</p>
</li>
</ul>
<div><pre><code>    <span>function</span> <span>mergeSort</span><span>(</span><span>array<span>,</span> left<span>,</span> right<span>,</span> temp</span><span>)</span> <span>{</span>
      <span>if</span> <span>(</span>left <span>&lt;</span> right<span>)</span> <span>{</span>
        <span>const</span> mid <span>=</span> Math<span>.</span><span>floor</span><span>(</span><span>(</span>left <span>+</span> right<span>)</span> <span>/</span> <span>2</span><span>)</span><span>;</span>
        <span>mergeSort</span><span>(</span>array<span>,</span> left<span>,</span> mid<span>,</span> temp<span>)</span>
        <span>mergeSort</span><span>(</span>array<span>,</span> mid <span>+</span> <span>1</span><span>,</span> right<span>,</span> temp<span>)</span>
        <span>merge</span><span>(</span>array<span>,</span> left<span>,</span> right<span>,</span> temp<span>)</span><span>;</span>
      <span>}</span>
      <span>return</span> array<span>;</span>
    <span>}</span>

    <span>function</span> <span>merge</span><span>(</span><span>array<span>,</span> left<span>,</span> right<span>,</span> temp</span><span>)</span> <span>{</span>
      <span>const</span> mid <span>=</span> Math<span>.</span><span>floor</span><span>(</span><span>(</span>left <span>+</span> right<span>)</span> <span>/</span> <span>2</span><span>)</span><span>;</span>
      <span>let</span> leftIndex <span>=</span> left<span>;</span>
      <span>let</span> rightIndex <span>=</span> mid <span>+</span> <span>1</span><span>;</span>
      <span>let</span> tempIndex <span>=</span> <span>0</span><span>;</span>
      <span>while</span> <span>(</span>leftIndex <span>&lt;=</span> mid <span>&amp;&amp;</span> rightIndex <span>&lt;=</span> right<span>)</span> <span>{</span>
        <span>if</span> <span>(</span>array<span>[</span>leftIndex<span>]</span> <span>&lt;</span> array<span>[</span>rightIndex<span>]</span><span>)</span> <span>{</span>
          temp<span>[</span>tempIndex<span>++</span><span>]</span> <span>=</span> array<span>[</span>leftIndex<span>++</span><span>]</span>
        <span>}</span> <span>else</span> <span>{</span>
          temp<span>[</span>tempIndex<span>++</span><span>]</span> <span>=</span> array<span>[</span>rightIndex<span>++</span><span>]</span>
        <span>}</span>
      <span>}</span>
      <span>while</span> <span>(</span>leftIndex <span>&lt;=</span> mid<span>)</span> <span>{</span>
        temp<span>[</span>tempIndex<span>++</span><span>]</span> <span>=</span> array<span>[</span>leftIndex<span>++</span><span>]</span>
      <span>}</span>
      <span>while</span> <span>(</span>rightIndex <span>&lt;=</span> right<span>)</span> <span>{</span>
        temp<span>[</span>tempIndex<span>++</span><span>]</span> <span>=</span> array<span>[</span>rightIndex<span>++</span><span>]</span>
      <span>}</span>
      tempIndex <span>=</span> <span>0</span><span>;</span>
      <span>for</span> <span>(</span><span>let</span> i <span>=</span> left<span>;</span> i <span>&lt;=</span> right<span>;</span> i<span>++</span><span>)</span> <span>{</span>
        array<span>[</span>i<span>]</span> <span>=</span> temp<span>[</span>tempIndex<span>++</span><span>]</span><span>;</span>
      <span>}</span>
    <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br></div></div><p>复杂度</p>
<ul>
<li>
<p>时间复杂度：<code>O(nlogn)</code></p>
</li>
<li>
<p>空间复杂度:<code>O(n)</code></p>
</li>
</ul>
<p>稳定性</p>
<ul>
<li>稳定</li>
</ul>
<h4 id="桶排序"> 桶排序</h4>
<ul>
<li>桶排序是一种排序的思想，其实现包括计数排序和基数排序两种，冒泡排序、选择排序、插入排序、归并排序、快速排序和堆排序都是基于比较的排序，而桶排序提出了一种新的思路，即基于数据状态的排序。</li>
<li>&quot;桶&quot;是一种容器，这个容器可以用多种数据结构实现，包括数组、队列或者栈</li>
</ul>
<h5 id="计数排序"> 计数排序</h5>
<p>概念</p>
<ul>
<li>计数排序不是基于比较的排序算法，其核心在于将输入的数据值转化为键存储在额外开辟的数组空间中。</li>
<li>作为一种线性时间复杂度的排序，计数排序要求输入的数据必须是有确定范围的整数</li>
</ul>
<p>思想</p>
<ul>
<li>找出待排序的数组中最大和最小的元素；</li>
<li>统计数组中每个值为i的元素出现的次数，存入数组C的第i项；</li>
<li>对所有的计数累加（从C中的第一个元素开始，每一项和前一项相加）；</li>
<li>反向填充目标数组：将每个元素i放在新数组的第C(i)项，每放一个元素就将C(i)减去1</li>
</ul>
<p>示例</p>
<p><img src="./../images/计数排序.gif" alt="" /></p>
<p>实现</p>
<div><pre><code><span>//从小到大计数排序</span>
<span>/**
* array 待排序数组
* maxValue 数组最大值
**/</span>
<span>function</span> <span>countingSort</span><span>(</span><span>array<span>,</span> maxValue</span><span>)</span> <span>{</span>
    <span>let</span> bucket <span>=</span> <span>newArray</span><span>(</span>maxValue <span>+</span> <span>1</span><span>)</span><span>,</span> 			<span>// 创建计数数组，保存数组计数，数据大小作为数组下标记录</span>
        sortedIndex <span>=</span> <span>0</span><span>,</span>							<span>// 待排序下标</span>
        arrLen <span>=</span> arr<span>.</span>length<span>,</span>
        bucketLen <span>=</span> maxValue <span>+</span> <span>1</span>					<span>// 空数组长度</span>
 
    <span>for</span><span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> arrLen<span>;</span> i<span>++</span><span>)</span> <span>{</span>				<span>// 遍历数组，统计数组每个数据出现次数</span>
        <span>if</span><span>(</span><span>!</span>bucket<span>[</span>array<span>[</span>i<span>]</span><span>]</span><span>)</span> <span>{</span>						<span>// 未出现的数据出现次数为0</span>
            bucket<span>[</span>array<span>[</span>i<span>]</span><span>]</span> <span>=</span> <span>0</span>
        <span>}</span>
        bucket<span>[</span>array<span>[</span>i<span>]</span><span>]</span><span>++</span>						   <span>//  统计数组每个数据出现次数	</span>
    <span>}</span>
 
    <span>for</span><span>(</span><span>let</span> j <span>=</span> <span>0</span><span>;</span> j <span>&lt;</span> bucketLen<span>;</span> j<span>++</span><span>)</span> <span>{</span>			<span>// 遍历计数数组</span>
        <span>while</span><span>(</span>bucket<span>[</span>j<span>]</span> <span>></span> <span>0</span><span>)</span> <span>{</span>
            array<span>[</span>sortedIndex<span>++</span><span>]</span> <span>=</span> j				<span>//	将数组填充回原数组</span>
            bucket<span>[</span>j<span>]</span><span>--</span>								<span>//	计数值减1</span>
        <span>}</span>
    <span>}</span>
 
    <span>return</span> array
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br></div></div><p>复杂度</p>
<ul>
<li>当输入的元素是 n 个 0到 k 之间的整数时，时间复杂度是O(n+k)，空间复杂度也是O(n+k)，其排序速度快于任何比较排序算法。</li>
<li>当k不是很大并且序列比较集中时，计数排序是一个很有效的排序算法</li>
</ul>
<p>稳定性</p>
<ul>
<li>稳定</li>
</ul>
<h5 id="桶排序-2"> 桶排序</h5>
<p>概念</p>
<ul>
<li>
<p>桶排序是计数排序的升级版。</p>
</li>
<li>
<p>它利用了函数的映射关系，高效与否的关键就在于这个映射函数的确定。</p>
</li>
<li>
<p>为了使桶排序更加高效，我们需要做到这两点：</p>
<ul>
<li>在额外空间充足的情况下，尽量增大桶的数量</li>
<li>使用的映射函数能够将输入的 N 个数据均匀的分配到 K 个桶中</li>
</ul>
</li>
<li>
<p>同时，对于桶中元素的排序，选择何种比较排序算法对于性能的影响至关重要</p>
</li>
<li>
<p>当输入的数据可以均匀的分配到每一个桶中时，桶排序最快</p>
</li>
<li>
<p>当输入的数据被分配到了同一个桶中时，桶排序最慢</p>
</li>
</ul>
<p>思想</p>
<ul>
<li>设置一个定量的数组当作空桶；</li>
<li>遍历输入数据，并且把数据一个一个放到对应的桶里去；</li>
<li>对每个不是空的桶进行排序；</li>
<li>从不是空的桶里把排好序的数据拼接起来。</li>
</ul>
<p>示例</p>
<ul>
<li>元素分布在桶中</li>
<li><img src="./../images/桶排序.png" alt="" /></li>
<li>然后，元素在每个桶中排序：</li>
<li><img src="./../images/桶排序2.png" alt="" /></li>
</ul>
<p>实现</p>
<div><pre><code><span>function</span> <span>bucketSort</span><span>(</span><span>arr<span>,</span> bucketSize</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>arr<span>.</span>length <span>===</span> <span>0</span><span>)</span> <span>{</span>
      <span>return</span> arr<span>;</span>
    <span>}</span>

    <span>var</span> i<span>;</span>
    <span>var</span> minValue <span>=</span> arr<span>[</span><span>0</span><span>]</span><span>;</span>
    <span>var</span> maxValue <span>=</span> arr<span>[</span><span>0</span><span>]</span><span>;</span>
    <span>for</span> <span>(</span>i <span>=</span> <span>1</span><span>;</span> i <span>&lt;</span> arr<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
      <span>if</span> <span>(</span>arr<span>[</span>i<span>]</span> <span>&lt;</span> minValue<span>)</span> <span>{</span>
          minValue <span>=</span> arr<span>[</span>i<span>]</span><span>;</span>                <span>// 输入数据的最小值</span>
      <span>}</span> <span>else</span> <span>if</span> <span>(</span>arr<span>[</span>i<span>]</span> <span>></span> maxValue<span>)</span> <span>{</span>
          maxValue <span>=</span> arr<span>[</span>i<span>]</span><span>;</span>                <span>// 输入数据的最大值</span>
      <span>}</span>
    <span>}</span>

    <span>//桶的初始化</span>
    <span>var</span> <span>DEFAULT_BUCKET_SIZE</span> <span>=</span> <span>5</span><span>;</span>            <span>// 设置桶的默认数量为5</span>
    bucketSize <span>=</span> bucketSize <span>||</span> <span>DEFAULT_BUCKET_SIZE</span><span>;</span>
    <span>var</span> bucketCount <span>=</span> Math<span>.</span><span>floor</span><span>(</span><span>(</span>maxValue <span>-</span> minValue<span>)</span> <span>/</span> bucketSize<span>)</span> <span>+</span> <span>1</span><span>;</span>  
    <span>var</span> buckets <span>=</span> <span>new</span> <span>Array</span><span>(</span>bucketCount<span>)</span><span>;</span>
    <span>for</span> <span>(</span>i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> buckets<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
        buckets<span>[</span>i<span>]</span> <span>=</span> <span>[</span><span>]</span><span>;</span>
    <span>}</span>

    <span>//利用映射函数将数据分配到各个桶中</span>
    <span>for</span> <span>(</span>i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> arr<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
        buckets<span>[</span>Math<span>.</span><span>floor</span><span>(</span><span>(</span>arr<span>[</span>i<span>]</span> <span>-</span> minValue<span>)</span> <span>/</span> bucketSize<span>)</span><span>]</span><span>.</span><span>push</span><span>(</span>arr<span>[</span>i<span>]</span><span>)</span><span>;</span>
    <span>}</span>

    arr<span>.</span>length <span>=</span> <span>0</span><span>;</span>
    <span>for</span> <span>(</span>i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> buckets<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
        <span>insertionSort</span><span>(</span>buckets<span>[</span>i<span>]</span><span>)</span><span>;</span>                      <span>// 对每个桶进行排序，这里使用了插入排序</span>
        <span>for</span> <span>(</span><span>var</span> j <span>=</span> <span>0</span><span>;</span> j <span>&lt;</span> buckets<span>[</span>i<span>]</span><span>.</span>length<span>;</span> j<span>++</span><span>)</span> <span>{</span>
            arr<span>.</span><span>push</span><span>(</span>buckets<span>[</span>i<span>]</span><span>[</span>j<span>]</span><span>)</span><span>;</span>                      
        <span>}</span>
    <span>}</span>

    <span>return</span> arr<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br></div></div><p>复杂度</p>
<ul>
<li>时间复杂度：遍历数组求最大值最小值为O(n)，遍历数组放入&quot;桶&quot;中复杂度为O(n)，遍历桶取出每个值的复杂度为O(n)，最终的时间复杂度为O(3n)，也就是O(n)</li>
<li>空间复杂度：额外的空间取决于元素的取值范围，总的来说为O(n)</li>
</ul>
<p>稳定性</p>
<ul>
<li>桶排序是否稳定取决于&quot;桶&quot;用什么数据结构实现</li>
<li>如果是队列，那么可以保证相同的元素&quot;取出去&quot;后的相对位置与&quot;放进来&quot;之前是相同的，即排序是稳定的</li>
<li>而如果用栈来实现&quot;桶&quot;，则排序一定是不稳定的，因为桶排序可以做到稳定，所以桶排序是稳定的排序算法</li>
</ul>
<h5 id="基数排序"> 基数排序</h5>
<p>概念</p>
<ul>
<li>
<p>基数排序是一种多关键字排序</p>
</li>
<li>
<p>多关键字排序：</p>
<ul>
<li>一般情况下，假设有n个记录的序列：
<ul>
<li>{R1,R2, ... , Rn}</li>
<li>且每个记录Ri含有d个关键字,且 d 个关键字也有序,最大关键字称为主关键字,最小关键字称为次关键字</li>
</ul>
</li>
</ul>
</li>
<li>
<p>基数排序有两种方法：</p>
<ul>
<li>
<p>MSD 从高位关键字开始进行排序</p>
</li>
<li>
<p>LSD 从低位关键字开始进行排序</p>
<ul>
<li>LSD 基数排序是按照低位先排序，然后收集；再按照高位排序，然后再收集；依次类推，直到最高位</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>示例</p>
<ul>
<li>假设一组数组中每个数据要求按照个位和十位两个关键字排序</li>
<li>LSD基数排序动画演示</li>
<li><img src="./../images/radixSort.gif" alt="" /></li>
</ul>
<p>实现</p>
<div><pre><code><span>//LSD Radix Sort</span>
<span>let</span> counter <span>=</span> <span>[</span><span>]</span><span>;</span>
<span>function</span> <span>radixSort</span><span>(</span><span>arr<span>,</span> maxDigit</span><span>)</span> <span>{</span>
    <span>let</span> mod <span>=</span> <span>10</span><span>;</span>
    <span>let</span> dev <span>=</span> <span>1</span><span>;</span>
    <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> maxDigit<span>;</span> i<span>++</span><span>,</span> dev <span>*=</span> <span>10</span><span>,</span> mod <span>*=</span> <span>10</span><span>)</span> <span>{</span>
        <span>for</span><span>(</span><span>let</span> j <span>=</span> <span>0</span><span>;</span> j <span>&lt;</span> arr<span>.</span>length<span>;</span> j<span>++</span><span>)</span> <span>{</span>
            <span>let</span> bucket <span>=</span> <span>parseInt</span><span>(</span><span>(</span>arr<span>[</span>j<span>]</span> <span>%</span> mod<span>)</span> <span>/</span> dev<span>)</span><span>;</span>
            <span>if</span><span>(</span>counter<span>[</span>bucket<span>]</span><span>==</span><span>null</span><span>)</span> <span>{</span>
                counter<span>[</span>bucket<span>]</span> <span>=</span> <span>[</span><span>]</span><span>;</span>
            <span>}</span>
            counter<span>[</span>bucket<span>]</span><span>.</span><span>push</span><span>(</span>arr<span>[</span>j<span>]</span><span>)</span><span>;</span>
        <span>}</span>
        <span>let</span> pos <span>=</span> <span>0</span><span>;</span>
        <span>for</span><span>(</span><span>let</span> j <span>=</span> <span>0</span><span>;</span> j <span>&lt;</span> counter<span>.</span>length<span>;</span> j<span>++</span><span>)</span> <span>{</span>
            <span>let</span> value <span>=</span> <span>null</span><span>;</span>
            <span>if</span><span>(</span>counter<span>[</span>j<span>]</span><span>!=</span><span>null</span><span>)</span> <span>{</span>
                <span>while</span> <span>(</span><span>(</span>value <span>=</span> counter<span>[</span>j<span>]</span><span>.</span><span>shift</span><span>(</span><span>)</span><span>)</span> <span>!=</span> <span>null</span><span>)</span> <span>{</span>
                      arr<span>[</span>pos<span>++</span><span>]</span> <span>=</span> value<span>;</span>
                <span>}</span>
          <span>}</span>
        <span>}</span>
    <span>}</span>
    <span>return</span> arr<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br></div></div><p>复杂度</p>
<ul>
<li>时间复杂度 O(d<em>2n) ：</em>
<ul>
<li>每一次关键字的桶分配都需要O(n)的时间复杂度，而且分配之后得到新的关键字序列又需要O(n)的时间复杂度。假如待排数据可以分为d个关键字，则基数排序的时间复杂度将是O(d*2n)</li>
</ul>
</li>
<li>空间复杂度 O(n+k)：
<ul>
<li>其中k为桶的数量。一般来说n&gt;&gt;k，因此额外空间需要大概n个左右</li>
</ul>
</li>
</ul>
<p>稳定性</p>
<ul>
<li>基数排序基于分别排序，分别收集，所以是稳定的</li>
</ul>
<h5 id="基数排序-vs-计数排序-vs-桶排序"> 基数排序 vs 计数排序 vs 桶排序</h5>
<ul>
<li>基数排序：根据键值的每位数字来分配桶；</li>
<li>计数排序：每个桶只存储单一键值；</li>
<li>桶排序：每个桶存储一定范围的数值；</li>
</ul>
<h4 id="排序比较"> 排序比较</h4>
<ul>
<li>复杂度和稳定性比较
<img src="./../images/算法比较.png" alt="" /></li>
</ul>
<h3 id="js-排序-api"> JS 排序 API</h3>
<h4 id="sort"> sort()</h4>
<ul>
<li>原理分析
<ul>
<li>在v8引擎中，对sort方法提供了2种排序算法：插入排序及快排序</li>
</ul>
</li>
</ul>
<p>参考</p>
<ul>
<li>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/sort" target="_blank" rel="noopener noreferrer">MDN</a></p>
</li>
<li>
<p><a href="https://zhuanlan.zhihu.com/p/33626637" target="_blank" rel="noopener noreferrer">sort原理</a></p>
</li>
<li></li>
</ul>
<hr>
<h3 id="递归"> 递归</h3>
<h3 id="回溯算法"> 回溯算法</h3>
<h3 id="动态规划"> 动态规划</h3>
<h3 id="贪心算法"> 贪心算法</h3>
<h3 id="参考资料"> 参考资料</h3>
<p><a href="http://data.biancheng.net/view/72.html" target="_blank" rel="noopener noreferrer">c语言实现数据结构和算法</a></p>
<p><a href="https://juejin.cn/post/6844903919722692621#heading-42" target="_blank" rel="noopener noreferrer">前端该如何准备数据结构和算法？</a></p>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-26T11:10:03.000Z</published>
  </entry>
  <entry>
    <title type="html">基础</title>
    <id>https://sewar-x.github.io/axios/</id>
    <link href="https://sewar-x.github.io/axios/"/>
    <updated>2021-06-25T19:47:29.000Z</updated>
    <content type="html"><![CDATA[<h2 id="基础"> 基础</h2>
<h3 id="概念和api"> 概念和API</h3>
<ul>
<li>概念：Axios 是一个基于 promise 的网络请求库，可以用于浏览器和 node.js。它是 <em><a href="https://www.lullabot.com/articles/what-is-an-isomorphic-application" target="_blank" rel="noopener noreferrer">isomorphic</a></em> 的(即同一套代码可以运行在浏览器和node.js中)。在服务端它使用原生 node.js <code>http</code> 模块, 而在客户端 (浏览端) 则使用 XMLHttpRequests。</li>
<li>特性:
<ul>
<li>从浏览器创建 <a href="https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequest" target="_blank" rel="noopener noreferrer">XMLHttpRequests</a></li>
<li>从 node.js 创建 <a href="http://nodejs.org/api/http.html" target="_blank" rel="noopener noreferrer">http</a> 请求</li>
<li>支持 <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise" target="_blank" rel="noopener noreferrer">Promise</a> API</li>
<li>拦截请求和响应</li>
<li>转换请求和响应数据</li>
<li>取消请求</li>
<li>自动转换JSON数据</li>
<li>客户端支持防御<a href="http://en.wikipedia.org/wiki/Cross-site_request_forgery" target="_blank" rel="noopener noreferrer">XSRF</a>：添加 <code>xsrf</code> 头，默认为 cookie的编码值： <code>{ 'X-XSRF-TOKEN':decodeURIComponent(document.cookie) }</code></li>
</ul>
</li>
<li>常用 API：
<ul>
<li><code>axios.request(config)</code></li>
<li><code>axios.get(url[, config])</code></li>
<li><code>axios.delete(url[, config])</code></li>
<li><code>axios.head(url[, config])</code></li>
<li><code>axios.options(url[, config])</code></li>
<li><code>axios.post(url[, data[, config]])</code></li>
<li><code>axios.put(url[, data[, config]])</code></li>
<li><code>axios.patch(url[, data[, config]])</code></li>
<li>并发请求：<code>Promise.all()</code></li>
<li>创建实例: <code>axios.create([config])</code></li>
</ul>
</li>
</ul>
<h3 id="执行流程"> 执行流程</h3>
<p><img src="./../flow-graph/axios流程.png" alt="" /></p>
<ul>
<li>axios 执行流程：
<ol>
<li>在实例化 axios 时，通过创建 axios 实例添加实例配置或修改默认配置</li>
<li>使用 <code>axios.interceptors.request.use</code> 往 axios 实例中添加请求/响应拦截器</li>
<li>请求方法调用 <code>request()</code> 方法 (axios的所有请求方法都是该方法的封装)</li>
<li>request() 方法中循环遍历 request interceptors 和 response interceptors，分别将 request 和 respose 拦截器实例加入 chain 队列头部和尾部，生成 [请求拦截器, dispatchRequest, 响应拦截器] 堆栈(使用了promise的链式调用)</li>
<li>循环遍历执行堆栈: 传入 config promise 对象 -&gt; 执行所有 request handler -&gt; dispatchRequest -&gt; Adapter 对象 -&gt; 根据环境调用 http/xhr 对象 -&gt; settle() 方法验证结果 -&gt; 执行所有响应拦截器对象 -&gt; 前端/服务器。</li>
<li>前端/服务器使用axios请求方法发起请求 (axios.get, axios.post ...)</li>
</ol>
</li>
</ul>
<h3 id="axios-实例"> axios 实例</h3>
<p>axios 实例使用方式:</p>
<ul>
<li>第1种使用方式：<code>axios(option)</code></li>
</ul>
<div><pre><code><span>axios</span><span>(</span><span>{</span>
  url<span>,</span>
  method<span>,</span>
  headers<span>,</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><ul>
<li>第2种使用方式：<code>axios(url[, option])</code></li>
</ul>
<div><pre><code><span>axios</span><span>(</span>url<span>,</span> <span>{</span>
  method<span>,</span>
  headers<span>,</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br></div></div><ul>
<li>第3种使用方式（对于get、delete等方法）：<code>axios[method</code>](url[, option])</li>
</ul>
<div><pre><code>axios<span>.</span><span>get</span><span>(</span>url<span>,</span> <span>{</span>
  headers<span>,</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><ul>
<li>第4种使用方式（对于post、put等方法）：<code>axios[method</code>](url[, data[, option]])</li>
</ul>
<div><pre><code>axios<span>.</span><span>post</span><span>(</span>url<span>,</span> data<span>,</span> <span>{</span>
  headers<span>,</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><ul>
<li>第5种使用方式：<code>axios.request(option)</code></li>
</ul>
<div><pre><code>axios<span>.</span><span>request</span><span>(</span><span>{</span>
  url<span>,</span>
  method<span>,</span>
  headers<span>,</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><h4 id="axios-默认实例"> axios 默认实例</h4>
<ul>
<li>
<p>导入默认 axios 实例：<code>import axios from 'axios'</code></p>
</li>
<li>
<p>axios 默认导出一个实例，修改了axios的默认配置，会影响所有的请求。</p>
<ul>
<li>
<p>当在 a.js 文件中导入 axios 并修改默认配置后</p>
<div><pre><code><span>// a.js</span>
<span>import</span> axios <span>from</span> <span>'axios'</span>
axios<span>.</span>defaults<span>.</span>baseURL <span>=</span> <span>'https://api.example.com'</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
<li>
<p>如果 b.js 中依赖 a.js 文件，在 b.js 中导入 axios，则 b.js 中的 axios 实例被 a.js 中影响</p>
<div><pre><code><span>//b.js</span>
<span>import</span> axios <span>from</span> <span>'axios'</span>
console<span>.</span><span>log</span><span>(</span>axios<span>.</span>defaults<span>.</span>baseURL<span>)</span> <span>//https://api.example.com</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
<li>
<p>解决方案： 每一个独立导入 axios 的文件中都创建新的 axios 实例，再修改配置。</p>
</li>
</ul>
</li>
</ul>
<p><strong>源码分析：</strong></p>
<ul>
<li>
<p>在入口文件中：使用默认配置创建一个 axios 实例并导出。定义在 <a href="https://github.com/axios/axios/blob/master/lib/axios.js" target="_blank" rel="noopener noreferrer">lib/axios.js</a></p>
<p><img src="./../images/默认实例.png" alt="" /></p>
</li>
<li>
<p>axios 实例对象定义默认配置和拦截器：定义在 <a href="https://github.com/axios/axios/blob/master/lib/core/Axios.js" target="_blank" rel="noopener noreferrer">lib/core/Axios.js</a></p>
</li>
</ul>
<p><img src="./../images/实例对象.png" alt="" /></p>
<h4 id="创建实例"> 创建实例</h4>
<ul>
<li>
<p>API ： <code>axios.create([config])</code></p>
<div><pre><code><span>const</span> instance <span>=</span> axios<span>.</span><span>create</span><span>(</span><span>{</span>
  baseURL<span>:</span> <span>'https://some-domain.com/api/'</span><span>,</span>
  timeout<span>:</span> <span>1000</span><span>,</span>
  headers<span>:</span> <span>{</span><span>'X-Custom-Header'</span><span>:</span> <span>'foobar'</span><span>}</span>
<span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div></li>
</ul>
<p><strong>源码分析</strong></p>
<ul>
<li>
<p><code>axios.create([config])</code> 通过传入实例参数，合并默认配置和传入配置项，使用原型式继承创建新的实例。定义在 <a href="https://github.com/axios/axios/blob/master/lib/axios.js" target="_blank" rel="noopener noreferrer">lib/axios.js</a></p>
<p><img src="./../images/create.png" alt="" /></p>
</li>
</ul>
<h3 id="配置"> 配置</h3>
<p>axios 配置分为三类：</p>
<ul>
<li>axios 默认配置：创建默认 axios 实例的默认配置；</li>
<li>axios 用户自定义实例配置： 通过 <code>axios.create(config)</code> 时传入的配置；</li>
<li>请求的 <code>config</code> 配置： 在实例使用时传入的配置项。</li>
</ul>
<h4 id="默认配置"> 默认配置</h4>
<ul>
<li>
<p>全局配置 <code>axios.defaults</code></p>
</li>
<li>
<p>自定义实例配置：</p>
<div><pre><code><span>// 创建实例时配置默认值</span>
<span>const</span> instance <span>=</span> axios<span>.</span><span>create</span><span>(</span><span>{</span>
  baseURL<span>:</span> <span>'https://api.example.com'</span>
<span>}</span><span>)</span><span>;</span>

<span>// 创建实例后修改默认值</span>
instance<span>.</span>defaults
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
<li>
<p>请求配置选项。只有 <code>url</code> 是必需的。如果没有指定 <code>method</code>，请求将默认使用 <code>GET</code> 方法。</p>
<div><pre><code><span>{</span>
  <span>// `url` 是用于请求的服务器 URL</span>
  url<span>:</span> <span>'/user'</span><span>,</span>

  <span>// `method` 是创建请求时使用的方法</span>
  method<span>:</span> <span>'get'</span><span>,</span> <span>// 默认值</span>

  <span>// `baseURL` 将自动加在 `url` 前面，除非 `url` 是一个绝对 URL。</span>
  <span>// 它可以通过设置一个 `baseURL` 便于为 axios 实例的方法传递相对 URL</span>
  baseURL<span>:</span> <span>'https://some-domain.com/api/'</span><span>,</span>

  <span>// `transformRequest` 允许在向服务器发送前，修改请求数据</span>
  <span>// 它只能用与 'PUT', 'POST' 和 'PATCH' 这几个请求方法</span>
  <span>// 数组中最后一个函数必须返回一个字符串， 一个Buffer实例，ArrayBuffer，FormData，或 Stream</span>
  <span>// 你可以修改请求头。</span>
  transformRequest<span>:</span> <span>[</span><span>function</span> <span>(</span><span>data<span>,</span> headers</span><span>)</span> <span>{</span>
    <span>// 对发送的 data 进行任意转换处理</span>

    <span>return</span> data<span>;</span>
  <span>}</span><span>]</span><span>,</span>

  <span>// `transformResponse` 在传递给 then/catch 前，允许修改响应数据</span>
  transformResponse<span>:</span> <span>[</span><span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
    <span>// 对接收的 data 进行任意转换处理</span>

    <span>return</span> data<span>;</span>
  <span>}</span><span>]</span><span>,</span>

  <span>// 自定义请求头</span>
  headers<span>:</span> <span>{</span><span>'X-Requested-With'</span><span>:</span> <span>'XMLHttpRequest'</span><span>}</span><span>,</span>

  <span>// `params` 是与请求一起发送的 URL 参数</span>
  <span>// 必须是一个简单对象或 URLSearchParams 对象</span>
  params<span>:</span> <span>{</span>
    <span>ID</span><span>:</span> <span>12345</span>
  <span>}</span><span>,</span>

  <span>// `paramsSerializer`是可选方法，主要用于序列化`params`</span>
  <span>// (e.g. https://www.npmjs.com/package/qs, http://api.jquery.com/jquery.param/)</span>
  <span>paramsSerializer</span><span>:</span> <span>function</span> <span>(</span><span>params</span><span>)</span> <span>{</span>
    <span>return</span> Qs<span>.</span><span>stringify</span><span>(</span>params<span>,</span> <span>{</span>arrayFormat<span>:</span> <span>'brackets'</span><span>}</span><span>)</span>
  <span>}</span><span>,</span>

  <span>// `data` 是作为请求体被发送的数据</span>
  <span>// 仅适用 'PUT', 'POST', 'DELETE 和 'PATCH' 请求方法</span>
  <span>// 在没有设置 `transformRequest` 时，则必须是以下类型之一:</span>
  <span>// - string, plain object, ArrayBuffer, ArrayBufferView, URLSearchParams</span>
  <span>// - 浏览器专属: FormData, File, Blob</span>
  <span>// - Node 专属: Stream, Buffer</span>
  data<span>:</span> <span>{</span>
    firstName<span>:</span> <span>'Fred'</span>
  <span>}</span><span>,</span>
  
  <span>// 发送请求体数据的可选语法</span>
  <span>// 请求方式 post</span>
  <span>// 只有 value 会被发送，key 则不会</span>
  data<span>:</span> <span>'Country=Brasil&amp;City=Belo Horizonte'</span><span>,</span>

  <span>// `timeout` 指定请求超时的毫秒数。</span>
  <span>// 如果请求时间超过 `timeout` 的值，则请求会被中断</span>
  timeout<span>:</span> <span>1000</span><span>,</span> <span>// 默认值是 `0` (永不超时)</span>

  <span>// `withCredentials` 表示跨域请求时是否需要使用凭证</span>
  withCredentials<span>:</span> <span>false</span><span>,</span> <span>// default</span>

  <span>// `adapter` 允许自定义处理请求，这使测试更加容易。</span>
  <span>// 返回一个 promise 并提供一个有效的响应 （参见 lib/adapters/README.md）。</span>
  <span>adapter</span><span>:</span> <span>function</span> <span>(</span><span>config</span><span>)</span> <span>{</span>
    <span>/* ... */</span>
  <span>}</span><span>,</span>

  <span>// `auth` HTTP Basic Auth</span>
  auth<span>:</span> <span>{</span>
    username<span>:</span> <span>'janedoe'</span><span>,</span>
    password<span>:</span> <span>'s00pers3cret'</span>
  <span>}</span><span>,</span>

  <span>// `responseType` 表示浏览器将要响应的数据类型</span>
  <span>// 选项包括: 'arraybuffer', 'document', 'json', 'text', 'stream'</span>
  <span>// 浏览器专属：'blob'</span>
  responseType<span>:</span> <span>'json'</span><span>,</span> <span>// 默认值</span>

  <span>// `responseEncoding` 表示用于解码响应的编码 (Node.js 专属)</span>
  <span>// 注意：忽略 `responseType` 的值为 'stream'，或者是客户端请求</span>
  <span>// Note: Ignored for `responseType` of 'stream' or client-side requests</span>
  responseEncoding<span>:</span> <span>'utf8'</span><span>,</span> <span>// 默认值</span>

  <span>// `xsrfCookieName` 是 xsrf token 的值，被用作 cookie 的名称</span>
  xsrfCookieName<span>:</span> <span>'XSRF-TOKEN'</span><span>,</span> <span>// 默认值</span>

  <span>// `xsrfHeaderName` 是带有 xsrf token 值的http 请求头名称</span>
  xsrfHeaderName<span>:</span> <span>'X-XSRF-TOKEN'</span><span>,</span> <span>// 默认值</span>

  <span>// `onUploadProgress` 允许为上传处理进度事件</span>
  <span>// 浏览器专属</span>
  <span>onUploadProgress</span><span>:</span> <span>function</span> <span>(</span><span>progressEvent</span><span>)</span> <span>{</span>
    <span>// 处理原生进度事件</span>
  <span>}</span><span>,</span>

  <span>// `onDownloadProgress` 允许为下载处理进度事件</span>
  <span>// 浏览器专属</span>
  <span>onDownloadProgress</span><span>:</span> <span>function</span> <span>(</span><span>progressEvent</span><span>)</span> <span>{</span>
    <span>// 处理原生进度事件</span>
  <span>}</span><span>,</span>

  <span>// `maxContentLength` 定义了node.js中允许的HTTP响应内容的最大字节数</span>
  maxContentLength<span>:</span> <span>2000</span><span>,</span>

  <span>// `maxBodyLength`（仅Node）定义允许的http请求内容的最大字节数</span>
  maxBodyLength<span>:</span> <span>2000</span><span>,</span>

  <span>// `validateStatus` 定义了对于给定的 HTTP状态码是 resolve 还是 reject promise。</span>
  <span>// 如果 `validateStatus` 返回 `true` (或者设置为 `null` 或 `undefined`)，</span>
  <span>// 则promise 将会 resolved，否则是 rejected。</span>
  <span>validateStatus</span><span>:</span> <span>function</span> <span>(</span><span>status</span><span>)</span> <span>{</span>
    <span>return</span> status <span>>=</span> <span>200</span> <span>&amp;&amp;</span> status <span>&lt;</span> <span>300</span><span>;</span> <span>// 默认值</span>
  <span>}</span><span>,</span>

  <span>// `maxRedirects` 定义了在node.js中要遵循的最大重定向数。</span>
  <span>// 如果设置为0，则不会进行重定向</span>
  maxRedirects<span>:</span> <span>5</span><span>,</span> <span>// 默认值</span>

  <span>// `socketPath` 定义了在node.js中使用的UNIX套接字。</span>
  <span>// e.g. '/var/run/docker.sock' 发送请求到 docker 守护进程。</span>
  <span>// 只能指定 `socketPath` 或 `proxy` 。</span>
  <span>// 若都指定，这使用 `socketPath` 。</span>
  socketPath<span>:</span> <span>null</span><span>,</span> <span>// default</span>

  <span>// `httpAgent` and `httpsAgent` define a custom agent to be used when performing http</span>
  <span>// and https requests, respectively, in node.js. This allows options to be added like</span>
  <span>// `keepAlive` that are not enabled by default.</span>
  httpAgent<span>:</span> <span>new</span> <span>http<span>.</span>Agent</span><span>(</span><span>{</span> keepAlive<span>:</span> <span>true</span> <span>}</span><span>)</span><span>,</span>
  httpsAgent<span>:</span> <span>new</span> <span>https<span>.</span>Agent</span><span>(</span><span>{</span> keepAlive<span>:</span> <span>true</span> <span>}</span><span>)</span><span>,</span>

  <span>// `proxy` 定义了代理服务器的主机名，端口和协议。</span>
  <span>// 您可以使用常规的`http_proxy` 和 `https_proxy` 环境变量。</span>
  <span>// 使用 `false` 可以禁用代理功能，同时环境变量也会被忽略。</span>
  <span>// `auth`表示应使用HTTP Basic auth连接到代理，并且提供凭据。</span>
  <span>// 这将设置一个 `Proxy-Authorization` 请求头，它会覆盖 `headers` 中已存在的自定义 `Proxy-Authorization` 请求头。</span>
  <span>// 如果代理服务器使用 HTTPS，则必须设置 protocol 为`https`</span>
  proxy<span>:</span> <span>{</span>
    protocol<span>:</span> <span>'https'</span><span>,</span>
    host<span>:</span> <span>'127.0.0.1'</span><span>,</span>
    port<span>:</span> <span>9000</span><span>,</span>
    auth<span>:</span> <span>{</span>
      username<span>:</span> <span>'mikeymike'</span><span>,</span>
      password<span>:</span> <span>'rapunz3l'</span>
    <span>}</span>
  <span>}</span><span>,</span>

  <span>// see https://axios-http.com/docs/cancellation</span>
  cancelToken<span>:</span> <span>new</span> <span>CancelToken</span><span>(</span><span>function</span> <span>(</span><span>cancel</span><span>)</span> <span>{</span>
  <span>}</span><span>)</span><span>,</span>

  <span>// `decompress` indicates whether or not the response body should be decompressed </span>
  <span>// automatically. If set to `true` will also remove the 'content-encoding' header </span>
  <span>// from the responses objects of all decompressed responses</span>
  <span>// - Node only (XHR cannot turn off decompression)</span>
  decompress<span>:</span> <span>true</span> <span>// 默认值</span>

<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br><span>93</span><br><span>94</span><br><span>95</span><br><span>96</span><br><span>97</span><br><span>98</span><br><span>99</span><br><span>100</span><br><span>101</span><br><span>102</span><br><span>103</span><br><span>104</span><br><span>105</span><br><span>106</span><br><span>107</span><br><span>108</span><br><span>109</span><br><span>110</span><br><span>111</span><br><span>112</span><br><span>113</span><br><span>114</span><br><span>115</span><br><span>116</span><br><span>117</span><br><span>118</span><br><span>119</span><br><span>120</span><br><span>121</span><br><span>122</span><br><span>123</span><br><span>124</span><br><span>125</span><br><span>126</span><br><span>127</span><br><span>128</span><br><span>129</span><br><span>130</span><br><span>131</span><br><span>132</span><br><span>133</span><br><span>134</span><br><span>135</span><br><span>136</span><br><span>137</span><br><span>138</span><br><span>139</span><br><span>140</span><br><span>141</span><br><span>142</span><br><span>143</span><br><span>144</span><br><span>145</span><br><span>146</span><br><span>147</span><br><span>148</span><br><span>149</span><br><span>150</span><br><span>151</span><br><span>152</span><br><span>153</span><br><span>154</span><br><span>155</span><br><span>156</span><br><span>157</span><br><span>158</span><br><span>159</span><br><span>160</span><br><span>161</span><br></div></div></li>
</ul>
<p><strong>源码分析：</strong></p>
<ul>
<li>
<p>默认配置选项在创建 axios 默认实例时传入，定义在 <a href="https://github.com/axios/axios/blob/master/lib/defaults.js" target="_blank" rel="noopener noreferrer">lib/defaults.js</a></p>
<div><pre><code><span>var</span> defaults <span>=</span> <span>{</span>
  adapter<span>:</span> <span>getDefaultAdapter</span><span>(</span><span>)</span><span>,</span>

  transformRequest<span>:</span> <span>[</span><span>function</span> <span>transformRequest</span><span>(</span><span>data<span>,</span> headers</span><span>)</span> <span>{</span>
    <span>normalizeHeaderName</span><span>(</span>headers<span>,</span> <span>'Accept'</span><span>)</span><span>;</span>
    <span>normalizeHeaderName</span><span>(</span>headers<span>,</span> <span>'Content-Type'</span><span>)</span><span>;</span>
    <span>if</span> <span>(</span>utils<span>.</span><span>isFormData</span><span>(</span>data<span>)</span> <span>||</span>
      utils<span>.</span><span>isArrayBuffer</span><span>(</span>data<span>)</span> <span>||</span>
      utils<span>.</span><span>isBuffer</span><span>(</span>data<span>)</span> <span>||</span>
      utils<span>.</span><span>isStream</span><span>(</span>data<span>)</span> <span>||</span>
      utils<span>.</span><span>isFile</span><span>(</span>data<span>)</span> <span>||</span>
      utils<span>.</span><span>isBlob</span><span>(</span>data<span>)</span>
    <span>)</span> <span>{</span>
      <span>return</span> data<span>;</span>
    <span>}</span>
    <span>if</span> <span>(</span>utils<span>.</span><span>isArrayBufferView</span><span>(</span>data<span>)</span><span>)</span> <span>{</span>
      <span>return</span> data<span>.</span>buffer<span>;</span>
    <span>}</span>
    <span>if</span> <span>(</span>utils<span>.</span><span>isURLSearchParams</span><span>(</span>data<span>)</span><span>)</span> <span>{</span>
      <span>setContentTypeIfUnset</span><span>(</span>headers<span>,</span> <span>'application/x-www-form-urlencoded;charset=utf-8'</span><span>)</span><span>;</span>
      <span>return</span> data<span>.</span><span>toString</span><span>(</span><span>)</span><span>;</span>
    <span>}</span>
    <span>if</span> <span>(</span>utils<span>.</span><span>isObject</span><span>(</span>data<span>)</span><span>)</span> <span>{</span>
      <span>setContentTypeIfUnset</span><span>(</span>headers<span>,</span> <span>'application/json;charset=utf-8'</span><span>)</span><span>;</span>
      <span>return</span> <span>JSON</span><span>.</span><span>stringify</span><span>(</span>data<span>)</span><span>;</span>
    <span>}</span>
    <span>return</span> data<span>;</span>
  <span>}</span><span>]</span><span>,</span>

  transformResponse<span>:</span> <span>[</span><span>function</span> <span>transformResponse</span><span>(</span><span>data</span><span>)</span> <span>{</span>
    <span>/*eslint no-param-reassign:0*/</span>
    <span>if</span> <span>(</span><span>typeof</span> data <span>===</span> <span>'string'</span><span>)</span> <span>{</span>
      <span>try</span> <span>{</span>
        data <span>=</span> <span>JSON</span><span>.</span><span>parse</span><span>(</span>data<span>)</span><span>;</span>
      <span>}</span> <span>catch</span> <span>(</span>e<span>)</span> <span>{</span> <span>/* Ignore */</span> <span>}</span>
    <span>}</span>
    <span>return</span> data<span>;</span>
  <span>}</span><span>]</span><span>,</span>

  <span>/**
   * A timeout in milliseconds to abort a request. If set to 0 (default) a
   * timeout is not created.
   */</span>
  timeout<span>:</span> <span>0</span><span>,</span>

  xsrfCookieName<span>:</span> <span>'XSRF-TOKEN'</span><span>,</span>
  xsrfHeaderName<span>:</span> <span>'X-XSRF-TOKEN'</span><span>,</span>

  maxContentLength<span>:</span> <span>-</span><span>1</span><span>,</span>
  maxBodyLength<span>:</span> <span>-</span><span>1</span><span>,</span>

  <span>validateStatus</span><span>:</span> <span>function</span> <span>validateStatus</span><span>(</span><span>status</span><span>)</span> <span>{</span>
    <span>return</span> status <span>>=</span> <span>200</span> <span>&amp;&amp;</span> status <span>&lt;</span> <span>300</span><span>;</span>
  <span>}</span>
<span>}</span><span>;</span>

defaults<span>.</span>headers <span>=</span> <span>{</span>
  common<span>:</span> <span>{</span>
    <span>'Accept'</span><span>:</span> <span>'application/json, text/plain, */*'</span>
  <span>}</span>
<span>}</span><span>;</span>

utils<span>.</span><span>forEach</span><span>(</span><span>[</span><span>'delete'</span><span>,</span> <span>'get'</span><span>,</span> <span>'head'</span><span>]</span><span>,</span> <span>function</span> <span>forEachMethodNoData</span><span>(</span><span>method</span><span>)</span> <span>{</span>
  defaults<span>.</span>headers<span>[</span>method<span>]</span> <span>=</span> <span>{</span><span>}</span><span>;</span>
<span>}</span><span>)</span><span>;</span>

utils<span>.</span><span>forEach</span><span>(</span><span>[</span><span>'post'</span><span>,</span> <span>'put'</span><span>,</span> <span>'patch'</span><span>]</span><span>,</span> <span>function</span> <span>forEachMethodWithData</span><span>(</span><span>method</span><span>)</span> <span>{</span>
  defaults<span>.</span>headers<span>[</span>method<span>]</span> <span>=</span> utils<span>.</span><span>merge</span><span>(</span><span>DEFAULT_CONTENT_TYPE</span><span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br></div></div></li>
</ul>
<h4 id="配置信息合并"> 配置信息合并</h4>
<ul>
<li>
<p>请求配置信息合并策略：请求的 <code>config</code> 参数 &gt; 实例的 <code>defaults</code> 属性 &gt; 库默认值。</p>
<div><pre><code><span>// 使用库提供的默认配置创建实例</span>
<span>// 此时超时配置的默认值是 `0`</span>
<span>const</span> instance <span>=</span> axios<span>.</span><span>create</span><span>(</span><span>)</span><span>;</span>

<span>// 重写库的超时默认值</span>
<span>// 现在，所有使用此实例的请求都将等待2.5秒，然后才会超时</span>
instance<span>.</span>defaults<span>.</span>timeout <span>=</span> <span>2500</span><span>;</span>

<span>// 重写此请求的超时时间，因为该请求需要很长时间</span>
instance<span>.</span><span>get</span><span>(</span><span>'/longRequest'</span><span>,</span> <span>{</span>
  timeout<span>:</span> <span>5000</span>
<span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div></li>
</ul>
<p><strong>源码分析</strong></p>
<ul>
<li>
<p>在 <code>axios.create(config)</code> 创建实例时候，将传入 配置项与默认配置项目通过 <code>mergeConfig</code> 合并，将实例配置覆盖默认配置。</p>
<p><img src="./../images/请求合并.png" alt="" /></p>
</li>
<li>
<p>在实际请求过程中，调用 <code>axios.request()</code> 方法请求，该方法将实际请求配置与默认配置合并，用实际配置覆盖默认配置。</p>
<p><img src="./../images/请求合并2.png" alt="" /></p>
</li>
<li>
<p><code>mergeConfig</code>定义在<a href="https://github.com/axios/axios/blob/e9965bfafc82d8b42765705061b9ebe2d5532493/lib/core/mergeConfig.js" target="_blank" rel="noopener noreferrer">lib/core/mergeConfig.js</a></p>
<div><pre><code>module<span>.</span><span>exports</span> <span>=</span> <span>function</span> <span>mergeConfig</span><span>(</span><span>config1<span>,</span> config2</span><span>)</span> <span>{</span>
  <span>// eslint-disable-next-line no-param-reassign</span>
  config2 <span>=</span> config2 <span>||</span> <span>{</span><span>}</span><span>;</span>
  <span>var</span> config <span>=</span> <span>{</span><span>}</span><span>;</span>

  <span>var</span> valueFromConfig2Keys <span>=</span> <span>[</span><span>'url'</span><span>,</span> <span>'method'</span><span>,</span> <span>'data'</span><span>]</span><span>;</span>
  <span>var</span> mergeDeepPropertiesKeys <span>=</span> <span>[</span><span>'headers'</span><span>,</span> <span>'auth'</span><span>,</span> <span>'proxy'</span><span>,</span> <span>'params'</span><span>]</span><span>;</span>
  <span>var</span> defaultToConfig2Keys <span>=</span> <span>[</span>
    <span>'baseURL'</span><span>,</span> <span>'transformRequest'</span><span>,</span> <span>'transformResponse'</span><span>,</span> <span>'paramsSerializer'</span><span>,</span>
    <span>'timeout'</span><span>,</span> <span>'timeoutMessage'</span><span>,</span> <span>'withCredentials'</span><span>,</span> <span>'adapter'</span><span>,</span> <span>'responseType'</span><span>,</span> <span>'xsrfCookieName'</span><span>,</span>
    <span>'xsrfHeaderName'</span><span>,</span> <span>'onUploadProgress'</span><span>,</span> <span>'onDownloadProgress'</span><span>,</span> <span>'decompress'</span><span>,</span>
    <span>'maxContentLength'</span><span>,</span> <span>'maxBodyLength'</span><span>,</span> <span>'maxRedirects'</span><span>,</span> <span>'transport'</span><span>,</span> <span>'httpAgent'</span><span>,</span>
    <span>'httpsAgent'</span><span>,</span> <span>'cancelToken'</span><span>,</span> <span>'socketPath'</span><span>,</span> <span>'responseEncoding'</span>
  <span>]</span><span>;</span>
  <span>var</span> directMergeKeys <span>=</span> <span>[</span><span>'validateStatus'</span><span>]</span><span>;</span>

  <span>function</span> <span>getMergedValue</span><span>(</span><span>target<span>,</span> source</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>utils<span>.</span><span>isPlainObject</span><span>(</span>target<span>)</span> <span>&amp;&amp;</span> utils<span>.</span><span>isPlainObject</span><span>(</span>source<span>)</span><span>)</span> <span>{</span>
      <span>return</span> utils<span>.</span><span>merge</span><span>(</span>target<span>,</span> source<span>)</span><span>;</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span>utils<span>.</span><span>isPlainObject</span><span>(</span>source<span>)</span><span>)</span> <span>{</span>
      <span>return</span> utils<span>.</span><span>merge</span><span>(</span><span>{</span><span>}</span><span>,</span> source<span>)</span><span>;</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span>utils<span>.</span><span>isArray</span><span>(</span>source<span>)</span><span>)</span> <span>{</span>
      <span>return</span> source<span>.</span><span>slice</span><span>(</span><span>)</span><span>;</span>
    <span>}</span>
    <span>return</span> source<span>;</span>
  <span>}</span>

  <span>function</span> <span>mergeDeepProperties</span><span>(</span><span>prop</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span><span>!</span>utils<span>.</span><span>isUndefined</span><span>(</span>config2<span>[</span>prop<span>]</span><span>)</span><span>)</span> <span>{</span>
      config<span>[</span>prop<span>]</span> <span>=</span> <span>getMergedValue</span><span>(</span>config1<span>[</span>prop<span>]</span><span>,</span> config2<span>[</span>prop<span>]</span><span>)</span><span>;</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>!</span>utils<span>.</span><span>isUndefined</span><span>(</span>config1<span>[</span>prop<span>]</span><span>)</span><span>)</span> <span>{</span>
      config<span>[</span>prop<span>]</span> <span>=</span> <span>getMergedValue</span><span>(</span><span>undefined</span><span>,</span> config1<span>[</span>prop<span>]</span><span>)</span><span>;</span>
    <span>}</span>
  <span>}</span>

  utils<span>.</span><span>forEach</span><span>(</span>valueFromConfig2Keys<span>,</span> <span>function</span> <span>valueFromConfig2</span><span>(</span><span>prop</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span><span>!</span>utils<span>.</span><span>isUndefined</span><span>(</span>config2<span>[</span>prop<span>]</span><span>)</span><span>)</span> <span>{</span>
      config<span>[</span>prop<span>]</span> <span>=</span> <span>getMergedValue</span><span>(</span><span>undefined</span><span>,</span> config2<span>[</span>prop<span>]</span><span>)</span><span>;</span>
    <span>}</span>
  <span>}</span><span>)</span><span>;</span>

  utils<span>.</span><span>forEach</span><span>(</span>mergeDeepPropertiesKeys<span>,</span> mergeDeepProperties<span>)</span><span>;</span>

  utils<span>.</span><span>forEach</span><span>(</span>defaultToConfig2Keys<span>,</span> <span>function</span> <span>defaultToConfig2</span><span>(</span><span>prop</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span><span>!</span>utils<span>.</span><span>isUndefined</span><span>(</span>config2<span>[</span>prop<span>]</span><span>)</span><span>)</span> <span>{</span>
      config<span>[</span>prop<span>]</span> <span>=</span> <span>getMergedValue</span><span>(</span><span>undefined</span><span>,</span> config2<span>[</span>prop<span>]</span><span>)</span><span>;</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>!</span>utils<span>.</span><span>isUndefined</span><span>(</span>config1<span>[</span>prop<span>]</span><span>)</span><span>)</span> <span>{</span>
      config<span>[</span>prop<span>]</span> <span>=</span> <span>getMergedValue</span><span>(</span><span>undefined</span><span>,</span> config1<span>[</span>prop<span>]</span><span>)</span><span>;</span>
    <span>}</span>
  <span>}</span><span>)</span><span>;</span>

  utils<span>.</span><span>forEach</span><span>(</span>directMergeKeys<span>,</span> <span>function</span> <span>merge</span><span>(</span><span>prop</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>prop <span>in</span> config2<span>)</span> <span>{</span>
      config<span>[</span>prop<span>]</span> <span>=</span> <span>getMergedValue</span><span>(</span>config1<span>[</span>prop<span>]</span><span>,</span> config2<span>[</span>prop<span>]</span><span>)</span><span>;</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span>prop <span>in</span> config1<span>)</span> <span>{</span>
      config<span>[</span>prop<span>]</span> <span>=</span> <span>getMergedValue</span><span>(</span><span>undefined</span><span>,</span> config1<span>[</span>prop<span>]</span><span>)</span><span>;</span>
    <span>}</span>
  <span>}</span><span>)</span><span>;</span>

  <span>var</span> axiosKeys <span>=</span> valueFromConfig2Keys
    <span>.</span><span>concat</span><span>(</span>mergeDeepPropertiesKeys<span>)</span>
    <span>.</span><span>concat</span><span>(</span>defaultToConfig2Keys<span>)</span>
    <span>.</span><span>concat</span><span>(</span>directMergeKeys<span>)</span><span>;</span>

  <span>var</span> otherKeys <span>=</span> Object
    <span>.</span><span>keys</span><span>(</span>config1<span>)</span>
    <span>.</span><span>concat</span><span>(</span>Object<span>.</span><span>keys</span><span>(</span>config2<span>)</span><span>)</span>
    <span>.</span><span>filter</span><span>(</span><span>function</span> <span>filterAxiosKeys</span><span>(</span><span>key</span><span>)</span> <span>{</span>
      <span>return</span> axiosKeys<span>.</span><span>indexOf</span><span>(</span>key<span>)</span> <span>===</span> <span>-</span><span>1</span><span>;</span>
    <span>}</span><span>)</span><span>;</span>

  utils<span>.</span><span>forEach</span><span>(</span>otherKeys<span>,</span> mergeDeepProperties<span>)</span><span>;</span>

  <span>return</span> config<span>;</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br></div></div></li>
</ul>
<hr>
<h4 id="响应信息"> 响应信息</h4>
<ul>
<li>
<p>响应信息。</p>
<div><pre><code><span>{</span>
  <span>// `data` 由服务器提供的响应</span>
  data<span>:</span> <span>{</span><span>}</span><span>,</span>

  <span>// `status` 来自服务器响应的 HTTP 状态码</span>
  status<span>:</span> <span>200</span><span>,</span>

  <span>// `statusText` 来自服务器响应的 HTTP 状态信息</span>
  statusText<span>:</span> <span>'OK'</span><span>,</span>

  <span>// `headers` 是服务器响应头</span>
  <span>// 所有的 header 名称都是小写，而且可以使用方括号语法访问</span>
  <span>// 例如: `response.headers['content-type']`</span>
  headers<span>:</span> <span>{</span><span>}</span><span>,</span>

  <span>// `config` 是 `axios` 请求的配置信息</span>
  config<span>:</span> <span>{</span><span>}</span><span>,</span>

  <span>// `request` 是生成此响应的请求</span>
  <span>// 在node.js中它是最后一个ClientRequest实例 (in redirects)，</span>
  <span>// 在浏览器中则是 XMLHttpRequest 实例</span>
  request<span>:</span> <span>{</span><span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br></div></div></li>
</ul>
<h3 id="拦截器"> 拦截器</h3>
<ul>
<li>
<p>添加拦截器：在请求或响应被 then 或 catch 处理前拦截它们。</p>
<div><pre><code><span>// 添加请求拦截器</span>
axios<span>.</span>interceptors<span>.</span>request<span>.</span><span>use</span><span>(</span><span>function</span> <span>(</span><span>config</span><span>)</span> <span>{</span>
    <span>// 在发送请求之前做些什么</span>
    <span>return</span> config<span>;</span>
  <span>}</span><span>,</span> <span>function</span> <span>(</span><span>error</span><span>)</span> <span>{</span>
    <span>// 对请求错误做些什么</span>
    <span>return</span> Promise<span>.</span><span>reject</span><span>(</span>error<span>)</span><span>;</span>
  <span>}</span><span>)</span><span>;</span>

<span>// 添加响应拦截器</span>
axios<span>.</span>interceptors<span>.</span>response<span>.</span><span>use</span><span>(</span><span>function</span> <span>(</span><span>response</span><span>)</span> <span>{</span>
    <span>// 2xx 范围内的状态码都会触发该函数。</span>
    <span>// 对响应数据做点什么</span>
    <span>return</span> response<span>;</span>
  <span>}</span><span>,</span> <span>function</span> <span>(</span><span>error</span><span>)</span> <span>{</span>
    <span>// 超出 2xx 范围的状态码都会触发该函数。</span>
    <span>// 对响应错误做点什么</span>
    <span>return</span> Promise<span>.</span><span>reject</span><span>(</span>error<span>)</span><span>;</span>
  <span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div></li>
<li>
<p>移除拦截器：</p>
<div><pre><code><span>const</span> myInterceptor <span>=</span> axios<span>.</span>interceptors<span>.</span>request<span>.</span><span>use</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span><span>/*...*/</span><span>}</span><span>)</span><span>;</span>
axios<span>.</span>interceptors<span>.</span>request<span>.</span><span>eject</span><span>(</span>myInterceptor<span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div></li>
<li>
<p>自定义的 axios 实例添加拦截器。</p>
<div><pre><code><span>const</span> instance <span>=</span> axios<span>.</span><span>create</span><span>(</span><span>)</span><span>;</span>
instance<span>.</span>interceptors<span>.</span>request<span>.</span><span>use</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span><span>/*...*/</span><span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div></li>
</ul>
<p><strong>源码分析</strong></p>
<ul>
<li>
<p>axios 对象中拦截器的定义在 axios 实例中： <a href="https://github.com/axios/axios/blob/e9965bfafc82d8b42765705061b9ebe2d5532493/lib/core/Axios.js" target="_blank" rel="noopener noreferrer">lib/core/Axios.js</a></p>
<div><pre><code><span>function</span> <span>Axios</span><span>(</span><span>instanceConfig</span><span>)</span> <span>{</span>
  <span>this</span><span>.</span>defaults <span>=</span> instanceConfig<span>;</span>
  <span>this</span><span>.</span>interceptors <span>=</span> <span>{</span>
    request<span>:</span> <span>new</span> <span>InterceptorManager</span><span>(</span><span>)</span><span>,</span> <span>//请求拦截器</span>
    response<span>:</span> <span>new</span> <span>InterceptorManager</span><span>(</span><span>)</span> <span>//响应拦截器</span>
  <span>}</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
<li>
<p>拦截器定义：拦截器实际是一个队列，use 方法往队列中添加拦截器。定义在<a href="https://github.com/axios/axios/blob/e9965bfafc82d8b42765705061b9ebe2d5532493/lib/core/InterceptorManager.js" target="_blank" rel="noopener noreferrer">lib/core/InterceptorManager.js</a></p>
<div><pre><code><span>//拦截器对象</span>
<span>function</span> <span>InterceptorManager</span><span>(</span><span>)</span> <span>{</span>
  <span>this</span><span>.</span>handlers <span>=</span> <span>[</span><span>]</span><span>;</span> <span>//拦截器堆栈</span>
<span>}</span>

<span>/**
 * Add a new interceptor to the stack
 * 往拦截器堆栈中添加拦截器
 * @param {Function} fulfilled The function to handle `then` for a `Promise`
 * @param {Function} rejected The function to handle `reject` for a `Promise`
 *
 * @return {Number} An ID used to remove interceptor later
 */</span>
<span>InterceptorManager</span><span>.</span>prototype<span>.</span><span>use</span> <span>=</span> <span>function</span> <span>use</span><span>(</span><span>fulfilled<span>,</span> rejected</span><span>)</span> <span>{</span>
  <span>this</span><span>.</span>handlers<span>.</span><span>push</span><span>(</span><span>{</span>
    fulfilled<span>:</span> fulfilled<span>,</span>
    rejected<span>:</span> rejected
  <span>}</span><span>)</span><span>;</span>
  <span>return</span> <span>this</span><span>.</span>handlers<span>.</span>length <span>-</span> <span>1</span><span>;</span>
<span>}</span><span>;</span>

<span>/**
 * Remove an interceptor from the stack
 *
 * @param {Number} id The ID that was returned by `use`
 */</span>
<span>InterceptorManager</span><span>.</span>prototype<span>.</span><span>eject</span> <span>=</span> <span>function</span> <span>eject</span><span>(</span><span>id</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>this</span><span>.</span>handlers<span>[</span>id<span>]</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>handlers<span>[</span>id<span>]</span> <span>=</span> <span>null</span><span>;</span>
  <span>}</span>
<span>}</span><span>;</span>

<span>/**
 * Iterate over all the registered interceptors
 *
 * This method is particularly useful for skipping over any
 * interceptors that may have become `null` calling `eject`.
 *
 * @param {Function} fn The function to call for each interceptor
 */</span>
<span>InterceptorManager</span><span>.</span>prototype<span>.</span><span>forEach</span> <span>=</span> <span>function</span> <span>forEach</span><span>(</span><span>fn</span><span>)</span> <span>{</span>
  utils<span>.</span><span>forEach</span><span>(</span><span>this</span><span>.</span>handlers<span>,</span> <span>function</span> <span>forEachHandler</span><span>(</span><span>h</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>h <span>!==</span> <span>null</span><span>)</span> <span>{</span>
      <span>fn</span><span>(</span>h<span>)</span><span>;</span>
    <span>}</span>
  <span>}</span><span>)</span><span>;</span>
<span>}</span><span>;</span>

</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br></div></div></li>
</ul>
<h3 id="请求方法"> 请求方法</h3>
<h4 id="核心方法-request"> 核心方法:request</h4>
<ul>
<li>
<p>Axios 构造函数的核心方法是 request 方法，各种 axios 的调用方式最终都是通过<code>axios.request(config)</code>方法发请求的。</p>
</li>
<li>
<p><code>axios.request(config)</code> 主要流程：</p>
<ul>
<li>合并请求配置；</li>
<li>创建循环队列，队列初始数据为请求适配器对象；</li>
<li>遍历请求拦截器队列，往循环队列头中添加请求拦截器；</li>
<li>遍历响应拦截器队列，往循环队列尾部添加响应拦截器；</li>
<li>串行执行异步循环队列；
<ul>
<li>注意：axios 请求是串行执行异步请求的，也就是说会按照请求入队列的逆序执行，某个请求的响应会等待上一个请求的响应返回后才能执行。</li>
</ul>
</li>
</ul>
</li>
<li>
<p>源码：定义在 <a href="https://github.com/axios/axios/blob/e9965bfafc82d8b42765705061b9ebe2d5532493/lib/core/Axios.js" target="_blank" rel="noopener noreferrer">lib/core/Axios.js</a></p>
<div><pre><code><span>Axios</span><span>.</span>prototype<span>.</span><span>request</span> <span>=</span> <span>function</span> <span>request</span><span>(</span><span>config</span><span>)</span> <span>{</span>
  <span>/*eslint no-param-reassign:0*/</span>
  <span>// Allow for axios('example/url'[, config]) a la fetch API</span>
  <span>if</span> <span>(</span><span>typeof</span> config <span>===</span> <span>'string'</span><span>)</span> <span>{</span>
    config <span>=</span> arguments<span>[</span><span>1</span><span>]</span> <span>||</span> <span>{</span><span>}</span><span>;</span>
    config<span>.</span>url <span>=</span> arguments<span>[</span><span>0</span><span>]</span><span>;</span>
  <span>}</span> <span>else</span> <span>{</span>
    config <span>=</span> config <span>||</span> <span>{</span><span>}</span><span>;</span>
  <span>}</span>
  <span>//合并用户传入配置和axios默认配置(this.defaults是创建axios实例时传入的mergeConfig(axios.defaults, instanceConfig))</span>
  config <span>=</span> <span>mergeConfig</span><span>(</span><span>this</span><span>.</span>defaults<span>,</span> config<span>)</span><span>;</span>

  <span>// Set config.method</span>
  <span>if</span> <span>(</span>config<span>.</span>method<span>)</span> <span>{</span>
    config<span>.</span>method <span>=</span> config<span>.</span>method<span>.</span><span>toLowerCase</span><span>(</span><span>)</span><span>;</span>
  <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>this</span><span>.</span>defaults<span>.</span>method<span>)</span> <span>{</span>
    config<span>.</span>method <span>=</span> <span>this</span><span>.</span>defaults<span>.</span>method<span>.</span><span>toLowerCase</span><span>(</span><span>)</span><span>;</span>
  <span>}</span> <span>else</span> <span>{</span>
    config<span>.</span>method <span>=</span> <span>'get'</span><span>;</span>
  <span>}</span>

  <span>// Hook up interceptors middleware</span>
  <span>//循环队列</span>
  <span>var</span> chain <span>=</span> <span>[</span>dispatchRequest<span>,</span> <span>undefined</span><span>]</span><span>;</span>
  <span>// 将config对象当作参数传给Primise.resolve方法</span>
  <span>var</span> promise <span>=</span> Promise<span>.</span><span>resolve</span><span>(</span>config<span>)</span><span>;</span>
  <span>//遍历所有请求拦截器，将所有请求拦截器添加到循环队列头部</span>
  <span>this</span><span>.</span>interceptors<span>.</span>request<span>.</span><span>forEach</span><span>(</span><span>function</span> <span>unshiftRequestInterceptors</span><span>(</span><span>interceptor</span><span>)</span> <span>{</span>
    chain<span>.</span><span>unshift</span><span>(</span>interceptor<span>.</span>fulfilled<span>,</span> interceptor<span>.</span>rejected<span>)</span><span>;</span>
  <span>}</span><span>)</span><span>;</span>
   <span>//遍历所有响应拦截器，将所有响应拦截器添加到循环队列尾部</span>
  <span>this</span><span>.</span>interceptors<span>.</span>response<span>.</span><span>forEach</span><span>(</span><span>function</span> <span>pushResponseInterceptors</span><span>(</span><span>interceptor</span><span>)</span> <span>{</span>
    chain<span>.</span><span>push</span><span>(</span>interceptor<span>.</span>fulfilled<span>,</span> interceptor<span>.</span>rejected<span>)</span><span>;</span>
  <span>}</span><span>)</span><span>;</span>

  <span>// 添加了拦截器后的chain数组大概会是这样的：</span>
  <span>// [</span>
  <span>//   requestFulfilledFn, requestRejectedFn, ..., </span>
  <span>//   dispatchRequest, undefined,</span>
  <span>//   responseFulfilledFn, responseRejectedFn, ....,</span>
  <span>// ]</span>
  <span>// config会按序通过 请求拦截器 - dispatchRequest方法 - 响应拦截器</span>
  <span>while</span> <span>(</span>chain<span>.</span>length<span>)</span> <span>{</span>
    <span>// 串行执行循环队列 chain</span>
    promise <span>=</span> promise<span>.</span><span>then</span><span>(</span>chain<span>.</span><span>shift</span><span>(</span><span>)</span><span>,</span> chain<span>.</span><span>shift</span><span>(</span><span>)</span><span>)</span><span>;</span>
  <span>}</span>

  <span>return</span> promise<span>;</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br></div></div></li>
</ul>
<p><strong>模拟实现请求队列</strong></p>
<div><pre><code><span>//请求1</span>
<span>let</span> <span>req1</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'request1 start->'</span><span>)</span>
    <span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
      <span>resolve</span><span>(</span><span>'request1->'</span><span>)</span>
      console<span>.</span><span>log</span><span>(</span><span>'request1 end->'</span><span>)</span>
    <span>}</span><span>,</span> <span>1000</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>// 请求2</span>
<span>let</span> <span>req2</span> <span>=</span> <span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'request2 start->'</span><span>)</span>
    <span>resolve</span><span>(</span>data <span>+</span> <span>'request2->'</span><span>)</span>
    console<span>.</span><span>log</span><span>(</span><span>'request2 end->'</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>// 请求3</span>
<span>let</span> <span>req3</span> <span>=</span> <span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'request3 start->'</span><span>)</span>
    <span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
      <span>resolve</span><span>(</span>data <span>+</span> <span>'request3->'</span><span>)</span>
      console<span>.</span><span>log</span><span>(</span><span>'request3 end->'</span><span>)</span>
    <span>}</span><span>,</span> <span>500</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>//响应1</span>
<span>let</span> <span>res1</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    <span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
      console<span>.</span><span>log</span><span>(</span><span>'get response1->'</span><span>)</span>
      <span>resolve</span><span>(</span><span>'response1 ->'</span><span>)</span>
    <span>}</span><span>,</span> <span>1100</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>
<span>//响应数据2</span>
<span>let</span> <span>res2</span> <span>=</span> <span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'get response2->'</span><span>)</span>
    <span>resolve</span><span>(</span>data <span>+</span> <span>'response2->'</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>let</span> <span>res3</span> <span>=</span> <span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    <span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
      console<span>.</span><span>log</span><span>(</span><span>'get response3->'</span><span>)</span>
      <span>resolve</span><span>(</span>data <span>+</span> <span>'response3->'</span><span>)</span>
    <span>}</span><span>,</span> <span>500</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>let</span> chain <span>=</span> <span>[</span><span>]</span>
<span>let</span> requeseInter <span>=</span> <span>[</span>req1<span>,</span>req2<span>,</span>req3<span>]</span>
<span>let</span> responseInter <span>=</span> <span>[</span>res1<span>,</span>res2<span>,</span>res3<span>]</span>

requeseInter<span>.</span><span>forEach</span><span>(</span><span>p</span><span>=></span>chain<span>.</span><span>unshift</span><span>(</span>p<span>)</span><span>)</span><span>;</span>
responseInter<span>.</span><span>forEach</span><span>(</span><span>p</span><span>=></span>chain<span>.</span><span>push</span><span>(</span>p<span>)</span><span>)</span><span>;</span>

<span>let</span> promise <span>=</span> Promise<span>.</span><span>resolve</span><span>(</span><span>{</span><span>}</span><span>)</span>
<span>while</span><span>(</span>chain<span>.</span>length<span>)</span><span>{</span>
    promise  <span>=</span> promise<span>.</span><span>then</span><span>(</span>chain<span>.</span><span>shift</span><span>(</span><span>)</span><span>)</span><span>;</span>
<span>}</span>
console<span>.</span><span>log</span><span>(</span>promise<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br></div></div><p>执行结果：<img src="./../images/moni-axios.png" alt="img" /></p>
<h4 id="其他请求方法"> 其他请求方法</h4>
<ul>
<li>
<p>Axios 实例就是一个 axios 应用，其他方法都是对 Axios 内容的扩展，axios 构造函数的核心方法是 request 方法，各种 axios 的调用方式最终都是通过 request 方法发请求的。</p>
</li>
<li>
<ul>
<li><code>axios.get(url[, config])</code></li>
<li><code>axios.delete(url[, config])</code></li>
<li><code>axios.head(url[, config])</code></li>
<li><code>axios.options(url[, config])</code></li>
<li><code>axios.post(url[, data[, config]])</code></li>
<li><code>axios.put(url[, data[, config]])</code></li>
<li><code>axios.patch(url[, data[, config]])</code></li>
</ul>
</li>
<li>
<ul>
<li><code>axios.get(url[, config])</code></li>
<li><code>axios.delete(url[, config])</code></li>
<li><code>axios.head(url[, config])</code></li>
<li><code>axios.options(url[, config]).</code></li>
<li><code>axios.post(url[, data[, config]])</code></li>
<li><code>axios.put(url[, data[, config]])</code></li>
<li><code>axios.patch(url[, data[, config]])</code></li>
</ul>
</li>
<li>
<p>实现：遍历方法名，合并配置项目参数，返回 reques 方法。定义在 <a href="https://github.com/axios/axios/blob/e9965bfafc82d8b42765705061b9ebe2d5532493/lib/core/Axios.js" target="_blank" rel="noopener noreferrer">lib/core/Axios.js</a></p>
<div><pre><code><span>// Provide aliases for supported request methods</span>
<span>//所有请求方法都是封装 request 方法</span>
utils<span>.</span><span>forEach</span><span>(</span><span>[</span><span>'delete'</span><span>,</span> <span>'get'</span><span>,</span> <span>'head'</span><span>,</span> <span>'options'</span><span>]</span><span>,</span> <span>function</span> <span>forEachMethodNoData</span><span>(</span><span>method</span><span>)</span> <span>{</span>
  <span>/*eslint func-names:0*/</span>
  <span>Axios</span><span>.</span>prototype<span>[</span>method<span>]</span> <span>=</span> <span>function</span><span>(</span><span>url<span>,</span> config</span><span>)</span> <span>{</span>
    <span>return</span> <span>this</span><span>.</span><span>request</span><span>(</span><span>mergeConfig</span><span>(</span>config <span>||</span> <span>{</span><span>}</span><span>,</span> <span>{</span>
      method<span>:</span> method<span>,</span>
      url<span>:</span> url
    <span>}</span><span>)</span><span>)</span><span>;</span>
  <span>}</span><span>;</span>
<span>}</span><span>)</span><span>;</span>

utils<span>.</span><span>forEach</span><span>(</span><span>[</span><span>'post'</span><span>,</span> <span>'put'</span><span>,</span> <span>'patch'</span><span>]</span><span>,</span> <span>function</span> <span>forEachMethodWithData</span><span>(</span><span>method</span><span>)</span> <span>{</span>
  <span>/*eslint func-names:0*/</span>
  <span>Axios</span><span>.</span>prototype<span>[</span>method<span>]</span> <span>=</span> <span>function</span><span>(</span><span>url<span>,</span> data<span>,</span> config</span><span>)</span> <span>{</span>
    <span>return</span> <span>this</span><span>.</span><span>request</span><span>(</span><span>mergeConfig</span><span>(</span>config <span>||</span> <span>{</span><span>}</span><span>,</span> <span>{</span>
      method<span>:</span> method<span>,</span>
      url<span>:</span> url<span>,</span>
      data<span>:</span> data
    <span>}</span><span>)</span><span>)</span><span>;</span>
  <span>}</span><span>;</span>
<span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br></div></div></li>
</ul>
<h3 id="发送请求"> 发送请求</h3>
<ul>
<li>
<p>在 axios 中请求方法都是通过 axios.request 完成，上面分析过：request 中初始队列中使用 <code>dispatchRequest</code> 方法，当拦截器队列中所有请求被串行执行完成后，执行 <code>dispatchRequest</code> 方法，最后执行响应拦截器。</p>
</li>
<li>
<p><code>dispatchRequest</code> 方法：定义在<a href="https://github.com/axios/axios/blob/e9965bfafc82d8b42765705061b9ebe2d5532493/lib/core/dispatchRequest.js" target="_blank" rel="noopener noreferrer">/lib/core/dispatchRequest.js</a></p>
<ul>
<li>检查是否取消，若取消则抛出请求取消错误；</li>
<li>处理请求配置 config、转换请求格式；</li>
<li>使用适配器发送请求；</li>
<li>最后处理响应数据：首先检查请求是否已经取消，已经取消抛出请求取消错误；否则转换响应数据并返回。</li>
</ul>
<div><pre><code><span>/**
 * Dispatch a request to the server using the configured adapter.
 * 使用配置的适配器发送请求
 * @param {object} config The config that is to be used for the request
 * @returns {Promise} The Promise to be fulfilled
 */</span>
module<span>.</span><span>exports</span> <span>=</span> <span>function</span> <span>dispatchRequest</span><span>(</span><span>config</span><span>)</span> <span>{</span>
  <span>throwIfCancellationRequested</span><span>(</span>config<span>)</span><span>;</span> <span>//如果已请求取消，抛出' Cancel '。</span>

  <span>// Ensure headers exist</span>
  config<span>.</span>headers <span>=</span> config<span>.</span>headers <span>||</span> <span>{</span><span>}</span><span>;</span>

  <span>// Transform request data</span>
  <span>//transformRequest 是在配置请求配置时候传入的自定义转换方法，在此处请求前转换。</span>
  config<span>.</span>data <span>=</span> <span>transformData</span><span>(</span>
    config<span>.</span>data<span>,</span>
    config<span>.</span>headers<span>,</span>
    config<span>.</span>transformRequest
  <span>)</span><span>;</span>

  <span>// Flatten headers</span>
  config<span>.</span>headers <span>=</span> utils<span>.</span><span>merge</span><span>(</span>
    config<span>.</span>headers<span>.</span>common <span>||</span> <span>{</span><span>}</span><span>,</span>
    config<span>.</span>headers<span>[</span>config<span>.</span>method<span>]</span> <span>||</span> <span>{</span><span>}</span><span>,</span>
    config<span>.</span>headers
  <span>)</span><span>;</span>

  utils<span>.</span><span>forEach</span><span>(</span>
    <span>[</span><span>'delete'</span><span>,</span> <span>'get'</span><span>,</span> <span>'head'</span><span>,</span> <span>'post'</span><span>,</span> <span>'put'</span><span>,</span> <span>'patch'</span><span>,</span> <span>'common'</span><span>]</span><span>,</span>
    <span>function</span> <span>cleanHeaderConfig</span><span>(</span><span>method</span><span>)</span> <span>{</span>
      <span>delete</span> config<span>.</span>headers<span>[</span>method<span>]</span><span>;</span>
    <span>}</span>
  <span>)</span><span>;</span>

  <span>var</span> adapter <span>=</span> config<span>.</span>adapter <span>||</span> defaults<span>.</span>adapter<span>;</span>

  <span>return</span> <span>adapter</span><span>(</span>config<span>)</span><span>.</span><span>then</span><span>(</span><span>function</span> <span>onAdapterResolution</span><span>(</span><span>response</span><span>)</span> <span>{</span> <span>//响应成功处理方法</span>
    <span>throwIfCancellationRequested</span><span>(</span>config<span>)</span><span>;</span>

    <span>// Transform response data</span>
    <span>// </span>
    response<span>.</span>data <span>=</span> <span>transformData</span><span>(</span>
      response<span>.</span>data<span>,</span>
      response<span>.</span>headers<span>,</span>
      config<span>.</span>transformResponse
    <span>)</span><span>;</span>

    <span>return</span> response<span>;</span>
  <span>}</span><span>,</span> <span>function</span> <span>onAdapterRejection</span><span>(</span><span>reason</span><span>)</span> <span>{</span> <span>//响应失败处理方法</span>
    <span>if</span> <span>(</span><span>!</span><span>isCancel</span><span>(</span>reason<span>)</span><span>)</span> <span>{</span>
      <span>throwIfCancellationRequested</span><span>(</span>config<span>)</span><span>;</span>

      <span>// Transform response data</span>
      <span>if</span> <span>(</span>reason <span>&amp;&amp;</span> reason<span>.</span>response<span>)</span> <span>{</span>
        reason<span>.</span>response<span>.</span>data <span>=</span> <span>transformData</span><span>(</span>
          reason<span>.</span>response<span>.</span>data<span>,</span>
          reason<span>.</span>response<span>.</span>headers<span>,</span>
          config<span>.</span>transformResponse
        <span>)</span><span>;</span>
      <span>}</span>
    <span>}</span>

    <span>return</span> Promise<span>.</span><span>reject</span><span>(</span>reason<span>)</span><span>;</span>
  <span>}</span><span>)</span><span>;</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br></div></div></li>
</ul>
<h3 id="适配器"> 适配器</h3>
<ul>
<li>Axios 是一个基于 promise 的网络请求库，可以用于浏览器和 node.js。它是 <em><a href="https://www.lullabot.com/articles/what-is-an-isomorphic-application" target="_blank" rel="noopener noreferrer">isomorphic</a></em> 的(即同一套代码可以运行在浏览器和node.js中)。在服务端它使用原生 node.js <code>http</code> 模块, 而在客户端 (浏览端) 则使用 XMLHttpRequests。</li>
<li>isomorphic 的实现，使用适配器设计模式来屏蔽平台的差异性，让使用者可以在浏览器端和 NodeJS 环境中使用同一套 API 发起 http 请求。</li>
<li>http请求适配器就是一个方法，在axios项目里，http请求适配器主要指两种：XHR 和 HTTP。
<ul>
<li>XHR：核心是浏览器端的XMLHttpRequest对象。</li>
<li>HTTP：核心是 node 的 http/https 的 request 方法。</li>
</ul>
</li>
</ul>
<p><strong>源码分析：</strong></p>
<ul>
<li>
<p>添加默认是适配器：</p>
<ul>
<li>
<p>在默认导入 axios 实例时，axios 使用默配置创建 axios 实例。</p>
</li>
<li>
<p>在 <a href="https://github.com/axios/axios/blob/master/lib/defaults.js" target="_blank" rel="noopener noreferrer">lib/defaults.js</a>默认配置中使用 <code>getDefaultAdapter</code> 获取默认 adapter 适配器。</p>
</li>
<li>
<p><code>getDefaultAdapter</code> 方法：判断 <code>XMLHttpRequest</code> 和 <code>process</code> 对象，获取 xhr/http 适配器。</p>
<p><img src="./../images/默认适配器.png" alt="" /></p>
</li>
</ul>
</li>
<li>
<p>使用适配器：</p>
<ul>
<li>在上节中分析到： request 串行请求遍历拦截器 -&gt; <code>dispatchRequest</code> 返送请求 -&gt; 返回适配器。流程中<code>dispatchRequest</code> 方法调用适配器。</li>
</ul>
</li>
<li>
<p>XHR 适配器： 创建 <code>XMLHttpReques</code> 对象，添加请求结果处理、中断超时处理、错误处理方法，进行参数转换、xsrf 处理、请求取消处理等。定义在<a href="https://github.com/axios/axios/blob/e9965bfafc82d8b42765705061b9ebe2d5532493/lib/adapters/xhr.js" target="_blank" rel="noopener noreferrer">lib/adapters/xhr.js</a></p>
</li>
<li>
<p>HTTP 适配器： 参数、配置处理，根据配置创建 http/https/httpsFollow/httpFollow 对象，发送请求。</p>
</li>
<li>
<p>适配器中结果返回后，统一调用 sette() 方法进行结果验证。</p>
</li>
<li>
<p>settle 方法： 使用配置参数中自定义验证方法验证结果。</p>
<div><pre><code><span>// /lib/core/settle.js</span>
module<span>.</span><span>exports</span> <span>=</span> <span>function</span> <span>settle</span><span>(</span><span>resolve<span>,</span> reject<span>,</span> response</span><span>)</span> <span>{</span>
  <span>var</span> validateStatus <span>=</span> response<span>.</span>config<span>.</span>validateStatus<span>;</span>
  <span>if</span> <span>(</span><span>!</span>validateStatus <span>||</span> <span>validateStatus</span><span>(</span>response<span>.</span>status<span>)</span><span>)</span> <span>{</span>
    <span>resolve</span><span>(</span>response<span>)</span><span>;</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>reject</span><span>(</span><span>createError</span><span>(</span>
      <span>'Request failed with status code '</span> <span>+</span> response<span>.</span>status<span>,</span>
      response<span>.</span>config<span>,</span>
      <span>null</span><span>,</span>
      response<span>.</span>request<span>,</span>
      response
    <span>)</span><span>)</span><span>;</span>
  <span>}</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div></li>
</ul>
<h3 id="取消请求"> 取消请求</h3>
<ul>
<li>
<p>使用 <em>cancel token</em> 取消一个请求：Axios 的 cancel token API 是基于被撤销 <a href="https://github.com/tc39/proposal-cancelable-promises" target="_blank" rel="noopener noreferrer">cancelable promises proposal</a>。</p>
</li>
<li>
<p>可以使用同一个 cancel token 取消多个请求。</p>
</li>
<li>
<p>取消请求方式：</p>
<ul>
<li>
<p>传递一个 executor 函数到 <code>CancelToken</code> 的构造函数来创建一个 cancel token：</p>
<div><pre><code><span>const</span> CancelToken <span>=</span> axios<span>.</span>CancelToken<span>;</span>
<span>let</span> cancel<span>;</span>

axios<span>.</span><span>get</span><span>(</span><span>'/user/12345'</span><span>,</span> <span>{</span>
  cancelToken<span>:</span> <span>new</span> <span>CancelToken</span><span>(</span><span>function</span> <span>executor</span><span>(</span><span>c</span><span>)</span> <span>{</span>
    <span>// executor 函数接收一个 cancel 函数作为参数，调用 cancel 函数可以取消请求操作</span>
    cancel <span>=</span> c<span>;</span>
  <span>}</span><span>)</span>
<span>}</span><span>)</span><span>;</span>

<span>// 取消请求</span>
<span>cancel</span><span>(</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div></li>
<li>
<p>使用 <code>CancelToken.source</code> 工厂方法创建一个 cancel token ：</p>
<div><pre><code><span>const</span> CancelToken <span>=</span> axios<span>.</span>CancelToken<span>;</span>
<span>const</span> source <span>=</span> CancelToken<span>.</span><span>source</span><span>(</span><span>)</span><span>;</span>

axios<span>.</span><span>get</span><span>(</span><span>'/user/12345'</span><span>,</span> <span>{</span>
  cancelToken<span>:</span> source<span>.</span>token
<span>}</span><span>)</span><span>.</span><span>catch</span><span>(</span><span>function</span> <span>(</span><span>thrown</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span>axios<span>.</span><span>isCancel</span><span>(</span>thrown<span>)</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'Request canceled'</span><span>,</span> thrown<span>.</span>message<span>)</span><span>;</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>// 处理错误</span>
  <span>}</span>
<span>}</span><span>)</span><span>;</span>

axios<span>.</span><span>post</span><span>(</span><span>'/user/12345'</span><span>,</span> <span>{</span>
  name<span>:</span> <span>'new name'</span>
<span>}</span><span>,</span> <span>{</span>
  cancelToken<span>:</span> source<span>.</span>token
<span>}</span><span>)</span>

<span>// 取消请求（message 参数是可选的）</span>
source<span>.</span><span>cancel</span><span>(</span><span>'Operation canceled by the user.'</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br></div></div></li>
</ul>
</li>
</ul>
<p><strong>源码分析：</strong></p>
<ul>
<li>
<p>取消请求判断时机：在 创建 axios实例 -&gt; 合并配置 -&gt; 添加拦截器 -&gt; 遍历拦截器队列 -&gt; dispatchRequest 分发请求前，检查请求是否取消 -&gt; 适配器发送请求时检查是否取消请求 -&gt; 适配器中响应数据返回到 <code>dispatchRequest</code> 方法后，再次检查请求是否取消。因此一共经过三次请求取消检查。</p>
<ul>
<li>第一次取消检查：dispatchRequest 分发请求前，检查请求是否取消。在<a href="https://github.com/axios/axios/blob/e9965bfafc82d8b42765705061b9ebe2d5532493/lib/core/dispatchRequest.js" target="_blank" rel="noopener noreferrer">/lib/core/dispatchRequest.js</a>。</li>
</ul>
<p><img src="./../images/axios取消检查1.png" alt="" /></p>
<ul>
<li>
<p>第二次取消检查：适配器发送请求时检查是否取消请求。</p>
<ul>
<li>XHR 适配器中：<img src="./../images/axios取消检查xhr.png" alt="" /></li>
<li>HTTP 是配器中：<img src="./../images/axios取消检查htp.png" alt="" /></li>
</ul>
</li>
<li>
<p>第三次取消检查：适配器中响应数据返回到 <code>dispatchRequest</code> 方法后，再次检查请求是否取消。</p>
<p><img src="./../images/axios取消检查3.png" alt="" /></p>
</li>
</ul>
</li>
<li>
<p>使用 cancel token 有两种不同使用方式：</p>
<ul>
<li>
<p>传递一个 executor 函数到 <code>CancelToken</code> 的构造函数来创建一个 cancel token。</p>
<ul>
<li>
<p><code>CancelToken</code> 的构造函数：</p>
<div><pre><code><span>/**
 * A `CancelToken` is an object that can be used to request cancellation of an operation.
 * 一个可以取消请求操作的 CancelToken 对象
 * @class
 * @param {Function} executor The executor function. 参数为执行器函数
 */</span>
<span>function</span> <span>CancelToken</span><span>(</span><span>executor</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>typeof</span> executor <span>!==</span> <span>'function'</span><span>)</span> <span>{</span> <span>//执行器必须为函数</span>
    <span>throw</span> <span>new</span> <span>TypeError</span><span>(</span><span>'executor must be a function.'</span><span>)</span><span>;</span>
  <span>}</span>

  <span>var</span> resolvePromise<span>;</span>
  <span>this</span><span>.</span>promise <span>=</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>promiseExecutor</span><span>(</span><span>resolve</span><span>)</span> <span>{</span>
    resolvePromise <span>=</span> resolve<span>;</span>
  <span>}</span><span>)</span><span>;</span>

  <span>var</span> token <span>=</span> <span>this</span><span>;</span> <span>// 保存对象上下文</span>
  <span>executor</span><span>(</span><span>function</span> <span>cancel</span><span>(</span><span>message</span><span>)</span> <span>{</span> <span>//执行执行器方法，传入 cancel 方法作为 执行器回调</span>
    <span>if</span> <span>(</span>token<span>.</span>reason<span>)</span> <span>{</span>
      <span>// Cancellation has already been requested</span>
      <span>return</span><span>;</span>
    <span>}</span>

    token<span>.</span>reason <span>=</span> <span>new</span> <span>Cancel</span><span>(</span>message<span>)</span><span>;</span> <span>//创建 cancel 对象</span>
    <span>resolvePromise</span><span>(</span>token<span>.</span>reason<span>)</span><span>;</span> <span>//返回 cancel 对象作为回调</span>
  <span>}</span><span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br></div></div></li>
<li>
<p><code>Cancle</code> 对象定义：只有一个 message 参数，表示取消信息，创建后 <code>__CANCEL__</code> 变量为 true，用于判断请求是否已经取消。</p>
<div><pre><code><span>/**
 * A `Cancel` is an object that is thrown when an operation is canceled.
 *
 * @class
 * @param {string=} message The message.
 */</span>
<span>function</span> <span>Cancel</span><span>(</span><span>message</span><span>)</span> <span>{</span>
  <span>this</span><span>.</span>message <span>=</span> message<span>;</span>
<span>}</span>

<span>Cancel</span><span>.</span>prototype<span>.</span><span>toString</span> <span>=</span> <span>function</span> <span>toString</span><span>(</span><span>)</span> <span>{</span>
  <span>return</span> <span>'Cancel'</span> <span>+</span> <span>(</span><span>this</span><span>.</span>message <span>?</span> <span>': '</span> <span>+</span> <span>this</span><span>.</span>message <span>:</span> <span>''</span><span>)</span><span>;</span>
<span>}</span><span>;</span>

<span>Cancel</span><span>.</span>prototype<span>.</span>__CANCEL__ <span>=</span> <span>true</span><span>;</span><span>//用于判断请求是否已经取消。</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div></li>
<li>
<p>判断请求是否取消：<code>isCancel()</code>，判断 Cancel 对象原型中 <code>__CANCEL__</code> 变量。 当调用执行器的 <code>cancel()</code> 方法后，会创建<code>Cancel</code>对象，该对象<code>__CANCEL__</code> 变量为 true，表示已经取消请求。</p>
<div><pre><code>module<span>.</span><span>exports</span> <span>=</span> <span>function</span> <span>isCancel</span><span>(</span><span>value</span><span>)</span> <span>{</span>
  <span>return</span> <span>!</span><span>!</span><span>(</span>value <span>&amp;&amp;</span> value<span>.</span>__CANCEL__<span>)</span><span>;</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
</ul>
</li>
<li>
<p>使用 <code>CancelToken.source</code> 工厂方法创建一个 cancel token 过程：<code>CancelToken.source()</code>方法返回 CancelToken 对象和该对象的取消方法。</p>
<ul>
<li>
<p><code>CancelToken.source()</code> 源码：</p>
<div><pre><code><span>/**
 * Returns an object that contains a new `CancelToken` and a function that, when called,
 * cancels the `CancelToken`.
 * 返回一个 CancelToken 对象 和 cancel 方法
 */</span>
CancelToken<span>.</span><span>source</span> <span>=</span> <span>function</span> <span>source</span><span>(</span><span>)</span> <span>{</span>
  <span>var</span> cancel<span>;</span>
  <span>// token 为  CancelToken 实例，传入默认执行器，使用 cancel 保存执行器的取消方法</span>
  <span>var</span> token <span>=</span> <span>new</span> <span>CancelToken</span><span>(</span><span>function</span> <span>executor</span><span>(</span><span>c</span><span>)</span> <span>{</span>
    cancel <span>=</span> c<span>;</span>
  <span>}</span><span>)</span><span>;</span>
  <span>return</span> <span>{</span>
    token<span>:</span> token<span>,</span>
    cancel<span>:</span> cancel
  <span>}</span><span>;</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="错误处理"> 错误处理</h3>
<ul>
<li>
<p>错误处理：<code>catch()</code>、 <code>validateStatus</code> 配置选项、</p>
<ul>
<li>
<div><pre><code>axios<span>.</span><span>get</span><span>(</span><span>'/user/12345'</span><span>)</span>
  <span>.</span><span>catch</span><span>(</span><span>function</span> <span>(</span><span>error</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>error<span>.</span>response<span>)</span> <span>{</span>
      <span>// 请求成功发出且服务器也响应了状态码，但状态代码超出了 2xx 的范围</span>
      console<span>.</span><span>log</span><span>(</span>error<span>.</span>response<span>.</span>data<span>)</span><span>;</span>
      console<span>.</span><span>log</span><span>(</span>error<span>.</span>response<span>.</span>status<span>)</span><span>;</span>
      console<span>.</span><span>log</span><span>(</span>error<span>.</span>response<span>.</span>headers<span>)</span><span>;</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span>error<span>.</span>request<span>)</span> <span>{</span>
      <span>// 请求已经成功发起，但没有收到响应</span>
      <span>// `error.request` 在浏览器中是 XMLHttpRequest 的实例，</span>
      <span>// 而在node.js中是 http.ClientRequest 的实例</span>
      console<span>.</span><span>log</span><span>(</span>error<span>.</span>request<span>)</span><span>;</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>// 发送请求时出了点问题</span>
      console<span>.</span><span>log</span><span>(</span><span>'Error'</span><span>,</span> error<span>.</span>message<span>)</span><span>;</span>
    <span>}</span>
    console<span>.</span><span>log</span><span>(</span>error<span>.</span>config<span>)</span><span>;</span>
  <span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br></div></div><p>使用 <code>validateStatus</code> 配置选项，可以自定义抛出错误的 HTTP code。</p>
<div><pre><code>axios<span>.</span><span>get</span><span>(</span><span>'/user/12345'</span><span>,</span> <span>{</span>
  <span>validateStatus</span><span>:</span> <span>function</span> <span>(</span><span>status</span><span>)</span> <span>{</span>
    <span>return</span> status <span>&lt;</span> <span>500</span><span>;</span> <span>// 处理状态码小于500的情况</span>
  <span>}</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><p>使用 <code>toJSON</code> 可以获取更多关于HTTP错误的信息。</p>
<div><pre><code>axios<span>.</span><span>get</span><span>(</span><span>'/user/12345'</span><span>)</span>
  <span>.</span><span>catch</span><span>(</span><span>function</span> <span>(</span><span>error</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span>error<span>.</span><span>toJSON</span><span>(</span><span>)</span><span>)</span><span>;</span>
  <span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br></div></div></li>
</ul>
</li>
</ul>
<h3 id="请求体编码"> 请求体编码</h3>
<p>默认情况下，axios将 JavaScript 对象序列化为 <code>JSON</code> 。</p>
<p>要以<code>application/x-www-form-urlencoded</code>格式发送数据，您可以使用以下选项之一。</p>
<p><strong>浏览器</strong></p>
<p>在浏览器中，可以使用<a href="https://developer.mozilla.org/en-US/docs/Web/API/URLSearchParams" target="_blank" rel="noopener noreferrer"><code>URLSearchParams</code></a> API，如下所示：</p>
<div><pre><code><span>const</span> params <span>=</span> <span>new</span> <span>URLSearchParams</span><span>(</span><span>)</span><span>;</span>
params<span>.</span><span>append</span><span>(</span><span>'param1'</span><span>,</span> <span>'value1'</span><span>)</span><span>;</span>
params<span>.</span><span>append</span><span>(</span><span>'param2'</span><span>,</span> <span>'value2'</span><span>)</span><span>;</span>
axios<span>.</span><span>post</span><span>(</span><span>'/foo'</span><span>,</span> params<span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br></div></div><blockquote>
<p>请注意，不是所有的浏览器(参见 <a href="http://www.caniuse.com/#feat=urlsearchparams" target="_blank" rel="noopener noreferrer">caniuse.com</a>)都支持 <code>URLSearchParams</code> ，但是可以使用<a href="https://github.com/WebReflection/url-search-params" target="_blank" rel="noopener noreferrer">polyfill</a> (确保 polyfill 全局环境)</p>
</blockquote>
<p>或者, 您可以使用<a href="https://github.com/ljharb/qs" target="_blank" rel="noopener noreferrer"><code>qs</code></a> 库编码数据:</p>
<div><pre><code><span>const</span> qs <span>=</span> <span>require</span><span>(</span><span>'qs'</span><span>)</span><span>;</span>
axios<span>.</span><span>post</span><span>(</span><span>'/foo'</span><span>,</span> qs<span>.</span><span>stringify</span><span>(</span><span>{</span> <span>'bar'</span><span>:</span> <span>123</span> <span>}</span><span>)</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div><p>或者用另一种方式 (ES6),</p>
<div><pre><code><span>import</span> qs <span>from</span> <span>'qs'</span><span>;</span>
<span>const</span> data <span>=</span> <span>{</span> <span>'bar'</span><span>:</span> <span>123</span> <span>}</span><span>;</span>
<span>const</span> options <span>=</span> <span>{</span>
  method<span>:</span> <span>'POST'</span><span>,</span>
  headers<span>:</span> <span>{</span> <span>'content-type'</span><span>:</span> <span>'application/x-www-form-urlencoded'</span> <span>}</span><span>,</span>
  data<span>:</span> qs<span>.</span><span>stringify</span><span>(</span>data<span>)</span><span>,</span>
  url<span>,</span>
<span>}</span><span>;</span>
<span>axios</span><span>(</span>options<span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div><p><strong>Node.js</strong></p>
<ul>
<li>Query string</li>
</ul>
<p>在 node.js 中， 可以使用 <a href="https://nodejs.org/api/querystring.html" target="_blank" rel="noopener noreferrer"><code>querystring</code></a> 模块，如下所示:</p>
<div><pre><code><span>const</span> querystring <span>=</span> <span>require</span><span>(</span><span>'querystring'</span><span>)</span><span>;</span>
axios<span>.</span><span>post</span><span>(</span><span>'http://something.com/'</span><span>,</span> querystring<span>.</span><span>stringify</span><span>(</span><span>{</span> foo<span>:</span> <span>'bar'</span> <span>}</span><span>)</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div><p>或者从<a href="https://nodejs.org/api/url.html" target="_blank" rel="noopener noreferrer">'url module'</a>中使用<a href="https://nodejs.org/api/url.html#url_class_urlsearchparams" target="_blank" rel="noopener noreferrer">'URLSearchParams'</a>，如下所示:</p>
<div><pre><code><span>const</span> url <span>=</span> <span>require</span><span>(</span><span>'url'</span><span>)</span><span>;</span>
<span>const</span> params <span>=</span> <span>new</span> <span>url<span>.</span>URLSearchParams</span><span>(</span><span>{</span> foo<span>:</span> <span>'bar'</span> <span>}</span><span>)</span><span>;</span>
axios<span>.</span><span>post</span><span>(</span><span>'http://something.com/'</span><span>,</span> params<span>.</span><span>toString</span><span>(</span><span>)</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><p>您也可以使用 <a href="https://github.com/ljharb/qs" target="_blank" rel="noopener noreferrer"><code>qs</code></a> 库。</p>
<p><strong>注意</strong></p>
<p>如果需要对嵌套对象进行字符串化处理，则最好使用 <code>qs</code> 库，因为 querystring 方法在该用例中存在已知问题(https://github.com/nodejs/node-v0.x-archive/issues/1665)。</p>
<p><strong>Form data</strong></p>
<p>在 node.js, 您可以使用 <a href="https://github.com/form-data/form-data" target="_blank" rel="noopener noreferrer"><code>form-data</code></a> 库，如下所示:</p>
<div><pre><code><span>const</span> FormData <span>=</span> <span>require</span><span>(</span><span>'form-data'</span><span>)</span><span>;</span>
 
<span>const</span> form <span>=</span> <span>new</span> <span>FormData</span><span>(</span><span>)</span><span>;</span>
form<span>.</span><span>append</span><span>(</span><span>'my_field'</span><span>,</span> <span>'my value'</span><span>)</span><span>;</span>
form<span>.</span><span>append</span><span>(</span><span>'my_buffer'</span><span>,</span> <span>new</span> <span>Buffer</span><span>(</span><span>10</span><span>)</span><span>)</span><span>;</span>
form<span>.</span><span>append</span><span>(</span><span>'my_file'</span><span>,</span> fs<span>.</span><span>createReadStream</span><span>(</span><span>'/foo/bar.jpg'</span><span>)</span><span>)</span><span>;</span>

axios<span>.</span><span>post</span><span>(</span><span>'https://example.com'</span><span>,</span> form<span>,</span> <span>{</span> headers<span>:</span> form<span>.</span><span>getHeaders</span><span>(</span><span>)</span> <span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div><p>或者, 使用一个拦截器:</p>
<div><pre><code>axios<span>.</span>interceptors<span>.</span>request<span>.</span><span>use</span><span>(</span><span>config</span> <span>=></span> <span>{</span>
  <span>if</span> <span>(</span>config<span>.</span>data <span>instanceof</span> <span>FormData</span><span>)</span> <span>{</span>
    Object<span>.</span><span>assign</span><span>(</span>config<span>.</span>headers<span>,</span> config<span>.</span>data<span>.</span><span>getHeaders</span><span>(</span><span>)</span><span>)</span><span>;</span>
  <span>}</span>
  <span>return</span> config<span>;</span>
<span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div><h2 id="应用"> 应用</h2>
<h3 id="axios-封装"> axios 封装</h3>
<p>一般为了业务需要，会对 axios 进行封装，统一处理：环境配置、 默认配置、添加拦截器、请求方法封装等。</p>
<p>不同应用对于 axios 封装会有不同的方法，SPA 单页面应用和 SSR 服务端渲染的同构应用对于 axios 的配置不同：</p>
<ul>
<li>SPA 应用接口请求主要在浏览器端，无需服务端，无需区分浏览器环境和服务端；</li>
<li>SSR 同构应用涉及 服务端和浏览器端接口请求，需要区分不同环境的请求。
<ul>
<li>服务端渲染无登录相关，请求无需 token 处理、无弹窗提示等。</li>
</ul>
</li>
</ul>
<p><strong>封装时区分内容：</strong></p>
<ul>
<li>
<p><strong>创建 axios 实例</strong>：封装 axios 时候尽量创建一个新的 axios 实例，避免引入 axios 默认实例。</p>
</li>
<li>
<p>原因：引入 axios 默认实，在修改 axios 默认实例的默认配置后，默认配置会影响到所有其他引入 axios 默认实例的接口。在 SPA 单页面应用中一般没什么问题，但是在同构应用中，不同客户端和服务端请求处理不同，因此使用默认引入的 axios 实例容易导致错误。</p>
</li>
<li>
<p><strong>环境配置</strong>：单页面应用仅仅区分 开发/测试/生产环境，同构应用还要区分 浏览器和服务端环境。</p>
<ul>
<li>
<p>开发/测试/生产环境：根据 <code>process.env.NODE_ENV</code> 配置不同的 <code>baseURL</code>，使项目只需执行相应打包命令，就可以在不同环境中自动切换请求主机地址。</p>
<div><pre><code><span>//单页面应用下，五服务端渲染环境时候封装方式</span>
<span>const</span> <span>getBaseUrl</span> <span>=</span> <span>(</span><span>env <span>=</span> process<span>.</span>env<span>.</span><span>NODE_ENV</span></span><span>)</span> <span>=></span> <span>{</span>
  <span>let</span> base <span>=</span> <span>{</span>
    production<span>:</span> <span>'/'</span><span>,</span>
    development<span>:</span> <span>'http://localhost:3000'</span><span>,</span>
    test<span>:</span> <span>'http://localhost:3001'</span><span>,</span>
  <span>}</span><span>[</span>env<span>]</span><span>;</span>
  <span>return</span> base<span>?</span> base <span>:</span> <span>'/'</span> <span>;</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div></li>
<li>
<p>浏览器和服务端环境：同构应用需要区分。根据 <code>process.env.isServer</code> (通过 webpack 打包时注入) 判断浏览器和 node 服务端运行环境，在浏览器和 node 服务端中，请求中 baseUrl 会略有差异：</p>
<ul>
<li>
<p>在 Node 服务器中，请求需要配置默认 baseUrl，因为服务器不知道请求域名/地址。</p>
</li>
<li>
<p>在浏览器中不需要配置 baseUrl，浏览器默认会使用当前页面下的域名作为 baseUrl 发送请求。（同构应用在服务端渲染时候获取初始接口数据，渲染静态化页面后进行客户端激活，客户端激活后处理客户端请求接口）</p>
<div><pre><code><span>//SSR同构应用下， 服务端渲染和客户端渲染同时存在</span>
<span>const</span> <span>getBaseUrl</span> <span>=</span> <span>(</span><span>env <span>=</span> process<span>.</span>env<span>.</span><span>NODE_ENV</span><span>,</span>isServer <span>=</span> process<span>.</span>env<span>.</span>isServer</span><span>)</span> <span>=></span> <span>{</span>
  <span>let</span> base <span>=</span> <span>{</span>
    production<span>:</span> <span>'/'</span><span>,</span>
    development<span>:</span> <span>'http://localhost:3000'</span><span>,</span>
    test<span>:</span> <span>'http://localhost:3001'</span><span>,</span>
  <span>}</span><span>[</span>env<span>]</span><span>;</span>
  <span>return</span> <span>(</span>isServer <span>===</span> <span>'server'</span><span>)</span><span>?</span> <span>(</span>base<span>?</span> base <span>:</span> <span>'/'</span><span>)</span> <span>:</span> <span>''</span> <span>;</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>默认配置/参数</strong>：如果是同构应用，需要区分浏览器环境和 Node 环境（服务端渲染），根据 <code>process.env.isServer</code> 判断。</p>
<ul>
<li>
<p>浏览器环境和 Node 环境的统一配置：</p>
<ul>
<li>配置超时时间 timeout 属性</li>
</ul>
</li>
<li>
<p>仅浏览器环境下配置：</p>
<ul>
<li>
<p>配置允许携带凭证 widthCredentials 属性设为 true 。客户端环境下添加， 服务端渲染时不需要。</p>
</li>
<li>
<p>添加 Token： 为了避免每次请求中手动添加 Token，可以在 header 中默认配置统一添加 token。，在 node 环境下(服务端渲染时) 不需要添加。</p>
<div><pre><code>client<span>.</span>defaults<span>.</span>headers <span>=</span> <span>{</span>
  post<span>:</span> <span>{</span>
    <span>"Content-Type"</span><span>:</span> <span>"application/json;charset=UTF-8"</span><span>,</span>
  <span>}</span><span>,</span>
  get <span>"User-Token"</span><span>(</span><span>)</span> <span>{</span>
    <span>return</span> <span>getAccessToken</span><span>(</span><span>)</span> <span>//获取 token 方法</span>
  <span>}</span><span>,</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>添加拦截器</strong>: 拦截器需要返回配置，否则整个请求不会进行。</p>
<ul>
<li>
<p>可以在请求拦截器中 添加token凭证、统一设置语言、统一设置内容类型、指定数据格式、添加默认参数、loading缓冲效果等</p>
<ul>
<li>
<p>如：请求拦截中添加请求默认参数。</p>
<div><pre><code><span>//示例</span>
client<span>.</span>interceptors<span>.</span>request<span>.</span><span>use</span><span>(</span>
  <span>function</span> <span>(</span><span>config</span><span>)</span> <span>{</span>
    <span>// console.log(config)</span>
    <span>if</span> <span>(</span>config<span>)</span> <span>{</span>
      <span>if</span> <span>(</span><span>[</span><span>"post"</span><span>,</span> <span>"delete"</span><span>,</span> <span>"put"</span><span>]</span><span>.</span><span>includes</span><span>(</span>config<span>.</span>method<span>)</span><span>)</span> <span>{</span>
        config<span>.</span>data<span>.</span>languageCode <span>=</span> languageCode<span>;</span>
        config<span>.</span>data<span>.</span>countryCode <span>=</span> countryCode<span>;</span>
        config<span>.</span>data<span>.</span>platform <span>=</span> platform<span>;</span>
      <span>}</span> <span>else</span> <span>if</span> <span>(</span>config<span>.</span>method <span>===</span> <span>"get"</span><span>)</span> <span>{</span>
        config<span>.</span>params<span>.</span>languageCode <span>=</span> languageCode<span>;</span>
        config<span>.</span>params<span>.</span>countryCode <span>=</span> countryCode<span>;</span>
        config<span>.</span>params<span>.</span>platform <span>=</span> platform<span>;</span>
      <span>}</span>
    <span>}</span>
    <span>return</span> config<span>;</span> <span>//返回这个配置，否则整个请求不会进行</span>
  <span>}</span><span>,</span>
  <span>function</span> <span>(</span><span>error</span><span>)</span> <span>{</span>
    <span>return</span> Promise<span>.</span><span>reject</span><span>(</span><span>{</span>
      errMsg<span>:</span> error <span>||</span> <span>"404(TimeOut)"</span><span>,</span>
      errCode<span>:</span> <span>404</span><span>,</span>
    <span>}</span><span>)</span><span>;</span>
  <span>}</span>
<span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br></div></div></li>
</ul>
</li>
<li>
<p>响应拦截器：过滤响应数据、根据状态码进行统一处理，如弹窗提示、清除 token、token过期处理、清除loading缓冲效果等。</p>
<ul>
<li>注意：弹窗提示、清除 token、token过期处理等需要在浏览器端环境下，Node 环境(服务端渲染)下不需要样式和 token。（ Token 是用户登录相关数据，服务端渲染时不需要用户登录）</li>
<li>token 过期处理：首先刷新 token、根据结果判断，刷新失败则清除 token 、退出登录、清除用户数据；刷新成功重新保存新的 token。</li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>请求方法封装</strong>：封装 <code>request</code>  方法，在 request 方法上进行其他请求方法 (get、post、delete、put ...) 二次封装。不同请求方法参数不同，封装方法参数。</p>
<div><pre><code><span>export</span> <span>function</span> <span>post</span><span>(</span><span>url<span>,</span> data <span>=</span> <span>{</span><span>}</span><span>,</span> config <span>=</span> <span>{</span><span>}</span></span><span>)</span> <span>{</span>
  <span>return</span> <span>request</span><span>(</span><span>{</span>
    method<span>:</span> <span>"post"</span><span>,</span>
    url<span>,</span>
    data<span>,</span>
    config<span>,</span>
  <span>}</span><span>)</span><span>;</span>
<span>}</span>

<span>export</span> <span>function</span> <span>get</span><span>(</span><span>url<span>,</span> params <span>=</span> <span>{</span><span>}</span></span><span>)</span> <span>{</span>
  <span>return</span> <span>request</span><span>(</span><span>{</span>
    url<span>,</span>
    params<span>,</span>
  <span>}</span><span>)</span><span>;</span>
<span>}</span>

<span>export</span> <span>function</span> <span>put</span><span>(</span><span>url<span>,</span> data <span>=</span> <span>{</span><span>}</span></span><span>)</span> <span>{</span>
  <span>return</span> <span>request</span><span>(</span><span>{</span>
    method<span>:</span> <span>"put"</span><span>,</span>
    url<span>,</span>
    data<span>,</span>
  <span>}</span><span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br></div></div></li>
</ul>
<p><strong>注意：</strong></p>
<ul>
<li>默认实例配置、新建实例配置、拦截器、请求方法 request 中都可以处理 默认配置、默认参数、token处理等。</li>
<li>区别： 根据配置合并策略：请求的 <code>config</code> 参数 &gt;(覆盖) 实例的 <code>defaults</code> 属性 &gt; (覆盖) 库默认值。</li>
</ul>
<h3 id="api-管理"> API 管理</h3>
<p>目前前后端开发基本采用前端分离开发模式，前端 API 管理主要为前端在业务层 API 中的划分方式。</p>
<p>常见采用划分方式：</p>
<ul>
<li>按照 API 文档模块划分 API 模块：axios 统一封装后，按照页面/模块功能划分文件目录。
<ul>
<li><img src="./../images/接口管理.png" alt="" /></li>
</ul>
</li>
</ul>
<p>常见 API 文档管理工具：</p>
<ul>
<li>Swagger</li>
<li>YApi</li>
<li>Eolinker</li>
<li>ShowDoc</li>
<li>DOClever</li>
<li>RAP2</li>
</ul>
<h4 id="mock-数据"> MOCK 数据</h4>
<p>在时机项目开发中，经常遇到：</p>
<ul>
<li>前端开发依赖于后端接口数据，但是后台人员不足或者无法立即到位，前端迟迟不能开工；</li>
<li>前端参照 ui 设计图，完成对应的静态页面（没有数据交互），待后台人员到位，再进行二次开发，协助完成接口对接。</li>
</ul>
<p>因此，为了解决此类困境，前端通常可以使用 mock api 方式 在前后端同时开发的时候，后端接口数据没有出来，前端可以mock假数据，模拟开发。</p>
<p>前端 mock 数据方法：</p>
<ul>
<li>使用文档管理工具 Mock，前端代理到文档管理工具地址；</li>
<li>mock 同步接口文档并使用 <a href="https://github.com/nuysoft/Mock/wiki/Getting-Started" target="_blank" rel="noopener noreferrer">Mock.js</a> 本地启动 mock 服务；
<ul>
<li>优点： 可以通过修改本地 mock 数据测试页面显示结果，提高开发效率；</li>
</ul>
</li>
</ul>
<h2 id="参考文章"> 参考文章</h2>
<p><a href="https://www.axios-http.cn/docs/intro" target="_blank" rel="noopener noreferrer">axios文档</a></p>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-25T19:47:29.000Z</published>
  </entry>
  <entry>
    <title type="html">8.1 树</title>
    <id>https://sewar-x.github.io/algorithms/tree/</id>
    <link href="https://sewar-x.github.io/algorithms/tree/"/>
    <updated>2021-07-15T12:37:23.000Z</updated>
    <content type="html"><![CDATA[<h2 id="_8-1-树"> 8.1 树</h2>
<p>不同于我们上面介绍的线性结构，树是一种非线性结构。</p>
<p>图：</p>
<p><img src="./../images/tree.png" alt="图片" /></p>
<p>它遵循：</p>
<ul>
<li>仅有唯一一个根节点，没有节点则为空树</li>
<li>除根节点外，每个节点都有并仅有唯一一个父节点</li>
<li>节点间不能形成闭环</li>
</ul>
<p>这就是树！</p>
<p>树有几个概念：</p>
<ul>
<li>拥有相同父节点的节点，互称为兄弟节点</li>
<li><strong>节点的深度</strong> ：从根节点到该节点所经历的边的个数</li>
<li><strong>节点的高度</strong> ：节点到叶节点的最长路径</li>
<li>树的高度：根节点的高度</li>
</ul>
<p><img src="./../images/tree2.png" alt="图片" /></p>
<p>B、C、D就互称为兄弟节点，其中，节点B的高度为2，节点B的深度为 1，树的高度为3</p>
<h4 id="高度"> 高度</h4>
<p>树的高度计算公式：</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxFGsUgS1HpzIOw9oJQazjQFfShXicAXENfOmy9zCNQNMpe6ZqAfQtiaxxQ/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<p>下图每个节点值都代表来当前节点的高度：</p>
<p><img src="./../images/tree3.png" alt="图片" /></p>
<h2 id="_8-2-二叉树"> 8.2 二叉树</h2>
<p>二叉树，故名思义，最多仅有两个子节点的树（最多能分两个叉的树🤦‍♀️）：</p>
<p>图：</p>
<p><img src="./../images/tree4.png" alt="图片" /></p>
<h2 id="_8-3-平衡二叉树"> 8.3 平衡二叉树</h2>
<p>二叉树中，每一个节点的左右子树的高度相差不能大于 1，称为平衡二叉树。</p>
<p><img src="./../images/tree6.png" alt="图片" /></p>
<p>另外还有满二叉树、完全二叉树等：</p>
<ul>
<li><strong>满二叉树</strong>：除了叶结点外每一个结点都有左右子叶且叶子结点都处在最底层的二叉树</li>
<li><strong>完全二叉树</strong>：深度为h，除第 h 层外，其它各层 (1～h-1) 的结点数都达到最大个数，第h 层所有的结点都连续集中在最左边</li>
</ul>
<h2 id="_8-4-在代码中如何去表示一棵二叉树"> 8.4 在代码中如何去表示一棵二叉树</h2>
<h3 id="_8-4-1-链式存储法"> 8.4.1 链式存储法</h3>
<p>二叉树的存储很简单，在二叉树中，我们看到每个节点都包含三部分：</p>
<ul>
<li>当前节点的 val</li>
<li>左子节点 left</li>
<li>右子节点 right</li>
</ul>
<p>所以我们可以将每个节点定义为：</p>
<div><pre><code>function Node(val) {
    // 保存当前节点 key 值
    this.val = val
    // 指向左子节点
    this.left = null
    // 指向右子节点
    this.right = null
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div><p>一棵二叉树可以由根节点通过左右指针连接起来形成一个树。</p>
<div><pre><code>function BinaryTree() {
  let Node = function (val) {
    this.val = val
    this.left = null
    this.right = null
  }
  let root = null
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div><h3 id="_8-4-2-数组存储法-适用于完全二叉树"> 8.4.2 数组存储法（适用于完全二叉树）</h3>
<p>下图就是一棵完全二叉树，</p>
<p>如果我们把根节点存放在位置 <code>i=1</code> 的位置，则它的左子节点位置为 <code>2i = 2</code> ，右子节点位置为 <code>2i+1 = 3</code> 。</p>
<p>如果我们选取 B 节点 <code>i=2</code> ，则它父节点为 <code>i/2 = 1</code> ，左子节点 <code>2i=4</code> ，右子节点  <code>2i+1=5</code> 。</p>
<p>以此类推，我们发现所有的节点都满足这三种关系：</p>
<ul>
<li>位置为 i 的节点，它的父节点位置为 <code>i/2</code></li>
<li>它的左子节点 <code>2i</code></li>
<li>它的右子节点  <code>2i+1</code></li>
</ul>
<p>因此，如果我们把完全二叉树存储在数组里（从下标为 1 开始存储），我们完全可以通过下标找到任意节点的父子节点。从而完整的构建出这个完全二叉树。这就是数组存储法。</p>
<p>数组存储法相对于链式存储法不需要为每个节点创建它的左右指针，更为节省内存。</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxFLJ6Lw6LLgBjoiaQPugAzHNmeaRIWs4iblzkicKYZb9WicfcHT7CTxTrlyw/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<h2 id="_8-5-二叉树的遍历"> 8.5 二叉树的遍历</h2>
<p>二叉树的遍历可分为：</p>
<ul>
<li>前序遍历</li>
<li>中序遍历</li>
<li>后序遍历</li>
</ul>
<p>所谓前、中、后，不过是根的顺序，即也可以称为先根遍历、中根遍历、后根遍历</p>
<h4 id="_1-前序遍历"> 1. 前序遍历</h4>
<p>对于二叉树中的任意一个节点，先打印该节点，然后是它的左子树，最后右子树</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxFPOMatZwFr8CCnDm90jh0hn16ev4jc95TnMicCPDaGRiavWgkKXRMmhVw/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<h4 id="_2-中序遍历"> 2. 中序遍历</h4>
<p>对于二叉树中的任意一个节点，先打印它的左子树，然后是该节点，最后右子树</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxF9f2QueBnSgoQwVZnNOiaB9Qhhxp15OpeGyu5bXrAIAr8NBZ7PZoicEFQ/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<h4 id="_3-后序遍历"> 3. 后序遍历</h4>
<p>对于二叉树中的任意一个节点，先打印它的左子树，然后是右子树，最后该节点</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxFlP79J1xUwf55fUiasJJtBrBVBefeHtCB9oia8HCczJkMZicWy7vxIS9Rg/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<h4 id="_4-代码实现-前序遍历为例"> 4. 代码实现（前序遍历为例）</h4>
<p>所以，遍历二叉树的过程也就是一个递归的过程，例如前序遍历，先遍历根节点，然后是根的左子树，最后右子树；遍历根节点的左子树的时候，又是先遍历左子树的根节点，然后左子树的左子树，左子树的右子树…….</p>
<p>所以，它的核心代码就是：</p>
<div><pre><code>// 前序遍历核心代码
const preOrderTraverseNode = (node) =&gt; {
    if(node) {
        // 先根节点
        result.push(node.val)
        // 然后遍历左子树
        preOrderTraverseNode(node.left)
        // 再遍历右子树
        preOrderTraverseNode(node.right)
    }
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><p>完整代码如下：</p>
<h5 id="递归实现"> 递归实现</h5>
<div><pre><code>// 前序遍历
const preorderTraversal = (root) =&gt; {
    let result = []
    var preOrderTraverseNode = (node) =&gt; {
        if(node) {
            // 先根节点
            result.push(node.val)
            // 然后遍历左子树
            preOrderTraverseNode(node.left)
            // 再遍历右子树
            preOrderTraverseNode(node.right)
        }
    }
    preOrderTraverseNode(root)
    return result
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div><p>我们既然可以使用递归实现，那么是否也可以使用迭代实现喃？</p>
<h5 id="迭代实现"> 迭代实现</h5>
<p>利用栈来记录遍历的过程，实际上，递归就使用了调用栈，所以这里我们可以使用栈来模拟递归的过程</p>
<ul>
<li>首先根入栈</li>
<li>将根节点出栈，将根节点值放入结果数组中</li>
<li>然后遍历左子树、右子树，因为栈是先入后出，所以，我们先右子树入栈，然后左子树入栈</li>
<li>继续出栈（左子树被出栈）…….</li>
</ul>
<p>依次循环出栈遍历入栈，直到栈为空，遍历完成</p>
<div><pre><code>// 前序遍历
const preorderTraversal = (root) =&gt; {
    const list = [];
    const stack = [];
    
    // 当根节点不为空的时候，将根节点入栈
    if(root) stack.push(root)
    while(stack.length &gt; 0) {
        const curNode = stack.pop()
        // 第一步的时候，先访问的是根节点
        list.push(curNode.val)
        
        // 我们先打印左子树，然后右子树
        // 所以先加入栈的是右子树，然后左子树
        if(curNode.right !== null) {
            stack.push(curNode.right)
        }
        if(curNode.left !== null) {
            stack.push(curNode.left)
        }
    }
    return list
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br></div></div><h5 id="复杂度分析"> 复杂度分析：</h5>
<p>空间复杂度：O(n)</p>
<p>时间复杂度：O(n)</p>
<p>至此，我们已经实现了二叉树的前序遍历，尝试思考一下二叉树的中序遍历如何实现喃？</p>
<h2 id="_8-6-二叉查找树-bst树"> 8.6 二叉查找树（BST树）</h2>
<p>有的笔者也称它为二叉搜索树，都是一个意思。</p>
<p>二叉树本身没有多大的意义，直到有位大佬提出一个 trick。</p>
<p>如果我们规定一颗二叉树上的元素拥有顺序，所有比它小的元素在它的左子树，比它大的元素在它的右子树，那么我们不就可以很快地查找某个元素了吗？</p>
<p>不得不说这是一个非常天才的想法，于是，二叉查找树诞生了。</p>
<p>所以，二叉查找树与二叉树不同的是，它在二叉树的基础上，增加了对二叉树上节点存储位置的限制：二叉搜索树上的每个节点都需要满足：</p>
<ul>
<li>左子节点值小于该节点值</li>
<li>右子节点值大于等于该节点值</li>
</ul>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxF2hAghe2aqBqtibGia4ibd6Iwuicg05vOjPAEOMMCWG3yP3aGfxtQ5lsSCw/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<p>在二叉树中，所有子节点值都是没有固定规律的，所以使用二叉树存储结构存储数据时，查找数据的时间复杂度为 O(n)，因为它要查找每一个节点。</p>
<p>而使用二叉查找树就不同了，例如上图，我们如果要查找 6 ，先从根节点 10 比较，6 比 10 小，则查找左子树，再与 8 比较，6 比 8 小，继续查找 8 的左子树，也就是 6，我们找到了元素，结束。</p>
<h3 id="_8-6-1-基本操作"> 8.6.1 基本操作</h3>
<div><pre><code>function BinarySearchTree() {
  let Node = function (key) {
    this.key = key
    this.left = null
    this.right = null
  }
  let root = null
  
  // 插入
  this.insert = function(key){}
  
  // 查找
  this.search = function(key){}
  
  // 删除
  this.remove = function(key){}
  
  // 最大值
  this.max = function(){}
  
  // 最小值
  this.min = function(){}
  
  // 中序遍历
  this.inOrderTraverse = function(){}
  
  // 先序遍历
  this.preOrderTraverse = function(){}
  
  // 后序遍历
  this.postOrderTraverse = function(){}
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br></div></div><p><strong>插入：</strong></p>
<ul>
<li>首先创建一个新节点</li>
<li>判断树是否为空，为空则设置插入的节点为根节点，插入成功，返回</li>
<li>如果不为空，则比较该节点与根结点，比根小，插入左子树，否则插入右子树</li>
</ul>
<div><pre><code>function insert(key) {
  // 创建新节点
  let newNode = new Node(key)
  // 判断是否为空树
  if(root === null) {
    root = newNode
  } else {
    insertNode(root, newNode)
  }
}

// 将 insertNode 插入到 node 子树上
function insertNode(node, newNode) {
  if(newNode.key &lt; node.key) {
    // 插入 node 左子树
    if(node.left === null) {
      node.left = newNode
    } else {
      insertNode(node.left, newNode)
    }
  } else {
    // 插入 node 右子树
    if(node.right === null) {
      node.right = newNode
    } else {
      insertNode(node.right, newNode)
    }
  }
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br></div></div><p><strong>最值：</strong></p>
<p>最小值：树最左端的节点</p>
<p>最大值：树最右端的节点</p>
<div><pre><code>// 最小值
function min() {
  let node = root
  if(node) {
    while(node &amp;&amp; node.left !== null) {
      node = node.left
    }
    return node.key
  }
  return null
}

// 最大值
function max() {
  let node = root
  if(node) {
    while(node &amp;&amp; node.right !== null) {
      node = node.right
    }
    return node.key
  }
  return null
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br></div></div><p><strong>查找：</strong></p>
<div><pre><code>function search(key) {
  return searchNode(root, key)
}

function searchNode(node, key) {
  if(node === null) 
    return false
  if(key &lt; node.key) {
    return searchNode(node.left, key)
  } else if(key &gt; node.key) {
    return searchNode(node.right, key)
  } else {
    return true
  }
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div><p><strong>删除：</strong></p>
<div><pre><code>function remove(key) {
  root = removeNode(root, key)
}

function removeNode(node, key) {
  if(node === null) 
    return null
  if(key &lt; node.key) {
    return removeNode(node.left, key)
  } else if(key &gt; node.key) {
    return removeNode(node.right, key)
  } else {
    // key = node.key 删除
    //叶子节点
    if(node.left === null &amp;&amp; node.right === null) {
      node = null
      return node
    }
    // 只有一个子节点
    if(node.left === null) {
      node = node.right
      return node
    }
    if(node.right === null) {
      node = node.left
      return node
    }
    // 有两个子节点
    // 获取右子树的最小值替换当前节点
    let minRight = findMinNode(node.right)
    node.key = minRight.key
    node.right = removeNode(node.right, minRight.key)
    return node
  }
}

// 获取 node 树最小节点
function findMinNode(node) {
  if(node) {
    while(node &amp;&amp; node.right !== null) {
      node = node.right
    }
    return node
  }
  return null
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br></div></div><h3 id="_8-6-2-遍历"> 8.6.2 遍历</h3>
<p><strong>中序遍历：</strong></p>
<p>顾名思义，中序遍历就是把根放在中间的遍历，即按先左节点、然后根节点、最后右节点（左根右）的遍历方式。</p>
<p>由于BST树任意节点都大于左子节点值小于等于右子节点值的特性，所以 <strong>中序遍历其实是对🌲进行排序操作</strong> ，并且是按从小到大的顺序排序。</p>
<div><pre><code>function inOrderTraverse(callback) {
  inOrderTraverseNode(root, callback)
}

function inOrderTraverseNode(node, callback) {
  if(node !== null) {
    // 先遍历左子树
    inOrderTraverseNode(node.left, callback)
    // 然后根节点
    callback(node.key)
    // 再遍历右子树
    inOrderTraverseNode(node.right, callback)
  }
}

// callback 每次将遍历到的结果打印到控制台
function callback(key) {
  console.log(key)
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div><p><strong>先序遍历：</strong></p>
<p>已经实现的中序遍历，先序遍历就很简单了，它是按根左右的顺序遍历</p>
<div><pre><code>function preOrderTraverse() {
  preOrderTraverseNode(root, callback)
}

function preOrderTraverseNode(node, callback) {
  if(node !== null) {
    // 先根节点
    callback(node.key)
    // 然后遍历左子树
    preOrderTraverseNode(node.left, callback)
    // 再遍历右子树
    preOrderTraverseNode(node.right, callback)
  }
}

// callback 每次将遍历到的结果打印到控制台
function callback(key) {
  console.log(key)
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div><p><strong>后序遍历：</strong></p>
<p>后序遍历按照左右根的顺序遍历，实现也很简单。</p>
<div><pre><code>function postOrderTraverse() {
  postOrderTraverseNode(root, callback)
}

function postOrderTraverseNode(node, callback) {
  if(node !== null) {
    // 先遍历左子树
    postOrderTraverseNode(node.left, callback)
    // 然后遍历右子树
    postOrderTraverseNode(node.right, callback)
    // 最后根节点
    callback(node.key)
  }
}

// callback 每次将遍历到的结果打印到控制台
function callback(key) {
  console.log(key)
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div><h3 id="_8-6-3-bst树的局限"> 8.6.3 BST树的局限</h3>
<p>在理想情况下，二叉树每多一层，可以存储的元素都增加一倍。也就是说 n 个元素的二叉搜索树，对应的树高为 O(logn)。所以我们查找元素、插入元素的时间也为 O(logn)。</p>
<p>当然这是理想情况下，但在实际应用中，并不是那么理想，例如一直递增或递减的给一个二叉查找树插入数据，那么所有插入的元素就会一直出现在一个树的左节点上，数型结构就会退化为链表结构，时间复杂度就会趋于 O(n)，这是不好的。</p>
<p>而我们上面的平衡树就可以很好的解决这个问题，所以平衡二叉查找树（<strong>AVL树，下一章节探讨</strong>）由此诞生。</p>
<h2 id="_8-7-平衡二叉查找树-avl树"> 8.7 平衡二叉查找树（AVL树）</h2>
<p>故名思义，既满足左右子树高度不大于 1， 又满足任意节点值大于它的左子节点值，小于等于它的右子节点值。</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxFcS51rt3xibblWvOS961wTykvu3hTmHZphvXPfZgibhTczicpHAYax2BLA/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<p>AVL 这个名字的由来，是它的两个发明者G. M. Adelson-Velsky 和 Evgenii Landis 的缩写，AVL最初是他们两人在1962 年的论文「An algorithm for the organization of information」中提出来一种数据结构</p>
<h2 id="_8-8-红黑树"> 8.8 红黑树</h2>
<p>红黑树也是一种特殊的「二叉查找树」。</p>
<p>到目前为止我们学习的 AVL 树和即将学习的红黑树都是二叉查找树的变体，可见二叉查找树真的是非常重要基础二叉树，如果忘了它的定义可以先回头看看。</p>
<p>红黑树是一种比较难的数据结构，面试中很少有面试官让你手写一个红黑树，最多的话是考察你是否理解红黑树，以及为什么有了 AVL 树还需要红黑树，本部分就主要介绍这块。</p>
<h3 id="_8-8-1-什么是红黑树"> 8.8.1 什么是红黑树</h3>
<p>红黑树是一种自平衡（并不是绝对平衡）的二叉查找树，它除了满足二分查找树的特点外，还满足以下条件：</p>
<ul>
<li>节点是红色或黑色</li>
<li>根节点必须是黑色节点</li>
<li>所有的叶子节点都必须是值为 NULL 的黑节点</li>
<li>如果一个节点是红色的，则它两个子节点都是黑色的</li>
<li>从任一节点到达它的每个叶子节点的所有的路径，都有相同数目的黑色节点</li>
</ul>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxFQLSsLpAiajBpnPnkFcGydia0Ybz1u2cBcpZ68WAaNic1Ynnaw4byegicSA/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<p>很多人不理解为神马要有那么多条条框框，这里引用王争老师的说法：</p>
<blockquote>
<p>我们都玩过魔方吧，其实魔方的复原是有固定算法的，遇到哪几面是什么样的，你就对应转几下，只要跟着这个复原步骤，最终肯定能把魔方复原。红黑树也是的，它也有固定的条条框框，在插入、删除时也有固定的调整方案。</p>
</blockquote>
<p>这些条条框框保证红黑树的自平衡，保证红黑树从根节点到达每一个叶子节点的最长路径不会超过最短路径的 2 倍。</p>
<p>而节点的路径长度决定着对节点的查询效率，这样我们确保了，最坏情况下的查找、插入、删除操作的时间复杂度不超过 O(logn) ，并且有较高的插入和删除效率。</p>
<h3 id="_8-8-2-红黑树-vs-平衡二叉树-avl树"> 8.8.2 红黑树 VS 平衡二叉树（AVL树）</h3>
<ul>
<li>插入和删除操作，一般认为红黑树的删除和插入会比 AVL 树更快。因为，红黑树不像 AVL 树那样严格的要求平衡因子小于等于1，这就减少了为了达到平衡而进行的旋转操作次数，可以说是牺牲严格平衡性来换取更快的插入和删除时间。</li>
<li>红黑树不要求有不严格的平衡性控制，但是红黑树的特点，使得任何不平衡都会在三次旋转之内解决。而 AVL 树如果不平衡，并不会控制旋转操作次数，旋转直到平衡为止。</li>
<li>查找操作，AVL树的效率更高。因为 AVL 树设计比红黑树更加平衡，不会出现平衡因子超过 1 的情况，减少了树的平均搜索长度。</li>
</ul>
<h2 id="_8-9-trie-树"> 8.9 Trie 树</h2>
<h3 id="_8-9-1-什么是-trie-树"> 8.9.1 什么是 Trie 树</h3>
<p>Trie 树，也称为字典树或前缀树，顾名思义，它是用来处理字符串匹配问题的数据结构，以及用来解决集合中查找固定前缀字符串的数据结构。</p>
<h3 id="_8-9-2-trie树的应用-字符串匹配"> 8.9.2 Trie树的应用：字符串匹配</h3>
<p>在搜索引擎中输入关键字，搜索引擎都会弹出下拉框，显示各种关键字提示，例如必应：</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxF1YtFeKQR6VPen15VemvvKyoOwJJQgIQUPpPVYsbO2YQy6ib3oY57fnw/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<p>必应是如何处理这一过程的喃？</p>
<p>或者，假设我们有n个单词的数据集，任意输入一串字符，如何在数据集中快速匹配出具有输入字符前缀的单词？</p>
<p>这样类似的问题还有很多，在日常开发中，遇到类似的问题，我们应该如何去处理？选择怎样的数据结构与算法？尤其是遇到大规模数据时，如何更高效的处理？</p>
<p>最简单的方法就是暴力，将数据集中的每个字符串，逐个字符的匹配输入字符，所有字符都匹配上则前缀匹配成功。这种方式也是我们开发当中最常用，最简单的方式，时间复杂度为 <code>O(m*n)</code> ，其中 <code>m</code> 为输入字符串长度， <code>n</code> 为数据集规模。</p>
<p>这个时间复杂度是很高的，当 <code>n</code> 很大时，暴力法性能就会很差，此时必须重新寻找合适的算法。</p>
<p>我们知道在树上查找、插入都比较方便，一般时间复杂度只与树的高度相关，我们可以通过树结构来处理，也就是我们要说的 Trie 树。其实，引擎搜索关键字提示底层也是通过 Trie 树实现的。</p>
<p>举个例子：假设数据集有：<code>are</code> 、 <code>add</code> 、 <code>adopt</code> 、<code>set</code> 、<code>so</code> ，它构键过程：</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxFwib4cMt4Zf3K6sKHYM5hP5fCpASrquNQ4dlJIS6ialxZskh9sWgFzemw/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<p>当所以的字符串插入完成，Trie树就构建完成了。</p>
<p><strong>Trie树的本质</strong>是利用字符串的公共前缀，将重复的前缀合并在一起，其中根节点不包含任何信息，每个节点表示一个字符串中的字符，从根节点到叶节点的路径，表示一个字符串。</p>
<p>在字符串匹配的时候，我们只要按照树的结构从上到下匹配即可。</p>
<h2 id="_8-10-b-树、b-树-感兴趣进"> 8.10 B 树、B+ 树（感兴趣进）</h2>
<h3 id="_8-10-1-多叉查找树"> 8.10.1 多叉查找树</h3>
<p>既然二叉查找树已经理解了，那多叉查找树就很好理解了，它与二叉查找树唯一不同的是，它是多叉的。也就是说，多叉查找树允许一个节点存储多个元素，并且可以拥有多个子树。</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxFZCeKjZrdgIVAQoo9AALudjrlS5aG2ibKjzC2n614PhqnebC3icBgetIQ/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<p>为什么在有二叉查找树的情况下，还要有多叉查找树喃？</p>
<p>我们知道树的深度越高，时间复杂度越高，性能越差，多叉查找树相对于二叉查找树来说，每个节点不止能拥有两个子节点，每层存放的节点数可比二叉查找树多，自然多叉查找树的的深度就要更小，性能也就更好。例如主要用于各大存储文件系统与数据库系统中的 B 树。</p>
<h3 id="_8-10-2-b-树-b-tree"> 8.10.2 B 树(B-tree)</h3>
<p>B 树，又称平衡多叉查找树。它是一种自平衡的多叉查找树。</p>
<p>如果一棵多路查找树满足以下规则，则称之为 B 树：</p>
<ul>
<li>排序方式：所有节点关键字是按递增次序排列，并遵循左小右大原则；</li>
<li>子节点数：非叶节点的子节点数&gt;1，且&lt;=M ，且M&gt;=2，空树除外（注：M阶代表一个树节点最多有多少个查找路径，M=M叉（或路），当M=2则是2叉树，M=3则是3叉）；</li>
<li>关键字数：枝节点（非根非叶）的关键字数量（K）应满足 (M+1)/2  &lt; K &lt; M</li>
<li>所有叶子节点均在同一层、叶子节点除了包含了关键字和关键字记录的指针外也有指向其子节点的指针只不过其指针地址都为null对应下图最后一层节点的空格子;</li>
</ul>
<p>最后，我们使用一张图加深一下理解：</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxFAQVuTIwichBvt6jUeyRVj0rxYlPyKj71WW51c6oic1UgFWr30qJHUrWw/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<h3 id="_8-10-3-b-树"> 8.10.3 B+树</h3>
<p>B+树与B树一样都是多叉平衡查找树（又名多路平衡查找树），B+树与B树不同的是：</p>
<ul>
<li>
<p>B+树改进了B树，让内结点（非叶节点）只作索引使用，去掉了其中指向data的指针，使得每个结点中能够存放更多的key， 因此能有更大的出度。</p>
<p>这有什么用？这样就意味着存放同样多的key，树的层高能进一步被压缩，使得检索的时间更短</p>
</li>
<li>
<p>中间节点的元素数量与子树一致，而B树子树数量与元素数量多1</p>
</li>
<li>
<p>叶子节点是一个链表，可以通过指针顺序查找</p>
</li>
</ul>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxFdI1ohgia2Ct2PYB4m9ZA4Zm6Bfy1dredTMqia2352obFzyv52EZpZFug/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<h2 id="_8-11-加深"> 8.11 加深</h2>
<h3 id="_8-11-1-二叉树的中序遍历"> 8.11.1 二叉树的中序遍历</h3>
<p><strong>递归实现：</strong></p>
<div><pre><code>// 中序遍历
const inorderTraversal = (root) =&gt; {
    let result = []
    var inorderTraversal = (node) =&gt; {
        if(node) {
            // 先遍历左子树
            inorderTraversal(node.left)
           // 再根节点
            result.push(node.val)
            // 最后遍历右子树
            inorderTraversal(node.right)
        }
    }
    inorderTraversal(root)
    return result
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div><p><strong>迭代实现：</strong></p>
<div><pre><code>const inorderTraversal = function(root) {
    let list = []
    let stack = []
    let node = root
    while(stack.length || node) { 
        if(node) {
            stack.push(node)
            node = node.left
            continue
        }
        node = stack.pop()
        list.push(node.val)
        node = node.right
    }
    return list
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div><p>进一步简化：</p>
<div><pre><code>// 中序遍历
const inorderTraversal = (root) =&gt; {
    let list = []
    let stack = []
    let node = root
    
    while(node || stack.length) {
    // 遍历左子树
      while(node) {
       stack.push(node)
        node = node.left
      }
      
      node = stack.pop()
      list.push(node.val)
      node = node.right
    }
    return list
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div><p><strong>复杂度分析：</strong></p>
<ul>
<li>空间复杂度：O(n)</li>
<li>时间复杂度：O(n)</li>
</ul>
<p>更多解答</p>
<h3 id="_8-11-2-二叉树的后序遍历"> 8.11.2 二叉树的后序遍历</h3>
<p><strong>递归实现</strong></p>
<div><pre><code>// 后序遍历
const postorderTraversal = function(root) {
    let result = []
    var postorderTraversalNode = (node) =&gt; {
        if(node) {
            // 先遍历左子树
            postorderTraversalNode(node.left)
            // 再遍历右子树
            postorderTraversalNode(node.right)
            // 最后根节点
            result.push(node.val)
        }
    }
    postorderTraversalNode(root)
    return result
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div><p><strong>迭代实现</strong></p>
<p><strong>解题思路：</strong> 后序遍历与前序遍历不同的是：</p>
<ul>
<li>后序遍历是左右根</li>
<li>而前序遍历是根左右</li>
</ul>
<p>如果我们把前序遍历的 <code>list.push(node.val)</code> 变更为 <code>list.unshift(node.val)</code> （遍历结果逆序），那么遍历顺序就由 <strong>根左右</strong> 变更为 <strong>右左根</strong></p>
<p>然后我们仅需将  <strong>右左根</strong> 变更为  <strong>左右根</strong> 即可完成后序遍</p>
<div><pre><code>// 后序遍历
const postorderTraversal = (root) =&gt; {
    const list = [];
    const stack = [];
    
    // 当根节点不为空的时候，将根节点入栈
    if(root) stack.push(root)
    while(stack.length &gt; 0) {
        const node = stack.pop()
        // 根左右=&gt;右左根
        list.unshift(node.val)
        
        // 先进栈左子树后右子树
        // 出栈的顺序就变更为先右后左
        // 右先头插法入list
        // 左再头插法入list
        // 实现右左根=&gt;左右根
        if(node.left !== null) {
            stack.push(node.left)
        }
        if(node.right !== null) {
            stack.push(node.right)
        }
    }
    return list
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br></div></div><p><strong>复杂度分析：</strong></p>
<ul>
<li>空间复杂度：O(n)</li>
<li>时间复杂度：O(n)</li>
</ul>
<p>更多解答</p>
<h3 id="_8-11-3-二叉树的层次遍历"> 8.11.3 二叉树的层次遍历</h3>
<p>给定一个二叉树，返回其节点值自底向上的层次遍历。（即按从叶子节点所在层到根节点所在的层，逐层从左向右遍历）</p>
<p>例如：给定二叉树 <code>[3,9,20,null,null,15,7]</code> ,</p>
<div><pre><code>    3
   / \
  9  20
    /  \
   15   7
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><p>返回其自底向上的层次遍历为：</p>
<div><pre><code>[
  [15,7],
  [9,20],
  [3]
]
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><h4 id="解法一-bfs-广度优先遍历"> 解法一：BFS（广度优先遍历）</h4>
<p>BFS 是按层层推进的方式，遍历每一层的节点。题目要求的是返回每一层的节点值，所以这题用 BFS 来做非常合适。BFS 需要用队列作为辅助结构，我们先将根节点放到队列中，然后不断遍历队列。</p>
<div><pre><code>const levelOrderBottom = function(root) {
    if(!root) return []
    let res = [], 
        queue = [root]
    while(queue.length) {
        let curr = [],
            temp = []
        while(queue.length) {
            let node = queue.shift()
            curr.push(node.val)
            if(node.left) temp.push(node.left)
            if(node.right) temp.push(node.right)
        }
        res.push(curr)
        queue = temp
    }
    return res.reverse()
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br></div></div><p><strong>复杂度分析</strong></p>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(n)</li>
</ul>
<h4 id="解法二-dfs-深度优先遍历"> 解法二：DFS（深度优先遍历）</h4>
<p>DFS 是沿着树的深度遍历树的节点，尽可能深地搜索树的分支</p>
<p>DFS 做本题的主要问题是：DFS 不是按照层次遍历的。为了让递归的过程中同一层的节点放到同一个列表中，在递归时要记录每个节点的深度 <code>depth</code> 。递归到新节点要把该节点放入 <code>depth</code> 对应列表的末尾。</p>
<p>当遍历到一个新的深度 <code>depth</code> ，而最终结果 <code>res</code> 中还没有创建 <code>depth</code> 对应的列表时，应该在 <code>res</code> 中新建一个列表用来保存该 <code>depth</code> 的所有节点。</p>
<div><pre><code>const levelOrderBottom = function(root) {
    const res = []
    var dep = function (node, depth){
        if(!node) return
        res[depth] = res[depth]||[]
        res[depth].push(node.val)
        dep(node.left, depth + 1)
        dep(node.right, depth + 1)
    }
    dep(root, 0)
    return res.reverse()   
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div><p><strong>复杂度分析：</strong></p>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(h)，h为树的高度</li>
</ul>
<p>更多解答</p>
<h3 id="_8-11-4-二叉树的层序遍历"> 8.11.4 二叉树的层序遍历</h3>
<p>给你一个二叉树，请你返回其按 <strong>层序遍历</strong> 得到的节点值。（即逐层地，从左到右访问所有节点）。</p>
<p>**示例：**二叉树：<code>[3,9,20,null,null,15,7]</code> ,</p>
<div><pre><code>    3
   / \
  9  20
    /  \
   15   7
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><p>返回其层次遍历结果：</p>
<div><pre><code>[
  [3],
  [9,20],
  [15,7]
]
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><p>感谢 <strong>@plane-hjh</strong> 的解答：</p>
<p>这道题和二叉树的层次遍历相似，只需要把 <code>reverse()</code> 去除就可以了</p>
<p><strong>广度优先遍历</strong></p>
<div><pre><code>const levelOrder = function(root) {
    if(!root) return []
    let res = [], 
        queue = [root]
    while(queue.length) {
        let curr = [],
            temp = []
        while(queue.length) {
            let node = queue.shift()
            curr.push(node.val)
            if(node.left) temp.push(node.left)
            if(node.right) temp.push(node.right)
        }
        res.push(curr)
        queue = temp
    }
    return res
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br></div></div><p><strong>深度优先遍历</strong></p>
<div><pre><code>const levelOrder = function(root) {
    const res = []
    var dep = function (node, depth){
        if(!node) return
        res[depth] = res[depth]||[]
        res[depth].push(node.val)
        dep(node.left, depth + 1)
        dep(node.right, depth + 1)
    }
    dep(root, 0)
    return res
}; 
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div><p>更多解答</p>
<h3 id="_8-11-5-重构二叉树-从前序与中序遍历序列构造二叉树"> 8.11.5 重构二叉树：从前序与中序遍历序列构造二叉树</h3>
<p>输入某二叉树的前序遍历和中序遍历的结果，请重建该二叉树。假设输入的前序遍历和中序遍历的结果中都不含重复的数字。</p>
<p>**注意:**你可以假设树中没有重复的元素。</p>
<p>例如，给出</p>
<div><pre><code>前序遍历 preorder = [3,9,20,15,7]
中序遍历 inorder = [9,3,15,20,7]
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div><p>返回如下的二叉树：</p>
<div><pre><code>    3
   / \
  9  20
    /  \
   15   7
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><p><strong>限制：</strong></p>
<div><pre><code>0 &lt;= 节点个数 &lt;= 5000
</code></pre>
<div><span>1</span><br></div></div><p>推荐 <strong>@luweiCN</strong> 的解答：</p>
<p>仔细分析前序遍历和中序遍历可以知道（以题目为例）：</p>
<ol>
<li>前序遍历的第一个元素一定是根节点，这里是<code>3</code></li>
<li>找到根节点之后，根节点在中序遍历中把数组一分为二，即两个数组<code>[9]</code>和<code>[15, 20, 7]</code>，这两个数组分别是根节点<code>3</code>的左子树和右子树的中序遍历。</li>
<li>前序遍历数组去掉根节点之后是<code>[9,20,15,7]</code>，而这个数组的第1项<code>[9]</code>（左子树的大小）和后3项<code>[20, 15, 7]</code>（右子树的大小）又分别是左子树和右子树的前序遍历 到这里已经很明显了，用递归</li>
</ol>
<div><pre><code>function TreeNode(val) {
    this.val = val;
    this.left = this.right = null;
}
const buildTree = function (preorder, inorder) {
    if (preorder.length) {
        let head = new TreeNode(preorder.shift());
        let index = inorder.indexOf(head.val);
        head.left = buildTree(
            preorder.slice(0, index),
            inorder.slice(0, index)
        );
        head.right = buildTree(preorder.slice(index), inorder.slice(index + 1));
        // 这里要注意，preorder前面shift一次长度比inorder小1
        return head;
    } else {
        return null;
    }
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div><p>更多解答</p>
<h3 id="_8-11-6-二叉树的最大深度"> 8.11.6 二叉树的最大深度</h3>
<p>给定一个二叉树，找出其最大深度。</p>
<p>二叉树的深度为根节点到最远叶子节点的最长路径上的节点数。</p>
<p><strong>说明:</strong> 叶子节点是指没有子节点的节点。</p>
<p>**示例：**给定二叉树 <code>[3,9,20,null,null,15,7]</code> ，</p>
<div><pre><code>    3
   / \
  9  20
    /  \
   15   7
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><p>返回它的最大深度 3</p>
<p><strong>解答：递归</strong></p>
<div><pre><code>const maxDepth = function(root) {
    if(!root) return 0 
    return 1 + Math.max(maxDepth(root.left), maxDepth(root.right))
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br></div></div><p><strong>复杂度分析：</strong></p>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(log⁡n)</li>
</ul>
<p>更多解答</p>
<h3 id="_8-11-7-二叉树的最近公共祖先"> 8.11.7 二叉树的最近公共祖先</h3>
<p>给定一个二叉树, 找到该树中两个指定节点的最近公共祖先。</p>
<p>百度百科中最近公共祖先的定义为：“对于有根树 T 的两个结点 p、q，最近公共祖先表示为一个结点 x，满足 x 是 p、q 的祖先且 x 的深度尽可能大（一个节点也可以是它自己的祖先）。”</p>
<p>例如，给定如下二叉树: root = [3,5,1,6,2,0,8,null,null,7,4]</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxFctcH7y2y8LuNOxFU1henb0bjbqNwLdVxRlr2LOR2pRfibHUicLT5VgDg/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<p><strong>示例 1:</strong></p>
<div><pre><code>输入: root = [3,5,1,6,2,0,8,null,null,7,4], p = 5, q = 1
输出: 3
解释: 节点 5 和节点 1 的最近公共祖先是节点 3。
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><p><strong>示例 2:</strong></p>
<div><pre><code>输入: root = [3,5,1,6,2,0,8,null,null,7,4], p = 5, q = 4
输出: 5
解释: 节点 5 和节点 4 的最近公共祖先是节点 5。因为根据定义最近公共祖先节点可以为节点本身。
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><p><strong>说明</strong>:</p>
<ul>
<li>所有节点的值都是唯一的。</li>
<li>p、q 为不同节点且均存在于给定的二叉树中。</li>
</ul>
<p><strong>解答：递归实现</strong></p>
<p><strong>解题思路：</strong></p>
<p>如果树为空树或 <code>p</code> 、 <code>q</code> 中任一节点为根节点，那么 <code>p</code> 、 <code>q</code> 的最近公共节点为根节点</p>
<p>如果不是，即二叉树不为空树，且 <code>p</code> 、 <code>q</code> 为非根节点，则递归遍历左右子树，获取左右子树的最近公共祖先，</p>
<ul>
<li>如果  <code>p</code> 、 <code>q</code> 节点在左右子树的最近公共祖先都存在，说明 <code>p</code> 、 <code>q</code> 节点分布在左右子树的根节点上，此时二叉树的最近公共祖先为 <code>root</code></li>
<li>若 <code>p</code> 、 <code>q</code> 节点在左子树最近公共祖先为空，那 <code>p</code> 、<code>q</code> 节点位于左子树上，最终二叉树的最近公共祖先为右子树上 <code>p</code> 、<code>q</code> 节点的最近公共祖先</li>
<li>若 <code>p</code> 、 <code>q</code> 节点在右子树最近公共祖先为空，同左子树 <code>p</code> 、 <code>q</code> 节点的最近公共祖先为空一样的判定逻辑</li>
<li>如果  <code>p</code> 、 <code>q</code> 节点在左右子树的最近公共祖先都为空，则返回 <code>null</code></li>
</ul>
<p><strong>代码实现：</strong></p>
<div><pre><code>const lowestCommonAncestor = function(root, p, q) {
    if(root == null || root == p || root == q) return root
    const left = lowestCommonAncestor(root.left, p, q)
    const right = lowestCommonAncestor(root.right, p, q)
    if(left === null) return right
    if(right === null) return left
    return root
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div><p><strong>复杂度分析：</strong></p>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(n)</li>
</ul>
<p>更多解答</p>
<h3 id="_8-11-8-平衡二叉树"> 8.11.8 平衡二叉树</h3>
<p>给定一个二叉树，判断它是否是高度平衡的二叉树。</p>
<p>本题中，一棵高度平衡二叉树定义为：</p>
<blockquote>
<p>一个二叉树每个节点 的左右两个子树的高度差的绝对值不超过1。</p>
</blockquote>
<p><strong>示例 1:</strong></p>
<p>给定二叉树 <code>[3,9,20,null,null,15,7]</code></p>
<div><pre><code>    3
   / \
  9  20
    /  \
   15   7
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><p>返回 <code>true</code> 。</p>
<p><strong>示例 2:</strong></p>
<p>给定二叉树 <code>[1,2,2,3,3,null,null,4,4]</code></p>
<div><pre><code>      1
     / \
    2   2
   / \
  3   3
 / \
4   4
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div><p>返回 <code>false</code> 。</p>
<h4 id="解答一-自顶向下-暴力法"> 解答一：自顶向下（暴力法）</h4>
<p><strong>解题思路：</strong> 自顶向下的比较每个节点的左右子树的最大高度差，如果二叉树中每个节点的左右子树最大高度差小于等于 <code>1</code> ，即每个子树都平衡时，此时二叉树才是平衡二叉树</p>
<p><strong>代码实现：</strong></p>
<div><pre><code>const isBalanced = function (root) {
  if(!root) return true
  return Math.abs(depth(root.left) - depth(root.right)) &lt;= 1
        &amp;&amp; isBalanced(root.left)
        &amp;&amp; isBalanced(root.right)
}
const depth = function (node) {
    if(!node) return -1
    return 1 + Math.max(depth(node.left), depth(node.right))
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div><p><strong>复杂度分析：</strong></p>
<ul>
<li>时间复杂度：O(nlogn)，计算 <code>depth</code> 存在大量冗余操作</li>
<li>空间复杂度：O(n)</li>
</ul>
<h4 id="解答二-自底向上-优化"> 解答二：自底向上（优化）</h4>
<p><strong>解题思路：</strong> 利用后续遍历二叉树（左右根），从底至顶返回子树最大高度，判定每个子树是不是平衡树 ，如果平衡，则使用它们的高度判断父节点是否平衡，并计算父节点的高度，如果不平衡，返回 <code>-1</code> 。</p>
<p>遍历比较二叉树每个节点 的左右子树深度：</p>
<ul>
<li>比较左右子树的深度，若差值大于 <code>1</code> 则返回一个标记 <code>-1</code> ，表示当前子树不平衡</li>
<li>左右子树有一个不是平衡的，或左右子树差值大于 <code>1</code> ，则二叉树不平衡</li>
<li>若左右子树平衡，返回当前树的深度（左右子树的深度最大值 <code>+1</code> ）</li>
</ul>
<p><strong>代码实现：</strong></p>
<div><pre><code>const isBalanced = function (root) {
    return balanced(root) !== -1
};
const balanced = function (node) {
    if (!node) return 0
    const left = balanced(node.left)
    const right = balanced(node.right)
    if (left === -1 || right === -1 || Math.abs(left - right) &gt; 1) {
        return -1
    }
    return Math.max(left, right) + 1
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div><p><strong>复杂度分析：</strong></p>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(n)</li>
</ul>
<p>更多解答</p>
<h3 id="_8-11-9-路径总和"> 8.11.9 路径总和</h3>
<p>给定一个二叉树和一个目标和，判断该树中是否存在根节点到叶子节点的路径，这条路径上所有节点值相加等于目标和。</p>
<p><strong>说明:</strong> 叶子节点是指没有子节点的节点。</p>
<p><strong>示例:</strong> 给定如下二叉树，以及目标和 <code>sum = 22</code> ，</p>
<div><pre><code>          5
         / \
        4   8
       /   / \
      11  13  4
     /  \      \
    7    2      1
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div><p>返回 <code>true</code> , 因为存在目标和为 <code>22</code> 的根节点到叶子节点的路径 <code>5-&gt;4-&gt;11-&gt;2</code>。</p>
<p><strong>解题思路：</strong></p>
<p>只需要遍历整棵树</p>
<ul>
<li>如果当前节点不是叶子节点，递归它的所有子节点，传递的参数就是 sum 减去当前的节点值；</li>
<li>如果当前节点是叶子节点，判断参数 sum 是否等于当前节点值，如果相等就返回 true，否则返回 false。</li>
</ul>
<p><strong>代码实现：</strong></p>
<div><pre><code>var hasPathSum = function(root, sum) {
  // 根节点为空
  if (root === null) return false;
  
  // 叶节点 同时 sum 参数等于叶节点值
  if (root.left === null &amp;&amp; root.right === null) return root.val === sum;

  // 总和减去当前值，并递归
  sum = sum - root.val
  return hasPathSum(root.left, sum) || hasPathSum(root.right, sum);
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><p>更多解答</p>
<h3 id="_8-11-10-对称二叉树"> 8.11.10 对称二叉树</h3>
<p>给定一个二叉树，检查它是否是镜像对称的。</p>
<p>例如，二叉树 <code>[1,2,2,3,4,4,3]</code> 是对称的。</p>
<div><pre><code>    1
   / \
  2   2
 / \ / \
3  4 4  3
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><p>但是下面这个 <code>[1,2,2,null,3,null,3]</code> 则不是镜像对称的:</p>
<div><pre><code>    1
   / \
  2   2
   \   \
   3    3
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><p><strong>进阶：</strong></p>
<p>你可以运用递归和迭代两种方法解决这个问题吗？</p>
<p><strong>解答：</strong></p>
<p>一棵二叉树对称，则需要满足：根的左右子树是镜像对称的</p>
<p>也就是说，每棵树的左子树都和另外一颗树的右子树镜像对称，左子树的根节点值与右子树的根节点值相等</p>
<p><img src="https://mmbiz.qpic.cn/mmbiz_png/bwG40XYiaOKkeAcq7DMpxvCDrsUJulZxFDry0IxgNibAu9b81l7XGy3oV6Y5tUvha0OJ7EBS49nk8lpxsvkAU8UQ/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1" alt="图片" /></p>
<p>所以，我们需要比较：</p>
<ul>
<li>左右子树的根节点值是否相等</li>
<li>左右子树是否镜像对称</li>
</ul>
<p>边界条件：</p>
<ul>
<li>左右子树都为 <code>null</code> 时，返回 <code>true</code></li>
<li>左右子树有一个 <code>null</code> 时，返回 <code>false</code></li>
</ul>
<h4 id="解法一-递归"> 解法一：递归</h4>
<div><pre><code>const isSymmetric = function(root) {
    if(!root) return true
    var isEqual = function(left, right) {
        if(!left &amp;&amp; !right) return true
        if(!left || !right) return false
        return left.val === right.val
         &amp;&amp; isEqual(left.left, right.right)
         &amp;&amp; isEqual(left.right, right.left)
    }
    return isEqual(root.left, root.right)
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><p><strong>复杂度分析：</strong></p>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(n)</li>
</ul>
<h4 id="解法二-迭代"> 解法二：迭代</h4>
<p>利用栈来记录比较的过程，实际上，递归就使用了调用栈，所以这里我们可以使用栈来模拟递归的过程</p>
<ul>
<li>首先根的左右子树入栈</li>
<li>将左右子树出栈，比较两个数是否互为镜像</li>
<li>如果左右子树的根节点值相等，则将左子树的 <code>left</code> 、右子树的 <code>right</code> 、左子树的 <code>right</code> 、右子树的 <code>left</code> 依次入栈</li>
<li>继续出栈（一次出栈两个进行比较）…….</li>
</ul>
<p>依次循环出栈入栈，直到栈为空</p>
<div><pre><code>const isSymmetric = function(root) {
    if(!root) return true
    let stack = [root.left, root.right]
    while(stack.length) {
        let right = stack.pop()
        let left = stack.pop()
        if(left &amp;&amp; right) {
            if(left.val !== right.val) return false
            stack.push(left.left)
            stack.push(right.right)
            stack.push(left.right)
            stack.push(right.left)
        } else if(left || right) {
            return false
        }
    }
    return true
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br></div></div><p><strong>复杂度分析：</strong></p>
<ul>
<li>时间复杂度：O(n)</li>
<li>空间复杂度：O(n)</li>
</ul>
<p>剑指Offer&amp;leetcode101：对称二叉树</p>
<h3 id="_8-11-11给定一个二叉树-找到该树中两个指定节点间的最短距离"> 8.11.11给定一个二叉树, 找到该树中两个指定节点间的最短距离</h3>
<blockquote>
<p>function TreeNode(val) { this.val = val; this.left = this.right = null; }</p>
</blockquote>
<p><strong>解答：</strong></p>
<p>求最近公共祖先节点，然后再求最近公共祖先节点到两个指定节点的路径，再求两个节点的路径之和</p>
<div><pre><code>const shortestDistance = function(root, p, q) {
    // 最近公共祖先
    let lowestCA = lowestCommonAncestor(root, p, q)
    // 分别求出公共祖先到两个节点的路经
    let pDis = [], qDis = []
    getPath(lowestCA, p, pDis)
    getPath(lowestCA, q, qDis)
    // 返回路径之和
    return (pDis.length + qDis.length)
}

// 最近公共祖先
const lowestCommonAncestor = function(root, p, q) {
    if(root === null || root === p || root === q) return root
    const left = lowestCommonAncestor(root.left, p, q)
    const right = lowestCommonAncestor(root.right, p, q)
    if(left === null) return right
    if(right === null) return left
    return root
}

const getPath = function(root, p, paths) {
    // 找到节点，返回 true
    if(root === p) return true
    // 当前节点加入路径中
    paths.push(root)
    let hasFound = false
    // 先找左子树
    if (root.left !== null)
        hasFound = getPath(root.left, p, paths)
    // 左子树没有找到，再找右子树
    if (!hasFound &amp;&amp; root.right !== null)
        hasFound = getPath(root.right, p, paths)
    // 没有找到，说明不在这个节点下面，则弹出
    if (!hasFound)
        paths.pop()
    return hasFound
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br></div></div><p>更多解答</p>
<h3 id="_8-11-12-二叉搜索树中第k小的元素"> 8.11.12 二叉搜索树中第K小的元素</h3>
<p>给定一个二叉搜索树，编写一个函数 <code>kthSmallest</code> 来查找其中第 <code>k</code> 个最小的元素。</p>
<p>**说明：**你可以假设 k 总是有效的，1 ≤ k ≤ 二叉搜索树元素个数。</p>
<p><strong>示例 1:</strong></p>
<div><pre><code>输入: root = [3,1,4,null,2], k = 1
   3
  / \
 1   4
  \
   2
输出: 1
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div><p><strong>示例 2:</strong></p>
<div><pre><code>输入: root = [5,3,6,2,4,null,null,1], k = 3
       5
      / \
     3   6
    / \
   2   4
  /
 1
输出: 3
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div><p>**进阶：**如果二叉搜索树经常被修改（插入/删除操作）并且你需要频繁地查找第 k 小的值，你将如何优化 kthSmallest 函数？</p>
<p><strong>解答：</strong></p>
<p>我们知道：中序遍历其实是对🌲进行排序操作 ，并且是按从小到大的顺序排序，所以本题就很简单了</p>
<p><strong>解题思路：</strong> 中序遍历二叉搜索树，输出第 k 个既可</p>
<p><strong>代码实现（递归）：</strong></p>
<div><pre><code>const kthSmallest = function(root, k) {
    let res = null
    let inOrderTraverseNode = function(node) {
        if(node !== null &amp;&amp; k &gt; 0) {
            // 先遍历左子树
            inOrderTraverseNode(node.left)
            // 然后根节点
            if(--k === 0) {
                res = node.val
                return 
            }
            // 再遍历右子树
            inOrderTraverseNode(node.right)
        }
    }
    inOrderTraverseNode(root)
    return res
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br></div></div><p><strong>复杂度分析：</strong></p>
<ul>
<li>时间复杂度：O(k)</li>
<li>空间复杂度：不考虑递归栈所占用的空间，空间复杂度为 O(1)</li>
</ul>
<p><strong>代码实现（迭代）：</strong></p>
<div><pre><code>const kthSmallest = function(root, k) {
    let stack = []
    let node = root
    
    while(node || stack.length) {
        // 遍历左子树
        while(node) {
            stack.push(node)
            node = node.left
        }
      
        node = stack.pop()
        if(--k === 0) {
            return node.val
        }
        node = node.right
    }
    return null
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div><p><strong>复杂度分析：</strong></p>
<ul>
<li>时间复杂度：O(H+K)</li>
<li>空间复杂度：空间复杂度为 O(H+K)</li>
</ul>
<p>更多解答</p>
<h3 id="_8-11-13-实现-trie-前缀树"> 8.11.13 实现 Trie（前缀树）</h3>
<p>实现一个 Trie (前缀树)，包含 <code>insert</code> , <code>search</code> , 和 <code>startsWith</code> 这三个操作。</p>
<p><strong>示例:</strong></p>
<div><pre><code>Trie trie = new Trie();

trie.insert(&quot;apple&quot;);
trie.search(&quot;apple&quot;);   // 返回 true
trie.search(&quot;app&quot;);     // 返回 false
trie.startsWith(&quot;app&quot;); // 返回 true
trie.insert(&quot;app&quot;);   
trie.search(&quot;app&quot;);     // 返回 true
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div><p><strong>说明:</strong></p>
<ul>
<li>你可以假设所有的输入都是由小写字母 a-z 构成的。</li>
<li>保证所有输入均为非空字符串。</li>
</ul>
<p><strong>解答：</strong></p>
<p>我们可以搭建一个初始 Trie 树结构：</p>
<div><pre><code>// Trie 树
var Trie = function() {};
// 插入
Trie.prototype.insert = function(word) {};
// 搜索
Trie.prototype.search = function(word) {};
// 前缀匹配
Trie.prototype.startsWith = function(prefix) {};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div><p><strong>1. 如何存储一个 Trie 树</strong></p>
<p>首先，我们需要实现一个 Trie 树，我们知道，二叉树的存储（链式存储）是通过左右指针来实现的，即二叉树中的节点：</p>
<div><pre><code>function BinaryTreeNode(key) {
    // 保存当前节点 key 值
    this.key = key
    // 指向左子节点
    this.left = null
    // 指向右子节点
    this.right = null
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div><p>在这里，它不止有两个字节点，它最多有 a-z 总共有26个子节点。最简单的实现是把她们保存在一个字典里：</p>
<ul>
<li><code>isEnd</code> ：当前是否是结束节点</li>
<li><code>children</code> ：当前节点的子节点，这里我们使用 <code>key:value</code> 形式实现，<code>key</code> 为子节点字符，<code>value</code> 指向子节点的孩子节点</li>
</ul>
<div><pre><code>var TrieNode = function() {
    // next 放入当前节点的子节点
    this.next = {};
    // 当前是否是结束节点
    this.isEnd = false;
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div><p>所以：</p>
<div><pre><code>// Trie 树
var Trie = function() {
    this.root = new TrieNode()
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br></div></div><p><strong>2. 插入</strong></p>
<ul>
<li>首先判断插入节点是否为空，为空则返回</li>
<li>遍历待插入字符，从根节点逐字符查找 Trie 树，如果字符查找失败则插入，否则继续查找下一个字符</li>
<li>待插入字符遍历完成，设置最后字符的 <code>isEnd</code> 为 <code>true</code></li>
<li>返回插入成功</li>
</ul>
<div><pre><code>Trie.prototype.insert = function(word) {
    if (!word) return false
    let node = this.root
    for (let i = 0; i &lt; word.length; i++) {
        if (!node.next[word[i]]) {
            node.next[word[i]] = new TrieNode()
        }
        node = node.next[word[i]]
    }
    node.isEnd = true
    return true
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div><p><strong>3. 搜索</strong></p>
<div><pre><code>Trie.prototype.search = function(word) {
    if (!word) return false
    let node = this.root
    for (let i = 0; i &lt; word.length; ++i) {
        if (node.next[word[i]]) {
            node = node.next[word[i]]
        } else {
            return false
        }
    }
    return node.isEnd
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div><p><strong>4. 前缀匹配</strong></p>
<div><pre><code>Trie.prototype.startsWith = function(prefix) {
    if (!prefix) return true
    let node = this.root
    for (let i = 0; i &lt; prefix.length; ++i) {
        if (node.next[prefix[i]]) {
            node = node.next[prefix[i]]
        } else {
            return false
        }
    }
    return true
};
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div>]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-07-15T12:37:23.000Z</published>
  </entry>
  <entry>
    <title type="html">CSS</title>
    <id>https://sewar-x.github.io/css/</id>
    <link href="https://sewar-x.github.io/css/"/>
    <updated>2021-07-15T12:37:23.000Z</updated>
    <content type="html"><![CDATA[<h1 id="css"> CSS</h1>
<h2 id="盒子模型"> 盒子模型</h2>
<h3 id="标准盒子模型"> 标准盒子模型</h3>
<p><img src="./../images/standar-box.png" alt="标准盒子模型图示" /></p>
<ul>
<li>总元素的宽度=宽度+左填充+右填充+左边框+右边框+左边距+右边距</li>
<li>总元素的高度=高度+顶部填充+底部填充+上边框+下边框+上边距+下边距</li>
</ul>
<h3 id="ie中的盒子模型"> IE中的盒子模型</h3>
<ul>
<li>
<p>只有margin和content ，content中包含border,padding</p>
</li>
<li>
<p>宽度=margin+content</p>
</li>
</ul>
<h3 id="控制盒子模型的属性"> 控制盒子模型的属性</h3>
<blockquote>
<p>box-sizing 属性</p>
</blockquote>
<ul>
<li>
<p>作用</p>
<ul>
<li>
<p>用于更改用于计算元素的宽度和高度默认的CSS盒子模型</p>
<ul>
<li>可以使用此属性来模拟不正确支持CSS盒子模型规范的游览器行为</li>
</ul>
</li>
</ul>
</li>
<li>
<p>取值</p>
<ul>
<li><strong>content-box：</strong> box-sizing 属性的默认值</li>
</ul>
</li>
<li>
<p>作用：设置为 标准盒子模型</p>
<ul>
<li>
<p>宽度和高度分别应用到元素的内容框</p>
</li>
<li>
<p>在宽度和高度之外绘制元素的内边距和边框</p>
<p><img src="./../images/standar-box2.png" alt="" /></p>
<ul>
<li>设置的 width = content</li>
<li>padding和border不被包含在定义的width和height之内
<ul>
<li>对象的实际宽度等于设置的width值和border、padding之和，即 ( Element width = width + border + padding )</li>
<li>此属性表现为标准模式下的盒模型</li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>border-box</strong></p>
<ul>
<li>作用：设置 IE 盒子模型</li>
</ul>
</li>
</ul>
</li>
<li>
<p>为元素设定的宽度和高度决定了元素的边框盒。
* 就是说，为元素指定的任何内边距和边框都将在已设定的宽度和高度内进行绘制。通过从已设定的宽度和高度分别减去边框和内边距才能得到内容的宽度和高度<img src="./../images/IE-box.png" alt="" />
* 设置的 width = content + padding + border
* padding和border被包含在定义的width和height之内</p>
<ul>
<li>对象的实际宽度就等于设置的width值，即使定义有border和padding也不会改变对象的实际宽度，即 ( Element width = width )
<ul>
<li>此属性表现为怪异模式下的盒模型</li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="文档流"> 文档流</h2>
<ul>
<li>
<p>概念</p>
<ul>
<li>将窗体自上而下分成一行一行，并在每行中按从左至右依次排放元素，称为文档流，也称为普通流</li>
</ul>
</li>
<li>
<p>HTML中全部元素都是盒模型，盒模型占用一定的空间，依次排放在HTML中，形成了文档流</p>
</li>
<li>
<p>脱离文档流</p>
<ul>
<li>概念：脱离文档流的元素的定位基于正常的文档流，当一个元素脱离文档流后，依然在文档流中的其他元素将忽略该元素并填补其原先的空间，元素脱离文档流之后，将不再在文档流中占据空间，而是处于浮动状态（可以理解为漂浮在文档流的上方）<img src="./../images/document-flow.jpg" alt="" /></li>
<li>让 div 脱离文档流方法
* position: absolute;
<ul>
<li>position: fixed;
<ul>
<li>元素的 position 值为 absolute 或 fixed 时，元素往 Z 轴方向移了一层，元素脱离了普通流，所以不再占据原来那层的空间，还会覆盖下层的元素</li>
</ul>
</li>
<li>该元素将变为块级元素，相当于给该元素设置了 display: block;（给一个内联元素，如 <code>&lt;span&gt;</code>，设置 absolute之后发现它可以设置宽高了）</li>
<li>如果该元素是块级元素，元素的宽度由原来的 width: 100%（占据一行），变为了 auto</li>
</ul>
</li>
<li>float</li>
</ul>
</li>
<li>
<p>层叠上下文和层叠顺序</p>
<ul>
<li>层叠上下文：指图层在z轴中的位置</li>
<li>一旦普通元素具有了层叠上下文，其层叠顺序就会变高</li>
<li><img src="./../images/ceng.png" alt="" /></li>
<li>特性：
* 层叠上下文的层叠水平要比普通元素高
<ul>
<li>层叠上下文可以阻断元素的混合模式</li>
<li>层叠上下文可以嵌套，内部层叠上下文及其所有子元素均受制于外部的层叠上下文</li>
<li>每个层叠上下文和兄弟元素独立，也就是当进行层叠变化或渲染的时候，只需要考虑后代元素</li>
<li>每个层叠上下文是自成体系的，当元素发生层叠的时候，整个元素被认为是在父层叠上下文的层叠顺序中</li>
</ul>
</li>
<li>分类：
<ul>
<li>根层叠上下文：
<ul>
<li>页面根元素具有层叠上下文，称之为“根层叠上下文”</li>
<li>因为根层叠上下文，所以绝对定位元素在left/top等值定位的时候，如果没有其他定位元素限制，会相对浏览器窗口定位</li>
</ul>
</li>
<li>定位元素与传统层叠上下文：
<ul>
<li>z-index值为数值的定位元素的传统层叠上下文</li>
<li>对于包含有 <code>position:relative/position:absolute/position:fixed</code> 声明的定位元素，当其z-index值不是auto的时候，会创建层叠上下文</li>
<li>当z-index嵌套错乱，看看是不是受父级的层叠上下文元素干扰了</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>
<p>层叠水平:</p>
<ul>
<li>决定了同一个层叠上下文中元素在z轴上的显示顺序</li>
</ul>
</li>
<li>
<p>普通元素的层叠水平优先由层叠上下文决定，因此，层叠水平的比较只有在当前层叠上下文元素中才有意义</p>
</li>
<li>
<p>注意：不要把层叠水平和CSS的z-index属性混为一谈，某些情况下z-index确实可以影响层叠水平，但是，只限于定位元素以及flex盒子的孩子元素，而层叠水平所有的元素都存在</p>
</li>
<li>
<p>层叠顺序：表示元素发生层叠时候有着特定的垂直显示顺序</p>
<ul>
<li>
<p>层叠顺序是一种规则<img src="./../images/cengdieshunxu.png" alt="" /></p>
</li>
<li>
<p>最低水平的border/background指的是层叠上下文元素的边框和背景色</p>
</li>
<li>
<p>每一个层叠顺序规则适用于一个完整的层叠上下文元素</p>
</li>
<li>
<p>原图没有呈现inline-block的层叠顺序，实际上，inline-block和inline水平元素是同等level级别</p>
</li>
<li>
<p>z-index:0实际上和z-index:auto单纯从层叠水平上看，是可以看成是一样的</p>
</li>
<li>
<p>层叠准则：</p>
<ul>
<li>谁大谁上：当具有明显的层叠水平标示的时候，如识别的z-indx值，在同一个层叠上下文领域，层叠水平值大的那一个覆盖小的那一个</li>
<li>后来居上：当元素的层叠水平一致、层叠顺序相同的时候，在DOM流中处于后面的元素会覆盖前面的元素</li>
</ul>
</li>
<li>
<p>在CSS和HTML领域，只要元素发生了重叠，都离不开上面这两个黄金准则</p>
</li>
</ul>
</li>
</ul>
<h2 id="position-定位"> Position(定位)</h2>
<ul>
<li>分类
<ul>
<li>static
<ul>
<li>HTML 元素的默认值，即没有定位，遵循正常的文档流对象</li>
<li>静态定位的元素不会受到 top, bottom, left, right影响</li>
</ul>
</li>
<li>fixed
<ul>
<li>元素的位置相对于浏览器窗口是固定位置,即使窗口是滚动的它也不会移动; Fixed定位使元素的位置与文档流无关，因此不占据空间; Fixed定位的元素和其他元素重叠</li>
<li>注意: Fixed 定位在 IE7 和 IE8 下需要描述 !DOCTYPE 才能支持</li>
</ul>
</li>
<li>sticky
<ul>
<li>粘性定位:  基于用户的滚动位置来定位; 粘性定位的元素是依赖于用户的滚动，在 position:relative 与 position:fixed 定位之间切换。 它的行为就像 position:relative; 而当页面滚动超出目标区域时，它的表现就像 position:fixed;，它会固定在目标位置。元素定位表现为在跨越特定阈值前为相对定位，之后为固定定位; 这个特定阈值指的是 top, right, bottom 或 left 之一，换言之，指定 top, right, bottom 或 left 四个阈值其中之一，才可使粘性定位生效。否则其行为与相对定位相同</li>
<li>注意: Internet Explorer, Edge 15 及更早 IE 版本不支持 sticky 定位。 Safari 需要使用 -webkit- prefix (查看以下实例)</li>
</ul>
</li>
<li>relative
<ul>
<li>相对定位元素的定位是相对其正常位置;移动相对定位元素，但它原本所占的空间不会改变; 相对定位元素经常被用来作为绝对定位元素的容器块</li>
</ul>
</li>
<li>absolute
<ul>
<li>绝对定位的元素的位置相对于最近的已定位(已经相对定位的)父元素，如果元素没有已定位的父元素，那么它的位置相对于<code>&lt;html&gt;</code>:</li>
<li>absolute 定位使元素的位置与文档流无关，因此不占据空间。</li>
<li>absolute 定位的元素和其他元素重叠</li>
<li>inherit: 从父元素继承 position 属性的值</li>
<li>控制: 位置通过top, bottom, left, right控制</li>
<li>堆叠顺序
<ul>
<li>元素的定位与文档流无关，所以它们可以覆盖页面上的其它元素</li>
<li>z-index属性: 指定了一个元素的堆叠顺序,具有更高堆叠顺序的元素总是在较低的堆叠顺序元素的前面</li>
<li>注意: 如果两个定位元素重叠，没有指定z - index，最后定位在HTML代码中的元素将被显示在最前面</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="float-浮动"> Float(浮动)</h2>
<ul>
<li>作用：
<ul>
<li>使元素向左或向右移动，其周围的元素也会重新排列</li>
<li>一个浮动元素会尽量向左或向右移动，直到它的外边缘碰到包含框或另一个浮动框的边框为止</li>
</ul>
</li>
<li>浮动元素对其他元素影响：
<ul>
<li>浮动元素之后的元素将围绕它</li>
<li>浮动元素之前的元素将不会受到影响</li>
</ul>
</li>
<li>清除浮动影响： <code>clear:both;</code>
<ul>
<li>元素浮动之后，周围的元素会重新排列，为了避免这种情况，使用 clear 属性。</li>
<li>clear 属性指定元素两侧不能出现浮动元素</li>
</ul>
</li>
<li>要点
<ul>
<li>只有左右浮动，没有上下浮动。</li>
<li>元素设置 float 之后，它会脱离普通流（和 position: absolute; 一样），不再占据原来那层的空间，还会覆盖下一层的元素。</li>
<li>浮动不会对该元素的上一个兄弟元素有任何影响</li>
<li>浮动之后，该元素的下一个兄弟元素会紧贴到该元素之前没有设置 float 的元素之后</li>
<li>如果该元素的下一个兄弟元素中有内联元素（通常是文字），则会围绕该元素显示，形成类似「文字围绕图片」的效果</li>
<li>下一个兄弟元素如果也设置了同一方向的 float，则会紧随该元素之后显示</li>
<li>该元素将变为块级元素，相当于给该元素设置了 display: block;</li>
</ul>
</li>
</ul>
<h2 id="元素类型"> 元素类型</h2>
<p>分类</p>
<ul>
<li>
<p>块级元素(block)</p>
<ul>
<li>特性：
<ul>
<li>总是独占一行，表现为另起一行开始，而且其后的元素也必须另起一行显示;</li>
<li>宽度(width)、高度(height)、内边距(padding)和外边距(margin)都可控制;</li>
</ul>
</li>
<li>例子: div ,  form , h1 , h2 , h3 , h4 , h5 , h6 , hr , isindex , menu , noframes , noscript , ol , p , pre , table , ul , li，address , blockquote , center , dir ，dl , fieldset</li>
</ul>
</li>
<li>
<p>内联元素(inline)</p>
<ul>
<li>特性：
<ul>
<li>和相邻的内联元素在同一行;</li>
<li>宽度(width)、高度(height)、内边距的top/bottom(padding-top/padding-bottom)和外边距的top/bottom(margin-top/margin-bottom)都不可改变，就是里面文字或图片的大小;</li>
<li>例子: a , abbr , acronym , b , bdo , big , br , cite , code , dfn , em , font , i , img , input , kbd , label , q , s , samp , select , small , span , strike , strong , sub , sup ,textarea , tt , u , var</li>
</ul>
</li>
</ul>
</li>
<li>
<p>行块级元素</p>
<ul>
<li>特性：拥有内在尺寸，可设置高宽，不会自动换行</li>
<li>例子:   button,input，textarea,select, img</li>
</ul>
</li>
<li>
<p>可变元素</p>
<ul>
<li>特性:   根据上下文关系确定该元素是块元素还是内联元素</li>
<li>例子:   applet ,button ,del ,iframe , ins ,map ,object , script</li>
</ul>
</li>
<li>
<p>设置方法:</p>
</li>
<li>
<p>display:block  -- 显示为块级元素</p>
</li>
<li>
<p>display:inline  -- 显示为内联元素</p>
</li>
<li>
<p>display:inline-block -- 显示为内联块元素，表现为同行显示并可修改宽高内外边距等属性</p>
</li>
<li>
<p>display:none</p>
</li>
<li>
<p>可以隐藏某个元素，且隐藏的元素不会占用任何空间</p>
</li>
<li>
<p>该元素不但被隐藏了，而且该元素原本占用的空间也会从页面布局中消失</p>
</li>
</ul>
<p>参考资料
<a href="https://www.runoob.com/cssref/pr-class-display.html" target="_blank" rel="noopener noreferrer">display取值</a></p>
<h2 id="单位"> 单位</h2>
<ul>
<li>
<p>分类</p>
<ul>
<li>按尺寸分类
* 相对长度单位
<ul>
<li>绝对长度单位</li>
<li>固定尺寸，它们采用的是物理度量单位：cm、mm、in、px、pt以及pc</li>
</ul>
</li>
<li>按照不同的参考元素
* 字体相对单位：em、ex、ch、rem
<ul>
<li>视窗相对单位：vw、vh、vmin、vmax</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="px"> px</h3>
<ul>
<li>概念：为像素单位。它是显示屏上显示的每一个小点，为显示的最小单位</li>
<li>单位：绝对尺寸单位,不能变动</li>
<li>原理：大小是根据用户屏幕显示器的分辨率决定的（因此不同的设备显示相同的像素值也可能会有不同的结果）</li>
<li>特点：使用px作为字体单位，其字体大小将不能被更改</li>
</ul>
<h3 id="em"> em</h3>
<ul>
<li>概念：相对于应用在当前元素的字体尺寸，如果当前文本的字体尺寸未被定义，则相对于浏览器的默认字体尺寸</li>
<li>单位：相对长度单位，可以变动</li>
<li>原理：根据当前元素的字号按比例计算
<ul>
<li>em作为font-size的单位时，其代表父元素的字体大小</li>
<li>em作为其他属性单位时，代表自身字体大小</li>
</ul>
</li>
<li>特点：em的值并不是固定不变的。em会继承父级元素字体的大小</li>
<li>em与px转换:
<ul>
<li>一般浏览器字体大小默认为16px，则1em =16px,那么0.625em=10px</li>
<li>当设置根元素Font-size:62.5%时,1em=10px</li>
</ul>
</li>
<li>注意：慎用em。
<ul>
<li>理由：em会叠加计算</li>
<li>实例	<img src="./../images/em-demo.png" alt="" />
<ul>
<li>解释：外层span的字号是16px（浏览器默认值），所以1.5em之后是24px。由于字号是继承的，导致内层span的字号继承过来是24px，再经过1.5em之后就成了36px</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="rem"> rem</h3>
<ul>
<li>
<p>概念：</p>
<ul>
<li>相对根节点html的字体大小来计算</li>
<li>rem作用于非根元素时，相对于根元素字体大小</li>
<li>rem作用于根元素字体大小时，相对于其出初始字体大小</li>
</ul>
</li>
<li>
<p>rem与px换算：浏览器默认的字号16px</p>
<ul>
<li>
<p>已知px换算成rem ：</p>
<ul>
<li>换算公式: rem=px/根元素字体大小</li>
<li>字号16px时px单位与rem之间的转换关系</li>
</ul>
<p><img src="./../images/px-rem.png" alt="" /></p>
<ul>
<li>注意：为了方便计算，时常将在<code>&lt;html&gt;</code>元素中设置 <code>font-size</code>值为62.5%,相当于在<code>&lt;html&gt;</code>中设置<code>font-size</code>为10px</li>
</ul>
<p><img src="./../images/px-rem2.png" alt="" /></p>
<ul>
<li>原理：览器默认的字号16px,设置font-size=62.5%,相当于16px*62.5%=10px</li>
</ul>
</li>
<li>
<p>已知rem换算成px：</p>
<ul>
<li>1rem等于html根元素设定的font-size的px值</li>
<li>实例：假如在css里面设定html{font-size:14px} ,则此时1rem=14px</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="百分比"> 百分比%</h3>
<ul>
<li>概念：相对于父元素的百分比值，百分比表示法所表示的仅仅是父元素“内容”的百分比，不包括padding</li>
<li>百分比基准：
<ul>
<li>若元素不存在定位： 则基于直接父元素的宽高度</li>
<li>若元素存在定位 且 定位为 relative， 则也基于直接父元素的宽高度</li>
<li>若元素存在定位 且 定位为 absolute， 则基于最近的相对定位（relative）的祖先元素的宽高度</li>
<li>若元素存在定位 且 定位为 fixed， 则始终基于body元素的宽高度</li>
</ul>
</li>
<li>注意：
<ul>
<li>高度百分比问题：若某元素的父元素没有确定高度，则无法有效使用height=XX%的样式
<ul>
<li>让高度百分比生效方法
<ul>
<li>1.给从根的父容器到子容器的所有容器都设置好百分比高度信息</li>
<li>2.给父容器设置具体的高度信息，比如直接写死在样式中，或者用javascript来设置</li>
<li>3.给父容器设置位置信息，让其得到高度信息</li>
</ul>
</li>
<li>示例，用css使body得到高度，从而使其中的div全屏</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="vw-vh"> vw/vh</h3>
<ul>
<li>视口：根据你浏览器窗口的大小的单位，不受显示器分辨率的影响
<ul>
<li>vw —— 视口宽度的 1/100</li>
<li>vh —— 视口高度的 1/100</li>
</ul>
</li>
<li>在pc端，视口宽高就是浏览器得宽高</li>
<li>在移动端，视口宽高不同，不过一般设置<code>&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1, maximum-scale=1&quot;&gt;</code></li>
<li>vmin：指选择vw和vh中最小的那个</li>
<li>vmax：选择最大的那个</li>
</ul>
<h3 id="css-单位选择"> CSS 单位选择</h3>
<ul>
<li>
<p>页面适配的方式</p>
<ul>
<li>使用px，结合Media Query进行阶梯式的适配；</li>
<li>使用%，按百分比自适应布局；%是参考父容器</li>
<li>使用rem，结合html元素的font-size来根据屏幕宽度适配；</li>
<li>使用vw、vh，直接根据视口宽高适配。vw和vh是参考视口</li>
</ul>
</li>
<li>
<p>移动端适配方案</p>
<ul>
<li>rem+动态计算根元素 font-size</li>
<li>rem+vw
* 使用方式
* 根元素设置font-size使用vw
* 代码中使用rem单位
<ul>
<li>原理:
* 1vw始终表示移动端视窗的1/100 ，当屏幕大小改变时候，1vw表示px大小也在变化
<ul>
<li>根元素font-size使用vw时,则显示字体大小随着屏幕宽度变化而变化</li>
<li>根元素font-size如果使用px，则显示字体大小固定，不随屏幕大小变化而变化</li>
<li>使用rem作为元素单位，则元素大小随根元素font-size大小px变化而变化</li>
</ul>
</li>
<li>计算方式
* 根元素font-size计算：
* 根元素font-size代表根元素字体默认大小,一般为16px
* 1vw表示移动端视窗的1/100
* 设计稿使用屏幕宽度为px, 设计稿中 1px = 100vw / 设计稿宽度px
* 则根元素font-size使用vw计算方式为: 100vw / 设计稿宽度px * 设置的根元素font-size大小
<ul>
<li>代码中rem单位计算方式：
<ul>
<li>1 rem=设计稿px/根元素字体大小font-size</li>
<li>当设置根元素font-size: 100px, 则代码中rem计算为: 设计稿px/100</li>
</ul>
</li>
<li>示例
<ul>
<li>假设设计稿屏幕宽度为750px,为了方便设置根元素font-size: 100px，根元素font-size计算100vw / 750px  * 100px = 13.33333333 vw</li>
</ul>
</li>
<li>代码单位计算：rem = 设计稿宽度px / 100px</li>
<li>使用场景
<ul>
<li>在视觉稿要求固定尺寸的元素上使用px</li>
<li>在字号、（大多数）间距上使用rem</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>​</p>
</li>
</ul>
<p>参考资料</p>
<ul>
<li><a href="https://segmentfault.com/a/1190000015867354" target="_blank" rel="noopener noreferrer">《对于页面适配，你应该使用px还是rem》</a></li>
</ul>
<h2 id="属性继承"> 属性继承</h2>
<ul>
<li>无继承性的属性：
* display
<ul>
<li>文本属性
* vertical-align：垂直文本对齐
<ul>
<li>text-decoration：规定添加到文本的装饰</li>
<li>text-shadow：文本阴影效果</li>
<li>white-space：空白符的处理</li>
<li>unicode-bidi：设置文本的方向</li>
</ul>
</li>
<li>盒子模型的属性：width、height、margin 、margin-top、margin-right、margin-bottom、margin-left、border、border-style、border-top-style、border-right-style、border-bottom-style、border-left-style、border-width、border-top-width、border-right-right、border-bottom-width、border-left-width、border-color、border-top-color、border-right-color、border-bottom-color、border-left-color、border-top、border-right、border-bottom、border-left、padding、padding-top、padding-right、padding-bottom、padding-left</li>
<li>背景属性：background、background-color、background-image、background-repeat、background-position、background-attachment</li>
<li>定位属性：float、clear、position、top、right、bottom、left、min-width、min-height、max-width、max-height、overflow、clip、z-index</li>
<li>生成内容属性：content、counter-reset、counter-increment</li>
<li>轮廓样式属性：outline-style、outline-width、outline-color、outline</li>
<li>页面样式属性：size、page-break-before、page-break-after</li>
<li>声音样式属性：pause-before、pause-after、pause、cue-before、cue-after、cue、play-during</li>
</ul>
</li>
<li>有继承性的属性：
* 字体系列属性：
* font：组合字体
* font-family：规定元素的字体系列
* font-weight：设置字体的粗细
* font-size：设置字体的尺寸
* font-style：定义字体的风格
* font-variant：设置小型大写字母的字体显示文本，这意味着所有的小写字母均会被转换为大写，但是所有使用小型大写字体的字母与其余文本相比，其字体尺寸更小。
* font-stretch：对当前的 font-family 进行伸缩变形。所有主流浏览器都不支持。
* font-size-adjust：为某个元素规定一个 aspect 值，这样就可以保持首选字体的 x-height
<ul>
<li>文本系列属性
* text-indent：文本缩进
<ul>
<li>text-align：文本水平对齐</li>
<li>line-height：行高</li>
<li>word-spacing：增加或减少单词间的空白（即字间隔）</li>
<li>letter-spacing：增加或减少字符间的空白（字符间距）</li>
<li>text-transform：控制文本大小写</li>
<li>direction：规定文本的书写方向</li>
<li>color：文本颜色</li>
</ul>
</li>
<li>元素可见性：visibility</li>
<li>表格布局属性：caption-side、border-collapse、border-spacing、empty-cells、table-layout</li>
<li>列表布局属性：list-style-type、list-style-image、list-style-position、list-style</li>
<li>生成内容属性：quotes</li>
<li>光标属性：cursor</li>
<li>页面样式属性：page、page-break-inside、windows、orphans</li>
<li>声音样式属性：speak、speak-punctuation、speak-numeral、speak-header、speech-rate、volume、voice-family、pitch、pitch-range、stress、richness、、azimuth、elevation</li>
</ul>
</li>
<li>所有元素可以继承的属性
* 元素可见性：visibility
<ul>
<li>光标属性：cursor</li>
</ul>
</li>
<li>内联元素可以继承的属性：
* 字体系列属性：除text-indent、text-align之外的文本系列属性</li>
<li>块级元素可以继承的属性：text-indent、text-align</li>
</ul>
<h2 id="css-轮廓-outline"> CSS 轮廓（outline）</h2>
<ul>
<li>轮廓（outline）是绘制于元素周围的一条线，位于边框边缘的外围，可起到突出元素的作用</li>
<li>轮廓（outline）属性指定元素轮廓的样式、颜色和宽度</li>
<li>注意
<ul>
<li>1.outline是不占空间的，既不会增加额外的width或者height（这样不会导致浏览器渲染时出现reflow或是repaint）</li>
<li>2.outline有可能是非矩形的（火狐浏览器下）</li>
<li>3.outline位于margin中间</li>
</ul>
</li>
</ul>
<h2 id="font字体"> font字体</h2>
<ul>
<li>
<p>使用外部字体 : @font-face</p>
</li>
<li>
<p>文字溢出省略</p>
<ul>
<li>
<p>单行文本的溢出显示省略号</p>
<div><pre><code><span>overflow</span><span>:</span> hidden<span>;</span>
<span>text-overflow</span><span>:</span>ellipsis<span>;</span>
<span>white-space</span><span>:</span> nowrap<span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><blockquote>
<p>text-overflow:ellipsis属性: 文本的溢出显示省略号,需要加宽度width属来兼容部分浏</p>
</blockquote>
</li>
<li>
<p>多行文本溢出显示省略号</p>
<div><pre><code><span>display</span><span>:</span> -webkit-box<span>;</span>
<span>-webkit-box-orient</span><span>:</span> vertical<span>;</span>
<span>-webkit-line-clamp</span><span>:</span> 3<span>;</span>
<span>overflow</span><span>:</span> hidden<span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br></div></div><blockquote>
<p>display: -webkit-box; 必须结合的属性 ，将对象作为弹性伸缩盒子模型显示
-webkit-box-orient 必须结合的属性 ，设置或检索伸缩盒对象的子元素的排列方式</p>
<p>-webkit-line-clamp用来限制在一个块元素显示的文本的行数</p>
</blockquote>
</li>
</ul>
</li>
</ul>
<h2 id="css对齐"> css对齐</h2>
<ul>
<li>
<p>水平居中</p>
<ul>
<li>
<p>元素居中对齐</p>
<ul>
<li>
<p>块元素居中</p>
<div><pre><code><span>display</span><span>:</span>block<span>;</span>
<span>margin</span><span>:</span> 0 auto<span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div><p>设置居中的元素为块元素，设置父元素text-align:center</p>
</li>
<li>
<p>弹性居中</p>
<div><pre><code><span>display</span><span>:</span> flex<span>;</span>
<span>justify-content</span><span>:</span> center<span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div></li>
<li>
<p>文本居中对齐: text-align: center</p>
</li>
<li>
<p>图片居中对齐</p>
<div><pre><code> <span>display</span><span>:</span> block<span>;</span>
 <span>margin</span><span>:</span> 0 auto<span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
<li>
<p>垂直居中</p>
<ul>
<li>vertical-align: middle;</li>
<li>父元素使用 padding; 设置padding的上下距离相同</li>
<li>line-height 属性值和 height 属性值相等，居中元素设置    line-height,  display: inline-block; 和vertical-align: middle;</li>
</ul>
</li>
<li>
<p>水平并且垂直居中</p>
<ul>
<li>父元素使用 padding 和 text-align: center</li>
<li>使用 position 和 transform</li>
</ul>
</li>
<li>
<p>左右对齐</p>
<ul>
<li>定位方式 : position: absolute; right: 0px;或则right: 0px;
<ul>
<li>提示： 当使用 position 来对齐元素时, 通常 <code>&lt;body&gt;</code> 元素会设置 margin 和 padding，这样可以避免在不同的浏览器中出现可见的差异，当使用 position 属性时，请始终设置 !DOCTYPE 声明</li>
</ul>
</li>
<li>使用 float 方式</li>
</ul>
</li>
</ul>
<h2 id="css-样式重置"> CSS 样式重置</h2>
<ul>
<li>HTML 标签在不设置任何样式的情况下，也会有一个默认的 CSS 样式，而不同内核浏览器对于这个默认值的设置则不尽相同，这样可能会导致同一套代码在不同浏览器上的显示效果不一致，而出现兼容性问题</li>
<li>因此，在初始化时，需要对常用标签的样式进行初始化，使其默认样式统一，这就是 CSS Reset方式</li>
</ul>
<div><pre><code><span>*</span><span>{</span> <span>margin</span><span>:</span>0<span>;</span> <span>padding</span><span>:</span>0<span>;</span> <span>}</span>
</code></pre>
<div><span>1</span><br></div></div><h2 id="引入-css-方式"> 引入 CSS 方式</h2>
<p>引入 CSS 的方式有 4 种方式可以在 HTML 中引入 CSS。其中有 2 种方式是在 HTML 文件中直接添加 CSS 代码，另外两种是引入 外部 CSS 文件。下面我们就来看看这些方式和它们的优缺点。</p>
<h3 id="内联方式"> 内联方式</h3>
<p>内联方式指的是直接在 HTML 标签中的 <strong>style</strong> 属性中添加 CSS。</p>
<p>示例：</p>
<div><pre><code><span><span><span>&lt;</span>div</span> <span><span>style</span><span><span>=</span><span>"</span><span><span>background</span><span>:</span> red</span><span>"</span></span></span><span>></span></span><span><span><span>&lt;/</span>div</span><span>></span></span>
</code></pre>
<div><span>1</span><br></div></div><p>这通常是个很糟糕的书写方式，它只能改变当前标签的样式，如果想要多个**<code>&lt;div&gt;</code>** 拥有相同的样式，你不得不重复地为每个 <strong><code>&lt;div&gt;</code></strong> 添加相同的样式，如果想要修改一种样式，又不得不修改所有的 style 中的代码。很显然，内联方式引入 CSS 代码会导致 HTML 代码变得冗长，且使得网页难以维护。</p>
<h3 id="嵌入方式"> 嵌入方式</h3>
<p>嵌入方式指的是在 HTML 头部中的 <strong><code>&lt;style&gt;</code></strong> 标签下书写 CSS 代码。</p>
<p>示例：</p>
<div><pre><code><span><span><span>&lt;</span>head</span><span>></span></span>
    <span><span><span>&lt;</span>style</span><span>></span></span><span><span>
    <span>.content</span> <span>{</span>
        <span>background</span><span>:</span> red<span>;</span>
    <span>}</span>
    </span></span><span><span><span>&lt;/</span>style</span><span>></span></span>
<span><span><span>&lt;/</span>head</span><span>></span></span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div><p>嵌入方式的 CSS 只对当前的网页有效。因为 CSS 代码是在 HTML 文件中，所以会使得代码比较集中，当我们写模板网页时这通常比较有利。因为查看模板代码的人可以一目了然地查看 HTML 结构和 CSS 样式。因为嵌入的 CSS 只对当前页面有效，所以当多个页面需要引入相同的 CSS 代码时，这样写会导致代码冗余，也不利于维护。</p>
<h3 id="链接方式"> 链接方式</h3>
<p>链接方式指的是使用 HTML 头部的 <strong><code>&lt;head&gt;</code></strong> 标签引入外部的 CSS 文件。</p>
<p>示例：</p>
<div><pre><code>&lt;head&gt;
    &lt;link rel=&quot;stylesheet&quot; type=&quot;text/css&quot; href=&quot;style.css&quot;&gt;
&lt;/head&gt;
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><p>这是最常见的也是最推荐的引入 CSS 的方式。使用这种方式，所有的 CSS 代码只存在于单独的 CSS 文件中，所以具有良好的可维护性。并且所有的 CSS 代码只存在于 CSS 文件中，CSS 文件会在第一次加载时引入，以后切换页面时只需加载 HTML 文件即可。</p>
<h3 id="导入方式"> 导入方式</h3>
<p>导入方式指的是使用 CSS 规则引入外部 CSS 文件。</p>
<p>示例：</p>
<div><pre><code>&lt;style&gt;
    @import url(style.css);
&lt;/style&gt;
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><h3 id="比较链接方式和导入方式"> 比较链接方式和导入方式</h3>
<p>链接方式（下面用 link 代替）和导入方式（下面用 <strong>@import</strong> 代替）都是引入外部的 CSS 文件的方式，下面我们来比较这两种方式，并且说明为什么不推荐使用 <strong>@import</strong>。</p>
<ul>
<li>link 属于 HTML，通过 <strong><link></strong> 标签中的 href 属性来引入外部文件，而 <strong>@import</strong> 属于 CSS，所以导入语句应写在 CSS 中，要注意的是导入语句应写在样式表的开头，否则无法正确导入外部文件；</li>
<li><strong>@import</strong> 是 CSS2.1 才出现的概念，所以如果浏览器版本较低，无法正确导入外部样式文件；</li>
<li>当 HTML 文件被加载时，link 引用的文件会同时被加载，而 <strong>@import</strong> 引用的文件则会等页面全部下载完毕再被加载；</li>
</ul>
<hr>
<p><strong>小结</strong>：我们应尽量使用 <strong><link></strong> 标签导入外部 CSS 文件，避免或者少用使用其他三种方式。</p>
<h2 id="样式优先级"> 样式优先级</h2>
<ul>
<li>
<p>多重样式优先级</p>
<ul>
<li>
<p>优先级是浏览器是通过判断哪些属性值与元素最相关以决定并应用到该元素上的。优先级仅由选择器组成的匹配规则决定的。</p>
</li>
<li>
<p>优先级就是分配给指定的CSS声明的一个权重，它由匹配的选择器中的每一种选择器类型的数值决定</p>
</li>
<li>
<p>优先级顺序：</p>
<ul>
<li>通用选择器（*）&gt;元素(类型)选择器 &gt; 类选择器 &gt; 属性选择器 &gt; 伪类 &gt; ID 选择器 &gt; 内联样式</li>
<li>!important 规则例外： 当 !important 规则被应用在一个样式声明中时,该样式声明会覆盖CSS中任何其他的声明, 无论它处在声明列表中的哪里. 尽管如此, !important规则还是与优先级毫无关系.</li>
</ul>
</li>
<li>
<p>经验法则</p>
<ul>
<li>Always 要优化考虑使用样式规则的优先级来解决问题而不是 !important
<ul>
<li>Only 只在需要覆盖全站或外部 css（例如引用的 ExtJs 或者 YUI ）的特定页面中使用 !important</li>
</ul>
</li>
<li>Never 永远不要在全站范围的 css 上使用 !important
<ul>
<li>Never 永远不要在你的插件中使用 !important</li>
</ul>
</li>
</ul>
</li>
<li>
<p>权重计算 <img src="./../images/quanzhong.png" alt="" /></p>
<ul>
<li>
<blockquote>
<p>1.内联样式表的权值最高 1000；</p>
<p>2.ID 选择器的权值为 100</p>
</blockquote>
</li>
</ul>
<blockquote>
<p>3.Class 类选择器的权值为 10</p>
<p>4.HTML 标签选择器的权值为 1</p>
</blockquote>
</li>
</ul>
</li>
<li>
<p>优先级：</p>
<ul>
<li>内联样式&gt;内部样式&gt;外部样式&gt;浏览器默认样式</li>
<li>外部样式放在内部样式后面,则外部样式会覆盖内部样式</li>
<li>连接样式的优先级
<ul>
<li>设置的优先顺序： link-visited-hover-actived</li>
</ul>
</li>
</ul>
</li>
<li>
<p>CSS 优先级法则</p>
<ul>
<li>选择器都有一个权值，权值越大越优先；</li>
<li>当权值相等时，后出现的样式表设置要优于先出现的样式表设置；</li>
<li>创作者的规则高于浏览者：即网页编写者设置的CSS 样式的优先权高于浏览器所设置的样式；</li>
<li>继承的CSS 样式不如后来指定的CSS 样式；</li>
<li>在同一组属性设置中标有“!important”规则的优先级最大
<img src="./../images/css优先级.png" alt="" /></li>
</ul>
</li>
<li>
<p>元素选择器：</p>
<ul>
<li>
<p>选择器优先级: !important&gt;id选择器&gt;类选择器&gt;标签&gt;通配符&gt;继承&gt;浏览器默认</p>
</li>
<li>
<p>越具体的对象的选择器优先级越高</p>
</li>
</ul>
</li>
</ul>
<p>参考资料</p>
<p><a href="https://www.runoob.com/w3cnote/css-style-priority.html" target="_blank" rel="noopener noreferrer">CSS 样式优先级</a></p>
<h2 id="css-sprites"> CSS Sprites</h2>
<ul>
<li>简介：
<ul>
<li>CSS Sprites在国内很多人叫css精灵，是一种网页图片应用处理方式</li>
<li>它允许将一个页面涉及到的所有零星图片都包含到一张大图中， 利用CSS的“background-image”，“background- repeat”，“background-position”的组合进行背景定位， 访问页面时避免图片载入缓慢的现象。</li>
</ul>
</li>
<li>优点
<ul>
<li>CSS Sprites能很好地减少网页的http请求，从而大大的提高页面的性能，这是CSS Sprites最大的优点，也是其被广泛传播和应用的主要原因；</li>
<li>CSS Sprites能减少图片的字节；</li>
<li>CSS Sprites解决了网页设计师在图片命名上的困扰，只需对一张集合的图片命名，不需要对每一个小图片进行命名，从而提高了网页制作效率。</li>
<li>CSS Sprites只需要修改一张或少张图片的颜色或样式来改变整个网页的风格。</li>
</ul>
</li>
<li>缺点
<ul>
<li>图片合并麻烦：图片合并时，需要把多张图片有序的合理的合并成一张图片，并留好足够的空间防止版块出现不必要的背景。</li>
<li>图片适应性差：在高分辨的屏幕下自适应页面，若图片不够宽会出现背景断裂。</li>
<li>图片定位繁琐：开发时需要通过工具测量计算每个背景单元的精确位置。</li>
<li>可维护性差：页面背景需要少许改动，可能要修改部分或整张已合并的图片，进而要改动css。在避免改动图片的前提下，又只能（最好）往下追加图片，但这样增加了图片字节。</li>
</ul>
</li>
</ul>
<h2 id="css-hack"> CSS Hack</h2>
<ul>
<li>概念：不同的浏览器对CSS的解析结果是不同的，因此会导致相同的CSS输出的页面效果不同，这就需要CSS Hack来解决浏览器局部的兼容性问题。而这个针对不同的浏览器写不同的CSS 代码的过程，就叫CSS Hack</li>
<li>Hack主要针对IE浏览器</li>
<li>常见的形式
<ul>
<li>CSS属性Hack</li>
<li>CSS选择符Hack</li>
<li>IE条件注释Hack</li>
</ul>
</li>
</ul>
<h2 id="居中方案"> 居中方案</h2>
<p><img src="./../images/居中方案.png" alt="image-20210713092852324" /></p>
<p>一般常见的几种居中的方法有：</p>
<h3 id="水平居中"> 水平居中</h3>
<p>水平居中分为块级元素和行内元素的居中方案。</p>
<ul>
<li>
<p>块级元素居中：</p>
<ul>
<li>
<p>设置 display:block; 块状元素的宽度width为固定值 和 块元素 margin: 0 auto；</p>
<div><pre><code><span>div</span> <span>{</span>
    <span>display</span><span>:</span>block<span>;</span>
    <span>width</span><span>:</span> 200px<span>;</span>
    <span>margin</span><span>:</span> 0 auto<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div></li>
</ul>
</li>
<li>
<p>行内元素居中：行内元素居中分为定宽块元素居中 和 不定宽块元素 居中。</p>
<ul>
<li>
<p>定宽块状元素居中：容器 div 添加  text-align:center;</p>
<ul>
<li>块状元素属性display 被设置为inline时，也是可以使用这种方法。</li>
<li>但子元素必须没有被float影响，否则无效</li>
</ul>
<div><pre><code><span>.container</span> <span>{</span>
    <span>background</span><span>:</span> <span>rgba</span><span>(</span>0<span>,</span> 0<span>,</span> 0<span>,</span> 0.5<span>)</span><span>;</span>
    <span>text-align</span><span>:</span> center<span>;</span>
    <span>font-size</span><span>:</span> 0<span>;</span>
<span>}</span><span>.box</span> <span>{</span>
    <span>display</span><span>:</span> inline-block<span>;</span>
  <span>width</span><span>:</span> 500px<span>;</span>
    <span>height</span><span>:</span> 400px<span>;</span>
  <span>background-color</span><span>:</span> pink<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div></li>
<li>
<p>不定宽块状元素居中：块状元素的宽度width不固定。</p>
<ul>
<li>
<p>在实际工作中我们会遇到需要为“不定宽度的块状元素”设置居中，比如网页上的分页导航，因为分页的数量是不确定的，所以我们不能通过设置宽度来限制它的弹性。</p>
</li>
<li>
<p>方法1：</p>
<ul>
<li>
<p>实现：</p>
<ul>
<li>使用 display : table；然后设该元素“左右margin”值为“auto”来实现居中；</li>
<li>或则 将要显示的元素加入到 table 标签当中，然后为 table 标签设置“左右margin”值为“auto”来实现居中。</li>
</ul>
</li>
<li>
<p>使用 table 原理:</p>
<ul>
<li>
<p>利用table标签的长度自适应性，table其长度根据其内文本长度决定，因此可以看做一个定宽度块元素，然后再利用定宽度块状居中的margin的方法，使其水平居中。</p>
<div><pre><code><span>.wrap</span><span>{</span>
    <span>background</span><span>:</span>#ccc<span>;</span>
    <span>display</span><span>:</span>table<span>;</span>
    <span>margin</span><span>:</span>0 auto<span>;</span>
<span>}</span>

&lt;div class=<span>"wrap"</span>>
  Hello world  
&lt;/div>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
<li>
<p>方法2：</p>
<ul>
<li>
<p>实现：</p>
<ul>
<li>给父元素设置 float， position:relative 和 left:50%，子元素设置 position:relative 和 left: -50% 来实现水平居中</li>
</ul>
</li>
<li>
<div><pre><code><span>.wrap</span><span>{</span>
    <span>float</span><span>:</span>left<span>;</span>
    <span>position</span><span>:</span>relative<span>;</span>
    <span>left</span><span>:</span>50%<span>;</span>
    <span>clear</span><span>:</span>both<span>;</span>
<span>}</span>
<span>.wrap-center</span><span>{</span>
    <span>background</span><span>:</span>#ccc<span>;</span>
    <span>position</span><span>:</span>relative<span>;</span>
    <span>left</span><span>:</span>-50%<span>;</span>
<span>}</span>

&lt;div class=<span>"wrap"</span>>
    &lt;div class=<span>"wrap-center"</span>>Hello world&lt;/div>
&lt;/div>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div></li>
</ul>
</li>
<li>
<p>方法3：</p>
<ul>
<li>
<p>设置块级元素的 display 为 inline 类型（设置为 行内元素 显示），然后使用 text-align:center 来实现居中效果</p>
<div><pre><code><span>.container</span><span>{</span>
    <span>text-align</span><span>:</span>center<span>;</span>
 <span>}</span>
<span>.container ul</span><span>{</span>
    <span>list-style</span><span>:</span>none<span>;</span>
    <span>margin</span><span>:</span>0<span>;</span>
    <span>padding</span><span>:</span>0<span>;</span>
    <span>display</span><span>:</span>inline<span>;</span>  
 <span>}</span>

&lt;div class=<span>"container"</span>>
    &lt;ul>
        &lt;li>Hello world&lt;/li>
        &lt;li>Hello world&lt;/li>
    &lt;/ul>
&lt;/div>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div><ul>
<li>优点：不用增加无语义标签；</li>
<li>缺点：将块状元素的 display 类型改为 inline，变成了行内元素，所以少了一些功能，比如设定长度值（变成inline-block就可以设置宽高）</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="垂直居中"> 垂直居中</h3>
<p>垂直居中可分为父元素高度确定的单行文本、父元素高度确定的多行文本。</p>
<ul>
<li>
<p>父元素高度确定的单行文本：</p>
<ul>
<li>
<p>实现：设置父元素的 height 和 line-height 高度一致。(height: 该元素的高度，line-height: 行高，指在文本中、行与行之间的 基线间的距离 )</p>
<div><pre><code><span>.wrap h2</span><span>{</span>
    <span>margin</span><span>:</span>0<span>;</span>
    <span>height</span><span>:</span>100px<span>;</span>
    <span>line-height</span><span>:</span>100px<span>;</span>
    <span>background</span><span>:</span>#ccc<span>;</span>
<span>}</span>

&lt;div class=<span>"wrap"</span>>
    &lt;h2>Hello world&lt;/h2>
&lt;/div>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div></li>
</ul>
</li>
<li>
<p>父元素高度确定的多行文本：两种实现方式。</p>
<ul>
<li>
<p>方法1：</p>
<ul>
<li>
<p>设置  display:table; vertical-align：middle；</p>
</li>
<li>
<p>或则 使用 table (包括tbody、tr、td)标签，同时设置 vertical-align：middle。</p>
</li>
<li>
<div><pre><code><span>.wrap</span><span>{</span>
    <span>background</span><span>:</span>#ccc<span>;</span>
    <span>display</span><span>:</span>table<span>;</span>
    <span>vertical-align</span><span>:</span>middle<span>;</span>
<span>}</span>

&lt;div class=<span>"wrap"</span>>
    &lt;p>Hello world&lt;/p>
    &lt;p>Hello world&lt;/p>
    &lt;p>Hello world&lt;/p>
&lt;/div>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div></li>
<li>
<div><pre><code><span>.wrap</span><span>{</span>
    <span>height</span><span>:</span>300px<span>;</span>
    <span>background</span><span>:</span>#ccc<span>;</span>
    <span>vertical-align</span><span>:</span>middle<span>;</span><span>/* td 标签默认情况下就默认设置了 vertical-align 为 middle，可以不需要显式地设置 */</span>
<span>}</span>

&lt;table>
    &lt;tbody>
        &lt;tr>
            &lt;td class=<span>"wrap"</span>>
                &lt;div>
                    &lt;p>Hello world&lt;/p>
                    &lt;p>Hello world&lt;/p>
                    &lt;p>Hello world&lt;/p>
                &lt;/div>
            &lt;/td>
        &lt;/tr>
    &lt;/tbody>
&lt;/table>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div></li>
</ul>
</li>
<li>
<p>方法2：设置块级元素的 display 为 table-cell（设置为表格单元显示），激活 vertical-align 属性；</p>
<ul>
<li>
<div><pre><code><span>.wrap</span><span>{</span>
    <span>height</span><span>:</span>300px<span>;</span>
    <span>background</span><span>:</span>#ccc<span>;</span>
    
    <span>display</span><span>:</span>table-cell<span>;</span><span>/*IE8以上及Chrome、Firefox*/</span>
    <span>vertical-align</span><span>:</span>middle<span>;</span><span>/*IE8以上及Chrome、Firefox*/</span>
<span>}</span>

&lt;div class=<span>"wrap"</span>>
    &lt;p>Hello world&lt;/p>
    &lt;p>Hello world&lt;/p>
    &lt;p>Hello world&lt;/p>
&lt;/div>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br></div></div></li>
<li>
<p>缺点：兼容性比较差， IE6、7 并不支持这个样式</p>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="水平垂直居中"> 水平垂直居中</h3>
<p><strong>已知高度和宽度的元素：</strong></p>
<ul>
<li>
<p>方法1：</p>
<ul>
<li>
<p>实现：</p>
<ul>
<li>
<p>父元素相对定位，居中元素绝对定位</p>
</li>
<li>
<p>设置四个方向的值都为 0，并将 margin 设置为 auto;</p>
</li>
<li>
<div><pre><code><span>.wrap</span> <span>{</span>
    <span>position</span><span>:</span> relative<span>;</span>
<span>}</span>
<span>div</span> <span>{</span>
    <span>position</span><span>:</span>absolute<span>;</span>
    <span>width</span><span>:</span>100px<span>;</span>
    <span>height</span><span>:</span> 100px<span>;</span>
    <span>margin</span><span>:</span>auto<span>;</span>
    <span>top</span><span>:</span>0<span>;</span>
    <span>bottom</span><span>:</span>0<span>;</span>
    <span>left</span><span>:</span>0<span>;</span>
    <span>right</span><span>:</span>0<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br></div></div></li>
</ul>
</li>
<li>
<p>原理：子元素依然相对于父元素定位，但是由于 <code>top:0</code> 和 <code>bottom:0</code> 无法同时满足，且该元素的 <code>margin</code> 又是自适应，因此最终变成了由上下外边距平分尺寸，从而达到垂直居中。至于水平居中，原理也是一样。</p>
</li>
</ul>
</li>
<li>
<p>方法2：</p>
<ul>
<li>
<p>实现：</p>
<ul>
<li>
<p>父元素相对定位，居中元素绝对定位</p>
</li>
<li>
<p>将元素的左上角通过 top:50% 和 left:50%定位到页面的中心</p>
</li>
<li>
<p>然后再通过 margin 负值来调整元素 的中心点到页面的中心</p>
<div><pre><code><span>.wrap</span> <span>{</span>
    <span>position</span><span>:</span> relative<span>;</span>
<span>}</span>
<span>div</span><span>{</span>
    <span>position</span><span>:</span>absolute<span>;</span> <span>/*绝对定位*/</span>
    <span>width</span><span>:</span>500px<span>;</span>
    <span>height</span><span>:</span>300px<span>;</span>
    <span>top</span><span>:</span>50%<span>;</span>
    <span>left</span><span>:</span>50%<span>;</span>
    <span>margin</span><span>:</span>-150px 0 0 -250px<span>;</span><span>/*外边距为自身宽高的一半*/</span>
    <span>background-color</span><span>:</span>pink<span>;</span><span>/*方便看效果*/</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div></li>
</ul>
</li>
<li>
<p>原理：<img src="./../images/水平垂直居中原理.png" alt="image-20210713090208312" /></p>
</li>
</ul>
</li>
</ul>
<p><strong>未知高度和宽度的元素</strong></p>
<ul>
<li>
<p>实现：</p>
<ul>
<li>
<p>父元素相对定位，居中元素绝对定位</p>
</li>
<li>
<p>将元素的左上角通过 top:50% 和 left:50%定位到页面的中心</p>
</li>
<li>
<p>通过 translate 来调整 元素 的中心点到页面的中心</p>
<div><pre><code><span>.wrap</span> <span>{</span>
    <span>position</span><span>:</span> relative<span>;</span>
<span>}</span>
<span>div</span><span>{</span>
    <span>position</span><span>:</span>absolute<span>;</span>
    <span>width</span><span>:</span>100px<span>;</span>
    <span>height</span><span>:</span> 50px<span>;</span>
    <span>top</span><span>:</span> 50%<span>;</span>
    <span>left</span><span>:</span>50%<span>;</span>
    <span>transform</span><span>:</span> <span>translate</span><span>(</span>-50%<span>,</span>-50%<span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div></li>
</ul>
</li>
<li>
<p>原理：由于 不知道子元素高度和宽度，因此通过 <code>transform: translateY(50%，50%);</code> 方式移动元素中心到页面中心。</p>
</li>
</ul>
<p><strong>任意元素水平垂直居中</strong></p>
<ul>
<li>
<p><strong>flex 弹性布局</strong></p>
<ul>
<li>
<p>方法1：</p>
<ul>
<li>
<p>实现：设置 弹性布局，通过 align-items:center 和 justify-content:center 设置容器的垂直和水平 方向上为居中对 ，它的子元素也可以实现垂直和水平的居中</p>
<div><pre><code><span>.wrap</span> <span>{</span>
    <span>display</span><span>:</span>flex<span>;</span>
    <span>justify-content</span><span>:</span> center<span>;</span>
    <span>align-items</span><span>:</span> center<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div></li>
</ul>
</li>
<li>
<p>方法2：</p>
<ul>
<li>
<p>实现：设置父元素弹性布局 display:flex；子元素边距自动 margin:auto;</p>
<div><pre><code><span>.wrap</span> <span>{</span>
    <span>display</span><span>:</span> flex<span>;</span>
<span>}</span>
<span>div</span><span>{</span> <span>/*子元素边距自动 margin:auto;*/</span>
    <span>margin</span><span>:</span> auto<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div></li>
</ul>
</li>
<li>
<p><strong>网格布局</strong></p>
<ul>
<li>
<p>方法1：</p>
<div><pre><code><span>.wrap</span> <span>{</span>
    <span>display</span><span>:</span> grid<span>;</span>
<span>}</span>
<span>div</span><span>{</span>
    <span>justify-self</span><span>:</span>center<span>;</span>
    <span>align-self</span><span>:</span>center<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
<li>
<p>方法2：</p>
<div><pre><code><span>.wrap</span> <span>{</span>
    <span>display</span><span>:</span> grid<span>;</span>
<span>}</span>
<span>div</span><span>{</span>
    <span>margin</span><span>:</span> auto<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div></li>
</ul>
</li>
<li>
<p><strong>伪元素实现</strong></p>
<ul>
<li>
<div><pre><code><span>.wrap</span> <span>{</span>
    <span>font-size</span><span>:</span> 0<span>;</span>  <span>/* 消除空隙 */</span>
    <span>text-align</span><span>:</span> center<span>;</span>  <span>/* 实现水平居中*/</span>
    <span>&amp;::before</span> <span>{</span>
        <span>content</span><span>:</span> <span>""</span><span>;</span>
        <span>display</span><span>:</span> inline-block<span>;</span>
        <span>width</span><span>:</span> 0<span>;</span>
        <span>height</span><span>:</span> 100%<span>;</span>
        <span>vertical-align</span><span>:</span> middle<span>;</span>
    <span>}</span>
<span>}</span>
<span>div</span><span>{</span> <span>/*子元素*/</span>
  <span>display</span><span>:</span> inline-block<span>;</span>
  <span>vertical-align</span><span>:</span> middle<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="常见布局方式"> 常见布局方式</h2>
<p><img src="./../images/常见布局.png" alt="image-20210712191310316" /></p>
<p><strong>参考资料</strong></p>
<p><a href="https://juejin.cn/post/6844903710070407182#heading-18" target="_blank" rel="noopener noreferrer">几种常见的CSS布局</a></p>
<h3 id="响应式布局"> 响应式布局</h3>
<h4 id="flexbox"> Flexbox</h4>
<p>弹性盒布局模型:</p>
<ul>
<li>Flex 是 FlexibleBox 的缩写，意为&quot;弹性布局&quot;，弹性布局中的元素是有伸展和收缩自身的能力的。 相比于原来的布局方式，如float、position，根据盒子模型，就可以计算出元素的展示尺寸（长宽非百分比），除非溢出，否则不依赖于父容器的大小。</li>
<li>“伸缩性”：目标就是为了撑满父元素</li>
<li>任何一个容器都可以指定为 Flex 布局。行内元素也可以使用 Flex 布局。
<ul>
<li>注意，设为 Flex 布 局以后，子元素的 float、cl ear 和 vertical-align 属性将失效。</li>
</ul>
</li>
<li>采用 Flex 布局的元素，称为 Flex 容器（flexcontainer），简称&quot;容器&quot;。它的所有子元素自动成 为容器成员，称为 Flex 项目（flexitem），简称&quot;项目&quot;。</li>
<li>容器默认存在两根轴：水平的主轴（mainaxis）和垂直的交叉轴（crossaxis），项目默认沿主 轴排列。</li>
<li>flex 属性有两类：设置在 flex 容器的属性 和 设置在 item 项目中的属性：
<ul>
<li>设置在 flex 容器上属性：</li>
<li>display: flex 设置 flex 布局</li>
<li>flex-direction 属性决定主轴的方向（即项目的排列方向）。包含 x 轴（横向排列）和 y 轴（纵向排列）</li>
<li>justify-content 属性定义了项目在主轴上的对齐方式。</li>
<li>align-items 属性定义项目在交叉轴上如何对齐。</li>
<li>flex-wrap 属性定义，如果一条轴线排不下，如何换行。</li>
<li>flex-flow 属性是 flex-direction 属性和 flex-wrap 属性的简写形式，默认值为 rownowrap。</li>
<li>align-content 属性定义了多根轴线的对齐方式。
<ul>
<li>如果项目只有一根轴线，该属性不起作用。</li>
</ul>
</li>
<li>设置在项目 item 上6 个属性：
<ul>
<li>order 属性：定义项目的排列顺序。数值越小，排列越靠前，默认为 0。</li>
<li>flex-grow 属性：定义项目的放大比例，默认为 0，即如果存在剩余空间，也不放大。</li>
<li>flex-shrink 属性：定义了项目的缩小比例，默认为 1，即如果空间不足，该项目将缩小。</li>
<li>flex-basis 属性：定义了在分配多余空间之前，项目占据的主轴空间。浏览器根据这个属性，计 算主轴是否有多余空间。它的默认 值为 auto，即项目的本来大小。</li>
<li>flex 属性：是 flex-grow，flex-shrink 和 flex-basis 的简写，默认值为 0 1 auto。</li>
<li>align-self 属性允许单个项目有与其他项目不一样的对齐方式，可覆盖 align-items 属性。默认 值为 auto，表示继承父 元素的 align-items 属性，如果没有父元素，则等同于 stretch。</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>总结：</p>
<ul>
<li>flex 布局是 CSS3 新增的一种布局方式，我们可以通过将一个元素的 display 属性值设置为 flex 从而使它成为一个 flex 容器，它的所有子元素都会成为它的项目。</li>
<li>一个容器默认有两条轴，一个是水平的主轴，一个是与主轴垂直的交叉轴。我们可以使用 flex-direction 来指定主轴的方向。</li>
<li>可以使用 justify-content 来指定元素在主轴上的排列方式，使用 align-items 来指定元素 在交叉轴上的排列方式。</li>
<li>还可以使用 flex-wrap 来规定当一行排列不下时的换行方式。</li>
<li>对于容器中的项目，我们可以使用 order 属性来指定项目的排列顺序，还可以使用 flex-grow 来指定当排列空间有剩余的时候， 项目的放大比例。还可以使用 flex-shrink 来指定当排列空间不足时，项目的缩小比例</li>
</ul>
<p><strong>参考资料</strong></p>
<p><a href="https://developer.mozilla.org/en-US/docs/Mozilla/Mobile/Viewport_meta_tag" target="_blank" rel="noopener noreferrer">viewport meta tag</a></p>
<p><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/Media_Queries/Using_media_queries" target="_blank" rel="noopener noreferrer">CSS media queries</a></p>
<p><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Flexible_Box_Layout" target="_blank" rel="noopener noreferrer">Flexbox</a></p>
<p><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Grid_Layout" target="_blank" rel="noopener noreferrer">CSS Grid</a></p>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-25T19:47:29.000Z</published>
  </entry>
  <entry>
    <title type="html">浏览器</title>
    <id>https://sewar-x.github.io/browser-performance/</id>
    <link href="https://sewar-x.github.io/browser-performance/"/>
    <updated>2021-07-19T02:52:17.000Z</updated>
    <content type="html"><![CDATA[<h2 id="浏览器"> 浏览器</h2>
<h2 id="浏览器组成"> 浏览器组成</h2>
<p><img src="./../images/liulanqi-zucheng.png" alt="" /></p>
<h2 id="浏览器内核"> 浏览器内核</h2>
<p><img src="./../images/liulanqi-neihe.png" alt="" /></p>
<h2 id="浏览器进程"> 浏览器进程</h2>
<p><img src="./../images/liulanqi-jincheng.png" alt="" /></p>
<h2 id="渲染进程"> 渲染进程</h2>
<p><img src="./../images/liulanqi-xiancheng.png" alt="" /></p>
<p><strong>线程之间关系</strong></p>
<p><img src="./../images/liulanqi-xiancheng2.png" alt="" /></p>
<p><img src="./../images/liulanqi-xiancheng3.png" alt="" /></p>
<p><strong>常见问题</strong></p>
<p><img src="./../images/liulanqi-wenti.png" alt="" /></p>
<h2 id="js-引擎"> JS 引擎</h2>
<p><img src="./../images/jsyingqing2.png" alt="" /></p>
<p><img src="./../images/jsyingqing.png" alt="" /></p>
<h2 id="输入url到页面展示过程"> 输入url到页面展示过程</h2>
<h3 id="总流程"> 总流程</h3>
<p><img src="./../images/url-pase1.png" alt="" /></p>
<p><img src="./../images/url-pase2.png" alt="" /></p>
<p><img src="./../images/url-pase3.png" alt="" /></p>
<h3 id="url-解析"> url 解析</h3>
<p>url 解析发生在浏览器中, 解析输入浏览器地址栏中的 url 过程.</p>
<p><strong>总体流程</strong></p>
<p><img src="./../images/http-0.png" alt="" /></p>
<h4 id="解析url"> 解析url</h4>
<p><img src="./../images/http-1.png" alt="" /></p>
<h4 id="查找-http-缓存"> 查找 HTTP 缓存</h4>
<p><img src="./../images/http缓存.png" alt="" /></p>
<ul>
<li>详细过程查看[浏览器缓存机制](./### 浏览器缓存机制)</li>
</ul>
<h3 id="网络分层传输"> 网络分层传输</h3>
<p>在进行页面的网络加载过程，就是网络是通信过程，其中包括通信对象和通信协议。</p>
<ul>
<li>
<p>分层管理模型：<img src="./../images/osi2.png" alt="" /></p>
</li>
<li>
<p>OSI 七层通信模型: <img src="./../images/osi.png" alt="" /></p>
</li>
<li>
<p>发送端与接受端之间传输过程，每一层加上该层所属首部字段，解析时去掉首部字段：</p>
<p><img src="./../images/tcp-ip.png" alt="" /></p>
</li>
</ul>
<h4 id="准备-http-报文"> 准备 HTTP 报文</h4>
<ul>
<li>当需要发起网络请求时，浏览器需要准备 HTTP 消息。</li>
</ul>
<h5 id="http-协议"> HTTP 协议</h5>
<ul>
<li>
<p>概念：</p>
<ul>
<li>HTTP 是超文本传输协议，它定义了客户端和服务器之间交换报文的格式和方式。</li>
<li>它使用 TCP 作为传 输层协议，保证了数据传输的可靠性。</li>
<li>默认使用 80 端口。</li>
</ul>
</li>
<li>
<p>特点：</p>
<ul>
<li>
<p>无状态的协议。HTTP 是一个无状态的协议，HTTP 服务器不会保存关于客户的任何信息。</p>
<ul>
<li>使用 cookie 状态管理，弥补 HTTP  无状态缺点。</li>
</ul>
</li>
<li>
<p>非持续连接：服务器必须为每一个请求的对象建立和维护 一个全新的连接。在 HTTP1.0 以前使用的非持续的连接。</p>
<p><img src="./../images/http-非持续链接.png" alt="image-20210706115424882" /></p>
</li>
</ul>
</li>
<li>
<p>HTTP 的两种连接模式：持续连接 和 非持续连接。</p>
<ul>
<li>持续连接：TCP 连接默认不关闭，可以被多个请求复用。HTTP1.1 以后默认采用的是持续的连接。
<ul>
<li><img src="./../images/http-tcp.png" alt="image-20210706115738082" /></li>
<li>使用：在请求时，加上 Connection: keep-a live 来要求服务器不要关闭 TCP 连接。</li>
<li>特点：只要客户端和服务端没有任意一端没有提出断开链接，则保持 TCP 链接状态。</li>
<li>优点：避免每次建 立 TCP 连接三次握手时所花费的时间。</li>
<li>目前对于同一个域，大多数浏 览器支持 同时建立 6 个持久连接。</li>
</ul>
</li>
<li>非持续连接：服务器必须为每一个请求的对象建立和维护 一个全新的连接。</li>
</ul>
</li>
<li>
<p>HTTP 缺点：</p>
<ul>
<li>HTTP 报文使用<strong>明文方式发送</strong>，可能被第三方窃听。</li>
<li>HTTP 报文可能被第三方截取后修改通信内容，接收方没有办法发现报文 内容的修改。</li>
<li>HTTP 还存在认证的问题，第三方可以冒充他人参与通信。</li>
</ul>
</li>
</ul>
<h5 id="请求报文内容"> 请求报文内容</h5>
<ul>
<li>
<p>HTTP 报文有两种，一种是请求报文，一种是响应报文。</p>
<p><img src="./../images/http-报文.png" alt="image-20210706143538394" /></p>
</li>
<li>
<p>请求报文：<img src="./../images/http请求报文.png" alt="" /></p>
</li>
<li>
<p>查看 HTTP 报头信息: 打开Chrome浏览器，在浏览器中任意位置右击；点击“检查”，打开浏览器内置的开发者调试平台；点击“<strong>Network</strong>”选项卡</p>
<ul>
<li><img src="./../images/http-header.png" alt="image-20210706144440626" /></li>
<li>HTTP Request Header 请求头对照表</li>
</ul>
<table>
<thead>
<tr>
<th>Header</th>
<th>解释</th>
<th>示例</th>
</tr>
</thead>
<tbody>
<tr>
<td>Accept</td>
<td>指定客户端能够接收的内容类型</td>
<td>Accept: text/plain, text/html</td>
</tr>
<tr>
<td>Accept-Charset</td>
<td>浏览器可以接受的字符编码集。</td>
<td>Accept-Charset: iso-8859-5</td>
</tr>
<tr>
<td>Accept-Encoding</td>
<td>指定浏览器可以支持的web服务器返回内容压缩编码类型。</td>
<td>Accept-Encoding: compress, gzip</td>
</tr>
<tr>
<td>Accept-Language</td>
<td>浏览器可接受的语言</td>
<td>Accept-Language: en,zh</td>
</tr>
<tr>
<td>Accept-Ranges</td>
<td>可以请求网页实体的一个或者多个子范围字段</td>
<td>Accept-Ranges: bytes</td>
</tr>
<tr>
<td>Authorization</td>
<td>HTTP授权的授权证书</td>
<td>Authorization: Basic QWxhZGRpbjpvcGVuIHNlc2FtZQ==</td>
</tr>
<tr>
<td>Cache-Control</td>
<td>指定请求和响应遵循的缓存机制</td>
<td>Cache-Control: no-cache</td>
</tr>
<tr>
<td>Connection</td>
<td>表示是否需要持久连接。（HTTP 1.1默认进行持久连接）</td>
<td>Connection: close</td>
</tr>
<tr>
<td>Cookie</td>
<td>HTTP请求发送时，会把保存在该请求域名下的所有cookie值一起发送给web服务器。</td>
<td>Cookie: $Version=1; Skin=new;</td>
</tr>
<tr>
<td>Content-Length</td>
<td>请求的内容长度</td>
<td>Content-Length: 348</td>
</tr>
<tr>
<td>Content-Type</td>
<td>请求的与实体对应的MIME信息</td>
<td>Content-Type: application/x-www-form-urlencoded</td>
</tr>
<tr>
<td>Date</td>
<td>请求发送的日期和时间</td>
<td>Date: Tue, 15 Nov 2010 08:12:31 GMT</td>
</tr>
<tr>
<td>Expect</td>
<td>请求的特定的服务器行为</td>
<td>Expect: 100-continue</td>
</tr>
<tr>
<td>From</td>
<td>发出请求的用户的Email</td>
<td>From: user@email.com</td>
</tr>
<tr>
<td>Host</td>
<td>指定请求的服务器的域名和端口号</td>
<td>Host: www.zcmhi.com</td>
</tr>
<tr>
<td>If-Match</td>
<td>只有请求内容与实体相匹配才有效</td>
<td>If-Match: “737060cd8c284d8af7ad3082f209582d”</td>
</tr>
<tr>
<td>If-Modified-Since</td>
<td>如果请求的部分在指定时间之后被修改则请求成功，未被修改则返回304代码</td>
<td>If-Modified-Since: Sat, 29 Oct 2010 19:43:31 GMT</td>
</tr>
<tr>
<td>If-None-Match</td>
<td>如果内容未改变返回304代码，参数为服务器先前发送的Etag，与服务器回应的Etag比较判断是否改变</td>
<td>If-None-Match: “737060cd8c284d8af7ad3082f209582d”</td>
</tr>
<tr>
<td>If-Range</td>
<td>如果实体未改变，服务器发送客户端丢失的部分，否则发送整个实体。参数也为Etag</td>
<td>If-Range: “737060cd8c284d8af7ad3082f209582d”</td>
</tr>
<tr>
<td>If-Unmodified-Since</td>
<td>只在实体在指定时间之后未被修改才请求成功</td>
<td>If-Unmodified-Since: Sat, 29 Oct 2010 19:43:31 GMT</td>
</tr>
<tr>
<td>Max-Forwards</td>
<td>限制信息通过代理和网关传送的时间</td>
<td>Max-Forwards: 10</td>
</tr>
<tr>
<td>Pragma</td>
<td>用来包含实现特定的指令</td>
<td>Pragma: no-cache</td>
</tr>
<tr>
<td>Proxy-Authorization</td>
<td>连接到代理的授权证书</td>
<td>Proxy-Authorization: Basic QWxhZGRpbjpvcGVuIHNlc2FtZQ==</td>
</tr>
<tr>
<td>Range</td>
<td>只请求实体的一部分，指定范围</td>
<td>Range: bytes=500-999</td>
</tr>
<tr>
<td>Referer</td>
<td>先前网页的地址，当前请求网页紧随其后,即来路</td>
<td>Referer: http://www.zcmhi.com/archives/71.html</td>
</tr>
<tr>
<td>TE</td>
<td>客户端愿意接受的传输编码，并通知服务器接受接受尾加头信息</td>
<td>TE: trailers,deflate;q=0.5</td>
</tr>
<tr>
<td>Upgrade</td>
<td>向服务器指定某种传输协议以便服务器进行转换（如果支持）</td>
<td>Upgrade: HTTP/2.0, SHTTP/1.3, IRC/6.9, RTA/x11</td>
</tr>
<tr>
<td>User-Agent</td>
<td>User-Agent的内容包含发出请求的用户信息</td>
<td>User-Agent: Mozilla/5.0 (Linux; X11)</td>
</tr>
<tr>
<td>Via</td>
<td>通知中间网关或代理服务器地址，通信协议</td>
<td>Via: 1.0 fred, 1.1 nowhere.com (Apache/1.1)</td>
</tr>
<tr>
<td>Warning</td>
<td>关于消息实体的警告信息</td>
<td>Warn: 199 Miscellaneous warning</td>
</tr>
</tbody>
</table>
</li>
</ul>
<h5 id="uri-和-url"> URI 和 URL</h5>
<ul>
<li>URI: Uniform Resource Identifier 指的是统一资源标识符
<ul>
<li>URI 指的是统一资源标识符，用唯一的标识来确定一个资源，它是一种抽象的定义，也就是说， 不管使用什么方法来定义，只要能唯一的标识一个资源，就可以称为 URI。</li>
<li>格式:<img src="./../images/uri.png" alt="image-20210706141548949" /></li>
</ul>
</li>
<li>URL: Uniform Resource Location 指的是统一资源定位符</li>
<li>URN: Universal Resource Name 指的是统一资源名称
<ul>
<li>URL 和 URN 是 URI 的子集，URL 可 以理解为使用地址来标识资源，URN 可以理解为使用名称来标识资源</li>
</ul>
</li>
</ul>
<h5 id="mime-类型"> MIME 类型</h5>
<ul>
<li><strong>媒体类型</strong>（通常称为 <strong>Multipurpose Internet Mail Extensions</strong> 或 <strong>MIME</strong> 类型 ）是一种标准，用来表示文档、文件或字节性质和格式。它在<a href="https://tools.ietf.org/html/rfc6838" target="_blank" rel="noopener noreferrer">IETF RFC 6838</a>中进行了定义和标准化。</li>
<li>详细类型参考<a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Basics_of_HTTP/MIME_types" target="_blank" rel="noopener noreferrer">MIME 类型 MDN</a></li>
</ul>
<h5 id="请求方法"> 请求方法</h5>
<ul>
<li>
<p>请求方法表示请求访问服务器的类型。 HTTP/1.0 和 HTTP/1.1 支持的方法有以下这些，浏览器默认发起 GET 请求，如果是使用脚本发起的请求，以脚本中指定的请求方法为准。</p>
<p><img src="./../images/http-协议版本.png" alt="image.png" /></p>
</li>
<li>
<p>options 方法：</p>
<ul>
<li>OPTIONS 请求与 HEAD 类似，一般也是用于客户端查看服务器的性能。</li>
<li>这个方法会请求服务 器返回该资源所支持的所有 HTTP 请 求方法，该方法会用'*'来代替资源名称，向服务器发送 OPTIONS 请求，可以测试服务器功能 是否正常。</li>
<li>JS 的 XMLHttpRequest 对象进行 CORS 跨域资源共享时，对于复杂请求，就是使用 OPTIONS 方法发送嗅探请求，以 判断是否有对指定资源的访问权限。</li>
</ul>
</li>
</ul>
<p><strong>常见问题：</strong></p>
<blockquote>
<p>get 请求传参长度的误区?</p>
</blockquote>
<ul>
<li>误区：我们经常说 get 请求参数的大小存在限制，而 post 请求的参数大小是无限制的。
<ul>
<li>实际上 HTTP 协议从未规定 GET/POST 的请求长度限制是多少。</li>
<li>对 get 请求参数的限制是来源于浏览器或 web 服务器，浏览器或 web 服务器限制了 url 的长度。</li>
<li>为了明确这个概念，我们必须再次强调下面几点:
<ul>
<li>HTTP 协议未规定 GET 和 POST 的长度限制</li>
<li>GET 的最大长度显示是因为浏览器和 web 服务器限制了 URI 的</li>
<li>不同的浏览器和 WEB 服务器，限制的最大长度不一</li>
<li>要支持 IE，则最大长度为 2083byte; 若只支持 Chrome，则最大长度 8182 byte</li>
</ul>
</li>
</ul>
</li>
</ul>
<blockquote>
<p>get 和 post 区别？</p>
</blockquote>
<ul>
<li>应用场景不同:
<ul>
<li>GET 请求是一个幂等的请求，一般 Get 请求用于对服务器资源不会 产生影响的场景，比如说请求一个网 页。</li>
<li>Post 不是一个幂等的请求，一般用于对服务器资源会产生影响的情景。比如注册用户 这一类的操作。</li>
<li>因为不同的应用场景，所以浏览器一般会对 Get 请求缓存，但很少对 Post 请求缓存.</li>
</ul>
</li>
<li>发送的报文格式不同:
<ul>
<li>Get 请求的报文中实体部分为空
<ul>
<li>Get 请求也可以将请求的参数放入 url 中向服务器发送，这样的做法相对于 Post 请求来说，一个方面是不太安全， 因为请求的 url 会被保留在历史记录中。并且浏览器由于对 url 有一个长度上的限制，所以 会影响 get 请求发送数据时 的长度。这个限制是浏览器规定的，并不是 RFC 规定的。</li>
</ul>
</li>
<li>Post 请求的报文中实体部分 一般为向服务器发送的数据, Post 的参数传递支持更多的数据类型</li>
</ul>
</li>
</ul>
<blockquote>
<p>get 和 post 请求在缓存方面的区别?</p>
</blockquote>
<ul>
<li>缓存一般只适用于那些不会更新服务端数据的请求。</li>
<li>get 请求类似于查找的过程，用户获取数据，可以不用每次都与数据库连接，所以可以使用缓存。</li>
<li>post 不同，post 做的一般是修改和删除的工作，所以必须与数据库交互，所以不能使用缓存。</li>
<li>因此 get 请求适合于请求缓</li>
</ul>
<h5 id="协议版本"> 协议版本</h5>
<ul>
<li>HTTP 的版本号，表示客户端使用的 HTTP 协议版本。目前为止，HTTP 的版本已经到了 HTTP/3 了。</li>
<li>HTTP 协议的发展：<img src="./../images/http-版本.png" alt="HTTP 协议的发展.png" /></li>
</ul>
<p><strong>常见问题</strong></p>
<blockquote>
<p>http1.1 和 http1.0 之间有哪些区别?</p>
</blockquote>
<p>http1.1 相对于 http1.0 有这样几个区别：</p>
<ul>
<li>连接方面的区别：
<ul>
<li>http1.1 默认使用持久连接，而 http1.0 默认使用非持久连接。</li>
<li>http1.1 通过使用持久连接来使多个 http 请求复用同一个 TCP 连接，以此来避免使用非持 久连接时每次需要建立连接的时延。</li>
</ul>
</li>
<li>资源请求方面的区别：
<ul>
<li>在 http1.0 中，存在一些浪费带宽的现象，例如客户端只是需要 某个对象的一部分，而服务器却将整个对象送过来了，并且不支持断点续传功能，http1.1 则 在请求头引入了 range 头域，它允许只请求资源的某个部分，即返回码是 206（Partial Content），这样就方便了开发者自由的选择以便于充分利用带宽和连接。</li>
</ul>
</li>
<li>缓存方面的区别：
<ul>
<li>在 http1.0 中主要使用 header 里的 If-Modified-Since,Expires 来做为缓存判断的标准，http1.1 则引入了更多的缓存控制策略例如 Etag、 If-Unmodified-Since、If-Match、If-None-Match 等更多可供选择的缓存头来控制缓存策略。</li>
</ul>
</li>
<li>http1.1 中还新增了 host 字段，用来指定服务器的域名。http1.0 中认为每台服务器 都绑定一个唯一的 IP 地址，因此，请求消息中的 URL 并没有传递主机名（hostname）。但 随着虚拟主机技术的发展，在一台物理服务器上可以存在多个虚拟主机，并且它们共享一个 IP 地址。因此有了 host 字段，就可以将请求发往同一台服务器上的不同网站。</li>
<li>http1.1 相对于 http1.0 还新增了很多方法，如 PUT、HEAD、OPTIONS 等。</li>
</ul>
<h4 id="二进制分帧-使用-http2"> 二进制分帧(使用 HTTP2 )</h4>
<ul>
<li>
<p>如果使用的是 HTTP/2 协议，HTTP/2 协议在传输层之上，应用层之下定义了一个二进制分帧层，我们可以把它对应到 OSI 七层模型中的表示层，表示层负责<strong>数据格式的转换。</strong></p>
<ul>
<li>分帧层把 HTTP 1.1 中的消息转换成了 HTTP/2 中的帧格式。<img src="./../images/http2.png" alt="image.png" /></li>
</ul>
</li>
<li>
<p>HTTP/2 新的特性：</p>
<ul>
<li>二进制协议。 HTTP/2 是一个二进制协议。在 HTTP/1.1 版中，报文的头信息必须是文本（ASCII 编码），数据体可以是文本，也可以是 二进制。HTTP/2 则是一个彻底的二进 制协议，头信息和数据体都是二进制，并且统称为&quot;帧&quot;，可以分为头信息帧和数 据帧。 帧的概念是它实现多路复用的基础。</li>
<li>多路复用。 HTTP/2 实现了多路复用，HTTP/2 仍然复用 TCP 连接，但是在一个连接里， 客户端和服务器都可以同时发送多个请求或回 应，而且不用按照顺序一一发送， 这样就避免了&quot;队头堵塞&quot;的问题。 <img src="./../images/http2-多路复用.png" alt="image.png" /></li>
<li>数据流。数据流 HTTP/2 使用了数据流的概念，因为 HTTP/2 的数据包是不按顺序发送的，同一 个连接里面连续的数据包，可能属于不同的 请求。因此，必须要对数据包做标 记 指出它属于哪个请求。HTTP/2 将每个请求或回应的所有数据包，称为一个 数据流。每 个数据流都有一个独一无二的编号。数据包发送的时候，都必须标 记数据流 ID ，用来区分它属于哪个数据流。</li>
<li>头信息压缩 。头信息压缩 HTTP/2 实现了头信息压缩，由于 HTTP 1.1 协议不带有状态，每次请求都必须 附上所有信息。所以，请求的很多字段都是 重复的，比如 Cookie 和 User Agent ，一模一样的内容，每次请求都必须附带，这会浪费很多带宽，也影响 速度。HTTP/2 对这一点做了优化，引入了头信息压缩机制。
<ul>
<li>一方面，头信息使用 gzip 或 compress 压缩后再发送；</li>
<li>另一方面， 客户端和服务器同时维护一张头信息 表，所有字段都会存入这个表，生成一个索引号，以后就不发送同样字段了，只 发送索引 号，这样就能提高速度了。</li>
</ul>
</li>
<li>服务器推送。 HTTP/2 允许服务器未经请求，主动向客户端发送资源，这叫做服务器推送。使 用服务器推送，提前给客户端推送必要的资源 ，这样就可以相对减少一些延迟 时间。这里需要注意的是 http2 下服务器主动推送的是静态资源，和 WebSocket 以及使用 SSE 等方式向客户端发送即时数据的推送是不同的。</li>
</ul>
</li>
<li>
<p>HTTP/2 协议缺点： 因为 HTTP/2 使用了多路复用，一般来说同一域名下只需要使用一个 TCP 连接。 由于多个数据流使用同一个 TCP 连接，遵 守同一个流量状态控制和拥塞控制。 只要一个数据流遭遇到拥塞，剩下的数据流就没法发出去，这样就导致了后面的 所有数据都 会被阻塞。HTTP/2 出现的这个问题是由于其使用 TCP 协议的问题， 与它本身的实现其实并没有多大关系</p>
</li>
</ul>
<h4 id="dns-域名解析"> DNS 域名解析</h4>
<ul>
<li><strong>DNS 查询目的：</strong>  获取 ip 地址。比如，一个域名为：abc.com 解析为 固定IP：211.214.1.XXX。</li>
<li><strong>DNS 概念：</strong> DNS 协议提供的是一种主机名到 IP 地址的转换服务，就是我们常说的域名系 统。它是一个由分层的 DNS 服务器组成的分 布式数据库，是定义了主机如何 查询这个分布式数据库的方式的应用层协议。</li>
<li><strong>DNS 查询过程：</strong> <img src="./../images/http-2.png" alt="" /></li>
<li><strong>DNS 缓存：</strong>
<ul>
<li>在一个请求链中，当某个 DNS 服务器接收到一个 DNS 回答后，它能够将回答中的信息缓存在本 地存储器中。</li>
<li>返回的资源记录中 的 TTL 代表了该条记录的缓存的时间。(TTL 是资源记录的生存时间，它定义了资源记录能够被其他的 DNS 服务 器缓存多长时间)</li>
</ul>
</li>
<li><strong>DNS 协议运行在 UDP 协议之上， 使用 53 号 端。</strong>
<ul>
<li>DNS 为什么使用 UDP 协议作为传输层？
<ul>
<li>DNS 使用 UDP 协议作为传输层协议的主要原因是为了避免使用 TCP 协议时造成的连接时延。</li>
<li>因为为了得到一个域名的 IP 地 址，往往会向多个域名服务器查询，如果使用 TCP 协议，那么每次请求都会存在连接时延，这样使 DNS 服务变得很慢，因为大 多数的地址查询请求，都是浏览器请求页面时发出的，这样会造成网页的等待时间过长。</li>
</ul>
</li>
<li>使用 UDP 协议作为 DNS 协议的问题：
<ul>
<li>问题一：
<ul>
<li>由于历史原因，物理链路的最小 MTU = 576，所 以为了限制报文长度不超过 576， UDP 的报文段的长度被限制在 512 个字节以内，这样一旦 DNS 的查询或者应答报文，超过了 512 字节，那么基于 UDP 的 DNS 协议就会被截断为 512 字节，那么有可能用户得到的 DNS 应答就是不完整的。</li>
<li>这里 DNS 报文的长度一旦超过限制，并不 会像 TCP 协议那样被拆分成多个报文段传输，因为 UDP 协议不会维护连接状态，所以我们没 有办法确定那几个报文段属于同一 个数据，UDP 只会将多余的数据给截取掉。</li>
<li>解决方案：使用 TCP 协议去请求报文。</li>
</ul>
</li>
<li>问题二：
<ul>
<li>安全问题，就是我们没有办法确定我们得到的应答，一定是一个安全 的应答，因为应答可以被他人伪造，</li>
<li>解决方案： DNS over HTTPS 来解决这个问题</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li><strong>DNS 实现负载均衡：</strong> DNS 可以用于在冗余的服务器上实现负载平衡。
<ul>
<li>因为现在一般的大型网站使用 多台服务器提供服务，因此一个域名可能会对应 多个服务器地址。</li>
<li>当用户发起 网站域名的 DNS 请求的时候，DNS 服务器返回这个域名所对应的服务器 IP 地址的集合，但在 每个回答中，会循环这些 IP 地址的顺序，用户一般会选择 排在前面的地址发送请求。以此将用户的请求均衡的分配到各个不 同的服务器 上，这样来实现负载均衡</li>
</ul>
</li>
</ul>
<p><strong>常见问题</strong></p>
<blockquote>
<p>域名带 www 与不带 www 的区别?</p>
</blockquote>
<ul>
<li>不带www的域名为顶级域名或一级域名 ，带 www 的为二级域名（）。</li>
<li>带 www 和 不带 www 是域名完全不同的两个网站，做域名解析的时候点完全不同的两个IP或网站。
<ul>
<li>在购买域名时，域名本身是不带有www的，但由于域名要通过DNS服务器解析后才可以使用，在这个过程中每一个域名是会指向一个web服务器ip地址，由于在很早之前网站方都会增加一个&quot;www&quot;的子域名来帮助客户以更多的路径访问网站，客户通常都会按照：&quot;www.++.com&quot;的形式来访问站点；</li>
<li>如果你没有做这个www的解析那么&quot;www.++.com&quot;就不能访问，只能访问不带www的顶级域名。对于普通用户来说，这个问题可能会造成他不能访问你的站，因为他只是知道用带有&quot;www&quot;的形式访问你的站点，可能不知道&quot;++.com&quot;也是同样可以访问的！</li>
</ul>
</li>
</ul>
<blockquote>
<p>带www与不带www对网站优化 和 收录 有什么影响？</p>
</blockquote>
<ul>
<li>不带 www 的比带 www 的 搜索引擎收录量更多。尤其在权重累计与收录方面。
<ul>
<li>新域名在买来后没有经过任何处理的情况下，搜索引擎会认为你的带www与不带的为两个域名，这其中就会有个首选域名的概念</li>
<li>百度判断不带www的域名为主域名，会给予比带www更高的权重，所以导致收录会相对较多。</li>
</ul>
</li>
<li>由于没有对域名做任何处理，不管哪个被认为是首选域名，百度仍会认为你是两个域名。这样就会分散网站权重，不利于我们优化网站。尤其不利于优化带www的域名。</li>
<li>优化：
<ul>
<li>使用 301跳转。比如你想把你的带www域名作为首选域名来优化，那么只需把顶级域名做301跳转到带www的域名上即可</li>
<li>在买好域名后直接对不带www的域名不进行解析，这样就只剩带www的为首选域名。</li>
</ul>
</li>
</ul>
<h4 id="arp-地址解析"> ARP 地址解析</h4>
<p>ARP 目的：获得ip地址后进行ip地址解析(ARP)， 获得硬件地址。</p>
<p><img src="./../images/http-3.1.png" alt="" /></p>
<p><img src="./../images/http-3.2.png" alt="" /></p>
<h4 id="三次握手建立tcp连接"> 三次握手建立TCP连接</h4>
<h5 id="三次握手过程"> 三次握手过程</h5>
<p><img src="./../images/http-5.png" alt="" /></p>
<ul>
<li>TCP 三次握手过程：TCP 三次握手的建立连接的过程就是相互确认初始序号的过程，告诉对方，什么样序号的报文段能够被正确接收。
<ul>
<li>第一次握手，客户端向服务器发送一个 SYN 连接请求报文段，报文段的首部中 SYN 标志位置为 1，序号字段是一个任选的 随机数。它代表的是客户端数据的 初始序号。</li>
<li>第二次握手，服务器端接收到客户端发送的 SYN 连接请求报文段后，服务器首 先会为该连接分配 TCP 缓存和变量，然后向 客端发送 SYN ACK 报文段， 报文段的首部中 SYN 和 ACK 标志位都被置为 1，代表这是一个对 SYN 连接 请求的确认， 同时序号字段是服务器端产生的一个任选的随机数，它代表的是 服务器端数据的初始序号。确认号字段为客户端发送的序号加 一。</li>
<li>第三次握手，客户端接收到服务器的肯定应答后，它也会为这次 TCP 连接分配 缓存和变量，同时向服务器端发送一个对服务 器端的报文段的确认。第三次握 手可以在报文段中携带数据。</li>
</ul>
</li>
</ul>
<p><strong>常见 问题</strong></p>
<blockquote>
<p>SYN 是需要消耗一个序列号的，下次发送对应的 ACK 序列号要加1，为什么？</p>
</blockquote>
<ul>
<li>凡是需要对端确认的，一定消耗TCP报文的序列号。</li>
<li>SYN 需要对端的确认， 而 ACK 并不需要，因此 SYN 消耗一个序列号而 ACK 不需要</li>
</ul>
<blockquote>
<p>为什么是三次握手不是两次？</p>
</blockquote>
<ul>
<li>第三次握手的作用是客户端对 服务器端的初始序号的确认。</li>
<li>如果只使用两次握手，那么服务器就没有办法知道 自己的序号是否 已被确认。同时这样也是为了防止失效的请求报文段被服务器 接收，而出现错误的情况</li>
</ul>
<h5 id="arq协议"> ARQ协议</h5>
<ul>
<li>ARQ协议：即自动重传请求（Automatic Repeat-reQuest），是OSI模型中数据链路层和传输层的错误纠正协议之一。</li>
<li>它通过使用确认和超时这两个机制，在不可靠服务的基础上实现可靠的信息传输。如果发送方在发送后一段时间之内没有收到确认帧，它通常会重新发送。</li>
<li>ARQ包括 停止等待ARQ协议 和 连续ARQ 协议。</li>
</ul>
<h6 id="等待arq协议"> 等待ARQ协议</h6>
<ul>
<li>概念：就是每发送完一个分组就停止发送，等待对方的确认。在收到确认后再发送下一个分组。</li>
</ul>
<p><img src="./../images/arq.png" alt="image-20210706194826007" /></p>
<ul>
<li>
<p><strong>等待 ARQ 过程：</strong></p>
<ul>
<li>A 发送分组 M1，发完就暂停发送，等待 B 的确认 (ACK)。B 收到了 M1 向 A 发送  ACK。A 在收到了对 M1 的确认后，就再发送下一个分组  M2。
<ul>
<li>在发送完一个分组后，必须暂时保留已发送的分组的副本，以备重发。</li>
<li>分组和确认分组都必须进行编号。</li>
</ul>
</li>
<li>如果 B 在规定的时间内没有收到已发送分组的肯定回答，则 A 重新发送上一个分组。</li>
<li>对于接受方 B 来说，每次接受到一个分组，就返回对这个分组的肯定应答，当收到 冗余的分组时，就直接丢弃，并返回一个对冗余 分组的确认。</li>
<li>当收到分组损坏 的情况的时候，直接丢弃。</li>
</ul>
</li>
<li>
<p><strong>超时重传：</strong></p>
<ul>
<li>在接收方 B 会出现两种情况：
<ul>
<li>B 接收 M1 时检测出了差错，就丢弃 M1，其他什么也不做（不通知 A 收到有差错的分组）。</li>
<li>M1 在传输过程中丢失了，这时 B 当然什么都不知道，也什么都不做。</li>
</ul>
</li>
<li>在这两种情况下，B 都不会发送任何信息。</li>
<li>如何保证 B 正确收到了 M1 呢？
<ul>
<li>解决方法：超时重传
<ul>
<li>A 为每一个已发送的分组都设置了一个超时计时器。</li>
<li>A 只要在超时计时器到期之前收到了相应的确认，就撤销该超时计时器，继续发送下一个分组 M2 。
<ul>
<li>超时计时器的重传时间应当比数据在分组传输的平均往返时间更长一些。</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>确认丢失：</strong></p>
<ul>
<li>若 B 所发送的对 M1 的确认丢失了，那么 A 在设定的超时重传时间内不能收到确认，但 A 并无法知道：是自己发送的分组出错、丢失了，或者 是 B 发送的确认丢失了。因此 A 在超时计时器到期后就要重传 M1。</li>
<li>假定 B 又收到了重传的分组 M1。这时 B 应采取两个行动：
<ul>
<li>第一，丢弃这个重复的分组 M1，不向上层交付。</li>
<li>第二，向 A 发送确认。不能认为已经发送过确认就不再发送，因为 A 之所以重传 M1 就表示 A 没有收到对 M1 的确认。</li>
</ul>
</li>
</ul>
<p><img src="./../images/ARQ-确认丢失.png" alt="image-20210706201433613" /></p>
</li>
<li>
<p><strong>确认迟到:</strong></p>
<ul>
<li>传输过程中没有出现差错，但 B 对分组 M1 的确认迟到了。</li>
<li>A 会收到重复的确认。对重复的确认的处理很简单：收下后就丢弃。</li>
<li>B 仍然会收到重复的 M1，并且同样要丢弃重复的 M1，并重传确认分组。</li>
<li><img src="./../images/ARQ-确认迟到.png" alt="image-20210706201525251" /></li>
</ul>
</li>
<li>
<p>优点： 简单。</p>
</li>
<li>
<p><strong>缺点：</strong> 每次发送分组必须等到分组确认后才能发送 下一个分组，这样会造成信道的利用率过低。</p>
<ul>
<li><img src="./../images/ARQ-信道利用率.png" alt="image-20210706202307763" />
<ul>
<li>当往返时间 RTT 远大于分组发送时间 TD 时，信道的利用率就会非常低。若出现重传，则对传送有用的数据信息来说，信道的利用率就还要降低。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h6 id="连续arq-协议"> 连续ARQ 协议</h6>
<ul>
<li>
<p>作用：连续 ARQ 协议是为了解决停止等待 ARQ 协议对于信道的利用率过低的问题。</p>
</li>
<li>
<p>连续 ARQ 协议原理：</p>
<ul>
<li>
<p>连续ARQ协议发送方采用流水线传输。发送方可以连续发送多个分组，不必每发完一个分组就停下来等待对方确认<img src="./../images/ARQ-连续发送.png" alt="img" /></p>
</li>
<li>
<p>通常是结合滑动窗口协议来使用的，发送方需要维持一个发送窗口，它通过连续发送一组分组，然后再等待对分组的 确认回答。</p>
<ul>
<li>
<p><img src="./../images/ARQ-滑动窗口.png" alt="img" /></p>
</li>
<li>
<p>位于发送窗口内的5个分组都可以连续发送出去，而不需要等待对方的确认，这样就提高了信道利用率。</p>
</li>
<li>
<p>连续ARQ协议规定，发送方每收到一个确认，就把发送窗口向前滑动一个分组的位置。例如上面的图（b），当发送方收到第一个分组的确认，就把发送窗口向前移动一个分组的位置。如果原来已经发送了前5个分组，则现在可以发送窗口内的第6个分组。</p>
</li>
</ul>
</li>
<li>
<p>接收方一般都是采用累积确认的方式。</p>
<ul>
<li>累积确认：
<ul>
<li>收方不必对收到的分组逐个发送确认。而是在收到几个分组后，对按序到达的最后一个分组发送确认。如果收到了这个分组确认信息，则表示到这个分组为止的所有分组都已经正确接收到了。</li>
<li>优点：容易实现，即使确认丢失也不必重传</li>
<li>缺点：不能正确的向发送方反映出接收方已经正确收到的所以分组的信息。</li>
</ul>
</li>
<li>Go-back-N（回退N）：
<ul>
<li>比如发送方发送了前5个分组，而中间的第3个分组丢失了，这时候接收方只能对前2个发出确认。而不知道后面3个分组的下落。因此只能把后面的3个分组都重传一次，这种机制叫Go-back-N（回退N），表示需要再退回来重传已发送过的N个分组。</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>
<p>对于如何处理分组 中可能出现的差错恢复情况，一般可以使用滑动窗口协议和选择重传协议来实现。</p>
</li>
</ul>
<h6 id="滑动窗口协议"> 滑动窗口协议</h6>
<ul>
<li>
<p>概念：滑动窗口协议在在发送方和接收方之间各自维持一个滑动窗口，发送发是发送窗口，接收方是接收窗口，而且这个窗口是随着时间变化可以向前滑动的。它允许发送方发送多个分组而不需等待确认。TCP的滑动窗口是以字节为单位的。</p>
<p><img src="./../images/ARQ-发送窗口.png" alt="img" /></p>
</li>
<li>
<p>发送窗口四个状态：</p>
<ul>
<li>已发送并收到确认的数据（不在发送窗口和发送缓冲区之内）</li>
<li>已发送但未收到确认的数据（位于发送窗口之内）</li>
<li>允许发送但尚未发送的数据（位于发送窗口之内）</li>
<li>发送窗口之外的缓冲区内暂时不允许发送的数据。</li>
</ul>
</li>
<li>
<p>接收窗口四个状态：</p>
<ul>
<li>已发送确认并交付主机的数据（不在接收窗口和接收缓冲区之内）</li>
<li>未按序收到的数据（位于接收窗口之内）</li>
<li>允许的数据（位于接收窗口之内）</li>
<li>不允许接收的数据（位于发送窗口之内）</li>
</ul>
</li>
<li>
<p>规则：</p>
<ul>
<li>凡是已经发送过的数据，在未收到确认之前，都必须暂时保留，以便在超时重传时使用。</li>
<li>只有当发送方A收到了接收方的确认报文段时，发送方窗口才可以向前滑动几个序号。</li>
<li>当发送方A发送的数据经过一段时间没有收到确认（由超时计时器控制），就要使用回退N步协议，回到最后接收到确认号的地方，重新发送这部分数据。</li>
</ul>
</li>
<li>
<p>应用：</p>
<ul>
<li>连续 ARQ 协议</li>
<li>TCP利用滑动窗口协议来进行流量控制</li>
</ul>
</li>
</ul>
<h5 id="tcp-协议"> <strong>TCP 协议</strong></h5>
<ul>
<li>
<p>TCP 是面向连接的运输层协议。在通信双方进行通信前，需要通过三次握手建 立连接。它需要在端系统中维护双方连接的状态信息。</p>
</li>
<li>
<p>TCP 协议提供的是点对点的服务。每一条 TCP 连接只能有两个端点，每一条TCP连接只能是点对点的（一对一）。</p>
</li>
<li>
<p>TCP 提供可靠交付的服务。TCP协议通过使用<strong>连续ARQ协议</strong>和<strong>滑动窗口协议</strong>，来保证数据传输的正确性，从而提供可靠的传输。</p>
</li>
<li>
<p>TCP 提供全双工通信。也就是说连接的双方的能够向对方发送和接收数据。</p>
</li>
<li>
<p>TCP 提供了<strong>拥塞控制机制</strong>，在网络拥塞的时候会控制发送数据的速率， 有助于减少数据包的丢失和减轻网络中的拥塞程度。</p>
</li>
<li>
<p>TCP 提供了<strong>流量控制机制</strong>，保证了通信双方的发送和接收速率相同。如果接收方可接收的缓存很小时，发送方会降低发送速率，避免因为缓存 填满而造成的数据包的丢失。</p>
</li>
<li>
<p>面向字节流：TCP 中的“流”(stream)指的是流入或流出进程的字节序列。</p>
<ul>
<li>“面向字节流”的含义是：虽然应用程序和 TCP 的交互是一次一个数据块，但 TCP 把应用程序交下来的数据看成仅仅是一连串无结构的字节流。</li>
<li>TCP 不保证接收方应用程序所收到的数据块和发送方应用程序所发出的数据块具有对应大小的关系。但接收方应用程序收到的字节流必须和发送方应用程序发出的字节流完全一样。</li>
</ul>
</li>
</ul>
<h5 id="tcp-可靠传输机制"> TCP 可靠传输机制</h5>
<ul>
<li>TCP 的可靠运输机制是基于 连续 ARQ 协议 和 滑动窗口协议 的。</li>
<li>TCP  滑动窗口协议：并不完全和滑动窗口协议相同，因为许多的 TCP 实现会将失序的报文段给缓存起来，并且发生重传时，只会重 传一个报文段，因此 TCP 协议的可靠传输机制更像是窗口滑动协议和选择重传协议的一个混合体。</li>
</ul>
<h5 id="tcp-拥塞控制机制"> TCP 拥塞控制机制</h5>
<ul>
<li>
<p>拥塞：</p>
<ul>
<li>概念：在某段时间，若对网络中某资源的需求超过了该资源所能提供的可用部分，网络的性能就要变坏。</li>
<li>影响：若网络中有许多资源同时产生拥塞，网络的性能就要明显变坏，整个网络的吞吐量将随输入负荷的增大而下降。</li>
<li>原因：所有资源需求 &gt; 可用资源</li>
</ul>
</li>
<li>
<p>拥塞控制：</p>
<ul>
<li>
<p>前提：网络能够承受现有的网络负荷。</p>
</li>
<li>
<p>作用：</p>
<ul>
<li>
<p>在网络拥塞的时候会控制发送数据的速率， 有助于减少数据包的丢失和减轻网络中的拥塞程度。</p>
</li>
<li>
<p>防止过多的数据注入到网络中，使网络中的路由器或链路不致过载。</p>
<p><img src="./../images/TCP-拥塞控制.png" alt="image-20210706234455818" /></p>
</li>
</ul>
</li>
<li>
<p>原理：TCP 的拥塞控制主要使用了四个机制，分别是慢开始、拥塞避免、快速重传和 快速恢复。</p>
</li>
</ul>
</li>
</ul>
<h6 id="慢开始"> 慢开始</h6>
<ul>
<li>
<p>作用：用来确定网络的负载能力。</p>
</li>
<li>
<p>思想：由小到大逐渐增大拥塞窗口数值。使用指数增长拥塞窗口方式。</p>
<ul>
<li>为在发送方刚开始发送数据的时候，并不知道网络中的 拥塞程度，所以先以较低的速率发送，进行试探</li>
<li>每次收到一个确认报文，就将发动窗口的长度加一，这样每个 RTT 时间后，发送窗口的长度就会加倍。</li>
<li>当发送窗口的大小达到一个阈值的时候就进入拥塞避免算法。</li>
<li><img src="./../images/TCP-慢开始2.png" alt="image-20210707001300166" /></li>
</ul>
</li>
<li>
<p>初始拥塞窗口 cwnd 设置：</p>
<ul>
<li>旧的规定：在刚刚开始发送报文段时，先把初始拥塞窗口cwnd 设置为 1 至 2 个发送方的最大报文段 SMSS (Sender Maximum Segment Size) 的数值。</li>
<li>新的 RFC 5681 把初始拥塞窗口 cwnd 设置为不超过2至4个SMSS 的数值。</li>
</ul>
</li>
<li>
<p>慢开始门限 ssthresh（状态变量）：防止拥塞窗口cwnd 增长过大引起网络拥塞。</p>
</li>
<li>
<p>拥塞窗口 cwnd  控制方法：在每收到一个对新的报文段的确认后，可以把拥塞窗口增加最多一个 SMSS 的数值。</p>
<ul>
<li>拥塞窗口cwnd每次的增加量 = min (N, SMSS)
<ul>
<li>其中 N 是原先未被确认的、但现在被刚收到的确认报文段所确认的字节数。</li>
<li>不难看出，当 N &lt; SMSS 时，拥塞窗口每次的增加量要小于 SMSS。</li>
</ul>
</li>
<li>用这样的方法逐步增大发送方的拥塞窗口 cwnd，可以使分组注入到网络的速率更加合理。</li>
</ul>
</li>
<li>
<p>初始请求的响应：</p>
<ul>
<li>初始请求的响应包含所接收数据的第一个字节。</li>
<li>”Time to First Byte“ (TTFB)是用户通过点击链接进行请求与收到第一个HTML包之间的时间。</li>
<li>第一块内容通常是14kb的数据。首先进行TCP慢开始<img src="./../images/慢开始.png" alt="" /></li>
</ul>
</li>
</ul>
<h6 id="拥塞避免"> 拥塞避免</h6>
<ul>
<li>思想： 为了避免可能发生的拥塞，让拥塞窗口 cwnd 缓慢地增大，即每经过一个往返时间 RTT 就把发送方的拥塞窗口 cwnd 加 1，而不是加倍，使拥塞窗口 cwnd 按线性规律缓慢增长。 这样将窗口的增长速率由指数增长，变为加法线性增长。</li>
<li>特点：在拥塞避免阶段，拥塞窗口 cwnd 按线性规律缓慢增长，比慢开始算法的拥塞窗口增长速率缓慢得多。</li>
<li>AIMD 算法：
<ul>
<li>在拥塞避免阶段，拥塞窗口是按照线性规律增大的。这常称为“加法增大” AI (Additive Increase)。</li>
<li>当出现超时或3个重复的确认时，就要把门限值设置为当前拥塞窗口值的一半，并大大减小拥塞窗口的数值。这常称为“乘法减小”MD (Multiplicative Decrease)。</li>
</ul>
</li>
<li>注意：
<ul>
<li>“拥塞避免”并非指完全能够避免了拥塞。利用以上的措施要完全避免网络拥塞还是不可能的。</li>
<li>“拥塞避免”是说在拥塞避免阶段把拥塞窗口控制为按线性规律增长，使网络比较不容易出现拥塞。</li>
</ul>
</li>
</ul>
<h6 id="快速重传"> 快速重传</h6>
<ul>
<li>
<p>作用：</p>
<ul>
<li>采用快重传FR (Fast Retransmission) 算法可以让发送方尽早知道发生了个别报文段的丢失。</li>
<li>使用快重传可以使整个网络的吞吐量提高约 20%</li>
</ul>
</li>
<li>
<p>思想：</p>
<ul>
<li>首先要求接收方不要等待自己发送数据时才进行捎带确认，而是要立即发送确认，即使收到了失序的报文段也要立即发出对已收到的报文段的重复确认。</li>
<li>发送方只要一连收到三个重复确认，就知道接收方确实没有收到报文段，因而应当立即进行重传（即“快重传”），这样就不会出现超时，发送方也不就会误认为出现了网络拥塞。</li>
</ul>
<p><img src="./../images/TCP-快重传.png" alt="image-20210707000229167" /></p>
</li>
</ul>
<h6 id="快速恢复"> 快速恢复</h6>
<ul>
<li>
<p>当发送端收到连续三个重复的确认时，由于发送方现在认为网络很可能没有发生拥塞，因此现在不执行慢开始算法，而是执行快恢复算法。</p>
</li>
<li>
<p>快恢复算法 FR (Fast Recovery) ：</p>
<ul>
<li>慢开始门限 ssthresh = 当前拥塞窗口 cwnd / 2 ；</li>
<li>新拥塞窗口 cwnd = 慢开始门限 ssthresh ；</li>
<li>开始执行拥塞避免算法，使拥塞窗口缓慢地线性增大。</li>
</ul>
</li>
</ul>
<p><strong>TCP 拥塞控制示例</strong></p>
<p><img src="./../images/TCP-慢开始.png" alt="image-20210707000416303" /></p>
<ul>
<li>当 TCP 连接进行初始化时，将拥塞窗口置为 1。图中的窗口单位不使用字节而使用报文段。</li>
<li>发送端的发送窗口不能超过拥塞窗口 cwnd 和接收端窗口 rwnd 中的最小值。我们假定接收端窗口足够大，因此现在发送窗口的数值等于拥塞窗口的数值。</li>
<li>慢开始1：
<ul>
<li>在执行慢开始算法时，拥塞窗口 cwnd=1，发送第一个报文段。</li>
<li>发送方每收到一个对新报文段的确认 ACK，就把拥塞窗口值加 1，然后开始下一轮的传输（请注意，横坐标是传输轮次，不是时间）。因此拥塞窗口 cwnd 随着传输轮次按指数规律增长。</li>
<li>当拥塞窗口 cwnd 增长到慢开始门限值ssthresh 时（图中的点 1，此时拥塞窗口cwnd = 16），就改为执行拥塞避免算法，拥塞窗口按线性规律增长。</li>
</ul>
</li>
<li>拥塞避免1：
<ul>
<li>当拥塞窗口 cwnd = 24 时，网络出现了超时（图中的点2），发送方判断为网络拥塞。</li>
<li>于是调整门限值 ssthresh = cwnd / 2 = 12，同时设置拥塞窗口 cwnd = 1，进入慢开始2阶段。</li>
</ul>
</li>
<li>慢开始2：
<ul>
<li>按照慢开始算法，发送方每收到一个对新报文段的确认ACK，就把拥塞窗口值加1。</li>
<li>当拥塞窗口cwnd = ssthresh = 12时（图中的点3，这是新的ssthresh值），改为执行拥塞避免算法，拥塞窗口按线性规律增大。</li>
</ul>
</li>
<li>拥塞避免3：
<ul>
<li>当拥塞窗口cwnd = 16时（图中的点4），出现了一个新的情况，就是发送方一连收到 3 个对同一个报文段的重复确认（图中记为3-ACK）。发送方改为执行快重传和快恢复算法。</li>
</ul>
</li>
<li>快速恢复：
<ul>
<li>在图的点4，发送方知道现在只是丢失了个别的报文段。于是不启动慢开始，而是执行快恢复算法。</li>
<li>这时，发送方调整门限值ssthresh = cwnd / 2 = 8，同时设置拥塞窗口cwnd = ssthresh = 8（见图中的点5），并开始执行拥塞避免算法。</li>
</ul>
</li>
</ul>
<p><strong>TCP 拥塞控制流程图</strong></p>
<p><img src="./../images/TCP拥塞控制流程图.png" alt="" /></p>
<p><strong>常见问题</strong></p>
<blockquote>
<p>增加资源能解决拥塞吗？</p>
</blockquote>
<ul>
<li>不能。</li>
<li>因为网络拥塞是一个非常复杂的问题。简单地增加资源，在许多情况下，不但不能解决拥塞问题，而且还可能使网络的性能更坏。</li>
<li>网络拥塞往往是由许多因素引起的。例如：
<ul>
<li>增大缓存，但未提高输出链路的容量和处理机的速度，排队等待时间将会大大增加，引起大量超时重传，解决不了网络拥塞；</li>
<li>提高处理机处理的速率会会将瓶颈转移到其他地方；</li>
</ul>
</li>
</ul>
<blockquote>
<p>拥塞控制与流量控制的区别 ?</p>
</blockquote>
<ul>
<li>流量控制：
<ul>
<li>往往指点对点通信量的控制，是个端到端的问题（接收端控制发送端）。</li>
<li>流量控制所要做的就是抑制发送端发送数据的速率，以便使接收端来得及接收。</li>
</ul>
</li>
<li>拥塞控制和流量控制之所以常常被弄混，是因为某些拥塞控制算法是向发送端发送控制报文，并告诉发送端，网络已出现麻烦，必须放慢发送速率。这点又和流量控制是很相似的。</li>
</ul>
<h5 id="tcp-流量控制机制"> TCP 流量控制机制</h5>
<ul>
<li>一般说来，我们总是希望数据传输得更快一些。但如果发送方把数据发送得过快，接收方就可能来不及接收，这就会造成数据的丢失。</li>
<li>作用：保证了通信双方的发送和接收速率相同。流量控制 (flow control) 就是让发送方的发送速率不要太快，既要让接收方来得及接收，也不要使网络发生拥塞。如果接收方可接收的缓存很小时，发送方会降低发送速率，避免因为缓存 填满而造成的数据包的丢失。</li>
<li>原理：利用滑动窗口机制可以很方便地在 TCP 连接上实现流量控制。</li>
<li><img src="./../images/ARQ-流量控制.png" alt="img" /></li>
</ul>
<h4 id="tls-协商-使用https"> TLS 协商 (使用HTTPS)</h4>
<h5 id="tls-协商过程"> TLS 协商过程</h5>
<p><img src="./../images/http-4.png" alt="" /></p>
<ul>
<li>SSL 链接断开后恢复：一共有两种方法来恢复断开的 SSL 连接，一种是使用 session ID，一种是 session ticket。
<ul>
<li>session ID 方式：</li>
<li>每一次的会话都有一个编号，当对话中断后，下一次重新连接时， 只要客户端给出这个编号，服务器 如果有这个编号的记录，那么双方就可以继续使用以前的秘钥，而不用重新生成一把。</li>
<li>目前所有 的浏览器都支持这一种方法。</li>
<li>缺点：session ID 只能够存在一台服务器上，如果我们的请求通过负载平衡 被转移到了其他的服务器上，那 么就无法恢复对话。</li>
<li>session ticket 方式：
<ul>
<li>session ticket 是服务器在上一次对话中发送给客 户的，这个 ticket 是加密的 ，只有服务器能够解密，里面包含了本次会话的信息，比如对话秘钥和加密方法等。</li>
<li>这样不管我们的请求是否转移到其他的服务器 上，当服务器将 ticket 解密以后，就能够获取上次对话的信息，就不用重新生成对话秘钥了。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h5 id="https-协议"> HTTPS 协议</h5>
<ul>
<li>概念：
<ul>
<li>HTTPS = HTTP + 加密 (SSL加密) + 认证 + 完整性保护</li>
<li>HTTPS 指的是超文本传输安全协议，HTTPS 是基于 HTTP 协议的，使用 TLS/SSL 来对数据加密。使用 TLS/ SSL 协议，所有的信息都是加密的，第三方没有办法窃听。</li>
<li>HTTPS 的端口号是 443。</li>
<li><img src="./../images/https.png" alt="image-20210707113037403" /></li>
</ul>
</li>
<li>特点：
<ul>
<li><strong>混合加密的方式实现信息的机密性</strong>，使其免受窃听者对数据的监听。</li>
<li>提供校验机制，<strong>摘要算法的方式来实现完整性</strong>，信息一旦被篡改，通信的双方会立刻发现。</li>
<li>配备身份证书，<strong>将服务器公钥放入到数字证书中</strong>，防止身份被冒充的情况出现。防止中间人攻击并建立用户信任。</li>
</ul>
</li>
</ul>
<h6 id="ssl-和-tls"> SSL 和 TLS</h6>
<ul>
<li>
<p>SSL / TLS：</p>
<ul>
<li>
<p>基本目标：实现两个应用实体之间的安全可靠通信。</p>
</li>
<li>
<p>位置：在发送方，SSL 接收应用层的数据，对数据进行加密，然后把加了密的数据送往 TCP 套接字。在接收方，SSL 从 TCP 套接字读取数据，解密后把数据交给应用层。</p>
<p><img src="./../images/SSL-TSL.png" alt="image-20210707114420012" /></p>
</li>
</ul>
</li>
<li>
<p>安全套接字层 SSL (Secure Socket Layer)  ：</p>
<ul>
<li>SSL 作用在端系统应用层的 HTTP 和运输层之间，在 TCP 之上建立起一个安全通道，为通过 TCP 传输的应用层数据提供安全保障。</li>
<li>1996年发布 SSL 3.0，成为 Web 安全的事实标准。</li>
<li>1999年，IETF 在 SSL 3.0 基础上推出了传输层安全标准 TLS，为所有基于 TCP 的网络应用提供安全数据传输服务。</li>
</ul>
</li>
<li>
<p>运输层安全 TLS (Transport Layer Security) ：</p>
<ul>
<li>作用：用于两个通信应用程序之间提供保密性和数据完整性。使用<code>对称加密</code>和 <code>非对称加密</code> 两种形式。</li>
<li>基本格式： <strong>密钥交换算法 - 签名算法 - 对称加密算法 - 摘要算法</strong> 组成的一个密码串。
<ul>
<li>密钥交换：ECDHE</li>
<li>签名和认证：ECDSA</li>
<li>对称加密算法：AES ，密钥的长度是 256 位</li>
<li>分组模式：GCM</li>
<li>摘要算法：SHA384</li>
</ul>
</li>
</ul>
</li>
</ul>
<h6 id="混合加密"> 混合加密</h6>
<ul>
<li>作用：通过混合加密的方式可以保证信息的机</li>
<li>密性，解决了窃听的风险。</li>
<li>HTTPS 采用的是对称加密和非对称加密结合的「混合加密」方式：
<ul>
<li>在通信建立前采用非对称加密的方式交换「会话秘钥」，后续就不再使用非对称加密。</li>
<li>在通信过程中全部使用对称加密的「会话秘钥」的方式加密明文数据。</li>
<li>详细过程：
<ul>
<li>在通信刚开始的时候使用非对称算法，比如 RSA、ECDHE ，首先解决<code>密钥交换</code>的问题。</li>
<li>然后用随机数产生对称算法使用的<code>会话密钥（session key）</code>，再用<code>公钥加密</code>。</li>
<li>对方拿到密文后用<code>私钥解密</code>，取出会话密钥。这样，双方就实现了对称密钥的安全交换。</li>
</ul>
</li>
</ul>
</li>
<li>采用「混合加密」的方式的原因：
<ul>
<li>对称加密只使用一个密钥，运算速度快，密钥必须保密，无法做到安全的密钥交换。</li>
<li>非对称加密使用两个密钥：公钥和私钥，公钥可以任意分发而私钥保密，解决了密钥交换问题但速度慢。</li>
</ul>
</li>
</ul>
<p><img src="./../images/混合加密.png" alt="image-20210707130707141" /></p>
<p><strong>对称加密</strong>：</p>
<ul>
<li>加密和解密时使用的密钥都是同样的密钥</li>
<li>只要保证了密钥的安全性，那么整个通信过程也就是具有了机密性</li>
<li><img src="./../images/对称加密.png" alt="image-20210707124429013" /></li>
<li>加密算法：
<ul>
<li>DES：
<ul>
<li>属于对称密钥密码体制，是一种分组密码。</li>
<li>DES 的保密性仅取决于对密钥的保密，其算法是公开的。目前较为严重的问题是 DES 的密钥的长度。现在已经设计出搜索 DES 密钥的专用芯片。56位 DES 已不再认为是安全的了。</li>
<li>加密算法：
<ul>
<li>在加密前，先对整个明文进行分组。每一个组长为 64 位。</li>
<li>然后对每一个 64 位 二进制数据进行加密处理，产生一组 64 位密文数据。</li>
<li>最后将各组密文串接起来，即得出整个的密文。</li>
<li>使用的密钥为 64 位（实际密钥长度为 56 位，有 8 位用于奇偶校验)。</li>
<li><img src="./../images/DES.png" alt="image-20210707125040119" /></li>
</ul>
</li>
</ul>
</li>
<li>3DES：
<ul>
<li>算法：使用两个 56 位的密钥。把一个 64 位明文用一个密钥加密，再用另一个密钥解密，然后再使用第一个密钥加密<img src="./../images/3DES.png" alt="image-20210707125206774" /></li>
</ul>
</li>
<li>AES：全称是<code>Advanced Encryption Standard(高级加密标准)</code>，它是 DES 算法的替代者，安全强度很高，性能也很好，是应用最广泛的对称加密算法。</li>
</ul>
</li>
</ul>
<p><strong>非对称加密</strong></p>
<ul>
<li>
<p>概念：<code>非对称加密(Asymmetrical Encryption)</code> 也被称为<code>公钥加密</code>，相对于对称加密来说，非对称加密是一种新的改良加密方式。</p>
</li>
<li>
<p>密钥：非对称加密中有两个密钥，一个是公钥，一个是私钥。</p>
<ul>
<li>加密：公钥进行加密，私钥进行解密。
<ul>
<li>使用公钥加密的文本只能使用私钥解密（公钥加密 -&gt; 私钥解密）</li>
<li>使用私钥加密的文本也可以使用公钥解密 （私钥加密 -&gt; 公钥解密）</li>
</ul>
</li>
<li>公开密钥可供任何人使用，私钥只有你自己能够知道。</li>
</ul>
</li>
<li>
<p>密钥传输：</p>
<ul>
<li>通过网络传输交换，它能够确保及时密钥被拦截，也不会暴露数据信息。</li>
<li>公钥不需要具有安全性，因为公钥需要在网络间进行传输，非对称加密可以解决<code>密钥交换</code>的问题。</li>
<li>网站保管私钥，在网上任意分发公钥，你想要登录网站只要用公钥加密就行了，密文只能由私钥持有者才能解密。而黑客因为没有私钥，所以就无法破解密文。</li>
</ul>
<p><img src="./../images/非对称加密.png" alt="image-20210707130001983" /></p>
</li>
<li>
<p>非对称加密算法：</p>
<ul>
<li><code>RSA</code> ：例如 <code>DHE_RSA_CAMELLIA128_GCM_SHA256</code>。它的安全性基于 <code>整数分解</code>，使用两个超大素数的乘积作为生成密钥的材料，想要从公钥推算出私钥是非常困难的。</li>
<li><code>ECC（Elliptic Curve Cryptography）</code>：基于<code>椭圆曲线离散对数</code>的数学难题，使用特定的曲线方程和基点生成公钥和私钥， ECDHE 用于密钥交换，ECDSA 用于数字签名。</li>
</ul>
</li>
</ul>
<h6 id="摘要算法"> 摘要算法</h6>
<ul>
<li>作用：摘要算法用来实现完整性，能够为数据生成独一无二的「指纹」，用于校验数据的完整性，解决了篡改的风险。</li>
</ul>
<p><img src="./../images/摘要算法.png" alt="img" /></p>
<ul>
<li>摘要算法：
<ul>
<li>客户端在发送明文之前会通过摘要算法算出明文的「指纹」</li>
<li>发送的时候把「指纹 + 明文」一同加密成密文后，发送给服务器</li>
<li>服务器解密后，用相同的摘要算法算出发送过来的明文，通过比较客户端携带的「指纹」和当前算出的「指纹」做比较，若「指纹」相同，说明数据是完整的。</li>
</ul>
</li>
<li>问题：摘要算法实现的报文鉴别可以防篡改，但不能防伪造，因而不能真正实现报文鉴别。</li>
<li>常见摘要算法：
<ul>
<li>MD5 ：
<ul>
<li>概念：全称是 <code>Message Digest Algorithm 5</code>，它是属于<code>密码哈希算法(cryptographic hash algorithm)</code>的一种，MD5 可用于从任意长度的字符串创建 128 位字符串值。</li>
<li>基本思想：用足够复杂的方法将报文的数据位充分“弄乱”，报文摘要代码中的每一位都与原来报文中的每一位有关。</li>
<li>计算步骤：
<ol>
<li>附加：把任意长的报文按模 2^64 计算其余数（64位），追加在报文的后面（长度项）。</li>
<li>填充：在报文和长度项之间填充 1~512 位，使得填充后的总长度是 512 的整数倍。填充的首位是 1，后面都是 0。</li>
<li>分组：把追加和填充后的报文分割为一个个 512 位的数据块，每个 512 位的报文数据再分成 4 个 128 位的数据块</li>
<li>计算：将 4 个 128 位的数据块依次送到不同的散列函数进行4轮计算。每一轮又都按 32 位的小数据块进行复杂的运算。一直到最后计算出 MD5 报文摘要代码（128位）。</li>
</ol>
</li>
</ul>
</li>
<li>安全散列算法（SHA-1）:
<ul>
<li>SHA 比 MD5 更安全，但计算起来却比 MD5 要慢些。</li>
<li>基本思想：
<ul>
<li>要求输入码长小于 264 位，输出码长为 160 位。</li>
<li>将明文分成若干 512 位的定长块，每一块与当前的报文摘要值结合，产生报文摘要的下一个中间结果，直到处理完毕</li>
<li>共扫描 5 遍，效率略低于 MD5，抗穷举性更高。</li>
</ul>
</li>
</ul>
</li>
<li>报文鉴别码 MAC：
<ul>
<li>全称是<code>message authentication code</code>，它通过 MAC 算法从消息和密钥生成，MAC 值允许验证者（也拥有秘密密钥）检测到消息内容的任何更改，从而保护了消息的数据完整性</li>
<li>基本思想：防范上述攻击，可以对散列进行一次加密。散列加密后的结果叫做报文鉴别码 MAC (Message Authentication Code)。</li>
<li>由于入侵者不掌握密钥 ，所以入侵者无法伪造发送者的报文鉴别码 MAC，因而无法伪造发送者发送的报文。这样就完成了对报文的鉴别。<img src="./../images/MAC.png" alt="image-20210707134651178" />
<ul>
<li>现在整个的报文是不需要加密的。</li>
<li>虽然从散列H导出报文鉴别码 MAC 需要加密算法，但由于散列 H 的长度通常都远远小于报文 X 的长度，因此这种加密不会消耗很多的计算资源。因此，使用鉴别码 MAC 就能够很方便地保护报文的完整性。</li>
</ul>
</li>
</ul>
</li>
<li>HMAC ：
<ul>
<li>MAC 更进一步的拓展，它是使用 MAC 值 + Hash 值的组合方式，HMAC 的计算中可以使用任何加密哈希函数，例如 SHA-256 等。<img src="./../images/HMAC.png" alt="image-20210707134841844" /></li>
</ul>
</li>
</ul>
</li>
</ul>
<h6 id="数字证书"> 数字证书</h6>
<ul>
<li>客户端先向服务器端索要公钥，然后用公钥加密信息，服务器收到密文后，用自己的私钥解密。</li>
<li>这就存在些问题，如何保证公钥不被篡改和信任度？
<ul>
<li>借助第三方权威机构 CA （数字证书认证机构），将服务器公钥放在数字证书（由数字证书认证机构颁发）中，只要证书是可信的，公钥就是可信的。通过数字证书的方式保证服务器公钥的身份，解决冒充的风险。<img src="./../images/CA.png" alt="img" /></li>
</ul>
</li>
</ul>
<h4 id="传输层-封装成报文段"> 传输层：封装成报文段</h4>
<ul>
<li>当连接建立之后，浏览器终于可以发出数据请求了。</li>
<li>应用程序将调用 Socket 库中的 write 方法将应用层准备好的 HTTP 消息交给协议栈，应用程序的数据一般都比较大，因此在传输层会按照网络包的大小对数据进行拆分。将数据拆分成报文段，并添加 TCP 协议头。</li>
</ul>
<p><img src="./../images/应用程序数据的拆分发送.png" alt="image.png" /></p>
<h4 id="网络层-封装成数据报"> 网络层：封装成数据报</h4>
<ul>
<li>IP 模块会接收 TCP 报文段，在前面添加 IP 头部和以太网的 MAC 头部后发送网络包。 ![image.png](../images/IP 数据报封装过程.png)</li>
</ul>
<h4 id="数据链路层-封装成帧"> 数据链路层：封装成帧</h4>
<ul>
<li>IP 传给网络接口层的数据单元称做 IP 数据报，网卡驱动程序从 IP 模块获取数据报之后，在开头加上报头和起始帧分界符，在末尾加上用于检测错误的帧校验序列。</li>
<li>加上报头、起始帧分界符和 FCS 之后，我们就可以将包通过网线发送出去了。这种通过以太网传输的比特流称做帧。 <img src="./../images/网卡发送出去的包.png" alt="image.png" /></li>
</ul>
<h4 id="物理层-数字转换为电信号"> 物理层: 数字转换为电信号</h4>
<ul>
<li>
<p>网卡的内部结构如下，网卡中的 MAC 模块从报头开始将数字信息按每个比特转换成电信号，然后由 PHY，或者叫 MAU 的信号收发模块发送出去。 在这里，将数字信息转换为电信号的速率就是网络的传输速率。<img src="./../images/网卡.png" alt="image.png" /></p>
</li>
<li>
<p>接下来，PHY（MAU）模块会将信号转换为可在网线上传输的格式，并通过网线发送出去。网卡将数字信息转换成电信号或光信号</p>
</li>
</ul>
<p><img src="./../images/网卡将数字信息转换成电信号或光信号.png" alt="image.png" /></p>
<h3 id="服务器处理"> 服务器处理</h3>
<ul>
<li>数据通过网络层传输后最终到达服务器，在服务器中处理分为两类：
<ul>
<li>使用了代理服务器的服务，数据先到达代理服务器，然后到达源服务器</li>
<li>源服务器：保存你的处理程序的服务器</li>
</ul>
</li>
</ul>
<h4 id="代理服务器处理"> 代理服务器处理</h4>
<h5 id="代理服务器"> 代理服务器</h5>
<ul>
<li>代理是一种介于客户端与 Web 服务器之间，对访问操作进行中转的机制</li>
<li>分类：<strong>正向代理</strong>和<strong>反向代理</strong>。</li>
<li>正向代理：正向代理服务器与客户端在同一个局域网中，正向代理服务器用来让客户端接入外网以访问外网资源
<ul>
<li>正向代理主要用于几种场景：防火墙、翻墙、对局域网对外网的访问进行监控和管理、缓存代理、隐藏访问者信息。</li>
</ul>
</li>
</ul>
<p><img src="./../images/正向代理.png" alt="image.png" /></p>
<ul>
<li>
<p>反向代理：反向代理服务器与服务端在同一个局域网中，反向代理服务器用来让外网的客户端接入局域网中的站点以访问站点中的资源。</p>
<ul>
<li>反向代理主要用于几种场景：
<ul>
<li>防火墙，保护和隐藏原始资源服务器</li>
<li>负载均衡，当有多台服务器时，使用代理服务器转发请求</li>
<li>反向代理缓存，用于缓存服务端的资源</li>
</ul>
</li>
</ul>
<p><img src="./../images/反向代理.png" alt="" /></p>
</li>
<li>
<p>通过在客户端与服务器之间添加代理服务器来分担 Web 服务器的负载， 一般有 3 种方法：</p>
<ul>
<li><strong>负载均衡</strong>
<ul>
<li>当用户访问量很高时，可以通过添加多台服务器来处理。通过负载均衡代理可以将请求均衡的转发到源服务器，以减轻源服务器的负载，提升服务端处理速度。</li>
</ul>
</li>
<li><strong>缓存代理服务器</strong>
<ul>
<li>代理服务器部署在服务端局域网中，代理服务器每次都要将请求转发至源服务器，当源服务器中资源位改变时返回 304，直接取缓存服务器中的资源，降低了源服务器的处理时间和响应数据的网络传输时间。</li>
</ul>
</li>
<li><strong>CDN 内容分发服务</strong>
<ul>
<li>一种将代理服务器部署在互联网边缘，且提供缓存刷新机制的服务，在代理服务器中有缓存且缓存有效时，直接返回缓存资源，而不需要再去源服务器请求，直接节约了网络传输时间和服务端处理时间。一般静态资源会发布到 CDN 缓存服务器上。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h5 id="cdn-内容分发"> CDN 内容分发</h5>
<ul>
<li>
<p>作用：返回缓存资源或回源 。</p>
</li>
<li>
<p>原理：内容分发服务的运营商会在互联网中部署很多缓存服务器，这些服务器既不在客户端的局域网中也不在服务端的局域网中，当客户端访问 Web 服务器时，会让客户端访问离用户最近的缓存服务器。<img src="./../images/CDN服务.png" alt="image.png" /></p>
</li>
<li>
<p>优点：提高网站的访问速度、减轻源服务器的 问压力。</p>
</li>
</ul>
<p><strong>使用 CDN 与未使用 CDN 过程：</strong></p>
<p>当用户访问一个网站时，如果没有 CDN，过程：</p>
<ol>
<li>
<p>浏览器要将域名解析为 IP 地址，所以需要向本地 DNS 发出请求。</p>
</li>
<li>
<p>本地 DNS 依次向根服务器、顶级域名服务器、权限服务器发出请求，得到网站服务器的 IP 地址。</p>
</li>
<li>
<p>本地 DNS 将 IP 地址发回给浏览器，浏览器向网站服务器 IP 地址发出请求并得到资源。</p>
<p><img src="./../images/cdn-not.jpg" alt="img" /></p>
</li>
</ol>
<p>如果用户访问的网站部署了 CDN，过程：</p>
<ol>
<li>浏览器要将域名解析为 IP 地址，所以需要向本地 DNS 发出请求。</li>
<li>本地 DNS 依次向根服务器、顶级域名服务器、权限服务器发出请求，得到全局负载均衡系统（GSLB）的 IP 地址。</li>
<li>本地 DNS 再向 GSLB 发出请求，GSLB 的主要功能是根据本地 DNS 的 IP 地址判断用户的位置，筛选出距离用户较近的本地负载均衡系统（SLB），并将该 SLB 的 IP 地址作为结果返回给本地 DNS。</li>
<li>本地 DNS 将 SLB 的 IP 地址发回给浏览器，浏览器向 SLB 发出请求。</li>
<li>SLB 根据浏览器请求的资源和地址，选出最优的缓存服务器发回给浏览器。</li>
<li>浏览器再根据 SLB 发回的地址重定向到缓存服务器。</li>
<li>如果缓存服务器有浏览器需要的资源，就将资源发回给浏览器。如果没有，就向源服务器请求资源，再发给浏览器并缓存在本地。</li>
</ol>
<p><img src="./../images/cdn.png" alt="img" /></p>
<p><strong>参考资料：</strong></p>
<ul>
<li>
<p><a href="https://www.zhihu.com/question/36514327/answer/193768864" target="_blank" rel="noopener noreferrer">CDN是什么？使用CDN有什么优势？</a></p>
</li>
<li>
<p><a href="https://link.zhihu.com/?target=https%3A//juejin.im/post/5d105e1af265da1b71530095" target="_blank" rel="noopener noreferrer">CDN原理简析</a></p>
</li>
</ul>
<h5 id="负载均衡器"> 负载均衡器</h5>
<ul>
<li>
<p>作用：转发请求。根据 Web 服务器的负载状况决定将请求转发到哪台  Web 服务器上。</p>
<ul>
<li>当服务器的访问量上升时，所有访问都打到同一台服务器上，服务器性能问题随之而来。我们可以将应用部署在多台 Web 服务器上，然后使用一种叫作负载均衡器的设备，客户端将请求发到负载均衡器上，然后由负载均衡器来判断将请求转发给哪台 Web 服务器。</li>
<li>对于客户端来说，负载均衡器就是一台 Web 服务器，我们需要要用负载均衡器的 IP 地址代替 Web 服务器的实际地址注册到 DNS 服务器上，客户端会认为负载均衡器就是一台 Web 服务器，并向其发送请求。<img src="./../images/负载均衡.png" alt="image.png" /></li>
</ul>
</li>
<li>
<p>负载均衡实现方式：</p>
<ul>
<li>反向代理：
<ul>
<li>用户的请求都发送到反向代理服务上，然后由反向代理服务器来转 发请求到真实的服务器上，以此来实 现集群的负载平衡。</li>
</ul>
</li>
<li>DNS ：
<ul>
<li>因一般的大型网站使用多台服务器提供服务，因此一个域名可能会对应多个服务器地址。</li>
<li>当用户向网站域名请求的时候，DNS 服务器返回这个域名 所对应的服务器 IP 地址的集合，但在 每个回答中，会循环这些 IP 地址的顺序，用户一般会选择排在前面的地址发送请求。以此将 用户的请求均衡的分配到各个不同的服 务器上，这样来实现负载均衡。</li>
<li>缺点：由于 DNS 服务器中存在缓存，所以有可能一个服务器出现故障后，域名解 析仍然返回的是那个 IP 地址，就会造成访问的问题。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h4 id="源服务器处理"> 源服务器处理</h4>
<ul>
<li>当 CDN 回源或者请求到达负载均衡器时，都需要将请求转发到源服务器，这时候源服务器就需要接收请求、处理和返回响应。</li>
</ul>
<h5 id="拆包和组装"> 拆包和组装</h5>
<ul>
<li>
<p>当网络包到达 Web 服务器之后，服务器就会接收这个包并进行处理。</p>
</li>
<li>
<p>根据用途，服务器可以分为很多种类，其硬件和操作系统与客户端是有所不同的。但是，网络相关的部分，如网卡、协议栈、Socket 库等功能和客户端却并无二致。无论硬件和操作系统如何变化，TCP 和 IP 的功能都是一样的，或者说这些功能规格都是统一的。</p>
</li>
<li>
<p>现在，服务端接收到的网络包格式如下： <img src="./../images/网卡接受出去的包.png" alt="image.png" /></p>
</li>
<li>
<p>服务端需要进行拆包，取出客户端的数据（应用程序负责生成的部分），然后找到服务端的应用程序并交给它处理。具体过程如下：</p>
<ul>
<li>
<p><strong>链路层</strong></p>
<ul>
<li>网卡驱动会根据 MAC 头部判断协议类型，并将包交给相应的协议栈。</li>
</ul>
</li>
<li>
<p><strong>网络层</strong></p>
<ul>
<li>协议栈的 IP 模块会检查 IP 头部：
<ul>
<li>检查接收方 IP 地址，判断是不是发给自己的；</li>
<li>检查 IP。</li>
</ul>
</li>
<li>头部的内容确定包是否被分片，如果是分片的包，则将包暂时存放在内存中，等所有分片全部到达之后将分片组装起来还原成原始包；</li>
<li>检查 IP 头部的协议号字段，并将包转交给相应的模块。例如，如果协议号为 06（十六进制），则将包转交给 TCP 模块；如果是 11（十六进制），则转交给 UDP 模块。</li>
</ul>
</li>
<li>
<p><strong>传输层</strong></p>
<ul>
<li>这里我们假设这个包被交给 TCP 模块处理。前面的步骤对于任何包都是一样的，但后面的 TCP 模块的操作则根据包的内容有所区别。
<ul>
<li>如果收到的是发起连接的包，TCP 模块会：
<ul>
<li>确认 TCP 头部的控制位 SYN；</li>
<li>检查接收方端口号；</li>
<li>为相应的等待连接套接字复制一个新的副本；</li>
<li>记录发送方 IP 地址和端口号等信息。</li>
</ul>
</li>
<li>收到数据包时，TCP 模块会：
<ul>
<li>根据收到的包的发送方 IP 地址、发送方端口号、接收方 IP 地址、接收方端口号找到相对应的套接字；</li>
<li>将数据块拼合起来并保存在接收缓冲区中；</li>
<li>向客户端返回 ACK</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>
<p>分配接收到的包过程：<img src="./../images/分配接收到的包.png" alt="image.png" /></p>
</li>
</ul>
<h5 id="处理请求"> 处理请求</h5>
<ul>
<li>
<p>网络包经过从链路层到网络层再到传输层的拆包和组装过程，最终将用户数据传送给了服务端的 Web 服务器。</p>
</li>
<li></li>
</ul>
<p><img src="./../images/http-7.1.png" alt="" /></p>
<p><img src="./../images/http-7.2.png" alt="" /></p>
<p><img src="./../images/http-7.3.png" alt="" /></p>
<h3 id="浏览器接受响应"> 浏览器接受响应</h3>
<ul>
<li>现在浏览器接收到了服务端的响应，先需要对响应进行解析。浏览器会根据响应的状态码和 HTTP 头部消息决定下一步操作。</li>
</ul>
<table>
<thead>
<tr>
<th><strong>状态码</strong></th>
<th><strong>含义</strong></th>
<th><strong>常见状态码</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>1xx</td>
<td>信息提示：接收的请求正在处理</td>
<td>101：Switching Protocals 服务器正在根据客户端的制定，将协议切换成 Update 首部所列的协议。可查阅 <a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Protocol_upgrade_mechanism" target="_blank" rel="noopener noreferrer">协议升级机制</a>。</td>
</tr>
<tr>
<td>2xx</td>
<td>成功：请求正常处理完毕</td>
<td>200：OK 请求成功，消息主体包含了所请求的资源</td>
</tr>
<tr>
<td>3xx</td>
<td>重定向：需要进一步操作以完成请求</td>
<td>301：Moved Permanently 永久重定向 302：Found 临时重定向（HTTP 1.0） 304：Not Modified 请求资源未修改，服务端不用返回资源，客户端可以使用本地资源 307：Temporary Redirect 临时重定向（HTTP 1.1） Chrome 用于内部重定向</td>
</tr>
<tr>
<td>4xx</td>
<td>客户端错误</td>
<td>401：Unanthorized 未授权 404：Not Found 服务器无法找到所请求 URL 对应的资源</td>
</tr>
<tr>
<td>5xx</td>
<td>服务器错误</td>
<td>500: Internal Server Error 服务端内部错误 502: Bad Gateway 代理网关错误</td>
</tr>
</tbody>
</table>
<h4 id="http重定向"> HTTP重定向</h4>
<ul>
<li>
<p>当单个页面、表单或者整个 Web 应用被迁移到新的 URL 下的时候，服务端告诉客户端需要重定向到其他位置查找资源。</p>
</li>
<li>
<p>过程：</p>
<ul>
<li>服务器通过发送特殊的响应（即 redirects）而触发。</li>
<li>浏览器判断响应的状态码为 3xx 。</li>
<li>浏览器在接收到重定向响应的时候，会采用该响应提供的新的 URL ，并立即进行加载；大多数情况下，除了会有一小部分性能损失之外，重定向操作对于用户来说是不可见的。</li>
</ul>
<p><img src="./../images/http重定向.png" alt="img" /></p>
</li>
<li>
<p>不同类型的重定向映射可以划分为三个类别：</p>
<ul>
<li><a href="https://wiki.developer.mozilla.org/zh-CN/docs/Web/HTTP/Redirections$edit#Permanent_redirections" target="_blank" rel="noopener noreferrer">永久重定向</a></li>
<li><a href="https://wiki.developer.mozilla.org/zh-CN/docs/Web/HTTP/Redirections$edit#Temporary_redirections" target="_blank" rel="noopener noreferrer">临时重定向</a></li>
<li><a href="https://wiki.developer.mozilla.org/zh-CN/docs/Web/HTTP/Redirections$edit#Special_redirections" target="_blank" rel="noopener noreferrer">特殊重定向</a></li>
</ul>
</li>
</ul>
<p>参考资料：<a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Redirections" target="_blank" rel="noopener noreferrer">HTTP 重定向</a></p>
<h4 id="检测响应体"> 检测响应体</h4>
<ul>
<li>常见的 HTTP 请求是请求 HTML 文档、静态资源（JS/CSS/图片）以及 Ajax 数据请求等</li>
<li>当浏览器拿到 HTTP 响应消息后，首先检查响应消息是否完整、有没有被压缩、它是什么类型等等，浏览器才能进行下一步的操作，例如如果是 HTML 文档，就需要渲染它。</li>
<li>根据 HTTP 响应报文，依次 检测截尾（Content-Length）、检测内容编码（Content-Encoding）、检测媒体类型和字符集（Content-Type）、综合判断响应实体的数据类型 和 处理响应体。</li>
</ul>
<p><strong>HTTP 响应报文</strong></p>
<ul>
<li>HTTP Responses Header 响应头对照表</li>
</ul>
<table>
<thead>
<tr>
<th>Header</th>
<th>解释</th>
<th>示例</th>
</tr>
</thead>
<tbody>
<tr>
<td>Accept-Ranges</td>
<td>表明服务器是否支持指定范围请求及哪种类型的分段请求</td>
<td>Accept-Ranges: bytes</td>
</tr>
<tr>
<td>Age</td>
<td>从原始服务器到代理缓存形成的估算时间（以秒计，非负）</td>
<td>Age: 12</td>
</tr>
<tr>
<td>Allow</td>
<td>对某网络资源的有效的请求行为，不允许则返回405</td>
<td>Allow: GET, HEAD</td>
</tr>
<tr>
<td>Cache-Control</td>
<td>告诉所有的缓存机制是否可以缓存及哪种类型</td>
<td>Cache-Control: no-cache</td>
</tr>
<tr>
<td>Content-Encoding</td>
<td>web服务器支持的返回内容压缩编码类型。</td>
<td>Content-Encoding: gzip</td>
</tr>
<tr>
<td>Content-Language</td>
<td>响应体的语言</td>
<td>Content-Language: en,zh</td>
</tr>
<tr>
<td>Content-Length</td>
<td>响应体的长度</td>
<td>Content-Length: 348</td>
</tr>
<tr>
<td>Content-Location</td>
<td>请求资源可替代的备用的另一地址</td>
<td>Content-Location: /index.htm</td>
</tr>
<tr>
<td>Content-MD5</td>
<td>返回资源的MD5校验值</td>
<td>Content-MD5: Q2hlY2sgSW50ZWdyaXR5IQ==</td>
</tr>
<tr>
<td>Content-Range</td>
<td>在整个返回体中本部分的字节位置</td>
<td>Content-Range: bytes 21010-47021/47022</td>
</tr>
<tr>
<td>Content-Type</td>
<td>返回内容的MIME类型</td>
<td>Content-Type: text/html; charset=utf-8</td>
</tr>
<tr>
<td>Date</td>
<td>原始服务器消息发出的时间</td>
<td>Date: Tue, 15 Nov 2010 08:12:31 GMT</td>
</tr>
<tr>
<td>ETag</td>
<td>请求变量的实体标签的当前值</td>
<td>ETag: “737060cd8c284d8af7ad3082f209582d”</td>
</tr>
<tr>
<td>Expires</td>
<td>响应过期的日期和时间</td>
<td>Expires: Thu, 01 Dec 2010 16:00:00 GMT</td>
</tr>
<tr>
<td>Last-Modified</td>
<td>请求资源的最后修改时间</td>
<td>Last-Modified: Tue, 15 Nov 2010 12:45:26 GMT</td>
</tr>
<tr>
<td>Location</td>
<td>用来重定向接收方到非请求URL的位置来完成请求或标识新的资源</td>
<td>Location: http://www.zcmhi.com/archives/94.html</td>
</tr>
<tr>
<td>Pragma</td>
<td>包括实现特定的指令，它可应用到响应链上的任何接收方</td>
<td>Pragma: no-cache</td>
</tr>
<tr>
<td>Proxy-Authenticate</td>
<td>它指出认证方案和可应用到代理的该URL上的参数</td>
<td>Proxy-Authenticate: Basic</td>
</tr>
<tr>
<td>refresh</td>
<td>应用于重定向或一个新的资源被创造，在5秒之后重定向（由网景提出，被大部分浏览器支持）</td>
<td>Refresh: 5; url=http://www.zcmhi.com/archives/94.html</td>
</tr>
<tr>
<td>Retry-After</td>
<td>如果实体暂时不可取，通知客户端在指定时间之后再次尝试</td>
<td>Retry-After: 120</td>
</tr>
<tr>
<td>Server</td>
<td>web服务器软件名称</td>
<td>Server: Apache/1.3.27 (Unix) (Red-Hat/Linux)</td>
</tr>
<tr>
<td>Set-Cookie</td>
<td>设置Http Cookie</td>
<td>Set-Cookie: UserID=JohnDoe; Max-Age=3600; Version=1</td>
</tr>
<tr>
<td>Trailer</td>
<td>指出头域在分块传输编码的尾部存在</td>
<td>Trailer: Max-Forwards</td>
</tr>
<tr>
<td>Transfer-Encoding</td>
<td>文件传输编码</td>
<td>Transfer-Encoding:chunked</td>
</tr>
<tr>
<td>Vary</td>
<td>告诉下游代理是使用缓存响应还是从原始服务器请求</td>
<td>Vary: *</td>
</tr>
<tr>
<td>Via</td>
<td>告知代理客户端响应是通过哪里发送的</td>
<td>Via: 1.0 fred, 1.1 nowhere.com (Apache/1.1)</td>
</tr>
<tr>
<td>Warning</td>
<td>警告实体可能存在的问题</td>
<td>Warning: 199 Miscellaneous warning</td>
</tr>
<tr>
<td>WWW-Authenticate</td>
<td>表明客户端请求实体应该使用的授权方案</td>
<td>WWW-Authenticate: Basic</td>
</tr>
</tbody>
</table>
<h4 id="断开tcp连接"> 断开TCP连接</h4>
<p><img src="./../images/http-8.png" alt="" /></p>
<p><strong>四次挥手过程：</strong></p>
<p>因为 TCP 连接是全双工的，也就是说通信的双方都可以向对方发送和接收消息， 所以断开连接需要双方的确认。</p>
<ul>
<li>
<p>第一次挥手，客户端认为没有数据要再发送给服务器端，它就向服务器发送一个 FIN 报文段，申请断开客户端到服务器端的 连接。发送后客户端进入 FIN_WAIT_1 状态。</p>
</li>
<li>
<p>第二次挥手，服务器端接收到客户端释放连接的请求后，向客户端发送一个确认 报文段，表示已经接收到了客户端释放连接的 请求，以后不再接收客户端发送 过来的数据。但是因为连接是全双工的，所以此时，服务器端还可以向客户端发 送数据。服务 器端进入 CLOSE_WAIT 状态。客户端收到确认后，进入 FIN_WAIT_2 状态。</p>
</li>
<li>
<p>第三次挥手，服务器端发送完所有数据后，向客户端发送 FIN 报文段，申请断 开服务器端到客户端的连接。发送后进入 LAS T_ACK 状态。</p>
</li>
<li>
<p>第四次挥手，客户端接收到 FIN 请求后，向服务器端发送一个确认应答，并进 入 TIME_WAIT 阶段。该阶段会持续一段时间， 这个时间为报文段在网络中的 最大生存时间，如果该时间内服务端没有重发请求的话，客户端进入 CLOSED 的 状态。如果收到 服务器的重发请求就重新发送确认报文段。服务器端收到客户 端的确认报文段后就进入 CLOSED 状态，这样全双工的连接就被 释放了。</p>
</li>
</ul>
<p>TCP 使用四次挥手的原因：</p>
<ul>
<li>因为 TCP 的连接是全双工的，所以需要双方分别释 放到对方的连接，单独一方的连接释放，只代 表不能再向对方发送数据，连接 处于的是半释放的状态。</li>
<li>最后一次挥手中，客户端会等待一段时间再关闭的原因，是为了防止发送给服务 器的确认报文段丢失或者出错，从而导致服务器 端不能正常关闭</li>
</ul>
<h3 id="浏览器解析"> 浏览器解析</h3>
<ul>
<li>“解析”是浏览器将通过网络接收的数据转换为DOM和CSSOM的步骤，通过渲染器把DOM和CSSOM在屏幕上绘制成页面</li>
</ul>
<h4 id="建立dom树"> 建立DOM树</h4>
<p><img src="./../images/html-dmo.png" alt="" /></p>
<ul>
<li>影响构建 DOM 时间：
<ul>
<li>DOM节点的数量越多/节点深度越深，构建DOM树所需的时间就越长。</li>
<li><code>&lt;script&gt;</code>标签阻塞渲染并停止HTML的解析。</li>
</ul>
</li>
</ul>
<h4 id="建立-cssdom"> 建立 CSSDOM</h4>
<p><img src="./../images/css-dom.png" alt="" /></p>
<h4 id="javascript-代码执行"> JavaScript 代码执行</h4>
<ul>
<li>当 CSS 被解析并创建 CSSOM 时，其他资源，包括 JavaScript 文件正在下载（preload scanner）。</li>
<li>JavaScript被解释、编译、解析和执行。脚本被解析为抽象语法树。一些浏览器引擎使用”Abstract Syntax Tree“并将其传递到解释器中，输出在主线程上执行的字节码。</li>
</ul>
<p>JavaScript代码的整个执行过程，分为两个阶段：</p>
<ul>
<li>代码编译阶段：编译阶段由编译器完成，将代码翻译成可执行代码，这个阶段作用域规则会确定。</li>
<li>代码执行阶段：由引擎完成，主要任务是执行可执行代码，执行上下文在这个阶段创建。</li>
</ul>
<p><img src="./../images/jszhixing.png" alt="" /></p>
<h4 id="构建辅助功能树"> 构建辅助功能树</h4>
<ul>
<li>
<p>浏览器还构建辅助设备用于分析和解释内容的辅助功能（<a href="https://developer.mozilla.org/en-US/docs/Learn/Accessibility" target="_blank" rel="noopener noreferrer">accessibility</a> ）树。可访问性对象模型（AOM）类似于DOM的语义版本。当DOM更新时，浏览器会更新辅助功能树。辅助技术本身无法修改可访问性树。*</p>
</li>
<li>
<p>在构建AOM之前，屏幕阅读器（<a href="https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Screen_Reader_Implementors_Guide" target="_blank" rel="noopener noreferrer">screen readers</a>）无法访问内容。</p>
</li>
</ul>
<h3 id="浏览器渲染"> 浏览器渲染</h3>
<ul>
<li>渲染步骤包括样式、布局、绘制，在某些情况下还包括合成。</li>
<li>过程：在解析步骤中创建的CSSOM树和DOM树组合成一个Render树，然后用于计算每个可见元素的布局，然后将其绘制到屏幕上。在某些情况下，可以将内容提升到它们自己的层并进行合成，通过在GPU而不是CPU上绘制屏幕的一部分来提高性能，从而释放主线程。</li>
</ul>
<h4 id="合成-render-树"> 合成 render 树</h4>
<ul>
<li>过程：将DOM和CSSOM组合成一个Render树，计算样式树或渲染树从DOM树的根开始构建，遍历每个可见节点。</li>
<li>每个可见节点都应用了其CSSOM规则。Render树保存所有具有内容和计算样式的可见节点——将所有相关样式匹配到DOM树中的每个可见节点，并根据CSS级联确定每个节点的计算样式。
<ul>
<li>可见节点计算规则：
<ul>
<li><code>&lt;head&gt;</code>和它的子节点以及任何具有<code>display: none</code>样式的结点，例如<code>script { display: none; }</code>（在user agent stylesheets可以看到这个样式）这些标签将不会显示，也就是它们不会出现在Render树上。</li>
<li>具有<code>visibility: hidden</code>的节点会出现在Render树上，因为它们会占用空间。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h4 id="布局-layout"> 布局 Layout</h4>
<ul>
<li>概念：
<ul>
<li>第一次确定节点的大小和位置称为布局。随后对节点大小和位置的重新计算称为回流。</li>
<li>布局是确定呈现树中所有节点的宽度、高度和位置，以及确定页面上每个对象的大小和位置的过程。</li>
</ul>
</li>
<li>过程：
<ul>
<li>构建渲染树后，开始布局，在渲染树上运行布局以计算每个节点的几何体。</li>
<li>渲染树标识显示哪些节点（即使不可见）及其计算样式，但不标识每个节点的尺寸或位置。为了确定每个对象的确切大小和位置，浏览器从渲染树的根开始遍历它：以视区的大小为基础，布局通常从body开始，用每个元素的框模型属性排列所有body的子孙元素的尺寸，为不知道其尺寸的替换元素（例如图像）提供占位符空间。</li>
</ul>
</li>
</ul>
<h4 id="绘制-paint"> 绘制 Paint</h4>
<ul>
<li>概念：
<ul>
<li>将各个节点绘制到屏幕上，第一次出现的节点称为<a href="https://developer.mozilla.org/en-US/docs/Glossary/first_meaningful_paint" target="_blank" rel="noopener noreferrer">first meaningful paint（FCP）</a></li>
<li>在绘制或光栅化阶段，浏览器将在布局阶段计算的每个框转换为屏幕上的实际像素；</li>
<li>绘画包括将元素的每个可视部分绘制到屏幕上，包括文本、颜色、边框、阴影和替换的元素（如按钮和图像）。</li>
</ul>
</li>
<li>绘制可以将布局树中的元素分解为多个层。将内容提升到GPU上的层（而不是CPU上的主线程）可以提高绘制和重新绘制性能。</li>
</ul>
<h3 id="合成-compositing"> 合成 Compositing</h3>
<ul>
<li>合成发生情况：为了确保平滑滚动和动画，占据主线程的所有内容，包括计算样式，以及回流和绘制，必须让浏览器在16.67毫秒内完成。在2048x 1536，iPad有超过314.5万像素将被绘制到屏幕上。那是很多像素需要快速绘制。为了确保重绘的速度比初始绘制的速度更快，屏幕上的绘图通常被分解成数层。如果发生这种情况，则需要进行合成。</li>
<li>概念：当文档的各个部分以不同的层绘制，相互重叠时，必须进行合成，以确保它们以正确的顺序绘制到屏幕上，并正确显示内容。</li>
<li>回流情况：当页面继续加载资产时，可能会发生回流，回流会触发重新绘制和重新组合。</li>
</ul>
<h4 id="重排-回流"> 重排/回流</h4>
<ul>
<li>对节点大小和位置的重新计算称为回流。</li>
</ul>
<p><img src="./../images/chongpai.png" alt="" /></p>
<ul>
<li>图片避免回流：定义图像的大小，就不需要重新绘制，只需要重新绘制需要重新绘制的层，并在必要时进行合成。</li>
</ul>
<h4 id="重绘"> 重绘</h4>
<p><img src="./../images/chonghui.png" alt="" /></p>
<p><strong>参考资料</strong></p>
<p><a href="https://juejin.cn/post/6930890886616350734/#heading-1" target="_blank" rel="noopener noreferrer">前端性能优化指南</a></p>
<h3 id="用户交互"> 用户交互</h3>
<ul>
<li>在用户交互过程中，用户触发的滚动等操作，会使页面进入回流和重绘阶段</li>
</ul>
<h3 id="关键渲染路径"> 关键渲染路径</h3>
<ul>
<li>通过上文的介绍，我们知道渲染树是由 DOM 和 CSSOM 组合而成的。因此，在 HTML 和 CSS 没有加载完成前，页面不会渲染任何内容。</li>
<li>换句话说，HTML 和 CSS 都是会阻塞页面渲染的资源。除此之外，JavaScript 也会阻塞页面渲染。浏览器在解析 HTML 时，如果遇到 <code>&lt;script&gt;</code> 标签时，一般会暂停解析，直到 JavaScript 执行完毕后才会继续解析剩余的 HTML。对于外部 JavaScript，在执行前还需额外等待请求的时间。这些首屏渲染所必须的关键资源，共同组成了关键渲染路径。</li>
</ul>
<h2 id="前端存储"> 前端存储</h2>
<ul>
<li><strong>分类</strong></li>
</ul>
<p><img src="./../images/cunchu1.png" alt="" /></p>
<ul>
<li><strong>存储技术发展史</strong></li>
</ul>
<p><img src="./../images/cunchu2.png" alt="" /></p>
<ul>
<li><strong>存储技术介绍</strong></li>
</ul>
<p><img src="./../images/cunchu3.png" alt="" /></p>
<h3 id="localstorage-sessionstorage"> LocalStorage &amp;&amp; sessionStorage</h3>
<ul>
<li>localStorage 有同源限制，因此不能跨域存取。
<ul>
<li>解决方案：通过<code>window.postMessage</code>，读写其他窗口的 LocalStorage 。使用参考<a href="https://www.ruanyifeng.com/blog/2016/04/same-origin-policy.html" target="_blank" rel="noopener noreferrer">浏览器同源政策及其规避方法</a></li>
</ul>
</li>
</ul>
<h3 id="cookie"> Cookie</h3>
<p><img src="./../images/cookiets.png" alt="" /></p>
<ul>
<li>
<p>cookie 同源限制：Cookie 是服务器写入浏览器的一小段信息，只有同源的网页才能共享。</p>
</li>
<li>
<p>两个网页一级域名相同，只是二级域名不同，浏览器允许通过设置<code>document.domain</code>共享 Cookie。</p>
<ul>
<li>A网页是<code>http://w1.example.com/a.html</code>，B网页是<code>http://w2.example.com/b.html</code>，那么只要设置相同的<code>document.domain</code>，两个网页就可以共享Cookie。</li>
</ul>
<blockquote>
<div><pre><code>document<span>.</span>domain <span>=</span> <span>'example.com'</span><span>;</span>
</code></pre>
<div><span>1</span><br></div></div></blockquote>
<p>现在，A网页通过脚本设置一个 Cookie。</p>
<blockquote>
<div><pre><code>document<span>.</span>cookie <span>=</span> <span>"test1=hello"</span><span>;</span>
</code></pre>
<div><span>1</span><br></div></div></blockquote>
<p>B网页就可以读到这个 Cookie。</p>
<blockquote>
<div><pre><code><span>var</span> allCookie <span>=</span> document<span>.</span>cookie<span>;</span>
</code></pre>
<div><span>1</span><br></div></div></blockquote>
</li>
<li>
<p>服务器也可以在设置Cookie的时候，指定Cookie的所属域名为一级域名，比如<code>.example.com</code>。</p>
<blockquote>
<div><pre><code><span>Set-Cookie:</span> key=value; domain=.example.com; path=/
</code></pre>
<div><span>1</span><br></div></div></blockquote>
<p>这样的话，二级域名和三级域名不用做任何设置，都可以读取这个Cookie。</p>
</li>
</ul>
<p><strong>常见问题</strong></p>
<blockquote>
<p>服务器代理转发时，该如何处理 cookie？</p>
</blockquote>
<ul>
<li>
<p>服务器端可以使用 Set-Cookie 的响应头部来配置 cookie 信息。</p>
</li>
<li>
<p>一条 cookie 包括了 5 个属性值 expires、domain、path、secure、HttpOnly。其中 expires 指定了 cookie 失 效的时间，domain 是域名、path 是路径，domain 和 path 一起限制了 cookie 能够被哪些 url 访问。secure 规定了 cookie 只能在确保安全的情况下传输，HttpOnly 规定了这个 cookie 只能被服务器访问，不能使用 js 脚本访问。</p>
</li>
<li>
<p>在发生 xhr 的跨域请求的时候，即使是同源下的 cookie，也不会被自动添加到请求头部，除非显示地规定。</p>
</li>
</ul>
<h3 id="session"> Session</h3>
<p><img src="./../images/session.png" alt="" /></p>
<h3 id="浏览器缓存机制"> 浏览器缓存机制</h3>
<p><img src="./../images/浏览器缓存.png" alt="" /></p>
<h4 id="缓存位置"> 缓存位置</h4>
<p>从缓存位置上来说分为四种，并且各自有优先级，当依次查找缓存且都没有命中的时候，才会去请求网络。</p>
<ul>
<li>Service Worker</li>
<li>Memory Cache</li>
<li>Disk Cache</li>
<li>Push Cache</li>
</ul>
<p><strong>Service Worker</strong></p>
<p>Service Worker 是运行在浏览器背后的独立线程，一般可以用来实现缓存功能。使用 Service Worker的话，传输协议必须为 HTTPS。因为 Service Worker 中涉及到请求拦截，所以必须使用 HTTPS 协议来保障安全。<strong>Service Worker 的缓存与浏览器其他内建的缓存机制不同，它可以让我们自由控制缓存哪些文件、如何匹配缓存、如何读取缓存，并且缓存是持续性的</strong>。</p>
<p>Service Worker 实现缓存功能一般分为三个步骤：首先需要先注册 Service Worker，然后监听到 install 事件以后就可以缓存需要的文件，那么在下次用户访问的时候就可以通过拦截请求的方式查询是否存在缓存，存在缓存的话就可以直接读取缓存文件，否则就去请求数据。</p>
<p>当 Service Worker 没有命中缓存的时候，我们需要去调用 fetch 函数获取数据。也就是说，如果我们没有在 Service Worker 命中缓存的话，会根据缓存查找优先级去查找数据。但是不管我们是从 Memory Cache 中还是从网络请求中获取的数据，浏览器都会显示我们是从 Service Worker 中获取的内容。</p>
<p><strong>Memory Cache</strong></p>
<p>Memory Cache 也就是内存中的缓存，主要包含的是当前中页面中已经抓取到的资源,例如页面上已经下载的样式、脚本、图片等。读取内存中的数据肯定比磁盘快,内存缓存虽然读取高效，可是缓存持续性很短，会随着进程的释放而释放。<strong>一旦我们关闭 Tab 页面，内存中的缓存也就被释放了</strong>。</p>
<p>**那么既然内存缓存这么高效，我们是不是能让数据都存放在内存中呢？**这是不可能的。计算机中的内存一定比硬盘容量小得多，操作系统需要精打细算内存的使用，所以能让我们使用的内存必然不多。</p>
<p>当我们访问过页面以后，再次刷新页面，可以发现很多数据都来自于内存缓存<img src="./../images/缓存.png" alt="" /></p>
<p>内存缓存中有一块重要的缓存资源是preloader相关指令（例如 <code>&lt;linkrel=&quot;prefetch&quot;&gt;</code>）下载的资源。总所周知preloader的相关指令已经是页面优化的常见手段之一，它可以一边解析js/css文件，一边网络请求下一个资源。</p>
<p>需要注意的事情是，<strong>内存缓存在缓存资源时并不关心返回资源的HTTP缓存头Cache-Control是什么值，同时资源的匹配也并非仅仅是对URL做匹配，还可能会对Content-Type，CORS等其他特征做校验</strong>。</p>
<p><strong>Disk Cache</strong></p>
<p>Disk Cache 也就是存储在硬盘中的缓存，读取速度慢点，但是什么都能存储到磁盘中，<strong>比之 Memory Cache 胜在容量和存储时效性上</strong>。</p>
<p>在所有浏览器缓存中，Disk Cache 覆盖面基本是最大的。它会根据 HTTP Herder 中的字段判断哪些资源需要缓存，哪些资源可以不请求直接使用，哪些资源已经过期需要重新请求。并且即使在跨站点的情况下，相同地址的资源一旦被硬盘缓存下来，就不会再次去请求数据。绝大部分的缓存都来自 Disk Cache，关于 HTTP 的协议头中的缓存字段，我们会在下文进行详细介绍。</p>
<p>**浏览器会把哪些文件丢进内存中？哪些丢进硬盘中？**关于这点，网上说法不一，不过以下观点比较靠得住：</p>
<ul>
<li>对于大文件来说，大概率是不存储在内存中的，反之优先</li>
<li>当前系统内存使用率高的话，文件优先存储进硬盘</li>
</ul>
<p><strong>Push Cache</strong></p>
<p>Push Cache（推送缓存）是 HTTP/2 中的内容，当以上三种缓存都没有命中时，它才会被使用。<strong>它只在会话（Session）中存在，一旦会话结束就被释放，并且缓存时间也很短暂</strong>，在Chrome浏览器中只有5分钟左右，同时它也并非严格执行HTTP头中的缓存指令。</p>
<p>Push Cache 在国内能够查到的资料很少，也是因为 HTTP/2 在国内不够普及。这里推荐阅读 <code>JakeArchibald</code>的 HTTP/2 push is tougher than I thought 这篇文章，文章中的几个结论：</p>
<ul>
<li>所有的资源都能被推送，并且能够被缓存,但是 Edge 和 Safari 浏览器支持相对比较差</li>
<li>可以推送 no-cache 和 no-store 的资源</li>
<li>一旦连接被关闭，Push Cache 就被释放</li>
<li>多个页面可以使用同一个HTTP/2的连接，也就可以使用同一个Push Cache。这主要还是依赖浏览器的实现而定，出于对性能的考虑，有的浏览器会对相同域名但不同的tab标签使用同一个HTTP连接。</li>
<li>Push Cache 中的缓存只能被使用一次</li>
<li>浏览器可以拒绝接受已经存在的资源推送</li>
<li>你可以给其他域名推送资源</li>
</ul>
<p>如果以上四种缓存都没有命中的话，那么只能发起请求来获取资源了。</p>
<p>那么为了性能上的考虑，大部分的接口都应该选择好缓存策略，<strong>通常浏览器缓存策略分为两种：强缓存和协商缓存，并且缓存策略都是通过设置 HTTP Header 来实现的</strong>。</p>
<h4 id="缓存机制"> 缓存机制</h4>
<ul>
<li>浏览器的缓存机制指的是通过在一段时间内保留已接收到的web资源的一个副本，如果在资源的有效时间内，发起了对这个资源的再一次请求，那么浏览器会直接使用缓存的副本，而不是向服务器发起请求。</li>
<li>使用浏览器缓存可以有效地提高页面的打开速度，减少不必要的网络带宽的消耗；</li>
</ul>
<p><img src="./../images/http-huancun.png" alt="" /></p>
<p><img src="./../images/http-huancun1.png" alt="" /></p>
<h4 id="强制缓存"> <strong>强制缓存</strong></h4>
<p><img src="./../images/qinagzhihuancun1.png" alt="" /></p>
<p><img src="./../images/qinagzhihuancun2.png" alt="" /></p>
<p><img src="./../images/qinagzhihuancun3.png" alt="" /></p>
<h4 id="协商缓存"> <strong>协商缓存</strong></h4>
<p><img src="./../images/xieshanghuncun.png" alt="" /></p>
<p><img src="./../images/xieshanghuncun2.png" alt="" /></p>
<p><strong>浏览器缓存存在问题：</strong></p>
<ul>
<li>问题：ajax能提高页面载入的速度主要的原因是通过ajax减少了重复数据的载入，也就是说在载入数据的同时将数据缓存到内存中，一旦数据被加载其中，只要我们没有刷新页面，这些数据就会一直被缓存在内存中，当我们提交 的URL与历史的URL一致时，就不需要提交给服务器，也就是不需要从服务器上面去获取数据，虽然这样降低了服务器的负载提高了用户的体验，但是我们不能获取最新的数据。为了保证我们读取的信息都是最新的，我们就需要禁止他的缓存功能。</li>
<li>解决方案：
<ul>
<li>1、在ajax发送请求前加上 anyAjaxObj.setRequestHeader(&quot;If-Modified-Since&quot;,&quot;0&quot;)。</li>
<li>2、在ajax发送请求前加上 anyAjaxObj.setRequestHeader(&quot;Cache-Control&quot;,&quot;no-cache&quot;)。</li>
<li>3、在URL后面加上一个随机数： &quot;fresh=&quot; + Math.random();。</li>
<li>4、在URL后面加上时间搓：&quot;nowtime=&quot; + new Date().getTime();。</li>
<li>5、如果是使用jQuery，直接这样就可以了$.ajaxSetup({cache:false})。这样页面的所有ajax都会执行这条语句就是不需要保存缓存记录。</li>
</ul>
</li>
</ul>
<p><strong>参考资料</strong>：</p>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Caching" target="_blank" rel="noopener noreferrer">HTTP 缓存 MDN</a></p>
<p><a href="https://mp.weixin.qq.com/s/UOSvXZcB0LF6p6HTk1pqlQ" target="_blank" rel="noopener noreferrer">彻底理解浏览器的缓存机制</a></p>
<p><a href="https://mp.weixin.qq.com/s/KNFXOwM1bcwcLVP98L7Qpg" target="_blank" rel="noopener noreferrer">深入理解浏览器的缓存机制</a></p>
<h3 id="service-workers-缓存"> Service Workers 缓存</h3>
<ul>
<li>Service Workers  缓存：Service Workers  能够拦截 HTTP 请求，进入任何传入的 HTTP 请求，并决定想要如何响应。在你的 Service Worker 中，可以编写逻辑来决定想要缓存的资源，以及需要满足什么条件和资源需要缓存多久。</li>
</ul>
<ul>
<li>与 HTTP 缓存比较：Web 服务器可以使用 Expires 首部来通知 Web 客户端，它可以使用资源的当前副本，直到指定的“过期时间”。反过来，浏览器可以缓存此资源，并且只有在有效期满后才会再次检查新版本。使用 HTTP 缓存意味着你要依赖服务器来告诉你何时缓存资源和何时过期。</li>
</ul>
<ul>
<li>
<p>**注册：**使用 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/ServiceWorkerContainer/register" target="_blank" rel="noopener noreferrer"><code>ServiceWorkerContainer.register()</code></a> 方法在该站点注册。其内容在 sw.js 文件中，可以在注册成功后执行。</p>
<div><pre><code><span>if</span><span>(</span><span>'serviceWorker'</span> <span>in</span> navigator<span>)</span> <span>{</span>
    navigator<span>.</span>serviceWorker<span>.</span><span>register</span><span>(</span><span>'/sw.js'</span><span>)</span><span>;</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
<li>
<p>**安装：**sw.js 监听 install 事件、<code>e.waitUntil()</code>等待 service worker 安装完成、Cache API 开启了一个缓存，并且将我们的应用所需要缓存的文件全部添加进去，当我们再次加载这些资源时，由请求 URL 确定的对应缓存就是可用的。</p>
<div><pre><code>self<span>.</span><span>addEventListener</span><span>(</span><span>'install'</span><span>,</span> <span>function</span><span>(</span><span>e</span><span>)</span> <span>{</span>
  console<span>.</span><span>log</span><span>(</span><span>'[Service Worker] Install'</span><span>)</span><span>;</span>
  e<span>.</span><span>waitUntil</span><span>(</span>
    caches<span>.</span><span>open</span><span>(</span>cacheName<span>)</span><span>.</span><span>then</span><span>(</span><span>function</span><span>(</span><span>cache</span><span>)</span> <span>{</span>
          console<span>.</span><span>log</span><span>(</span><span>'[Service Worker] Caching all: app shell and content'</span><span>)</span><span>;</span>
      <span>return</span> cache<span>.</span><span>addAll</span><span>(</span>contentToCache<span>)</span><span>;</span>
    <span>}</span><span>)</span>
  <span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div><ul>
<li>API 介绍：
<ul>
<li><code>e.waitUntil()</code>：等待 service worker 安装完成。<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/ExtendableEvent/waitUntil" target="_blank" rel="noopener noreferrer"><code>ExtendableEvent.waitUntil</code></a>方法告诉事件分发器该事件仍在进行。这个方法也可以用于检测进行的任务是否成功。在服务工作线程中，这个方法告诉浏览器事件一直进行，直至 promise 解决，浏览器不应该在事件中的异步操作完成之前终止服务工作线程。
<ul>
<li><code>caches</code> 是一个特殊的 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/CacheStorage" target="_blank" rel="noopener noreferrer"><code>CacheStorage</code></a> 对象，它能在 Service Worker 指定的范围内提供数据存储的能力。因为 Web Storage 的执行是同步的，在 Service Worker 中使用 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Web_Storage_API" target="_blank" rel="noopener noreferrer">Web Storage</a> 将不会有效果，所以我们使用 Cache API 作为替代。</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>激活：</strong> 监听<code>activate</code> 事件。删除那些我们已经不需要的文件或者做一些清理工作。</p>
<div><pre><code>self<span>.</span><span>addEventListener</span><span>(</span><span>'activate'</span><span>,</span> <span>event</span> <span>=></span> <span>{</span>
  <span>const</span> currentCaches <span>=</span> <span>[</span><span>PRECACHE</span><span>]</span><span>;</span>
  <span>//遍历缓存,查找不存在于自定义缓存列表中的浏览器缓存，并清除</span>
  event<span>.</span><span>waitUntil</span><span>(</span>
    caches<span>.</span><span>keys</span><span>(</span><span>)</span><span>.</span><span>then</span><span>(</span><span>cacheNames</span> <span>=></span> <span>{</span><span>//查找缓存</span>
      <span>return</span> cacheNames<span>.</span><span>filter</span><span>(</span><span>cacheName</span> <span>=></span> <span>!</span>currentCaches<span>.</span><span>includes</span><span>(</span>cacheName<span>)</span><span>)</span><span>;</span>
    <span>}</span><span>)</span><span>.</span><span>then</span><span>(</span><span>cachesToDelete</span> <span>=></span> <span>{</span>
      <span>return</span> Promise<span>.</span><span>all</span><span>(</span>cachesToDelete<span>.</span><span>map</span><span>(</span><span>cacheToDelete</span> <span>=></span> <span>{</span>
        <span>return</span> caches<span>.</span><span>delete</span><span>(</span>cacheToDelete<span>)</span><span>;</span> <span>//清除缓存</span>
      <span>}</span><span>)</span><span>)</span><span>;</span>
    <span>}</span><span>)</span><span>.</span><span>then</span><span>(</span><span>(</span><span>)</span> <span>=></span> self<span>.</span>clients<span>.</span><span>claim</span><span>(</span><span>)</span><span>)</span>
  <span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br></div></div></li>
<li>
<p>**响应请求：**通过 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/FetchEvent" target="_blank" rel="noopener noreferrer"><code>FetchEvent</code></a> 事件去响应请求。通过使用 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/FetchEvent/respondWith" target="_blank" rel="noopener noreferrer"><code>FetchEvent.respondWith</code></a> 方法，你可以任意修改对于这些请求的响应。拦截页面请求，现在缓存中查找，未找到才去请求。</p>
<div><pre><code>self<span>.</span><span>addEventListener</span><span>(</span><span>'fetch'</span><span>,</span> <span>function</span><span>(</span><span>event</span><span>)</span> <span>{</span>
  console<span>.</span><span>log</span><span>(</span><span>'Handling fetch event for'</span><span>,</span> event<span>.</span>request<span>.</span>url<span>)</span><span>;</span>
 <span>//事件处理程序侦听 fetch 事件。当触发时返回一个promise给受控页面。</span>
  event<span>.</span><span>respondWith</span><span>(</span>
    <span>// promise在 Cache 对象中查询第一个匹配URL请求。如果没有发现匹配项，该代码将转而从网络获取响应。</span>
    caches<span>.</span><span>match</span><span>(</span>event<span>.</span>request<span>)</span><span>.</span><span>then</span><span>(</span><span>function</span><span>(</span><span>response</span><span>)</span> <span>{</span>
      <span>if</span> <span>(</span>response<span>)</span> <span>{</span>
        console<span>.</span><span>log</span><span>(</span><span>'Found response in cache:'</span><span>,</span> response<span>)</span><span>;</span>

        <span>return</span> response<span>;</span>
      <span>}</span>
      console<span>.</span><span>log</span><span>(</span><span>'No response found in cache. About to fetch from network...'</span><span>)</span><span>;</span>

      <span>return</span> <span>fetch</span><span>(</span>event<span>.</span>request<span>)</span><span>.</span><span>then</span><span>(</span><span>function</span><span>(</span><span>response</span><span>)</span> <span>{</span>
        console<span>.</span><span>log</span><span>(</span><span>'Response from network is:'</span><span>,</span> response<span>)</span><span>;</span>

        <span>return</span> response<span>;</span>
      <span>}</span><span>)</span><span>.</span><span>catch</span><span>(</span><span>function</span><span>(</span><span>error</span><span>)</span> <span>{</span>
        console<span>.</span><span>error</span><span>(</span><span>'Fetching failed:'</span><span>,</span> error<span>)</span><span>;</span>

        <span>throw</span> error<span>;</span>
      <span>}</span><span>)</span><span>;</span>
    <span>}</span><span>)</span>
  <span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br></div></div></li>
</ul>
<p>参考资料：</p>
<ul>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Service_Worker_API" target="_blank" rel="noopener noreferrer">Service Worker API MDN</a></li>
</ul>
<h2 id="性能优化指标"> 性能优化指标</h2>
<h3 id="rail-性能模型"> RAIL 性能模型</h3>
<ul>
<li>
<p><strong>RAIL</strong>是一种以<strong>用户为中心的</strong>性能模型，它提供了一种考虑性能的结构。该模型将用户体验分解为关键操作（例如，点击、滚动、加载），并帮助您为每个操作定义性能目标。</p>
</li>
<li>
<p>关键操作：<img src="./../images/rail.png" alt="" /></p>
</li>
<li>
<p>聚焦用户：</p>
<ul>
<li>以下是用户对性能延迟的感知：</li>
</ul>
<table>
<thead>
<tr>
<th>延迟时间</th>
<th>用户感知</th>
</tr>
</thead>
<tbody>
<tr>
<td>0-16ms</td>
<td>很流畅</td>
</tr>
<tr>
<td>0-100ms</td>
<td>基本流畅</td>
</tr>
<tr>
<td>100-1000ms</td>
<td>感觉到网站上有一些加载任务</td>
</tr>
<tr>
<td>1000ms or more</td>
<td>失去耐心了</td>
</tr>
<tr>
<td>10000ms or more</td>
<td>直接离开，不会再访问了</td>
</tr>
</tbody>
</table>
</li>
<li>
<p><strong>Response：</strong> 事件处理最好在50ms内完成。</p>
<ul>
<li>目标：用户的输入到响应的时间不超过100ms，给用户的感受是瞬间就完成了。</li>
<li>优化方案：
<ul>
<li>事件处理函数在50ms内完成，考虑到idle task的情况，事件会排队，等待时间大概在50ms。适用于click，toggle，starting animations等，不适用于drag和scroll。</li>
<li>复杂的js计算尽可能放在后台，如web worker，避免对用户输入造成阻塞</li>
<li>超过50ms的响应，一定要提供反馈，比如倒计时，进度百分比等。</li>
</ul>
</li>
<li>idle task：除了要处理输入事件，浏览器还有其它任务要做，这些任务会占用部分时间，一般情况会花费50ms的时间，输入事件的响应则排在其后。
<ul>
<li>idle task 对 input response 的影响：<img src="./../images/idle-task.png" alt="" /></li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>Animation：</strong>  在10ms内产生一帧。</p>
<ul>
<li>
<p>目标：产生每一帧的时间不要超过10ms，为了保证浏览器60帧，每一帧的时间在16ms左右，但浏览器需要用6ms来渲染每一帧。旨在视觉上的平滑。用户对帧率变化感知很敏感。一般情况下，每一帧都必须完成下面的所有：</p>
<p><img src="./../images/渲染流程.png" alt="" /></p>
<ul>
<li>16秒的时间中包含浏览器将新帧绘制到屏幕上的时间，所以留给上述5步的时间几乎之后10ms。</li>
</ul>
</li>
<li>
<p>优化方案：</p>
<ul>
<li>在一些高压点上，比如动画，不要使用 cpu，如：取offset，设置style等操作。尽可能地保证60帧的体验。</li>
<li>在渲染性能上，针对不同的动画做一些特定优化</li>
</ul>
</li>
<li>
<p>动画不只是UI的视觉效果，以下行为都属于</p>
<ul>
<li>视觉动画，如渐隐渐显，tweens，loading等</li>
<li>滚动，包含弹性滚动，松开手指后，滚动会持续一段距离</li>
<li>拖拽，缩放，经常伴随着用户行为</li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>Idle：</strong>  最大化空闲时间</p>
<ul>
<li>
<p>目标：最大化空闲时间，以增大50ms内响应用户输入的几率</p>
</li>
<li>
<p>优化方案：</p>
<ul>
<li>用空闲时间来完成一些延后的工作，如先加载页面可见的部分，然后利用空闲时间加载剩余部分，此处可以使用 ；<a href="https://developer.mozilla.org/en-US/docs/Web/API/Window/requestIdleCallback" target="_blank" rel="noopener noreferrer">requestIdleCallback API</a></li>
<li>在空闲时间内执行的任务尽量控制在50ms以内，如果更长的话，会影响input handle的pending时间；</li>
<li>如果用户在空闲时间任务进行时进行交互，必须以此为最高优先级，并暂停空闲时间的任务；</li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>Load:</strong>  传输内容到页面可交互的时间不超过5秒</p>
<ul>
<li>
<p>如果页面加载比较慢，用户的交点可能会离开。加载很快的页面，用户平均停留时间会变长，跳出率会更低，也就有更高的广告查看率</p>
</li>
<li>
<p>目标:</p>
<ul>
<li>优化加载速度，可以根据设备、网络等条件。目前，比较好的一个方式是，让你的页面在一个中配的3G网络手机上打开时间不超过5秒</li>
<li>对于第二次打开，尽量不超过2秒</li>
</ul>
</li>
<li>
<p>优化方案</p>
<ul>
<li>在手机设备上测试加载性能，选用中配的3G网络（400kb/s，400ms RTT），可以使用 <a href="https://www.webpagetest.org/easy" target="_blank" rel="noopener noreferrer">WebPageTest</a> 来测试</li>
<li>要注意的是，即使用户的网络是4G，但因为丢包或者网络波动，可能会比预期的更慢</li>
<li><a href="https://web.dev/render-blocking-resources/" target="_blank" rel="noopener noreferrer">禁用渲染阻塞的资源，延后加载</a></li>
<li>可以采用 <a href="https://web.dev/native-lazy-loading/" target="_blank" rel="noopener noreferrer">lazy load</a>，<a href="https://web.dev/reduce-javascript-payloads-with-code-splitting/" target="_blank" rel="noopener noreferrer">code-splitting</a> 等 <a href="https://web.dev/fast/" target="_blank" rel="noopener noreferrer">其他优化</a> 手段，让第一次加载的资源更少</li>
</ul>
</li>
</ul>
</li>
<li>
<p>分析RAIL用的工具</p>
<ul>
<li><a href="https://developers.google.com/web/tools/chrome-devtools" target="_blank" rel="noopener noreferrer">Chrome DevTools</a></li>
<li><a href="https://web.dev/measure/" target="_blank" rel="noopener noreferrer">Lighthouse</a></li>
<li><a href="https://webpagetest.org/easy" target="_blank" rel="noopener noreferrer">WebPageTest</a></li>
</ul>
</li>
</ul>
<p><strong>参考资料</strong></p>
<p><a href="https://developers.google.com/web/tools/chrome-devtools/profile/evaluate-performance/rail?hl=en" target="_blank" rel="noopener noreferrer">The RAIL Performance Model</a></p>
<h3 id="优化指标"> 优化指标</h3>
<h4 id="总览"> 总览</h4>
<p><img src="./../images/性能优化指标.png" alt="" /></p>
<h4 id="lighthouse-性能指标"> <strong>LightHouse 性能指标</strong></h4>
<p>Lighthouse 中指标 (Metric) 包括以下内容：</p>
<ul>
<li>
<p><a href="https://web.dev/first-contentful-paint/" target="_blank" rel="noopener noreferrer">First Contentful Paint</a>：FCP 测量在用户导航到您的页面后浏览器呈现第一段 DOM 内容所需的时间。页面上的图像、非白色<code>&lt;canvas&gt;</code>元素和 SVG 被视为 DOM 内容；不包括iframe 内的任何内容。</p>
</li>
<li>
<p><a href="https://web.dev/first-meaningful-paint/" target="_blank" rel="noopener noreferrer">First Meaningful Paint</a>：(FMP)</p>
<ul>
<li>概念：FMP度量用户何时可以看到页面的主要内容。FMP的原始分数是用户初始化页面加载到页面呈现主要折叠内容之间的时间(以秒为单位)。FMP本质上显示了在最大的折叠上的布局变化发生之后的显示时间。</li>
<li>在 Lighthouse 6.0 中已不推荐使用 FMP，建议使用 <a href="https://web.dev/largest-contentful-paint/" target="_blank" rel="noopener noreferrer">Largest Contentful Paint</a> 代替。</li>
</ul>
</li>
<li>
<p><a href="https://web.dev/speed-index/" target="_blank" rel="noopener noreferrer">Speed Index</a>：（SI）Speed Index度量页面加载过程中内容可视化显示的速度。Lighthouse首先在浏览器中捕捉页面加载的视频，并计算帧间的视觉进程。然后Lighthouse使用Speedline Node.js模块生成速度指数分数。</p>
</li>
<li>
<p><a href="https://web.dev/first-cpu-idle/" target="_blank" rel="noopener noreferrer">First CPU Idle</a>：页面第一次可以响应用户输入的时间。</p>
<ul>
<li>在 Lighthouse 6.0 中，已不推荐使用 FCI，原因是虽然 FCI 提供了比 TTI 更有意义的度量，但这种差异还不足以证明维护两个类似度量的合理性。建议考虑使用 <a href="https://web.dev/lighthouse-total-blocking-time/" target="_blank" rel="noopener noreferrer">Total Blocking Time</a> 和 TTI。</li>
</ul>
</li>
<li>
<p><a href="https://web.dev/interactive/" target="_blank" rel="noopener noreferrer">Time to Interactive</a>：（TTI）表示网页第一次 <strong>完全达到可交互状态</strong> 的时间点，浏览器已经可以持续性的响应用户的输入。完全达到可交互状态的时间点是在最后一个长任务（Long Task）完成的时间, 并且在随后的 5 秒内网络和主线程是空闲的。</p>
<ul>
<li>
<p><img src="./../images/tti.png" alt="" /></p>
</li>
<li>
<p>长任务是需要 50 毫秒以上才能完成的任务</p>
</li>
<li>
<p>测量方式：TTI 的计算依赖于对 Long Task 和主线程是否空闲的观察，目前并不在 Web 性能标准规范中，但在一些性能监视工具中实现了。Google 提供了采集 TTI 的 API：<a href="https://github.com/GoogleChromeLabs/tti-polyfill" target="_blank" rel="noopener noreferrer">tti-polyfill</a></p>
<div><pre><code><span>const</span> ttiPolyfill <span>=</span> <span>require</span><span>(</span><span>'tti-polyfill'</span><span>)</span><span>;</span>
ttiPolyfill<span>.</span><span>getFirstConsistentlyInteractive</span><span>(</span><span>)</span><span>.</span><span>then</span><span>(</span><span>(</span><span>tti</span><span>)</span> <span>=></span> <span>{</span>
  window<span>.</span>perfData<span>.</span><span>push</span><span>(</span><span>{</span>
    <span>'tti'</span><span>:</span> tti
  <span>}</span><span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div></li>
</ul>
</li>
<li>
<p><a href="https://web.dev/lighthouse-max-potential-fid/" target="_blank" rel="noopener noreferrer">Max Potential First Input Delay</a>：（FID）</p>
<ul>
<li>从用户第一次与页面交互（例如单击链接、点击按钮等）到浏览器实际能够响应该交互的时间。</li>
<li>输入延迟是因为浏览器的主线程正忙于做其他事情，所以不能响应用户。发生这种情况的一个常见原因是浏览器正忙于解析和执行应用程序加载的大型 JavaScript 文件。</li>
<li>第一次输入延迟通常发生在第一次内容绘制（FCP）和可持续交互时间（TTI）之间，因为页面已经呈现了一些内容，但还不能可靠地交互。</li>
<li>如果用户刚好在主线程最忙时与页面交互，延迟响应的时间就会较长，如果与页面交互是在主线程空闲期间，浏览器可能会立即响应。所以对于 FID 这个指标，我们需要关注的是整体的 FID 值分布，而不是单一值。</li>
<li><img src="./../images/FID.png" alt="" /></li>
<li>测量方式：Google 提供了一个 JS 库 <a href="https://github.com/GoogleChromeLabs/first-input-delay" target="_blank" rel="noopener noreferrer">github.com/GoogleChrom…</a> 用于测量 FID</li>
</ul>
</li>
<li>
<p><a href="https://web.dev/lighthouse-total-blocking-time/" target="_blank" rel="noopener noreferrer">Total Blocking Time</a>：TBT测量页面被阻止响应用户输入(如鼠标点击、屏幕点击或键盘按下)的总时间。该总和是通过添加所有长任务的阻塞部分之间的第一次有内容的绘制和时间到交互式。任何执行时间超过50毫秒的任务都是长任务。50ms后的时间量为阻塞部分。例如，如果Lighthouse检测到一个70毫秒长的任务，那么阻塞部分将是20毫秒</p>
</li>
<li>
<p><a href="https://web.dev/lighthouse-largest-contentful-paint/" target="_blank" rel="noopener noreferrer">Largest Contentful Paint</a>：（LCP）</p>
<ul>
<li>
<p>可视区域中最大的内容元素呈现到屏幕上的时间，用以估算页面的主要内容对用户可见时间。</p>
</li>
<li>
<p>测量方法：</p>
<div><pre><code><span>try</span> <span>{</span>
  <span>const</span> po <span>=</span> <span>new</span> <span>PerformanceObserver</span><span>(</span><span>(</span><span>entryList</span><span>)</span> <span>=></span> <span>{</span>
    <span>const</span> entries <span>=</span> entryList<span>.</span><span>getEntries</span><span>(</span><span>)</span><span>;</span>
    <span>const</span> lastEntry <span>=</span> entries<span>[</span>entries<span>.</span>length <span>-</span> <span>1</span><span>]</span><span>;</span>

    <span>// 优先取 renderTime，如果没有则取 loadTime</span>
    <span>let</span> lcp <span>=</span> lastEntry<span>.</span>renderTime <span>||</span> lastEntry<span>.</span>loadTime<span>;</span>
    window<span>.</span>perfData<span>.</span><span>push</span><span>(</span><span>{</span>
      <span>'LCP'</span><span>,</span> lcp
    <span>}</span><span>)</span><span>;</span>
  <span>}</span><span>)</span><span>;</span>
  po<span>.</span><span>observe</span><span>(</span><span>{</span>type<span>:</span> <span>'largest-contentful-paint'</span><span>}</span><span>)</span><span>;</span>
<span>}</span> <span>catch</span> <span>(</span>e<span>)</span> <span>{</span>
  <span>// Do nothing </span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div></li>
</ul>
<p>}</p>
<div><pre><code>


</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
</ul>
<h4 id="关键指标"> <strong>关键指标</strong></h4>
<ul>
<li>
<p>当从浏览器打开一个页面，经历：白屏 → 背景图 → 出现部分内容 → 首屏内容全部出现，但图片还在加载中 → 首屏内容全部出现，图片也已经加载完成。</p>
<p><img src="./../images/首屏过程.png" alt="" /></p>
</li>
<li>
<p>在此过程中的关键指标：</p>
<table>
<thead>
<tr>
<th>用户体验核心指标</th>
<th>定义</th>
<th>衡量指标</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>白屏时间</strong></td>
<td>页面开始有内容的时间，在没有内容之前是白屏</td>
<td>FP</td>
</tr>
<tr>
<td><strong>首次渲染内容</strong></td>
<td>浏览器首次绘制来自 DOM 的内容的时间</td>
<td>FCP</td>
</tr>
<tr>
<td><strong>重要内容渲染时间</strong></td>
<td>主要内容绘制到屏幕上的时间</td>
<td>FMP</td>
</tr>
<tr>
<td><strong>首屏时间</strong></td>
<td>可视区域内容已完全呈现的时间</td>
<td>FSP</td>
</tr>
<tr>
<td><strong>可交互时间</strong></td>
<td>用户第一次可以与页面交互的时间</td>
<td>FCI</td>
</tr>
<tr>
<td><strong>可流畅交互时间</strong></td>
<td>用户第一次可以持续与页面交互的时间</td>
<td>TTI</td>
</tr>
</tbody>
</table>
</li>
</ul>
<p><strong>白屏时间（FP或FCP）：</strong></p>
<ul>
<li>
<p>概念：</p>
<ul>
<li>从开始加载到浏览器<strong>首次绘制像素</strong>到屏幕上的时间，也就是页面在屏幕上首次发生视觉变化的时间。</li>
<li>变化可能是简单的背景色更新或不引人注意的内容</li>
<li>First Paint 不包括默认的背景绘制，但包括非默认的背景绘制。</li>
<li>通常认为浏览器开始渲染 <code>&lt;body&gt;</code> 标签或者解析完 <code>&lt;head&gt;</code>标签的时刻就是页面白屏结束的时间点</li>
</ul>
</li>
<li>
<p><img src="./../images/白屏.png" alt="" /></p>
</li>
<li>
<p>计算白屏时间:</p>
<ul>
<li>
<p>可使用 Performance API 时：白屏时间 = firstPaint - performance.timing.navigationStart</p>
</li>
<li>
<p>不可使用 Performance API 时：白屏时间 = firstPaint - performance.timing.navigationStart</p>
<p><img src="./../images/白屏测量.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
<p><strong>首屏时间（FSP）：</strong></p>
<ul>
<li>
<p>概念：页面从开始加载到首屏内容全部绘制完成的时间，用户可以看到首屏的全部内容。</p>
<p><img src="./../images/首屏时间过程.png" alt="" /></p>
</li>
<li>
<p>计算首屏时间方法：</p>
<ul>
<li>首屏模块标签标记法：	一种传统的采集方式，通常适用于首屏内容不需要通过拉取数据才能生存以及页面不考虑图片等资源加载的情况。
<ul>
<li>首屏时间= firstScreen - performance.timing.navigationStart;</li>
<li><img src="./../images/首屏模块标签标记法.png" alt="" /></li>
</ul>
</li>
<li>统计首屏内加载最慢的图片的时间：通常首屏内容加载最慢的就是图片资源,因此会把首屏内加载最慢的图片的时间当做首屏的时间。
<ul>
<li>方法：首屏时间=加载最慢的图片的时间点 - performance.timing.navigationStart</li>
<li>由于浏览器对每个页面的 TCP 连接数有限制，使得并不是所有图片都能立刻开始下载和显示，因此在 DOM树 构建完成后将会去遍历首屏内的所有图片标签，并且监听所有图片标签 onload 事件，最终遍历图片标签的加载时间的最大值，并用这个最大值减去 navigationStart 即可获得近似的首屏时间</li>
</ul>
</li>
<li>自定义首屏内容计算法：由于统计首屏内图片完成加载的时间比较复杂。因此我们在业务中通常会通过自定义模块内容，来简化计算首屏时间。
<ul>
<li>方法：忽略图片等资源加载情况，只考虑页面主要 DOM，只考虑首屏的主要模块，而不是严格意义首屏线以上的所有内容。</li>
</ul>
</li>
</ul>
</li>
<li>
<p>现代浏览器问题：不会等待CSS树（所有CSS文件下载和解析完成）和DOM树（整个body标签解析完成）构建完成而是马上开始显示中间结果，所以经常在低网速的环境中，观察到页面由上至下缓慢显示完，或者先显示文本内容后再重绘成带有格式的页面内容</p>
</li>
<li>
<p>影响首屏内容完整绘制：避免横向屏幕外和纵向屏幕外元素的绘制阻塞首屏内容的渲染。</p>
<ul>
<li>如在开发过程中，把内容列表代码放在导航代码前面，浏览器会先渲染完列表内容再渲染导航，如果超过屏幕外的列表内容很长，会严重影响首屏内导航显示到屏幕的时间。</li>
</ul>
</li>
</ul>
<h2 id="性能监控"> 性能监控</h2>
<p><img src="./../images/性能监控.png" alt="" /></p>
<ul>
<li>前端性能监控：代码监控、工具监控、插件分析工具。
<ul>
<li>**代码监控：**依托于js代码并部署到需监控的页面，手动计算时间差或者使用浏览器的的API进行数据统计。
<ul>
<li>影响代码监控数据的因素：
<ul>
<li>浏览器渲染机制；</li>
<li>浏览器对API的实现程度，如：<code>performance API</code>；</li>
</ul>
</li>
</ul>
</li>
<li>**工具监控：**不用将统计代码部署到页面中，输入需统计的url并且选择运行次url的浏览器版本，通过工具对 url 进行请求分析后便可以给出各种性能指标，比如瀑布流、静态文件数量、首屏渲染时间等。</li>
<li><strong>插件分析工具：</strong> 通过插件分析开发时各依赖包的大小，通过减少主要包体积优化。</li>
</ul>
</li>
</ul>
<h3 id="代码监控"> 代码监控</h3>
<ul>
<li>代码监控主要使用<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Navigation_timing_API" target="_blank" rel="noopener noreferrer"><strong>Navigation Timing API</strong></a> 提供的可用于衡量一个网站性能的数据。主要提供以下两个接口：
<ul>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Performance" target="_blank" rel="noopener noreferrer"><code>Performance</code></a>：获取到当前页面中与性能相关的信息。它是 High Resolution Time API 的一部分，同时也融合了 Performance Timeline API、<a href="https://developer.mozilla.org/en-US/docs/Web/API/Navigation_timing_API" target="_blank" rel="noopener noreferrer">Navigation Timing API</a>、 <a href="https://developer.mozilla.org/en-US/docs/Web/API/User_Timing_API" target="_blank" rel="noopener noreferrer">User Timing API</a> 和 <a href="https://developer.mozilla.org/en-US/docs/Web/API/Resource_Timing_API" target="_blank" rel="noopener noreferrer">Resource Timing API</a>。</li>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/PerformanceNavigationTiming" target="_blank" rel="noopener noreferrer"><code>PerformanceNavigationTiming</code></a>：提供了方法和属性来存取关于游览器文档navigation事件的相关数据。如文档实际加载/卸载的时间。</li>
</ul>
</li>
</ul>
<h4 id="performance-api"> Performance API</h4>
<ul>
<li>
<p><a href="https://www.w3.org/TR/2014/WD-navigation-timing-2-20140325/" target="_blank" rel="noopener noreferrer"><code>window.performance</code></a> 是W3C性能小组引入的新的API，目前IE9以上的浏览器都支持。</p>
</li>
<li>
<p>Performance API 用于精确度量、控制、增强浏览器的性能表现。这个 API 为测量网站性能， 提供以前没有办法做到的精度。</p>
</li>
<li>
<p>结构：<img src="./../images/perfomance.png" alt="" /></p>
<ul>
<li>
<p>​	<code>memory</code>：JavaScript 占用的内存。</p>
</li>
<li>
<p><code>navigation</code>：统计网页导航相关的数据：</p>
<ol>
<li><code>redirectCount</code>:重定向的数量（只读），但是这个接口有同源策略限制，即仅能检测同源的重定向；</li>
<li>type 返回值：
<ul>
<li>0 : TYPE_NAVIGATE (用户通过常规导航方式访问页面，比如点一个链接，或者一般的get方式)</li>
<li>1 : TYPE_RELOAD (用户通过刷新，包括JS调用刷新接口等方式访问页面)</li>
<li>2 : TYPE_BACK_FORWARD (用户通过后退按钮访问本页面)</li>
</ul>
</li>
</ol>
</li>
<li>
<p><code>timing</code>：统计数据 网络、解析等时间数据。</p>
<p><img src="./../images/timing-api.png" alt="" /></p>
<ul>
<li>
<blockquote>
<ul>
<li>
<p><code>startTime</code>：有些浏览器实现为<a href="https://msdn.microsoft.com/en-us/library/ff974724(v=vs.85).aspx" target="_blank" rel="noopener noreferrer"><code>navigationStart</code></a>，代表浏览器开始unload前一个页面文档的开始时间节点。比如我们当前正在浏览 baidu.com，在地址栏输入google.com并回车，浏览器的执行动作依次为：<strong>unload当前文档（即baidu.com）-&gt;请求下一文档（即google.com）</strong>。navigationStart的值便是触发unload当前文档的时间节点。</p>
<blockquote>
<p>如果当前文档为空，则navigationStart的值等于fetchStart。</p>
</blockquote>
</li>
<li>
<p><code>redirectStart</code>和<code>redirectEnd</code>：如果页面是由redirect而来，则redirectStart和redirectEnd分别代表redirect开始和结束的时间节点；</p>
</li>
<li>
<p><code>unloadEventStart</code>和<code>unloadEventEnd</code>：如果前一个文档和请求的文档是同一个域的，则<code>unloadEventStart</code>和<code>unloadEventEnd</code>分别代表浏览器unload前一个文档的开始和结束时间节点。否则两者都等于0；</p>
</li>
<li>
<p><code>fetchStart</code>是指在浏览器发起任何请求之前的时间值。在fetchStart和<code>domainLookupStart</code>之间，浏览器会检查当前文档的缓存；</p>
</li>
<li>
<p><code>domainLookupStart</code>和<code>domainLookupEnd</code>分别代表DNS查询的开始和结束时间节点。如果浏览器没有进行DNS查询（比如使用了cache），则两者的值都等于<code>fetchStart</code>；</p>
</li>
<li>
<p><code>connectStart</code>和<code>connectEnd</code>分别代表TCP建立连接和连接成功的时间节点。如果浏览器没有进行TCP连接（比如使用持久化连接webscoket），则两者都等于<code>domainLookupEnd</code>；</p>
</li>
<li>
<p><code>secureConnectionStart</code>：可选。如果页面使用HTTPS，它的值是安全连接握手之前的时刻。如果该属性不可用，则返回undefined。如果该属性可用，但没有使用HTTPS，则返回0；</p>
</li>
<li>
<p><code>requestStart</code>代表浏览器发起请求的时间节点，请求的方式可以是请求服务器、缓存、本地资源等；</p>
</li>
<li>
<p><code>responseStart</code>和<code>responseEnd</code>分别代表浏览器收到从服务器端（或缓存、本地资源）响应回的第一个字节和最后一个字节数据的时刻；</p>
</li>
<li>
<p><code>domLoading</code>代表浏览器开始解析html文档的时间节点。我们知道IE浏览器下的document有<code>readyState</code>属性，<code>domLoading</code>的值就等于<code>readyState</code>改变为<code>loading</code>的时间节点；</p>
</li>
<li>
<p><code>domInteractive</code>代表浏览器解析html文档的状态为<code>interactive</code>时的时间节点。<code>domInteractive</code>并非DOMReady，它早于DOMReady触发，代表html文档解析完毕（即dom tree创建完成）但是内嵌资源（比如外链css、js等）还未加载的时间点；</p>
</li>
<li>
<p><code>domContentLoadedEventStart</code>：代表<code>DOMContentLoaded</code>事件触发的时间节点：</p>
<blockquote>
<p>页面文档完全加载并解析完毕之后,会触发DOMContentLoaded事件，HTML文档不会等待样式文件,图片文件,子框架页面的加载(load事件可以用来检测HTML页面是否完全加载完毕(fully-loaded))。</p>
</blockquote>
</li>
<li>
<p><code>domContentLoadedEventEnd</code>：代表<code>DOMContentLoaded</code>事件完成的时间节点，此刻用户可以对页面进行操作，也就是jQuery中的domready时间；</p>
</li>
<li>
<p><code>domComplete</code>：html文档完全解析完毕的时间节点；</p>
</li>
<li>
<p><code>loadEventStart</code>和<code>loadEventEnd</code>分别代表onload事件触发和结束的时间节点</p>
</li>
</ul>
</blockquote>
</li>
</ul>
</li>
<li>
<p><strong>计算性能指标：</strong></p>
<ul>
<li>DNS查询耗时 = domainLookupEnd - domainLookupStart</li>
<li>TCP链接耗时 = connectEnd - connectStart</li>
<li>request 请求耗时 = responseEnd - responseStart</li>
<li>解析 dom 树耗时 = domComplete - domInteractive</li>
<li>白屏时间 = domloadng - fetchStart</li>
<li>domready 时间 = domContentLoadedEventEnd - fetchStart</li>
<li>onload 时间 = loadEventEnd - fetchStart</li>
</ul>
</li>
</ul>
</li>
<li>
<p>使用 getTime 来计算脚本耗时:</p>
<ul>
<li>缺点:</li>
<li>getTime 方法（以及 Date 对象的其他方法） 都只能精确到毫秒级别（一秒的千分之一），无法计算更小的时间差。
<ul>
<li>这种写法只能获取代码运行过程中的时间进度，无法知道一些后台事件的时间进度，比如浏览器用 了多少时间从服务器加载网页。</li>
</ul>
</li>
<li>实现:</li>
</ul>
<div><pre><code><span>// 计算加载时间</span>
<span>function</span> <span>getPerformanceTiming</span> <span>(</span><span>)</span> <span>{</span> 
    <span>var</span> performance <span>=</span> window<span>.</span>performance<span>;</span>
  
    <span>if</span> <span>(</span><span>!</span>performance<span>)</span> <span>{</span>
        <span>// 当前浏览器不支持</span>
        console<span>.</span><span>log</span><span>(</span><span>'你的浏览器不支持 performance 接口'</span><span>)</span><span>;</span>
        <span>return</span><span>;</span>
    <span>}</span>
  
    <span>var</span> t <span>=</span> performance<span>.</span>timing<span>;</span>
    <span>var</span> times <span>=</span> <span>{</span><span>}</span><span>;</span>
  
    <span>//【重要】页面加载完成的时间</span>
    <span>//【原因】这几乎代表了用户等待页面可用的时间</span>
    times<span>.</span>loadPage <span>=</span> t<span>.</span>loadEventEnd <span>-</span> t<span>.</span>navigationStart<span>;</span>
  
    <span>//【重要】解析 DOM 树结构的时间</span>
    <span>//【原因】反省下你的 DOM 树嵌套是不是太多了！</span>
    times<span>.</span>domReady <span>=</span> t<span>.</span>domComplete <span>-</span> t<span>.</span>responseEnd<span>;</span>
  
    <span>//【重要】重定向的时间</span>
    <span>//【原因】拒绝重定向！比如，http://example.com/ 就不该写成 http://example.com</span>
    times<span>.</span>redirect <span>=</span> t<span>.</span>redirectEnd <span>-</span> t<span>.</span>redirectStart<span>;</span>
  
    <span>//【重要】DNS 查询时间</span>
    <span>//【原因】DNS 预加载做了么？页面内是不是使用了太多不同的域名导致域名查询的时间太长？</span>
    <span>// 可使用 HTML5 Prefetch 预查询 DNS ，见：[HTML5 prefetch](http://segmentfault.com/a/1190000000633364)           </span>
    times<span>.</span>lookupDomain <span>=</span> t<span>.</span>domainLookupEnd <span>-</span> t<span>.</span>domainLookupStart<span>;</span>
  
    <span>//【重要】读取页面第一个字节的时间</span>
    <span>//【原因】这可以理解为用户拿到你的资源占用的时间，加异地机房了么，加CDN 处理了么？加带宽了么？加 CPU 运算速度了么？</span>
    <span>// TTFB 即 Time To First Byte 的意思</span>
    <span>// 维基百科：https://en.wikipedia.org/wiki/Time_To_First_Byte</span>
    times<span>.</span>ttfb <span>=</span> t<span>.</span>responseStart <span>-</span> t<span>.</span>navigationStart<span>;</span>
  
    <span>//【重要】内容加载完成的时间</span>
    <span>//【原因】页面内容经过 gzip 压缩了么，静态资源 css/js 等压缩了么？</span>
    times<span>.</span>request <span>=</span> t<span>.</span>responseEnd <span>-</span> t<span>.</span>requestStart<span>;</span>
  
    <span>//【重要】执行 onload 回调函数的时间</span>
    <span>//【原因】是否太多不必要的操作都放到 onload 回调函数里执行了，考虑过延迟加载、按需加载的策略么？</span>
    times<span>.</span>loadEvent <span>=</span> t<span>.</span>loadEventEnd <span>-</span> t<span>.</span>loadEventStart<span>;</span>
  
    <span>// DNS 缓存时间</span>
    times<span>.</span>appcache <span>=</span> t<span>.</span>domainLookupStart <span>-</span> t<span>.</span>fetchStart<span>;</span>
  
    <span>// 卸载页面的时间</span>
    times<span>.</span>unloadEvent <span>=</span> t<span>.</span>unloadEventEnd <span>-</span> t<span>.</span>unloadEventStart<span>;</span>
  
    <span>// TCP 建立连接完成握手的时间</span>
    times<span>.</span>connect <span>=</span> t<span>.</span>connectEnd <span>-</span> t<span>.</span>connectStart<span>;</span>
  
    <span>return</span> times<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br></div></div></li>
<li>
<p>获取首次渲染、首次有内容的渲染
<img src="./../images/首屏时间.png" alt="" /></p>
</li>
</ul>
<h4 id="resource-timing-api"> Resource Timing API</h4>
<ul>
<li>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Resource_Timing_API" target="_blank" rel="noopener noreferrer"><code>Resource Timing API</code></a> 为网络事件(如重定向的开始和结束事件, DNS查找的开始和结束事件, 请求开始, 响应开始和结束时间等)生成有高分辨率时间戳( <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/DOMHighResTimeStamp" target="_blank" rel="noopener noreferrer">high-resolution timestamps</a> )的资源加载时间线, 并提供了资源大小和资源类型.</p>
</li>
<li>
<p>PerformanceResourceTiming 接口在 Performance Timeline 面板上有展现，并且其扩展 PerformanceEntry 接口。</p>
</li>
<li>
<p>使用 <strong>performance.getEntries()方法</strong>  数组形式，返回一个 PerformanceEntry 列表，这些请求的时间统计信息，有多少个请求，返回数组就会有多少个成员。有以下属性：</p>
<ul>
<li>
<p>name：这个属性必须返回请求资源的解析URL。如果重定向到其他URL，此属性也不更改。</p>
</li>
<li>
<p>entryType：entryType属性返回 DOMString “resource”。entryType 类型不同数组中的对象结构也不同。initiatorType 表：</p>
</li>
<li>
<table>
<thead>
<tr>
<th>**值 **</th>
<th>**描述 **</th>
</tr>
</thead>
<tbody>
<tr>
<td>mark</td>
<td>通过 mark() 方法添加到数组中的对象</td>
</tr>
<tr>
<td>paint</td>
<td>通过 measure() 方法添加到数组中的对象</td>
</tr>
<tr>
<td>measure</td>
<td>first-contentful-paint 首次内容绘制</td>
</tr>
<tr>
<td>resource</td>
<td>所有资源加载时间，用处最多</td>
</tr>
</tbody>
</table>
</li>
<li>
<p>startTime：startTime属性返回一个 DOMHighResTimeStamp [ HR-TIME-2 ]，表示用户代理获取资源开始排队等待之前的时间。如果在获取资源时存在 HTTP 重定向或类同情况，并且所有重定向或类同情况都来自与当前文档同源，或者通过了timing  allow check 算法，则此属性返回 redirectStart 的值。否则，此属性返回 fetchStart 的值。</p>
</li>
<li>
<p>duration：该duration属性返回一个 DOMHighResTimeStamp，等于 responseEnd 和 startTime 之间的差。</p>
</li>
</ul>
</li>
</ul>
<h3 id="工具监控"> 工具监控</h3>
<h4 id="lighthouse"> LightHouse</h4>
<h5 id="安装-使用"> 安装/使用</h5>
<p>针对不同的适用场景，我们可以通过多种方式来安装并使用 Lighthouse：</p>
<ul>
<li>浏览器插件。Chrome 插件的形式提供了更加友好的用户界面，方便读取报告。</li>
<li>Chrome DevTools。该工具集成在最新版本的 Chrome 浏览器中，无需安装即可使用。</li>
<li>Lighthouse CLI 命令行工具。方便将 Lighthouse 集成到持续集成系统中。</li>
<li>编程的方式。我们也能通过 Node.js 模块引入 Lighthouse 工具包，以编程的形式来使用它。</li>
</ul>
<p>具体安装方式查看 <a href="https://lavas-project.github.io/pwa-book/appendix01/1-lighthouse.html#%E4%BD%BF%E7%94%A8-lighthouse-%E6%B5%8B%E8%AF%84-pwa" target="_blank" rel="noopener noreferrer">使用 Lighthouse</a></p>
<h5 id="测评报告"> 测评报告</h5>
<ul>
<li>
<p>使用 Lighthouse 对网站进行测评后，我们会得到一份评分报告，它包含了：</p>
<ul>
<li>性能（Performance）</li>
<li>访问无障碍（Accessibility）</li>
<li>最佳实践（Best Practice）</li>
<li>搜索引擎优化（SEO）</li>
<li>PWA（Progressive Web App）</li>
</ul>
<p><img src="./../images/lighthouse1.png" alt="" /></p>
</li>
<li>
<p>报告中带有相关优化建议：</p>
<ul>
<li>性能（Performance）结果:<img src="./../images/lighthouse2.png" alt="" />
<ul>
<li>性能评分分为了三部分：
<ul>
<li><a href="https://web.dev/lighthouse-performance/#metrics" target="_blank" rel="noopener noreferrer">Metrics</a> 部分的指标项会直接影响分数，可以作为我们的主要参考点。</li>
<li><a href="https://web.dev/lighthouse-performance/#opportunities" target="_blank" rel="noopener noreferrer">Opportunities</a> 提供了详细的建议和文档，来解释低分的原因，帮助我们具体进行实现和改进。</li>
<li><a href="https://web.dev/lighthouse-performance/#diagnostics" target="_blank" rel="noopener noreferrer">Diagnostics</a> 部分的列表项则为进一步改善性能的实验和调整，给出了指导。</li>
</ul>
</li>
</ul>
</li>
<li>访问无障碍（Accessibility）测试结果：<img src="./../images/lighthouse3.png" alt="" /></li>
<li>最佳实践（Best Practice）测试结果：<img src="./../images/lighthouse4.png" alt="" /></li>
<li>SEO 测试结果：<img src="./../images/lighthouse5.png" alt="" /></li>
<li>PWA 测试结果：<img src="./../images/lighthouse6.png" alt="" /></li>
</ul>
</li>
</ul>
<h5 id="performance-评分"> Performance 评分</h5>
<ul>
<li>
<p>性能评分的分值区间是：0到100。</p>
<ul>
<li>如果出现0分，通常是在运行 Lighthouse 时发生了错误，满分100分代表了站点已经达到了98分位值的数据，而50分则是75分位值的数据。</li>
<li>分值区间和颜色的对应关系如下：
<ul>
<li>0 - 49（慢）：红色</li>
<li>50 - 89（平均值）： 橙色</li>
<li>90 - 100（快）： 绿色</li>
</ul>
</li>
</ul>
</li>
<li>
<p>性能指标评分的计算方式：每一项性能指标对评分的贡献都有其计算逻辑，Lighthouse 会将原始的性能值映射成为 0-100 之间的数字。评分使用 HTTPArchive 上的真实站点性能数据作为样本，统计出对数正态分布。</p>
<ul>
<li>例如，您的 FCP 分数是基于<a href="https://httparchive.org/reports/loading-speed#fcp" target="_blank" rel="noopener noreferrer">HTTP 存档中的数据</a>，您的页面的 FCP 时间与真实网站的 FCP 时间的比较。例如，在 99% 中执行的站点在大约 1.2 秒内呈现 FCP。如果您网站的 FCP 为 1.2 秒，则您的 FCP 分数为 99。请参阅<a href="https://web.dev/performance-scoring/#metric-scores" target="_blank" rel="noopener noreferrer">如何确定指标分数</a>以了解如何设置 Lighthouse 分数阈值。参考<a href="https://web.dev/first-contentful-paint/#how-lighthouse-determines-your-fcp-score" target="_blank" rel="noopener noreferrer">Lighthouse 如何确定分数</a></li>
</ul>
</li>
<li>
<p>性能评分的分配权重：各个指标对性能评分的贡献并不相同，Lighthouse 提供了<a href="https://docs.google.com/spreadsheets/d/1Cxzhy5ecqJCucdf1M0iOzM8mIxNc7mmx107o5nj38Eo/edit#gid=0" target="_blank" rel="noopener noreferrer">评分详情表</a>，形式如下图，来查阅具体权重分配情况。权重较大的指标，对性能评分的影响更大一些，最终的总体性能评分是这些性能指标分数的加权平均值。我们可以用这个分数计算的<a href="https://docs.google.com/spreadsheets/d/1dXH-bXX3gxqqpD1f7rp6ImSOhobsT1gn_GQ2fGZp8UU/edit?ts=59fb61d2#gid=283330180" target="_blank" rel="noopener noreferrer">表格</a>作为参考，以了解不同的指标是如何影响最终评分的。<img src="./../images/性能评分权重表.png" alt="" /></p>
<p>从表中可以看出，性能的指标项权重分配如下：</p>
<ul>
<li>3X - 首次内容绘制</li>
<li>1X - 首次有效绘制</li>
<li>2X - 首次 CPU 空闲</li>
<li>5X - 可交互时间</li>
<li>4X - 速度指标</li>
<li>0X - 输入延迟估值</li>
</ul>
</li>
<li>
<p>不同 Lighthouse 版本对权重不同：</p>
<ul>
<li><a href="https://web.dev/performance-scoring/#lighthouse-8" target="_blank" rel="noopener noreferrer">Lighthouse 8 </a></li>
</ul>
<table>
<thead>
<tr>
<th style="text-align:left">Audit</th>
<th style="text-align:left">Weight</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><a href="https://web.dev/first-contentful-paint/" target="_blank" rel="noopener noreferrer">First Contentful Paint</a></td>
<td style="text-align:left">10%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/speed-index/" target="_blank" rel="noopener noreferrer">Speed Index</a></td>
<td style="text-align:left">10%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/lcp/" target="_blank" rel="noopener noreferrer">Largest Contentful Paint</a></td>
<td style="text-align:left">25%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/interactive/" target="_blank" rel="noopener noreferrer">Time to Interactive</a></td>
<td style="text-align:left">10%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/lighthouse-total-blocking-time/" target="_blank" rel="noopener noreferrer">Total Blocking Time</a></td>
<td style="text-align:left">30%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/cls/" target="_blank" rel="noopener noreferrer">Cumulative Layout Shift</a></td>
<td style="text-align:left">15%</td>
</tr>
</tbody>
</table>
<ul>
<li><a href="https://web.dev/performance-scoring/#lighthouse-6" target="_blank" rel="noopener noreferrer">Lighthouse 6 </a></li>
</ul>
<table>
<thead>
<tr>
<th style="text-align:left">Audit</th>
<th style="text-align:left">Weight</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><a href="https://web.dev/first-contentful-paint/" target="_blank" rel="noopener noreferrer">First Contentful Paint</a></td>
<td style="text-align:left">15%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/speed-index/" target="_blank" rel="noopener noreferrer">Speed Index</a></td>
<td style="text-align:left">15%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/lcp/" target="_blank" rel="noopener noreferrer">Largest Contentful Paint</a></td>
<td style="text-align:left">25%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/interactive/" target="_blank" rel="noopener noreferrer">Time to Interactive</a></td>
<td style="text-align:left">15%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/lighthouse-total-blocking-time/" target="_blank" rel="noopener noreferrer">Total Blocking Time</a></td>
<td style="text-align:left">25%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/cls/" target="_blank" rel="noopener noreferrer">Cumulative Layout Shift</a></td>
<td style="text-align:left">5%</td>
</tr>
</tbody>
</table>
<ul>
<li>Lighthouse 5</li>
</ul>
<table>
<thead>
<tr>
<th style="text-align:left">Audit</th>
<th style="text-align:left">Weight</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><a href="https://web.dev/first-contentful-paint/" target="_blank" rel="noopener noreferrer">First Contentful Paint</a></td>
<td style="text-align:left">20%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/speed-index/" target="_blank" rel="noopener noreferrer">Speed Index</a></td>
<td style="text-align:left">27%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/first-meaningful-paint/" target="_blank" rel="noopener noreferrer">First Meaningful Paint</a></td>
<td style="text-align:left">7%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/interactive/" target="_blank" rel="noopener noreferrer">Time to Interactive</a></td>
<td style="text-align:left">33%</td>
</tr>
<tr>
<td style="text-align:left"><a href="https://web.dev/first-cpu-idle/" target="_blank" rel="noopener noreferrer">First CPU Idle</a></td>
<td style="text-align:left">13%</td>
</tr>
</tbody>
</table>
</li>
<li>
<p>使用<a href="https://googlechrome.github.io/lighthouse/scorecalc/" target="_blank" rel="noopener noreferrer">Lighthouse 评分计算器</a>来帮助了解您应该为达到某个 Lighthouse 性能分数而设定的阈值。</p>
</li>
</ul>
<p><strong>参考资料</strong></p>
<p><a href="https://web.dev/lighthouse-performance/" target="_blank" rel="noopener noreferrer">Lighthoust Performance audits</a></p>
<h2 id="性能优化方案"> 性能优化方案</h2>
<h3 id="总览-2"> 总览</h3>
<p><img src="./../images/优化方案.png" alt="" /></p>
<h3 id="网络加载优化"> 网络加载优化</h3>
<h4 id="资源提示"> 资源提示</h4>
<p>此规范定义了 HTML 的 <code>&lt;Link&gt;</code> 元素的 rel 属性值，包括 dns-prefetch、preconnect、prefetch 和 prerender。我们可以使用这些资源提示让用用户代理帮助我们预解析 DNS、预链接、预加载资源以及预处理资源以提高页面性能。</p>
<h5 id="dns-预解析-dns-prefetch"> <strong>DNS 预解析 (DNS-Prefetch)：</strong></h5>
<ul>
<li>通过 DNS 预解析来告诉浏览器未来我们可能从某个特定的 URL 获取资源，当浏览器真正使用到该域中的某个资源时就可以尽快地完成 DNS 解析。</li>
<li>方法： <code>&lt;link rel=&quot;dns-prefetch&quot; href=&quot;//example.com&quot;&gt;</code></li>
</ul>
<h5 id="预连接-preconnect"> <strong>预连接 (Preconnect)：</strong></h5>
<ul>
<li>
<p>与 DNS 预解析类似，preconnect 不仅完成 DNS 预解析，同时还将进行 TCP 握手和建立传输层协议</p>
</li>
<li>
<p>方法： <code>&lt;link rel=&quot;preconnect&quot; href=&quot;http://example.com&quot;&gt;</code></p>
<p><img src="./../images/preconnect.png" alt="image-20210707235115997" /></p>
</li>
<li>
<p>应用：</p>
<ul>
<li>只对一些立即用到的第三方站点添加 preconnect 处理，要节省用 preconnect。</li>
<li>用 preconnect 给所有的第三方站点都做提前链接很低效，可以为重要的第三方站点做 preconnect ,其余的都做 dns-prefetch. 可节约20-120ms。</li>
</ul>
</li>
</ul>
<h5 id="预获取-prefetching"> <strong>预获取 (Prefetching)：</strong></h5>
<ul>
<li>告诉浏览器获取下一次导航可能需要的资源。大多数情况下，这意味着将以极低的优先级来获取资源（因为浏览器知道当前页面中需要的所有内容比我们认为在下一页中需要的资源更重要）。这意味着 prefetch 的主要用处是加快下一个导航的速度，而不是当前的导航。</li>
<li>方法：<code>&lt;link rel=&quot;prefetch&quot; href=&quot;image.png&quot;&gt;</code></li>
<li>与预解析DNS区别：与 DNS 预解析不同，预获取真正请求并下载了资源，并储存在缓存中。但预获取还依赖于一些条件，某些预获取可能会被浏览器忽略，例如从一个非常缓慢的网络中获取一个庞大的字体文件。并且，Firefox 只会在浏览器闲置时进行资源预获取。</li>
</ul>
<h5 id="子资源-subresources"> <strong>子资源 (Subresources)：</strong></h5>
<ul>
<li>另一个预获取方式，这种方式指定的预获取资源具有最高的优先级，在所有 prefetch 项之前进行</li>
<li>方法：<code>&lt;link rel=&quot;subresource&quot; href=&quot;styles.css&quot;&gt;</code></li>
<li>rel=prefetch 为将来的页面提供了一种低优先级的资源预加载方式，而 rel=subresource 为当前页面提供了一种高优先级的资源预加载。所以，如果资源是当前页面必须的，或者资源需要尽快可用，那么最好使用 subresource 而不是 prefetch</li>
</ul>
<h5 id="预渲染-prerender"> <strong>预渲染 (Prerender)：</strong></h5>
<ul>
<li>prerender 可以预先加载文档的所有资源。这类似于在一个隐藏的 tab 页中打开了某个链接 – 将下载所有资源、创建 DOM 结构、完成页面布局、应用 CSS 样式和执行 JavaScript 脚本等。当用户真正访问该链接时，隐藏的页面就切换为可见，使页面看起来就是瞬间加载完成一样</li>
<li>方法：<code>&lt;link rel=&quot;prerender&quot; href=&quot;http://example.com&quot;&gt;</code></li>
<li>注意：不要滥用该特性，当你知道用户一定会点击某个链接时才可以进行预渲染，否则浏览器将无条件地下载所有预渲染需要的资源，所有预加载技术都存在一个潜在的风险：对资源预测错误，而预加载的开销（抢占 CPU 资源，消耗电池，浪费带宽等）是高昂的，所以必须谨慎行事。虽然很难确定用户下一步将访问哪些资源，但高可信的场景确实存在：
<ul>
<li>如果用户完成一个带有明显结果的搜索，那么结果页面很可能会被加载</li>
<li>如果用户进入到登陆页面，那么登陆成功的页面很可能会被加载</li>
<li>如果用户阅读一个多页的文章或访问一个分页的结果集，那么下一页很可能会被加载</li>
</ul>
</li>
</ul>
<h5 id="预加载-preload"> <strong>预加载(Preload)：</strong></h5>
<ul>
<li>
<p>告诉用户代理预读取资源而不必执行它们，允许将资源加载与执行分离，细粒度控制资源何时和如何加载。</p>
</li>
<li>
<p>方法：<code>&lt;link rel=&quot;preload&quot; href=&quot;image.png&quot;&gt;</code></p>
</li>
<li>
<p>示例：应用程序可以使用 preload 关键字启动 CSS 资源的早期、高优先级和非呈现阻塞获取，然后应用程序可以在适当的时间应用这些获取：</p>
<div><pre><code>Using markup
<span>&lt;</span><span>!</span><span>--</span> preload stylesheet resource via declarative markup <span>--</span><span>></span>
<span>&lt;</span>link rel<span>=</span><span>"preload"</span> href<span>=</span><span>"/styles/other.css"</span> <span>as</span><span>=</span><span>"style"</span><span>></span>

<span>&lt;</span><span>!</span><span>--</span> or<span>,</span> preload stylesheet resource via JavaScript <span>--</span><span>></span>
<span>&lt;</span>script<span>></span>
<span>var</span> res <span>=</span> document<span>.</span><span>createElement</span><span>(</span><span>"link"</span><span>)</span><span>;</span>
res<span>.</span>rel <span>=</span> <span>"preload"</span><span>;</span>
res<span>.</span>as <span>=</span> <span>"style"</span><span>;</span>
res<span>.</span>href <span>=</span> <span>"styles/other.css"</span><span>;</span>
document<span>.</span>head<span>.</span><span>appendChild</span><span>(</span>res<span>)</span><span>;</span>
<span>&lt;</span><span>/</span>script<span>></span>

Using <span>HTTP</span> Header
Link<span>:</span> <span>&lt;</span>https<span>:</span><span>/</span><span>/</span>example<span>.</span>com<span>/</span>other<span>/</span>styles<span>.</span>css<span>></span><span>;</span> rel<span>=</span>preload<span>;</span> <span>as</span><span>=</span>style
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div></li>
<li>
<p>假设您页面的<a href="https://web.dev/critical-request-chains" target="_blank" rel="noopener noreferrer">关键请求链</a>如下所示：</p>
<div><pre><code>index.html
|--app.js   
	|--styles.css   
	|--ui.js
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br></div></div><ul>
<li>
<p><code>index.html</code>文件声明<code>&lt;script src=&quot;app.js&quot;&gt;</code>. 当<code>app.js</code>运行时，它调用<code>fetch()</code>以下载<code>styles.css</code>和<code>ui.js</code>。在下载、解析和执行最后 2 个资源之前，页面不会显示完整。使用上面的示例，Lighthouse 将标记<code>styles.css</code>和<code>ui.js</code>作为候选。</p>
</li>
<li>
<p>潜在的节省取决于如果您声明了预加载链接，浏览器可以提前多长时间启动请求。例如，如果<code>app.js</code>下载、解析和执行需要 200 毫秒，则每个资源的潜在节省为 200 毫秒，因为<code>app.js</code>不再是每个请求的瓶颈。</p>
</li>
<li>
<p>无预加载过程： app.js 加载后再加载 ui.js 和 style.css<img src="./../images/chrome-preload.png" alt="" /></p>
</li>
<li>
<p>使用预加载过程：app.js 、 ui.js 和 style.css 同时加载</p>
<p><img src="./../images/chrome-preload2.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
<h5 id="preload-与-prefetch比较"> preload 与 prefetch比较</h5>
<ul>
<li>prefetch 和 preload 都可以声明一个资源及其获取属性，但在用户代理获取资源的方式和时间上有所不同：</li>
<li>prefetch 是可选的，通常是用于后续导航可能使用的资源的低优先级获取；
<ul>
<li>preload 是当前导航所必需的资源的强制获取。开发人员应该合理使用它们来最小化资源争用和优化加载性能。</li>
<li>在 preload 中 as 属性对于保证正确的优先级、请求匹配、请求对应正确的内容安全策略（<a href="http://www.html5rocks.com/en/tutorials/security/content-security-policy/" target="_blank" rel="noopener noreferrer">Content-Security-Policy</a> ）指令以及基于资源类型发送正确的 Accept 首部是必需的。</li>
</ul>
</li>
</ul>
<h4 id="避免链关键请求"> 避免链关键请求</h4>
<ul>
<li>
<p><a href="https://developers.google.com/web/fundamentals/performance/critical-rendering-path" target="_blank" rel="noopener noreferrer">关键请求链</a>是一系列对页面渲染很重要的依赖网络请求。链的长度越大，下载量越大，对页面加载性能的影响就越大。</p>
</li>
<li>
<p><a href="https://developers.google.com/web/tools/lighthouse/" target="_blank" rel="noopener noreferrer">Lighthouse</a>报告加载了高优先级的关键请求：</p>
</li>
</ul>
<p><img src="./../images/chrome-关键链接请求.png" alt="Lighthouse 最小化关键请求深度审计的屏幕截图" /></p>
<ul>
<li>
<p><a href="https://web.dev/critical-request-chains/#how-lighthouse-identifies-critical-request-chains" target="_blank" rel="noopener noreferrer">Lighthouse 如何识别关键请求链</a>：</p>
<ul>
<li>Lighthouse 使用网络优先级作为识别阻塞渲染的关键资源的代理。有关<a href="https://docs.google.com/document/d/1bCDuq9H1ih9iNjgzyAL0gpwNFiEP4TZS-YLRp_RuMlc/edit" target="_blank" rel="noopener noreferrer">Chrome</a>如何定义这些优先级的更多信息，请参阅 Google 的<a href="https://docs.google.com/document/d/1bCDuq9H1ih9iNjgzyAL0gpwNFiEP4TZS-YLRp_RuMlc/edit" target="_blank" rel="noopener noreferrer">Chrome 资源优先级和调度</a>。</li>
<li>有关关键请求链、资源大小和下载资源所用时间的数据是从<a href="https://github.com/ChromeDevTools/devtools-protocol" target="_blank" rel="noopener noreferrer">Chrome 远程调试协议中</a>提取的。</li>
</ul>
</li>
<li>
<p><a href="https://web.dev/critical-request-chains/#how-to-reduce-the-effect-of-critical-request-chains-on-performance" target="_blank" rel="noopener noreferrer">如何减少关键请求链对性能的影响</a>：</p>
<ul>
<li>使用关键请求链审计结果首先定位对页面加载影响最大的资源：
<ul>
<li>尽量减少关键资源的数量：消除它们、推迟下载、将它们标记为<code>async</code>，等等。</li>
<li>优化关键字节数以减少下载时间（往返次数）。</li>
<li>优化剩余关键资源的加载顺序：尽早下载所有关键资产，缩短关键路径长度。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h4 id="减少http请求"> 减少http请求</h4>
<ul>
<li>
<p>HTTP连接产生的开销：域名解析—TCP连接—发送请求—等待—下载资源—解析时间。中间有个等待和下载资源，这些都比较消耗时间，所以我们为了解决http产生的开销，尽量让他减少http连接。</p>
<ul>
<li><img src="./../images/chrome-http.png" alt="" />
<ul>
<li>Queueing: 在请求队列中的时间。</li>
<li>Stalled: 从TCP 连接建立完成，到真正可以传输数据之间的时间差，此时间包括代理协商时间。</li>
<li>Proxy negotiation: 与代理服务器连接进行协商所花费的时间。</li>
<li>DNS Lookup: 执行DNS查找所花费的时间，页面上的每个不同的域都需要进行DNS查找。</li>
<li>Initial Connection / Connecting: 建立连接所花费的时间，包括TCP握手/重试和协商SSL。</li>
<li>SSL: 完成SSL握手所花费的时间。</li>
<li>Request sent: 发出网络请求所花费的时间，通常为一毫秒的时间。</li>
<li>Waiting(TFFB): TFFB 是发出页面请求到接收到应答数据第一个字节的时间。</li>
<li>Content Download: 接收响应数据所花费的时间。</li>
</ul>
</li>
</ul>
</li>
<li>
<p><a href="https://web.dev/total-byte-weight/#how-the-lighthouse-network-payload-audit-fails" target="_blank" rel="noopener noreferrer">Lighthouse 审计网络负载</a></p>
<ul>
<li><a href="https://developers.google.com/web/tools/lighthouse/" target="_blank" rel="noopener noreferrer">Lighthouse</a>显示页面请求的所有资源的总大小（以<a href="https://en.wikipedia.org/wiki/Kibibyte" target="_blank" rel="noopener noreferrer">千字节 (KiB)</a>为单位<a href="https://en.wikipedia.org/wiki/Kibibyte" target="_blank" rel="noopener noreferrer">）</a>。最大的请求首先呈现：</li>
</ul>
<p><img src="./../images/chrome-资源请求.png" alt="Lighthouse 的屏幕截图避免巨大的网络负载审计" /></p>
<ul>
<li>根据<a href="https://httparchive.org/reports/state-of-the-web?start=latest#bytesTotal" target="_blank" rel="noopener noreferrer">HTTP 存档数据</a>，网络有效负载的中位数在 1,700 到 1,900 KiB 之间。为了帮助显示最高的有效负载，Lighthouse 会标记总网络请求超过 5,000 KiB 的页面。</li>
</ul>
</li>
<li>
<p>减少HTTP请求的方式：</p>
<ul>
<li>推迟请求，直到需要它们。</li>
<li>资源压缩。</li>
<li>使用缓存。</li>
<li>减少页面中的元素：网页中的的图片、form、flash等等元素都会发出HTTP请求，尽可能的减少页面中非必要的元素，可以减少HTTP请求的次数</li>
<li>图片地图（Image Maps）：也就是图像热点，图像地图就是把一张图片分成若干区域,每个区域指向不同的URL地址，这些区域也称为热点。Image Maps只适用于连续的图标</li>
<li>CSS Sprites(CSS精灵)：图片是增加HTTP请求的最大可能者，把全站的图标都放在一个图像文件中，然后用CSS的background-image和background-position属性定位来显示其中的一小部分</li>
<li>合并css：合并脚本和CSS文件，可以减少了HTTP请求</li>
<li>合并js：少用location.reload()，使用location.reload() 会刷新页面，刷新页面时页面所有资源（css，js，img等）会重新请求服务器；建议使用 <code>location.href=&quot;当前页url&quot;</code> 代替 location.reload() ，使用 location.href 浏览器会读取本地缓存资源。</li>
<li>动态页面静态化：动态网页实际上并不是独立存在于服务器上的网页文件，只有当用户请求时服务器才返回一个完整的网页
用户访问动态页面需要与数据库服务器进行数据交换</li>
</ul>
</li>
</ul>
<h4 id="避免重定向"> 避免重定向</h4>
<ul>
<li>
<p>重定向会降低页面加载速度。当浏览器请求已重定向的资源时，服务器通常会返回这样的 HTTP 响应：</p>
<div><pre><code><span>HTTP/1.1 <span>301 Moved Permanently</span></span>
<span>Location:</span> /path/to/new/location
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div><ul>
<li>然后浏览器必须在新位置发出另一个 HTTP 请求以检索资源。跨网络的这种额外行程可能会延迟数百毫秒的资源加载。</li>
</ul>
</li>
<li>
<p><a href="https://developers.google.com/web/tools/lighthouse/" target="_blank" rel="noopener noreferrer">Lighthouse</a>标记具有多个重定向的页面：</p>
<p><img src="./../images/chrome-chongdingxiang.png" alt="img" /></p>
<ul>
<li>当页面有两个或多个重定向时，该页面无法通过此审核。</li>
</ul>
</li>
<li>
<p><a href="https://web.dev/redirects/#how-to-eliminate-redirects" target="_blank" rel="noopener noreferrer">如何消除重定向</a>:</p>
<ul>
<li>
<p>将指向标记资源的链接指向资源的当前位置。避免重定向<a href="https://developers.google.com/web/fundamentals/performance/critical-rendering-path/" target="_blank" rel="noopener noreferrer">关键渲染路径</a>所需的资源尤为重要。</p>
</li>
<li>
<p>如果您使用重定向将移动用户转移到您页面的移动版本，请考虑重新设计您的网站以使用<a href="https://developers.google.com/web/fundamentals/design-and-ux/responsive/" target="_blank" rel="noopener noreferrer">响应式设计</a></p>
</li>
</ul>
</li>
</ul>
<h4 id="使用-http2"> 使用 HTTP2</h4>
<ul>
<li>
<p>HTTP2 相比 HTTP1.1 有如下几个优点：</p>
<ul>
<li>
<p>解析速度快：服务器解析 HTTP1.1 的请求时，必须不断地读入字节，直到遇到分隔符 CRLF 为止。而解析 HTTP2 的请求就不用这么麻烦，因为 HTTP2 是基于帧的协议，每个帧都有表示帧长度的字段。</p>
</li>
<li>
<p>多路复用：HTTP1.1 如果要同时发起多个请求，就得建立多个 TCP 连接，因为一个 TCP 连接同时只能处理一个 HTTP1.1 的请求。在 HTTP2 上，多个请求可以共用一个 TCP 连接，这称为多路复用。同一个请求和响应用一个流来表示，并有唯一的流 ID 来标识。 多个请求和响应在 TCP 连接中可以乱序发送，到达目的地后再通过流 ID 重新组建。</p>
</li>
<li>
<p>首部压缩：HTTP2 提供了首部压缩功能。</p>
</li>
<li>
<p>优先级：HTTP2 可以对比较紧急的请求设置一个较高的优先级，服务器在收到这样的请求后，可以优先处理。</p>
</li>
<li>
<p>流量控制：由于一个 TCP 连接流量带宽（根据客户端到服务器的网络带宽而定）是固定的，当有多个请求并发时，一个请求占的流量多，另一个请求占的流量就会少。流量控制可以对不同的流的流量进行精确控制。</p>
</li>
<li>
<p>服务器推送：HTTP2 新增的一个强大的新功能，就是服务器可以对一个客户端请求发送多个响应。除了对最初请求的响应外，服务器还可以额外向客户端推送资源，而无需客户端明确地请求。</p>
</li>
</ul>
</li>
</ul>
<h4 id="静态资源使用-cdn"> 静态资源使用 CDN</h4>
<ul>
<li>通过将静态资源(例如javascript，css，图片等等）缓存到离用户很近的相同网络运营商的CDN节点上</li>
<li>使用 CDN 优势：不但能提升用户的访问速度，还能节省服务器的带宽消耗，降低负载</li>
<li>过程：其实这是CDN服务商在全国各个省份部署计算节点，CDN加速将网站的内容缓存在网络边缘,不同地区的用户就会访问到离自己最近的相同网络线路上的CDN节点，当请求达到CDN节点后，节点会判断自己的内容缓存是否有效，如果有效，则立即响应缓存内容给用户，从而加快响应速度。如果CDN节点的缓存失效，它会根据服务配置去我们的内容源服务器获取最新的资源响应给用户，并将内容缓存下来以便响应给后续访问的用户。因此，一个地区内只要有一个用户先加载资源，在CDN中建立了缓存，该地区的其他后续用户都能因此而受益</li>
</ul>
<h4 id="http-缓存"> HTTP 缓存</h4>
<ul>
<li>HTTP 缓存可以加快重复访问时的页面加载时间。</li>
<li>当浏览器请求资源时，提供资源的服务器可以告诉浏览器它应该临时存储或<em>缓存</em>资源多长时间。对于该资源的任何后续请求，浏览器使用其本地副本而不是从网络获取它。</li>
</ul>
<p><strong><a href="https://web.dev/uses-long-cache-ttl/#how-the-lighthouse-cache-policy-audit-fails" target="_blank" rel="noopener noreferrer">Lighthouse 如何审计缓存</a></strong></p>
<ul>
<li><a href="https://developers.google.com/web/tools/lighthouse/" target="_blank" rel="noopener noreferrer">Lighthouse</a>标记所有未缓存的静态资源：</li>
</ul>
<p><img src="./../images/chrome-缓存.png" alt="具有高效缓存策略审计的 Lighthouse Serve 静态资产的屏幕截图" /></p>
<ul>
<li>
<p>如果满足以下所有条件，Lighthouse 会认为资源可缓存：</p>
<ul>
<li>资源是字体、图像、媒体文件、脚本或样式表。</li>
<li>资源拥有<code>200</code>，<code>203</code>或<code>206</code> <a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Status" target="_blank" rel="noopener noreferrer">HTTP状态代码</a>。</li>
<li>该资源没有明确的无缓存策略。</li>
</ul>
</li>
<li>
<p>当页面未通过审核时，Lighthouse 会将结果列在一个包含三列的表格中：</p>
</li>
</ul>
<table>
<thead>
<tr>
<th><strong>网址</strong></th>
<th>可缓存资源的位置</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>缓存TTL</strong></td>
<td>资源的当前缓存持续时间</td>
</tr>
<tr>
<td><strong>尺寸</strong></td>
<td>如果标记的资源已被缓存，您的用户将保存的数据的估计</td>
</tr>
</tbody>
</table>
<p><strong>缓存实现</strong></p>
<ul>
<li>现代浏览器都实现了 HTTP 缓存机制。浏览器在初次获取资源后，会根据 HTTP 响应头部的 <code>Cache-Control</code> 和 <code>ETag</code> 字段，来决定该资源的缓存策略。</li>
<li><code>Cache-Control</code> 有多个可能的字段值：
<ul>
<li><code>no-store</code>：表明任何缓存不得存储该资源，每次请求都会从服务端返回完整的内容。</li>
<li><code>no-cache</code>：表明必须向服务端发一次请求，该请求头部带有 <code>If-None-Match</code> 等资源校验信息。服务端将会验证该资源是否被修改过，详细见下文 <code>ETag</code> 的描述。</li>
<li><code>public</code>：表明该资源可以被 CDN 或代理等中间人缓存。</li>
<li><code>private</code>：与 <code>public</code> 相反，表明任何中间人不得缓存该资源，资源可能与隐私信息相关。</li>
<li><code>max-age</code>：指定了资源缓存的过期时间（秒），仅当缓存过期后才会向服务端发送请求。</li>
</ul>
</li>
<li><code>ETag</code> 是由服务端决定的一个资源校验字段。如果浏览器缓存已经过期，那么会向服务端询问该资源是否被修改过。服务端会将 HTTP 请求头部的 <code>If-None-Match</code> 与 <code>ETag</code> 进行比对。如果相等，则表明资源并没有被修改过，服务端返回 <code>304 Not Modified</code>，浏览器可以直接从缓存获取资源，并刷新缓存过期时间。否则就需要返回该资源的完整内容。</li>
<li>为了达到最佳缓存效果，常用的做法是：
<ul>
<li>对于 HTML 文件，设置 <code>Cache-Control: no-cache</code>。</li>
<li>对于 CSS 和 JavaScript 等静态资源，上线构建的时候在文件名中插入一段仅与文件内容相关的哈希值，并使用 <code>Cache-Control: max-age=31536000</code> 进行长缓存。</li>
<li>这样的效果是，对于绝大多数静态资源的请求都可以使用缓存来响应，并且仅在文件内容变更时刷新缓存。</li>
</ul>
</li>
</ul>
<p><strong><a href="https://web.dev/uses-long-cache-ttl/#how-to-verify-cached-responses-in-chrome-devtools" target="_blank" rel="noopener noreferrer">如何在 Chrome DevTools 中验证缓存的响应</a></strong></p>
<ul>
<li>
<p>要查看浏览器从其缓存中获取哪些资源，请在 Chrome DevTools 中打开<strong>网络</strong>选项卡：</p>
<ul>
<li>按<code>Control+Shift+J</code>（或<code>Command+Option+J</code>在 Mac 上）打开 DevTools。</li>
<li>单击<strong>网络</strong>选项卡。</li>
</ul>
</li>
<li>
<p>Chrome DevTools 中的<strong>Size</strong>列可以帮助您验证资源是否已被缓存：</p>
<ul>
<li><img src="./../images/chrome-缓存检查.png" alt="" /></li>
<li>Chrome 从内存缓存中提供请求最多的资源，速度非常快，但在浏览器关闭时会被清除。</li>
</ul>
</li>
<li>
<p>要验证资源的<code>Cache-Control</code>标头是否按预期设置，请检查其 HTTP 标头数据：</p>
<ul>
<li>单击请求表的<strong>名称</strong>列下的请求URL 。</li>
<li>单击<strong>标题</strong>选项卡。
<ul>
<li><img src="./../images/chrome-缓存头.png" alt="" /></li>
<li><code>Cache-Control</code>通过“**标题”**选项卡检查标题。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="渲染优化"> 渲染优化</h3>
<h4 id="渲染机制优化"> 渲染机制优化</h4>
<h5 id="创建-pwa-应用"> 创建 PWA 应用</h5>
<h6 id="使用-app-shell"> 使用 App Shell</h6>
<h6 id="添加-骨架屏"> 添加 骨架屏</h6>
<h6 id="使用-service-worker-缓存"> 使用 service worker 缓存</h6>
<h5 id="服务端渲染-ssr"> 服务端渲染 SSR</h5>
<h4 id="浏览器渲染优化"> 浏览器渲染优化</h4>
<p>浏览器渲染时  构成首屏渲染所必须的关键资源，因此通过缩短关键渲染路径来优化首屏性能。</p>
<h5 id="消除渲染阻塞资源"> 消除渲染阻塞资源</h5>
<p>Lighthouse 报告的“机会”部分列出了阻止页面首次绘制的所有 URL。目标是通过内联关键资源、推迟非关键资源和删除任何未使用的资源来减少这些阻止渲染的 URL 的影响</p>
<p><img src="./../images/render-block.png" alt="" /></p>
<p><a href="https://developers.google.com/web/tools/lighthouse/" target="_blank" rel="noopener noreferrer">Lighthouse</a>标记了两种阻止渲染的 URL：脚本和样式表。</p>
<p>一个<code>&lt;script&gt;</code>标签：</p>
<ul>
<li>是在<code>&lt;head&gt;</code>文档中。</li>
<li>没有<code>defer</code>属性。</li>
<li>没有<code>async</code>属性。</li>
</ul>
<p>一个<code>&lt;link rel=&quot;stylesheet&quot;&gt;</code>标签：</p>
<ul>
<li>没有<code>disabled</code>属性。当此属性存在时，浏览器不会下载样式表。</li>
<li>没有<code>media</code>专门匹配用户设备的属性。<code>media=&quot;all&quot;</code>被认为是渲染阻塞。</li>
</ul>
<h6 id="如何识别关键资源"> <a href="https://web.dev/render-blocking-resources/#how-to-identify-critical-resources" target="_blank" rel="noopener noreferrer">如何识别关键资源</a></h6>
<p>减少渲染阻塞资源影响的第一步是确定什么是关键的，什么不是。使用Chrome DevTools 中的<a href="https://developer.chrome.com/docs/devtools/coverage/" target="_blank" rel="noopener noreferrer">Coverage 选项卡</a>来识别非关键 CSS 和 JS。当您加载或运行页面时，该选项卡会告诉您使用了多少代码，以及加载了多少代码：</p>
<p><img src="./../images/chrome-关键资源.png" alt="" /></p>
<p>通过仅加载所需的代码和样式来减小页面大小。单击 URL 以在“源”面板中检查该文件。CSS 文件中的样式和 JavaScript 文件中的代码用两种颜色标记：</p>
<ul>
<li>**绿色（关键）：**首次绘制所需的样式；对页面核心功能至关重要的代码。</li>
<li>**红色（非关键）：**适用于不立即可见的内容的样式；代码未在页面的核心功能中使用。</li>
</ul>
<h6 id="非核心代码异步加载"> 非核心代码异步加载</h6>
<ul>
<li><code>&lt;script/&gt;</code> 标签 <strong>async 属性：</strong> 异步加载脚本，加载后立即执行。</li>
<li>当浏览器解析到 <code>&lt;script async&gt;</code> 时，会对该 JavaScript 文件发起异步请求。请求成功后才会暂停 HTML 解析，并立即执行 JavaScript。</li>
<li>在实际项目中容易发现，多个 <code>&lt;script async&gt;</code> 的执行顺序是不确定的。而且在 JavaScript 执行时，页面的 DOM 可能还未全部构建完成。</li>
<li><code>&lt;script/&gt;</code> 标签  <strong>defer 属性：</strong> 立即加载脚本，加载完成后不执行，延迟至渲染完成执行脚本。
<ul>
<li>浏览器同样会发起异步请求，但 JavaScript 会延迟到 HTML 解析完毕后依次执行。此时 JavaScript 已经能和完整的 DOM 进行交互了。</li>
</ul>
</li>
<li>动态创建 script 标签替代 <code>&lt;script/&gt;</code></li>
</ul>
<h6 id="延迟非关键-css-加载"> 延迟非关键 CSS 加载</h6>
<ul>
<li>
<p>通过 <code>&lt;link rel=&quot;stylesheet&quot;&gt;</code> 引用的 CSS 都会在加载时阻塞页面渲染。但 Web 应用中往往会有一些首屏渲染时用不到的 CSS，如弹框的样式等。为了使这些非关键 CSS 不阻塞页面渲染，我们可以通过别的途径声明 CSS 的加载。</p>
</li>
<li>
<p>通过 <code>&lt;link rel=&quot;preload&quot;&gt;</code> 预加载我们所需的资源。这里仅需稍作改动，就能满足我们的要求。</p>
<ul>
<li>
<div><pre><code><span><span>&lt;!</span><span>DOCTYPE</span> <span>HTML</span><span>></span></span>
<span><span><span>&lt;</span>html</span><span>></span></span>
<span><span><span>&lt;</span>head</span><span>></span></span>
  <span>&lt;!-- ... --></span>
  <span><span><span>&lt;</span>link</span> <span>rel</span><span><span>=</span><span>"</span>preload<span>"</span></span> <span>as</span><span><span>=</span><span>"</span>style<span>"</span></span> <span>href</span><span><span>=</span><span>"</span>/dist/index.css<span>"</span></span> <span>onload</span><span><span>=</span><span>"</span>this.onload=null;this.rel=<span>'</span>stylesheet<span>'</span><span>"</span></span><span>></span></span>
  <span>&lt;!-- ... --></span>
<span><span><span>&lt;/</span>head</span><span>></span></span>
<span><span><span>&lt;/</span>html</span><span>></span></span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div></li>
<li>
<p>在 <code>&lt;link rel=&quot;preload&quot;&gt;</code> 中监听了 <code>onload</code> 事件，并在 CSS 加载完成后，通过修改 <code>rel</code> 为 <code>stylesheet</code> 来将 CSS 应用到页面上，这就绕过了 CSS 会阻塞页面渲染的限制了。在需要考虑浏览器兼容性的情况下，推荐使用 <a href="https://github.com/filamentgroup/loadCSS" target="_blank" rel="noopener noreferrer">loadCSS</a> 来实现非关键 CSS 的加载。</p>
</li>
<li>
<p>对于首屏渲染所必须的关键 CSS，可以考虑通过 <code>&lt;style&gt;</code> 内联，或使用 HTTP/2 服务端推送的方式来加载，可以节省一次网络请求。</p>
</li>
</ul>
</li>
</ul>
<h5 id="requestanimationframe"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window/requestAnimationFrame" target="_blank" rel="noopener noreferrer">requestAnimationFrame()</a></h5>
<ul>
<li>
<p>用户对于不流畅的滚动或动画十分敏感，一般要求页面帧率应达到每秒 60 帧。在这个帧率下，每帧的渲染需要在 16ms 内完成。但浏览器还需要花费大约 6ms 的时间将一帧绘制到屏幕上，从而只给我们留下了 10ms 时间生成一帧。</p>
</li>
<li>
<p>JavaScript 可以进行样式更新，而动画则是以 16ms 为间隔的一连串的样式更新组成。最直接的想法是，可以通过 <code>setInterval()</code> 或递归调用 <code>setTimeout()</code> 来编写动画，如下代码所示。</p>
<div><pre><code><span>function</span> <span>render</span> <span>(</span><span>)</span> <span>{</span>
  element<span>.</span>style<span>.</span>top <span>=</span> element<span>.</span>offsetTop <span>+</span> <span>1</span> <span>+</span> <span>'px'</span>
  element<span>.</span>style<span>.</span>left <span>=</span> element<span>.</span>offsetLeft <span>+</span> <span>1</span> <span>+</span> <span>'px'</span>
  <span>setTimeout</span><span>(</span>render<span>,</span> <span>16</span><span>)</span>
<span>}</span>

<span>setTimeout</span><span>(</span>render<span>,</span> <span>16</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div><ul>
<li>
<p>上述代码实现了一个性能非常差劲的动画，其效果是使一个绝对定位的元素，从页面左上至右下漂浮。但我们用肉眼就能发现，动画产生了卡顿和掉帧的现象。这是由于 <code>setTimeout</code> 和 <code>setInterval</code> 的回调函数会在浏览器渲染两帧之间的任何时间点执行，而定时器的 16ms 又不是特别精确，从而有可能使一次样式变更错过浏览器渲染。</p>
</li>
<li>
<p>为了实现平滑流畅的动画效果，我们应该使用 <code>requestAnimationFrame()</code> 方法来代替定时器</p>
</li>
</ul>
</li>
<li>
<p><code>requestAnimationFrame()</code> 方法接收一个回调函数为参数，其回调函数不在浏览器事件循环中调度，而是在浏览器渲染下一帧之前执行，这可以确保动画不会掉帧。<code>requestAnimationFrame</code> 的回调函数接收一个高精度时间戳 <code>timestamp</code>，类似于 <code>performance.now()</code> 的返回值，代表回调被执行的精确时间，其用法通常如下代码所示。</p>
<div><pre><code><span>function</span> <span>render</span> <span>(</span><span>timestamp</span><span>)</span> <span>{</span>
  <span>// 执行渲染的具体逻辑</span>
  <span>// ...</span>
  <span>requestAnimationFrame</span><span>(</span>render<span>)</span>
<span>}</span>

<span>requestAnimationFrame</span><span>(</span>render<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
<li>
<p>对于需要长时间执行的任务，我们可以尝试把它拆分成多个小任务，然后通过 <code>requestAnimationFrame()</code> 进行调度，以确保页面的流畅性。上文提到生成一帧的时间应该在 10ms 内，所以保守估计，帧间任务的执行时间最好不要超过 3 ~ 4ms，通常做法如下代码所示。</p>
<div><pre><code><span>function</span> <span>scheduleWork</span> <span>(</span><span>timestamp</span><span>)</span> <span>{</span>
  <span>let</span> currentTimestamp
  <span>while</span> <span>(</span>currentTimestamp <span>-</span> timestamp <span>&lt;</span> <span>4</span> <span>&amp;&amp;</span> tasks<span>.</span>length <span>></span> <span>0</span><span>)</span> <span>{</span>
    <span>performWork</span><span>(</span>tasks<span>.</span><span>pop</span><span>(</span><span>)</span><span>)</span>
    currentTimestamp <span>=</span> performance<span>.</span><span>now</span><span>(</span><span>)</span>
  <span>}</span>
  tasks<span>.</span>length <span>></span> <span>0</span> <span>&amp;&amp;</span> <span>requestAnimationFrame</span><span>(</span>scheduleWork<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div></li>
</ul>
<p><strong>参考资料</strong></p>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window/requestAnimationFrame" target="_blank" rel="noopener noreferrer">window.requestAnimationFrame MDN</a></p>
<h5 id="requestidlecallback"> requestIdleCallback()</h5>
<ul>
<li>
<p>**<code>window.requestIdleCallback()</code>**方法将在浏览器的空闲时段内调用的函数排队。这使开发者能够在主事件循环上执行后台和低优先级工作，而不会影响延迟关键事件，如动画和输入响应。函数一般会按先进先调用的顺序执行，然而，如果回调函数指定了执行超时时间<code>timeout</code>，则有可能为了在超时前执行函数而打乱执行顺序。</p>
</li>
<li>
<p><code>requestIdleCallback</code> 的回调函数接收一个参数 <code>deadline</code>，包含 <code>timeRemaining()</code> 方法和 <code>didTimeout</code> 属性。<code>timeRemaining()</code> 返回一个高精度时间戳，表示还剩多少时间执行任务。事件应该在 50ms 内处理，所以 <code>timeRemaining()</code> 的初始值为 50ms。</p>
<div><pre><code><span>function</span> <span>scheduleWork</span> <span>(</span><span>deadline</span><span>)</span> <span>{</span>
  <span>while</span> <span>(</span>deadline<span>.</span><span>timeRemaining</span><span>(</span><span>)</span> <span>></span> <span>5</span> <span>&amp;&amp;</span> tasks<span>.</span>length <span>></span> <span>0</span><span>)</span> <span>{</span>
    <span>performWork</span><span>(</span>tasks<span>.</span><span>pop</span><span>(</span><span>)</span><span>)</span>
  <span>}</span>
  tasks<span>.</span>length <span>></span> <span>0</span> <span>&amp;&amp;</span> <span>requestIdleCallback</span><span>(</span>scheduleWork<span>)</span>
<span>}</span>

<span>requestIdleCallback</span><span>(</span>scheduleWork<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div><ul>
<li>
<p>假设每个任务都能在 5ms 内完成，那么我们可以设定，当剩余空闲时间大于 5ms 时，继续处理剩余任务，否则就优先确保页面能及时响应用户操作，并将剩余的任务重新调度。</p>
</li>
<li>
<p>由于我们并不知道浏览器何时才会空闲，因此 <code>requestIdleCallback</code> 回调函数的执行时机是不确定的。但它提供了一个超时机制，能确保在等待超时后执行回调函数，此时 <code>timeRemaining()</code> 为 <code>0</code>，而 <code>didTimeout</code> 为 <code>true</code>。</p>
<div><pre><code><span>requestIdleCallback</span><span>(</span>scheduleWork<span>,</span> <span>{</span>timeout<span>:</span> <span>2000</span><span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br></div></div><p>不过我们最好不要指定超时时间，因为这还是有可能导致页面无法及时响应用户操作。另外，由于不确定的执行时机，我们绝不应该在回调函数里操作 DOM，或进行任何样式变更，而应该放在 <code>requestAnimationFrame</code> 的回调函数中执行。</p>
</li>
</ul>
</li>
</ul>
<h5 id="使用-web-workers"> 使用 Web Workers</h5>
<ul>
<li>对于需要长时间执行的任务，另一个解决方案是使用 Web Workers</li>
</ul>
<h5 id="优化绘制与合成"> 优化绘制与合成</h5>
<ul>
<li>优化绘制与合成减少重绘。修改元素几何形态相关的样式属性，会触发页面重新布局。</li>
<li>对于绘制来说，修改绝大多数样式属性都会导致页面重绘，这很难避免。仅有的例外是 <code>transform</code> 和 <code>opacity</code>，这是由于它们可以仅由合成器操作图层来实现。另外，合成器运行在单独的线程里，即使浏览器主线程被阻塞，其效果也不会受到影响。所以，<code>transform</code> 和 <code>opacity</code> 非常适合用于实现动画效果，但我们仍需要通过 <code>will-change</code> 为它们创建独立的图层，避免影响其他图层的绘制。</li>
</ul>
<div><pre><code><span>.moving-element</span> <span>{</span>
  <span>will-change</span><span>:</span> transform<span>,</span> opacity<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><p>请参阅<a href="https://developers.google.com/web/fundamentals/performance/rendering/stick-to-compositor-only-properties-and-manage-layer-count" target="_blank" rel="noopener noreferrer">坚持只使用合成器的属性并管理层数</a>和<a href="https://www.html5rocks.com/en/tutorials/speed/high-performance-animations/" target="_blank" rel="noopener noreferrer">高性能动画</a>。</p>
<h5 id="延迟加载插件"> 延迟加载插件</h5>
<ul>
<li>
<p><a href="https://web.dev/third-party-javascript/" target="_blank" rel="noopener noreferrer">插件/第三方资源</a>通常用于展示广告或视频以及与社交媒体集成。默认方法是在页面加载后立即加载第三方资源，但这可能会不必要地减慢页面加载速度。如果第三方内容不重要，可以通过<a href="https://web.dev/fast/#lazy-load-images-and-video" target="_blank" rel="noopener noreferrer">延迟加载</a>来降低这种性能成本。</p>
</li>
<li>
<p>Lighthouse 如何检测可延迟的插件嵌入:</p>
<ul>
<li>
<p>Lighthouse 寻找可以延迟的第三方插件，例如社交按钮小部件或视频嵌入（例如，YouTube 嵌入式播放器）。有关可延期产品和可用外观的数据<a href="https://github.com/patrickhulce/third-party-web/" target="_blank" rel="noopener noreferrer">在第三方网络中维护</a>。</p>
</li>
<li>
<p>如果页面加载属于这些第三方嵌入之一的资源，审核将失败。</p>
<p><img src="./../images/chrome-第三方资源.png" alt="Lighthouse 第三方外观审核突出了 Vimeo 嵌入式播放器和 Drift 实时聊天。" /></p>
</li>
</ul>
</li>
<li>
<p><a href="https://web.dev/third-party-facades/#how-to-defer-third-parties-with-a-facade" target="_blank" rel="noopener noreferrer">如何延迟加载插件</a></p>
<ul>
<li>不要将第三方嵌入直接添加到您的 HTML，而是使用看起来类似于实际嵌入的插件的静态元素替换插件样式。当发生交互时加载实际插件，过程：
<ul>
<li>加载时：向页面添加插件样式的静态元素。</li>
<li>鼠标悬停时：预连接到第三方资源。</li>
<li>单击时：用第三方插件替换静态元素。</li>
</ul>
</li>
</ul>
</li>
<li>
<p>示例：使用 youtube 图片替代实际视频，当鼠标悬停时加载视频资源，单击时才使用视频替换图片</p>
<p><img src="./../images/chrome-第三方资源示例.png" alt="" /></p>
</li>
</ul>
<p><strong>参考资料</strong></p>
<p><a href="https://lavas-project.github.io/pwa-book/chapter08/2-rendering-performance.html" target="_blank" rel="noopener noreferrer">渲染性能</a></p>
<h3 id="服务端优化"> 服务端优化</h3>
<p>Lighthouse 报告的 Opportunities 部分报告 Time to First Byte，即用户浏览器接收页面内容的第一个字节所需的时间：</p>
<p><img src="./../images/chorme-ttfb.png" alt="" /></p>
<ul>
<li><a href="https://web.dev/time-to-first-byte/#slow-server-response-times-affect-performance" target="_blank" rel="noopener noreferrer">缓慢的服务器响应时间会影响性能</a>
<ul>
<li>当浏览器等待服务器响应主文档请求的时间超过 600 毫秒时，此审核将失败。用户不喜欢页面加载时间过长。服务器响应时间慢是页面加载时间过长的可能原因之一。</li>
<li>当用户在 Web 浏览器中导航到某个 URL 时，浏览器会发出网络请求以获取该内容。您的服务器接收请求并返回页面内容。</li>
<li>服务器可能需要做很多工作才能返回包含用户想要的所有内容的页面。例如，如果用户正在查看他们的订单历史记录，服务器需要从数据库中获取每个用户的历史记录，然后将该内容插入到页面中。</li>
<li>优化服务器以尽快完成这样的工作是减少用户等待页面加载时间的一种方法。</li>
</ul>
</li>
</ul>
<h4 id="nginx-开启gzip-压缩"> Nginx 开启Gzip 压缩</h4>
<ul>
<li>
<p>Nginx实现资源压缩的原理是通过ngx_http_gzip_module模块拦截请求，并对需要做gzip的类型做gzip，ngx_http_gzip_module是Nginx默认集成的，不需要重新编译，直接开启即可。</p>
</li>
<li>
<p>Nginx 配置：在nginx.conf中加入以下内容。保存并重启 nginx。</p>
<div><pre><code><span>server</span><span>{</span>
    <span>gzip</span> on<span>;</span>
    <span>gzip_buffers</span> <span>32</span> <span>4</span>K<span>;</span>
    <span>gzip_comp_level</span> <span>6</span><span>;</span>
    <span>gzip_min_length</span> <span>100</span><span>;</span>
    <span>gzip_types</span> application<span>/</span>javascript text<span>/</span>css text<span>/</span>xml<span>;</span>
    <span>gzip_disable</span> <span>"MSIE [1-6]\."</span><span>;</span> <span>#配置禁用gzip条件，支持正则。此处表示ie6及以下不启用gzip（因为ie低版本不支持）</span>
    <span>gzip_vary</span> on<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div><blockquote>
<p>gzip配置的常用参数:</p>
<p>gzip on|off; #是否开启gzip</p>
<p>gzip_buffers 32 4K| 16 8K #缓冲(压缩在内存中缓冲几块? 每块多大?)</p>
<p>gzip_comp_level [1-9] #推荐6 压缩级别(级别越高,压的越小,越浪费CPU计算资源)</p>
<p>gzip_disable #正则匹配UA 什么样的Uri不进行gzip</p>
<p>gzip_min_length 200 # 开始压缩的最小长度</p>
<p>gzip_http_version 1.0|1.1 # 开始压缩的http协议版本(可以不设置,目前几乎全是1.1协议)</p>
<p>gzip_proxied # 设置请求者代理服务器,该如何缓存内容</p>
<p>gzip_types text/plain application/xml # 对哪些类型的文件用压缩 如txt,xml,html ,css</p>
<p>gzip_vary on|off # 是否传输gzip压缩标志</p>
</blockquote>
</li>
<li>
<p>检查：通过F12看请求的响应头部，强制刷新页面（为了避免缓存，需要强制刷新）</p>
<ul>
<li><img src="./../images/gzip.png" alt="image-20210719101848079" />
<ul>
<li>Content-Encoding:gzip 说明开启了gzip压缩</li>
<li>Transfet-Encoding:chunked 说明压缩后分块传输</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>参考资料</strong></p>
<p><a href="https://www.cnblogs.com/Renyi-Fan/p/11047490.html" target="_blank" rel="noopener noreferrer">nginx中如何设置gzip</a></p>
<h3 id="构建优化"> 构建优化</h3>
<p><strong>参考资料</strong></p>
<p><a href="http://webpack.wuhaolin.cn/4%E4%BC%98%E5%8C%96/" target="_blank" rel="noopener noreferrer">深入浅出 webpack-优化</a></p>
<h3 id="图片资源优化"> 图片资源优化</h3>
<p>Lighthouse 报告的“机会”部分列出了所有未优化的图像，并可能以<a href="https://en.wikipedia.org/wiki/Kibibyte" target="_blank" rel="noopener noreferrer">千字节 (KiB) 为单位</a>节省。优化这些图像，使页面加载速度更快并消耗更少的数据：</p>
<p><img src="./../images/chrome-images.png" alt="" /></p>
<ul>
<li><a href="https://web.dev/uses-optimized-images/#how-lighthouse-flags-images-as-optimizable" target="_blank" rel="noopener noreferrer">Lighthouse 如何将图像标记为可优化</a>
<ul>
<li>Lighthouse 收集页面上所有的 JPEG 或 BMP 图片，将每张图片的压缩级别设置为 85，然后将原始版本与压缩版本进行比较。如果潜在节省为 4KiB 或更大，Lighthouse 会将图像标记为可优化。</li>
</ul>
</li>
</ul>
<p><strong>常见的图片格式及使用场景</strong></p>
<ul>
<li>
<p><strong>BMP</strong>，是无损的、既支持索引色也支持直接色的点阵图。这种图片格式几乎没有对数据进行压缩，所以BMP格式的图片通常是较大的文件。</p>
</li>
<li>
<p><strong>GIF</strong>是无损的、采用索引色的点阵图。采用LZW压缩算法进行编码。文件小，是GIF格式的优点，同时，GIF格式还具有支持动画以及透明的优点。但是GIF格式仅支持8bit的索引色，所以GIF格式适用于对色彩要求不高同时需要文件体积较小的场景。</p>
</li>
<li>
<p><strong>JPEG</strong>是有损的、采用直接色的点阵图。JPEG的图片的优点是采用了直接色，得益于更丰富的色彩，JPEG非常适合用来存储照片，与GIF相比，JPEG不适合用来存储企业Logo、线框类的图。因为有损压缩会导致图片模糊，而直接色的选用，又会导致图片文件较GIF更大。</p>
</li>
<li>
<p><strong>PNG-8</strong>是无损的、使用索引色的点阵图。PNG是一种比较新的图片格式，PNG-8是非常好的GIF格式替代者，在可能的情况下，应该尽可能的使用PNG-8而不是GIF，因为在相同的图片效果下，PNG-8具有更小的文件体积。除此之外，PNG-8还支持透明度的调节，而GIF并不支持。除非需要动画的支持，否则没有理由使用GIF而不是PNG-8。</p>
</li>
<li>
<p><strong>PNG-24</strong>是无损的、使用直接色的点阵图。PNG-24的优点在于它压缩了图片的数据，使得同样效果的图片，PNG-24格式的文件大小要比BMP小得多。当然，PNG24的图片还是要比JPEG、GIF、PNG-8大得多。</p>
</li>
<li>
<p><strong>SVG</strong>是无损的矢量图。SVG是矢量图意味着SVG图片由直线和曲线以及绘制它们的方法组成。当放大SVG图片时，看到的还是线和曲线，而不会出现像素点。这意味着SVG图片在放大时，不会失真，所以它非常适合用来绘制Logo、Icon等。</p>
</li>
<li>
<p><strong>WebP</strong>是谷歌开发的一种新图片格式，WebP是同时支持有损和无损压缩的、使用直接色的点阵图。从名字就可以看出来它是为Web而生的，什么叫为Web而生呢？就是说相同质量的图片，WebP具有更小的文件体积。现在网站上充满了大量的图片，如果能够降低每一个图片的文件大小，那么将大大减少浏览器和服务器之间的数据传输量，进而降低访问延迟，提升访问体验。目前只有Chrome浏览器和Opera浏览器支持WebP格式，兼容性不太好。</p>
</li>
</ul>
<p><strong>图片格式比较</strong></p>
<ul>
<li>在无损压缩的情况下，相同质量的WebP图片，文件大小要比PNG小26%；</li>
<li>在有损压缩的情况下，具有相同图片精度的WebP图片，文件大小要比JPEG小25%~34%；</li>
<li>WebP图片格式支持图片透明度，一个无损压缩的WebP图片，如果要支持透明度只需要22%的格外文件大小。</li>
</ul>
<h4 id="图片懒加载"> 图片懒加载</h4>
<h4 id="字体图标代替图片图标"> 字体图标代替图片图标</h4>
<h4 id="用视频替换动画-gif"> 用视频替换动画 GIF</h4>
<p>Lighthouse 报告的“机会”部分列出了所有动画 GIF，以及通过将这些 GIF 转换为视频所实现的估计节省的秒数：</p>
<p><img src="./../images/chrome-gif.png" alt="Lighthouse 使用视频格式进行动画内容审核的截图" /></p>
<ul>
<li>
<p>请参阅<a href="https://web.dev/performance-scoring" target="_blank" rel="noopener noreferrer">Lighthouse 性能评分</a>帖子，了解如何计算页面的整体性能评分。</p>
</li>
<li>
<p><a href="https://web.dev/efficient-animated-content/#why-you-should-replace-animated-gifs-with-video" target="_blank" rel="noopener noreferrer">为什么你应该用视频替换动画 GIF</a></p>
<ul>
<li>大型 GIF 无法提供动画内容。通过将大型 GIF 转换为视频，您可以节省大量用户的带宽。考虑将 MPEG4/WebM 视频用于动画，将 PNG/WebP 用于静态图像而不是 GIF，以节省网络字节。</li>
</ul>
</li>
<li>
<p>参考：<a href="https://web.dev/efficient-animated-content/" target="_blank" rel="noopener noreferrer">为动画内容使用视频格式</a></p>
</li>
</ul>
<h4 id="调整图片大小"> 调整图片大小</h4>
<p>Lighthouse 报告的“机会”部分列出了页面中尺寸不合适的所有图像，以及以<a href="https://en.wikipedia.org/wiki/Kibibyte" target="_blank" rel="noopener noreferrer">千字节 (KiB)</a>为单位的潜在节省。调整这些图像的大小以保存数据并缩短页面加载时间：</p>
<p><img src="./../images/chrome-图片大小.png" alt="" /></p>
<ul>
<li><a href="https://web.dev/uses-responsive-images/#how-lighthouse-calculates-oversized-images" target="_blank" rel="noopener noreferrer">Lighthouse 如何计算超大图像</a>
<ul>
<li>对于页面上的每个图像，Lighthouse 会将渲染图像的大小与实际图像的大小进行比较。渲染大小还考虑了设备像素比。如果渲染的大小至少比实际大小小 4KiB，则图像未通过审核。</li>
</ul>
</li>
</ul>
<h5 id="图像-cdn"> <a href="https://web.dev/image-cdns/" target="_blank" rel="noopener noreferrer">图像 CDN</a></h5>
<ul>
<li>将图像 CDN 视为用于转换图像的 Web 服务 API</li>
</ul>
<h5 id="响应式图片"> 响应式图片</h5>
<ul>
<li>响应式图片的优点是浏览器能够根据屏幕大小自动加载合适的图片。
<ul>
<li>
<p>通过 <code>picture</code> 实现</p>
<div><pre><code><span><span><span>&lt;</span>picture</span><span>></span></span>
    <span><span><span>&lt;</span>source</span> <span>srcset</span><span><span>=</span><span>"</span>banner_w1000.jpg<span>"</span></span> <span>media</span><span><span>=</span><span>"</span>(min-width: 801px)<span>"</span></span><span>></span></span>
    <span><span><span>&lt;</span>source</span> <span>srcset</span><span><span>=</span><span>"</span>banner_w800.jpg<span>"</span></span> <span>media</span><span><span>=</span><span>"</span>(max-width: 800px)<span>"</span></span><span>></span></span>
    <span><span><span>&lt;</span>img</span> <span>src</span><span><span>=</span><span>"</span>banner_w800.jpg<span>"</span></span> <span>alt</span><span><span>=</span><span>"</span><span>"</span></span><span>></span></span>
<span><span><span>&lt;/</span>picture</span><span>></span></span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div></li>
<li>
<p>通过 <code>@media</code> 实现</p>
<div><pre><code><span><span>@media</span> <span>(</span><span>min-width</span><span>:</span> 769px<span>)</span></span> <span>{</span>
    <span>.bg</span> <span>{</span>
        <span>background-image</span><span>:</span> <span><span>url</span><span>(</span>bg1080.jpg<span>)</span></span><span>;</span>
    <span>}</span>
<span>}</span>
<span><span>@media</span> <span>(</span><span>max-width</span><span>:</span> 768px<span>)</span></span> <span>{</span>
    <span>.bg</span> <span>{</span>
        <span>background-image</span><span>:</span> <span><span>url</span><span>(</span>bg768.jpg<span>)</span></span><span>;</span>
    <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div></li>
<li>
<p>参阅提供<a href="https://web.dev/serve-responsive-images" target="_blank" rel="noopener noreferrer">响应式图像</a>以了解更多信息。</p>
</li>
</ul>
</li>
</ul>
<h5 id="图片压缩"> 图片压缩</h5>
<ul>
<li>
<p>例如 JPG 格式的图片，100% 的质量和 90% 质量的通常看不出来区别，尤其是用来当背景图的时候。我经常用 PS 切背景图时， 将图片切成 JPG 格式，并且将它压缩到 60% 的质量，基本上看不出来区别。</p>
</li>
<li>
<p>压缩方法有两种：</p>
<ul>
<li>一是通过 webpack 插件 <code>image-webpack-loader</code></li>
<li>二是通过在线网站进行压缩。
<ul>
<li>tinypng</li>
</ul>
</li>
</ul>
</li>
</ul>
<h5 id="使用基于矢量的图像格式"> 使用基于矢量的图像格式</h5>
<ul>
<li>使用基于矢量的图像格式，如 SVG。使用有限数量的代码，SVG 图像可以缩放到任何大小。请参阅<a href="https://developers.google.com/web/fundamentals/design-and-ux/responsive/images#replace_complex_icons_with_svg" target="_blank" rel="noopener noreferrer">用 SVG 替换复杂图标</a>以了解更多信息。</li>
</ul>
<h4 id="使用-webp-图像"> <a href="https://web.dev/serve-images-webp" target="_blank" rel="noopener noreferrer">使用 WebP 图像</a></h4>
<ul>
<li>WebP 的优势体现在它具有更优的图像数据压缩算法，能带来更小的图片体积，而且拥有肉眼识别无差异的图像质量；同时具备了无损和有损的压缩模式、Alpha 透明以及动画的特性，在 JPEG 和 PNG 上的转化效果都相当优秀、稳定和统一。</li>
<li>AVIF 和 WebP 是图像格式，与较旧的 JPEG 和 PNG 对应物相比，它们具有出色的压缩和质量特性。以这些格式而不是 JPEG 或 PNG 对您的图像进行编码意味着它们将加载得更快并消耗更少的蜂窝数据。</li>
</ul>
<p><strong>参考资料：</strong></p>
<p><a href="https://www.zhihu.com/question/27201061" target="_blank" rel="noopener noreferrer">WebP 相对于 PNG、JPG 有什么优势？</a></p>
<h3 id="js-编码优化"> JS 编码优化</h3>
<h4 id="减少-javascript-执行时间"> 减少 JavaScript 执行时间</h4>
<ul>
<li>
<p>当您的 JavaScript 需要很长时间来执行时，它会以多种方式降低您的页面性能：</p>
<ul>
<li>
<p><strong>网络成本</strong></p>
<p>更多的字节等于更长的下载时间。</p>
</li>
<li>
<p><strong>解析和编译成本</strong></p>
<p>JavaScript 在主线程上被解析和编译。当主线程繁忙时，页面无法响应用户输入。</p>
</li>
<li>
<p><strong>执行成本</strong></p>
<p>JavaScript 也在主线程上执行。如果您的页面在真正需要之前运行了大量代码，这也会延迟您的<a href="https://web.dev/interactive" target="_blank" rel="noopener noreferrer">Time To Interactive</a>，这是与用户如何看待您的页面速度相关的关键指标之一。</p>
</li>
<li>
<p><strong>内存成本</strong></p>
<p>如果您的 JavaScript 持有大量引用，则可能会消耗大量内存。页面在消耗大量内存时会出现卡顿或缓慢。内存泄漏会导致您的页面完全冻结。</p>
</li>
</ul>
</li>
<li>
<p><a href="https://web.dev/bootup-time/#how-to-speed-up-javascript-execution" target="_blank" rel="noopener noreferrer">如何加快 JavaScript 执行速度</a></p>
<ul>
<li><a href="https://web.dev/reduce-javascript-payloads-with-code-splitting" target="_blank" rel="noopener noreferrer">通过实现代码拆分，只发送用户需要的代码</a>。</li>
<li><a href="https://web.dev/reduce-network-payloads-using-text-compression" target="_blank" rel="noopener noreferrer">缩小和压缩您的代码</a>。</li>
<li><a href="https://web.dev/remove-unused-code" target="_blank" rel="noopener noreferrer">删除未使用的代码</a>。</li>
<li><a href="https://web.dev/apply-instant-loading-with-prpl" target="_blank" rel="noopener noreferrer">通过使用 PRPL 模式缓存您的代码来减少网络旅行</a>。</li>
</ul>
</li>
<li>
<p><a href="https://web.dev/bootup-time/#how-the-lighthouse-javascript-execution-time-audit-fails" target="_blank" rel="noopener noreferrer">Lighthouse 如何审计 JavaScript 执行时间</a></p>
<ul>
<li>当 JavaScript 执行时间超过 2 秒时，<a href="https://developers.google.com/web/tools/lighthouse/" target="_blank" rel="noopener noreferrer">Lighthouse 会</a>显示警告。当执行时间超过 3.5 秒时审计失败：</li>
</ul>
<p><img src="./../images/chrome-js.png" alt="Lighthouse Reduce JavaScript 执行时间审计截图" /></p>
</li>
</ul>
<h4 id="避免过大的-dom"> 避免过大的 DOM</h4>
<p>大型 DOM 树可以通过多种方式降低页面性能：</p>
<ul>
<li>
<p><strong>网络效率和负载性能</strong></p>
<p>大型 DOM 树通常包含许多在用户首次加载页面时不可见的节点，这会不必要地增加用户的数据成本并减慢加载时间。</p>
</li>
<li>
<p><strong>运行时性能</strong></p>
<p>当用户和脚本与您的页面交互时，浏览器必须不断<a href="https://developers.google.com/web/fundamentals/performance/rendering/reduce-the-scope-and-complexity-of-style-calculations?utm_source=lighthouse&amp;utm_medium=cli" target="_blank" rel="noopener noreferrer">重新计算节点的位置和样式</a>。大型 DOM 树与复杂的样式规则相结合会严重减慢渲染速度。</p>
</li>
<li>
<p><strong>内存性能</strong></p>
<p>如果您的 JavaScript 使用通用查询选择器，例如<code>document.querySelectorAll('li')</code>，您可能会在不知不觉中存储对大量节点的引用，这可能会压倒用户设备的内存容量。</p>
</li>
</ul>
<p><a href="https://web.dev/dom-size/#how-the-lighthouse-dom-size-audit-fails" target="_blank" rel="noopener noreferrer">Lighthouse 如何审查 DOM 大小</a></p>
<ul>
<li>
<p><a href="https://developers.google.com/web/tools/lighthouse/" target="_blank" rel="noopener noreferrer">Lighthouse</a>报告页面的总 DOM 元素、页面的最大 DOM 深度及其最大子元素：<img src="./../images/chrome-dom.png" alt="Lighthouse 的截图避免了过多的 DOM 大小审计" /></p>
<ul>
<li>Lighthouse 使用 DOM 树标记页面：
<ul>
<li>总共有 1,500 多个节点。</li>
<li>具有大于 32 个节点的深度。</li>
<li>有一个超过 60 个子节点的父节点。</li>
</ul>
</li>
</ul>
</li>
<li>
<p><a href="https://web.dev/dom-size/#how-to-optimize-the-dom-size" target="_blank" rel="noopener noreferrer">如何优化 DOM 大小</a></p>
<ul>
<li>
<p>一般来说，寻找方法只在需要时创建 DOM 节点，并在不再需要时销毁节点。</p>
</li>
<li>
<p>如果您当前正在传送大型 DOM 树，请尝试加载您的页面并手动注意显示哪些节点。也许您可以从最初加载的文档中删除未显示的节点，并仅在相关的用户交互（例如滚动或单击按钮）之后创建它们。</p>
</li>
<li>
<p>如果您在运行时创建 DOM 节点，<a href="https://developers.google.com/web/tools/chrome-devtools/javascript/breakpoints#dom" target="_blank" rel="noopener noreferrer">Subtree Modification DOM Change Breakpoints</a>可以帮助您确定节点何时被创建。</p>
</li>
<li>
<p>如果您无法避免使用大型 DOM 树，另一种提高渲染性能的方法是简化您的 CSS 选择器。有关更多信息，请参阅 Google 的<a href="https://developers.google.com/web/fundamentals/performance/rendering/reduce-the-scope-and-complexity-of-style-calculations" target="_blank" rel="noopener noreferrer">减少样式计算的范围和复杂性</a></p>
</li>
</ul>
</li>
</ul>
<h3 id="css-编码优化"> CSS 编码优化</h3>
<h4 id="压缩css"> 压缩CSS</h4>
<p>缩小 CSS 文件可以提高页面加载性能。CSS 文件通常比它们需要的要大。例如：</p>
<div><pre><code><span>/* Header background should match brand colors. */</span>
<span>h1</span> <span>{</span>  <span>background-color</span><span>:</span> #000000<span>;</span><span>}</span>
<span>h2</span> <span>{</span>  <span>background-color</span><span>:</span> #000000<span>;</span><span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><p>可以简化为：</p>
<div><pre><code><span>h1, h2</span> <span>{</span> <span>background-color</span><span>:</span> #000000<span>;</span> <span>}</span>
</code></pre>
<div><span>1</span><br></div></div><p>从浏览器的角度来看，这两个代码示例在功能上是等效的，但第二个示例使用的字节更少。Minifiers 可以通过删除空格进一步提高字节效率：</p>
<div><pre><code><span>h1,h2</span><span>{</span><span>background-color</span><span>:</span>#000000<span>;</span><span>}</span>
</code></pre>
<div><span>1</span><br></div></div><p>一些压缩器使用巧妙的技巧来最小化字节。例如，颜色值<code>#000000</code>可以进一步减少到<code>#000</code>，这是它的速记等价物。</p>
<p>Lighthouse 根据它在您的 CSS 中找到的注释和空白字符来估计潜在的节省。这是一个保守的估计。如前所述，缩小器可以执行巧妙的优化（例如减少<code>#000000</code>到<code>#000</code>）以进一步减小文件大小。因此，如果您使用压缩器，您可能会看到比 Lighthouse 报告的更多的节省。</p>
<h3 id="其他编码优化"> 其他编码优化</h3>
<h4 id="优化-js-第三方库引入"> 优化 JS 第三方库引入</h4>
<ul>
<li>
<p>要将广告网络、社交媒体按钮、A/B 测试或分析服务添加到您的页面，您通常需要将第三方脚本添加到您的 HTML 中。这些<a href="https://web.dev/third-party-javascript/" target="_blank" rel="noopener noreferrer">第三方脚本会显着影响您的页面加载性能</a>。</p>
</li>
<li>
<p><a href="https://web.dev/third-party-summary/#how-the-lighthouse-audit-for-third-party-code-fails" target="_blank" rel="noopener noreferrer">第三方代码的 Lighthouse 审计如何失败</a></p>
<ul>
<li>Lighthouse 标记具有阻止<a href="https://developer.mozilla.org/en-US/docs/Glossary/Main_thread" target="_blank" rel="noopener noreferrer">主线程</a>250 毫秒或更长时间的第三方代码的页面：<img src="./../images/chrome-第三方库.png" alt="Lighthouse 截图 减少第三方代码审计的影响" /></li>
<li>第三方脚本是托管在与您使用 Lighthouse 审核的 URL 域不同的域上的任何脚本。随着页面加载，Lighthouse 计算每个第三方脚本阻塞主线程的时间。如果总阻塞时间大于 250 毫秒，则审计失败。</li>
</ul>
</li>
<li>
<p>优化第三方引入脚本方式：</p>
<ul>
<li>在标签上使用<code>async</code> 或<code>defer</code>属性<code>&lt;script&gt;</code></li>
<li>使用预链接和预加载</li>
<li>懒加载</li>
<li>第三方 CDN 托管
<ul>
<li>第三方供应商通常会提供他们托管的 JavaScript 文件的 URL，通常位于<a href="https://en.wikipedia.org/wiki/Content_delivery_network" target="_blank" rel="noopener noreferrer">内容交付网络 (CDN) 上</a>。这种方法的好处是您可以快速开始——只需复制和粘贴 URL——并且没有维护开销。第三方供应商处理服务器配置和脚本更新。</li>
<li>但是因为它们与您的其他资源不在同一来源，所以从公共 CDN 加载文件会产生网络成本。浏览器需要执行 DNS 查找，建立新的 HTTP 连接，并在安全来源上与供应商的服务器执行 SSL 握手。</li>
<li>当使用来自第三方服务器的文件时，您几乎无法控制缓存。依赖其他人的缓存策略可能会导致不必要地频繁从网络重新获取脚本</li>
</ul>
</li>
<li>自托管第三方脚本：自托管第三方脚本是一个选项，可让您更好地控制脚本的加载过程。
<ul>
<li>优点：
<ul>
<li>减少 DNS 查找和往返时间。</li>
<li>改进<a href="https://developers.google.com/web/fundamentals/performance/optimizing-content-efficiency/http-caching" target="_blank" rel="noopener noreferrer">HTTP 缓存</a>标头。</li>
<li>利用<a href="https://developers.google.com/web/fundamentals/performance/http2/" target="_blank" rel="noopener noreferrer">HTTP/2 服务器推送</a>。</li>
</ul>
</li>
<li>缺点：脚本可能会过时，并且在 API 更改或安全修复时不会自动更新。</li>
</ul>
</li>
<li>使用 service worker 缓存来自第三方服务器的脚本</li>
</ul>
</li>
</ul>
<p><strong>参考资料</strong></p>
<p><a href="https://web.dev/third-party-summary/" target="_blank" rel="noopener noreferrer">Reduce the impact of third-party code</a></p>
<h4 id="beacon-数据上报"> Beacon  数据上报</h4>
<ul>
<li>
<p>我们经常需要尝试在卸载（unload）文档之前向 Web 服务上报性能数据。过早的发送数据可能导致错过收集数据的机会。但对于开发者来说保证在文档卸载期间发送数据一直是一个困难，因为用户代理通常会忽略在 unload 事件处理器中产生的异步 XMLHttpRequest。</p>
</li>
<li>
<p>为了解决这个问题，通常要在 unload 或者 beforeunload 事件处理器中发起一个同步 XMLHttpRequest 来发送数据。同步的 XMLHttpRequest 迫使用户代理延迟卸载文档，使得下一个导航出现的更晚，而下一个页面对于这种较差的载入表现无能为力。</p>
</li>
<li>
<p>使用 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Beacon_API" target="_blank" rel="noopener noreferrer">navigator.sendBeacon()</a> 方法会使用户代理在有机会时异步地向服务器发送数据，同时不会延迟页面的卸载或影响下一导航的载入性能。</p>
<div><pre><code>window<span>.</span><span>addEventListener</span><span>(</span><span>'unload'</span><span>,</span> logData<span>,</span> <span>false</span><span>)</span><span>;</span>

<span>function</span> <span>logData</span><span>(</span><span>)</span> <span>{</span>
    navigator<span>.</span><span>sendBeacon</span><span>(</span><span>"/log"</span><span>,</span> analyticsData<span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div></li>
<li>
<p>考虑到此方法的兼容性，应该在浏览器不支持 navigator.sendBeacon 时使用其他方法发送数，例如同步的 XMLHttpRequest。</p>
</li>
</ul>
<p><strong>参考资料</strong></p>
<p><a href="https://web.dev/fast/" target="_blank" rel="noopener noreferrer">Fast load times</a></p>
<p><a href="https://juejin.cn/post/6844904153869713416#heading-15" target="_blank" rel="noopener noreferrer">前端性能优化指南</a></p>
<p><a href="https://zhuanlan.zhihu.com/p/121056616" target="_blank" rel="noopener noreferrer">前端性能优化 24 条建议</a></p>
<h2 id="性能优化实践"> 性能优化实践</h2>
<p><strong>参考资料</strong></p>
<p><a href="https://mp.weixin.qq.com/s/_7Mk39yWEkxOrnyYU_wvpQ" target="_blank" rel="noopener noreferrer">从页面加载到数据请求，前端页面性能优化实践分享</a></p>
<p><a href="https://mp.weixin.qq.com/s/YqHHbsxg8SbbtrY8gwhS9w" target="_blank" rel="noopener noreferrer">从实际项目入手，如何监测性能问题、如何解决</a></p>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-07-02T09:19:06.000Z</published>
  </entry>
  <entry>
    <title type="html">Node</title>
    <id>https://sewar-x.github.io/node/</id>
    <link href="https://sewar-x.github.io/node/"/>
    <updated>2021-06-26T11:10:03.000Z</updated>
    <content type="html"><![CDATA[<h2 id="node"> Node</h2>
<p>I am trying to summarize...</p>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-26T11:10:03.000Z</published>
  </entry>
  <entry>
    <title type="html">JavaScript 基础</title>
    <id>https://sewar-x.github.io/javascript/</id>
    <link href="https://sewar-x.github.io/javascript/"/>
    <updated>2021-07-15T12:37:23.000Z</updated>
    <content type="html"><![CDATA[<h2 id="javascript-基础"> JavaScript 基础</h2>
<h3 id="es6新特性"> ES6新特性</h3>
<ol>
<li>let const</li>
<li>字符串、数组、对象的方法扩展</li>
<li>symbol、set、map新的数据类型和数据结构</li>
<li>proxy代理拦截</li>
<li>异步解决方案：promise、generate，async、await</li>
<li>class类</li>
<li>module模块</li>
</ol>
<p>详情可参考：<a href="https://es6.ruanyifeng.com/" target="_blank" rel="noopener noreferrer">ES6 入门教程 - 阮一峰</a></p>
<h3 id="严格模式的限制"> 严格模式的限制</h3>
<ol>
<li>变量必须声明后再使用</li>
<li>函数的参数不能有同名属性，否则报错</li>
<li>不能使用with语句</li>
<li>不能对只读属性赋值，否则报错</li>
<li>不能使用前缀0表示八进制数，否则报错</li>
<li>不能删除不可删除的属性，否则报错</li>
<li>不能删除变量delete prop，会报错，只能删除属性delete global[prop]</li>
<li>eval不会在它的外层作用域引入变量</li>
<li>eval和arguments不能被重新赋值</li>
<li>arguments不会自动反映函数参数的变化</li>
<li>不能使用arguments.callee</li>
<li>不能使用arguments.caller</li>
<li>禁止this指向全局对象</li>
<li>不能使用fn.caller和fn.arguments获取函数调用的堆栈</li>
<li>增加了保留字（比如protected、static和interface）</li>
</ol>
<h3 id="script-标签加载脚本"> Script 标签加载脚本</h3>
<p><img src="./../images/script3.png" alt="" /></p>
<p><img src="./../images/script2.png" alt="script2" /></p>
<p><img src="./../images/script1.png" alt="script1" /></p>
<h3 id="js-延迟加载方式"> JS 延迟加载方式</h3>
<p>js延迟加载，也就是等页面加载完成之后再加载JavaScript文件。js延迟加载有助于提高页面加载速度</p>
<p>一般有以下几种方式：</p>
<ul>
<li>defer属性</li>
<li>async属性</li>
<li>动态创建DOM方式：对文档的加载事件进行监听，当文档加载完成后再动态的创建script标签来引入js脚本</li>
<li>使用setTimeout延迟方法</li>
<li>让JS最后加载</li>
</ul>
<h3 id="内存分类"> 内存分类</h3>
<p><img src="./../images/neicunfenlei.png" alt="" /></p>
<h3 id="数据类型"> 数据类型</h3>
<ul>
<li>基本类型：Number、Boolean、String、null、undefined、symbol（ES6 新增的）， BigInt（ES2020）</li>
<li>引用类型：Object</li>
<li>基本包装类型（Array，Function）</li>
</ul>
<p><img src="./../images/data-type.png" alt="" /></p>
<p><strong>基本数据类型：</strong></p>
<p><img src="./../images/jibenshujuleixing1.png" alt="" /></p>
<p><img src="./../images/jibenshujuleixing2.png" alt="jibenshujuleixing2" /></p>
<p><img src="./../images/jibenshujuleixing3.png" alt="jibenshujuleixing1" /></p>
<p><strong>引用类型：</strong></p>
<p><img src="./../images/yingyongleixing1.png" alt="" /></p>
<p><img src="./../images/yingyongleixing2.png" alt="yingyongleixing1" /></p>
<p><strong>包装类型:</strong></p>
<p><img src="./../images/baozhuangleixing1.png" alt="" /></p>
<p><img src="./../images/baozhuangleixing2.png" alt="baozhuangleixing1" /></p>
<h3 id="undefined-null"> undefined &amp; null</h3>
<blockquote>
<p><strong>undefined与undeclared区别?</strong></p>
</blockquote>
<ul>
<li>已在作用域中声明但还没有赋值的变量，是undefined的。</li>
<li>还没有在作用域中声明过的变量，是undeclared的。</li>
<li>对于undeclared变量的引用，浏览器会报引用错误，如ReferenceError:bisnotdefined。但是我们可以使用typeof的安全防范机制来避免报错，因为对于undeclared（或者notdefined）变量，typeof会返回&quot;undefined&quot;。</li>
</ul>
<blockquote>
<p><strong>null和undefined区别?</strong></p>
</blockquote>
<ul>
<li>Undefined和Null都是基本数据类型，这两个基本数据类型分别都只有一个值，就是undefined和null。</li>
<li>undefined代表的含义是未定义，null代表的含义是空对象。</li>
<li>一般变量声明了但还没有定义的时候会返回undefined，null主要用于赋值给一些可能会返回对象的变量，作为初始化。</li>
<li>undefined在js中不是一个保留字，这意味着我们可以使用undefined来作为一个变量名，这样的做法是非常危险的，它会影响我们对undefined值的判断。但是我们可以通过一些方法获得安全的undefined值，比如说void0。</li>
<li>当我们对两种类型使用typeof进行判断的时候，Null类型化会返回“object”，这是一个历史遗留的问题。</li>
<li>当我们使用双等号对两种类型的值进行比较时会返回true，使用三个等号时会返回false</li>
</ul>
<blockquote>
<p><strong>如何获取安全的undefined值?</strong></p>
</blockquote>
<ul>
<li>用 void 0 来获得 undefined</li>
<li>因为undefined是一个标识符，所以可以被当作变量来使用和赋值，但是这样会影响undefined的正常判断。表达式void___没有返回值，因此返回结果是undefined。void并不改变表达式的结果，只是让表达式不返回值。</li>
</ul>
<h3 id="nan"> NaN</h3>
<blockquote>
<p><strong>NaN 是什么，用 typeof 会输出什么？</strong></p>
</blockquote>
<ul>
<li>
<p>Not a Number，表示非数字，NaN意指“不是一个数字”（notanumber），NaN是一个“警戒值”（sentinelvalue，有特殊用途的常规值），用于指出数字类型中的错误情况，即“执行数学运算没有成功，这是失败后返回的结果”。</p>
</li>
<li>
<p>typeof NaN === 'number'</p>
</li>
<li>
<p>NaN是一个特殊值，它和自身不相等，是唯一一个非自反（自反，reflexive，即x===x不成立）的值。而NaN!=NaN为true。</p>
</li>
</ul>
<blockquote>
<p><strong>isNaN() 和 Number.isNaN() 函数的区别?</strong></p>
</blockquote>
<ul>
<li>函数 isNaN() 接收参数后，会尝试将这个参数转换为数值，任何不能被转换为数值的的值都会返回true，因此非数字值传入也会返回true，会影响NaN的判断</li>
<li>函数 Number.isNaN() 会首先判断传入参数是否为数字，如果是数字再继续判断是否为NaN，这种方法对于NaN的判断更为准确</li>
<li>总结： isNaN() 判断不准确，非数字返回 true；Number.isNaN() 判断更为准确</li>
</ul>
<h3 id="精度丢失"> 精度丢失</h3>
<ul>
<li>解释一：</li>
</ul>
<p><img src="./../images/jingduduishi.png" alt="" /></p>
<ul>
<li>
<p>解释二：</p>
<p>JavaScirpt 使用 Number 类型来表示数字（整数或浮点数），遵循 IEEE 754 标准，通过 64 位来表示一 个数字（1 + 11 + 52）</p>
<ul>
<li>1 符号位，0 表示正数，1 表示负数 s</li>
<li>11 指数位（e）</li>
<li>52 尾数，小数部分（即有效数字）</li>
</ul>
<p>最大安全数字：Number.MAX_SAFE_INTEGER = Math.pow(2, 53) - 1，转换成整数就是 16 位，所以 0.1 === 0.1，是因为通过 toPrecision(16) 去有效位之后，两者是相等的。</p>
<p>在两数相加时，会先转换成二进制，0.1 和 0.2 转换成二进制的时候尾数会发生无限循环，然后进行对阶 运算，JS 引擎对二进制进行截断，所以造成精度丢失。</p>
<p>总结：<strong>精度丢失可能出现在进制转换和对阶运算中</strong></p>
</li>
</ul>
<h3 id="整数相关问题"> 整数相关问题</h3>
<blockquote>
<p><strong>JS整数是怎么表示的？</strong></p>
</blockquote>
<p>​	通过 Number 类型来表示，遵循 IEEE754 标准，通过 64 位来表示一个数字，（1 + 11 + 52），最大安 全数字是 Math.pow(2, 53) - 1，对于 16 位十进制。（符号位 + 指数位 + 小数部分有效位）</p>
<blockquote>
<p><strong>Number() 的存储空间是多大？如果后台发送了一个超过最大自己的数字怎么办</strong></p>
</blockquote>
<p>​	Math.pow(2, 53) ，53 为有效数字，会发生截断，等于 JS 能支持的最大数字</p>
<h3 id="显示转换-隐式转换"> 显示转换&amp;隐式转换</h3>
<p><strong>显示转换</strong></p>
<p>显示转换一般指使用Number、String和Boolean三个构造函数，手动将各种类型的值，转换成数字、字符串或者布尔值</p>
<p><strong>隐式转换</strong>：</p>
<p>运算符两边数据类型不一致，编译器自动转换数据类型</p>
<ul>
<li>转成string类型：+号两边如果有一边是字符串，则会把另一边转换成字符串，然后进行拼接</li>
<li>转成number类型：
<ul>
<li>以下几种运算符会将任何数据转换成number类型再运算，如果无法转换则为NaN
<ul>
<li>数学正号 ： +num</li>
<li>当 +/-号写在一个变量名的前面，此时表示数学的正/负数</li>
<li>自增自减（++ --）</li>
<li>算术运算符（+ - * / %）</li>
<li>比较运算符(&gt; &lt; &gt;= &lt;= == != === !==)
说明：全等与不全等会先比较值（此时会隐式转换再比较），然后再比较数据类型</li>
</ul>
</li>
</ul>
</li>
<li>转成boolean类型：逻辑非（！）会将任何数据转为boolean类型再运算</li>
</ul>
<h3 id="运算符规则"> 运算符规则</h3>
<p><strong>加号规则</strong></p>
<p><img src="./../images/cuozofu+.png" alt="" /></p>
<p><strong>减号规则</strong></p>
<p><img src="./../images/caozuofu-.png" alt="" /></p>
<p><strong>等号规则</strong></p>
<p><img src="./../images/cauozuofu=.png" alt="" /></p>
<p><strong>布尔操作符</strong></p>
<p><img src="./../images/booleancaozuo.png" alt="" /></p>
<h3 id="尾后逗号"> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Trailing_commas" target="_blank" rel="noopener noreferrer">尾后逗号</a></h3>
<p><strong>尾后逗号</strong> （有时叫做“终止逗号”）在向 JavaScript 代码添加元素、参数、属性时十分有用。如果你想要添加新的属性，并且上一行已经使用了尾后逗号，你可以仅仅添加新的一行，而不需要修改上一行。这使得版本控制的代码比较（diff）更加清晰，代码编辑过程中遇到的麻烦更少。</p>
<p>JavaScript 一开始就支持数组字面量中的尾后逗号，随后向对象字面量（ECMAScript 5）中添加了尾后逗号。最近（ECMAScript 2017），又将其添加到函数参数中</p>
<p>但是，<a href="https://developer.mozilla.org/zh-CN/docs/Glossary/JSON" target="_blank" rel="noopener noreferrer">JSON</a> 不允许尾后逗号。</p>
<blockquote>
<p>[,,,] 的长度是多少？</p>
</blockquote>
<div><pre><code><span>let</span> a <span>=</span> <span>[</span><span>,</span><span>,</span><span>,</span><span>]</span><span>;</span>
a<span>.</span>length <span>// 3</span>
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div><h3 id="symbol"> Symbol</h3>
<p><strong>Symbol 是什么？</strong></p>
<ul>
<li>
<p><code>Symbol</code>是ES6 引入的一种新的原始数据类型，表示独一无二的值。</p>
</li>
<li>
<p>Symbol 值通过<code>Symbol</code>函数生成。</p>
</li>
</ul>
<p><strong>Symbol 作用？</strong></p>
<ul>
<li>保证对象每个属性的名字都是独一无二，防止命名冲突；</li>
<li>提供遍历接口，布置了 symbol.iterator 的对象才可以使用 for···of 循环，可以统一处理数据结构。调用之后回返回一个遍历器对象，包含有一个 next 方法，使用 next 方法后有两个返回值 value 和 done 分别表示函数当前执行位置的值和是否遍历完毕。</li>
<li><a href="https://es6.ruanyifeng.com/#docs/symbol#%E5%AE%9E%E4%BE%8B%EF%BC%9A%E6%B6%88%E9%99%A4%E9%AD%94%E6%9C%AF%E5%AD%97%E7%AC%A6%E4%B8%B2" target="_blank" rel="noopener noreferrer">消除魔术字符串</a></li>
</ul>
<p><strong>Symbol注意点:</strong></p>
<ul>
<li>1.Symbol函数前不能使用new命令，否则会报错。</li>
<li>2.Symbol函数可以接受一个字符串作为参数，表示对Symbol实例的描述，主要是为了在控制台显示，或者转为字符串时，比较容易区分。</li>
<li>3.Symbol作为属性名，该属性不会出现在for...in、for...of循环中，也不会被Object.keys()、Object.getOwnPropertyNames()、JSON.stringify()返回。</li>
<li>4.Object.getOwnPropertySymbols方法返回一个数组，成员是当前对象的所有用作属性名的Symbol值。</li>
<li>5.Symbol.for接受一个字符串作为参数，然后搜索有没有以该参数作为名称的Symbol值。如果有，就返回这个Symbol值，否则就新建并返回一个以该字符串为名称的Symbol值。</li>
<li>6.Symbol.keyFor方法返回一个已登记的Symbol类型值的key</li>
</ul>
<p><strong>参考：</strong></p>
<p><a href="https://es6.ruanyifeng.com/#docs/symbol" target="_blank" rel="noopener noreferrer">ECMAScript 6 入门-Symbol</a></p>
<h3 id="typeof-操作符"> typeof 操作符</h3>
<p><img src="./../images/typeof.png" alt="" /></p>
<p><strong>内部属性[[Class]]:</strong></p>
<p>​	所有typeof返回值为&quot;object&quot;的对象（如数组）都包含一个内部属性[[Class]]（我们可以把它看作一个内部的分类，而非传统的面向对象意义上的类）。这个属性无法直接访问，一般通过Object.prototype.toString(..)来查看</p>
<div><pre><code><span>Object</span><span>.</span>prototype<span>.</span><span>toString</span><span>.</span><span>call</span><span>(</span><span>[</span><span>1</span><span>,</span><span>2</span><span>,</span><span>3</span><span>]</span><span>)</span><span>;</span>  <span>//"[objectArray]"</span>
<span>Object</span><span>.</span>prototype<span>.</span><span>toString</span><span>.</span><span>call</span><span>(</span><span><span>/</span><span>regex-literal</span><span>/</span><span>i</span></span><span>)</span><span>;</span>  <span>//"[objectRegExp]"</span>
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div><h3 id="instanceof-操作符"> instanceof 操作符</h3>
<p><img src="./../images/instanceof-mind.png" alt="" /></p>
<h3 id="数据类型判断方法"> 数据类型判断方法</h3>
<ul>
<li>
<p>判断数据类型方法</p>
<ul>
<li>
<p>typeof 操作符</p>
</li>
<li>
<p>Object.prototype.toString.call()</p>
<p><img src="./../images/toString.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
<h3 id="数组创建和检测"> 数组创建和检测</h3>
<p><img src="./../images/array.png" alt="" /></p>
<h3 id="数组方法"> 数组方法</h3>
<p><img src="./../images/array5.png" alt="" /></p>
<p><img src="./../images/array3.png" alt="array3" /></p>
<p><img src="./../images/array2.png" alt="array2" /></p>
<p><img src="./../images/array1.png" alt="array1" /></p>
<h3 id="数组迭代"> 数组迭代</h3>
<p><img src="./../images/array4.png" alt="" /></p>
<blockquote>
<p><strong>indexOf 和 findIndex 的区别?</strong></p>
</blockquote>
<ul>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/findIndex" target="_blank" rel="noopener noreferrer">indexOf</a> ：查找值作为第一个参数，采用 <code>===</code> 比较，更多的是用于查找基本类型，如果是对象类型，则是判断是否是同一个对象的引用</li>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/findIndex" target="_blank" rel="noopener noreferrer">findIndex</a> ：比较函数作为第一个参数，多用于非基本类型(例如对象)的数组索引查找，或查找条件很复杂</li>
</ul>
<h3 id="for-in-与-for-of-比较"> <a href="https://es6.ruanyifeng.com/#docs/iterator#%E4%B8%8E%E5%85%B6%E4%BB%96%E9%81%8D%E5%8E%86%E8%AF%AD%E6%B3%95%E7%9A%84%E6%AF%94%E8%BE%83" target="_blank" rel="noopener noreferrer">for...in 与 for...of 比较</a></h3>
<p><strong><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Statements/for...in" target="_blank" rel="noopener noreferrer">for...in 循环</a></strong>：</p>
<ul>
<li><code>for...in</code>循环主要是为遍历对象而设计的，不适用于遍历数组；</li>
<li>缺点：
<ul>
<li>数组的键名是数字，但是<code>for...in</code>循环是以字符串作为键名“0”、“1”、“2”等等。</li>
<li><code>for...in</code>循环不仅遍历数字键名，还会遍历手动添加的其他键，甚至包括原型链上的键。</li>
<li>某些情况下，<code>for...in</code>循环会以任意顺序遍历键名</li>
</ul>
</li>
</ul>
<p><strong><a href="https://es6.ruanyifeng.com/#docs/iterator#for---of-%E5%BE%AA%E7%8E%AF" target="_blank" rel="noopener noreferrer">for...of 循环</a></strong>：</p>
<ul>
<li>一个数据结构只要部署了<code>Symbol.iterator</code>属性，就被视为具有 iterator 接口，就可以用<code>for...of</code>循环遍历它的成员；</li>
<li><code>for...of</code>循环内部调用的是数据结构的<code>Symbol.iterator</code>方法；</li>
<li><code>for...of</code>循环可以使用的范围包括数组、Set 和 Map 结构、某些类似数组的对象（比如<code>arguments</code>对象、DOM NodeList 对象）、 Generator 对象，字符串.</li>
<li>优点：
<ul>
<li>有着同<code>for...in</code>一样的简洁语法，但是没有<code>for...in</code>那些缺点。</li>
<li>不同于<code>forEach</code>方法，它可以与<code>break</code>、<code>continue</code>和<code>return</code>配合使用。</li>
<li>提供了遍历所有数据结构的统一操作接口</li>
</ul>
</li>
</ul>
<h3 id="类数组"> 类数组</h3>
<p><strong>类数组概念:</strong></p>
<ul>
<li>JavaScript中，数组是一个特殊的对象，其 property 名为正整数，且其l ength 属性会随着数组成员的增减而发生变化，同时又从 Array 构造函数中继承了一些用于进行数组操作的方法。</li>
<li>而对于一个普通的对象来说，如果它的所有 property 名均为正整数，同时也有相应的 <em>length</em> 属性，那么虽然该对象并不是由Array构造函数所创建的，它依然呈现出数组的行为，在这种情况下，这些对象被称为　“类数组对象”　。</li>
<li>总结： 拥有<code>length</code>属性的对象</li>
</ul>
<p><strong>类数组包含:</strong></p>
<ul>
<li>DOM方法的返回结果： document.getElementsByTagName() 返回的就是一个类数组对象</li>
<li>函数 arguments 对象</li>
<li>函数也可以被看作是类数组对象，因为它含有length属性值，代表可接收的参数个数</li>
</ul>
<p><strong>类数组转数组的方法：</strong></p>
<ul>
<li>扩展运算符 ...</li>
<li>Array.from(arrayLike)</li>
<li>Array.prototype.slice.apply(arguments)</li>
<li>Array.prototype.splice.call(arrayLike,0)</li>
<li>Array.prototype.concat.apply([],arrayLike)</li>
</ul>
<p><strong>参考资料：</strong></p>
<p><a href="https://github.com/mqyqingfeng/Blog/issues/14" target="_blank" rel="noopener noreferrer">JavaScript深入之类数组对象与arguments</a></p>
<h3 id="对象访问"> 对象访问</h3>
<p><img src="./../images/object.png" alt="" /></p>
<h3 id="对象方法"> 对象方法</h3>
<p><img src="./../images/object2.png" alt="" /></p>
<p><img src="./../images/object6.png" alt="" /></p>
<h3 id="对象属性"> 对象属性</h3>
<ul>
<li>对象属性包括
<ul>
<li>数据属性</li>
<li>访问器属性</li>
</ul>
</li>
</ul>
<p><img src="./../images/object3.png" alt="" /></p>
<p><img src="./../images/object4.png" alt="" /></p>
<h3 id="对象属性方法"> 对象属性方法</h3>
<p><img src="./../images/object5.png" alt="" /></p>
<h3 id="防篡改对象"> 防篡改对象</h3>
<p><img src="./../images/object7.png" alt="" /></p>
<h3 id="假值对象"> 假值对象</h3>
<ul>
<li>浏览器在某些特定情况下，在常规JavaScript语法基础上自己创建了一些外来值，这些就是“假值对象”。</li>
<li>假值对象看起来和普通对象并无二致（都有属性，等等），但将它们强制类型转换为布尔值时结果为false最常见的例子是document.all，它是一个类数组对象，包含了页面上的所有元素，由DOM（而不是JavaScript引擎）提供给JavaScript程序使用</li>
</ul>
<h3 id="内置对象"> 内置对象</h3>
<p>全局的对象（globalobjects）或称标准内置对象，不要和&quot;全局对象（globalobject）&quot;混淆。</p>
<p>这里说的全局的对象是说在全局作用域里的对象。</p>
<p>全局作用域中的其他对象可以由用户的脚本创建或由宿主程序提供。</p>
<p>标准内置对象的分类：</p>
<ul>
<li>（1）值属性。这些全局属性返回一个简单值，这些值没有自己的属性和方法。例如Infinity、NaN、undefined、null字面量</li>
<li>（2）函数属性。全局函数可以直接调用，不需要在调用时指定所属对象，执行结束后会将结果直接返回给调用者。例如eval()、parseFloat()、parseInt()等</li>
<li>（3）基本对象。基本对象是定义或使用其他对象的基础。基本对象包括一般对象、函数对象和错误对象。例如Object、Function、Boolean、Symbol、Error等</li>
<li>（4）数字和日期对象。用来表示数字、日期和执行数学计算的对象。例如Number、Math、Date</li>
<li>（5）字符串。用来表示和操作字符串的对象。例如String、RegExp</li>
<li>（6）可索引的集合对象。这些对象表示按照索引值来排序的数据集合，包括数组和类型数组，以及类数组结构的对象。例如Array</li>
<li>（7）使用键的集合对象。这些集合对象在存储数据时会使用到键，支持按照插入顺序来迭代元素。例如Map、Set、WeakMap、WeakSet</li>
<li>（8）矢量集合。SIMD矢量集合中的数据会被组织为一个数据序列。例如SIMD等</li>
<li>（9）结构化数据。这些对象用来表示和操作结构化的缓冲区数据，或使用JSON编码的数据。例如JSON等</li>
<li>（10）控制抽象对象。例如Promise、Generator等</li>
<li>（11）反射。例如Reflect、Proxy</li>
<li>（12）国际化，为了支持多语言处理而加入ECMAScript的对象。例如Intl、Intl.Collator等</li>
<li>（13）WebAssembly（</li>
<li>14）其他例如arguments</li>
</ul>
<p><strong>总结</strong>：</p>
<ul>
<li>内置对象主要指的是在程序执行前存在全局作用域里的由js定义的一些全局值属性、函数和用来实例化其他对象的构造函数对象。</li>
<li>一般我们经常用到的如全局变量值NaN、undefined，全局函数如parseInt()、parseFloat()用来实例化对象的构造函数如Date、Object等，还有提供数学计算的单体内置对象如Math对象。</li>
</ul>
<p>详细资料可以参考：<a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects" target="_blank" rel="noopener noreferrer">《标准内置对象的分类》</a><a href="https://segmentfault.com/a/1190000011467723" target="_blank" rel="noopener noreferrer">《JS所有内置对象属性和方法汇总》</a></p>
<h3 id="new操作符"> new操作符</h3>
<blockquote>
<p>new操作符具体干了什么?</p>
</blockquote>
<ul>
<li>创建一个空对象</li>
<li>继承了该函数的原型</li>
<li>属性和方法被加入到 this 引用的对象中</li>
<li>新创建的对象由 this 所引用，并且最后隐式的返回 this</li>
</ul>
<p><img src="./../images/new-demo.png" alt="" /></p>
<h3 id="深浅拷贝"> 深浅拷贝</h3>
<p><img src="./../images/copy.png" alt="" /></p>
<p><strong>浅拷贝</strong>：仅拷贝第一层</p>
<ul>
<li><code>Object.assign</code>，</li>
<li>展开运算符 <code>...</code></li>
</ul>
<p><strong>深拷贝</strong>：</p>
<p><code>JSON.parse(JSON.stringify(object))</code> <strong>缺陷</strong>：</p>
<ul>
<li>
<p>使用<code>JSON.parse(JSON.stringify(object))</code>拷贝对象存在以下缺陷：</p>
<ul>
<li>
<p>函数、undefined 丢失；</p>
</li>
<li>
<p>正则表达式、Error对象 只得到空对象；</p>
</li>
<li>
<p>NaN、Infinity、-Infinity 会变成null;</p>
</li>
<li>
<p>会忽略 symbol;</p>
</li>
<li>
<p>时间对象 <code>Date()</code>，拷贝后时间将是字符串的形式，而不是对象的形式;</p>
<p><img src="./../images/test-json.parse.png" alt="" /></p>
</li>
<li>
<p>JSON.stringify()只能拷贝对象的可枚举的自有属性。</p>
<ul>
<li>
<p>如果对象中的对象是有构造函数生成的， 则使用JSON.parse(JSON.stringify(obj))深拷贝后，会丢弃对象的constructor；</p>
<p><img src="./../images/test-json.parse2.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="set-集合"> Set 集合</h3>
<p><img src="./../images/set.png" alt="set" /></p>
<h3 id="weakset"> WeakSet</h3>
<p><img src="./../images/weakset.png" alt="weakset" /></p>
<h3 id="map-集合"> Map 集合</h3>
<p><img src="./../images/map.png" alt="map" /></p>
<h3 id="set、map、object-比较"> Set、Map、Object 比较</h3>
<p><img src="./../images/weakmap.png" alt="" /></p>
<blockquote>
<p><strong>为什么要引入：<a href="http://es6.ruanyifeng.com/#docs/set-map#WeakSet" target="_blank" rel="noopener noreferrer">WeakSet</a> 和 <a href="http://es6.ruanyifeng.com/#docs/set-map#WeakMap" target="_blank" rel="noopener noreferrer">WeakMap</a>？</strong></p>
</blockquote>
<ul>
<li>js 引擎会对未使用的引用需要进行垃圾回收，需要及时清除未使用的引用。但有时候不容易记住所有未使用的引用，所以容易导致内存泄漏。</li>
<li>因此，引入一种方法，在新建引用的时候就声明，哪些引用必须手动清除，哪些引用可以忽略不计，当其他引用消失以后，垃圾回收机制就可以释放内存。这样就能大大减轻程序员的负担，你只要清除主要引用就可以了。</li>
<li>ES6 考虑到了这一点，推出了两种新的数据结构：<a href="http://es6.ruanyifeng.com/#docs/set-map#WeakSet" target="_blank" rel="noopener noreferrer">WeakSet</a> 和 <a href="http://es6.ruanyifeng.com/#docs/set-map#WeakMap" target="_blank" rel="noopener noreferrer">WeakMap</a>。它们对于值的引用都是不计入垃圾回收机制的，所以名字里面才会有一个&quot;Weak&quot;，表示这是弱引用。</li>
<li>基本上，如果你要往对象上添加数据，又不想干扰垃圾回收机制，就可以使用 WeakMap。</li>
</ul>
<h3 id="变量声明方式"> 变量声明方式</h3>
<p><img src="./../images/bianliangshengmin.png" alt="" /></p>
<p><img src="./../images/const.png" alt="" /></p>
<h3 id="变量提升和函数提升"> 变量提升和函数提升</h3>
<ul>
<li>
<p>提升：js代码执行前引擎会先进行预编译，预编译期间会将函数声明提升至其对应作用域的最顶端；</p>
</li>
<li>
<p>变量提升：变量的声明会被自动移到函数或者全局代码的最顶上；移动的仅仅是声明，变量的定义并不会随之提升；</p>
</li>
<li>
<p>函数提升：直接在函数声明之前调用该函数，并能成功执行它，而不会提升函数表达式；</p>
</li>
</ul>
<p><img src="./../images/bianliangtisheng.png" alt="" /></p>
<p><strong>变量提升相关题目</strong></p>
<ul>
<li>
<p>题目1：</p>
<div><pre><code><span>var</span> name <span>=</span> <span>"world"</span><span>;</span>
     <span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
     	<span>if</span> <span>(</span><span>typeof</span> name <span>==</span> <span>'undefined'</span><span>)</span> <span>{</span>
     		<span>var</span> name <span>=</span> <span>'yang'</span><span>;</span>
     		console<span>.</span><span>log</span><span>(</span><span>'Hello '</span> <span>+</span> name<span>)</span>
     	<span>}</span> <span>else</span> <span>{</span>
     		console<span>.</span><span>log</span><span>(</span><span>'Hello '</span> <span>+</span> name<span>)</span>
     	<span>}</span>
 <span>}</span><span>)</span><span>(</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div><ul>
<li>
<p>输出结果：Hello yang</p>
</li>
<li>
<p>代码相当于</p>
<div><pre><code><span>var</span> name <span>=</span> <span>"world"</span><span>;</span>
<span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
    <span>var</span> name<span>;</span>
    <span>if</span> <span>(</span><span>typeof</span> name <span>==</span> <span>'undefined'</span><span>)</span> <span>{</span>
        <span>var</span> name <span>=</span> <span>'yang'</span><span>;</span>
        console<span>.</span><span>log</span><span>(</span><span>'Hello '</span> <span>+</span> name<span>)</span>
    <span>}</span> <span>else</span> <span>{</span>
        console<span>.</span><span>log</span><span>(</span><span>'Hello '</span> <span>+</span> name<span>)</span>
    <span>}</span>
<span>}</span><span>)</span><span>(</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div></li>
</ul>
</li>
</ul>
<h3 id="this-和-this-绑定"> this 和 this 绑定</h3>
<ul>
<li>this 绑定主要有：
<ul>
<li>默认绑定</li>
<li>隐式绑定</li>
<li>显示绑定： call()、apply()、bind()</li>
<li>new 绑定</li>
<li>函数绑定</li>
</ul>
</li>
</ul>
<p><img src="./../images/this.png" alt="" /></p>
<p><img src="./../images/new-bingding.png" alt="" /></p>
<p><img src="./../images/hanshu-this.png" alt="" /></p>
<ul>
<li>this 绑定判断</li>
</ul>
<p><img src="./../images/this-panduan.png" alt="" /></p>
<ul>
<li>
<p>this 使用</p>
<p><img src="./../images/this-use.png" alt="" /></p>
</li>
</ul>
<p><strong>相关问题：</strong></p>
<blockquote>
<p><strong>如果一个构造函数，bind了一个对象，用这个构造函数创建出的实例会继承这个对象的属性吗？为什么?</strong></p>
</blockquote>
<p>​		不会继承，因为根据 this 绑定四大规则，new 绑定的优先级高于 bind 显示绑定，通过 new 进行构造函数调用时，会创建一个新对象，这个新对象会代替 bind 的对象绑定，作为此函数的 this，并且在此函数没有返回对象的情况下，返回这个新建的对象。</p>
<h3 id="执行环境-环境栈"> 执行环境&amp;环境栈</h3>
<p><strong>执行环境</strong></p>
<p><img src="./../images/zhixinghuanjing.png" alt="" /></p>
<p><strong>环境栈</strong></p>
<p><img src="./../images/huanjingzhan.png" alt="" /></p>
<h3 id="作用域-作用域链"> 作用域&amp;作用域链</h3>
<p><img src="./../images/zuoyonyu-zuoyongyulian.png" alt="" /></p>
<p><strong>作用域:</strong></p>
<ul>
<li>作用域是变量与函数的可访问范围，即作用域控制着变量与函数的可见性和生命周期。</li>
<li>作用域是一套规则，在当前作用域以及嵌套的子作用域中根据标识符名称进行变量查找。</li>
</ul>
<p><img src="./../images/zuoyongyu.png" alt="" /></p>
<p><strong>作用域链:</strong></p>
<ul>
<li>作用域链的作用是保证执行环境里有权访问的变量和函数是有序的</li>
<li>作用域链的变量只能向上访问，变量访问到window对象即被终止，作用域链向下访问变量是不被允许的。</li>
</ul>
<p><img src="./../images/zuoyongyulian.png" alt="" /></p>
<p><img src="./../images/yanchangzuoyongyu.png" alt="" /></p>
<p><strong>闭包相关题目</strong></p>
<ul>
<li>
<p>题目一：输出结果</p>
<div><pre><code><span>for</span><span>(</span><span>var</span> i<span>=</span><span>0</span><span>;</span> i<span>&lt;=</span><span>5</span><span>;</span> i<span>++</span><span>)</span> <span>{</span>
    <span>setTimeout</span><span>(</span><span>(</span><span>)</span><span>=></span><span>{</span>
        console<span>.</span><span>log</span><span>(</span>i<span>)</span>
    <span>}</span><span>,</span><span>500</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><ul>
<li>输出： 6,6,6,6,6,6</li>
<li>分析：该题目涉及 JS 时间循环机制 和 作用域 考察
<ul>
<li>JS 循环机制：
<ul>
<li>JS是单线程环境，也就是说代码的执行是从上到下，依次执行，这样的执行称为同步执行；遇到异步任务，会将异步任务添加到异步事件队列；等待异步任务执行完成，会将异步任务结果放入回调队列；等主线程所有任务执行完成，将执行回调队列中的事件。</li>
<li>在此代码中：
<ul>
<li>for循环是同步代码，而setTimeout中的是异步代码，JS碰到这个有同步和异步的情况下会先从上到下执行同步代码，碰到异步的代码会将其插入到任务队列当中等待</li>
<li>setTimeout是延时，也就是说碰到setTimeout这个异步的代码块会根据它里面的第二个参数：延时时间来将代码插入到任务队列当中，上面代码在1000ms之后将它插入到任务队列当中。</li>
<li>同步代码都执行完成之后，那么JS引擎就空闲了，这个时候就轮到任务队列中的异步代码依次加载了</li>
</ul>
</li>
</ul>
</li>
<li>作用域链：
<ul>
<li>作用域是变量等资源的作用范围</li>
<li>当同步代码执行完毕开始执行异步的setTimeout代码时，setTimeout中需要变量 i,而执行的时候在当前的作用域中开始找，找不到变量i的定义，这个时候就把创建这个函数的作用域作为当前作用域</li>
<li>再次寻找，创建这个函数的作用域就是全局作用域，也就是找到了for循环中i，找到了之后就结束寻找变量i的过程</li>
<li>由于这个时候的i是全局的，并且保存变量 6，因此所有定时任务都输出全局作用域的变量 i 的结果。</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="闭包"> 闭包</h3>
<p><img src="./../images/bibao.png" alt="" /></p>
<p><strong>相关问题</strong></p>
<blockquote>
<p><strong>什么是闭包？</strong></p>
</blockquote>
<ul>
<li>
<p>闭包是有权限访问其他函数作用域的局部变量的一个<strong>函数</strong></p>
</li>
<li>
<p>判断闭包：函数内部使用了该函数外部的变量，则该函数是闭包。</p>
<div><pre><code><span>function</span> <span>closureOuter</span><span>(</span><span>)</span><span>{</span>
  <span>let</span> outerVar <span>=</span> <span>'outer func var'</span>
  <span>return</span> <span>function</span><span>(</span><span>)</span><span>{</span> <span>// 闭包函数，引用了外部函数变量</span>
    <span>return</span> outerVar 
  <span>}</span>
<span>}</span>
<span>let</span> test <span>=</span> <span>closureOuter</span><span>(</span><span>)</span>  <span>// 使用test接受匿名函数</span>
name <span>=</span> <span>null</span> <span>// 释放引用内存</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div></li>
</ul>
<blockquote>
<p>为什么其他非闭包的函数，没有权限访问另一个函数的内部作用域?</p>
</blockquote>
<ul>
<li>在JS中，变量的作用域属于函数作用域，在函数执行后作用域就会被清理、内存也随之被收回;</li>
<li>闭包是建立在一个函数内部的子函数，由于其可访问上级作用域的原因，因此存在对上级作用域的引用计数（无法垃圾回收），即使上级函数执行完，作用域也不会随之销毁；</li>
<li>这时的子函数—也就是闭包，便拥有了访问上级作用域中的变量的权限，即使上级函数执行完后，作用域内的值也不会被销毁。</li>
</ul>
<blockquote>
<p><strong>闭包产生本质？</strong></p>
</blockquote>
<ul>
<li>当前函数定义了其他函数，当前环境中存在指向父级作用域的引用</li>
<li>本质上闭包就是将函数内部和函数外部连接起来的方式</li>
</ul>
<blockquote>
<p><strong>闭包解决了什么？</strong></p>
</blockquote>
<ul>
<li>
<p>作用域链的变量只能向上访问，变量访问到window对象即被终止，作用域链向下访问变量是不被允许的。闭包就是用于解决这个问题所产生的</p>
<p><img src="./../images/闭包作用域.png" alt="" /></p>
</li>
<li>
<p>由于闭包可以缓存下级作用域，那么就使得函数外部打破了“函数作用域”的束缚，可以访问函数内部的变量。</p>
</li>
</ul>
<blockquote>
<p><strong>闭包作用？</strong></p>
</blockquote>
<ul>
<li>可以读取函数内部的变量；</li>
<li>保存上级作用域，实现方法或属性的私有化；</li>
</ul>
<blockquote>
<p><strong>闭包特性？</strong></p>
</blockquote>
<ul>
<li>函数内部嵌套函数</li>
<li>函数内部可以引用外部的变量</li>
<li>参数和变量不会被垃圾回收机制回收</li>
</ul>
<blockquote>
<p><strong>如何产生闭包？</strong></p>
</blockquote>
<ul>
<li>函数中返回一个函数</li>
<li>函数当作参数传递</li>
<li>IIFE 执行函数</li>
<li>定时器 setTimeout 和自执行函数</li>
<li>所有的回调函数</li>
</ul>
<blockquote>
<p><strong>闭包的this指向</strong></p>
</blockquote>
<ul>
<li>闭包的 this 指向的是 window</li>
</ul>
<blockquote>
<p><strong>闭包应用场景</strong>？</p>
</blockquote>
<ul>
<li>
<p>回调函数</p>
</li>
<li>
<p>柯里化 bind</p>
</li>
<li>
<p>模块</p>
</li>
<li>
<p>创建私有变量</p>
</li>
<li>
<p>模仿块级作用域</p>
<ul>
<li>
<p>概念：JavaScript 没有块级作用域的概念。这意味着在块语句中定义的变量，实际上是在包含函数中而非语句中创建的</p>
</li>
<li>
<p>要点：</p>
<ul>
<li>创建并立即调用一个函数，这样既可以执行其中的代码，又不会在内存中留下对该函数的引用</li>
<li>函数内部的所有变量都会被立即销毁——除非将某些变量赋值给了包含作用域（即外部作用域）中的变量</li>
</ul>
</li>
<li>
<p>语法：</p>
<ul>
<li>
<div><pre><code><span>(</span><span>function</span><span>(</span><span>)</span><span>{</span>
<span>//这里是块级作用域</span>
<span>}</span><span>)</span><span>(</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
<li>
<p>解释：以上代码定义并立即调用了一个匿名函数。将函数声明包含在一对圆括号中，表示它实际上是一个函数表达式。而紧随其后的另一对圆括号会立即调用这个函数</p>
</li>
</ul>
</li>
<li>
<p>实例：<img src="./../images/闭包实例4.png" alt="image-20210715181307848" /></p>
<ul>
<li>在匿名函数中定义的任何变量，都会在执行结束时被销毁；</li>
<li>变量 i 只能在循环中使用，使用后即被销毁；</li>
<li>在私有作用域中能够访问变量 count ，是因为这个匿名函数是一个闭包，它能够访问包含作用域中的所有变量</li>
</ul>
</li>
</ul>
</li>
</ul>
<blockquote>
<p><strong>滥用闭包存在什么问题？</strong></p>
</blockquote>
<ul>
<li>会造成网页的性能问题，在IE中可能导致内存泄漏。
<ul>
<li>原因：
<ul>
<li>JS使用垃圾回收机制的原理是标记清除，当一个变量所属的函数作用域被销毁了，变量所存在的作用域没有了，这时JS会给这个变量打上一个标记，待下次垃圾回收时会将其清除。</li>
<li>闭包函数中引用了外部函数的变量，并且外部函数返回的函数赋给了全局作用域中的变量，导致了原本应该被回收的闭包外部函数的变量一直被引用无法回收，这样会导致内存泄漏。</li>
</ul>
</li>
<li>解决方法：全局作用域中的变量复制为 null。</li>
</ul>
</li>
</ul>
<blockquote>
<p><strong>栈内存释放</strong></p>
</blockquote>
<ul>
<li>即使外部函数栈内存释放闭包依旧存在
<ul>
<li>堆内存：存储引用类型，对象类型就是键值对，函数就是代码字符串。将引用类型的空间地址赋值为 <code>null</code>。</li>
<li>栈内存：提供代码的执行环境和存储基本类型。函数执行完后函数的私有作用域会被释放掉。</li>
</ul>
</li>
</ul>
<blockquote>
<p><strong>匿名函数与闭包区别?</strong></p>
</blockquote>
<ul>
<li>闭包中引用了其他作用域链上的变量</li>
</ul>
<p><strong>实例解析</strong></p>
<ul>
<li>
<p>闭包使用实例：</p>
<ul>
<li><img src="./../images/闭包使用.png" alt="image-20210715165629637" /></li>
<li>图示：<img src="./../images/闭包图形.png" alt="image-20210715165706895" /></li>
<li><img src="./../images/闭包解析过程.png" alt="image-20210715165752815" /></li>
</ul>
</li>
<li>
<p><strong>闭包只能取得包含函数中任何变量的最后一个值</strong></p>
<ul>
<li>原因：闭包所保存的是整个变量对象，而不是某个特殊的变量</li>
<li>实例：<img src="./../images/闭包实例.png" alt="image-20210715170248139" />
<ul>
<li>创建另一个匿名函数强制让闭包将变量i传递给该匿名函数</li>
<li>匿名函数(闭包)活动对象中保存变量副本,数组每一个变量则保存匿名函数引用</li>
<li>图示：<img src="./../images/闭包实例2.png" alt="image-20210715170437808" /></li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>闭包形参优先级高于变量提升的变量</strong></p>
<ul>
<li>实例：<img src="./../images/闭包实例3.png" alt="image-20210715170659490" />
<ul>
<li>在函数内部，调用 foo.n 优先访问的是形参，而不是内部定义的变量；console.log 访问的按作用域链优先访问内部变量</li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>使用闭包创建私有变量</strong></p>
<ul>
<li>概念：
<ul>
<li>JavaScript 中没有私有成员的概念；所有对象属性都是公有的</li>
<li>但是任何在函数中定义的变量，都可以认为是私有变量</li>
<li>可以使用闭包来实现公有方法，而通过公有方法可以访问在包含作用域中定义的变量</li>
</ul>
</li>
<li>特权方法：
<ul>
<li>概念：有权访问私有变量的公有方法叫做特权方法</li>
<li>为自定义类型创建私有变量和特权方法：
<ul>
<li>基本模式：<img src="./../images/特权方法.png" alt="image-20210715185337006" /></li>
<li>原理：能够在构造函数中定义特权方法，是因为特权方法作为闭包有权访问在构造函数中定义的所有变量和函数，使用函数声明创建局部特权方法</li>
<li>作用：利用私有和特权成员，可以隐藏那些不应该被直接修改的数据</li>
</ul>
</li>
</ul>
</li>
<li>静态私有变量：
<ul>
<li>概念：在私有作用域中定义私有变量或函数，同样也可以创建特权方法</li>
<li>基本模式：<img src="./../images/静态变量.png" alt="image-20210715185513942" /></li>
<li>原理：公有方法是在原型上定义，使用了函数表达式</li>
<li>与在构造函数中定义特权方法的主要区别：
<ul>
<li>私有变量和函数是由实例共享的</li>
<li>特权方法是在原型上定义的，因此所有实例都使用同一个函数</li>
<li>特权方法作为一个闭包，总是保存着对包含作用域的引用</li>
</ul>
</li>
<li>实例<img src="./../images/闭包创建私有变量.png" alt="image-20210715185637370" /></li>
</ul>
</li>
<li>为单例创建私有变量和特权方法：
<ul>
<li>模块模式：通过为单例添加私有变量和特权方法能够使其得到增强
<ul>
<li><img src="./../images/模块模式.png" alt="image-20210715185725868" /></li>
<li>解释：
<ul>
<li>使用了一个返回对象的匿名函数</li>
<li>在这个匿名函数内部，首先定义了私有变量和函数。然后，将一个对象字面量作为函数的值返回</li>
<li>返回的对象字面量中只包含可以公开的属性和方法</li>
<li>由于这个对象是在匿名函数内部定义的，因此它的公有方法有权访问私有变量和函数</li>
<li>从本质上来讲，这个对象字面量定义的是单例的公共接口</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>增强的模块模式：在返回对象之前加入对其增强的代码
<ul>
<li><img src="./../images/增强的模块模式.png" alt="image-20210715185825041" /></li>
<li>适用情况：单例必须是某种类型的实例，同时还必须添加某些属性和（或）方法对其加以增强的情况</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>闭包题目</strong></p>
<ul>
<li>
<p>题目1：预测输出结果。</p>
<div><pre><code><span>for</span> <span>(</span><span>var</span> i <span>=</span> <span>1</span><span>;</span> i <span>&lt;=</span> <span>5</span><span>;</span> i<span>++</span><span>)</span> <span>{</span>
    <span>(</span><span>function</span><span>(</span><span>i</span><span>)</span><span>{</span>
        <span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
        	console<span>.</span><span>log</span><span>(</span>i<span>)</span><span>;</span>
     	<span>}</span><span>,</span> <span>1000</span> <span>)</span><span>;</span>
	<span>}</span><span>)</span><span>(</span>i<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div><ul>
<li>
<p>输出：1，2，3，4，5</p>
</li>
<li>
<p>分析：首先同步执行 for 循环，循环内部 包含一个闭包函数，通过参数形式传入 i，闭包中保存变量 i 副本；当遇到 setTimeout 时，打印闭包中的 i 副本，因此每次输出 i 都是递增值</p>
</li>
<li>
<p>使用 ES5 方式：</p>
<div><pre><code><span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;=</span> <span>5</span><span>;</span> i<span>++</span><span>)</span> <span>{</span>
	<span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
    	console<span>.</span><span>log</span><span>(</span>i<span>)</span><span>;</span>
    <span>}</span><span>,</span> <span>1000</span> <span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div></li>
</ul>
</li>
</ul>
<h3 id="prototype-对象"> Prototype 对象</h3>
<p><img src="./../images/yuanxingduixiang.png" alt="" /></p>
<p><img src="./../images/yuanxingduixiang2.png" alt="" /></p>
<ul>
<li>
<p>示例：</p>
<blockquote>
<p>使用 Person 构造函数和 Person.prototype 创建实例</p>
</blockquote>
<ul>
<li>语法一：<img src="./../images/yuanxingduixiang-demo.png" alt="" />
<ul>
<li>图示：<img src="./../images/yuanxingduixiang-demo5.png" alt="" /></li>
</ul>
</li>
<li>语法二：<img src="./../images/yuanxingduixiang-demo2.png" alt="" />
<ul>
<li>问题
constructor 属性不指向 Person
无法通过 constructor 确定对象的类型</li>
<li>改进：<img src="./../images/yuanxingduixiang-demo3.png" alt="" />
<ul>
<li>问题问题：
<ul>
<li>这种方式重设 constructor 属性会导致它的 [[Enumerable]] 特性被设置为 true</li>
<li>默认情况下，原生的 constructor 属性是不可枚举的</li>
<li>切断了构造函数与最初原型之间的联系<img src="./../images/yuanxingduixiang-demo4.png" alt="" />
<ul>
<li>Person.prototype 原来指向 Person Prototype，重写后指向了 New Person Prototype 新的原型对象</li>
</ul>
</li>
<li>重写原型对象切断了现有原型与任何之前已经存在的对象实例之间的联系；它们引用的仍然是最初的原型</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="prototype属性和proto-属性"> Prototype属性和proto 属性</h3>
<p><img src="./../images/prototype.png" alt="" /></p>
<p><img src="./../images/proto.png" alt="" /></p>
<p><img src="./../images/prototype-proto.png" alt="" /></p>
<p><strong>获取原型方法:</strong></p>
<ul>
<li>
<p>p.<strong>proto</strong></p>
</li>
<li>
<p>p.constructor.prototype</p>
</li>
<li>
<p>Object.getPrototypeOf(p)</p>
</li>
</ul>
<h3 id="construct-属性"> construct 属性</h3>
<p><img src="./../images/construct.png" alt="" /></p>
<p><img src="./../images/construct2.png" alt="" /></p>
<h3 id="原型-原型链"> 原型&amp;原型链</h3>
<p><img src="./../images/yuanxing.png" alt="" /></p>
<p><img src="./../images/yuanxinglian2.png" alt="" /></p>
<p><img src="./../images/yuanxinglianshixian.png" alt="" /></p>
<p><img src="./../images/morenyuanxing.png" alt="" /></p>
<ul>
<li>注意：
<ul>
<li>通过 <code>Object.prototype.__proto__</code> 查找原型，原型链的终点是 null。
<ul>
<li><img src="./../images/原型链查找.png" alt="image-20210713011124954" /></li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="object-defineproperty-和-proxy-比较"> Object.defineProperty 和 Proxy 比较</h3>
<p><img src="./../images/Object.defineProperty.png" alt="" /></p>
<blockquote>
<p><strong>如何监听一个变量的变化?</strong></p>
</blockquote>
<ul>
<li>ES5 的 Object.defineProperty()</li>
<li>ES6 的 Proxy</li>
</ul>
<p>Proxy 相比于 defineProperty 的优势：</p>
<ul>
<li>基于 <code>Proxy</code> 和 <code>Reflect</code> ，可以原生监听数组，可以监听对象属性的添加和删除</li>
<li>不需要深度遍历监听：判断当前 <code>Reflect.get</code> 的返回值是否为 <code>Object</code> ，如果是则再通过 <code>reactive</code> 方法做代理， 这样就实现了深度观测</li>
<li>只在 <code>getter</code> 时才对对象的下一层进行劫持(优化了性能)</li>
</ul>
<h3 id="es5-函数"> ES5 函数</h3>
<p><strong>函数定义</strong></p>
<p><img src="./../images/ES5function1.png" alt="" /></p>
<p><img src="./../images/ES5function4.png" alt="" /></p>
<p><img src="./../images/ES5function5.png" alt="" /></p>
<p><img src="./../images/ES5function6.png" alt="" /></p>
<p><strong>函数参数</strong></p>
<p><img src="./../images/ES5function2.png" alt="" /></p>
<p><img src="./../images/ES5function3.png" alt="" /></p>
<h3 id="箭头函数"> 箭头函数</h3>
<p><img src="./../images/jiantouhanshu.png" alt="" /></p>
<h3 id="箭头函数与普通函数的区别"> 箭头函数与普通函数的区别</h3>
<p><strong>普通函数</strong>：</p>
<p>​	普通函数通过 function 关键字定义， this 无法结合词法作用域使用，在运行时绑定，只取决于函数的调用方式，在哪里被调用，调用位置。（取决于调用者，和是否独立运行）</p>
<p><strong>箭头函数：</strong></p>
<ol>
<li>函数体内的this对象，就是定义时所在的对象，而不是使用时所在的对象;</li>
<li>箭头函数常用于回调函数中，包括事件处理器或定时器;</li>
<li>没有原型、没有 this、没有 super，没有 arguments，没有 new.target;</li>
<li>不可以使用arguments对象，该对象在函数体内不存在。如果要用，可以用Rest参数代替;</li>
<li>不可以使用yield命令，因此箭头函数不能用作Generator函数;</li>
<li>不可以当作构造函数，也就是说，不可以使用new命令，否则会抛出一个错误:
<ul>
<li>一个函数内部有两个方法：[[Call]] 和 [[Construct]]</li>
<li>在通过 new 进行函数调用时，会执行[[construct]] 方法，创建一个实例对象，然后再执行这个函数体，将函数的 this 绑定在这个实例对象上当直接调用时，执行 [[Call]] 方法</li>
<li>直接执行函数体箭头函数没有 [[Construct]] 方法，不能被用作构造函数调用，当使用 new 进行函数调用时会报错</li>
</ul>
</li>
</ol>
<h3 id="其他函数"> 其他函数</h3>
<p><img src="./../images/functionType1.png" alt="" /></p>
<p><img src="./../images/functionType2.png" alt="" /></p>
<p><img src="./../images/functionType13.png" alt="" /></p>
<h3 id="函数柯里化"> 函数柯里化</h3>
<p><img src="./../images/hanshukelihua.png" alt="" /></p>
<h3 id="创建对象方式有哪些"> 创建对象方式有哪些</h3>
<p><img src="./../images/createObject.png" alt="" /></p>
<h3 id="创建对象方式比较"> 创建对象方式比较</h3>
<p><img src="./../images/createObject1.png" alt="" /></p>
<p><img src="./../images/createObject2.png" alt="createObject6" /></p>
<p><img src="./../images/createObject3.png" alt="createObject5" /></p>
<p><img src="./../images/createObject4.png" alt="createObject4" /></p>
<p><img src="./../images/createObject5.png" alt="createObject3" /></p>
<p><img src="./../images/createObject6.png" alt="createObject2" /></p>
<p><img src="./../images/createObject7.png" alt="createObject1" /></p>
<h3 id="对象继承方式"> 对象继承方式</h3>
<p><img src="./../images/objectInherit.png" alt="" /></p>
<h3 id="对象继承方式比较"> 对象继承方式比较</h3>
<p><img src="./../images/inherit1.png" alt="" /></p>
<p><img src="./../images/inherit2.png" alt="inherit5" /></p>
<p><img src="./../images/inherit3.png" alt="inherit4" /></p>
<p><img src="./../images/inherit4.png" alt="inherit3" /></p>
<p><img src="./../images/inherit5.png" alt="inherit2" /></p>
<p><img src="./../images/inherit6.png" alt="inherit1" /></p>
<p><img src="./../images/inherit7.png" alt="" /></p>
<h3 id="class-类"> Class 类</h3>
<p><img src="./../images/class.png" alt="class" /></p>
<p><img src="./../images/class2.png" alt="class2" /><img src="./../images/class3.png" alt="" /></p>
<h3 id="class-继承"> Class 继承</h3>
<p><img src="./../images/class-extend.png" alt="" /></p>
<p><img src="./../images/class-extend3.png" alt="" /></p>
<p><img src="./../images/class-extend2.png" alt="class-extend2" /></p>
<h3 id="宏任务-微任务"> 宏任务&amp;微任务</h3>
<p><strong>事件循环</strong>：event loop 宏任务：macrotask 也称为 task 微任务：microtask 也称为 jobs</p>
<p><strong>微任务</strong></p>
<ul>
<li>process.nextTick (Node)</li>
<li>promise.then()，Promise.resolve()</li>
<li>Object.observe</li>
<li>MutationObserver (浏览器)</li>
</ul>
<p><strong>宏任务</strong></p>
<ul>
<li>script(主程序代码)</li>
<li>setTimeout, setInterval, setImmediate,</li>
<li>I/O, UI rendering</li>
<li>回调函数（事件、ajax等）</li>
</ul>
<p><img src="./../images/task=performance.png" alt="" /></p>
<p><strong>过程</strong>： 在一次事件循环中，会先执行js线程的主任务，然后会去查找是否有微任务microtask（promise），如果有那就优先执行所有微任务，如果没有，在去查找宏任务macrotask（setTimeout、setInterval）进行执行。</p>
<p>一个事件循环的执行步骤：</p>
<ul>
<li>执行同步代码，这属于宏任务
<ul>
<li>在执行宏任务时，如遇到微任务，将微任务添加到本次循环的微任务队列中；</li>
</ul>
</li>
<li>执行所有微任务</li>
<li>渲染 UI</li>
<li>然后开始下一轮 Event loop，执行宏任务中的异步代码</li>
</ul>
<p><img src="./../images/event-loop.gif" alt="" /></p>
<h3 id="浏览器事件循环"> 浏览器事件循环</h3>
<ul>
<li>
<p>js是单线程运行的，在代码执行的时候，通过将不同函数的执行上下文压入执行栈中来保证代码的有序执行。</p>
</li>
<li>
<p>在执行同步代码的时候，如果遇到了异步事件，将异步事件添加到事件队列，继续执行执行栈中的其他任务。</p>
</li>
<li>
<p>当异步事件执行完毕后，再将异步事件对应的回调加入到任务队列中等待执行。</p>
</li>
<li>
<p>任务队列可以分为宏任务对列和微任务对列，当当前执行栈中的事件执行完毕后，js引擎首先会判断微任务对列中是否有任务可以执行，如果有就将微任务队首的事件压入栈中执行。当微任务对列中的任务都执行完成后再去判断宏任务对列中的任务。</p>
</li>
</ul>
<p><img src="./../images/eventCycle.png" alt="" /></p>
<p><strong>参考资料</strong></p>
<p><a href="https://juejin.cn/post/6844903764202094606#heading-26" target="_blank" rel="noopener noreferrer">一次弄懂Event Loop</a></p>
<h3 id="node-事件循环"> Node 事件循环</h3>
<ul>
<li>
<p>Node 事件循环模型：</p>
<ul>
<li>
<p>Node 的 event loop 分为6个阶段，它们会按照顺序反复运行，分别如下:</p>
<ul>
<li><code>timers</code>：执行 <code>setTimeout()</code> 和 <code>setInterval()</code> 中到期的callback;</li>
<li><code>I/O callbacks</code>：上一轮循环中有少数的 <code>I/O callback</code> 会被延迟到这一轮的这一阶段执行;</li>
<li><code>idle, prepare</code>：队列的移动，仅内部使用;</li>
<li><code>poll</code>：最为重要的阶段，执行 <code>I/O callback</code>，在适当的条件下会阻塞在这个阶段</li>
<li><code>check</code>：执行 <code>setImmediate</code> 的 callback</li>
<li><code>close callbacks</code>：执行 close 事件的 callback，例如 <code>socket.on(&quot;close&quot;,func)</code></li>
</ul>
<p><img src="./../images/event-loop-node2.png" alt="" /></p>
</li>
<li>
<p>不同于浏览器的是，在每个阶段完成后，而不是 macroTask 任务完成后，microTask队列就会被执行。这就导致了<strong>同样的代码在不同的上下文环境下会出现不同的结果</strong>.</p>
</li>
<li>
<p>如果在 timers 阶段执行时创建了 setImmediate 则会在此轮循环的 check 阶段执行，如果在 timers 阶段创建了 setTimeout，由于 timers 已取出完毕，则会进入下轮循环，check 阶段创建 timers 任务同理。</p>
</li>
</ul>
</li>
<li>
<p>Node 事件循环底层实现：</p>
<ul>
<li>
<p><code>Node</code>中的<code>Event Loop</code>是基于<code>libuv</code>实现的，而<code>libuv</code>是 <code>Node</code> 的新跨平台抽象层，libuv使用异步，事件驱动的编程方式，核心是提供<code>i/o</code>的事件循环和异步回调。</p>
</li>
<li>
<p>libuv的<code>API</code>包含有时间，非阻塞的网络，异步文件操作，子进程等等。 <code>Event Loop</code>就是在<code>libuv</code>中实现的。</p>
<p><img src="./../images/event-loop-node.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
<h3 id="浏览器-node-事件循环区别"> 浏览器&amp;Node 事件循环区别</h3>
<ul>
<li>
<p>执行顺序区别：</p>
<ul>
<li>
<p>浏览器先执行所有宏任务，然后再执行所有微任务</p>
</li>
<li>
<p>Node 按 6 阶段顺序执行，先执行 timer 任务，后执行microtask队列的所有任务</p>
</li>
<li>
<div><pre><code><span>setTimeout</span><span>(</span><span>(</span><span>)</span><span>=></span><span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'timer1'</span><span>)</span>

    Promise<span>.</span><span>resolve</span><span>(</span><span>)</span><span>.</span><span>then</span><span>(</span><span>function</span><span>(</span><span>)</span> <span>{</span>
        console<span>.</span><span>log</span><span>(</span><span>'promise1'</span><span>)</span>
    <span>}</span><span>)</span>
<span>}</span><span>,</span> <span>0</span><span>)</span>

<span>setTimeout</span><span>(</span><span>(</span><span>)</span><span>=></span><span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'timer2'</span><span>)</span>

    Promise<span>.</span><span>resolve</span><span>(</span><span>)</span><span>.</span><span>then</span><span>(</span><span>function</span><span>(</span><span>)</span> <span>{</span>
        console<span>.</span><span>log</span><span>(</span><span>'promise2'</span><span>)</span>
    <span>}</span><span>)</span>
<span>}</span><span>,</span> <span>0</span><span>)</span>

浏览器输出：
time1
promise1
time2
promise2

Node输出：
time1
time2
promise1
promise2
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br></div></div><ul>
<li>浏览器：两个setTimeout作为两个MacroTask, 所以先输出timer1, promise1，再输出timer2，promise2；</li>
<li>Node：最初timer1和timer2就在timers阶段中。开始时首先进入timers阶段，执行timer1的回调函数，打印timer1，并将promise1.then回调放入microtask队列，同样的步骤执行timer2，打印timer2；
至此，timer阶段执行结束，event loop进入下一个阶段之前，执行microtask队列的所有任务，依次打印promise1、promise2。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="异步编程方式"> 异步编程方式</h3>
<ul>
<li>
<p>回调函数</p>
<ul>
<li>优点：简单、容易理解；</li>
<li>缺点：不利于维护，代码耦合高</li>
</ul>
</li>
<li>
<p>事件监听: 采用时间驱动模式，取决于某个事件是否发生</p>
<ul>
<li>优点：容易理解，可以绑定多个事件，每个事件可以指定多个回调函；</li>
<li>缺点：事件驱动型，流程不够清晰</li>
</ul>
</li>
<li>
<p>发布/订阅(观察者模式): 类似于事件监听，但是可以通过 消息中心，了解现在有多少发布者，多少订阅者 Promise对象</p>
<ul>
<li>优点：可以利用then方法，进行链式写法；可以书写错误时的回调函数；</li>
<li>缺点：编写和理解，相对比较难</li>
</ul>
</li>
<li>
<p>Promise 函数</p>
</li>
<li>
<p>Generator 函数</p>
<ul>
<li>优点：函数体内外的数据交换、错误处理机制；</li>
<li>缺点：流程管理不方便</li>
</ul>
</li>
<li>
<p>async 函数</p>
<ul>
<li>优点：内置执行器、更好的语义、更广的适用性、返回的是Promise、结构清晰；</li>
<li>缺点：错误处理机制</li>
</ul>
</li>
</ul>
<p>异步处理方法的比较: 参考 <a href="https://es6.ruanyifeng.com/#docs/async#%E4%B8%8E%E5%85%B6%E4%BB%96%E5%BC%82%E6%AD%A5%E5%A4%84%E7%90%86%E6%96%B9%E6%B3%95%E7%9A%84%E6%AF%94%E8%BE%83" target="_blank" rel="noopener noreferrer">《ES6》</a></p>
<p><img src="./../images/async1.png" alt="" /></p>
<h3 id="模拟异步操作方式"> 模拟异步操作方式</h3>
<p><img src="./../images/async2.png" alt="" /></p>
<h3 id="pomise-对象"> Pomise 对象</h3>
<p>promise 相关知识点和题目参考: <a href="https://gitee.com/jokerxw/myNotes/tree/master/javascript/%E5%8E%9F%E7%90%86%E5%AE%9E%E7%8E%B0/promise" target="_blank" rel="noopener noreferrer">promise 总结</a></p>
<h3 id="async-await-操作"> async await 操作</h3>
<p><img src="./../images/async-awiat1.png" alt="" /></p>
<p><img src="./../images/async-awiat2.png" alt="" /></p>
<p><img src="./../images/async-awiat3.png" alt="" /></p>
<p><img src="./../images/async-awiat4.png" alt="" /></p>
<p><img src="./../images/async-awiat5.png" alt="" /></p>
<p>async 函数，使得异步操作变得更加方便，它就是 Generator 函数的语法糖</p>
<ul>
<li>内置执行器：Generator 函数的执行必须靠执行器，<code>async</code>函数自带执行器</li>
<li>更好的语义</li>
<li>更广的适用性：<code>async</code>函数的<code>await</code>命令后面，可以是 Promise 对象和原始类型的值（数值、字符串和布尔值，但这时会自动转成立即 resolved 的 Promise 对象）</li>
<li>返回值是 Promise</li>
</ul>
<p><strong>注意：</strong></p>
<ul>
<li>
<p>await 后面代码会立即执行，执行后结果和 await 下面代码会被当做回调函数内容添加到微任务队列中</p>
<ul>
<li>
<p>示例：</p>
<div><pre><code><span>async</span> <span>function</span> <span>async1</span> <span>(</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'async1 start'</span><span>)</span><span>;</span>
    <span>await</span> <span>async2</span><span>(</span><span>)</span><span>;</span>
    console<span>.</span><span>log</span><span>(</span><span>'async1 end'</span><span>)</span><span>;</span>
<span>}</span>

<span>async</span> <span>function</span> <span>async2</span> <span>(</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'async2'</span><span>)</span><span>;</span>
<span>}</span>

console<span>.</span><span>log</span><span>(</span><span>'script start'</span><span>)</span><span>;</span>

<span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'setTimeout'</span><span>)</span><span>;</span>
<span>}</span><span>,</span> <span>0</span><span>)</span><span>;</span>

<span>async1</span><span>(</span><span>)</span> 

<span>new</span> <span>Promise</span> <span>(</span><span>function</span> <span>(</span><span>resolve</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'promise1'</span><span>)</span><span>;</span>
    <span>resolve</span><span>(</span><span>)</span><span>;</span>
<span>}</span><span>)</span><span>.</span><span>then</span> <span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'promise2'</span><span>)</span><span>;</span>
<span>}</span><span>)</span>

console<span>.</span><span>log</span><span>(</span><span>'script end'</span><span>)</span><span>;</span>

<span>//------输出--------</span>
script start
async1 start
async2
promise1
script end
async1 end
promise2
setTimeout

</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br></div></div><blockquote>
<p>注意以下几点:</p>
<ul>
<li>定义 async1()，async2()，不会立即执行</li>
<li>await async2(); 表示先执行 async2()，然后将 await 后面的代码当作 .then() 中的回调来处理，因此执行 async2() 以后，返回值和后面代码添加到微任务队列中</li>
<li>初始化 promise，promise 中的代码会立即执行</li>
</ul>
</blockquote>
</li>
</ul>
</li>
</ul>
<p>参考：<a href="https://es6.ruanyifeng.com/#docs/async" target="_blank" rel="noopener noreferrer">《ES6 教程》</a></p>
<h3 id="reflect-对象"> Reflect 对象</h3>
<p><img src="./../images/reflect.png" alt="" /></p>
<h3 id="proxy-对象"> <a href="https://es6.ruanyifeng.com/#docs/proxy" target="_blank" rel="noopener noreferrer">Proxy 对象</a></h3>
<p><img src="./../images/proxy1.png" alt="" /></p>
<p><img src="./../images/proxy2.png" alt="proxy2" /></p>
<p><img src="./../images/proxy3.png" alt="proxy3" /></p>
<p><img src="./../images/proxy4.png" alt="proxy4" /></p>
<p><img src="./../images/proxy5.png" alt="proxy5" /></p>
<h3 id="模块加载方案和区别"> 模块加载方案和区别</h3>
<p><img src="./../images/module1.png" alt="" /></p>
<h3 id="commonjs-模块"> CommonJS 模块</h3>
<p>commonjs 核心就是 require 语句</p>
<p><strong>require() 的基本用法</strong></p>
<p>当 Node 遇到 require(X) 时，按下面的顺序处理。</p>
<p>（1）如果 X 是内置模块（比如 require('http'）)
　　a. 返回该模块。
　　b. 不再继续执行。</p>
<p>（2）如果 X 以 &quot;./&quot; 或者 &quot;/&quot; 或者 &quot;../&quot; 开头
　　a. 根据 X 所在的父模块，确定 X 的绝对路径。
　　b. 将 X 当成文件，依次查找下面文件，只要其中有一个存在，就返回该文件，不再继续执行。</p>
<blockquote>
<ul>
<li>X</li>
<li>X.js</li>
<li>X.json</li>
<li>X.node</li>
</ul>
</blockquote>
<p>c. 将 X 当成目录，依次查找下面文件，只要其中有一个存在，就返回该文件，不再继续执行。</p>
<blockquote>
<ul>
<li>X/package.json（main字段）</li>
<li>X/index.js</li>
<li>X/index.json</li>
<li>X/index.node</li>
</ul>
</blockquote>
<p>（3）如果 X 不带路径
　　a. 根据 X 所在的父模块，确定 X 可能的安装目录。
　　b. 依次在每个目录中，将 X 当成文件名或目录名加载。</p>
<p>（4） 抛出 &quot;not found&quot;</p>
<p><strong>参考资料：</strong></p>
<p><a href="http://www.ruanyifeng.com/blog/2015/05/require.html" target="_blank" rel="noopener noreferrer">require() 源码解读</a></p>
<h3 id="es6模块"> ES6模块</h3>
<p><img src="./../images/module-es6.png" alt="module-es6" /></p>
<ul>
<li>ES6 模块命令：</li>
</ul>
<p><img src="./../images/module-es6-1.png" alt="module-es6-1" /></p>
<p><img src="./../images/module-es6-2.png" alt="module-es6-2" /></p>
<p><img src="./../images/module-es6-4.png" alt="module-es6-4" /></p>
<p><img src="./../images/module-es6-3.png" alt="module-es6-3" /></p>
<p><img src="./../images/module-es6-5.png" alt="" /></p>
<h3 id="模块加载混合引用"> 模块加载混合引用</h3>
<p><img src="./../images/module-hunhe.png" alt="" /></p>
<h3 id="模块加载循环引用"> 模块加载循环引用</h3>
<p><img src="./../images/module-xunhuan.png" alt="" /></p>
<h3 id="定时器"> 定时器</h3>
<p><img src="./../images/dingshi1.png" alt="" /></p>
<p><img src="./../images/dingshi2.png" alt="" /></p>
<p><img src="./../images/dingshi3.png" alt="" /></p>
<blockquote>
<p><strong>为什么使用setTimeout实现setInterval？怎么模拟？</strong></p>
</blockquote>
<ul>
<li>
<p>setInterval的作用是每隔一段指定时间执行一个函数，但是这个执行不是真的到了时间立即执行，它真正的作用是每隔一段时间将事件加入事件队列中去，只有当当前的执行栈为空的时候，才能去从事件队列中取出事件执行。</p>
</li>
<li>
<p>所以可能会出现这样的情况，就是当前执行栈执行的时间很长，导致事件队列里边积累多个定时器加入的事件，当执行栈结束的时候，这些事件会依次执行，因此就不能到间隔一段时间执行的效果。</p>
</li>
<li>
<p>针对setInterval的这个缺点，我们可以使用setTimeout递归调用来模拟setInterval，这样我们就确保了只有一个事件结束了，我们才会触发下一个定时器事件，这样解决了setInterval的问题</p>
<div><pre><code><span>//思路是使用递归函数，不断地去执行 setTimeout 从而达到 setInterval 的效果</span>
<span>function</span> <span>mySetInterval</span><span>(</span><span>fn<span>,</span>timeout</span><span>)</span><span>{</span> 
    <span>//控制器，控制定时器是否继续执行</span>
    <span>var</span> timer <span>=</span> <span>{</span> flag<span>:</span><span>true</span> <span>}</span><span>;</span> 
    <span>//设置递归函数，模拟定时器执行。</span>
    <span>function</span> <span>interval</span><span>(</span><span>)</span><span>{</span>
        <span>if</span><span>(</span>timer<span>.</span>flag<span>)</span><span>{</span>
            <span>fn</span><span>(</span><span>)</span><span>;</span>
            <span>setTimeout</span><span>(</span>interval<span>,</span>timeout<span>)</span><span>;</span>
        <span>}</span>
    <span>}</span>
    <span>//启动定时器</span>
	<span>setTimeout</span><span>(</span>interval<span>,</span>timeout<span>)</span><span>;</span>
    <span>//返回控制器</span>
    <span>return</span> timer<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div></li>
</ul>
<h3 id="垃圾回收"> 垃圾回收</h3>
<p><img src="./../images/lajihuishou.png" alt="" /><strong>V8引擎的垃圾回收机制</strong></p>
<p>v8的垃圾回收机制基于分代回收机制，这个机制又基于世代假说，这个假说有两个特点:</p>
<ul>
<li>一是新生的对象容易早死；</li>
<li>二是不死的对象会活得更久；</li>
</ul>
<p>基于这个假说，v8引擎将内存分为了新生代和老生代。</p>
<ul>
<li>
<p>新创建的对象或者只经历过一次的垃圾回收的对象被称为新生代。</p>
</li>
<li>
<p>经历过多次垃圾回收的对象被称为老生代。</p>
</li>
</ul>
<p>新生代被分为From和To两个空间，To一般是闲置的。当From空间满了的时候会执行Scavenge算法进行垃圾回收。</p>
<p>当我们执行垃圾回收算法的时候应用逻辑将会停止，等垃圾回收结束后再继续执行。</p>
<p>这个算法分为三步：</p>
<ul>
<li>（1）首先检查From空间的存活对象，如果对象存活则判断对象是否满足晋升到老生代的条件，如果满足条件则晋升到老生代。如果不满足条件则移动To空间。</li>
<li>（2）如果对象不存活，则释放对象的空间。</li>
<li>（3）最后将From空间和To空间角色进行交换。</li>
</ul>
<p>新生代对象晋升到老生代有两个条件：</p>
<ul>
<li>（1）第一个是判断是对象否已经经过一次Scavenge回收。若经历过，则将对象从From空间复制到老生代中；若没有经历，则复制到To空间。</li>
<li>（2）第二个是To空间的内存使用占比是否超过限制。当对象从From空间复制到To空间时，若To空间使用超过25%，则对象直接晋升到老生代中。设置25%的原因主要是因为算法结束后，两个空间结束后会交换位置，如果To空间的内存太小，会影响后续的内存分配。</li>
</ul>
<p>老生代采用了标记清除法和标记压缩法：</p>
<ul>
<li>标记清除法首先会对内存中存活的对象进行标记，标记结束后清除掉那些没有标记的对象。由于标记清除后会造成很多的内存碎片，不便于后面的内存分配。</li>
<li>所以了解决内存碎片的问题引入了标记压缩法。</li>
</ul>
<p><strong>参考资料：</strong></p>
<p><a href="https://www.jianshu.com/p/b8ed21e8a4fb" target="_blank" rel="noopener noreferrer">深入理解V8的垃圾回收原理</a></p>
<p><a href="https://zhuanlan.zhihu.com/p/23992332" target="_blank" rel="noopener noreferrer">JavaScript 中的垃圾回收</a></p>
<h3 id="内存泄漏"> 内存泄漏</h3>
<p><strong>定义</strong></p>
<p>​	指由于疏忽或错误造成程序未能释放已经不再使用的内存的情况。内存泄漏并非指内存在物理上的消失，而是应用程序分配某段内存后，由于设计错误，失去了对该段内存的控制，因而造成了内存的浪费。</p>
<p><strong>哪些操作会造成内存泄漏?</strong></p>
<p>造成内存泄漏情况：</p>
<ul>
<li>意外的全局变量
<ul>
<li>由于使用未声明的变量，而意外的创建了一个全局变量，而使这个变量一直留在内存中无法被回收</li>
</ul>
</li>
<li>被遗忘的计时器或回调函数
<ul>
<li>设置了setInterval定时器，而忘记取消它，如果循环函数有对外部变量的引用的话，那么这个变量会被一直留在内存中，而无法被回收</li>
</ul>
</li>
<li>脱离DOM的引用
<ul>
<li>获取一个DOM元素的引用，而后面这个元素被删除，由于我们一直保留了对这个元素的引用，所以它也无法被回收</li>
</ul>
</li>
<li>闭包
<ul>
<li>不合理的使用闭包，从而导致某些变量一直被留在内存当中
<ul>
<li>闭包会导致原始作用域链不释放，造成内存泄漏（占用）；</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>内存泄漏的识别方法:</strong></p>
<ul>
<li>
<p><strong>浏览器</strong>:</p>
<ul>
<li>
<p>Chrome 浏览器查看内存占用，按照以下步骤操作:</p>
<ul>
<li>
<p>打开开发者工具，选择 Timeline 面板</p>
</li>
<li>
<p>在顶部的<code>Capture</code>字段里面勾选 Memory</p>
</li>
<li>
<p>点击左上角的录制按钮。</p>
</li>
<li>
<p>在页面上进行各种操作，模拟用户的使用情况。</p>
</li>
<li>
<p>一段时间后，点击对话框的 stop 按钮，面板上就会显示这段时间的内存占用情况</p>
<p><img src="./../images/neicunxielou.png" alt="" /></p>
</li>
<li>
<p>如果内存占用基本平稳，接近水平，就说明不存在内存泄漏</p>
</li>
<li>
<p>正常情况： <img src="./../images/内存泄漏.png" alt="" /></p>
</li>
<li>
<p>内存泄漏：<img src="./../images/neicunxielou2.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>命令行</strong></p>
<ul>
<li>
<p>命令行可以使用 Node 提供的<a href="https://nodejs.org/api/process.html#process_process_memoryusage" target="_blank" rel="noopener noreferrer"><code>process.memoryUsage</code></a>方法</p>
<div><pre><code>console<span>.</span><span>log</span><span>(</span>process<span>.</span><span>memoryUsage</span><span>(</span><span>)</span><span>)</span><span>;</span>
<span>// { rss: 27709440,</span>
<span>//  heapTotal: 5685248,</span>
<span>//  heapUsed: 3449392,</span>
<span>//  external: 8772 }</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div></li>
<li>
<p><code>process.memoryUsage</code>返回一个对象，包含了 Node 进程的内存占用信息。该对象包含四个字段，单位是字节，<a href="http://stackoverflow.com/questions/12023359/what-do-the-return-values-of-node-js-process-memoryusage-stand-for" target="_blank" rel="noopener noreferrer">含义</a>如下:</p>
<ul>
<li>rss（resident set size）：所有内存占用，包括指令区和堆栈。</li>
<li>heapTotal：&quot;堆&quot;占用的内存，包括用到的和没用到的。</li>
<li>heapUsed：用到的堆的部分。</li>
<li>external： V8 引擎内部的 C++ 对象占用的内存。</li>
</ul>
<p><img src="./../images/neicunxielou3.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
<p><strong>参考资料：</strong></p>
<p><a href="http://www.ruanyifeng.com/blog/2017/04/memory-leak.html" target="_blank" rel="noopener noreferrer">JavaScript 内存泄漏程</a></p>
<p><a href="https://segmentfault.com/a/1190000008901861" target="_blank" rel="noopener noreferrer">javascript典型内存泄漏及chrome的排查方法</a></p>
<h3 id="ajax-是什么"> AJax 是什么？</h3>
<p><img src="./../images/ajax1.png" alt="" /></p>
<h3 id="创建-ajax-过程"> 创建 Ajax 过程</h3>
<p><img src="./../images/ajax2.png" alt="" /></p>
<ul>
<li>示例：</li>
</ul>
<p><img src="./../images/ajax3.png" alt="" /></p>
<h3 id="消息推送技术"> 消息推送技术</h3>
<p><img src="./../images/tuisongjishu.png" alt="" /></p>
<p><strong>websocket 与 Ajax 区别</strong></p>
<p><img src="./../images/wesock-ajax.png" alt="" /></p>
<h3 id="sse实现"> SSE实现</h3>
<p><img src="./../images/sse.png" alt="" /></p>
<h3 id="websocket-实现"> websocket 实现</h3>
<p><img src="./../images/websocket2.png" alt="" /></p>
<p><img src="./../images/websocket.png" alt="" /></p>
<h3 id="跨页面通信方法"> 跨页面通信方法</h3>
<p>在浏览器中，我们可以同时打开多个Tab页，每个Tab页可以粗略理解为一个“独立”的运行环境，即使是全局对象也不会在多个Tab间共享。然而有些时候，我们希望能在这些“独立”的Tab页面之间同步页面的数据、信息或状态，因此需要跨页面通信。</p>
<p>跨页面通信分为两类</p>
<ul>
<li>同源跨页面通信
<ul>
<li>广播模式：Broadcast Channe / Service Worker / LocalStorage + StorageEvent</li>
<li>共享存储模式：Shared Worker / IndexedDB / cookie</li>
<li>口口相传模式：window.open + window.opener</li>
<li>基于服务端：Websocket / Comet / SSE 等</li>
</ul>
</li>
<li>非同源跨页面通信
<ul>
<li>嵌入同源 iframe 作为“桥”，将非同源页面通信转换为同源页面通信</li>
</ul>
</li>
</ul>
<p><strong>同源跨页面通信方案</strong></p>
<ul>
<li>
<p><strong><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/BroadcastChannel" target="_blank" rel="noopener noreferrer">BroadcastChannel</a></strong></p>
<ul>
<li>
<p>接口代理了一个命名频道，可以让指定 <a href="https://developer.mozilla.org/zh-CN/docs/Glossary/Origin" target="_blank" rel="noopener noreferrer">origin</a> 下的任意 <a href="https://developer.mozilla.org/zh-CN/docs/Glossary/Browsing_context" target="_blank" rel="noopener noreferrer">browsing context</a> 来订阅它。它允许同源的不同浏览器窗口，Tab页，frame或者 iframe 下的不同文档之间相互通信。通过触发一个 <code>message</code> 事件，消息可以广播到所有监听了该频道的 <code>BroadcastChannel</code> 对象。</p>
</li>
<li>
<p>实现：</p>
<div><pre><code><span>// 发送消息页面</span>
<span>const</span> channel <span>=</span> <span>new</span> <span>BroadcastChannel</span><span>(</span><span>'tabs'</span><span>)</span>
channel<span>.</span><span>postMessage</span><span>(</span><span>'hello'</span><span>)</span>

<span>// 监听消息页面</span>
<span>const</span> channel <span>=</span> <span>new</span> <span>BroadcastChannel</span><span>(</span><span>'tabs'</span><span>)</span>
channel<span>.</span><span>onmessage</span> <span>=</span> <span>evt</span> <span>=></span> <span>{</span>
<span>// evt.data</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div></li>
<li>
<p>缺点：</p>
</li>
<li>
<p><code>BroadcastChannel</code>方案兼容性差些（chrome &gt; 58），但比<code>localStorage</code>方案生命周期短（不会持久化）。</p>
</li>
</ul>
<hr>
</li>
<li>
<p><strong><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window/localStorage" target="_blank" rel="noopener noreferrer">LocalStorage</a></strong></p>
<ul>
<li>
<p>当 LocalStorage 变化时，会触发<code>storage</code>事件。利用这个特性，我们可以在发送消息时，把消息写入到某个 LocalStorage 中；然后在各个页面内，通过监听<code>storage</code>事件即可收到通知。</p>
<p><code>storage</code>事件只有在值真正改变时才会触发。</p>
<p>触发写入操作的页面下的 <code>storage listener</code>不会被触发。</p>
<p>safari隐身模式下无法设置<code>localStorage</code>值</p>
</li>
<li>
<p>实现：</p>
<div><pre><code><span>//监听消息页面</span>
window<span>.</span><span>addEventListener</span><span>(</span><span>'storage'</span><span>,</span> <span>function</span> <span>(</span><span>e</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>e<span>.</span>key <span>===</span> <span>'broadcast'</span><span>)</span> <span>{</span>
        <span>const</span> data <span>=</span> <span>JSON</span><span>.</span><span>parse</span><span>(</span>e<span>.</span>newValue<span>)</span><span>;</span>
        <span>const</span> text <span>=</span> <span>'[receive] '</span> <span>+</span> data<span>.</span>msg <span>+</span> <span>' —— tab '</span> <span>+</span> data<span>.</span>from<span>;</span>
        console<span>.</span><span>log</span><span>(</span><span>'[Storage I] receive message:'</span><span>,</span> text<span>)</span><span>;</span>
    <span>}</span>
<span>}</span><span>)</span><span>;</span>
<span>//发送消息页面</span>
window<span>.</span>localStorage<span>.</span><span>setItem</span><span>(</span><span>'broadcast'</span><span>,</span> <span>'123'</span><span>)</span><span>;</span> <span>// 第一次设置值触发 storage 事件</span>
window<span>.</span>localStorage<span>.</span><span>setItem</span><span>(</span><span>'broadcast'</span><span>,</span> <span>'123'</span><span>)</span><span>;</span> <span>// 第二次设置相同值不会触发 storage 事件</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><hr>
</li>
</ul>
</li>
<li>
<p><strong><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/SharedWorker" target="_blank" rel="noopener noreferrer">Shared Worker</a></strong></p>
<ul>
<li><code>**SharedWorker**</code> 接口代表一种特定类型的 worker，可以从几个浏览上下文中<em>访问</em>，例如几个窗口、iframe 或其他 worker。它们实现一个不同于普通 worker 的接口，具有不同的全局作用域</li>
</ul>
</li>
</ul>
<p><strong>非同源跨页面通信方案</strong></p>
<ul>
<li>
<p><strong>获取句柄</strong>：</p>
<ul>
<li>
<p>父页面通过<code>window.open(url, name)</code>方式打开的子页面可以获取句柄，然后通过<code>postMessage</code>完成通讯需求。</p>
</li>
<li>
<p>实现：</p>
<div><pre><code><span>// parent.html</span>
<span>const</span> childPage <span>=</span> window<span>.</span><span>open</span><span>(</span><span>'child.html'</span><span>,</span> <span>'child'</span><span>)</span>

childPage<span>.</span><span>onload</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
   childPage<span>.</span><span>postMessage</span><span>(</span><span>'hello'</span><span>,</span> location<span>.</span>origin<span>)</span>
<span>}</span>

<span>// child.html</span>
window<span>.</span><span>onmessage</span> <span>=</span> <span>event</span> <span>=></span> <span>{</span>
	<span>// event.data</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div></li>
<li>
<p>缺点：</p>
<ul>
<li>
<p>当指定<code>window.open</code>的第二个name参数时，再次调用<code>window.open('****', 'child')</code>会使之前已经打开的同name子页面刷新</p>
</li>
<li>
<p>只能与自己打开的页面完成通讯，应用面相对较窄；但优点是在跨域场景中依然可以使用该方案。</p>
</li>
<li>
<p>由于安全策略，异步请求之后再调用<code>window.open</code>会被浏览器阻止，不过可以通过句柄设置子页面的url即可实现类似效果。</p>
<div><pre><code><span>// 首先先开一个空白页</span>
<span>const</span> tab <span>=</span> window<span>.</span><span>open</span><span>(</span><span>'about:blank'</span><span>)</span>

<span>// 请求完成之后设置空白页的url</span>
<span>fetch</span><span>(</span><span>/* ajax */</span><span>)</span><span>.</span><span>then</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
   tab<span>.</span>location<span>.</span>href <span>=</span> <span>'****'</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>iframe:</strong></p>
<p>嵌入同源 iframe 作为“桥”，将非同源页面通信转换为同源页面通信。由于 iframe 与父页面间可以通过指定<code>origin</code>来忽略同源限制，因此可以在每个页面中嵌入一个 iframe ，而这些 iframe 由于使用的是一个 url，因此属于同源页面，其通信方式可以复用上面第一部分提到的各种方式。</p>
<ul>
<li>
<p>实现：</p>
<div><pre><code><span>// 页面中监听 iframe 发来的消息</span>
window<span>.</span><span>addEventListener</span><span>(</span><span>'message'</span><span>,</span> <span>function</span> <span>(</span><span>e</span><span>)</span> <span>{</span>
    <span>// …… do something</span>
<span>}</span><span>)</span><span>;</span>
<span>//当页面要与其他的同源或非同源页面通信时，会先给 iframe 发送消息</span>
window<span>.</span>frames<span>[</span><span>0</span><span>]</span><span>.</span>window<span>.</span><span>postMessage</span><span>(</span>mydata<span>,</span> <span>'*'</span><span>)</span><span>;</span>
<span>//frame 收到消息后，会使用某种跨页面消息通信技术在所有 iframe 间同步消息，例如下面使用的 Broadcast Channel：</span>
<span>const</span> bc <span>=</span> <span>new</span> <span>BroadcastChannel</span><span>(</span><span>'broadcast'</span><span>)</span><span>;</span>
<span>// 收到来自页面的消息后，在 iframe 间进行广播</span>
window<span>.</span><span>addEventListener</span><span>(</span><span>'message'</span><span>,</span> <span>function</span> <span>(</span><span>e</span><span>)</span> <span>{</span>
    bc<span>.</span><span>postMessage</span><span>(</span>e<span>.</span>data<span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>  
<span>//其他 iframe 收到通知后，则会将该消息同步给所属的页面</span>
<span>// 对于收到的（iframe）广播消息，通知给所属的业务页面</span>
bc<span>.</span><span>onmessage</span> <span>=</span> <span>function</span> <span>(</span><span>e</span><span>)</span> <span>{</span>
    window<span>.</span>parent<span>.</span><span>postMessage</span><span>(</span>e<span>.</span>data<span>,</span> <span>'*'</span><span>)</span><span>;</span>
<span>}</span><span>;</span>

</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br></div></div><p><img src="./../images/iframe-tongxin.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
<hr>
<p><strong>参考：</strong></p>
<p><a href="https://blog.csdn.net/p3118601/article/details/88966864" target="_blank" rel="noopener noreferrer">前端跨页面通信，你知道哪些方法？</a></p>
<p><a href="https://zhuanlan.zhihu.com/p/29368435" target="_blank" rel="noopener noreferrer">跨页面通信的各种姿势*</a></p>
<hr>
<h3 id="iframe框架"> Iframe框架</h3>
<ul>
<li>
<p>概念：</p>
<ul>
<li><strong>HTML内联框架元素 (<code>&lt;iframe&gt;</code>)</strong> 表示嵌套的<a href="https://developer.mozilla.org/zh-CN/docs/Glossary/Browsing_context" target="_blank" rel="noopener noreferrer">browsing context</a>。它能够将另一个HTML页面嵌入到当前页面中。</li>
<li>每个嵌入的浏览上下文（embedded browsing context）都有自己的<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/History" target="_blank" rel="noopener noreferrer">会话历史记录(session history)</a>和<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Document" target="_blank" rel="noopener noreferrer">DOM树</a>。</li>
<li>包含嵌入内容的浏览上下文称为<em>父级浏览上下文</em>。顶级浏览上下文（没有父级）通常是由 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window" target="_blank" rel="noopener noreferrer"><code>Window</code></a> 对象表示的浏览器窗口。</li>
</ul>
</li>
<li>
<p>属性： iframe的特性都是依赖参数配置实现：</p>
<table>
<thead>
<tr>
<th>属性</th>
<th>解释</th>
<th>备注</th>
</tr>
</thead>
<tbody>
<tr>
<td>allow</td>
<td>可以为iframe指定<a href="https://link.juejin.cn/?target=https%3A%2F%2Fdeveloper.mozilla.org%2Fen-US%2Fdocs%2FWeb%2FHTTP%2FFeature_Policy" target="_blank" rel="noopener noreferrer">特性策略</a></td>
<td></td>
</tr>
<tr>
<td>allowfullscreen</td>
<td>是否允许iframe调用requestFullscreen()方法激活全屏模式，这个属性等同于allow属性的这个配置：<code>allow=&quot;fullscreen&quot;</code></td>
<td></td>
</tr>
<tr>
<td>allowpaymentrequest</td>
<td>是否允许一个跨域的iframe调用<a href="https://link.juejin.cn/?target=https%3A%2F%2Fdeveloper.mozilla.org%2Fen-US%2Fdocs%2FWeb%2FAPI%2FPayment_Request_API" target="_blank" rel="noopener noreferrer">支付请求API</a></td>
<td></td>
</tr>
<tr>
<td>csp</td>
<td>内嵌的资源强制实行同源策略</td>
<td></td>
</tr>
<tr>
<td>height</td>
<td>iframe的高度，默认150px</td>
<td></td>
</tr>
<tr>
<td>importance</td>
<td>标识在iframe属性src指示的资源的下载优先级，有<code>auto</code>/<code>high</code>/<code>low</code>三个等级</td>
<td></td>
</tr>
<tr>
<td>name</td>
<td>内嵌的浏览内容的目标名称</td>
<td></td>
</tr>
<tr>
<td>referrerpolicy</td>
<td>指示当获取frame的资源的时候携带的referrer，默认是<code>no-referrer-when-downgrade</code>，也就是仅当发生协议降级（如 HTTPS 页面引入 HTTP 资源，从 HTTPS 页面跳到 HTTP 等）时不发送 Referrer 信息。这个规则是现在大部分浏览器默认所采用的；</td>
<td></td>
</tr>
<tr>
<td>sandbox</td>
<td>在frame上的内容上运用额外的一下限制: 1. script脚本不能执行；2. 不能发送ajax请求；3. 不能使用本地存储，即localStorage,cookie等； 4. 不能创建新的弹窗和window； 5. 不能发送表单 6. 不能加载额外插件比如flash等</td>
<td></td>
</tr>
</tbody>
</table>
</li>
<li>
<p>优点：</p>
<ul>
<li>iframe能够原封不动的把嵌入的网页展现出来。</li>
<li>如果有多个网页引用iframe，那么你只需要修改iframe的内容，就可以实现调用的每一个页面内容的更改，方便快捷。</li>
<li>网页如果为了统一风格，头部和版本都是一样的，就可以写成一个页面，用iframe来嵌套，可以增加代码的可重用。</li>
<li>如果遇到加载缓慢的第三方内容如图标和广告，这些问题可以由iframe来解决。</li>
</ul>
</li>
<li>
<p>缺点：</p>
<ul>
<li>iframe会阻塞主页面的onload事件；动态设置<code>src</code>可解决这个问题。</li>
<li>iframe和主页面共享连接池，而浏览器对相同域的连接有限制，所以会影响页面的并行加载。会产生很多页面，不容易管理。</li>
<li>iframe框架页面会增加服务器的http请求。加载了新页面，增加了<code>css</code>与<code>js</code>文件的请求，即额外增加了<code>HTTP</code>请求。</li>
<li>不利于搜索引擎优化（SEO）。现在的搜索引擎爬虫还不能很好的处理iframe中的内容。</li>
<li>iframe 如果框架个数多的话，可能会出现上下、左右滚动条，会分散访问者的注意力，用户体验度差。</li>
<li>设备兼容性差。很多的移动设备无法完全显示框架</li>
</ul>
</li>
<li>
<p>应用：</p>
<ul>
<li>实现Ajax：可以使用<code>iframe</code>进行实现异步请求发送，来模拟<code>Ajax</code>的请求操作，<code>Ajax</code>的异步请求完成操作为<code>XHR.readyState === 4</code>执行<code>callback</code>，<code>iframe</code>使用<code>iframe.onload</code>执行<code>callback</code>，还可以实现一个轮询长连接
<ul>
<li>现在基本上都是用Ajax来代替iframe，所以iframe已经渐渐的退出了前端开发。如果需要使用iframe，最好是通过javascript动态给iframe添加src属性值</li>
</ul>
</li>
<li>加载广告：广告是与原文无关的，假如硬编码进去，会造成网页布局的紊乱,而且这样势必需要引入额外的<code>css</code>和<code>js</code>文件，极大的降低了网页的安全性，使用<code>iframe</code>便可以解决这些问题。</li>
<li>提交表单：可以使用<code>iframe</code>提交表单来避免整个页面的刷新，还可以实现无刷新文件上传的操作。</li>
</ul>
</li>
<li>
<p>常见问题：</p>
</li>
</ul>
<p><strong>参考资料</strong></p>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/iframe" target="_blank" rel="noopener noreferrer"><code>&lt;iframe&gt;</code> MDN</a></p>
<h3 id="bom"> BOM</h3>
<p><img src="./../images/bom.png" alt="" /></p>
<h3 id="dom"> DOM</h3>
<p><img src="./../images/dom.png" alt="" /></p>
<h3 id="dom事件分类"> DOM事件分类</h3>
<p><img src="./../images/event.png" alt="" /></p>
<h3 id="事件流"> 事件流</h3>
<p>事件流是网页元素接收事件的顺序，&quot;DOM2级事件&quot;规定的事件流包括三个阶段：</p>
<ul>
<li>事件捕获阶段</li>
<li>处于目标阶段</li>
<li>事件冒泡阶段</li>
</ul>
<p><img src="./../images/event-io.png" alt="event-io" /></p>
<ul>
<li>例题：</li>
</ul>
<p><img src="./../images/event-demo.png" alt="" /></p>
<h3 id="事件是如果实现的"> 事件是如果实现的？</h3>
<p>基于<strong>发布订阅模式</strong>，就是在浏览器加载的时候会读取事件相关的代码，但是只有实际等到具体的事件触发的时候才会执行。</p>
<p>比如点击按钮，这是个事件（Event），而负责处理事件的代码段通常被称为事件处理程序（EventHandler），也就是「启动对话框的显示」这个动作。</p>
<p>在 Web 端，我们常见的就是 DOM 事件：</p>
<ul>
<li>DOM0 级事件，直接在 html 元素上绑定 on-event，比如 onclick，取消的话，dom.onclick =null，同一个事件只能有一个处理程序，后面的会覆盖前面的。</li>
<li>DOM2 级事件，通过 addEventListener 注册事件，通过 removeEventListener 来删除事件，一个事件可以有多个事件处理程序，按顺序执行，捕获事件和冒泡事件。</li>
<li>DOM3级事件，增加了事件类型，比如 UI 事件，焦点事件，鼠标事件。</li>
</ul>
<h3 id="事件委托-代理"> 事件委托(代理)</h3>
<ul>
<li>
<p>概念：事件委托本质上是利用了浏览器事件冒泡的机制。因为事件在冒泡过程中会上传到父节点，并且父节点可以通过事件对象获取到目标节点，把子节点的监听函数定义在父节点上，由父节点的监听函数统一处理多个子元素的事件，这种方式称为事件代理。</p>
</li>
<li>
<p>为什么要用事件委托？</p>
<ul>
<li>使用事件代理可以不必要为每一个子元素都绑定一个监听事件，这样减少了内存上的消耗和减少DOM操作。
<ul>
<li>每个函数都是一个对象，是对象就会占用内存，对象越多，内存占用率越大。</li>
<li>在JavaScript中，添加到页面上的事件处理程序数量将直接关系到页面的整体运行性能，因为需要不断的与dom节点进行交互，访问dom的次数越多，引起浏览器<strong>重绘与重排</strong>的次数也就越多，就会延长整个页面的交互就绪时间；</li>
</ul>
</li>
<li>使用事件代理可以实现事件的动态绑定。
<ul>
<li>比如：新增了一个子节点，我们并不需要单独地为它添加一个监听事件，它所发生的事件会交给父元素中的监听函数来处理</li>
</ul>
</li>
</ul>
</li>
<li>
<p>优点：</p>
<ul>
<li>动态添加和修改元素，减少dom事件绑定的次数。将事件绑定到父节点上，通过事件冒泡获取到实际点击的叶子节点。</li>
<li>减少管理的函数。不用为每个元素都添加监听函数。同一个父节点下面类似的子元素，可以通过委托给父元素的监听函数来处理事件。</li>
<li>减少 JavaScript 和 DOM 节点之间的关联，这样也就减少了因循环引用而带来的内存泄漏发生的概率。</li>
</ul>
</li>
</ul>
<ul>
<li>
<p>应用：</p>
<blockquote>
<p>实现事件代理的效果跟直接给节点的事件效果相同。如：列表中点击 <code>&lt;li&gt;</code> 触发事件使用事件代理实现</p>
</blockquote>
<p>解决方案:</p>
<ul>
<li>
<p>Event 对象提供了一个属性叫 target，可以返回事件的目标节点；标准浏览器用ev.target，IE浏览器用event.srcElement；通过 target.nodeName 获取签名。</p>
</li>
<li>
<p>根据标签名称在 <code>&lt;li&gt;</code> 父节点获取 <code>target.nodeName</code> 判断子节点类型，分类处理。</p>
<div><pre><code>window<span>.</span><span>onload</span> <span>=</span> <span>function</span><span>(</span><span>)</span><span>{</span>
        <span>let</span> myul <span>=</span> document<span>.</span><span>getElementById</span><span>(</span><span>'ul'</span><span>)</span><span>;</span>
        myul<span>.</span><span>onclick</span> <span>=</span> <span>function</span><span>(</span><span>ev</span><span>)</span><span>{</span> <span>// 标准浏览器ev ie是event</span>
            <span>let</span> ev <span>=</span> ev <span>||</span> window<span>.</span>event <span>// 兼容ie</span>
            <span>let</span> target <span>=</span> ev<span>.</span>target <span>||</span> ev<span>.</span>srcElement
            <span>switch</span><span>(</span> target<span>.</span>nodeName<span>.</span><span>toLowerCase</span><span>(</span><span>)</span><span>)</span><span>{</span>
                <span>case</span> <span>'li'</span><span>:</span> <span>//do somthing...</span>
            <span>}</span>
        <span>}</span>
    <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div></li>
</ul>
</li>
</ul>
<hr>
<h2 id="js-兼容性"> JS 兼容性</h2>
<ul>
<li>兼容性产生原因：浏览器内核不同，不同 js 引擎对 js 的实现存在差异
<ul>
<li>Trident ['traɪd(ə)nt] 内核（IE 内核）：IE、浏览器兼容模式</li>
<li>Blink 内核（Chromium 内核）：Chrome、Opera</li>
<li>Gecko ['gekəʊ] 内核（FireFox 内核）：FireFox</li>
<li>Webkit 内核：Safari</li>
</ul>
</li>
<li>目前主要是 IE 浏览器与其他浏览 firefox 浏览器的差异。</li>
</ul>
<h3 id="兼容性问题"> 兼容性问题</h3>
<ul>
<li>const
<ul>
<li>现有问题：在IE中不能使用<code>const</code>关键字。如<code>const constVar = 32;</code>在IE中这是语法错误。</li>
<li>解决方法：不使用 <code>const</code>，以 <code>var</code> 代替。</li>
</ul>
</li>
<li>多余的逗号</li>
<li>现有问题：firefox 中对象文字常量容许多余的逗号，在IE中不允许。下面语句在IE中非法: <code>var obj = { ‘key’ : ‘aaa’, }</code></li>
<li>解决方法：去掉多余逗号。</li>
</ul>
<p><strong>元素查找问题</strong></p>
<ul>
<li>
<p>document.all[name]</p>
<ul>
<li>现有问题：Firefox不支持document.all[name]</li>
<li>解决方法：使用getElementsByName(name)，getElementById(id)等来替代。</li>
</ul>
</li>
<li>
<p>集合类对象问题</p>
</li>
<li>
<p>现有问题：IE中对许多集合类对象取用时可以用 ()，但在Firefox只能用[]。</p>
<ul>
<li>如：IE中可以使用<code>document.forms(“formName”)</code>来返回名字为”formName”的Form，但在Firefox却行不通。</li>
</ul>
</li>
<li>
<p>解决方法：使用[]，上例中可以改为<code>document.forms[“formName”]</code></p>
</li>
<li>
<p>HTML元素的ID在<a href="http://lib.csdn.net/base/javascript" target="_blank" rel="noopener noreferrer">JavaScript</a>可见</p>
<ul>
<li>现有问题：IE中HTML元素中的ID可以作为document的下属对象变量名直接使用。在Firefox中不能。</li>
<li>解决方法：使用 <code>getElementById(“idName”)</code>代替 idName 作为对象变量使用。</li>
</ul>
</li>
<li>
<p><code>eval(idName)</code>取得对象</p>
<ul>
<li>现有问题：在IE中，利用eval(idName)可以取得ID为idName的HTML对象，在Firefox中不能。</li>
<li>解决方法：用 <code>getElementById(idName)</code> 代替 <code>eval(idName)</code>。</li>
</ul>
</li>
<li>
<p>变量名与某HTML对象ID相同</p>
<ul>
<li>现有问题：在Firefox中，因为对象ID不作为HTML对象的名称，所以可以使用与HTML对象id相同的变量名，IE中不能。</li>
<li>解决方法：在声明变量时，一律加上var，以避免歧义，这样在IE中亦可正常运行。此外，最好不要取与HTML对象id相同的变量名，以减少错误。</li>
<li>注：3、4和5都属于同一类的问题。</li>
</ul>
</li>
<li>
<p>Frame</p>
<ul>
<li>现有问题：在IE中可以用 <code>window.top.frameId</code> 和 <code>window.top.frameName</code> 来得到该 Frame 所代表的 Window，Firefox中只能用 <code>window.top.frameName</code>。</li>
<li>解决方法：将 Frame 的Id和Name设置成相同，使用 <code>window.top.frameName</code> 来访问Frame。</li>
</ul>
</li>
</ul>
<p><strong>DOM 操作</strong></p>
<ul>
<li>
<p>创建DOM</p>
<ul>
<li>
<p>现有问题：IE 使用 ActiveXObject() 创建 DOM， firefox 使用 <code>document.implementation.createDocument</code></p>
</li>
<li>
<p>解决方法:</p>
<div><pre><code><span>function</span> <span>createXmlDom</span><span>(</span><span>)</span> <span>{</span>
    <span>var</span> oXmlDom<span>;</span>
    <span>if</span> <span>(</span>Window<span>.</span>ActiveXObject<span>)</span> <span>{</span> <span>// IE</span>
     oXmlDom <span>=</span> <span>new</span> <span>ActiveXObject</span><span>(</span><span>"Microsoft.XmlDom"</span><span>)</span><span>;</span>
    <span>}</span> <span>else</span> <span>{</span> <span>// Firefox</span>
     oXmlDom <span>=</span> document<span>.</span>implementation<span>.</span><span>createDocument</span><span>(</span><span>""</span><span>,</span> <span>""</span><span>,</span> <span>null</span><span>)</span><span>;</span>
    <span>}</span>
   <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div></li>
</ul>
</li>
<li>
<p>设置元素的文本内容。</p>
<ul>
<li>
<p>现有问题：IE使用innerText，而Firefox使用 textContent 来设置元素文本内容。</p>
</li>
<li>
<p>解决方法：如果文本内容不包含 <code>&lt;</code> 和 <code>&gt;</code> 等特殊字符，可以使用 innerHTML。否则，可以使用：</p>
<ul>
<li>
<div><pre><code><span>var</span> child <span>=</span> elem<span>.</span>firstChild<span>;</span>
<span>if</span> <span>(</span>child <span>!=</span> <span>null</span><span>)</span> elem<span>.</span><span>removeChild</span><span>(</span>child<span>)</span><span>;</span>
elem<span>.</span><span>appendChild</span><span>(</span>document<span>.</span><span>createTextNode</span><span>(</span>content<span>)</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
<li>
<p>parentElement，parent.children</p>
<ul>
<li>现有问题：IE可以使用 parentElement 获得父结点，parent.children 得到结点的所有孩子结点。Firefox不支持。</li>
<li>解决方法：使用 parentNode 和 parent.childNodes。</li>
</ul>
</li>
<li>
<p>对childNodes的解释。</p>
</li>
<li>
<p>现有问题：IE和Firefox中对childNodes的解释不同，IE不会包含空白文本结点，而Firefox会包含。</p>
</li>
<li>
<p>解决方法：使用childNodes过滤文本结点，如下：</p>
<div><pre><code>  <span>var</span> children <span>=</span> elem<span>.</span>childNodes<span>;</span>
  <span>for</span> <span>(</span>i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> children<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
   <span>if</span> <span>(</span>children<span>[</span>i<span>]</span><span>.</span>nodeType <span>!=</span> <span>3</span><span>)</span> <span>{</span> <span>// 过滤文本结点</span>
    <span>// ...</span>
   <span>}</span>
  <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div></li>
<li>
<p>对 <code>document.getElementsByName</code> 的解释。</p>
<ul>
<li>现有问题：IE中 <code>getElementsByName</code> 只会检查和元素，而在Firefox下会检查所有元素。</li>
<li>解决方法：不要使用 <code>getElementsByName</code> 检查除和之外的元素，如果要获得单个元素，尽量使用 <code>getElementById</code>。</li>
</ul>
</li>
<li>
<p>对 <code>document.getElementById</code> 的解释。</p>
<ul>
<li>现有问题：IE中 <code>getElementById</code> 不仅检查Id属性，也会检查Name属性，当Name属性匹配参数时也会返回该元素。而在Firefox中只会检查Id属性。</li>
<li>解决方法：尽量保持Id和Name相同，不要让一个元素name属性和另一个元素的id属性相同。</li>
</ul>
</li>
</ul>
<p><strong>事件</strong></p>
<ul>
<li>
<p>event.x与event.y问题</p>
<ul>
<li>
<p>现有问题：在IE中，event对象有x,y 属性，Firefox中没有。</p>
</li>
<li>
<p>解决方法：在Firefox中，与event.x 等效的是 event.pageX。可以使用：</p>
</li>
<li>
<div><pre><code>mX <span>=</span> event<span>.</span>x <span>?</span> event<span>.</span>x <span>:</span> event<span>.</span>pageX<span>;</span>
</code></pre>
<div><span>1</span><br></div></div></li>
</ul>
</li>
<li>
<p>获取事件及事件对象目标</p>
<ul>
<li>
<p>存在问题：</p>
<ul>
<li>在IE中，event对象为window的属性，作用于全局作用域，而在Firefox中，event对象是最为事件的参数存在的</li>
<li>在IE中通过event对象的srcElement属性访问，而在火狐中通过event对象的target属性访问。</li>
</ul>
</li>
<li>
<p>解决方案：</p>
</li>
<li>
<div><pre><code><span>//获取事件对象的兼容性写法</span>
 <span>getEvent</span><span>:</span> <span>function</span><span>(</span><span>event</span><span>)</span><span>{</span>
  <span>return</span> event <span>?</span> event <span>:</span> window<span>.</span>event<span>;</span>
 <span>}</span><span>,</span>
 <span>//获取事件对象目标的兼容性写法</span>
 <span>getTarget</span><span>:</span> <span>function</span><span>(</span><span>event</span><span>)</span><span>{</span>
  <span>return</span> event<span>.</span>target <span>||</span> event<span>.</span>srcElement<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div></li>
</ul>
</li>
<li>
<p>添加事件方法</p>
<ul>
<li>
<p>现有问题：IE中使用attachEvent来添加事件，Firefox中使用addEventListener。</p>
</li>
<li>
<p>解决方法：如下，注意事件参数的区别，一个是click，一个是onclick。</p>
</li>
<li>
<div><pre><code>addHandler：<span>function</span><span>(</span><span>element<span>,</span>type<span>,</span>handler</span><span>)</span><span>{</span>
 <span>if</span><span>(</span>element<span>.</span>addEventListener<span>)</span><span>{</span><span>//检测是否为DOM2级方法</span>
  element<span>.</span><span>addEventListener</span><span>(</span>type<span>,</span> handler<span>,</span> <span>false</span><span>)</span><span>;</span>
 <span>}</span><span>else</span> <span>if</span> <span>(</span>element<span>.</span>attachEvent<span>)</span><span>{</span><span>//检测是否为IE级方法</span>
  element<span>.</span><span>attachEvent</span><span>(</span><span>"on"</span> <span>+</span> type<span>,</span> handler<span>)</span><span>;</span>
 <span>}</span> <span>else</span> <span>{</span><span>//检测是否为DOM0级方法</span>
  element<span>[</span><span>"on"</span> <span>+</span> type<span>]</span> <span>=</span> handler<span>;</span>
 <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div></li>
</ul>
</li>
<li>
<p>移除事件方法</p>
<ul>
<li>
<p>问题：IE中使用detachEvent来移除事件，Firefox中使用 removeEventListener。</p>
</li>
<li>
<p>解决方法：</p>
<div><pre><code>removeHandler：<span>function</span><span>(</span><span>element<span>,</span> type<span>,</span> handler</span><span>)</span><span>{</span>
  <span>if</span> <span>(</span>element<span>.</span>removeEventListener<span>)</span><span>{</span>
   element<span>.</span><span>removeEventListener</span><span>(</span>type<span>,</span> handler<span>,</span> <span>false</span><span>)</span><span>;</span>
  <span>}</span> <span>else</span> <span>if</span> <span>(</span>element<span>.</span>detachEvent<span>)</span><span>{</span>
   element<span>.</span><span>detachEvent</span><span>(</span><span>"on"</span> <span>+</span> type<span>,</span> handler<span>)</span><span>;</span>
  <span>}</span> <span>else</span> <span>{</span>
   element<span>[</span><span>"on"</span> <span>+</span> type<span>]</span> <span>=</span> <span>null</span><span>;</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div></li>
</ul>
</li>
<li>
<p>阻止浏览器默认事件的兼容性写法：</p>
<div><pre><code><span>preventDefault</span><span>:</span> <span>function</span><span>(</span><span>event</span><span>)</span><span>{</span>
  <span>if</span> <span>(</span>event<span>.</span>preventDefault<span>)</span><span>{</span>
   event<span>.</span><span>preventDefault</span><span>(</span><span>)</span><span>;</span>
  <span>}</span> <span>else</span> <span>{</span>
   event<span>.</span>returnValue <span>=</span> <span>false</span><span>;</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
<li>
<p>阻止事件冒泡的兼容性写法:</p>
<div><pre><code><span>stopPropagation</span><span>:</span> <span>function</span><span>(</span><span>event</span><span>)</span><span>{</span>
  <span>if</span> <span>(</span>event<span>.</span>stopPropagation<span>)</span><span>{</span>
   event<span>.</span><span>stopPropagation</span><span>(</span><span>)</span><span>;</span>
  <span>}</span> <span>else</span> <span>{</span>
   event<span>.</span>cancelBubble <span>=</span> <span>true</span><span>;</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
<li>
<p>mouseover和mouseout 事件才包含的获取相关元素的方法:</p>
<div><pre><code><span>//mouseover和mouseout 事件才包含的获取相关元素的方法</span>
<span>getRelatedTarget</span><span>:</span> <span>function</span><span>(</span><span>event</span><span>)</span><span>{</span>
 <span>if</span> <span>(</span>event<span>.</span>relatedTarget<span>)</span><span>{</span>
  <span>return</span> event<span>.</span>relatedTarget<span>;</span>
 <span>}</span> <span>else</span> <span>if</span> <span>(</span>event<span>.</span>toElement<span>)</span><span>{</span>
  <span>return</span> event<span>.</span>toElement<span>;</span>
 <span>}</span> <span>else</span> <span>if</span> <span>(</span>event<span>.</span>fromElement<span>)</span><span>{</span>
  <span>return</span> event<span>.</span>fromElement<span>;</span>
 <span>}</span> <span>else</span> <span>{</span>
  <span>return</span> <span>null</span><span>;</span>
 <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div></li>
<li>
<p>鼠标滚轮判断 :</p>
<ul>
<li>
<p>对于mousedown 和mouseup 事件来说，则在其 event 对象存在一个button 属性， 表示按下或释放的按钮。</p>
</li>
<li>
<p>DOM的button 属性可能有如下3 个值：0 表示主鼠标按钮，1 表示中间的鼠 标按钮（鼠标滚轮按钮），2 表示次鼠标按钮。</p>
<ul>
<li>在常规的设置中，主鼠标按钮就是鼠标左键，而次鼠标 按钮就是鼠标右键。</li>
</ul>
</li>
<li>
<p>IE8 及之前版本也提供了button 属性，但这个属性的值与DOM 的button 属性有很大差异。</p>
<ul>
<li>0：表示没有按下按钮。</li>
<li>1：表示按下了主鼠标按钮。</li>
<li>2：表示按下了次鼠标按钮。</li>
<li>3：表示同时按下了主、次鼠标按钮。</li>
<li>4：表示按下了中间的鼠标按钮。</li>
<li>5：表示同时按下了主鼠标按钮和中间的鼠标按钮。</li>
<li>6：表示同时按下了次鼠标按钮和中间的鼠标按钮。</li>
<li>7：表示同时按下了三个鼠标按钮。</li>
</ul>
</li>
<li>
<p>解决方案：</p>
<div><pre><code>
<span>getButton</span><span>:</span> <span>function</span><span>(</span><span>event</span><span>)</span><span>{</span>
 <span>if</span> <span>(</span>document<span>.</span>implementation<span>.</span><span>hasFeature</span><span>(</span><span>"MouseEvents"</span><span>,</span> <span>"2.0"</span><span>)</span><span>)</span><span>{</span>
  <span>return</span> event<span>.</span>button<span>;</span>
 <span>}</span> <span>else</span> <span>{</span>
  <span>switch</span><span>(</span>event<span>.</span>button<span>)</span><span>{</span>
   <span>case</span> <span>0</span><span>:</span>
   <span>case</span> <span>1</span><span>:</span>
   <span>case</span> <span>3</span><span>:</span>
   <span>case</span> <span>5</span><span>:</span>
   <span>case</span> <span>7</span><span>:</span>
   <span>return</span> <span>0</span><span>;</span>
   <span>case</span> <span>2</span><span>:</span>
   <span>case</span> <span>6</span><span>:</span>
   <span>return</span> <span>2</span><span>;</span>
   <span>case</span> <span>4</span><span>:</span>
   <span>return</span> <span>1</span><span>;</span>
  <span>}</span>
 <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br></div></div></li>
<li>
<p>取得鼠标滚轮增量值（delta）的方法:</p>
<div><pre><code><span>getWheelDelta</span><span>:</span> <span>function</span><span>(</span><span>event</span><span>)</span><span>{</span>
 <span>if</span> <span>(</span>event<span>.</span>wheelDelta<span>)</span><span>{</span>
  <span>return</span> <span>(</span>client<span>.</span>engine<span>.</span>opera <span>&amp;&amp;</span> client<span>.</span>engine<span>.</span>opera <span>&lt;</span> <span>9.5</span> <span>?</span>
   <span>-</span>event<span>.</span>wheelDelta <span>:</span> event<span>.</span>wheelDelta<span>)</span><span>;</span>
 <span>}</span> <span>else</span> <span>{</span>
  <span>return</span> <span>-</span>event<span>.</span>detail <span>*</span> <span>40</span><span>;</span><span>//firefox中的值为+3表示向上滚，-3表示向下滚</span>
 <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div></li>
</ul>
</li>
<li>
<p>跨浏览器的方式取得字符编码:</p>
<div><pre><code><span>getCharCode</span><span>:</span> <span>function</span><span>(</span><span>event</span><span>)</span><span>{</span>
 <span>if</span> <span>(</span><span>typeof</span> event<span>.</span>charCode <span>==</span> <span>"number"</span><span>)</span><span>{</span>
  <span>return</span> event<span>.</span>charCode<span>;</span>
 <span>}</span> <span>else</span> <span>{</span>
  <span>return</span> event<span>.</span>keyCode<span>;</span>
 <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
<li>
<p>访问剪贴板中的数据:</p>
<div><pre><code><span>getClipboardText</span><span>:</span> <span>function</span><span>(</span><span>event</span><span>)</span><span>{</span>
  <span>var</span> clipboardData <span>=</span> <span>(</span>event<span>.</span>clipboardData <span>||</span> window<span>.</span>clipboardData<span>)</span><span>;</span>
  <span>return</span> clipboardData<span>.</span><span>getData</span><span>(</span><span>"text"</span><span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br></div></div></li>
<li>
<p>设置剪贴板中的数据:</p>
<div><pre><code><span>setClipboardText</span><span>:</span> <span>function</span><span>(</span><span>event<span>,</span> value</span><span>)</span><span>{</span>
  <span>if</span> <span>(</span>event<span>.</span>clipboardData<span>)</span><span>{</span>
   <span>return</span> event<span>.</span>clipboardData<span>.</span><span>setData</span><span>(</span><span>"text/plain"</span><span>,</span> value<span>)</span><span>;</span>
  <span>}</span> <span>else</span> <span>if</span> <span>(</span>window<span>.</span>clipboardData<span>)</span><span>{</span>
   <span>return</span> window<span>.</span>clipboardData<span>.</span><span>setData</span><span>(</span><span>"text"</span><span>,</span> value<span>)</span><span>;</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
</ul>
<p><strong>XML 兼容性</strong></p>
<ul>
<li>
<p>创建XMLHttpRequest</p>
<ul>
<li>
<p>现有问题：Firefox使用XMLHttpRequest，IE使用ActiveXObject。</p>
</li>
<li>
<p>解决方法：</p>
</li>
<li>
<div><pre><code><span>if</span> <span>(</span>window<span>.</span>XMLHttpRequest<span>)</span> <span>{</span>
   req <span>=</span> <span>new</span> <span>XMLHttpRequest</span><span>(</span><span>)</span><span>;</span>
 <span>}</span> <span>else</span> <span>if</span> <span>(</span>window<span>.</span>ActiveXObject<span>)</span> <span>{</span>
   req <span>=</span> <span>new</span> <span>ActiveXObject</span><span>(</span><span>"Microsoft.XMLHTTP"</span><span>)</span><span>;</span>
 <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div></li>
</ul>
</li>
<li>
<p>加载XML</p>
<ul>
<li>
<p>现有问题：如果要加载外部文件IE和Firefox都可以用:</p>
<div><pre><code>oXmlDom<span>.</span>async<span>=</span><span>false</span><span>;</span>      <span>// 这在Firefox中是必须的</span>
oXmlDom<span>.</span><span>load</span><span>(</span><span>"test.xml"</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div><p>但是它们加载xml字符串的方式不一样，IE中直接可以使用 <code>oXmlDom.loadXML(&quot;&lt;root&gt;&lt;child/&gt;&lt;/root&gt;&quot;)</code>，而Firefox要使用DOMParser:</p>
<div><pre><code><span>var</span> oParser <span>=</span> <span>new</span> <span>DOMParser</span><span>(</span><span>)</span><span>;</span>
<span>var</span> oXmlDom <span>=</span> oParser<span>.</span><span>parseFromString</span><span>(</span><span>"&lt;root/>"</span><span>,</span> <span>"text/xml"</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div></li>
<li>
<p>解决方法：比较好的方法是给Firefox产生的XMLDom加上loadXML方法：</p>
<div><pre><code><span>if</span> <span>(</span>isFirefox<span>)</span> <span>{</span> <span>// 需要浏览器检测</span>
 <span>Document</span><span>.</span>prototype<span>.</span><span>loadXML</span> <span>=</span> <span>function</span><span>(</span><span>sXml</span><span>)</span> <span>{</span>
  <span>var</span> oParser <span>=</span> <span>new</span> <span>DOMParser</span><span>(</span><span>)</span><span>;</span>
  <span>var</span> oXmlDom <span>=</span> oParser<span>.</span><span>parseFromString</span><span>(</span>sXml<span>,</span> <span>"text/xml"</span><span>)</span><span>;</span>
  <span>while</span> <span>(</span><span>this</span><span>.</span>firstChild<span>)</span> <span>this</span><span>.</span><span>removeChild</span><span>(</span><span>this</span><span>.</span>firstChild<span>)</span><span>;</span>
  <span>for</span> <span>(</span><span>var</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> oXmlDom<span>.</span>childNodes<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
   <span>var</span> oNewNode <span>=</span> <span>this</span><span>.</span><span>importNode</span><span>(</span>oXmlDom<span>.</span>childNodes<span>[</span>i<span>]</span><span>,</span> <span>true</span><span>)</span><span>;</span>
   <span>this</span><span>.</span><span>appendChild</span><span>(</span>oNewNode<span>)</span><span>;</span>
  <span>}</span>
 <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><p>这样在IE和Firefox就可以调用loadXML方法了。</p>
</li>
</ul>
</li>
</ul>
<h3 id="兼容性统一处理方案"> 兼容性统一处理方案</h3>
<hr>
<h2 id="原理实现"> 原理实现</h2>
<h3 id="实现-new-操作符"> 实现 new 操作符</h3>
<p>new操作符作用: 创建一个给定构造函数的实例</p>
<ul>
<li>原理
<ul>
<li>（1）创建一个空对象；</li>
<li>（2）将空对象的[prototype]原型指向构造函数的原型对象；</li>
<li>（3）将构造函数的执行上下文this绑定到空对象；</li>
<li>（4）返回空对象</li>
</ul>
</li>
</ul>
<div><pre><code><span>/**

 \* @param {Function} construct 构造函数

 \* @param {any} args  其余参数

 \* @return obj 绑定了构造函数上下文的空对象

 */</span>

<span>function</span> <span>myNew</span><span>(</span><span>construct<span>,</span> <span>...</span>args</span><span>)</span> <span>{</span>

  <span>let</span> obj <span>=</span> <span>{</span><span>}</span> <span>//创建空对象</span>

  obj<span>.</span>__proto__ <span>=</span> construct<span>.</span>prototype <span>//将空对象的[prototype]原型指向构造函数的原型对象</span>

  <span>construct</span><span>.</span><span>apply</span><span>(</span>obj<span>,</span> args<span>)</span> <span>//将构造函数的执行上下文this绑定到空对象</span>

  <span>return</span> obj

<span>}</span>

<span>//-----------------------------------------使用-------------------------------------------------------</span>

<span>function</span> <span>Person</span><span>(</span><span>name<span>,</span> age</span><span>)</span> <span>{</span>

  <span>this</span><span>.</span>name <span>=</span> name

  <span>this</span><span>.</span>age <span>=</span> age

<span>}</span>

<span>let</span> person1 <span>=</span> <span>myNew</span><span>(</span>Person<span>,</span> <span>'joker'</span><span>,</span> <span>20</span><span>)</span>

console<span>.</span><span>log</span><span>(</span><span><span>`</span><span>name is </span><span><span>${</span>person1<span>.</span>name<span>}</span></span><span>, age is </span><span><span>${</span>person1<span>.</span>age<span>}</span></span><span>`</span></span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br></div></div><h3 id="实现-instanceof-操作符"> 实现 instanceof 操作符</h3>
<ul>
<li>
<p>instanceof操作符号作用:  a instanceof B 用于判断a实例是否为B对象的原型</p>
</li>
<li>
<p>原理</p>
<ul>
<li>(1)获取对象的原型prototype；</li>
<li>(2)获取实例的__proto__属性；</li>
<li>(3) 判断实例__proto__是否等于对象的prototype，相等返回true，否则获取实例原型的原型属性(<strong>proto</strong>.<strong>proto</strong>)；继续判断... 如果循环，直到实例的__proto__属性返回null，则表示退出循环</li>
</ul>
</li>
</ul>
<p><strong>实现一</strong></p>
<div><pre><code><span>/**

 \* 递归实现instanceof

 \* @param {Object} inst 实例

 \* @param {obj} 对象  其余参数

 \* @return true || false

 */</span>

<span>function</span> <span>myInstanceof</span><span>(</span><span>inst<span>,</span> obj</span><span>)</span> <span>{</span>

  <span>let</span> proto <span>=</span> inst<span>.</span>__proto__ <span>//获取实例的__proto__属性</span>

  <span>if</span> <span>(</span>proto<span>)</span> <span>{</span> <span>//如果实例__proto__属性存在，则沿着__proto__原型链查找</span>

    <span>if</span> <span>(</span>proto <span>===</span> obj<span>.</span>prototype<span>)</span> <span>{</span> <span>//在实例原型链中找到d对象的prototype对象</span>

      <span>return</span> <span>true</span>

    <span>}</span> <span>else</span> <span>{</span> <span>//沿着__proto__原型链递归查找</span>

      <span>return</span> <span>myInstanceof</span><span>(</span>proto<span>,</span> obj<span>)</span>

    <span>}</span>

  <span>}</span> <span>else</span> <span>{</span> <span>//已经查找达到原型链顶部，不存在对象的prototype对象</span>

    <span>return</span> <span>false</span>

  <span>}</span>

<span>}</span>



<span>/**

 \* 非递归实现instanceof

 \* 分析：非递归版实现可以通过递归版本转换，递归转换非递归实现核心为需要寻找函数的出口条件，没有找到一直循环查找

 \* @param {Object} inst 实例

 \* @param {obj} 对象  其余参数

 \* @return true || false

 */</span>

<span>function</span> <span>myInstanceof</span><span>(</span><span>inst<span>,</span> obj</span><span>)</span> <span>{</span>

  <span>let</span> proto <span>=</span> inst<span>.</span>__proto__ <span>//获取实例的__proto__属性</span>

  <span>while</span> <span>(</span>proto <span>!=</span> <span>null</span><span>)</span> <span>{</span> <span>//出口条件: 实例的__proto__为null，当实例__proto__为null时表示查找到实例原型链的最顶端</span>

​    <span>if</span> <span>(</span>proto <span>===</span> obj<span>.</span>prototype<span>)</span> <span>{</span> <span>//在实例的原型链中查找对象的prototype对象</span>

​      <span>return</span> <span>true</span>

​    <span>}</span>

​    proto <span>=</span> proto<span>.</span>__proto__ <span>//沿着实例的原型链查找</span>

  <span>}</span>

  <span>return</span> <span>false</span>

<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br></div></div><p><strong>实现二</strong></p>
<div><pre><code><span>/*
 * --- 手动实现 instanceof ---
 */</span>

<span>function</span> <span>newInstanceOf</span> <span>(</span><span>leftValue<span>,</span> rightValue</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span><span>typeof</span> leftValue <span>!==</span> <span>'object'</span> <span>||</span> rightValue <span>==</span> <span>null</span><span>)</span> <span>{</span> 
        <span>return</span> <span>false</span><span>;</span>
    <span>}</span>
    
    <span>let</span> rightProto <span>=</span> rightValue<span>.</span>prototype<span>;</span>
    leftValue <span>=</span> leftValue<span>.</span>__proto__<span>;</span>
    
    <span>while</span> <span>(</span><span>true</span><span>)</span> <span>{</span>
        <span>if</span> <span>(</span>leftValue <span>===</span> <span>null</span><span>)</span> <span>return</span> <span>false</span><span>;</span>
        <span>if</span> <span>(</span>leftValue <span>===</span> rightProto<span>)</span> <span>return</span> <span>true</span><span>;</span>
        leftValue <span>=</span> leftValue<span>.</span>__proto__<span>;</span>
    <span>}</span>
<span>}</span>

<span>/*
 * ------ 验证 ------
 */</span>

<span>const</span> a <span>=</span> <span>[</span><span>]</span><span>;</span>
<span>const</span> b <span>=</span> <span>{</span><span>}</span><span>;</span>

<span>function</span> <span>Foo</span> <span>(</span><span>)</span> <span>{</span><span>}</span>

<span>var</span> c <span>=</span> <span>new</span> <span>Foo</span><span>(</span><span>)</span>
<span>function</span> <span>Child</span> <span>(</span><span>)</span> <span>{</span><span>}</span>
<span>function</span> <span>Father</span><span>(</span><span>)</span> <span>{</span><span>}</span>
<span>Child</span><span>.</span>prototype <span>=</span> <span>new</span> <span>Father</span><span>(</span><span>)</span>
<span>var</span> d <span>=</span> <span>new</span> <span>Child</span><span>(</span><span>)</span>

<span>//------测试用例-----</span>
console<span>.</span><span>log</span><span>(</span><span>newInstanceOf</span><span>(</span>a<span>,</span> Array<span>)</span><span>)</span> <span>// true</span>
console<span>.</span><span>log</span><span>(</span><span>newInstanceOf</span><span>(</span>b<span>,</span> Object<span>)</span><span>)</span> <span>// true</span>
console<span>.</span><span>log</span><span>(</span><span>newInstanceOf</span><span>(</span>b<span>,</span> Array<span>)</span><span>)</span> <span>// false</span>
console<span>.</span><span>log</span><span>(</span><span>newInstanceOf</span><span>(</span>a<span>,</span> Object<span>)</span><span>)</span> <span>// true</span>
console<span>.</span><span>log</span><span>(</span><span>newInstanceOf</span><span>(</span>c<span>,</span> Foo<span>)</span><span>)</span> <span>// true</span>
console<span>.</span><span>log</span><span>(</span><span>newInstanceOf</span><span>(</span>d<span>,</span> Child<span>)</span><span>)</span> <span>// true</span>
console<span>.</span><span>log</span><span>(</span><span>newInstanceOf</span><span>(</span>d<span>,</span> Father<span>)</span><span>)</span> <span>// true</span>
console<span>.</span><span>log</span><span>(</span><span>newInstanceOf</span><span>(</span><span>123</span><span>,</span> Object<span>)</span><span>)</span> <span>// false </span>
console<span>.</span><span>log</span><span>(</span><span>123</span> <span>instanceof</span> <span>Object</span><span>)</span> <span>// false</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br></div></div><blockquote>
<p>这个问题既考察了 instanceof 的原理，又考察了原型链，还考察了代码能力。</p>
<p>在实现代码中，我们判断 leftValue 是否为 rightValue 的实例，思想是在 leftValue 的原型链上，即 leftValue.<strong>proto</strong> 上寻找是否存在 rightValue.prototype。原理图如下:</p>
<p><img src="./../images/instanceof-demo.png" alt="" /></p>
</blockquote>
<h3 id="实现-object-create"> 实现 Object.create()</h3>
<ul>
<li>原理： 将传入的对象作为原型</li>
</ul>
<div><pre><code><span>function</span> <span>create</span><span>(</span><span>obj</span><span>)</span> <span>{</span>
  <span>function</span> <span>F</span><span>(</span><span>)</span> <span>{</span><span>}</span>
  <span>F</span><span>.</span>prototype <span>=</span> obj
  <span>return</span> <span>new</span> <span>F</span><span>(</span><span>)</span>

<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div><h3 id="实现-call"> 实现 <a href="https://developer.mozilla.org/zh-CN/search?q=call" target="_blank" rel="noopener noreferrer">call()</a></h3>
<ul>
<li>
<p>作用
在特定的作域中调用函数，实际上等于设置函数体内 this 对象的值，扩充函数的作用域</p>
</li>
<li>
<p>接收多个参数
第一个参数:在其中运行函数的作用域，一般为this
其余参数:传递给函数的参数必须逐个列举出来</p>
</li>
<li>
<p>返回值
使用调用者提供的 this 值和参数调用该函数的返回值。若该方法没有返回值，则返回 undefined</p>
</li>
<li>
<p>原理</p>
<ul>
<li>
<p>将&quot;调用call方法的函数&quot;的原型添加到call()方法第一个参数对象的作用域链上</p>
<blockquote>
<p>​	添加原型过程：</p>
<p>​	1.当函数调用call()方法时候传入第一个参数为调用者this绑定的对象，参数为空默认为全局对象，既window
​	2.因为call()方法是挂在到Function.prototype指向的对象(既Funtion的原型对象)内的,所以当一个函数调用call()方法时候，call()方法内部的this是指向调用者对象的
​	3.call()内部将this保存到传入的第一个参数对象中(既需要绑定this的对象)，作为参数对象的一个属性
​	4.保存this后，则call()方法的this对象(既调用call方法的函数)与传入参数对象形成一条作用域链，在该作用域链中this对象(既调用call方法的函数）在作用域链的最前端
​	5.然后通过传入参数对象调用“调用call方法的函数”,此时“调用call方法的函数”的this指向与参数对象形成的作用域链
​	6.当查找某个属性时，如果&quot;调用call()方法的函数&quot;没有该属性，就会沿着这条作用域链去参数对象中查找该属性</p>
</blockquote>
</li>
</ul>
</li>
</ul>
<div><pre><code><span>/**
 * 模拟实现call()方法
 * call()方法原理：通过将调用call方法的函数作用域扩展到第一个参数对象的作用域链中,因此调用call的函数通过this可以访问到参数对象的属性和方法
 * @param {Object} context, 扩展的作用域对象
 * @param {any} args  其余参数
 * return result 函数调用结果
 */</span>
<span>Function</span><span>.</span>prototype<span>.</span><span>myCall</span> <span>=</span> <span>function</span> <span>(</span><span>context <span>=</span> window<span>,</span> <span>...</span>args</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>this</span> <span>===</span> <span>Function</span><span>.</span>prototype<span>)</span> <span>{</span> <span>//判断当前this是否为函数,防止Function.prototype.myCall() 直接调用;直接调用this指向funcion原型对象</span>
    console<span>.</span><span>log</span><span>(</span><span>this</span><span>)</span> <span>//输出 ƒ () { [native code] }</span>
    <span>return</span> <span>undefined</span>
  <span>}</span>
  context <span>=</span> context <span>||</span> window <span>//context 为可选参数,如果不传的话默认上下文为 window</span>
  <span>let</span> fn <span>=</span> <span>Symbol</span><span>(</span><span>)</span> <span>//为context 创建一个 Symbol（保证不会重名）属性,将当前函数赋值给这个属性</span>
  <span>//因为调用myCall方法的函数是调用了自己原型链对象上的myCall方法,所以当前myCall方法内的this指向调用myCall方法的函数</span>
  context<span>[</span>fn<span>]</span> <span>=</span> <span>this</span>  <span>//将调用myCall方法的函数挂在到传入的contex原型对象上,成为context对象的属性;调用函数作用域添加到参数对象的作用域链上</span>
  <span>let</span> result <span>=</span> context<span>[</span>fn<span>]</span><span>(</span><span>...</span>args<span>)</span> <span>//在context对象上调用调用myCall方法的函数,此时myCall可以访问到参数对象的作用域链上的属性和方法</span>
  <span>delete</span> context<span>[</span>fn<span>]</span>   <span>//调用完成后在context对象上删除调用myCall方法的函数,既从参数对象的作用域链中删除调用myCall的函数的作用域</span>
  <span>return</span> result   <span>//返回结果</span>
<span>}</span>
<span>//myCall使用示例</span>
<span>let</span> obj <span>=</span> <span>{</span> a<span>:</span> <span>1</span> <span>}</span>
<span>var</span> a <span>=</span> <span>2</span>
<span>function</span> <span>greet</span><span>(</span><span>)</span> <span>{</span>
  console<span>.</span><span>log</span><span>(</span><span>'a is '</span> <span>+</span> <span>this</span><span>.</span>a <span>+</span> <span>' greet is '</span> <span>+</span> <span>this</span><span>)</span>
<span>}</span>
<span>greet</span><span>(</span><span>)</span><span>//输出a is 2 greet is [object Window]</span>
greet<span>.</span><span>myCall</span><span>(</span><span>)</span><span>//a is 2 greet is [object Window]</span>
greet<span>.</span><span>myCall</span><span>(</span>obj<span>)</span><span>//输出a is 1 greet is [object Object]</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br></div></div><h3 id="实现-apply"> 实现 <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Function/apply" target="_blank" rel="noopener noreferrer">apply()</a></h3>
<ul>
<li>作用
在特定的作域中调用函数，实际上等于设置函数体内 this 对象的值，扩充函数的作用域</li>
<li>接收2个参数
第一个参数:在其中运行函数的作用域，一般为this
第二个参数:参数数组，可以是 Array 的实例，也可以是arguments 对象</li>
<li>返回值
调用有指定this值和参数的函数的结果</li>
</ul>
<div><pre><code><span>/**
 * apply()方法原理和callf()方法原理一样,但是第二个参数不同:apply的第二个参数为数组
 * @param {Object} context, 扩展的作用域对象
 * @param {any} args  其余参数
 * return result 函数调用结果
 */</span>
<span>Function</span><span>.</span>prototype<span>.</span><span>myApply</span> <span>=</span> <span>function</span> <span>(</span><span>context <span>=</span> window<span>,</span> args</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>this</span> <span>===</span> <span>Function</span><span>.</span>prototype<span>)</span> <span>{</span>
    <span>return</span> <span>undefined</span>
  <span>}</span>

  context <span>=</span> context <span>||</span> window
  <span>let</span> result
  <span>let</span> fn <span>=</span> <span>Symbol</span><span>(</span><span>)</span>
  context<span>[</span>fn<span>]</span> <span>=</span> <span>this</span>
  <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>args<span>)</span><span>)</span> <span>{</span> <span>//当前参数为数组</span>
    result <span>=</span> context<span>[</span>fn<span>]</span><span>(</span><span>...</span>args<span>)</span>
  <span>}</span> <span>else</span> <span>{</span> <span>//参数为空</span>
    result <span>=</span> context<span>[</span>fn<span>]</span><span>(</span><span>)</span>
  <span>}</span>
  <span>delete</span> context<span>[</span>fn<span>]</span>
  <span>return</span> result
<span>}</span>

</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br></div></div><ul>
<li>call() 和 apply()方法比较
<ul>
<li>区别： call()方法接受的是参数列表，而apply()方法接受的是一个参数数组</li>
</ul>
</li>
</ul>
<h3 id="实现-bind"> 实现 <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Function/bind" target="_blank" rel="noopener noreferrer">bind()</a></h3>
<ul>
<li>创建一个函数的实例，其 this 值会被绑定到传给 bind() 函数的值</li>
</ul>
<div><pre><code><span>/**
 * Function.prototype.bind(thisArg[, arg1[, arg2[, ...]]])函数参数
 * 函数参数：
 *  thisArg,  调用绑定函数时作为 this 参数传递给目标函数的值
 *  arg1, arg2, ...  当目标函数被调用时,被预置入绑定函数的参数列表中的参数。
 *  返回值 function 返回一个原函数的拷贝,并拥有指定的 this 值和初始参数
 */</span>

<span>/**
 * 实现方式一
 * bind()最简单的实现
 * 思想：给目标函数指定作用域
 * @param {object} context 绑定的作用域对象
 */</span>
<span>Function</span><span>.</span>prototype<span>.</span><span>myBind</span> <span>=</span> <span>function</span> <span>(</span><span>context</span><span>)</span> <span>{</span>
  <span>let</span> self <span>=</span> <span>this</span> <span>//保存调用函数的作用域</span>
  <span>return</span> <span>function</span> <span>(</span><span>context</span><span>)</span> <span>{</span>
    <span>self</span><span>.</span><span>apply</span><span>(</span>context<span>,</span> arguments<span>)</span>
  <span>}</span>
<span>}</span>

<span>/**
 * 实现方式二
 * 考虑bind()函数柯里化情况？？
 * 思想：获取函数参数,返回一个闭包函数,用于收集函数其余参数
 * @param {object} context 绑定的作用域对象
 */</span>

<span>Function</span><span>.</span>prototype<span>.</span><span>myBind</span> <span>=</span> <span>function</span> <span>(</span><span>context</span><span>)</span> <span>{</span>
  <span>let</span> self <span>=</span> <span>this</span> <span>//保存调用函数的作用域</span>
  <span>let</span> args <span>=</span> <span>Array</span><span>.</span>prototype<span>.</span><span>slice</span><span>.</span><span>call</span><span>(</span>arguments<span>,</span> <span>1</span><span>)</span> <span>//获取第二个参数之后的所有参数(第一个参数为context)</span>
  <span>return</span> <span>function</span> <span>(</span><span>)</span> <span>{</span> <span>//返回闭包</span>
    <span>let</span> innerArgs <span>=</span> <span>Array</span><span>.</span>prototype<span>.</span><span>slice</span><span>.</span><span>call</span><span>(</span>arguments<span>)</span> <span>//收集第二个以后参数</span>
    <span>self</span><span>.</span><span>apply</span><span>(</span>context<span>,</span> args<span>.</span><span>concat</span><span>(</span>innerArgs<span>)</span><span>)</span> <span>//合并参数并用调用bind()的函数调用apply</span>
  <span>}</span>
<span>}</span>

<span>/**
 * 实现方式三(推荐)
 * Javascript的函数还可以作为构造函数,那么绑定后的函数用这种方式调用时,情况有所不同
 * 思想：通过设置一个中转构造函数F,使绑定后的函数与调用bind()的函数处于同一原型链上,
 * 用new操作符调用绑定后的函数,返回的对象也能正常使用instanceof,因此这是最严谨的bind()实现
 * @param {object} context 绑定的作用域对象
 */</span>
<span>Function</span><span>.</span>prototype<span>.</span><span>myBind</span> <span>=</span> <span>function</span> <span>(</span><span>context</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>this</span> <span>===</span> <span>Function</span><span>.</span>prototype<span>)</span> <span>{</span> <span>//防止直接调用</span>
    <span>throw</span> <span>TypeError</span><span>(</span><span>'Error'</span><span>)</span>
  <span>}</span>

  <span>let</span> self <span>=</span> <span>this</span> <span>//保存调用者函数的作用域</span>
  <span>let</span> args <span>=</span> <span>Array</span><span>.</span>prototype<span>.</span><span>slice</span><span>.</span><span>call</span><span>(</span>arguments<span>,</span> <span>1</span><span>)</span> <span>//获取第二个参数之后的所有参数(第一个参数为context)</span>
  <span>F</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span> <span>}</span> <span>//创建一个中转构造函数</span>
  <span>let</span> <span>bound</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span> <span>//闭包,用于收集bind()的其余参数</span>
    <span>let</span> innerArgs <span>=</span> <span>Array</span><span>.</span>prototype<span>.</span><span>slice</span><span>.</span><span>call</span><span>(</span>arguments<span>)</span> <span>//获取回调函数参数</span>
    <span>self</span><span>.</span><span>apply</span><span>(</span><span>this</span> <span>instanceof</span> <span>F</span> <span>&amp;&amp;</span> context <span>?</span> <span>this</span> <span>:</span> context <span>||</span> window<span>,</span> args<span>.</span><span>concat</span><span>(</span>innerArgs<span>)</span><span>)</span>
  <span>}</span>
  <span>F</span><span>.</span>prototype <span>=</span> self<span>.</span>prototype <span>//F继承当前函数,与调用bind的函数作用域链链接</span>
  bound<span>.</span>prototype <span>=</span> <span>new</span> <span>F</span><span>(</span><span>)</span> <span>//闭包函数继承F函数,使作用域链接与调用bind()函数的作用域链链接,最总形成 bound -> F -> 调用bind的函数的作用链  </span>
  <span>return</span> bound
<span>}</span>

<span>/**
 * 简化价写法(推荐)
 * @param {object} context 绑定的作用域对象
 */</span>
<span>Function</span><span>.</span>prototype<span>.</span><span>myBind</span> <span>=</span> <span>function</span> <span>(</span><span>context<span>,</span> <span>...</span>args</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>this</span> <span>===</span> <span>Function</span><span>.</span>prototype<span>)</span> <span>{</span>
    <span>throw</span> <span>TypeError</span><span>(</span><span>'Error'</span><span>)</span>
  <span>}</span>
  <span>let</span> self <span>=</span> <span>this</span> <span>//保存调用bind方法的函数作用域</span>
  <span>return</span> <span>function</span> <span>F</span><span>(</span><span><span>...</span>args2</span><span>)</span> <span>{</span> <span>//返回闭包</span>
    <span>//判断是否为构造函数调用,如果是则使用new调用当前构造函数？？</span>
    <span>if</span> <span>(</span><span>this</span> <span>instanceof</span> <span>F</span><span>)</span> <span>{</span>
      <span>return</span> <span>new</span> <span>self</span><span>(</span><span>...</span>args<span>,</span> <span>...</span>args2<span>)</span>
    <span>}</span>
    <span>//否则使用apply，将context和处理好的参数传入</span>
    <span>return</span> <span>self</span><span>.</span><span>apply</span><span>(</span>context<span>,</span> args<span>.</span><span>concat</span><span>(</span>args2<span>)</span><span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br></div></div><ul>
<li>注意问题
<ul>
<li>显示绑定对箭头函数无效，箭头函数在定义时绑定了外层对象</li>
<li>如果你传入了一个原始值（字符串类型、布尔类型或者数字类型）来当作 this 的绑定对象，这个原始值会被转换成它的对象形式（也就是 new String(..) 、 new Boolean(..) 或者new Number(..) ）。这通常被称为“装箱”</li>
<li>把 null 或者 undefined 作为 this 的绑定对象传入 call 、 apply 或者 bind ，这些值在调用时会被忽略，实际应用的是默认绑定规则</li>
<li>使用null作为绑定方法的绑定对象产生的问题
<ul>
<li>如果函数并不关心 this 的话，你仍然需要传入一个占位值，这时 null 可能是一个不错的选择</li>
<li>总是使用 null 来忽略 this 绑定可能产生一些副作用。</li>
<li>如果某个函数确实使用了this （比如第三方库中的一个函数），</li>
<li>那默认绑定规则会把 this 绑定到全局对象（在浏览器中这个对象是 window ）</li>
<li>解决方法：
<ul>
<li>使用空对象作为绑定方法的绑定对象，在 JavaScript 中创建一个空对象最简单的方法都是 Object.create(null)，Object.create(null) 和 {} 很像，但是并不会创建 Object.prototype 这个委托，所以它比 {} “更空”</li>
<li>如果想“更安全”地忽略 this 绑定，你可以使用一个 Object.create(null) 对象，以保护全局对象</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="实现-sort"> 实现 sort()</h3>
<blockquote>
<p><strong>讲下 V8 sort 的大概思路，并手写一个 sort 的实现</strong></p>
</blockquote>
<ul>
<li>
<p>关于 <code>Array.prototype.sort()</code> ，ES 规范并没有指定具体的算法，在 V8 引擎中，  <strong>7.0 版本之前</strong> ，数组长度小于10时， <code>Array.prototype.sort()</code> 使用的是插入排序，否则用快速排序。</p>
</li>
<li>
<p>在 V8 引擎 <strong>7.0 版本之后</strong> 就舍弃了快速排序，因为它不是稳定的排序算法，在最坏情况下，时间复杂度会降级到 O(n2)。于是采用了一种混合排序的算法：<strong>TimSort</strong> 。</p>
</li>
</ul>
<p><img src="./../images/paixu.png" alt="" /></p>
<h3 id="实现准时的-settimeout"> 实现准时的 setTimeout</h3>
<ul>
<li>
<p>setTimeout 存在问题：因为 setTimtout 为宏任务，在 setTimeout 任务推入任务队列时，有可能在 JS 时间循环中主线程存在其他任务，因此 setTimeout 任务存在时间误差。参考本节  中解释。</p>
</li>
<li>
<p>实现准时 setTomeout 有四种方式：</p>
</li>
</ul>
<table>
<thead>
<tr>
<th style="text-align:left"></th>
<th style="text-align:left">while</th>
<th style="text-align:left">Web Worker</th>
<th style="text-align:left">requestAnimationFrame</th>
<th style="text-align:left">setTimeout 系统时间补偿</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">准确度</td>
<td style="text-align:left">高</td>
<td style="text-align:left">高</td>
<td style="text-align:left">低</td>
<td style="text-align:left">高</td>
</tr>
<tr>
<td style="text-align:left">主线程阻塞</td>
<td style="text-align:left">阻塞</td>
<td style="text-align:left">一般</td>
<td style="text-align:left">不阻塞</td>
<td style="text-align:left">不阻塞</td>
</tr>
<tr>
<td style="text-align:left">评分</td>
<td style="text-align:left">⭐️⭐️</td>
<td style="text-align:left">⭐️⭐️⭐️</td>
<td style="text-align:left">⭐️</td>
<td style="text-align:left">⭐️⭐️⭐️⭐️⭐️</td>
</tr>
</tbody>
</table>
<p><strong>while 循环实现</strong></p>
<div><pre><code><span>function</span> <span>timer</span><span>(</span><span>time</span><span>)</span> <span>{</span>
    <span>const</span> startTime <span>=</span> Date<span>.</span><span>now</span><span>(</span><span>)</span><span>;</span>
    <span>while</span><span>(</span><span>true</span><span>)</span> <span>{</span>
        <span>const</span> now <span>=</span> Date<span>.</span><span>now</span><span>(</span><span>)</span><span>;</span>
        <span>if</span><span>(</span>now <span>-</span> startTime <span>>=</span> time<span>)</span> <span>{</span>
            console<span>.</span><span>log</span><span>(</span><span>'误差'</span><span>,</span> now <span>-</span> startTime <span>-</span> time<span>)</span><span>;</span>
            <span>return</span><span>;</span>
        <span>}</span>
    <span>}</span>
<span>}</span>
<span>timer</span><span>(</span><span>5000</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><blockquote>
<p>打印：误差 0</p>
</blockquote>
<ul>
<li>优点：方式很精确</li>
<li>缺点： js 是单线程运行，使用这样的方式强行霸占线程会使得页面进入卡死状态。</li>
</ul>
<p><strong>Web Worker</strong></p>
<ul>
<li>
<p>通过 Web Worker 在另一个线程来执行定时任务.</p>
</li>
<li>
<p>一个 worker 的简单的示例</p>
<div><pre><code><span>// main.js</span>
<span>var</span> myWorker <span>=</span> <span>new</span> <span>Worker</span><span>(</span><span>'worker.js'</span><span>)</span><span>;</span>

<span>// 监听 worker</span>
myWorker<span>.</span><span>onmessage</span> <span>=</span> <span>function</span><span>(</span><span>e</span><span>)</span> <span>{</span>
  result<span>.</span>textContent <span>=</span> e<span>.</span>data<span>;</span>
  console<span>.</span><span>log</span><span>(</span><span>'Message received from worker'</span><span>)</span><span>;</span>
<span>}</span>
first<span>.</span><span>onchange</span> <span>=</span> <span>function</span><span>(</span><span>)</span> <span>{</span>
  <span>// 向 worker 发送数据</span>
  myWorker<span>.</span><span>postMessage</span><span>(</span><span>[</span>first<span>.</span>value<span>,</span>second<span>.</span>value<span>]</span><span>)</span><span>;</span>
  console<span>.</span><span>log</span><span>(</span><span>'Message posted to worker'</span><span>)</span><span>;</span>
<span>}</span>
<span>// worker.js</span>
<span>onmessage</span> <span>=</span> <span>function</span><span>(</span><span>e</span><span>)</span> <span>{</span>
  <span>// 接受主线程的数据</span>
  console<span>.</span><span>log</span><span>(</span><span>'Message received from main script'</span><span>)</span><span>;</span>
  <span>var</span> workerResult <span>=</span> <span>'Result: '</span> <span>+</span> <span>(</span>e<span>.</span>data<span>[</span><span>0</span><span>]</span> <span>*</span> e<span>.</span>data<span>[</span><span>1</span><span>]</span><span>)</span><span>;</span>
  console<span>.</span><span>log</span><span>(</span><span>'Posting message back to main script'</span><span>)</span><span>;</span>
  <span>// 向主线程发送数据</span>
  <span>postMessage</span><span>(</span>workerResult<span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br></div></div><p>那么接下来我们就要加 worker 和 while 相结合，以下为创建 worker 部分</p>
<div><pre><code><span>// worker生成器</span>
<span>const</span> <span>createWorker</span> <span>=</span> <span>(</span><span>fn<span>,</span> options</span><span>)</span> <span>=></span> <span>{</span>
    <span>const</span> blob <span>=</span> <span>new</span> <span>Blob</span><span>(</span><span>[</span><span>'('</span> <span>+</span> fn<span>.</span><span>toString</span><span>(</span><span>)</span> <span>+</span> <span>')()'</span><span>]</span><span>)</span><span>;</span>
    <span>const</span> url <span>=</span> <span>URL</span><span>.</span><span>createObjectURL</span><span>(</span>blob<span>)</span><span>;</span>
    <span>if</span> <span>(</span>options<span>)</span> <span>{</span>
        <span>return</span> <span>new</span> <span>Worker</span><span>(</span>url<span>,</span> options<span>)</span><span>;</span>
    <span>}</span>
    <span>return</span> <span>new</span> <span>Worker</span><span>(</span>url<span>)</span><span>;</span>
<span>}</span> 
<span>// worker 部分</span>
<span>const</span> worker <span>=</span> <span>createWorker</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
    <span>onmessage</span> <span>=</span> <span>function</span> <span>(</span><span>e</span><span>)</span> <span>{</span>
        <span>const</span> date <span>=</span> Date<span>.</span><span>now</span><span>(</span><span>)</span><span>;</span>
        <span>while</span> <span>(</span><span>true</span><span>)</span> <span>{</span>
            <span>const</span> now <span>=</span> Date<span>.</span><span>now</span><span>(</span><span>)</span><span>;</span>
            <span>if</span><span>(</span>now <span>-</span> date <span>>=</span> e<span>.</span>data<span>)</span> <span>{</span>
                <span>postMessage</span><span>(</span><span>1</span><span>)</span><span>;</span>
                <span>return</span><span>;</span>
            <span>}</span>
        <span>}</span>
    <span>}</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br></div></div><p>我们通过在 worker 中写入一个 while 循环，当达到我们的预取时间的时候，再向主线程发送一个完成事件，就不会因为主线程的其他代码的干扰而造成数据不准的情况。</p>
<div><pre><code><span>let</span> isStart <span>=</span> <span>false</span><span>;</span>
<span>function</span> <span>timer</span><span>(</span><span>)</span> <span>{</span>
    worker<span>.</span><span>onmessage</span> <span>=</span> <span>function</span> <span>(</span><span>e</span><span>)</span> <span>{</span>
       <span>cb</span><span>(</span><span>)</span>
        <span>if</span> <span>(</span>isStart<span>)</span> <span>{</span>
            worker<span>.</span><span>postMessage</span><span>(</span>speed<span>)</span><span>;</span>
        <span>}</span> 
    <span>}</span>
    worker<span>.</span><span>postMessage</span><span>(</span>speed<span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div></li>
</ul>
<p><strong>requestAnimationFrame()</strong></p>
<ul>
<li>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window/requestAnimationFrame" target="_blank" rel="noopener noreferrer"><strong><code>window.requestAnimationFrame()</code></strong> </a>:告诉浏览器——你希望执行一个动画，并且要求浏览器在下次重绘之前调用指定的回调函数更新动画。该方法需要传入一个回调函数作为参数，该回调函数会在浏览器下一次重绘之前执行，回调函数执行次数通常是每秒60次，也就是每16.7ms 执行一次，但是并不一定保证为 16.7 ms。</p>
</li>
<li>
<p>实现:</p>
<ul>
<li>
<div><pre><code><span>// 模拟代码</span>
<span>function</span> <span>setTimeout2</span> <span>(</span><span>cb<span>,</span> delay</span><span>)</span> <span>{</span>
    <span>let</span> startTime <span>=</span> Date<span>.</span><span>now</span><span>(</span><span>)</span>
    <span>loop</span><span>(</span><span>)</span>
  
    <span>function</span> <span>loop</span> <span>(</span><span>)</span> <span>{</span>
      <span>const</span> now <span>=</span> Date<span>.</span><span>now</span><span>(</span><span>)</span>
      <span>if</span> <span>(</span>now <span>-</span> startTime <span>>=</span> delay<span>)</span> <span>{</span>
        <span>cb</span><span>(</span><span>)</span><span>;</span>
        <span>return</span><span>;</span>
      <span>}</span>
      <span>requestAnimationFrame</span><span>(</span>loop<span>)</span>
    <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div></li>
<li>
<p>缺点: 由于 16.7 ms 间隔执行，在使用间隔很小的定时器，很容易导致时间的不准确</p>
</li>
</ul>
</li>
</ul>
<p><strong>setTimeout 系统时间补偿</strong></p>
<ul>
<li>
<p>原理:当每一次定时器执行时后，都去获取系统的时间来进行修正，虽然每次运行可能会有误差，但是通过系统时间对每次运行的修复，能够让后面每一次时间都得到一个补偿</p>
<ul>
<li>
<p>setTimetout 时序:</p>
<p><img src="./../images/settimeout时序.png" alt="" /></p>
</li>
<li>
<p>setTimeout 时间补偿:</p>
<p><img src="./../images/settimeout时间补偿.png" alt="" /></p>
</li>
</ul>
</li>
<li>
<p>实现:</p>
<div><pre><code><span>function</span> <span>timer</span><span>(</span><span>)</span> <span>{</span>
   <span>var</span> speed <span>=</span> <span>500</span><span>,</span>
   counter <span>=</span> <span>1</span><span>,</span> 
   start <span>=</span> <span>new</span> <span>Date</span><span>(</span><span>)</span><span>.</span><span>getTime</span><span>(</span><span>)</span><span>;</span>
   
   <span>function</span> <span>instance</span><span>(</span><span>)</span>
   <span>{</span>
    <span>var</span> ideal <span>=</span> <span>(</span>counter <span>*</span> speed<span>)</span><span>,</span>
    real <span>=</span> <span>(</span><span>new</span> <span>Date</span><span>(</span><span>)</span><span>.</span><span>getTime</span><span>(</span><span>)</span> <span>-</span> start<span>)</span><span>;</span>
    
    counter<span>++</span><span>;</span>

    <span>var</span> diff <span>=</span> <span>(</span>real <span>-</span> ideal<span>)</span><span>;</span>
    form<span>.</span>diff<span>.</span>value <span>=</span> diff<span>;</span>

    window<span>.</span><span>setTimeout</span><span>(</span><span>function</span><span>(</span><span>)</span> <span>{</span> <span>instance</span><span>(</span><span>)</span><span>;</span> <span>}</span><span>,</span> <span>(</span>speed <span>-</span> diff<span>)</span><span>)</span><span>;</span> <span>// 通过系统时间进行修复</span>

   <span>}</span><span>;</span>
   
   window<span>.</span><span>setTimeout</span><span>(</span><span>function</span><span>(</span><span>)</span> <span>{</span> <span>instance</span><span>(</span><span>)</span><span>;</span> <span>}</span><span>,</span> speed<span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br></div></div></li>
</ul>
<p><strong>参考资料</strong></p>
<p><a href="https://mp.weixin.qq.com/s/1ComSa7Rw-p_NIfg0dup7Q" target="_blank" rel="noopener noreferrer">如何实现准时的setTimeout</a></p>
<h3 id="实现深拷贝"> 实现深拷贝</h3>
<p>深拷贝考虑问题：</p>
<ul>
<li>基础类型</li>
<li>引用类型
<ul>
<li>RegExp、Date、函数 不是 JSON 安全的</li>
<li>会丢失 constructor，所有的构造函数都指向 Object</li>
<li>破解循环引用</li>
</ul>
</li>
</ul>
<p>实现一：</p>
<div><pre><code><span>function</span> <span>deepCopy</span><span>(</span><span>obj</span><span>)</span> <span>{</span>
    <span>let</span> result <span>=</span> <span>null</span>
    <span>if</span> <span>(</span><span>typeof</span> obj <span>===</span> <span>'object'</span><span>)</span> <span>{</span>
        result <span>=</span> obj<span>.</span>constructor <span>===</span> Array<span>?</span> <span>[</span><span>]</span> <span>:</span> <span>{</span><span>}</span><span>;</span>
        <span>for</span> <span>(</span><span>let</span> i <span>in</span> obj<span>)</span> <span>{</span>
            result<span>[</span>i<span>]</span> <span>=</span> <span>typeof</span> obj<span>[</span>i<span>]</span> <span>===</span> <span>'object'</span> <span>?</span> <span>deepCopy</span><span>(</span>obj<span>[</span>i<span>]</span><span>)</span> <span>:</span> obj<span>[</span>i<span>]</span><span>;</span>    
        <span>}</span>  
    <span>}</span> <span>else</span> <span>{</span>
       	result <span>=</span> obj<span>;</span>  
    <span>}</span>
    <span>return</span> result<span>;</span>
<span>}</span>

</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br></div></div><p>实现二：</p>
<p><img src="./../images/deepCopy.png" alt="" /></p>
<h3 id="实现防抖"> 实现防抖</h3>
<ul>
<li>
<p>防抖作用：防止触发频率过高</p>
</li>
<li>
<p>思想：不管事件触发频率多高，事件总是在n秒后执行；如果一个事件在触发后n秒内再次触发，就清除前一次事件的计时，以后一次事件触发事件为准冲重新计时n秒，n秒后执行</p>
</li>
</ul>
<p><img src="./../images/防抖节流.gif" alt="" /></p>
<ul>
<li>
<p>原理：当调用debounce以后立即执行，返回闭包函数给调用者，往后执行都是执行闭包函数，闭包中可以访问debounce中定义的定时器变量</p>
</li>
<li>
<p>实现流程：</p>
<ul>
<li>
<p>1.通过计时延迟事件执行；</p>
</li>
<li>
<p>2.当事件在计时事件内触发，则重新计时；</p>
</li>
<li>
<p>3.通过闭包来保存计时变量；</p>
</li>
<li>
<p>4.为了使this指向明确，返回闭包用普通函数，重新计时用箭头函数（this并非指向debounce的调用者，而是执行返回闭包的调用）</p>
</li>
<li>
<p>5.对参数透传</p>
<div><pre><code><span>/**
 * @param {Function} callback  事件回调函数
 * @param {Number} time  延迟执行时间
 * @return funcion 闭包
 */</span>
<span>function</span> <span>debounce</span><span>(</span><span>callback<span>,</span> time</span><span>)</span> <span>{</span>
  <span>let</span> timer <span>=</span> <span>null</span> <span>//设置定时器变量</span>
  <span>return</span> <span>function</span> <span>(</span><span><span>...</span>args</span><span>)</span> <span>{</span> <span>//返回闭包来保存定时器变量(闭包中的this指向闭包的调用者)</span>
    <span>clearTimeout</span><span>(</span>timer<span>)</span> <span>//清除定时器</span>
    timer <span>=</span> <span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span> <span>//重新定时(箭头函数的this绑定定义时外层对象的this，既闭包的this，闭包的this指向闭包的调用者)</span>
      <span>callback</span><span>.</span><span>apply</span><span>(</span><span>this</span><span>,</span> args<span>)</span> <span>//注意通过apply绑定回调的this</span>
    <span>}</span><span>,</span> time<span>)</span><span>;</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div></li>
</ul>
</li>
<li>
<p>扩展题：实现防抖中调用立即执行一次，往后触发事件等待n秒后执行</p>
<ul>
<li>思想：给debounce函数添加标记，用于标记是否为第一次执行</li>
</ul>
<div><pre><code><span>/** 
 * @param {Function} callback  事件回调函数
 * @param {Number} time  延迟执行时间
 * @return funcion 闭包
 */</span>
<span>function</span> <span>debounce</span><span>(</span><span>callback<span>,</span> time<span>,</span> flag</span><span>)</span> <span>{</span>
  <span>let</span> timer <span>=</span> <span>null</span>
  <span>return</span> <span>function</span> <span>(</span><span><span>...</span>args</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span><span>!</span>timer <span>&amp;&amp;</span> flag<span>)</span> <span>{</span>
      console<span>.</span><span>log</span><span>(</span><span>'立即执行一次'</span><span>)</span>
      <span>callback</span><span>.</span><span>apply</span><span>(</span><span>this</span><span>,</span> args<span>)</span>
    <span>}</span>
    <span>clearTimeout</span><span>(</span>timer<span>)</span>
    timer <span>=</span> <span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
      <span>callback</span><span>.</span><span>apply</span><span>(</span><span>this</span><span>,</span> args<span>)</span>
    <span>}</span><span>,</span> time<span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br></div></div></li>
<li>
<p>debounce的应用场景:</p>
<div><pre><code><span>function</span> <span>print</span><span>(</span><span>params</span><span>)</span> <span>{</span>
  console<span>.</span><span>log</span><span>(</span><span>'print param is'</span><span>,</span> params<span>)</span>
<span>}</span>
<span>//窗口大小变化，调整样式</span>
window<span>.</span><span>addEventListener</span><span>(</span><span>'resize'</span><span>,</span> <span>debounce</span><span>(</span>print<span>,</span> <span>200</span><span>,</span> <span>true</span><span>)</span><span>)</span> <span>//调整窗口,输出为 event事件</span>
<span>//搜索框，输入后1000毫秒搜索</span>
<span>debounce</span><span>(</span>fetchSelectData<span>,</span> <span>300</span><span>)</span>
<span>//表单验证，输入1000毫秒后验证</span>
<span>debounce</span><span>(</span>validator<span>,</span> <span>1000</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div></li>
</ul>
<h3 id="实现节流"> 实现节流</h3>
<ul>
<li>
<p>思想：当持续触发事件时，保证一定时间段内只调用一次事件处理函数；在一次事件触发后在触发后的指定时间内触发事件都忽略，只有在指定时间间隔后触发事件生效</p>
</li>
<li>
<p>实现方式：使用时间戳、使用定时器、结合时间戳和定时器三种实现方式</p>
</li>
<li>
<p>实现一：<strong>使用时间戳实现节流</strong></p>
<ul>
<li>
<p>思想：调用函数时保存当前时间戳，立即返回闭包，闭包中计算下次调用时间戳和上次调用时间戳差值，如果差值大于节流时间，则调用回调</p>
</li>
<li>
<p>缺点：第一次事件一定触发，最后一次事件不触发 ？？（测试现象：返回闭包时候会立即执行一次回调，原因：？？）</p>
<div><pre><code> <span>/**
 * @param {Function} callback  事件回调函数
 * @param {Number} time  延迟执行时间
 * @return function 闭包
 */</span>
<span>function</span> <span>throttle</span><span>(</span><span>callback<span>,</span> time</span><span>)</span> <span>{</span>
  <span>let</span> pretime <span>=</span> Date<span>.</span><span>now</span><span>(</span><span>)</span> <span>//记录前一次调用时时间戳</span>
  <span>return</span> <span>function</span> <span>(</span><span><span>...</span>args</span><span>)</span> <span>{</span> <span>//返回闭包比较时间差</span>
    <span>let</span> nexttime <span>=</span> Date<span>.</span><span>now</span><span>(</span><span>)</span> <span>//记录当前调用时时间戳</span>
    <span>if</span> <span>(</span>nexttime <span>-</span> pretime <span>>=</span> time<span>)</span> <span>{</span> <span>//时间戳差值大于节流时间，执行回调</span>
      pretime <span>=</span> Date<span>.</span><span>now</span><span>(</span><span>)</span> <span>//重新记录执行时间戳</span>
      <span>callback</span><span>.</span><span>apply</span><span>(</span><span>this</span><span>,</span> args<span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div></li>
</ul>
</li>
<li>
<p>实现二：<strong>使用定时间实现节流</strong></p>
<ul>
<li>
<p>思想：调用函数时候定义定时器变量，立即返回闭包，闭包中判断定时器变量是否为空，如果为空执行回调</p>
</li>
<li>
<p>缺点：第一次事件不触发，最后一次事件一定触发？？（测试现象：返回闭包时候不执行回调，原因：？？）</p>
<div><pre><code><span>/**
* @param {Function} callback  事件回调函数
 * @param {Number} time  延迟执行时间
 * @return function 闭包
 */</span>
<span>function</span> <span>throttle</span><span>(</span><span>callback<span>,</span> time</span><span>)</span> <span>{</span>
  <span>let</span> timer <span>=</span> <span>null</span> <span>//定义定时器变量</span>
  <span>return</span> <span>function</span> <span>(</span><span><span>...</span>args</span><span>)</span> <span>{</span> <span>//返回闭包</span>
    <span>if</span> <span>(</span><span>!</span>timer<span>)</span> <span>{</span> <span>//定时器变量为空，设置time时间后执行回调</span>
      timer <span>=</span> <span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
        timer <span>=</span> <span>null</span> <span>//清空计时变量(该操作要在调用回调之前清空)</span>
        <span>callback</span><span>.</span><span>apply</span><span>(</span><span>this</span><span>,</span> args<span>)</span>
      <span>}</span><span>,</span> time<span>)</span><span>;</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div></li>
</ul>
</li>
<li>
<p>实现三：<strong>结合使用时间戳和定时器</strong></p>
<ul>
<li>
<p>思想：时间戳方式第一次事件触发时候执行，在限流时间内再次触发用定时器延迟执行执行事件，如果在限流时间过后再次调用，清除定时时间事件</p>
</li>
<li>
<p>优点： 第一次触发事件和最后一次触发事件都会执行</p>
<div><pre><code><span>/*
 * @param {Function} callback  事件回调函数
 * @param {Number} time  延迟执行时间
 * @return function 闭包
 */</span>
<span>function</span> <span>throttle</span><span>(</span><span>callback<span>,</span> time</span><span>)</span> <span>{</span>
  <span>let</span> pretime <span>=</span> Date<span>.</span><span>now</span><span>(</span><span>)</span>
  <span>let</span> timer <span>=</span> <span>null</span>
  <span>return</span> <span>function</span> <span>(</span><span><span>...</span>args</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>Date<span>.</span><span>now</span><span>(</span><span>)</span> <span>-</span> pretime <span>></span> time<span>)</span> <span>{</span> <span>//在限流时间后触发事件</span>
      <span>clearTimeout</span><span>(</span>timer<span>)</span> <span>//当限流时间过后并在定时时间到达之前再次触发事件，就清除定时事件</span>
      timer <span>=</span> <span>null</span> <span>//清空定时器</span>
      pretime <span>=</span> Date<span>.</span><span>now</span><span>(</span><span>)</span>
      <span>callback</span><span>.</span><span>apply</span><span>(</span><span>this</span><span>,</span> args<span>)</span> <span>//执行事件</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>!</span>timer<span>)</span> <span>{</span> <span>//如果在限流时间内触发事件并且定时为空(既定时事件不存在)，添加定时事件</span>
      timer <span>=</span> <span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
        <span>callback</span><span>.</span><span>apply</span><span>(</span><span>this</span><span>,</span> args<span>)</span>
      <span>}</span><span>,</span> time<span>)</span><span>;</span>
    <span>}</span>
  <span>}</span>
<span>}</span>

</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br></div></div></li>
</ul>
</li>
</ul>
<h3 id="实现函数柯里化"> 实现函数柯里化</h3>
<ul>
<li>
<p>概念: 用于创建已经设置好了一个或多个参数的函数,将多参数函数转变成单参数函数</p>
</li>
<li>
<p>思想: 通过收集传入被柯里化函数fn的参数(n1,n2,n3...),返回一个函数curry，该curry函数中将传入的被柯里化函数fn的参数(n1,n2,n3...)设置为默认参数,并接收fn函数的其余参数</p>
</li>
<li>
<p>实现方式一：</p>
<div><pre><code><span>/** 
 * 实现方法一
 * @param {Function} fn 被柯里化函数
 * @param  {any} args 柯里化默认参数
 */</span>
<span>function</span> <span>currying</span><span>(</span><span>fn<span>,</span> <span>...</span>args</span><span>)</span> <span>{</span>
  <span>//参数个数大于需要被柯里化的函数参数个数或则参数收集完毕,直接调用被柯里化的函数fn并传入参数</span>
  <span>if</span> <span>(</span>args<span>.</span>length <span>>=</span> fn<span>.</span>length<span>)</span> <span>{</span>
    <span>return</span> <span>fn</span><span>(</span><span>...</span>args<span>)</span><span>;</span>
  <span>}</span> <span>else</span> <span>{</span> <span>//如果参数个数小于被柯里化的函数参数个数,则递归调用fn,继续收集fn的参数</span>
    <span>return</span> <span>(</span><span><span>...</span>args2</span><span>)</span> <span>=></span> <span>currying</span><span>(</span>fn<span>,</span> <span>...</span>args<span>,</span> <span>...</span>args2<span>)</span><span>;</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br></div></div></li>
<li>
<p>实现方式二：</p>
<div><pre><code><span>/**
 * 实现方法二
 * @param {Function} fn 被柯里化函数
 * @param  {any} args 柯里化默认参数
 */</span>
<span>function</span> <span>curring</span><span>(</span><span>fn<span>,</span> <span>...</span>args</span><span>)</span> <span>{</span>
  <span>let</span> _this <span>=</span> <span>this</span> <span>//保存当前作用域</span>
  <span>let</span> args <span>=</span> args <span>||</span> <span>[</span><span>]</span>
  <span>let</span> len <span>=</span> fn<span>.</span>length <span>//获取函数参数长度</span>
  <span>//返回柯里化函数</span>
  <span>return</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
    <span>//获取柯里化函数参数</span>
    <span>let</span> _args <span>=</span> <span>Array</span><span>.</span>prototype<span>.</span><span>slice</span><span>.</span><span>call</span><span>(</span>arguments<span>)</span>
    <span>//收集柯里化函数参数(将新传入的参数push进入之前传入的默认参数中)</span>
    <span>Array</span><span>.</span>prototype<span>.</span><span>push</span><span>.</span><span>apply</span><span>(</span>args<span>,</span> _args<span>)</span>
    <span>//如果参数个数小于fn函数需要的参数个数,则柯里化函数递归继续收集参数</span>
    <span>if</span> <span>(</span>_args<span>.</span>length <span>&lt;</span> len<span>)</span> <span>{</span>
      <span>return</span> <span>curring</span><span>.</span><span>call</span><span>(</span>_this<span>,</span> fn<span>,</span> _args<span>)</span> <span>//注意使用call修改作用域,将新的参数传入</span>
    <span>}</span> <span>else</span> <span>{</span> <span>//否则参数收集完毕,调用函数fn</span>
      <span>return</span> <span>curring</span><span>.</span><span>apply</span><span>(</span><span>this</span><span>,</span> _args<span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br></div></div></li>
<li>
<p>柯里化函数使用:</p>
<div><pre><code><span>//定义打印函数</span>
<span>function</span> <span>print</span><span>(</span><span>param1<span>,</span> param2<span>,</span> param3</span><span>)</span> <span>{</span>
  console<span>.</span><span>log</span><span>(</span><span><span>`</span><span>param1 is </span><span><span>${</span>param1<span>}</span></span><span>, param2 is </span><span><span>${</span>param2<span>}</span></span><span>, param3 is </span><span><span>${</span>param3<span>}</span></span><span>`</span></span><span>)</span>
<span>}</span>
<span>//进行打印柯里化,传入一个默认参数:1,返回curryingFun函数,该函数设置1为print函数的默认参数,并接收其余参数</span>
<span>const</span> curryingFun <span>=</span> <span>currying</span><span>(</span>print<span>)</span><span>(</span><span>1</span><span>)</span>
<span>//调用柯里化后的打印函数,传入打印函数其余参数</span>
<span>curryingFun</span><span>(</span><span>2</span><span>,</span> <span>3</span><span>)</span> <span>//输出param1 is 1, param2 is 2, param3 is 3</span>
<span>//注意: 如果curryingFun函数参数个数小于print函数的参数,将返回curring函数,继续收集其余参数</span>
<span>curryingFun</span><span>(</span><span>2</span><span>)</span> <span>// 输出函数(...args2) => currying(fn, ...args, ...args2)</span>
<span>//进行打印柯里化,传入一个默认参数:4,5</span>
<span>const</span> curryingFun2 <span>=</span> <span>currying</span><span>(</span>print<span>)</span><span>(</span><span>4</span><span>)</span><span>(</span><span>5</span><span>)</span> <span>//注意:该写法等价于curryingFun2 = currying(print)(4,5) </span>
<span>curryingFun2</span><span>(</span><span>6</span><span>)</span> <span>//输出param1 is 4, param2 is 5, param3 is 6</span>
<span>//传入打印函数柯里化后的函数的参数大于打印函数的函数的参数个数时,多余参数将被忽略</span>
<span>curryingFun2</span><span>(</span><span>6</span><span>)</span><span>(</span><span>7</span><span>)</span><span>//输出param1 is 4, param2 is 5, param3 is 6</span>

<span>/**
 * currying(print)(4)(5) 与 currying(print)(4,5) 区别：
 * currying(print)(4)(5) 会递归两次,收集两次函数的默认参数
 * currying(print)(4,5)  会递归一次,收集一次函数的默认参数
 */</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br></div></div></li>
</ul>
<h3 id="柯里化函数题目"> 柯里化函数题目</h3>
<ul>
<li>
<p>题目一：</p>
<blockquote>
<p>实现一个add方法，使计算结果能够满足如下预期：</p>
<p>add(1)(2)(3) = 6;</p>
<p>add(1, 2, 3)(4) = 10;</p>
<p>add(1)(2)(3)(4)(5) = 15;</p>
</blockquote>
<div><pre><code><span>function</span> <span>add</span><span>(</span><span>params</span><span>)</span> <span>{</span>
  <span>//保存第一次传入的参数</span>
  <span>let</span> _args <span>=</span> <span>Array</span><span>.</span>prototype<span>.</span><span>slice</span><span>.</span><span>call</span><span>(</span>arguments<span>)</span>

  <span>//返回闭包来收集将第二次以后执行的参数</span>
  <span>let</span> <span>_adder</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
    _args<span>.</span><span>push</span><span>(</span><span>...</span>arguments<span>)</span>
    <span>return</span> _adder
  <span>}</span>
  <span>//重写toString方法,返回参数相加结果</span>
  _adder<span>.</span><span>toString</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
    <span>return</span> _args<span>.</span><span>reduce</span><span>(</span><span>(</span><span>a<span>,</span> b</span><span>)</span> <span>=></span> <span>{</span>
      <span>return</span> a <span>+</span> b
    <span>}</span><span>)</span>
  <span>}</span>
  <span>return</span> _adder
<span>}</span>

<span>add</span><span>(</span><span>1</span><span>)</span><span>(</span><span>2</span><span>)</span><span>(</span><span>3</span><span>)</span>                <span>// 6</span>
<span>add</span><span>(</span><span>1</span><span>,</span> <span>2</span><span>,</span> <span>3</span><span>)</span><span>(</span><span>4</span><span>)</span>             <span>// 10</span>
<span>add</span><span>(</span><span>1</span><span>)</span><span>(</span><span>2</span><span>)</span><span>(</span><span>3</span><span>)</span><span>(</span><span>4</span><span>)</span><span>(</span><span>5</span><span>)</span>          <span>// 15</span>
<span>add</span><span>(</span><span>2</span><span>,</span> <span>6</span><span>)</span><span>(</span><span>1</span><span>)</span>                <span>// 9</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br></div></div></li>
</ul>
<h3 id="实现图片懒加载"> 实现图片懒加载</h3>
<ul>
<li>
<p>html: 加载loading图片是在html部分就实现的.</p>
<p><img src="./../images/lanjiazai2.png" alt="" /></p>
</li>
<li>
<p>javascript:</p>
<div><pre><code><span>// onload是等所有的资源文件加载完毕以后再绑定事件</span>
window<span>.</span><span>onload</span> <span>=</span> <span>function</span><span>(</span><span>)</span><span>{</span>
	<span>// 获取图片列表，即img标签列表</span>
	<span>var</span> imgs <span>=</span> document<span>.</span><span>querySelectorAll</span><span>(</span><span>'img'</span><span>)</span><span>;</span>

	<span>// 获取到浏览器顶部的距离</span>
	<span>function</span> <span>getTop</span><span>(</span><span>e</span><span>)</span><span>{</span>
		<span>return</span> e<span>.</span>offsetTop<span>;</span>
	<span>}</span>

	<span>// 懒加载实现</span>
	<span>function</span> <span>lazyload</span><span>(</span><span>imgs</span><span>)</span><span>{</span>
		<span>// 可视区域高度</span>
		<span>var</span> h <span>=</span> window<span>.</span>innerHeight<span>;</span>
		<span>//滚动区域高度</span>
		<span>var</span> s <span>=</span> document<span>.</span>documentElement<span>.</span>scrollTop <span>||</span> document<span>.</span>body<span>.</span>scrollTop<span>;</span>
		<span>for</span><span>(</span><span>var</span> i<span>=</span><span>0</span><span>;</span>i<span>&lt;</span>imgs<span>.</span>length<span>;</span>i<span>++</span><span>)</span><span>{</span>
			<span>//图片距离顶部的距离大于可视区域和滚动区域之和时懒加载</span>
			<span>if</span> <span>(</span><span>(</span>h<span>+</span>s<span>)</span><span>></span><span>getTop</span><span>(</span>imgs<span>[</span>i<span>]</span><span>)</span><span>)</span> <span>{</span>
				<span>// 真实情况是页面开始有2秒空白，所以使用setTimeout定时2s</span>
				<span>(</span><span>function</span><span>(</span><span>i</span><span>)</span><span>{</span>
					<span>setTimeout</span><span>(</span><span>function</span><span>(</span><span>)</span><span>{</span>
						<span>// 不加立即执行函数i会等于9</span>
						<span>// 隐形加载图片或其他资源，</span>
						<span>//创建一个临时图片，这个图片在内存中不会到页面上去。实现隐形加载</span>
						<span>var</span> temp <span>=</span> <span>new</span> <span>Image</span><span>(</span><span>)</span><span>;</span>
						temp<span>.</span>src <span>=</span> imgs<span>[</span>i<span>]</span><span>.</span><span>getAttribute</span><span>(</span><span>'data-src'</span><span>)</span><span>;</span><span>//只会请求一次</span>
						<span>// onload判断图片加载完毕，真是图片加载完毕，再赋值给dom节点</span>
						temp<span>.</span><span>onload</span> <span>=</span> <span>function</span><span>(</span><span>)</span><span>{</span>
							<span>// 获取自定义属性data-src，用真图片替换假图片</span>
							imgs<span>[</span>i<span>]</span><span>.</span>src <span>=</span> imgs<span>[</span>i<span>]</span><span>.</span><span>getAttribute</span><span>(</span><span>'data-src'</span><span>)</span>
						<span>}</span>
					<span>}</span><span>,</span><span>2000</span><span>)</span>
				<span>}</span><span>)</span><span>(</span>i<span>)</span>
			<span>}</span>
		<span>}</span>
	<span>}</span>
	<span>lazyload</span><span>(</span>imgs<span>)</span><span>;</span>

	<span>// 滚屏函数</span>
	window<span>.</span><span>onscroll</span> <span>=</span><span>function</span><span>(</span><span>)</span><span>{</span>
		<span>lazyload</span><span>(</span>imgs<span>)</span><span>;</span>
	<span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br></div></div></li>
</ul>
<h3 id="实现-jsonp"> 实现 JSONP</h3>
<ul>
<li>
<p>实现步骤：</p>
<ul>
<li>
<p>1.将传入的data数据转化为url字符串形式</p>
</li>
<li>
<p>2.处理url中的回调函数</p>
</li>
<li>
<p>3.创建一个script标签并插入到页面中</p>
</li>
<li>
<p>4.挂载回调函数</p>
<div><pre><code><span>(</span><span>function</span> <span>(</span><span>window<span>,</span> document</span><span>)</span> <span>{</span>
  <span>"use strict"</span>
  <span>var</span> <span>jsonp</span> <span>=</span> <span>function</span> <span>(</span><span>url<span>,</span> data<span>,</span> callback</span><span>)</span> <span>{</span>
    <span>// 1.将传入的data数据转化为url字符串形式</span>
    <span>// {id:1,name:'jack'} => id=1&amp;name=jack</span>
    <span>var</span> dataString <span>=</span> url<span>.</span><span>indexof</span><span>(</span><span>'?'</span><span>)</span> <span>==</span> <span>-</span><span>1</span> <span>?</span> <span>'?'</span> <span>:</span> <span>'&amp;'</span>
    <span>for</span> <span>(</span><span>var</span> key <span>in</span> data<span>)</span> <span>{</span>
      dataString <span>+=</span> key <span>+</span> <span>'='</span> <span>+</span> data<span>[</span>key<span>]</span> <span>+</span> <span>'&amp;'</span>
    <span>}</span>
    <span>// 2 处理url中的回调函数</span>
    <span>// cbFuncName回调函数的名字 ：my_json_cb_名字的前缀 + 随机数（把小数点去掉）</span>
    <span>var</span> cbFuncName <span>=</span> <span>'my_json_cb_'</span> <span>+</span> Math<span>.</span><span>random</span><span>(</span><span>)</span><span>.</span><span>toString</span><span>(</span><span>)</span><span>.</span><span>replace</span><span>(</span><span>'.'</span><span>,</span> <span>''</span><span>)</span>
    dataString <span>+=</span> <span>'callback='</span> <span>+</span> cbFuncName
    <span>// 3.创建一个script标签并插入到页面中</span>
    <span>var</span> scriptEle <span>=</span> document<span>.</span><span>createElement</span><span>(</span><span>'script'</span><span>)</span>
    scriptEle<span>.</span>src <span>=</span> url <span>+</span> dataString
    <span>// 4.挂载回调函数</span>
    window<span>[</span>cbFuncName<span>]</span> <span>=</span> <span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
      <span>callback</span><span>(</span>data<span>)</span>
      <span>// 处理完回调函数的数据之后，删除jsonp的script标签</span>
      document<span>.</span>body<span>.</span><span>removeChild</span><span>(</span>scriptEle<span>)</span>
    <span>}</span>
    document<span>.</span>body<span>.</span><span>appendChild</span><span>(</span>scriptEle<span>)</span>
  <span>}</span>
  window<span>.</span>$jsonp <span>=</span> jsonp
<span>}</span><span>)</span><span>(</span>window<span>,</span> document<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br></div></div></li>
</ul>
</li>
</ul>
<h3 id="实现单例模式"> 实现单例模式</h3>
<ul>
<li>
<p>概念：单例模式涉及到一个单一的类，该类负责创建自己的对象，同时确保只有单个对象被创建，这个类提供了一种访问其唯一的对象的方式，可以直接访问，不需要实例化该类的对象</p>
</li>
<li>
<p>要点：</p>
<ul>
<li>1、单例类只能有一个实例；</li>
<li>2、单例类必须自己创建自己的唯一实例；</li>
<li>3、单例类必须给所有其他对象提供这一实例</li>
</ul>
</li>
<li>
<p>实现：通过闭包保存对象实例的变量，每次新建/获取实例时候，判断该变量是否为空，为空则新建实例对象并返回，否则直接返回该实例</p>
<div><pre><code><span>let</span> <span>Singleton</span> <span>=</span> <span>function</span> <span>(</span><span>params</span><span>)</span> <span>{</span>
  <span>this</span><span>.</span>params <span>=</span> params
<span>}</span>

<span>Singleton</span><span>.</span>prototype<span>.</span><span>getParams</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
  <span>return</span> <span>this</span><span>.</span>params
<span>}</span>

<span>Singleton</span><span>.</span>prototype<span>.</span>getInstance <span>=</span> <span>(</span><span>function</span><span>(</span><span>params</span><span>)</span><span>{</span>
  <span>let</span> instance <span>=</span> <span>null</span>
  <span>return</span> <span>function</span><span>(</span><span>)</span><span>{</span>
    <span>if</span><span>(</span>instance<span>)</span><span>{</span>
      <span>return</span> instance
    <span>}</span><span>else</span> <span>{</span>
      instance <span>=</span> <span>new</span> <span>Singleton</span><span>(</span>params<span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span><span>)</span><span>(</span><span>)</span>

<span>//---------------使用--------------</span>
<span>var</span> a <span>=</span> Singleton<span>.</span><span>getInstance</span><span>(</span><span>'ConardLi'</span><span>)</span><span>;</span>
<span>var</span> b <span>=</span> Singleton<span>.</span><span>getInstance</span><span>(</span><span>'ConardLi2'</span><span>)</span><span>;</span>

console<span>.</span><span>log</span><span>(</span>a<span>===</span>b<span>)</span><span>;</span>   <span>//true</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br></div></div></li>
</ul>
<h3 id="观察者模式"> 观察者模式</h3>
<ul>
<li>
<p>定义：指多个对象间存在一对多的依赖关系，当一个对象的状态发生改变时，所有依赖于它的对象都得到通知并被自动更新。</p>
</li>
<li>
<p>优点：</p>
<ul>
<li>降低了目标与观察者之间的耦合关系，两者之间是抽象耦合关系。符合依赖倒置原则。</li>
<li>目标与观察者之间建立了一套触发机。</li>
</ul>
</li>
<li>
<p>缺点：</p>
<ul>
<li>目标与观察者之间的依赖关系并没有完全解除，而且有可能出现循环引用。</li>
<li>当观察者对象很多时，通知的发布会花费很多时间，影响程序的效率。</li>
</ul>
</li>
<li>
<p>结构：</p>
</li>
</ul>
<p>​	实现观察者模式时要注意具体目标对象和具体观察者对象之间不能直接调用，否则将使两者之间紧密耦合起来，这违反了面向对象的设计原则。</p>
<p>观察者模式的主要角色:</p>
<ol>
<li>抽象主题（Subject）角色：也叫抽象目标类，它提供了一个用于保存观察者对象的聚集类和增加、删除观察者对象的方法，以及通知所有观察者的抽象方法。</li>
<li>具体主题（Concrete Subject）角色：也叫具体目标类，它实现抽象目标中的通知方法，当具体主题的内部状态发生改变时，通知所有注册过的观察者对象。</li>
<li>抽象观察者（Observer）角色：它是一个抽象类或接口，它包含了一个更新自己的抽象方法，当接到具体主题的更改通知时被调用。</li>
<li>具体观察者（Concrete Observer）角色：实现抽象观察者中定义的抽象方法，以便在得到目标的更改通知时更新自身的状态。</li>
</ol>
<p>观察者模式结构图：</p>
<p><img src="./../images/观察者模式结构图.png" alt="" /></p>
<p>观察者模式：</p>
<p><img src="./../images/观察者模式.png" alt="" /></p>
<h4 id="通用实现"> 通用实现</h4>
<p>ObserverList 实现：</p>
<div><pre><code><span>function</span> <span>ObserverList</span><span>(</span><span>)</span><span>{</span> <span>//观察者队列，保存观察者对象</span>
  <span>this</span><span>.</span>observerList <span>=</span> <span>[</span><span>]</span><span>;</span>
<span>}</span>
 
<span>ObserverList</span><span>.</span>prototype<span>.</span><span>add</span> <span>=</span> <span>function</span><span>(</span> <span>obj</span> <span>)</span><span>{</span> <span>//添加观察者</span>
  <span>return</span> <span>this</span><span>.</span>observerList<span>.</span><span>push</span><span>(</span> obj <span>)</span><span>;</span>
<span>}</span><span>;</span>
 
<span>ObserverList</span><span>.</span>prototype<span>.</span><span>count</span> <span>=</span> <span>function</span><span>(</span><span>)</span><span>{</span> <span>//观察者数量</span>
  <span>return</span> <span>this</span><span>.</span>observerList<span>.</span>length<span>;</span>
<span>}</span><span>;</span>
 
<span>ObserverList</span><span>.</span>prototype<span>.</span><span>get</span> <span>=</span> <span>function</span><span>(</span> <span>index</span> <span>)</span><span>{</span> <span>//获取观察者</span>
  <span>if</span><span>(</span> index <span>></span> <span>-</span><span>1</span> <span>&amp;&amp;</span> index <span>&lt;</span> <span>this</span><span>.</span>observerList<span>.</span>length <span>)</span><span>{</span>
    <span>return</span> <span>this</span><span>.</span>observerList<span>[</span> index <span>]</span><span>;</span>
  <span>}</span>
<span>}</span><span>;</span>
  
<span>ObserverList</span><span>.</span>prototype<span>.</span><span>indexOf</span> <span>=</span> <span>function</span><span>(</span> <span>obj<span>,</span> startIndex</span> <span>)</span><span>{</span>  <span>//获取观察者位置</span>
  <span>let</span> i <span>=</span> startIndex<span>;</span>
  <span>while</span><span>(</span> i <span>&lt;</span> <span>this</span><span>.</span>observerList<span>.</span>length <span>)</span><span>{</span>
    <span>if</span><span>(</span> <span>this</span><span>.</span>observerList<span>[</span>i<span>]</span> <span>===</span> obj <span>)</span><span>{</span>
      <span>return</span> i<span>;</span>
    <span>}</span>
    i<span>++</span><span>;</span>
  <span>}</span>
 
  <span>return</span> <span>-</span><span>1</span><span>;</span>
<span>}</span><span>;</span>
 
<span>ObserverList</span><span>.</span>prototype<span>.</span><span>removeAt</span> <span>=</span> <span>function</span><span>(</span> <span>index</span> <span>)</span><span>{</span> <span>//移除观察者</span>
  <span>this</span><span>.</span>observerList<span>.</span><span>splice</span><span>(</span> index<span>,</span> <span>1</span> <span>)</span><span>;</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br></div></div><p>Subject对象 实现</p>
<div><pre><code><span>function</span> <span>Subject</span><span>(</span><span>)</span><span>{</span> 
  <span>this</span><span>.</span>observers <span>=</span> <span>new</span> <span>ObserverList</span><span>(</span><span>)</span><span>;</span>
<span>}</span>
 
<span>Subject</span><span>.</span>prototype<span>.</span><span>addObserver</span> <span>=</span> <span>function</span><span>(</span> <span>observer</span> <span>)</span><span>{</span>
  <span>this</span><span>.</span>observers<span>.</span><span>add</span><span>(</span> observer <span>)</span><span>;</span>
<span>}</span><span>;</span>
 
<span>Subject</span><span>.</span>prototype<span>.</span><span>removeObserver</span> <span>=</span> <span>function</span><span>(</span> <span>observer</span> <span>)</span><span>{</span>
  <span>this</span><span>.</span>observers<span>.</span><span>removeAt</span><span>(</span> <span>this</span><span>.</span>observers<span>.</span><span>indexOf</span><span>(</span> observer<span>,</span> <span>0</span> <span>)</span> <span>)</span><span>;</span>
<span>}</span><span>;</span>
 
<span>Subject</span><span>.</span>prototype<span>.</span><span>notify</span> <span>=</span> <span>function</span><span>(</span> <span>context</span> <span>)</span><span>{</span> <span>//通知观察者变更</span>
  <span>let</span> observerCount <span>=</span> <span>this</span><span>.</span>observers<span>.</span><span>count</span><span>(</span><span>)</span><span>;</span>
  <span>for</span><span>(</span><span>let</span> i<span>=</span><span>0</span><span>;</span> i <span>&lt;</span> observerCount<span>;</span> i<span>++</span><span>)</span><span>{</span>
    <span>this</span><span>.</span>observers<span>.</span><span>get</span><span>(</span>i<span>)</span><span>.</span><span>update</span><span>(</span> context <span>)</span><span>;</span>
  <span>}</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br></div></div><p>Observer对象实现</p>
<div><pre><code><span>function</span> <span>Observer</span> <span>(</span> data <span>=</span> <span>''</span> <span>)</span> <span>{</span>
    <span>this</span><span>.</span>data <span>=</span> data
    <span>Observer</span><span>.</span>prototype<span>.</span><span>update</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
        console<span>.</span><span>log</span><span>(</span><span><span>`</span><span>更新数据: </span><span><span>${</span><span>this</span><span>.</span>data<span>}</span></span><span>`</span></span><span>)</span>
    <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div><p>测试结果</p>
<div><pre><code><span>var</span> subject <span>=</span> <span>new</span> <span>Subject</span><span>(</span><span>)</span><span>;</span>
<span>var</span> observer1 <span>=</span> <span>new</span> <span>Observer</span><span>(</span><span>'im observer1'</span><span>)</span><span>;</span>
<span>var</span> observer2 <span>=</span> <span>new</span> <span>Observer</span><span>(</span><span>'im observer2'</span><span>)</span><span>;</span>
<span>var</span> observer3 <span>=</span> <span>new</span> <span>Observer</span><span>(</span><span>'im observer3'</span><span>)</span><span>;</span>

subject<span>.</span><span>addObserver</span><span>(</span>observer1<span>)</span><span>;</span>
subject<span>.</span><span>addObserver</span><span>(</span>observer2<span>)</span><span>;</span>
subject<span>.</span><span>addObserver</span><span>(</span>observer3<span>)</span><span>;</span>
subject<span>.</span><span>notify</span><span>(</span><span>)</span><span>;</span>

<span>// 输出结果：</span>
<span>// 更新数据: im observer1</span>
<span>// 更新数据: im observer2</span>
<span>// 更新数据: im observer3</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div><h4 id="defineproperty-实现"> <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/defineProperty" target="_blank" rel="noopener noreferrer">defineProperty</a> 实现</h4>
<div><pre><code><span>const</span> person <span>=</span> <span>{</span>
  name<span>:</span> <span>'SeWard'</span><span>,</span>
  age<span>:</span> <span>26</span>
<span>}</span><span>;</span>

Object<span>.</span><span>keys</span><span>(</span>person<span>)</span><span>.</span><span>forEach</span><span>(</span><span>function</span><span>(</span><span>key</span><span>)</span> <span>{</span> <span>//循环遍历观察对象属性</span>
  Object<span>.</span><span>defineProperty</span><span>(</span>person<span>,</span> key<span>,</span> <span>{</span>
    enumerable<span>:</span> <span>true</span><span>,</span>
    configurable<span>:</span> <span>true</span><span>,</span>
    <span>get</span><span>:</span> <span>function</span><span>(</span><span>)</span> <span>{</span>
      console<span>.</span><span>log</span><span>(</span><span>'get data:'</span><span>,</span> key<span>)</span><span>;</span>
    <span>}</span><span>,</span>
    <span>set</span><span>:</span> <span>function</span><span>(</span><span>newVal</span><span>)</span> <span>{</span>
      <span>// 当属性值发生变化时我们可以进行额外操作</span>
      console<span>.</span><span>log</span><span>(</span><span><span>`</span><span>set new value </span><span><span>${</span>newVal<span>}</span></span><span>`</span></span><span>)</span><span>;</span>
    <span>}</span><span>,</span>
  <span>}</span><span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>

person<span>.</span>name <span>=</span> <span>'my blog'</span><span>;</span>
<span>//输出：set new value my blog</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br></div></div><h4 id="proxy实现"> <a href="https://es6.ruanyifeng.com/#docs/proxy" target="_blank" rel="noopener noreferrer">Proxy</a>实现</h4>
<div><pre><code><span>const</span> observerList <span>=</span> <span>new</span> <span>Set</span><span>(</span><span>)</span><span>;</span> <span>//观察者队列</span>
<span>const</span> <span>subject</span> <span>=</span> <span>fn</span> <span>=></span> observerList<span>.</span><span>add</span><span>(</span>fn<span>)</span><span>;</span> <span>//订阅观察者对象</span>
<span>const</span> <span>observable</span> <span>=</span> <span>obj</span> <span>=></span> <span>new</span> <span>Proxy</span><span>(</span>obj<span>,</span> <span>{</span>  <span>//可观察对象</span>
  	<span>set</span><span>:</span> <span>function</span><span>(</span><span>target<span>,</span> key<span>,</span> value<span>,</span> receiver</span><span>)</span> <span>{</span>
      <span>const</span> result <span>=</span> Reflect<span>.</span><span>set</span><span>(</span>target<span>,</span> key<span>,</span> value<span>,</span> receiver<span>)</span><span>;</span>
      observerList<span>.</span><span>forEach</span><span>(</span><span>observer</span> <span>=></span> <span>observer</span><span>(</span><span>)</span><span>)</span><span>;</span> <span>//通知观察者队列所有观察者更新数据</span>
      <span>return</span> result<span>;</span>
	<span>}</span> 
<span>}</span><span>)</span><span>;</span>

<span>const</span> person <span>=</span> <span>observable</span><span>(</span><span>{</span>
  name<span>:</span> <span>'SeWard'</span><span>,</span>
  age<span>:</span> <span>26</span>
<span>}</span><span>)</span><span>;</span>

<span>function</span> <span>update</span><span>(</span><span>)</span> <span>{</span>
  console<span>.</span><span>log</span><span>(</span><span><span>`</span><span>update: </span><span><span>${</span>person<span>.</span>name<span>}</span></span><span>, </span><span><span>${</span>person<span>.</span>age<span>}</span></span><span>`</span></span><span>)</span>
<span>}</span>

<span>subject</span><span>(</span>update<span>)</span><span>;</span>

<span>//测试</span>
person<span>.</span>age <span>=</span> <span>25</span>
<span>//输出 update: SeWard, 25</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br></div></div><h3 id="发布订阅者模式"> 发布订阅者模式</h3>
<ul>
<li>定义:</li>
</ul>
<p>发布订阅者模式（Publish/Subscribe），事件的发布者只发布事件，不关心这个事件被谁获取了，通常将事件发给一个中间件，由中间件再去分发事件</p>
<p>事件的订阅者只关心事件本身，不关心这个事件是谁发布的，通常在中间件中去注册观察某个事件。</p>
<p>中间件中去维护事件类别对应的订阅者列表，当收到事件后，去对应列表中通知订阅者</p>
<ul>
<li>结构</li>
</ul>
<p><img src="./../images/发布订阅者模式.png" alt="" /></p>
<ul>
<li>实现</li>
</ul>
<div><pre><code><span>const</span> Observer <span>=</span> <span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
    <span>let</span> topics <span>=</span> <span>{</span><span>}</span><span>;</span> <span>// 回调函数存放的数组</span>
    <span>return</span> <span>{</span>
        <span>// 订阅方法,将订阅者注册的消息推入到消息队列中，因此我们需要接受两个参数：消息类型以及相应的处理动作</span>
        <span>subscribe</span><span>:</span> <span>function</span> <span>(</span><span>type<span>,</span> fn</span><span>)</span> <span>{</span>
            <span>if</span> <span>(</span><span>!</span>topics<span>[</span>type<span>]</span><span>)</span> <span>{</span> <span>// 如果此消息不存在则创建</span>
                topics<span>[</span>type<span>]</span> <span>=</span> <span>[</span><span>]</span><span>;</span>
            <span>}</span>
            topics<span>[</span>type<span>]</span><span>.</span><span>push</span><span>(</span>fn<span>)</span><span>;</span>  <span>// 将动作推入到该消息对应的动作执行序列中</span>
        <span>}</span><span>,</span>
      <span>// 发布方法,当观察者发布一个消息时将所有订阅者订阅的消息一次执行。因此应接受两个参数：消息类型以及动作执行时需要传递的参数</span>
        <span>publish</span><span>:</span> <span>function</span> <span>(</span><span>type<span>,</span> args</span><span>)</span> <span>{</span>
            <span>if</span> <span>(</span><span>!</span>topics<span>[</span>type<span>]</span><span>)</span> <span>{</span>  <span>// 如果该消息没有被注册，则返回</span>
                <span>return</span><span>;</span>
            <span>}</span>
            <span>let</span> self <span>=</span> <span>this</span><span>;</span>
            <span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
                <span>let</span> subscribers <span>=</span> topics<span>[</span>type<span>]</span><span>,</span> len <span>=</span> subscribers <span>?</span> subscribers<span>.</span>length<span>:</span> <span>0</span><span>;</span>
                <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> len<span>;</span> i<span>++</span><span>)</span> <span>{</span>
                    subscribers<span>[</span>i<span>]</span><span>.</span><span>call</span><span>(</span>self<span>,</span> args<span>)</span><span>;</span> <span>// 依次执行注册的消息对应的动作序列</span>
                <span>}</span>
            <span>}</span><span>,</span> <span>0</span><span>)</span><span>;</span>
        <span>}</span><span>,</span>
        <span>// 取消订阅方法,将订阅者注销的消息从消息队列中清除，因此应接受两个参数：消息类型以及执行的某一动作。</span>
        <span>unsubscribe</span><span>:</span><span>function</span> <span>(</span><span>type<span>,</span> fn</span><span>)</span> <span>{</span>
            <span>let</span> subscribers <span>=</span> topics<span>[</span>type<span>]</span><span>;</span>
            <span>if</span> <span>(</span>subscribers <span>instanceof</span> <span>Array</span><span>)</span> <span>{</span> <span>// 判断是否存在</span>
                <span>let</span> i <span>=</span> subscribers<span>.</span>length <span>-</span> <span>1</span><span>;</span>
                <span>for</span> <span>(</span><span>;</span>i <span>>=</span> <span>0</span><span>;</span> i<span>--</span> <span>)</span> <span>{</span>
                    <span>// 如果存在该动作则在消息动作序列中移除响应动作</span>
                    subscribers<span>[</span>i<span>]</span> <span>===</span> fn <span>&amp;&amp;</span> subscribers<span>.</span><span>splice</span><span>(</span>i<span>,</span> <span>1</span><span>)</span><span>;</span>
                <span>}</span>
            <span>}</span>
        <span>}</span>
    <span>}</span>
<span>}</span><span>)</span><span>(</span><span>)</span><span>;</span>

<span>//-----------测试--------------------</span>

<span>var</span> <span>listener1</span> <span>=</span> <span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
	console<span>.</span><span>log</span><span>(</span><span>'listener1:'</span> <span>+</span> data<span>)</span><span>;</span>
<span>}</span>
<span>var</span> <span>listener2</span> <span>=</span> <span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
	console<span>.</span><span>log</span><span>(</span><span>'listener2:'</span> <span>+</span> data<span>)</span><span>;</span>
<span>}</span>
Observer<span>.</span><span>subscribe</span><span>(</span><span>'example1'</span><span>,</span> listener1<span>)</span><span>;</span>
Observer<span>.</span><span>subscribe</span><span>(</span><span>'example1'</span><span>,</span> listener2<span>)</span><span>;</span>
 
<span>//发布通知</span>
Observer<span>.</span><span>publish</span><span>(</span><span>'example1'</span><span>,</span> <span>'hello world!'</span><span>)</span><span>;</span>
Observer<span>.</span><span>publish</span><span>(</span><span>'example1'</span><span>,</span> <span>[</span><span>'test'</span><span>,</span> <span>'a'</span><span>,</span> <span>'b'</span><span>,</span> <span>'c'</span><span>]</span><span>)</span><span>;</span>

<span>//----结果-----</span>
listener1<span>:</span>hello world<span>!</span>
listener2<span>:</span>hello world<span>!</span>
listener1<span>:</span>test<span>,</span>a<span>,</span>b<span>,</span>c
listener2<span>:</span>test<span>,</span>a<span>,</span>b<span>,</span>c
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br></div></div><h3 id="实现generator"> 实现Generator</h3>
<ul>
<li>
<p><a href="https://es6.ruanyifeng.com/#docs/generator" target="_blank" rel="noopener noreferrer">Generator</a>使用示例:</p>
<div><pre><code><span>function</span><span>*</span> <span>foo</span><span>(</span><span>)</span> <span>{</span>
 <span>yield</span> <span>'result1'</span>
 <span>yield</span> <span>'result2'</span>
 <span>yield</span> <span>'result3'</span>
<span>}</span>

<span>const</span> gen <span>=</span> <span>foo</span><span>(</span><span>)</span>
console<span>.</span><span>log</span><span>(</span>gen<span>.</span><span>next</span><span>(</span><span>)</span><span>.</span>value<span>)</span>
console<span>.</span><span>log</span><span>(</span>gen<span>.</span><span>next</span><span>(</span><span>)</span><span>.</span>value<span>)</span>
console<span>.</span><span>log</span><span>(</span>gen<span>.</span><span>next</span><span>(</span><span>)</span><span>.</span>value<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div></li>
<li>
<p>根据示例模式实现:</p>
<ul>
<li>
<p>原理：Generator实现的核心在于上下文的保存，函数并没有真的被挂起，每一次yield，其实都执行了一遍传入的生成器函数，只是在这个过程中间用了一个context对象储存上下文，使得每次执行生成器函数的时候，都可以从上一个执行结果开始执行，看起来就像函数被挂起了一样</p>
</li>
<li>
<p><a href="https://juejin.im/post/5bd85cfbf265da0a9e535c10" target="_blank" rel="noopener noreferrer">源码参考</a></p>
<div><pre><code><span>/**
 * 生成器函数根据yield语句将代码分割为switch-case块，后续通过切换_context.prev和_context.next来分别执行各个case
 */</span> 
<span>function</span> <span>gen$</span><span>(</span><span>_context</span><span>)</span> <span>{</span>
  <span>while</span> <span>(</span><span>1</span><span>)</span> <span>{</span>
    <span>switch</span> <span>(</span>_context<span>.</span>prev <span>=</span> _context<span>.</span>next<span>)</span> <span>{</span>
      <span>case</span> <span>0</span><span>:</span>
        _context<span>.</span>next <span>=</span> <span>2</span><span>;</span>
        <span>return</span> <span>'result1'</span><span>;</span>

      <span>case</span> <span>2</span><span>:</span>
        _context<span>.</span>next <span>=</span> <span>4</span><span>;</span>
        <span>return</span> <span>'result2'</span><span>;</span>

      <span>case</span> <span>4</span><span>:</span>
        _context<span>.</span>next <span>=</span> <span>6</span><span>;</span>
        <span>return</span> <span>'result3'</span><span>;</span>

      <span>case</span> <span>6</span><span>:</span>
      <span>case</span> <span>"end"</span><span>:</span>
        <span>return</span> _context<span>.</span><span>stop</span><span>(</span><span>)</span><span>;</span>
    <span>}</span>
  <span>}</span>
<span>}</span>

<span>// context 用于储存函数执行上下文</span>
<span>var</span> context <span>=</span> <span>{</span>
  next<span>:</span> <span>0</span><span>,</span>
  prev<span>:</span> <span>0</span><span>,</span>
  done<span>:</span> <span>false</span><span>,</span>
  <span>stop</span><span>:</span> <span>function</span> <span>stop</span><span>(</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>done <span>=</span> <span>true</span>
  <span>}</span>
<span>}</span>

<span>/**
 * invoke 定义next()，用于执行gen$(_context)来跳到下一步
 * 当我们调用g.next()，就相当于调用invoke()方法
 */</span>
<span>let</span> <span>gen</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
  <span>return</span> <span>{</span>
    <span>next</span><span>:</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
      value <span>=</span> context<span>.</span>done <span>?</span> <span>undefined</span> <span>:</span> <span>gen$</span><span>(</span>context<span>)</span>
      done <span>=</span> context<span>.</span>done
      <span>return</span> <span>{</span>
        value<span>,</span>
        done
      <span>}</span>
    <span>}</span>
  <span>}</span>
<span>}</span>

<span>// 测试使用</span>
<span>var</span> g <span>=</span> <span>gen</span><span>(</span><span>)</span>
g<span>.</span><span>next</span><span>(</span><span>)</span>  <span>// {value: "result1", done: false}</span>
g<span>.</span><span>next</span><span>(</span><span>)</span>  <span>// {value: "result2", done: false}</span>
g<span>.</span><span>next</span><span>(</span><span>)</span>  <span>// {value: "result3", done: false}</span>
g<span>.</span><span>next</span><span>(</span><span>)</span>  <span>// {value: undefined, done: true}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br></div></div></li>
</ul>
</li>
</ul>
<h3 id="实现-async-await"> 实现 async await</h3>
<ul>
<li>
<p>async 函数的实现原理，就是将 Generator 函数和自动执行器，包装在一个函数里</p>
</li>
<li>
<p>实现一：<a href="https://es6.ruanyifeng.com/#docs/async#async-%E5%87%BD%E6%95%B0%E7%9A%84%E5%AE%9E%E7%8E%B0%E5%8E%9F%E7%90%86" target="_blank" rel="noopener noreferrer">《ES6 教程》中实现</a></p>
<div><pre><code><span>async</span> <span>function</span> <span>fn</span><span>(</span><span>args</span><span>)</span> <span>{</span>
  <span>// ...</span>
<span>}</span>

<span>// 等同于</span>

<span>function</span> <span>fn</span><span>(</span><span>args</span><span>)</span> <span>{</span>
  <span>return</span> <span>spawn</span><span>(</span><span>function</span><span>*</span> <span>(</span><span>)</span> <span>{</span>
    <span>// ...</span>
  <span>}</span><span>)</span><span>;</span>
<span>}</span>

<span>// spawn 方法实现</span>
<span>function</span> <span>spawn</span><span>(</span><span>genF</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    <span>const</span> gen <span>=</span> <span>genF</span><span>(</span><span>)</span><span>;</span>
    <span>function</span> <span>step</span><span>(</span><span>nextF</span><span>)</span> <span>{</span>
      <span>let</span> next<span>;</span>
      <span>try</span> <span>{</span>
        next <span>=</span> <span>nextF</span><span>(</span><span>)</span><span>;</span>
      <span>}</span> <span>catch</span><span>(</span>e<span>)</span> <span>{</span>
        <span>return</span> <span>reject</span><span>(</span>e<span>)</span><span>;</span>
      <span>}</span>
      <span>if</span><span>(</span>next<span>.</span>done<span>)</span> <span>{</span>
        <span>return</span> <span>resolve</span><span>(</span>next<span>.</span>value<span>)</span><span>;</span>
      <span>}</span>
      Promise<span>.</span><span>resolve</span><span>(</span>next<span>.</span>value<span>)</span><span>.</span><span>then</span><span>(</span><span>function</span><span>(</span><span>v</span><span>)</span> <span>{</span>
        <span>step</span><span>(</span><span>function</span><span>(</span><span>)</span> <span>{</span> <span>return</span> gen<span>.</span><span>next</span><span>(</span>v<span>)</span><span>;</span> <span>}</span><span>)</span><span>;</span>
      <span>}</span><span>,</span> <span>function</span><span>(</span><span>e</span><span>)</span> <span>{</span>
        <span>step</span><span>(</span><span>function</span><span>(</span><span>)</span> <span>{</span> <span>return</span> gen<span>.</span><span>throw</span><span>(</span>e<span>)</span><span>;</span> <span>}</span><span>)</span><span>;</span>
      <span>}</span><span>)</span><span>;</span>
    <span>}</span>
    <span>step</span><span>(</span><span>function</span><span>(</span><span>)</span> <span>{</span> <span>return</span> gen<span>.</span><span>next</span><span>(</span><span>undefined</span><span>)</span><span>;</span> <span>}</span><span>)</span><span>;</span>
  <span>}</span><span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br></div></div><ul>
<li>所有的<code>async</code>函数都可以写成上面的第二种形式，其中的<code>spawn</code>函数就是自动执行器</li>
</ul>
</li>
<li>
<p>实现二：</p>
</li>
</ul>
<div><pre><code><span>function</span> <span>asyncToGenerator</span><span>(</span><span>generatorFunc</span><span>)</span> <span>{</span>
  <span>// 返回的是一个新的函数</span>
  <span>return</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>

    <span>// 先调用generator函数 生成迭代器</span>
    <span>// 对应 var gen = testG()</span>
    <span>const</span> gen <span>=</span> <span>generatorFunc</span><span>.</span><span>apply</span><span>(</span><span>this</span><span>,</span> arguments<span>)</span>

    <span>// 返回一个promise 因为外部是用.then的方式 或者await的方式去使用这个函数的返回值的</span>
    <span>// var test = asyncToGenerator(testG)</span>
    <span>// test().then(res => console.log(res))</span>
    <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>

      <span>// 内部定义一个step函数 用来一步一步的跨过yield的阻碍</span>
      <span>// key有next和throw两种取值，分别对应了gen的next和throw方法</span>
      <span>// arg参数则是用来把promise resolve出来的值交给下一个yield</span>
      <span>function</span> <span>step</span><span>(</span><span>key<span>,</span> arg</span><span>)</span> <span>{</span>
        <span>let</span> generatorResult

        <span>// 这个方法需要包裹在try catch中</span>
        <span>// 如果报错了 就把promise给reject掉 外部通过.catch可以获取到错误</span>
        <span>try</span> <span>{</span>
          generatorResult <span>=</span> gen<span>[</span>key<span>]</span><span>(</span>arg<span>)</span>
        <span>}</span> <span>catch</span> <span>(</span>error<span>)</span> <span>{</span>
          <span>return</span> <span>reject</span><span>(</span>error<span>)</span>
        <span>}</span>

        <span>// gen.next() 得到的结果是一个 { value, done } 的结构</span>
        <span>const</span> <span>{</span> value<span>,</span> done <span>}</span> <span>=</span> generatorResult

        <span>if</span> <span>(</span>done<span>)</span> <span>{</span>
          <span>// 如果已经完成了 就直接resolve这个promise</span>
          <span>// 这个done是在最后一次调用next后才会为true</span>
          <span>// 以本文的例子来说 此时的结果是 { done: true, value: 'success' }</span>
          <span>// 这个value也就是generator函数最后的返回值</span>
          <span>return</span> <span>resolve</span><span>(</span>value<span>)</span>
        <span>}</span> <span>else</span> <span>{</span>
          <span>// 除了最后结束的时候外，每次调用gen.next()</span>
          <span>// 其实是返回 { value: Promise, done: false } 的结构，</span>
          <span>// 这里要注意的是Promise.resolve可以接受一个promise为参数</span>
          <span>// 并且这个promise参数被resolve的时候，这个then才会被调用</span>
          <span>return</span> Promise<span>.</span><span>resolve</span><span>(</span>
            <span>// 这个value对应的是yield后面的promise</span>
            value
          <span>)</span><span>.</span><span>then</span><span>(</span>
            <span>// value这个promise被resove的时候，就会执行next</span>
            <span>// 并且只要done不是true的时候 就会递归的往下解开promise</span>
            <span>// 对应gen.next().value.then(value => {</span>
            <span>//    gen.next(value).value.then(value2 => {</span>
            <span>//       gen.next() </span>
            <span>//</span>
            <span>//      // 此时done为true了 整个promise被resolve了 </span>
            <span>//      // 最外部的test().then(res => console.log(res))的then就开始执行了</span>
            <span>//    })</span>
            <span>// })</span>
            <span>function</span> <span>onResolve</span><span>(</span><span>val</span><span>)</span> <span>{</span>
              <span>step</span><span>(</span><span>"next"</span><span>,</span> val<span>)</span>
            <span>}</span><span>,</span>
            <span>// 如果promise被reject了 就再次进入step函数</span>
            <span>// 不同的是，这次的try catch中调用的是gen.throw(err)</span>
            <span>// 那么自然就被catch到 然后把promise给reject掉啦</span>
            <span>function</span> <span>onReject</span><span>(</span><span>err</span><span>)</span> <span>{</span>
              <span>step</span><span>(</span><span>"throw"</span><span>,</span> err<span>)</span>
            <span>}</span><span>,</span>
          <span>)</span>
        <span>}</span>
      <span>}</span>
      <span>step</span><span>(</span><span>"next"</span><span>)</span>
    <span>}</span><span>)</span>
  <span>}</span>
<span>}</span>

</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br></div></div><ul>
<li>参考来源：<a href="https://juejin.cn/post/6844904102053281806" target="_blank" rel="noopener noreferrer">《手写async await的最简实现（20行）》</a></li>
</ul>
<hr>
<h2 id="应用题"> 应用题</h2>
<h3 id="图片预加载和懒加载"> 图片预加载和懒加载</h3>
<p><strong>图片预加载：</strong></p>
<ul>
<li>概念：将所需的资源提前请求加载到本地，这样后面在需要用到时就直接从缓存取资源。</li>
<li>作用：减少用户的等待时间，提高用户的体验</li>
<li>方法：使用js中的image对象，通过为image对象来设置scr属性，来实现图片的预加载</li>
</ul>
<p><strong>图片懒加载：</strong></p>
<ul>
<li>
<p>概念：懒加载也叫延迟加载，指的是在长网页中延迟加载图片的时机，当用户需要访问时，再去加载。</p>
</li>
<li>
<p>作用：</p>
<ul>
<li>提高网站的首屏加载速度，提升用户的体验</li>
<li>减少请求数或延迟请求数，减少服务器的压力</li>
</ul>
</li>
<li>
<p>适用场景：图片很多，页面很长的场景</p>
</li>
<li>
<p>懒加载图示：<img src="./../images/lanjiazai.gif" alt="" /></p>
<ul>
<li>图片滚动到可视区域时，浏览器才真正请求图片资源</li>
</ul>
</li>
<li>
<p>实现原理：</p>
<ul>
<li>一张图片就是一个<code>&lt;img&gt;</code>标签，浏览器是否发起请求图片是根据<code>&lt;img&gt;</code>的 src 属性，所以实现懒加载的关键就是，在图片没有进入可视区域时，先不给<code>&lt;img&gt;</code>的 src 赋值，这样浏览器就不会发送请求了;</li>
<li>将页面上的图片的 src 属性设置为空字符串，将图片的真实路径保存在一个自定义属性中</li>
<li>当页面滚动的时候，进行判断，如果图片进入页面可视区域内，则从自定义属性中取出真实路径赋值给图片的 src 属性，此时浏览器才会真正请求加载图片资源。</li>
</ul>
</li>
<li>
<p>实现步骤：</p>
<ul>
<li>初始图片效果
<ul>
<li>方式1：加载loading图片</li>
<li>方式2：<a href="https://developer.mozilla.org/zh-CN/docs/Web/Progressive_web_apps/Loading#%E7%94%A8_css_%E5%88%B6%E9%80%A0%E6%A8%A1%E7%B3%8A%E6%95%88%E6%9E%9C" target="_blank" rel="noopener noreferrer">用 CSS 制造模糊效果</a></li>
</ul>
</li>
<li>按需加载：判断哪些图片要加载【重点】。
<ul>
<li>方式1：<a href="https://developer.mozilla.org/en-US/docs/Web/API/Intersection_Observer_API" target="_blank" rel="noopener noreferrer">Intersection_Observer</a>：应用会新建一个它的实例。当一个或多个监听对象跟 Observer 发生交互时（即图片出现在视口中时），作为参数传递的函数可以用来处理一些回调事务（例如图片加载）。我们可以迭代每一个对象，并对它们进行相应的处理：当图片可见时，我们开始加载真正的图片并且停止监听这张图片，因为在图片加载完成之后，我们已经没必要再知道它的状态了。</li>
<li>方式2：使用 JS 监听并计算</li>
</ul>
</li>
<li>隐形加载图片</li>
<li>替换真图片</li>
</ul>
</li>
<li>
<p>判断图片是否进入可视区域：</p>
<ul>
<li>
<p>方式2：使用 JS 监听并计算。</p>
<ul>
<li>
<p>让在浏览器可视区域的图片显示，可视区域外的不显示，所以当图片距离顶部的距离top-height等于可视区域h和滚动区域高度s之和时说明图片马上就要进入可视区了，就是说当 <code>top-height&lt;=s+h</code> 时，图片在可视区</p>
<p><img src="./../images/lanjiazai.png" alt="" /></p>
</li>
<li>
<p>使用 API：</p>
<ul>
<li>页可见区域宽： document.body.clientWidth;</li>
<li>网页可见区域高： document.body.clientHeight;</li>
<li>网页可见区域宽： document.body.offsetWidth (包括边线的宽);</li>
<li>网页可见区域高： document.body.offsetHeight (包括边线的宽);</li>
<li>网页正文全文宽： document.body.scrollWidth;</li>
<li>网页正文全文高： document.body.scrollHeight;</li>
<li>网页被卷去的高： document.body.scrollTop;</li>
<li>网页被卷去的左： document.body.scrollLeft;</li>
<li>网页正文部分上： window.screenTop;</li>
<li>网页正文部分左： window.screenLeft;</li>
<li>屏幕分辨率的高： window.screen.height;</li>
<li>屏幕分辨率的宽： window.screen.width;</li>
<li>屏幕可用工作区高度： window.screen.availHeight;</li>
<li>HTMLElement.offsetTop 为只读属性，它返回当前元素相对于其 offsetParent 元素的顶部的距离。</li>
<li>window.innerHeight：浏览器窗口的视口（viewport）高度（以像素为单位）；如果有水平滚动条，也包括滚动条高度。</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>图片预加载和懒加载比较：</strong></p>
<ul>
<li>两者的行为是相反的，一个是提前加载，一个是迟缓甚至不加载。</li>
<li>懒加载对服务器前端有一定的缓解压力作用，预加载则会增加服务器前端压力</li>
</ul>
<p><strong>常用插件</strong></p>
<p><strong>参考资料：</strong></p>
<p><a href="https://blog.csdn.net/w1418899532/article/details/90515969" target="_blank" rel="noopener noreferrer">js实现图片懒加载原理</a></p>
<p><a href="https://juejin.cn/post/6844903614138286094" target="_blank" rel="noopener noreferrer">懒加载和预加载</a></p>
<h3 id="异步循环"> 异步循环</h3>
<blockquote>
<p>异步循环：在循环中包含异步操作</p>
</blockquote>
<ul>
<li>
<p>解决思路： promise + async wait: 使用promise封装异步操作，使用async await执行循环</p>
<div><pre><code><span>let</span> <span>getSomething</span> <span>=</span> <span>function</span> <span>(</span><span>param</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>reslove<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>
    <span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
      <span>reslove</span><span>(</span><span><span>`</span><span>get </span><span><span>${</span>param<span>}</span></span><span>`</span></span><span>)</span>
    <span>}</span><span>,</span> <span>1000</span><span>)</span><span>;</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>let</span> <span>getAll</span> <span>=</span> <span>async</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
  <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> <span>10</span><span>;</span> i<span>++</span><span>)</span> <span>{</span>
    <span>let</span> result <span>=</span> <span>await</span> <span>getSomething</span><span>(</span>i<span>)</span>
    console<span>.</span><span>log</span><span>(</span>result<span>)</span>
  <span>}</span>
<span>}</span>

<span>getAll</span><span>(</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div></li>
</ul>
<h3 id="url-拆解问题"> URL 拆解问题</h3>
<blockquote>
<ul>
<li>--- 题目描述 --</li>
<li>实现一个函数，可以对 url 中的 query 部分做拆解，返回一个 key: value 形式的 object</li>
<li>--- 实例 ---</li>
<li>输入：'http://sample.com/?a=1&amp;e&amp;b=2&amp;c=xx&amp;d#hash'</li>
<li>输出：{a: 1, b: 2, c: 'xx', d: ''}</li>
</ul>
</blockquote>
<div><pre><code><span>function</span> <span>getQueryObj</span><span>(</span><span>url</span><span>)</span> <span>{</span>
    <span>// TODO</span>
    <span>let</span> arr <span>=</span> url<span>.</span><span>split</span><span>(</span><span>'?'</span><span>)</span><span>[</span><span>1</span><span>]</span><span>.</span><span>split</span><span>(</span><span>'#'</span><span>)</span><span>[</span><span>0</span><span>]</span><span>.</span><span>split</span><span>(</span><span>'&amp;'</span><span>)</span><span>;</span>
    <span>const</span> res <span>=</span> <span>{</span><span>}</span><span>;</span>
    arr<span>.</span><span>forEach</span><span>(</span><span>e</span> <span>=></span> <span>{</span>
        <span>const</span> <span>[</span>key<span>,</span> value<span>]</span> <span>=</span> e<span>.</span><span>split</span><span>(</span><span>'='</span><span>)</span><span>;</span>
        <span>if</span> <span>(</span><span>!</span>value<span>)</span> <span>{</span>
            res<span>[</span>key<span>]</span> <span>=</span> <span>''</span><span>;</span>
        <span>}</span> <span>else</span> <span>{</span>
            res<span>[</span>key<span>]</span> <span>=</span> value<span>;</span>
        <span>}</span>
    <span>}</span><span>)</span>
    <span>return</span> res<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div><blockquote>
<ul>
<li>--- 题目描述 ---</li>
<li>实现一个 parseParam 函数，将 url 转化为指定结果</li>
<li>--- 测试用例 ---</li>
<li>输入：url = 'http://www.domain.com/?user=anonymous&amp;id=123&amp;id=456&amp;city=%E5%8C%97%E4%BA%AC&amp;enabled'</li>
<li>输出：
{
user:'anonymous',
id:[123,456],// 重复出现的 key 要组装成数组，能被转成数字的就转成数字类型
city:'北京',// 中文需解码
enabled: true // 未指定值的 key 与约定为 true
}</li>
</ul>
</blockquote>
<div><pre><code><span>const</span> <span>parseParam</span> <span>=</span> <span>(</span><span>url</span><span>)</span> <span>=></span> <span>{</span>
    <span>const</span> arr <span>=</span> url<span>.</span><span>split</span><span>(</span><span>'?'</span><span>)</span><span>[</span><span>1</span><span>]</span><span>.</span><span>split</span><span>(</span><span>'&amp;'</span><span>)</span><span>;</span>
    <span>const</span> res <span>=</span> <span>{</span><span>}</span><span>;</span>
    arr<span>.</span><span>forEach</span><span>(</span><span>(</span><span>e</span><span>)</span> <span>=></span> <span>{</span>
        <span>const</span> <span>[</span>key<span>,</span> value<span>]</span> <span>=</span> e<span>.</span><span>split</span><span>(</span><span>'='</span><span>)</span><span>;</span>
        <span>if</span> <span>(</span>value <span>===</span> <span>undefined</span><span>)</span> <span>{</span>
            res<span>[</span>key<span>]</span> <span>=</span> <span>true</span><span>;</span>
        <span>}</span> <span>else</span> <span>{</span>
            <span>if</span> <span>(</span>key <span>in</span> res<span>)</span> <span>{</span>
                Array<span>.</span><span>isArray</span><span>(</span>res<span>[</span>key<span>]</span><span>)</span> <span>?</span> res<span>[</span>key<span>]</span><span>.</span><span>push</span><span>(</span>value<span>)</span> <span>:</span> res<span>[</span>key<span>]</span> <span>=</span> <span>[</span>res<span>[</span>key<span>]</span><span>]</span><span>.</span><span>concat</span><span>(</span>value<span>)</span><span>;</span>
            <span>}</span> <span>else</span> <span>{</span>
                res<span>[</span>key<span>]</span> <span>=</span> <span>decodeURI</span><span>(</span>value<span>)</span>
            <span>}</span>
        <span>}</span>
    <span>}</span><span>)</span>
    <span>return</span> res<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br></div></div><h3 id="将-http-header-转换成对象"> 将 HTTP header 转换成对象</h3>
<blockquote>
<ul>
<li>
<p>--- 题目描述 ---</p>
</li>
<li>
<p>实现一个方法，把 HTTP 文本形式(字符串)的 header 转换成 JS 对象。</p>
</li>
<li>
<p>--- 测试用例 ---</p>
</li>
<li>
<p>输入：</p>
<p>`Accept-Ranges: bytes</p>
<p>Cache-Control: max-age=6000, public</p>
<p>Connection: keep-alive</p>
<p>Content-Type: application/javascript`</p>
</li>
<li>
<p>输出：</p>
<p>{</p>
<p>&quot;Accept-Ranges&quot;: &quot;bytes&quot;,</p>
<p>&quot;Cache-Control&quot;: &quot;max-age=6000, public&quot;,</p>
<p>Connection: &quot;keep-alive&quot;,</p>
<p>&quot;Content-Type&quot;: &quot;application/javascript&quot;</p>
<p>}</p>
</li>
</ul>
</blockquote>
<p>解题思路：</p>
<ul>
<li>首先将每行数据作为数组的一个元素</li>
<li>将每个元素使用冒号分割，前面为 <code>key</code>，后面为 <code>value</code>。</li>
</ul>
<div><pre><code><span>const</span> <span>parseHeader</span> <span>=</span> <span>(</span><span>s</span><span>)</span> <span>=></span> <span>{</span>
    <span>let</span> res <span>=</span> <span>{</span><span>}</span><span>;</span>
    <span>let</span> arr <span>=</span> s<span>.</span><span>split</span><span>(</span><span>"\n"</span><span>)</span><span>;</span> <span>//获取单行字符串</span>
    arr<span>.</span><span>forEach</span><span>(</span><span>(</span><span>e</span><span>)</span> <span>=></span> <span>{</span>
        <span>let</span> tmp <span>=</span> e<span>.</span><span>split</span><span>(</span><span>": "</span><span>)</span><span>;</span>
        res<span>[</span>tmp<span>[</span><span>0</span><span>]</span><span>]</span> <span>=</span> tmp<span>[</span><span>1</span><span>]</span><span>;</span>
    <span>}</span><span>)</span>
    <span>return</span> res<span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div><p><a href="https://github.com/mqyqingfeng/Blog" target="_blank" rel="noopener noreferrer">冴羽的博客</a></p>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-25T19:47:29.000Z</published>
  </entry>
  <entry>
    <title type="html">操作系统</title>
    <id>https://sewar-x.github.io/operating-system/</id>
    <link href="https://sewar-x.github.io/operating-system/"/>
    <updated>2021-07-03T15:53:56.000Z</updated>
    <content type="html"><![CDATA[<h2 id="操作系统"> 操作系统</h2>
<h2 id="操作系统概述"> 操作系统概述</h2>
<p><img src="./../images/operaSystem-charper1-1.png" alt="" /></p>
<p><img src="./../images/operaSystem-charper1-2.png" alt="" /></p>
<p><img src="./../images/operaSystem-charper1-3.png" alt="" /></p>
<p><img src="./../images/operaSystem-charper1-4.png" alt="" /></p>
<h2 id="程序执行"> 程序执行</h2>
<p><img src="./../images/operaSystem-charpe2-1.png" alt="" /></p>
<h2 id="进程"> 进程</h2>
<p><img src="./../images/operaSystem-charpe2-2.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe2-3.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe2-4.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe2-5.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe2-6.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe2-7.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe2-8.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe2-9.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe2-10.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe2-11.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe2-12.png" alt="" /></p>
<h2 id="线程"> 线程</h2>
<p><img src="./../images/operaSystem-charpe2-13.png" alt="" /><img src="./../images/operaSystem-charpe2-14.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe2-15.png" alt="" /></p>
<h2 id="处理机调度"> 处理机调度</h2>
<p><img src="./../images/operaSystem-charpe3-1.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe3-2.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe3-3.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe3-4.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe3-5.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe3-6.png" alt="" /></p>
<h2 id="死锁"> 死锁</h2>
<p><img src="./../images/operaSystem-charpe3-777.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe3-8.png" alt="" /><img src="./../images/operaSystem-charpe3-9.png" alt="" /></p>
<h2 id="存储器"> 存储器</h2>
<p><img src="./../images/operaSystem-charpe4-1.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe4-2.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe4-3.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe4-4.png" alt="" /></p>
<h2 id="存储管理"> 存储管理</h2>
<p><img src="./../images/operaSystem-charpe5-1.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe5-2.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe5-3.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe5-4.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe5-5.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe5-6.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe5-7.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe5-8.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe5-9.png" alt="" /></p>
<h2 id="虚拟存储"> 虚拟存储</h2>
<p><img src="./../images/operaSystem-charpe6-1.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe6-2.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe6-3.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe6-4.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe6-5.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe6-6.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe6-7.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe6-8.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe6-9.png" alt="" /></p>
<p><img src="./../images/operaSystem-charpe6-10.png" alt="" /></p>
<h2 id="输入-输出"> 输入/输出</h2>
<h2 id="文件管理"> 文件管理</h2>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-26T11:10:03.000Z</published>
  </entry>
  <entry>
    <title type="html">Promise 基础和实现</title>
    <id>https://sewar-x.github.io/promise/</id>
    <link href="https://sewar-x.github.io/promise/"/>
    <updated>2021-06-25T19:47:29.000Z</updated>
    <content type="html"><![CDATA[<h1 id="promise-基础和实现"> Promise 基础和实现</h1>
<h2 id="promise-基础"> Promise 基础</h2>
<h3 id="概念"> 概念</h3>
<p><img src="./../images/promise1.png" alt="" /></p>
<p><img src="./../images/promise2.png" alt="promise2" /></p>
<p><img src="./../images/promise3.png" alt="promise3" /></p>
<p><img src="./../images/promise4.png" alt="promise1" /></p>
<h3 id="then"> then()</h3>
<p><img src="./../images/promise-then.png" alt="" /></p>
<p><img src="./../images/promise-then2.png" alt="promise-then" /></p>
<h3 id="错误处理"> 错误处理</h3>
<p><img src="./../images/promise-err.png" alt="" /></p>
<h3 id="promise-值穿透"> Promise 值穿透</h3>
<p><img src="./../images/promise-chuantou.png" alt="" /></p>
<h3 id="finally"> finally()</h3>
<p><img src="./../images/promise-fianlly.png" alt="" /></p>
<h3 id="包装pomise-对象"> 包装Pomise 对象</h3>
<p><img src="./../images/promise-baozhuang.png" alt="" /></p>
<h3 id="并发方法"> 并发方法</h3>
<p><img src="./../images/promise-bingfa.png" alt="" /></p>
<p>promise 基础直接参考 阮一峰<a href="https://es6.ruanyifeng.com/#docs/promise" target="_blank" rel="noopener noreferrer">《ECMAScript 6 入门》</a></p>
<h2 id="原理实现"> 原理实现</h2>
<p>强烈推荐先看 《<a href="https://mp.weixin.qq.com/s/UNzYgpnKzmW6bAapYxnXRQ" target="_blank" rel="noopener noreferrer">图解 Promise 实现原理</a> 》文章，文章内容将原理讲述透彻，该系列文章有如下几个章节组成：</p>
<ol>
<li>
<p><a href="https://mp.weixin.qq.com/s?__biz=MzI4NjY4MTU5Nw==&amp;mid=2247486661&amp;idx=1&amp;sn=8e4b3056aa9c110ca08047d0917290f4&amp;chksm=ebd87c57dcaff54168d1a8f94b074fa814270b9753d8c1e7eebe3b4203254ecb0e6989ba1f19&amp;scene=21#wechat_redirect" target="_blank" rel="noopener noreferrer">图解 Promise 实现原理（一）—— 基础实现</a></p>
</li>
<li>
<p><a href="https://mp.weixin.qq.com/s?__biz=MzI4NjY4MTU5Nw==&amp;mid=2247486706&amp;idx=2&amp;sn=9434eb4f5ea43e46de70a6486afbffbf&amp;chksm=ebd87c60dcaff57669d389cf114a993b15df789b1b14fe1f4c89e38d304d79489dc5394e9296&amp;scene=178&amp;cur_album_id=1500522652875194368#rd" target="_blank" rel="noopener noreferrer">图解 Promise 实现原理（二）—— Promise 链式调用</a></p>
</li>
<li>
<p><a href="https://mp.weixin.qq.com/s?__biz=MzI4NjY4MTU5Nw==&amp;mid=2247486850&amp;idx=2&amp;sn=647638dbb430da2c23a1320033fc806f&amp;chksm=ebd87d10dcaff40681acb2ee93b5516ae190e1111938fc72da12178e3d7f9c9fe5ffa0569254&amp;scene=178&amp;cur_album_id=1500522652875194368#rd" target="_blank" rel="noopener noreferrer">图解 Promise 实现原理（三）—— Promise 原型方法实现</a></p>
</li>
<li>
<p><a href="https://mp.weixin.qq.com/s?__biz=MzI4NjY4MTU5Nw==&amp;mid=2247486966&amp;idx=2&amp;sn=5c90d287040a811ed06517518fee8e34&amp;chksm=ebd87d64dcaff4726226d01cbb7b12aea5f16429a06b8f8cfbc23a35ecf955c899cd139283b7&amp;scene=178&amp;cur_album_id=1500522652875194368#rd" target="_blank" rel="noopener noreferrer">图解 Promise 实现原理（四）—— Promise 静态方法实现</a></p>
</li>
</ol>
<p>Promise 核心原理实现主要分为：基础方法+链式调用+延迟机制+状态管理+异常处理</p>
<ul>
<li>Promise() 对象最基础包含 constructor()、then()、resolve() 三个方法；</li>
<li>链式调用主要原理： then() 返回一个新的 Promise() 对象；</li>
<li>延迟机制：
<ul>
<li>目的:  保证在 resolve 执行之前，then 方法已经注册完所有的回调;</li>
<li>实现：
<ul>
<li>方法1： 使用 setTimeout() 模拟实现</li>
<li>方法2： 通过判断状态变化</li>
</ul>
</li>
</ul>
</li>
<li>状态管理：Promise 对象的状态改变，只有两种可能。
<ul>
<li>Pending 变为 Resolved ：promise变为FULFILLED状态后具有一个唯一的value；</li>
<li>Pending 变为 Rejected： promise变为REJECTED状态后具有一个唯一的reason；</li>
<li><img src="./../images/promiseState.png" alt="" /></li>
</ul>
</li>
<li>异常处理：在执行 onFulfilled 或者 onRejected 时，出现的异常处理方法，使用 try-catch 捕获错误，然后将相应的 Promise 状态设置为 rejected 状态
<ul>
<li>不管是错误也好，异常也罢，最终都是通过 reject 实现的，可见最终对于错误以及异常的处理，都可以通过 then 中的 onRejected 来处理。所以单独增加一个 catch 方法，它是 .then(null, onRejected) 的别名</li>
</ul>
</li>
</ul>
<div><pre><code><span>class</span> <span>Promise</span> <span>{</span>
  callbacks <span>=</span> <span>[</span><span>]</span><span>;</span> <span>//异步回调函数队列</span>
  state <span>=</span> <span>'pending'</span><span>;</span><span>//增加状态</span>
  value <span>=</span> <span>null</span><span>;</span><span>//保存当前promise 异步操作执行结果	</span>
  <span>constructor</span><span>(</span><span>fn</span><span>)</span> <span>{</span>
    <span>fn</span><span>(</span><span>this</span><span>.</span><span>_resolve</span><span>.</span><span>bind</span><span>(</span><span>this</span><span>)</span><span>,</span> <span>this</span><span>.</span><span>_reject</span><span>.</span><span>bind</span><span>(</span><span>this</span><span>)</span><span>)</span><span>;</span><span>// 创建Promise对象时立即执行 fn 函数</span>
  <span>}</span>
  <span>then</span><span>(</span><span>onFulfilled<span>,</span> onRejected</span><span>)</span> <span>{</span> <span>// then 函数仅仅是注册了后续需要执行的回调</span>
    <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>  <span>// 链式调用关键：返回新的promise对象</span>
      <span>this</span><span>.</span><span>_handle</span><span>(</span><span>{</span>
        onFulfilled<span>:</span> onFulfilled <span>||</span> <span>null</span><span>,</span> <span>// then方法成功回调函数</span>
        onRejected<span>:</span> onRejected <span>||</span> <span>null</span><span>,</span> <span>// then方法失败回调函数</span>
        resolve<span>:</span> resolve<span>,</span> <span>// 新的Promise 对象 resolve 方法</span>
        reject<span>:</span> reject  <span>// 新的Promise 对象 reject 方法</span>
      <span>}</span><span>)</span><span>;</span>
    <span>}</span><span>)</span><span>;</span>
  <span>}</span>
  <span>catch</span><span>(</span>onError<span>)</span> <span>{</span>
    <span>return</span> <span>this</span><span>.</span><span>then</span><span>(</span><span>null</span><span>,</span> onError<span>)</span><span>;</span>
  <span>}</span>
  <span>finally</span><span>(</span>onDone<span>)</span> <span>{</span>
    <span>if</span> <span>(</span><span>typeof</span> onDone <span>!==</span> <span>'function'</span><span>)</span> <span>return</span> <span>this</span><span>.</span><span>then</span><span>(</span><span>)</span><span>;</span>
 
    <span>let</span> Promise <span>=</span> <span>this</span><span>.</span>constructor<span>;</span>
    <span>return</span> <span>this</span><span>.</span><span>then</span><span>(</span>
      <span>value</span> <span>=></span> Promise<span>.</span><span>resolve</span><span>(</span><span>onDone</span><span>(</span><span>)</span><span>)</span><span>.</span><span>then</span><span>(</span><span>(</span><span>)</span> <span>=></span> value<span>)</span><span>,</span>
      <span>reason</span> <span>=></span> Promise<span>.</span><span>resolve</span><span>(</span><span>onDone</span><span>(</span><span>)</span><span>)</span><span>.</span><span>then</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span> <span>throw</span> reason <span>}</span><span>)</span>
    <span>)</span><span>;</span>
  <span>}</span>
  <span>_handle</span><span>(</span><span>callback</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span><span>this</span><span>.</span>state <span>===</span> <span>'pending'</span><span>)</span> <span>{</span>
      <span>this</span><span>.</span>callbacks<span>.</span><span>push</span><span>(</span>callback<span>)</span><span>;</span>
      <span>return</span><span>;</span>
    <span>}</span>
 	<span>//获取回调方法</span>
    <span>let</span> cb <span>=</span> <span>this</span><span>.</span>state <span>===</span> <span>'fulfilled'</span> <span>?</span> callback<span>.</span>onFulfilled <span>:</span> callback<span>.</span>onRejected<span>;</span>
 
    <span>if</span> <span>(</span><span>!</span>cb<span>)</span> <span>{</span><span>//如果then中没有传递任何东西</span>
      cb <span>=</span> <span>this</span><span>.</span>state <span>===</span> <span>'fulfilled'</span> <span>?</span> callback<span>.</span>resolve <span>:</span> callback<span>.</span>reject<span>;</span>
      <span>cb</span><span>(</span><span>this</span><span>.</span>value<span>)</span><span>;</span>
      <span>return</span><span>;</span>
    <span>}</span>
 
    <span>let</span> ret<span>;</span>
 
    <span>try</span> <span>{</span>
      <span>// 执行当前返回异步操作结果的promise的回调函数，并将异步操作结果this.value传入回调函数</span>
      ret <span>=</span> <span>cb</span><span>(</span><span>this</span><span>.</span>value<span>)</span><span>;</span>
      cb <span>=</span> <span>this</span><span>.</span>state <span>===</span> <span>'fulfilled'</span> <span>?</span> callback<span>.</span>resolve <span>:</span> callback<span>.</span>reject<span>;</span>
    <span>}</span> <span>catch</span> <span>(</span>error<span>)</span> <span>{</span>
      ret <span>=</span> error<span>;</span>
      cb <span>=</span> callback<span>.</span>reject <span>// 回调函数执行报错，执行回调的 reject </span>
    <span>}</span> <span>finally</span> <span>{</span>
      <span>//调用当前返回异步操作结果的promise的回调函数的resolve/reject方法，并传入回调执行结果</span>
      <span>cb</span><span>(</span>ret<span>)</span><span>;</span>
    <span>}</span>
 
  <span>}</span>
  <span>_resolve</span><span>(</span><span>value</span><span>)</span> <span>{</span> <span>//回调函数的执行是在 resolve 方法里面执行的</span>
    <span>// Promise 异步操作结果为 Promise 对象,调用 异步操作结果的 then()</span>
    <span>if</span> <span>(</span>value <span>&amp;&amp;</span> <span>(</span><span>typeof</span> value <span>===</span> <span>'object'</span> <span>||</span> <span>typeof</span> value <span>===</span> <span>'function'</span><span>)</span><span>)</span> <span>{</span>
      <span>var</span> then <span>=</span> value<span>.</span>then<span>;</span>
      <span>if</span> <span>(</span><span>typeof</span> then <span>===</span> <span>'function'</span><span>)</span> <span>{</span>
        <span>then</span><span>.</span><span>call</span><span>(</span>value<span>,</span> <span>this</span><span>.</span><span>_resolve</span><span>.</span><span>bind</span><span>(</span><span>this</span><span>)</span><span>,</span> <span>this</span><span>.</span><span>_reject</span><span>.</span><span>bind</span><span>(</span><span>this</span><span>)</span><span>)</span><span>;</span>
        <span>return</span><span>;</span>
      <span>}</span>
    <span>}</span>
 
    <span>this</span><span>.</span>state <span>=</span> <span>'fulfilled'</span><span>;</span><span>//改变状态</span>
    <span>this</span><span>.</span>value <span>=</span> value<span>;</span><span>//保存结果</span>
    <span>this</span><span>.</span>callbacks<span>.</span><span>forEach</span><span>(</span><span>callback</span> <span>=></span> <span>this</span><span>.</span><span>_handle</span><span>(</span>callback<span>)</span><span>)</span><span>;</span> <span>//执行下一个回调函数</span>
  <span>}</span>
  <span>_reject</span><span>(</span><span>error</span><span>)</span> <span>{</span> <span>//回调函数的执行是在reject 方法里面执行的</span>
    <span>this</span><span>.</span>state <span>=</span> <span>'rejected'</span><span>;</span>
    <span>this</span><span>.</span>value <span>=</span> error<span>;</span>
    <span>this</span><span>.</span>callbacks<span>.</span><span>forEach</span><span>(</span><span>callback</span> <span>=></span> <span>this</span><span>.</span><span>_handle</span><span>(</span>callback<span>)</span><span>)</span><span>;</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br></div></div><h3 id="参考资料"> 参考资料</h3>
<p><a href="https://mp.weixin.qq.com/s/UNzYgpnKzmW6bAapYxnXRQ" target="_blank" rel="noopener noreferrer">图解 Promise 实现原理</a></p>
<p><a href="http://www.conardli.top/docs/JavaScript/%E6%A8%A1%E6%8B%9F%E5%AE%9E%E7%8E%B0promise.html#%E5%9F%BA%E7%A1%80%E7%89%88%E6%9C%AC" target="_blank" rel="noopener noreferrer">模拟实现Promise</a></p>
<h1 id="promise-相关题目"> Promise 相关题目</h1>
<h2 id="基础题目"> 基础题目</h2>
<h3 id="执行顺序"> 执行顺序</h3>
<p>执行顺序题目主要结合 JavaScript 的事件循环 和 Promise 的宏任务/微任务考察：</p>
<ul>
<li>Promise 新建后executor就会立即执行</li>
<li>调用 resolve() 或 reject() 并不会终结 Promise 的 executor 函数的执行</li>
<li>Promise 的状态一旦变更为成功或者失败，则不会再次改变</li>
<li>宏任务： script(主程序代码)，setTimeout(), setInterval(), setImmediate(), I/O, requestAnimationFrame()，UI rendering</li>
<li>微任务： Promise.resolve()，Promise.reject()，Promises.then()，Promises.finally()，process.nextTick()，Object.observe()， MutationObserver()</li>
<li>执行顺序：script(主程序代码)—&gt;process.nextTick—&gt;Promises.then()——&gt;setTimeout——&gt;setInterval——&gt;setImmediate——&gt; I/O——&gt;UI rendering</li>
</ul>
<p>任务执行流程：</p>
<p><img src="./../images/task=performance.png" alt="" /></p>
<ul>
<li></li>
</ul>
<div><pre><code><span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
  console<span>.</span><span>log</span><span>(</span><span>3</span><span>)</span><span>;</span>
<span>}</span><span>,</span> <span>0</span><span>)</span><span>;</span>
Promise<span>.</span><span>resolve</span><span>(</span><span>)</span><span>.</span><span>then</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
  console<span>.</span><span>log</span><span>(</span><span>2</span><span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>
console<span>.</span><span>log</span><span>(</span><span>1</span><span>)</span><span>;</span>
<span>//输出: 1 2 3</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div><ul>
<li></li>
</ul>
<div><pre><code><span>const</span> promise <span>=</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>1</span><span>)</span><span>;</span>
    <span>resolve</span><span>(</span><span>)</span><span>;</span>
    console<span>.</span><span>log</span><span>(</span><span>2</span><span>)</span><span>;</span>
    <span>reject</span><span>(</span><span>'error'</span><span>)</span><span>;</span>
<span>}</span><span>)</span>
promise<span>.</span><span>then</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>3</span><span>)</span><span>;</span>
<span>}</span><span>)</span><span>.</span><span>catch</span><span>(</span><span>e</span> <span>=></span> console<span>.</span><span>log</span><span>(</span>e<span>)</span><span>)</span>
console<span>.</span><span>log</span><span>(</span><span>4</span><span>)</span><span>;</span>
<span>//输出: 1 2 4 3</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><blockquote>
<p>规则一: promise<code>构造函数的代码会立即执行，</code>then<code>或者</code>reject`里面的代码会放入异步微任务队列，在宏任务结束后会立即执行。</p>
<p>规则二：<code>promise</code>的状态一旦变更为成功或者失败，则不会再次改变，所以执行结果为：1,2,4,3。而<code>catch</code>里面的函数不会再执行</p>
</blockquote>
<ul>
<li></li>
</ul>
<div><pre><code><span>const</span> promise <span>=</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>
        <span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
             console<span>.</span><span>log</span><span>(</span><span>'once'</span><span>)</span>
             <span>resolve</span><span>(</span><span>'success'</span><span>)</span>
        <span>}</span><span>,</span> <span>1000</span><span>)</span>
 <span>}</span><span>)</span>
promise<span>.</span><span>then</span><span>(</span><span>(</span><span>res</span><span>)</span> <span>=></span> <span>{</span>
       console<span>.</span><span>log</span><span>(</span>res<span>)</span>
     <span>}</span><span>)</span>
promise<span>.</span><span>then</span><span>(</span><span>(</span><span>res</span><span>)</span> <span>=></span> <span>{</span>
     console<span>.</span><span>log</span><span>(</span>res<span>)</span>
 <span>}</span><span>)</span>

<span>//执行结果: 过一秒打印 once,success,success</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div><blockquote>
<p><code>promise</code>的构造函数只会执行一次，而<code>then</code>方法可以多次调用，但是第二次是直接返回结果，不会有异步等待的时间</p>
</blockquote>
<ul>
<li></li>
</ul>
<div><pre><code><span>const</span> <span>p1</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>(</span><span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>
	console<span>.</span><span>log</span><span>(</span><span>1</span><span>)</span><span>;</span>
	<span>let</span> p2 <span>=</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>
		console<span>.</span><span>log</span><span>(</span><span>2</span><span>)</span><span>;</span>
		<span>const</span> timeOut1 <span>=</span> <span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
			console<span>.</span><span>log</span><span>(</span><span>3</span><span>)</span><span>;</span>
			<span>resolve</span><span>(</span><span>4</span><span>)</span><span>;</span>
		<span>}</span><span>,</span> <span>0</span><span>)</span>
		<span>resolve</span><span>(</span><span>5</span><span>)</span><span>;</span>
	<span>}</span><span>)</span><span>;</span>
	<span>resolve</span><span>(</span><span>6</span><span>)</span><span>;</span>
	p2<span>.</span><span>then</span><span>(</span><span>(</span><span>arg</span><span>)</span> <span>=></span> <span>{</span>
		console<span>.</span><span>log</span><span>(</span>arg<span>)</span><span>;</span>
	<span>}</span><span>)</span><span>;</span>

<span>}</span><span>)</span><span>)</span><span>;</span>
<span>const</span> timeOut2 <span>=</span> <span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
	console<span>.</span><span>log</span><span>(</span><span>8</span><span>)</span><span>;</span>
	<span>const</span> p3 <span>=</span> <span>new</span> <span>Promise</span><span>(</span><span>reject</span> <span>=></span> <span>{</span>
		<span>reject</span><span>(</span><span>9</span><span>)</span><span>;</span>
	<span>}</span><span>)</span><span>.</span><span>then</span><span>(</span><span>res</span> <span>=></span> <span>{</span>
		console<span>.</span><span>log</span><span>(</span>res<span>)</span>
	<span>}</span><span>)</span>
<span>}</span><span>,</span> <span>0</span><span>)</span>


<span>p1</span><span>(</span><span>)</span><span>.</span><span>then</span><span>(</span><span>(</span><span>arg</span><span>)</span> <span>=></span> <span>{</span>
	console<span>.</span><span>log</span><span>(</span>arg<span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>
console<span>.</span><span>log</span><span>(</span><span>10</span><span>)</span><span>;</span>

<span>// 输出: 1 2 10 5 6 8 9 3</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br></div></div><blockquote>
<p>第一步：</p>
<p>​	执行主程序宏任务，执行 p1，p2，conole.log(10)，输出：1,2,10。
​	这时候事件循环里面有异步任务<code>timeOut1,timeOut2,p2.then,p1.then</code>
​	<code>p2.then,p1.then</code>为微任务，<code>timeOut1,timeOut2</code>为宏任务</p>
<p>第二步：</p>
<p>​	宏任务执行完后<code>Event Loop</code>会去任务队列取异步任务，微任务会优先执行。</p>
<p>​	根据微任务进入异步队列顺序，这时候会先后执行 <code>p2.then -&gt; p1.then</code>，打印 5,6。</p>
<p>第三步：</p>
<p>​	微任务执行完了，开始宏任务，由于2个<code>settimeout</code>等待时间一样，所以会执行先进入异步队列的顺序，<code>timeOut2</code>先后打印：8。执行宏任务的过程中，<code>p3.then</code> 微任务进入了微任务队列，宏任务执行完毕会执行微任务，输出：9。</p>
<p>​	之后继续执行下一个宏任务<code>timeOut1</code>, 输出：3。</p>
<p>第四步：</p>
<p>​	结合规则6，由于p2这个<code>Promise</code>对象的执行结果已经确定，所以4不会被打印</p>
</blockquote>
<ul>
<li><strong>错误捕获</strong></li>
</ul>
<div><pre><code>Promise<span>.</span><span>resolve</span><span>(</span><span>)</span>
  <span>.</span><span>then</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
    <span>return</span> <span>new</span> <span>Error</span><span>(</span><span>'error!!!'</span><span>)</span>
  <span>}</span><span>)</span>
  <span>.</span><span>then</span><span>(</span><span>(</span><span>res</span><span>)</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'then: '</span><span>,</span> res<span>)</span>
  <span>}</span><span>)</span>
  <span>.</span><span>catch</span><span>(</span><span>(</span><span>err</span><span>)</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'catch: '</span><span>,</span> err<span>)</span>
  <span>}</span><span>)</span>
<span>//输出：then:  Error: error!!!</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><blockquote>
<p>这里的陷阱在于只有抛出错误时才会执行 .catch()，题目中并不是抛出错误，所以执行 .then()。</p>
<p>改成如下方式会执行 .catch():</p>
<p>return Promise.reject(new Error('error!!!'))
throw new Error('error!!!')</p>
</blockquote>
<ul>
<li></li>
</ul>
<div><pre><code><span>const</span> promise <span>=</span> Promise<span>.</span><span>resolve</span><span>(</span><span>)</span>
    <span>.</span><span>then</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
        <span>return</span> promise
    <span>}</span><span>)</span>
promise<span>.</span><span>catch</span><span>(</span>console<span>.</span>error<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><blockquote>
<p>答案
程序报错：<code>TypeError: Chaining cycle detected for promise #&lt;Promise&gt;</code>
解读
<code>.then() 或 .catch() 返回的值不能是 promise 本身，否则会造成死循环</code></p>
</blockquote>
<ul>
<li></li>
</ul>
<div><pre><code><span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>1</span><span>)</span>
    Promise<span>.</span><span>resolve</span><span>(</span><span>3</span><span>)</span><span>.</span><span>then</span><span>(</span><span>data</span> <span>=></span> console<span>.</span><span>log</span><span>(</span>data<span>)</span><span>)</span>
<span>}</span><span>,</span> <span>0</span><span>)</span>

<span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>2</span><span>)</span>
<span>}</span><span>,</span> <span>0</span><span>)</span>

<span>//输出：1 3 2</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div><blockquote>
<p>先执行整体代码，没有发现微任务；
再执行下一个宏任务—setTimeout回调函数，打印出1；
遇到微任务—Promise 回调函数；
立即执行微任务，打印出3；
微任务队列被清空，执行下一个宏任务—下一个setTimeout回调函数，打印出2。</p>
</blockquote>
<ul>
<li></li>
</ul>
<div><pre><code><span>var</span> pro <span>=</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>res<span>,</span>rej</span><span>)</span><span>=></span><span>{</span>
    <span>res</span><span>(</span><span>1</span><span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>
pro<span>.</span><span>then</span><span>(</span><span>res</span><span>=></span><span>{</span>
    console<span>.</span><span>log</span><span>(</span>res<span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>
<span>setTimeout</span><span>(</span><span>(</span><span>)</span><span>=></span><span>{</span>
console<span>.</span><span>log</span><span>(</span><span>2</span><span>)</span><span>;</span>
<span>}</span><span>)</span>
pro<span>.</span><span>then</span><span>(</span><span>res</span><span>=></span><span>{</span>
    console<span>.</span><span>log</span><span>(</span>res<span>)</span><span>;</span>
<span>}</span><span>)</span>

<span>//输出： 1 1 2</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div><blockquote>
<p>promise 对象赋值给了变量 pro，每个 <code>.then()</code> 都是独立的，遇到 .then() 都会加入微任务队列，然后进行事件循环</p>
</blockquote>
<ul>
<li></li>
</ul>
<div><pre><code><span>async</span> <span>function</span> <span>async1</span> <span>(</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'async1 start'</span><span>)</span><span>;</span>
    <span>await</span> <span>async2</span><span>(</span><span>)</span><span>;</span>
    console<span>.</span><span>log</span><span>(</span><span>'async1 end'</span><span>)</span><span>;</span>
<span>}</span>

<span>async</span> <span>function</span> <span>async2</span> <span>(</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'async2'</span><span>)</span><span>;</span>
<span>}</span>

console<span>.</span><span>log</span><span>(</span><span>'script start'</span><span>)</span><span>;</span>

<span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'setTimeout'</span><span>)</span><span>;</span>
<span>}</span><span>,</span> <span>0</span><span>)</span><span>;</span>

<span>async1</span><span>(</span><span>)</span> 

<span>new</span> <span>Promise</span> <span>(</span><span>function</span> <span>(</span><span>resolve</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'promise1'</span><span>)</span><span>;</span>
    <span>resolve</span><span>(</span><span>)</span><span>;</span>
<span>}</span><span>)</span><span>.</span><span>then</span> <span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'promise2'</span><span>)</span><span>;</span>
<span>}</span><span>)</span>

console<span>.</span><span>log</span><span>(</span><span>'script end'</span><span>)</span><span>;</span>

<span>//------输出--------</span>
script start
async1 start
async2
promise1
script end
async1 end
promise2
setTimeout

</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br></div></div><blockquote>
<p>注意以下几点:</p>
<ul>
<li>定义 async1()，async2()，不会立即执行</li>
<li>await async2(); 表示先执行 async2()，然后将 await 后面的代码当作 .then() 中的回调来处理，因此执行 async2() 以后，返回值和后面代码添加到微任务队列中</li>
<li>初始化 promise，promise 中的代码会立即执行</li>
</ul>
</blockquote>
<h3 id="promise-封装异步操作"> Promise 封装异步操作</h3>
<ul>
<li>
<p>题目一：使用 promise 封装 setTimeout</p>
<ul>
<li>实现:
<ul>
<li>参数： ms 毫秒数</li>
<li>返回值： 新的promise</li>
</ul>
</li>
</ul>
<div><pre><code><span>const</span> <span>wait</span> <span>=</span> <span>ms</span> <span>=></span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve，reject</span><span>)</span> <span>=></span> <span>setTimeout</span><span>(</span>resolve<span>,</span>ms<span>)</span> <span>)</span>
</code></pre>
<div><span>1</span><br></div></div><div><pre><code><span>const</span> <span>wait</span> <span>=</span> <span>ms</span> <span>=></span> <span>new</span> <span>Promise</span><span>(</span><span>resolve</span> <span>=></span> <span>setTimeout</span><span>(</span>resolve<span>,</span>ms<span>)</span> <span>)</span>
</code></pre>
<div><span>1</span><br></div></div><ul>
<li>
<p>使用</p>
<div><pre><code><span>wait</span><span>(</span><span>10000</span><span>)</span>
    <span>.</span><span>then</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>saySomething</span><span>(</span><span>"10 seconds"</span><span>)</span><span>)</span>
    <span>.</span><span>catch</span><span>(</span>failureCallback<span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
<li>
<p>分析：因为 <code>setTimeout</code> 并不会真的执行失败，那么我们可以在这种情况下忽略 reject</p>
</li>
</ul>
</li>
<li>
<p>题目二：使用 promise 封装 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Geolocation/getCurrentPosition" target="_blank" rel="noopener noreferrer">navigator.geolocation.getCurrentPosition(success, error, options)</a> 接口</p>
<ul>
<li>
<p>实现：</p>
<ul>
<li>
<p>参数： options</p>
</li>
<li>
<p>返回值： promise</p>
</li>
<li>
<div><pre><code><span>getLocation</span> <span>=</span> <span>options</span> <span>=></span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>
    navigator<span>.</span>geolocation<span>.</span><span>getCurrentPosition</span><span>(</span>resolve<span>,</span>reject<span>,</span>options<span>)</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
<li>
<p>题目三：封装 XMLHttpRequest</p>
<ul>
<li>
<p>实现</p>
<ul>
<li>
<div><pre><code><span>function</span> <span>myAsyncFunction</span><span>(</span><span>url</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>
    <span>const</span> xhr <span>=</span> <span>new</span> <span>XMLHttpRequest</span><span>(</span><span>)</span><span>;</span>
    xhr<span>.</span><span>open</span><span>(</span><span>"GET"</span><span>,</span> url<span>)</span><span>;</span>
    xhr<span>.</span><span>onload</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>resolve</span><span>(</span>xhr<span>.</span>responseText<span>)</span><span>;</span>
    xhr<span>.</span><span>onerror</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>reject</span><span>(</span>xhr<span>.</span>statusText<span>)</span><span>;</span>
    xhr<span>.</span><span>send</span><span>(</span><span>)</span><span>;</span>
  <span>}</span><span>)</span><span>;</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="promise-特性相关"> Promise 特性相关</h3>
<ul>
<li>
<p>题目一： <strong>如何取消一个Promise？</strong></p>
<ul>
<li>
<p>在要停止的<code>promise</code>链位置添加一个方法，返回一个永远不执行<code>resolve</code>或者<code>reject</code>的<code>Promise</code>，那么这个<code>promise</code>永远处于<code>pending</code>状态，所以永远也不会向下执行<code>then</code>或<code>catch</code>了。这样我们就停止了一个<code>promise</code>链</p>
<div><pre><code>Promise<span>.</span><span>resolve</span><span>(</span><span>)</span><span>.</span><span>then</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'ok1'</span><span>)</span>
    <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>)</span><span>=></span><span>{</span><span>}</span><span>)</span>  <span>// 返回“pending”状态的Promise对象</span>
<span>}</span><span>)</span><span>.</span><span>then</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
    <span>// 后续的函数不会被调用</span>
    console<span>.</span><span>log</span><span>(</span><span>'ok2'</span><span>)</span>
<span>}</span><span>)</span><span>.</span><span>catch</span><span>(</span><span>err</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'err->'</span><span>,</span> err<span>)</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div></li>
<li>
<p>Promise.race竞速方法</p>
<div><pre><code><span>let</span> p1 <span>=</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>
    <span>resolve</span><span>(</span><span>'ok1'</span><span>)</span>
<span>}</span><span>)</span>

<span>let</span> p2 <span>=</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>
    <span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span><span>resolve</span><span>(</span><span>'ok2'</span><span>)</span><span>}</span><span>,</span> <span>10</span><span>)</span>
<span>}</span><span>)</span>

Promise<span>.</span><span>race</span><span>(</span><span>[</span>p2<span>,</span> p1<span>]</span><span>)</span><span>.</span><span>then</span><span>(</span><span>(</span><span>result</span><span>)</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span>result<span>)</span> <span>//ok1</span>
<span>}</span><span>)</span><span>.</span><span>catch</span><span>(</span><span>(</span><span>error</span><span>)</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span>error<span>)</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br></div></div></li>
</ul>
</li>
<li>
<p>题目二：<strong>如果向Promise.all()和Promise.race()传递空数组，运行结果会有什么不同？</strong></p>
<ul>
<li><img src="./../images/promise-test.png" alt="" /></li>
<li>all会立即决议，决议结果是fullfilled，值是undefined; race会永远都不决议，程序卡死</li>
</ul>
</li>
<li>
<p>题目三：<strong>Promise链上返回的最后一个Promise出错了怎么办？</strong></p>
<ul>
<li>
<p><code>catch</code>在<code>promise</code>链式调用的末尾调用，用于捕获链条中的错误信息，但是<code>catch</code>方法内部也可能出现错误，所以有些<code>promise</code>实现中增加了一个方法<code>done</code>，<code>done</code>相当于提供了一个不会出错的<code>catch</code>方法，并且不再返回一个<code>promise</code>，一般用来结束一个<code>promise</code>链</p>
<div><pre><code><span>done</span><span>(</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span><span>catch</span><span>(</span><span>reason</span> <span>=></span> <span>{</span> <span>//调用 Promise 的 catch 方法</span>
      console<span>.</span><span>log</span><span>(</span><span>'done'</span><span>,</span> reason<span>)</span><span>;</span>
      <span>throw</span> reason<span>;</span> <span>// 向外抛出错误</span>
    <span>}</span><span>)</span><span>;</span>
 <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div></li>
</ul>
</li>
</ul>
<h3 id="应用题"> 应用题</h3>
<ul>
<li>题目一：<strong>顺序加载10张图片，图片地址已知，但是同时最多加载3张图片，要求用<code>promise</code>实现</strong></li>
</ul>
<div><pre><code><span>const</span> baseUrl <span>=</span> <span>'http://img.aizhifou.cn/'</span><span>;</span>
<span>const</span> urls <span>=</span> <span>[</span><span>'1.png'</span><span>,</span> <span>'2.png'</span><span>,</span> <span>'3.png'</span><span>,</span> <span>'4.png'</span><span>,</span> <span>'5.png'</span><span>,</span><span>'6.png'</span><span>,</span> <span>'7.png'</span><span>,</span> <span>'8.png'</span><span>,</span> <span>'9.png'</span><span>,</span> <span>'10.png'</span><span>]</span><span>;</span>
<span>const</span> <span>loadImg</span> <span>=</span> <span>function</span> <span>(</span><span>url<span>,</span> i</span><span>)</span> <span>{</span>
	<span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>
		<span>try</span> <span>{</span>
			<span>// 加载一张图片</span>
			<span>let</span> image <span>=</span> <span>new</span> <span>Image</span><span>(</span><span>)</span><span>;</span>
			image<span>.</span><span>onload</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
				<span>resolve</span><span>(</span>i<span>)</span>
			<span>}</span>
			image<span>.</span><span>onerror</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
				<span>reject</span><span>(</span>i<span>)</span>
			<span>}</span><span>;</span>
			image<span>.</span>src <span>=</span> baseUrl <span>+</span> url<span>;</span>
		<span>}</span> <span>catch</span> <span>(</span>e<span>)</span> <span>{</span>
			<span>reject</span><span>(</span>i<span>)</span>
		<span>}</span>
	<span>}</span><span>)</span>
<span>}</span>
<span>function</span> <span>startLoadImage</span><span>(</span><span>urls<span>,</span> limits<span>,</span> endHandle</span><span>)</span> <span>{</span>
	<span>// 当前存在的promise队列</span>
	<span>let</span> promiseMap <span>=</span> <span>{</span><span>}</span><span>;</span>
	<span>// 当前索引对应的加载状态，无论成功，失败都会标记为true，格式： {0: true, 1: true, 2: true...}</span>
	<span>let</span> loadIndexMap <span>=</span> <span>{</span><span>}</span><span>;</span>
	<span>// 当前以及加载到的索引，方便找到下一个未加载的索引，为了节省性能，其实可以不要</span>
	<span>let</span> loadIndex <span>=</span> <span>0</span><span>;</span>
	<span>const</span> <span>loadAImage</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
		<span>// 所有的资源都进入了异步队列</span>
		<span>if</span> <span>(</span>Object<span>.</span><span>keys</span><span>(</span>loadIndexMap<span>)</span><span>.</span>length <span>===</span> urls<span>.</span>length<span>)</span> <span>{</span>
			<span>// 所有的资源都加载完毕，或者进入加载状态，递归结束</span>
			<span>const</span> promiseList <span>=</span> Object<span>.</span><span>keys</span><span>(</span>promiseMap<span>)</span><span>.</span><span>reduce</span><span>(</span><span>(</span><span>arr<span>,</span> item</span><span>)</span> <span>=></span> <span>{</span>arr<span>.</span><span>push</span><span>(</span>promiseMap<span>[</span>item<span>]</span><span>)</span><span>;</span> <span>return</span> arr<span>}</span><span>,</span> <span>[</span><span>]</span><span>)</span>
			Promise<span>.</span><span>all</span><span>(</span>promiseList<span>)</span><span>.</span><span>then</span><span>(</span><span>res</span> <span>=></span> <span>{</span>
				<span>// 这里如果没有加载失败，就会在所有加载完毕后执行，如果其中某个错误了，这里的结果就不准确，不过这个不是题目要求的。</span>
				console<span>.</span><span>log</span><span>(</span><span>'all'</span><span>)</span><span>;</span>
				endHandle <span>&amp;&amp;</span> <span>endHandle</span><span>(</span><span>)</span>
			<span>}</span><span>)</span><span>.</span><span>catch</span><span>(</span><span>(</span><span>e</span><span>)</span> <span>=></span> <span>{</span>
				console<span>.</span><span>log</span><span>(</span><span>'end:'</span> <span>+</span> e<span>)</span><span>;</span>
			<span>}</span><span>)</span>
		<span>}</span> <span>else</span> <span>{</span>
			<span>// 遍历，知道里面有3个promise</span>
			<span>while</span> <span>(</span>Object<span>.</span><span>keys</span><span>(</span>promiseMap<span>)</span><span>.</span>length <span>&lt;</span> limits<span>)</span> <span>{</span>
				<span>for</span> <span>(</span><span>let</span> i <span>=</span> loadIndex<span>;</span> i <span>&lt;</span> urls<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
					<span>if</span> <span>(</span>loadIndexMap<span>[</span>i<span>]</span> <span>===</span> <span>undefined</span><span>)</span> <span>{</span>
						loadIndexMap<span>[</span>i<span>]</span> <span>=</span> <span>false</span><span>;</span>
						promiseMap<span>[</span>i<span>]</span> <span>=</span> <span>loadImg</span><span>(</span>urls<span>[</span>i<span>]</span><span>,</span> i<span>)</span><span>;</span>
						loadIndex <span>=</span> i<span>;</span>
						<span>break</span><span>;</span>
					<span>}</span>
				<span>}</span>
			<span>}</span>
			<span>// 获取当前正在进行的promise列表，利用reduce从promiseMap里面获取</span>
			<span>const</span> promiseList <span>=</span> Object<span>.</span><span>keys</span><span>(</span>promiseMap<span>)</span><span>.</span><span>reduce</span><span>(</span><span>(</span><span>arr<span>,</span> item</span><span>)</span> <span>=></span> <span>{</span>arr<span>.</span><span>push</span><span>(</span>promiseMap<span>[</span>item<span>]</span><span>)</span><span>;</span> <span>return</span> arr<span>}</span><span>,</span> <span>[</span><span>]</span><span>)</span>
			Promise<span>.</span><span>race</span><span>(</span>promiseList<span>)</span><span>.</span><span>then</span><span>(</span><span>(</span><span>index</span><span>)</span> <span>=></span> <span>{</span>
				<span>// 其中一张加载成功，删除当前promise，让PromiseList小于limit，开始递归，加载下一张</span>
				console<span>.</span><span>log</span><span>(</span><span>'end:'</span> <span>+</span> index<span>)</span><span>;</span>
				loadIndexMap<span>[</span>index<span>]</span> <span>=</span> <span>true</span><span>;</span>
				<span>delete</span> promiseMap<span>[</span>index<span>]</span><span>;</span>
				<span>loadAImage</span><span>(</span><span>)</span><span>;</span>
			<span>}</span><span>)</span><span>.</span><span>catch</span><span>(</span><span>e</span> <span>=></span> <span>{</span>
				<span>// 加载失败也继续</span>
				console<span>.</span><span>log</span><span>(</span><span>'end：'</span> <span>+</span> e<span>)</span><span>;</span>
				loadIndexMap<span>[</span>e<span>]</span> <span>=</span> <span>true</span><span>;</span>
				<span>delete</span> promiseMap<span>[</span>e<span>]</span><span>;</span>
				<span>loadAImage</span><span>(</span><span>)</span><span>;</span>
			<span>}</span><span>)</span>
		<span>}</span>
	<span>}</span>
	<span>loadAImage</span><span>(</span><span>)</span>
<span>}</span>

<span>startLoadImage</span><span>(</span>urls<span>,</span> <span>3</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br></div></div><blockquote>
<p>将代码复制到chrome浏览器可以看到下面的运行结果：
<img src="./../images/demo1.png" alt="1.png" />
可以看到，所有图片加载完成，在没有失败的情况下，打印出来<code>all</code>
解析：<code>Promise.race</code>方法接受的参数中有一个<code>promise</code>对象返回结果了就会立即触发成功或者失败的函数。这里利用这个特性，先将<code>promise</code>队列循环加入，直到达到限制，等待<code>race</code>，<code>race</code>后又加入一个<code>promise</code>，利用递归一直循环这个过程，到最后用<code>promise.all</code>捕获剩下的图片加载</p>
</blockquote>
<h2 id="promisec串行"> Promisec串行</h2>
<h3 id="概念-2"> 概念</h3>
<ul>
<li>有多个任务，多个任务必须顺序执行（下一个任务依赖上一个任务结果）</li>
<li>使用 Promise 进行异步操作的顺序处理</li>
<li>Promise 串行执行实现原理： 利用 Promise.then() 的链式调用</li>
</ul>
<h3 id="实现"> 实现</h3>
<p>模拟三个异步函数，每一个函数依赖上一个函数结果</p>
<div><pre><code><span>/</span> 异步函数 p1
<span>let</span> <span>p1</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    <span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
      <span>resolve</span><span>(</span><span>'p1->'</span><span>)</span>
    <span>}</span><span>,</span> <span>1000</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>// 异步函数 p2</span>
<span>let</span> <span>p2</span> <span>=</span> <span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    <span>resolve</span><span>(</span>data <span>+</span> <span>'p2->'</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>// 异步函数 p3</span>
<span>let</span> <span>p3</span> <span>=</span> <span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    <span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
      <span>resolve</span><span>(</span>data <span>+</span> <span>'p3->'</span><span>)</span>
    <span>}</span><span>,</span> <span>500</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br></div></div><ul>
<li>
<p>实现一：<strong>使用then链式操作</strong></p>
<ul>
<li>缺点：书写比较繁琐</li>
</ul>
<div><pre><code><span>p1</span><span>(</span><span>)</span>
  <span>.</span><span>then</span><span>(</span><span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
    <span>return</span> <span>p2</span><span>(</span>data<span>)</span>
  <span>}</span><span>)</span>
  <span>.</span><span>then</span><span>(</span><span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
    <span>return</span> <span>p3</span><span>(</span>data<span>)</span>
  <span>}</span><span>)</span>
  <span>.</span><span>then</span><span>(</span><span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span>data<span>)</span>
  <span>}</span><span>)</span>
<span>// 输出: p1->p2->p3-></span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div></li>
<li>
<p>实现二：<strong>forEach循环构建队列</strong></p>
<div><pre><code><span>function</span> <span>queue</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>let</span> sequence <span>=</span> Promise<span>.</span><span>resolve</span><span>(</span><span>)</span>
  arr<span>.</span><span>forEach</span><span>(</span><span>function</span> <span>(</span><span>item</span><span>)</span> <span>{</span>
    sequence <span>=</span> sequence<span>.</span><span>then</span><span>(</span>item<span>)</span>
  <span>}</span><span>)</span>
  <span>return</span> sequence
<span>}</span>

<span>// 执行队列</span>
<span>queue</span><span>(</span><span>[</span>p1<span>,</span> p2<span>,</span> p3<span>]</span><span>)</span>
  <span>.</span><span>then</span><span>(</span><span>data</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span>data<span>)</span>
  <span>}</span><span>)</span>
<span>// 输出: p1->p2->p3-></span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div></li>
<li>
<p>实现三：<strong>reduce循环构建队列</strong></p>
<div><pre><code><span>function</span> <span>queue</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>return</span> arr<span>.</span><span>reduce</span><span>(</span>
    <span>(</span><span>promiseChain<span>,</span> currentFunction</span><span>)</span> <span>=></span> promiseChain<span>.</span><span>then</span><span>(</span>currentFunction<span>)</span><span>,</span>
    Promise<span>.</span><span>resolve</span><span>(</span><span>)</span>	<span>//	将初始值使用 promise 包裹</span>
  <span>)</span><span>;</span>
<span>}</span>

<span>// 执行队列</span>
<span>queue</span><span>(</span><span>[</span>p1<span>,</span> p2<span>,</span> p3<span>]</span><span>)</span>
  <span>.</span><span>then</span><span>(</span><span>data</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span>data<span>)</span>
  <span>}</span><span>)</span>
<span>// 输出: p1->p2->p3-></span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br></div></div><div><pre><code><span>function</span> <span>queue</span><span>(</span><span>tasks</span><span>)</span> <span>{</span>
    <span>function</span> <span>recordValue</span><span>(</span><span>results<span>,</span> value</span><span>)</span> <span>{</span>
        results<span>.</span><span>push</span><span>(</span>value<span>)</span><span>;</span>
        <span>return</span> results<span>;</span>
    <span>}</span>
    <span>var</span> pushValue <span>=</span> <span>recordValue</span><span>.</span><span>bind</span><span>(</span><span>null</span><span>,</span> <span>[</span><span>]</span><span>)</span><span>;</span>
    <span>return</span> tasks<span>.</span><span>reduce</span><span>(</span><span>function</span> <span>(</span><span>promise<span>,</span> task</span><span>)</span> <span>{</span>
        <span>return</span> promise<span>.</span><span>then</span><span>(</span><span>(</span><span>)</span> <span>=></span> task<span>)</span><span>.</span><span>then</span><span>(</span>pushValue<span>)</span><span>;</span>
    <span>}</span><span>,</span> Promise<span>.</span><span>resolve</span><span>(</span><span>)</span><span>)</span><span>;</span>
<span>}</span>


<span>// 执行队列</span>
<span>queue</span><span>(</span><span>[</span>p1<span>,</span> p2<span>,</span> p3<span>]</span><span>)</span>
  <span>.</span><span>then</span><span>(</span><span>data</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span>data<span>)</span>
  <span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br></div></div></li>
<li>
<p>实现四：<strong>使用async、await构建队列</strong></p>
<div><pre><code><span>async</span> <span>function</span> <span>queue</span><span>(</span><span>arr</span><span>)</span> <span>{</span>
  <span>let</span> res <span>=</span> <span>null</span>
  <span>for</span> <span>(</span><span>let</span> promise <span>of</span> arr<span>)</span> <span>{</span>
    res <span>=</span> <span>await</span> <span>promise</span><span>(</span>res<span>)</span>
  <span>}</span>
  <span>return</span> <span>await</span> res
<span>}</span>

<span>// 执行队列</span>
<span>queue</span><span>(</span><span>[</span>p1<span>,</span> p2<span>,</span> p3<span>]</span><span>)</span>
  <span>.</span><span>then</span><span>(</span><span>data</span> <span>=></span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span>data<span>)</span>
  <span>}</span><span>)</span>
<span>// 输出: p1->p2->p3-></span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div></li>
</ul>
<h3 id="应用"> 应用</h3>
<h4 id="axios请求-响应拦截过程"> axios请求/响应拦截过程</h4>
<ul>
<li>axios中请求/响应是过程是顺序执行 [请求拦截器, dispatchRequest, 响应拦截器]  队列的过程</li>
<li>队列中请求拦截器， dispatchRequest，响应拦截器 为Promise 对象</li>
<li><a href="https://github.com/axios/axios/blob/master/lib/core/Axios.js" target="_blank" rel="noopener noreferrer">源码</a></li>
<li><img src="./../images/axios流程.png" alt="" /></li>
</ul>
<p>模拟实现：</p>
<div><pre><code><span>//请求1</span>
<span>let</span> <span>req1</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'request1 start->'</span><span>)</span>
    <span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
      <span>resolve</span><span>(</span><span>'request1->'</span><span>)</span>
      console<span>.</span><span>log</span><span>(</span><span>'request1 end->'</span><span>)</span>
    <span>}</span><span>,</span> <span>1000</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>// 请求2</span>
<span>let</span> <span>req2</span> <span>=</span> <span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'request2 start->'</span><span>)</span>
    <span>resolve</span><span>(</span>data <span>+</span> <span>'request2->'</span><span>)</span>
    console<span>.</span><span>log</span><span>(</span><span>'request2 end->'</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>// 请求3</span>
<span>let</span> <span>req3</span> <span>=</span> <span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'request3 start->'</span><span>)</span>
    <span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
      <span>resolve</span><span>(</span>data <span>+</span> <span>'request3->'</span><span>)</span>
      console<span>.</span><span>log</span><span>(</span><span>'request3 end->'</span><span>)</span>
    <span>}</span><span>,</span> <span>500</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>//响应1</span>
<span>let</span> <span>res1</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    <span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
      console<span>.</span><span>log</span><span>(</span><span>'get response1->'</span><span>)</span>
      <span>resolve</span><span>(</span><span>'response1 ->'</span><span>)</span>
    <span>}</span><span>,</span> <span>1100</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>
<span>//响应数据2</span>
<span>let</span> <span>res2</span> <span>=</span> <span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'get response2->'</span><span>)</span>
    <span>resolve</span><span>(</span>data <span>+</span> <span>'response2->'</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>let</span> <span>res3</span> <span>=</span> <span>function</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
  <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>function</span> <span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>{</span>
    <span>setTimeout</span><span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
      console<span>.</span><span>log</span><span>(</span><span>'get response3->'</span><span>)</span>
      <span>resolve</span><span>(</span>data <span>+</span> <span>'response3->'</span><span>)</span>
    <span>}</span><span>,</span> <span>500</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>let</span> chain <span>=</span> <span>[</span><span>]</span>
<span>let</span> requeseInter <span>=</span> <span>[</span>req1<span>,</span>req2<span>,</span>req3<span>]</span>
<span>let</span> responseInter <span>=</span> <span>[</span>res1<span>,</span>res2<span>,</span>res3<span>]</span>

requeseInter<span>.</span><span>forEach</span><span>(</span><span>p</span><span>=></span>chain<span>.</span><span>unshift</span><span>(</span>p<span>)</span><span>)</span><span>;</span>
responseInter<span>.</span><span>forEach</span><span>(</span><span>p</span><span>=></span>chain<span>.</span><span>push</span><span>(</span>p<span>)</span><span>)</span><span>;</span>

<span>let</span> promise <span>=</span> Promise<span>.</span><span>resolve</span><span>(</span><span>{</span><span>}</span><span>)</span>
<span>while</span><span>(</span>chain<span>.</span>length<span>)</span><span>{</span>
    promise  <span>=</span> promise<span>.</span><span>then</span><span>(</span>chain<span>.</span><span>shift</span><span>(</span><span>)</span><span>)</span><span>;</span>
<span>}</span>
console<span>.</span><span>log</span><span>(</span>promise<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br></div></div><p>执行结果：<img src="./../images/moni-axios.png" alt="" /></p>
<h2 id="promise-并行"> Promise 并行</h2>
<ul>
<li>并行：多个异步请求同时进行</li>
</ul>
<h3 id="并行执行-api"> 并行执行 API</h3>
<ul>
<li>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise/all" target="_blank" rel="noopener noreferrer">Promise.all(iterable)</a></p>
</li>
<li>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise/any" target="_blank" rel="noopener noreferrer">Promise.any()</a></p>
</li>
<li>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise/race" target="_blank" rel="noopener noreferrer">Promise.race(iterable)</a></p>
</li>
<li>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise/allSettled" target="_blank" rel="noopener noreferrer">Promise.allSettled()</a></p>
</li>
</ul>
<h3 id="并发控制"> 并发控制</h3>
<h4 id="概念-3"> 概念</h4>
<p>​	要求执行 n 个异步任务，但不能 n 个异步任务同时执行 (内存资源限制等原因)，需要控制每次 m ( m&lt;n ) 个异步任务同时进行，但最终结果是执行 n 个异步任务</p>
<h4 id="思想"> 思想</h4>
<ul>
<li>使用 Promise 并行执行 API : Promise.all()、Promise.allSettled()、Promise.race()、Promise.any() 并行执行异步，控制每次并行执行的 Promise 实例;</li>
<li>Promise 并发限制，其实根源上就是控制 Promise 的实例化个数；Promise 并不是因为调用 Promise.all 才执行，而是在实例化 Promise 对象的时候就执行了;</li>
<li>要实现并发限制，只能从 Promise 实例化上下手，把生成 Promises 数组的控制权，交给并发控制逻辑</li>
</ul>
<h4 id="通用实现"> 通用实现</h4>
<ul>
<li>
<p>实现一： <strong>使用 async await 和 Promise.all 封装并发任务 (推荐)</strong></p>
<ul>
<li>思路：循环截取列表中 limit 个数据并发执行</li>
<li>缺点：
<ul>
<li>每次并发执行 limit 个，需要等待全部结果返回后，才能执行下一次并发</li>
</ul>
</li>
</ul>
<div><pre><code><span>/**
 * @params list {Array} - 要迭代的数组
 * @params limit {Number} - 并发数量控制数
 * @params asyncHandle {Function} - 对 list 的每一个项的异步处理函数，参数为当前处理项，必须 return 一个 Promise 来确定是否继续进行迭代 ( 异步处理 Promise 实例化必须在 asyncHandle 中完成 )
 * @return {Promise} - 返回一个 Promise 值来确认所有数据是否迭代完成
**/</span>
<span>async</span> <span>function</span> <span>concurrencyLimit</span><span>(</span><span>list<span>,</span>limit<span>,</span>asyncHandler</span><span>)</span> <span>{</span>
    <span>for</span><span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> list<span>.</span>length<span>;</span> i <span>+=</span> limit<span>)</span> <span>{</span> 
        <span>let</span> partiaList  <span>=</span> list<span>.</span><span>slice</span><span>(</span>i<span>,</span>i <span>+</span> limit<span>)</span><span>;</span> <span>//遍历列表，每次截取 limit 个数据</span>
        console<span>.</span><span>log</span><span>(</span><span><span>`</span><span>partiaList is </span><span><span>${</span>partiaList<span>}</span></span><span>`</span></span><span>)</span>
        <span>await</span> <span>(</span><span>(</span><span>)</span><span>=></span> <span>{</span> <span>//遍历截取的并发数据，使用 asyncHandler 包装异步操作，并返回 Promise 数组，并发执行</span>
            <span>return</span> Promise<span>.</span><span>all</span><span>(</span> partiaList<span>.</span><span>map</span><span>(</span> <span>value</span> <span>=></span> <span>asyncHandler</span><span>(</span>value<span>)</span><span>)</span> <span>)</span>
        <span>}</span><span>)</span><span>(</span><span>)</span><span>;</span>
    <span>}</span>
<span>}</span>


<span>//------------------------------------------测试demo---------------------------------------------</span>
<span>//模拟获取 10 个数据结果</span>
<span>let</span> dataLists <span>=</span> Array<span>.</span><span>from</span><span>(</span><span>{</span>length<span>:</span> <span>10</span><span>}</span><span>,</span> <span>(</span><span>v<span>,</span> i</span><span>)</span> <span>=></span> i<span>)</span><span>;</span> <span>//生成 1-10的数组，待并发执行数据</span>
<span>let</span> successfulData <span>=</span> <span>[</span><span>]</span> <span>//成功获取的数据</span>
<span>let</span> errData <span>=</span> <span>[</span><span>]</span> <span>// 获取失败的数据</span>
<span>concurrencyLimit</span><span>(</span>dataLists<span>,</span> <span>3</span><span>,</span> <span>(</span><span>curItem</span><span>)</span><span>=></span><span>{</span> <span>// 异步处理函数</span>
    <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>
        <span>setTimeout</span><span>(</span><span>(</span><span>)</span><span>=></span><span>{</span>
            console<span>.</span><span>log</span><span>(</span><span><span>`</span><span>return data is </span><span><span>${</span>curItem<span>}</span></span><span>`</span></span><span>)</span> 
            <span>if</span><span>(</span>curItem <span>%</span><span>3</span> <span>==</span> <span>0</span><span>)</span> <span>{</span> <span>// 如果数据是 3 的倍数，获取失败</span>
                <span>reject</span><span>(</span><span>{</span>
                 	data<span>:</span> curItem<span>,</span>
                	status<span>:</span> <span>404</span> 
                <span>}</span><span>)</span>
            <span>}</span>
            <span>resolve</span><span>(</span><span>{</span>
                data<span>:</span> curItem<span>,</span>
                status<span>:</span> <span>200</span>
            <span>}</span><span>)</span><span>;</span>
        <span>}</span><span>,</span> Math<span>.</span><span>random</span><span>(</span><span>)</span> <span>*</span> <span>5000</span><span>)</span>  <span>// 设置随机响应时间</span>
    <span>}</span><span>)</span><span>.</span><span>then</span><span>(</span><span>res</span> <span>=></span> <span>{</span>
        successfulData<span>.</span><span>push</span><span>(</span>res<span>)</span> <span>//获取成功数据保存进入数组</span>
    <span>}</span><span>)</span><span>.</span><span>catch</span><span>(</span><span>err</span> <span>=></span> <span>{</span>
        console<span>.</span><span>log</span><span>(</span><span><span>`</span><span>get fail: </span><span><span>${</span>err<span>.</span>data<span>}</span></span><span>`</span></span><span>)</span> 
        errData<span>.</span><span>push</span><span>(</span>err<span>)</span> <span>//获取失败数据保存</span>
    <span>}</span><span>)</span><span>;</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br></div></div></li>
<li>
<p>实现二：<strong>Promise.all 并发执行并递归</strong></p>
<ul>
<li>思路：使用 Promise 并发执行 limit 个异步操作，然后通过递归和 then 的链式调用形成 limit 条并发执行的执行链</li>
<li>缺点：
<ul>
<li>最后只返回 limit 条链的最后执行的结果，不能记录每条执行链的执行顺序和所有结果</li>
<li>捕获的错误数据也会返回</li>
</ul>
</li>
<li>使用递归，如果并发请求量大，会占用较大内存
<ul>
<li>不能按顺序发起请求</li>
</ul>
</li>
</ul>
<div><pre><code><span>/**
 * @params list {Array} - 要迭代的数组
 * @params limit {Number} - 并发数量控制数
 * @params asyncHandle {Function} - 对 list 的每一个项的异步处理函数，参数为当前处理项，必须 return 一个 Promise 来确定是否继续进行迭代 ( 异步处理 Promise 实例化必须在 asyncHandle 中完成 )
 * @return {Promise} - 返回一个 Promise 值来确认所有数据是否迭代完成
 */</span>
<span>const</span> <span>concurrencyLimit</span> <span>=</span> <span>(</span><span>list<span>,</span> limit<span>,</span> asyncHandle</span><span>)</span> <span>=></span> <span>{</span>
    <span>let</span> <span>recursion</span> <span>=</span> <span>(</span><span>tasks</span><span>)</span> <span>=></span> <span>{</span> <span>//递归迭代数组，形成一条执行链</span>
        <span>return</span> <span>asyncHandle</span><span>(</span>tasks<span>.</span><span>shift</span><span>(</span><span>)</span><span>)</span> <span>//获取队列头数据，并用异步处理函数处理，返回一个 Promise 实例</span>
            <span>.</span><span>then</span><span>(</span><span>(</span><span>res</span><span>)</span><span>=></span><span>{</span> <span>// 返回异步处理结果 </span>
                <span>if</span> <span>(</span>tasks<span>.</span>length <span>></span> <span>0</span><span>)</span> <span>{</span>
                    <span>return</span> <span>recursion</span><span>(</span>tasks<span>)</span>   <span>// 数组还未迭代完，递归继续进行迭代</span>
                <span>}</span> <span>else</span> <span>{</span> <span>//迭代完成，返回最终数据</span>
                    console<span>.</span><span>log</span><span>(</span><span>'res is '</span><span>,</span>res<span>)</span>
                    <span>return</span> res 
                <span>}</span>
            <span>}</span><span>)</span><span>.</span><span>catch</span><span>(</span><span>err</span> <span>=></span> <span>{</span> <span>//捕获错误</span>
            	console<span>.</span><span>log</span><span>(</span><span>'err data is'</span><span>,</span> err<span>.</span>data<span>)</span>
            	<span>if</span> <span>(</span>tasks<span>.</span>length <span>></span> <span>0</span><span>)</span> <span>{</span> <span>//数组未迭代完成，继续迭代</span>
                    <span>return</span> <span>recursion</span><span>(</span>tasks<span>)</span>   <span>// 数组还未迭代完，递归继续进行迭代</span>
                <span>}</span> <span>else</span> <span>{</span> <span>//迭代完成，返回最终数据</span>
                    <span>return</span> err
                <span>}</span>
        	<span>}</span><span>)</span>
    <span>}</span><span>;</span>
    
    <span>let</span> listCopy <span>=</span> <span>[</span><span>]</span><span>.</span><span>concat</span><span>(</span>list<span>)</span><span>;</span> <span>// 拷贝需要处理的列表</span>
    <span>let</span> asyncList <span>=</span> <span>[</span><span>]</span><span>;</span> <span>// 正在进行的数量为 limit 个并发异步操作</span>
    <span>while</span><span>(</span>limit<span>--</span><span>)</span> <span>{</span> <span>// 控制并发数量，通过 Promise.all 形成 limit 条并发执行的执行链</span>
        asyncList<span>.</span><span>push</span><span>(</span> <span>recursion</span><span>(</span>listCopy<span>)</span> <span>)</span><span>;</span> <span>//实例化 limit 个 Promise 异步操作实例</span>
    <span>}</span>
    <span>return</span> Promise<span>.</span><span>all</span><span>(</span>asyncList<span>)</span>
    	<span>.</span><span>then</span><span>(</span> <span>response</span> <span>=></span> <span>{</span> <span>// 返回 limit 条并发执行的执行链的最后一个执行结果</span>
        	console<span>.</span><span>log</span><span>(</span><span><span>`</span><span>all data is </span><span>`</span></span><span>,</span>response<span>)</span>
    	<span>}</span><span>)</span>
    	<span>.</span><span>catch</span><span>(</span><span>err</span> <span>=></span> <span>{</span>
        	console<span>.</span><span>log</span><span>(</span><span><span>`</span><span>error: </span><span><span>${</span>err<span>}</span></span><span>`</span></span><span>)</span>
    	<span>}</span><span>)</span><span>;</span>  <span>// 所有并发异步操作都完成后，本次并发控制迭代完成</span>
<span>}</span>


<span>//-------------测试demo--------------------</span>
<span>let</span> dataLists <span>=</span> Array<span>.</span><span>from</span><span>(</span><span>{</span>length<span>:</span> <span>10</span><span>}</span><span>,</span> <span>(</span><span>v<span>,</span> i</span><span>)</span> <span>=></span> i<span>)</span><span>;</span> <span>//生成 1-10的数组</span>
<span>concurrencyLimit</span><span>(</span>dataLists<span>,</span> <span>3</span><span>,</span> <span>(</span><span>curItem</span><span>)</span><span>=></span><span>{</span>
    <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span>reject</span><span>)</span> <span>=></span> <span>{</span>
        <span>setTimeout</span><span>(</span><span>(</span><span>)</span><span>=></span><span>{</span>
            console<span>.</span><span>log</span><span>(</span><span><span>`</span><span>return data is </span><span><span>${</span>curItem<span>}</span></span><span>`</span></span><span>)</span>
            <span>if</span><span>(</span>curItem <span>%</span> <span>4</span> <span>==</span> <span>0</span><span>)</span> <span>{</span> <span>// 如果数据是 4 的倍数，获取失败</span>
            	<span>reject</span><span>(</span><span>{</span>
                 	data<span>:</span> curItem<span>,</span>
                	status<span>:</span> <span>404</span> 
                <span>}</span><span>)</span>
            <span>}</span>
            <span>resolve</span><span>(</span><span>{</span>
                data<span>:</span> curItem<span>,</span>
                status<span>:</span> <span>200</span>
            <span>}</span><span>)</span><span>;</span>
        <span>}</span><span>,</span> Math<span>.</span><span>random</span><span>(</span><span>)</span> <span>*</span> <span>5000</span><span>)</span>  
    <span>}</span><span>)</span><span>;</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br></div></div></li>
<li>
<p>实现三：<strong>使用递归和队列 (推荐)</strong></p>
<ul>
<li>思路：将待请求数据存入数组，形成并发执行的异步操作队列，然后进行遍历，取出数组中每一项数据执行异步操作。进行调用控制并发数的同时，每结束一个请求并发起一个新的请求。使用递归的方式，添加一个请求队列，然后我们只要维护这个队列，每次发起一个请求就添加进去，结束一个就丢出来，继而实现了控制并发</li>
</ul>
<div><pre><code><span>/**
 * @params list {Array} - 要迭代的任务数组
 * @params limit {Number} - 并发数量控制数
 * @params asyncHandle {Function} - 对 list 的每一个任务的异步处理函数，参数为当前处理项，必须 return 一个 Promise 来确定是否继续进行迭代 ( 异步处理 Promise 实例化必须在 asyncHandle 中完成 )
 * @params callback {Function} - 所有异步操作结束后执行的回调
 * @return {Promise} - 返回一个 Promise 值来确认所有数据是否迭代完成
**/</span>
<span>const</span> <span>concurrencyLimit</span> <span>=</span> <span>(</span><span>list<span>,</span> limit<span>,</span> asyncHandle<span>,</span> callback</span><span>)</span> <span>=></span> <span>{</span>
    <span>const</span> count <span>=</span> list<span>.</span>length<span>;</span> <span>// 异步任务数量</span>
    <span>const</span> tasksQueue <span>=</span> <span>[</span><span>]</span><span>;</span> <span>//并发任务队列，每次执行一个任务就添加进去，结束一个就丢出来，继而实现了控制并发</span>
    <span>const</span> results <span>=</span> <span>[</span><span>]</span><span>;</span> <span>// 任务执行结果</span>
    <span>let</span>   tasksList <span>=</span> <span>[</span><span>]</span><span>.</span><span>concat</span><span>(</span>list<span>)</span><span>;</span> <span>// 拷贝需要处理的任务列表</span>
    
    <span>const</span> <span>resultHandle</span> <span>=</span> <span>(</span><span>res</span><span>)</span> <span>=></span> <span>{</span> <span>//结果处理函数，维护请求队列</span>
        console<span>.</span><span>log</span><span>(</span><span>'current tasks queue is:'</span><span>,</span>tasksQueue<span>)</span><span>;</span>
        <span>const</span> resultNum <span>=</span> results<span>.</span><span>push</span><span>(</span>res<span>)</span><span>;</span> <span>// 保存结果，并返回执行完成的异步任务数量</span>
        <span>if</span><span>(</span>resultNum <span>&lt;</span> count <span>&amp;&amp;</span> tasksList<span>.</span>length <span>></span> <span>0</span><span>)</span> <span>{</span> <span>//返回结束任务数量和待处理任务列表不为空</span>
            tasksQueue<span>.</span><span>shift</span><span>(</span><span>)</span><span>;</span> <span>//任务队列中移除已经完成的异步任务</span>
            <span>handleTask</span><span>(</span>tasksList<span>.</span><span>shift</span><span>(</span><span>)</span><span>)</span> <span>//从任务列表中添加一个任务进行处理</span>
        <span>}</span> <span>else</span> <span>if</span><span>(</span>resultNum <span>===</span> count <span>&amp;&amp;</span> tasksList<span>.</span>length <span>===</span> <span>0</span><span>)</span> <span>{</span> 
            <span>//任务列表任务处理完成并所有执行结果返回</span>
            <span>'function'</span> <span>===</span> <span>typeof</span> callback <span>&amp;&amp;</span> <span>callback</span><span>(</span>results<span>)</span>
        <span>}</span> 
    <span>}</span>
    
    <span>const</span> <span>handleTask</span> <span>=</span> <span>(</span><span>task</span><span>)</span> <span>=></span> <span>{</span> <span>//任务处理方法，控制任务并发数量</span>
        <span>let</span> request <span>=</span> <span>asyncHandle</span><span>(</span>task<span>)</span> <span>//任务处理</span>
        	<span>.</span><span>then</span><span>(</span><span>res</span> <span>=></span> <span>{</span>
           		<span>resultHandle</span><span>(</span>res<span>)</span><span>;</span> 
       		<span>}</span><span>)</span><span>.</span><span>catch</span><span>(</span><span>res</span> <span>=></span> <span>{</span>
				<span>resultHandle</span><span>(</span>res<span>)</span><span>;</span> 
            <span>}</span><span>)</span><span>;</span>
        
        <span>if</span><span>(</span>tasksQueue<span>.</span><span>push</span><span>(</span>request<span>)</span> <span>&lt;</span> limit<span>)</span> <span>{</span> <span>//并发执行任务数量小于限制数量,[].push 操作后返回当前数组长度</span>
            <span>handleTask</span><span>(</span>tasksList<span>.</span><span>shift</span><span>(</span><span>)</span><span>)</span><span>;</span> <span>// 添加并发任务处理</span>
        <span>}</span>
    <span>}</span>
    
    <span>//将任务列表任务添加到任务处理方法</span>
    <span>handleTask</span><span>(</span>tasksList<span>.</span><span>shift</span><span>(</span><span>)</span><span>)</span><span>;</span> 
<span>}</span>

<span>//--------------------测试demo--------------------------------------</span>
<span>let</span> dataLists <span>=</span> Array<span>.</span><span>from</span><span>(</span><span>{</span>length<span>:</span> <span>10</span><span>}</span><span>,</span> <span>(</span><span>v<span>,</span> i</span><span>)</span> <span>=></span> i<span>)</span><span>;</span> <span>//生成 1-10的数组</span>
<span>const</span> <span>callback</span> <span>=</span> <span>(</span><span>results</span><span>)</span> <span>=></span> <span>{</span>
  console<span>.</span><span>log</span><span>(</span><span>'all tasks finished! results is:'</span><span>,</span>results<span>)</span><span>;</span>
<span>}</span><span>;</span>
<span>concurrencyLimit</span><span>(</span>dataLists<span>,</span> <span>3</span><span>,</span> <span>(</span><span>curItem</span><span>)</span><span>=></span><span>{</span>
    <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span>reject</span><span>)</span> <span>=></span> <span>{</span>
        <span>setTimeout</span><span>(</span><span>(</span><span>)</span><span>=></span><span>{</span>
            console<span>.</span><span>log</span><span>(</span><span><span>`</span><span>return data is </span><span><span>${</span>curItem<span>}</span></span><span>`</span></span><span>)</span>
             <span>if</span><span>(</span>curItem <span>%</span> <span>3</span> <span>==</span> <span>0</span><span>)</span> <span>{</span> <span>// 如果数据是 3 的倍数，获取失败</span>
            	<span>reject</span><span>(</span><span>{</span>
                 	data<span>:</span> curItem<span>,</span>
                	status<span>:</span> <span>404</span> 
                <span>}</span><span>)</span>
            <span>}</span>
            <span>resolve</span><span>(</span><span>{</span>
                data<span>:</span> curItem<span>,</span>
                status<span>:</span> <span>200</span>
            <span>}</span><span>)</span><span>;</span>
        <span>}</span><span>,</span> Math<span>.</span><span>random</span><span>(</span><span>)</span> <span>*</span> <span>5000</span><span>)</span>  
    <span>}</span><span>)</span><span>;</span>
<span>}</span><span>,</span>callback<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br></div></div></li>
</ul>
<h4 id="应用-2"> 应用</h4>
<ul>
<li>
<p>题目一：<strong>网页中预加载20张图片资源，分步加载，一次加载10张，两次完成，怎么控制图片请求的并发，怎样感知当前异步请求是否已完成？</strong></p>
<p>答案参考 <a href="https://zhuanlan.zhihu.com/p/29792886" target="_blank" rel="noopener noreferrer">Promise 异步流程控制</a></p>
</li>
</ul>
<h2 id="参考"> 参考</h2>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise" target="_blank" rel="noopener noreferrer">Promise</a></p>
<p><a href="https://promisesaplus.com/" target="_blank" rel="noopener noreferrer">Promises/A+</a></p>
<p><a href="https://es6.ruanyifeng.com/#docs/promise#Promise-all" target="_blank" rel="noopener noreferrer">es6文档</a></p>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Using_promises" target="_blank" rel="noopener noreferrer">使用 Promise</a></p>
<p><a href="http://liubin.org/promises-book/#ch2-promise-all" target="_blank" rel="noopener noreferrer">promise并行</a></p>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-25T19:47:29.000Z</published>
  </entry>
  <entry>
    <title type="html">Vue-Router</title>
    <id>https://sewar-x.github.io/vueRouter/</id>
    <link href="https://sewar-x.github.io/vueRouter/"/>
    <updated>2021-06-29T22:43:23.000Z</updated>
    <content type="html"><![CDATA[<h2 id="vue-router"> Vue-Router</h2>
<h3 id="路由基础"> 路由基础</h3>
<ul>
<li>
<p>路由作用：根据不同的路径映射到不同的视图。</p>
</li>
<li>
<p>监听<a href="https://router.vuejs.org/zh/guide/essentials/dynamic-matching.html#%E5%93%8D%E5%BA%94%E8%B7%AF%E7%94%B1%E5%8F%82%E6%95%B0%E7%9A%84%E5%8F%98%E5%8C%96" target="_blank" rel="noopener noreferrer">复用组件</a> 路由参数变化：watch (监测变化) <code>$route</code> 对象。</p>
<div><pre><code><span>const</span> User <span>=</span> <span>{</span>
  template<span>:</span> <span>'...'</span><span>,</span>
  watch<span>:</span> <span>{</span>
    <span>$route</span><span>(</span><span>to<span>,</span> <span>from</span></span><span>)</span> <span>{</span>
      <span>// 对路由变化作出响应...</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div></li>
<li>
<p>配置 <code>routes</code> 常用参数：</p>
<ul>
<li>路由路径：<code>path</code> 参数。</li>
<li><a href="https://router.vuejs.org/zh/guide/essentials/dynamic-matching.html#%E5%8A%A8%E6%80%81%E8%B7%AF%E7%94%B1%E5%8C%B9%E9%85%8D" target="_blank" rel="noopener noreferrer">动态路由匹配</a>： 一个“路径参数”使用冒号 <code>:</code> 标记。 如：<code>{ path: '/user/:id', component: User }</code></li>
<li><a href="https://router.vuejs.org/zh/guide/essentials/passing-props.html#%E8%B7%AF%E7%94%B1%E7%BB%84%E4%BB%B6%E4%BC%A0%E5%8F%82" target="_blank" rel="noopener noreferrer">路由组件传参</a>：使用 <code>props</code> 将组件和路由解耦。</li>
<li><a href="https://router.vuejs.org/zh/guide/essentials/named-routes.html#%E5%91%BD%E5%90%8D%E8%B7%AF%E7%94%B1" target="_blank" rel="noopener noreferrer">路由命名</a>： <code>name</code> 参数。</li>
<li>组件： <code>component</code> 参数。</li>
<li><a href="https://router.vuejs.org/zh/guide/essentials/named-views.html#%E5%B5%8C%E5%A5%97%E5%91%BD%E5%90%8D%E8%A7%86%E5%9B%BE" target="_blank" rel="noopener noreferrer">嵌套组件</a>：<code>children</code> 参数。</li>
<li><a href="https://router.vuejs.org/zh/guide/essentials/redirect-and-alias.html#%E9%87%8D%E5%AE%9A%E5%90%91" target="_blank" rel="noopener noreferrer">重定向</a>：<code>redirect</code> 参数。</li>
<li><a href="https://router.vuejs.org/zh/guide/essentials/redirect-and-alias.html#%E9%87%8D%E5%AE%9A%E5%90%91" target="_blank" rel="noopener noreferrer">别名</a>：<code>alias</code> 参数。</li>
<li><a href="https://router.vuejs.org/zh/guide/advanced/meta.html" target="_blank" rel="noopener noreferrer">元信息</a>：<code>meta</code> 参数。</li>
</ul>
</li>
<li>
<p>内置组件：</p>
<ul>
<li>视图切换：<a href="https://router.vuejs.org/zh/api/#router-link" target="_blank" rel="noopener noreferrer"><code>&lt;router-link&gt;</code> 组件</a></li>
<li>视图渲染：<a href="https://router.vuejs.org/zh/api/#aria-current-value" target="_blank" rel="noopener noreferrer"><code>&lt;router-view&gt;</code> 组件</a></li>
</ul>
</li>
<li>
<p><a href="https://router.vuejs.org/zh/guide/advanced/navigation-guards.html#%E5%85%A8%E5%B1%80%E5%89%8D%E7%BD%AE%E5%AE%88%E5%8D%AB" target="_blank" rel="noopener noreferrer">导航守卫</a>：</p>
<ul>
<li>全局守卫：
<ul>
<li>全局前置守卫：<code>router.beforeEach</code>，导航触发时，全局前置守卫按照创建顺序调用。</li>
<li>全局解析守卫：<code>router.beforeResolve</code>，导航被确认之前，同时在所有组件内守卫和异步路由组件被解析之后，解析守卫就被调用。</li>
<li>全局后置守卫：<code>router.afterEach</code>。</li>
</ul>
</li>
<li>路由独享守卫：<code>beforeEnter</code>。单个路由独享的钩子函数，它是在路由配置上直接进行定义的。</li>
<li>组件内守卫：它们是直接在路由组 件内部直接进行定义的。
<ul>
<li><code>beforeRouteEnter</code></li>
<li><code>beforeRouteUpdate</code></li>
<li><code>beforeRouteLeave</code></li>
</ul>
</li>
</ul>
</li>
<li>
<p>导航解析流程</p>
<ul>
<li>导航被触发。</li>
<li>在失活的组件里调用 <code>beforeRouteLeave</code> 守卫。</li>
<li>调用全局的 <code>beforeEach</code> 守卫。</li>
<li>在重用的组件里调用 <code>beforeRouteUpdate</code> 守卫 (2.2+)。</li>
<li>在路由配置里调用 <code>beforeEnter</code>。</li>
<li>解析异步路由组件。</li>
<li>在被激活的组件里调用 <code>beforeRouteEnter</code>。</li>
<li>调用全局的 <code>beforeResolve</code> 守卫 (2.5+)。</li>
<li>导航被确认。</li>
<li>调用全局的 <code>afterEach</code> 钩子。</li>
<li>触发 DOM 更新。</li>
<li>调用 <code>beforeRouteEnter</code> 守卫中传给 <code>next</code> 的回调函数，创建好的组件实例会作为回调函数的参数传入。</li>
</ul>
</li>
<li>
<p>路由对象：</p>
<ul>
<li><a href="https://router.vuejs.org/zh/api/#router-%E5%AE%9E%E4%BE%8B%E5%B1%9E%E6%80%A7" target="_blank" rel="noopener noreferrer"><strong>this.$router</strong></a>：<code>router</code> 的 Vue 根实例。是一个全局路由对象，包含了路由跳转的方法、钩子函数等。</li>
<li><a href="https://router.vuejs.org/zh/api/#%E8%B7%AF%E7%94%B1%E5%AF%B9%E8%B1%A1" target="_blank" rel="noopener noreferrer"><strong>this.$route</strong></a>：当前激活的<a href="https://router.vuejs.org/zh/api/#%E8%B7%AF%E7%94%B1%E5%AF%B9%E8%B1%A1" target="_blank" rel="noopener noreferrer">路由信息对象</a>。  每一个路由都会有一个 route 对象，是一个局部对象，包含path,params,hash,query,fullPath,matched,name等路由信息参数。
<ul>
<li>里面的属性是 immutable (不可变) 的，通过 watch 监测变化</li>
<li>它包含了当前 URL 解析得到的信息，还有 URL 匹配到的<strong>路由记录 (route records)</strong>。</li>
<li>每次成功的导航后都会产生一个新的对象。</li>
<li>路由对象出现在多个地方:
<ul>
<li>在组件内，即 <code>this.$route</code></li>
<li>在 <code>$route</code> 观察者回调内</li>
<li><code>router.match(location)</code> 的返回值</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>
<p><a href="https://router.vuejs.org/zh/guide/advanced/lazy-loading.html#%E8%B7%AF%E7%94%B1%E6%87%92%E5%8A%A0%E8%BD%BD" target="_blank" rel="noopener noreferrer">路由懒加载</a>：</p>
<ul>
<li>将异步组件定义为返回一个 Promise 的工厂函数</li>
<li>在 Webpack 2 中，我们可以使用<a href="https://github.com/tc39/proposal-dynamic-import" target="_blank" rel="noopener noreferrer">动态 import (opens new window)</a>语法来定义代码分块点</li>
<li>注释语法来提供 chunk name : <code>webpackChunkName</code></li>
</ul>
</li>
<li>
<p>路由传参：使用 <code>router.push()</code>切换路由。</p>
<ul>
<li>
<p><code>params</code> 传参：</p>
<ul>
<li>
<p>只能使用 <code>name</code>，不能使用 <code>path</code>；如果提供了 <code>path</code>，<code>params</code> 会被忽略</p>
<div><pre><code><span>// 这里的 params 不生效</span>
router<span>.</span><span>push</span><span>(</span><span>{</span> path<span>:</span> <span>'/user'</span><span>,</span> params<span>:</span> <span>{</span> userId <span>}</span><span>}</span><span>)</span> <span>// -> /user</span>
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div></li>
<li>
<p>参数不会显示在路径上</p>
</li>
<li>
<p>浏览器强制刷新参数会被清空</p>
<div><pre><code><span>// 传递参数</span>
<span>this</span><span>.</span>$router<span>.</span><span>push</span><span>(</span><span>{</span>
  name<span>:</span> Home<span>,</span>
  params<span>:</span> <span>{</span>
      test<span>:</span> <span>'hello'</span>
  <span>}</span>
<span>}</span><span>)</span>
router<span>.</span><span>push</span><span>(</span><span>{</span> name<span>:</span> <span>'user'</span><span>,</span> params<span>:</span> <span>{</span> userId<span>:</span><span>123</span> <span>}</span><span>}</span><span>)</span> <span>// -> /user/123</span>
<span>// 接收参数</span>
<span>const</span> p <span>=</span> <span>this</span><span>.</span>$route<span>.</span>params
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div></li>
</ul>
</li>
<li>
<p><code>query</code> 传参：带查询参数。</p>
<ul>
<li>
<p>参数会显示在路径上，刷新不会被清空</p>
</li>
<li>
<p><code>name</code> 可以使用 <code>path</code> 路径</p>
<div><pre><code><span>// 传递参数</span>
<span>this</span><span>.</span>$router<span>.</span><span>push</span><span>(</span><span>{</span>
  	name<span>:</span> Home<span>,</span>
  	query<span>:</span> <span>{</span>
		test<span>:</span><span>"hello"</span>
	<span>}</span>
<span>}</span><span>)</span>
<span>// 带查询参数，变成 /register?plan=private</span>
router<span>.</span><span>push</span><span>(</span><span>{</span> path<span>:</span> <span>'register'</span><span>,</span> query<span>:</span> <span>{</span> plan<span>:</span> <span>'private'</span> <span>}</span><span>}</span><span>)</span>
<span>// 接收参数</span>
<span>const</span> q <span>=</span> <span>this</span><span>.</span>$route<span>.</span>query
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="总体流程"> 总体流程</h3>
<p><img src="./../flow-graph/路由原理.png" alt="" /></p>
<h3 id="路由对象"> 路由对象</h3>
<p>VueRouter 的实现是一个类，它的定义在 <a href="https://github.com/vuejs/vue-router/blob/v3.0.1/src/index.js" target="_blank" rel="noopener noreferrer">src/index.js</a>中：</p>
<div><pre><code><span>export</span> <span>default</span> <span>class</span> <span>VueRouter</span> <span>{</span>
  <span>static</span> <span>install</span><span>:</span> <span>(</span><span>)</span> <span>=></span> <span>void</span><span>;</span>
  <span>static</span> version<span>:</span> string<span>;</span>

  app<span>:</span> any<span>;</span>
  apps<span>:</span> Array<span>&lt;</span>any<span>></span><span>;</span>
  ready<span>:</span> boolean<span>;</span>
  readyCbs<span>:</span> Array<span>&lt;</span>Function<span>></span><span>;</span>
  options<span>:</span> RouterOptions<span>;</span>
  mode<span>:</span> string<span>;</span>
  history<span>:</span> HashHistory <span>|</span> HTML5History <span>|</span> AbstractHistory<span>;</span>
  matcher<span>:</span> Matcher<span>;</span>
  fallback<span>:</span> boolean<span>;</span>
  beforeHooks<span>:</span> Array<span>&lt;</span><span>?</span>NavigationGuard<span>></span><span>;</span>
  resolveHooks<span>:</span> Array<span>&lt;</span><span>?</span>NavigationGuard<span>></span><span>;</span>
  afterHooks<span>:</span> Array<span>&lt;</span><span>?</span>AfterNavigationHook<span>></span><span>;</span>

  <span>constructor</span> <span>(</span><span>options<span>:</span> RouterOptions <span>=</span> <span>{</span><span>}</span></span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>app <span>=</span> <span>null</span>
    <span>this</span><span>.</span>apps <span>=</span> <span>[</span><span>]</span>
    <span>this</span><span>.</span>options <span>=</span> options
    <span>this</span><span>.</span>beforeHooks <span>=</span> <span>[</span><span>]</span>
    <span>this</span><span>.</span>resolveHooks <span>=</span> <span>[</span><span>]</span>
    <span>this</span><span>.</span>afterHooks <span>=</span> <span>[</span><span>]</span>
    <span>this</span><span>.</span>matcher <span>=</span> <span>createMatcher</span><span>(</span>options<span>.</span>routes <span>||</span> <span>[</span><span>]</span><span>,</span> <span>this</span><span>)</span>

    <span>let</span> mode <span>=</span> options<span>.</span>mode <span>||</span> <span>'hash'</span>
    <span>this</span><span>.</span>fallback <span>=</span> mode <span>===</span> <span>'history'</span> <span>&amp;&amp;</span> <span>!</span>supportsPushState <span>&amp;&amp;</span> options<span>.</span>fallback <span>!==</span> <span>false</span>
    <span>if</span> <span>(</span><span>this</span><span>.</span>fallback<span>)</span> <span>{</span>
      mode <span>=</span> <span>'hash'</span>
    <span>}</span>
    <span>if</span> <span>(</span><span>!</span>inBrowser<span>)</span> <span>{</span>
      mode <span>=</span> <span>'abstract'</span>
    <span>}</span>
    <span>this</span><span>.</span>mode <span>=</span> mode

    <span>switch</span> <span>(</span>mode<span>)</span> <span>{</span>
      <span>case</span> <span>'history'</span><span>:</span>
        <span>this</span><span>.</span>history <span>=</span> <span>new</span> <span>HTML5History</span><span>(</span><span>this</span><span>,</span> options<span>.</span>base<span>)</span>
        <span>break</span>
      <span>case</span> <span>'hash'</span><span>:</span>
        <span>this</span><span>.</span>history <span>=</span> <span>new</span> <span>HashHistory</span><span>(</span><span>this</span><span>,</span> options<span>.</span>base<span>,</span> <span>this</span><span>.</span>fallback<span>)</span>
        <span>break</span>
      <span>case</span> <span>'abstract'</span><span>:</span>
        <span>this</span><span>.</span>history <span>=</span> <span>new</span> <span>AbstractHistory</span><span>(</span><span>this</span><span>,</span> options<span>.</span>base<span>)</span>
        <span>break</span>
      <span>default</span><span>:</span>
        <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
          <span>assert</span><span>(</span><span>false</span><span>,</span> <span><span>`</span><span>invalid mode: </span><span><span>${</span>mode<span>}</span></span><span>`</span></span><span>)</span>
        <span>}</span>
    <span>}</span>
  <span>}</span>

  <span>match</span> <span>(</span>
    raw<span>:</span> RawLocation<span>,</span>
    current<span>?</span><span>:</span> Route<span>,</span>
    redirectedFrom<span>?</span><span>:</span> Location
  <span>)</span><span>:</span> Route <span>{</span>
    <span>return</span> <span>this</span><span>.</span>matcher<span>.</span><span>match</span><span>(</span>raw<span>,</span> current<span>,</span> redirectedFrom<span>)</span>
  <span>}</span>

  <span>get</span> <span>currentRoute</span> <span>(</span><span>)</span><span>:</span> <span>?</span>Route <span>{</span>
    <span>return</span> <span>this</span><span>.</span>history <span>&amp;&amp;</span> <span>this</span><span>.</span>history<span>.</span>current
  <span>}</span>

  <span>init</span> <span>(</span><span>app<span>:</span> any</span><span>)</span> <span>{</span>
    process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> <span>assert</span><span>(</span>
      install<span>.</span>installed<span>,</span>
      <span><span>`</span><span>not installed. Make sure to call \`Vue.use(VueRouter)\` </span><span>`</span></span> <span>+</span>
      <span><span>`</span><span>before creating root instance.</span><span>`</span></span>
    <span>)</span>

    <span>this</span><span>.</span>apps<span>.</span><span>push</span><span>(</span>app<span>)</span>

    <span>if</span> <span>(</span><span>this</span><span>.</span>app<span>)</span> <span>{</span>
      <span>return</span>
    <span>}</span>

    <span>this</span><span>.</span>app <span>=</span> app

    <span>const</span> history <span>=</span> <span>this</span><span>.</span>history

    <span>if</span> <span>(</span>history <span>instanceof</span> <span>HTML5History</span><span>)</span> <span>{</span>
      history<span>.</span><span>transitionTo</span><span>(</span>history<span>.</span><span>getCurrentLocation</span><span>(</span><span>)</span><span>)</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span>history <span>instanceof</span> <span>HashHistory</span><span>)</span> <span>{</span>
      <span>const</span> <span>setupHashListener</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
        history<span>.</span><span>setupListeners</span><span>(</span><span>)</span>
      <span>}</span>
      history<span>.</span><span>transitionTo</span><span>(</span>
        history<span>.</span><span>getCurrentLocation</span><span>(</span><span>)</span><span>,</span>
        setupHashListener<span>,</span>
        setupHashListener
      <span>)</span>
    <span>}</span>

    history<span>.</span><span>listen</span><span>(</span><span>route</span> <span>=></span> <span>{</span>
      <span>this</span><span>.</span>apps<span>.</span><span>forEach</span><span>(</span><span>(</span><span>app</span><span>)</span> <span>=></span> <span>{</span>
        app<span>.</span>_route <span>=</span> route
      <span>}</span><span>)</span>
    <span>}</span><span>)</span>
  <span>}</span>

  <span>beforeEach</span> <span>(</span>fn<span>:</span> Function<span>)</span><span>:</span> Function <span>{</span>
    <span>return</span> <span>registerHook</span><span>(</span><span>this</span><span>.</span>beforeHooks<span>,</span> fn<span>)</span>
  <span>}</span>

  <span>beforeResolve</span> <span>(</span>fn<span>:</span> Function<span>)</span><span>:</span> Function <span>{</span>
    <span>return</span> <span>registerHook</span><span>(</span><span>this</span><span>.</span>resolveHooks<span>,</span> fn<span>)</span>
  <span>}</span>

  <span>afterEach</span> <span>(</span>fn<span>:</span> Function<span>)</span><span>:</span> Function <span>{</span>
    <span>return</span> <span>registerHook</span><span>(</span><span>this</span><span>.</span>afterHooks<span>,</span> fn<span>)</span>
  <span>}</span>

  <span>onReady</span> <span>(</span><span>cb<span>:</span> Function<span>,</span> errorCb<span>?</span><span>:</span> Function</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>history<span>.</span><span>onReady</span><span>(</span>cb<span>,</span> errorCb<span>)</span>
  <span>}</span>

  <span>onError</span> <span>(</span><span>errorCb<span>:</span> Function</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>history<span>.</span><span>onError</span><span>(</span>errorCb<span>)</span>
  <span>}</span>

  <span>push</span> <span>(</span><span>location<span>:</span> RawLocation<span>,</span> onComplete<span>?</span><span>:</span> Function<span>,</span> onAbort<span>?</span><span>:</span> Function</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>history<span>.</span><span>push</span><span>(</span>location<span>,</span> onComplete<span>,</span> onAbort<span>)</span>
  <span>}</span>

  <span>replace</span> <span>(</span><span>location<span>:</span> RawLocation<span>,</span> onComplete<span>?</span><span>:</span> Function<span>,</span> onAbort<span>?</span><span>:</span> Function</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>history<span>.</span><span>replace</span><span>(</span>location<span>,</span> onComplete<span>,</span> onAbort<span>)</span>
  <span>}</span>

  <span>go</span> <span>(</span><span>n<span>:</span> number</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>history<span>.</span><span>go</span><span>(</span>n<span>)</span>
  <span>}</span>

  <span>back</span> <span>(</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span><span>go</span><span>(</span><span>-</span><span>1</span><span>)</span>
  <span>}</span>

  <span>forward</span> <span>(</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span><span>go</span><span>(</span><span>1</span><span>)</span>
  <span>}</span>

  <span>getMatchedComponents</span> <span>(</span>to<span>?</span><span>:</span> RawLocation <span>|</span> Route<span>)</span><span>:</span> Array<span>&lt;</span>any<span>></span> <span>{</span>
    <span>const</span> route<span>:</span> any <span>=</span> to
      <span>?</span> to<span>.</span>matched
        <span>?</span> to
        <span>:</span> <span>this</span><span>.</span><span>resolve</span><span>(</span>to<span>)</span><span>.</span>route
      <span>:</span> <span>this</span><span>.</span>currentRoute
    <span>if</span> <span>(</span><span>!</span>route<span>)</span> <span>{</span>
      <span>return</span> <span>[</span><span>]</span>
    <span>}</span>
    <span>return</span> <span>[</span><span>]</span><span>.</span><span>concat</span><span>.</span><span>apply</span><span>(</span><span>[</span><span>]</span><span>,</span> route<span>.</span>matched<span>.</span><span>map</span><span>(</span><span>m</span> <span>=></span> <span>{</span>
      <span>return</span> Object<span>.</span><span>keys</span><span>(</span>m<span>.</span>components<span>)</span><span>.</span><span>map</span><span>(</span><span>key</span> <span>=></span> <span>{</span>
        <span>return</span> m<span>.</span>components<span>[</span>key<span>]</span>
      <span>}</span><span>)</span>
    <span>}</span><span>)</span><span>)</span>
  <span>}</span>

  <span>resolve</span> <span>(</span>
    to<span>:</span> RawLocation<span>,</span>
    current<span>?</span><span>:</span> Route<span>,</span>
    append<span>?</span><span>:</span> boolean
  <span>)</span><span>:</span> <span>{</span>
    location<span>:</span> Location<span>,</span>
    route<span>:</span> Route<span>,</span>
    href<span>:</span> string<span>,</span>
    normalizedTo<span>:</span> Location<span>,</span>
    resolved<span>:</span> Route
  <span>}</span> <span>{</span>
    <span>const</span> location <span>=</span> <span>normalizeLocation</span><span>(</span>
      to<span>,</span>
      current <span>||</span> <span>this</span><span>.</span>history<span>.</span>current<span>,</span>
      append<span>,</span>
      <span>this</span>
    <span>)</span>
    <span>const</span> route <span>=</span> <span>this</span><span>.</span><span>match</span><span>(</span>location<span>,</span> current<span>)</span>
    <span>const</span> fullPath <span>=</span> route<span>.</span>redirectedFrom <span>||</span> route<span>.</span>fullPath
    <span>const</span> base <span>=</span> <span>this</span><span>.</span>history<span>.</span>base
    <span>const</span> href <span>=</span> <span>createHref</span><span>(</span>base<span>,</span> fullPath<span>,</span> <span>this</span><span>.</span>mode<span>)</span>
    <span>return</span> <span>{</span>
      location<span>,</span>
      route<span>,</span>
      href<span>,</span>
      normalizedTo<span>:</span> location<span>,</span>
      resolved<span>:</span> route
    <span>}</span>
  <span>}</span>

  <span>addRoutes</span> <span>(</span><span>routes<span>:</span> Array<span>&lt;</span>RouteConfig<span>></span></span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>matcher<span>.</span><span>addRoutes</span><span>(</span>routes<span>)</span>
    <span>if</span> <span>(</span><span>this</span><span>.</span>history<span>.</span>current <span>!==</span> <span>START</span><span>)</span> <span>{</span>
      <span>this</span><span>.</span>history<span>.</span><span>transitionTo</span><span>(</span><span>this</span><span>.</span>history<span>.</span><span>getCurrentLocation</span><span>(</span><span>)</span><span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br><span>93</span><br><span>94</span><br><span>95</span><br><span>96</span><br><span>97</span><br><span>98</span><br><span>99</span><br><span>100</span><br><span>101</span><br><span>102</span><br><span>103</span><br><span>104</span><br><span>105</span><br><span>106</span><br><span>107</span><br><span>108</span><br><span>109</span><br><span>110</span><br><span>111</span><br><span>112</span><br><span>113</span><br><span>114</span><br><span>115</span><br><span>116</span><br><span>117</span><br><span>118</span><br><span>119</span><br><span>120</span><br><span>121</span><br><span>122</span><br><span>123</span><br><span>124</span><br><span>125</span><br><span>126</span><br><span>127</span><br><span>128</span><br><span>129</span><br><span>130</span><br><span>131</span><br><span>132</span><br><span>133</span><br><span>134</span><br><span>135</span><br><span>136</span><br><span>137</span><br><span>138</span><br><span>139</span><br><span>140</span><br><span>141</span><br><span>142</span><br><span>143</span><br><span>144</span><br><span>145</span><br><span>146</span><br><span>147</span><br><span>148</span><br><span>149</span><br><span>150</span><br><span>151</span><br><span>152</span><br><span>153</span><br><span>154</span><br><span>155</span><br><span>156</span><br><span>157</span><br><span>158</span><br><span>159</span><br><span>160</span><br><span>161</span><br><span>162</span><br><span>163</span><br><span>164</span><br><span>165</span><br><span>166</span><br><span>167</span><br><span>168</span><br><span>169</span><br><span>170</span><br><span>171</span><br><span>172</span><br><span>173</span><br><span>174</span><br><span>175</span><br><span>176</span><br><span>177</span><br><span>178</span><br><span>179</span><br><span>180</span><br><span>181</span><br><span>182</span><br><span>183</span><br><span>184</span><br><span>185</span><br><span>186</span><br><span>187</span><br><span>188</span><br><span>189</span><br><span>190</span><br><span>191</span><br><span>192</span><br><span>193</span><br><span>194</span><br><span>195</span><br></div></div><h3 id="路由模式"> 路由模式</h3>
<ul>
<li>
<p>vue 中定义了三种路由模式：<code>hash</code>、<code>history</code>、<code>abstract</code> 。</p>
<ul>
<li>
<p><strong>hash 模式：</strong> 使用 url 的 hash 值来作为路由。支持所有浏览器。一般用于单页应用。</p>
<ul>
<li>表现：
<ul>
<li>url 中带有 “#” 符号，如 <code>http://localhost:8080/#/</code>；</li>
<li>页面跳转时 “#” 后面 hash 值改变；当 URL 改变时，页面不会重新加载（实际有重新获取数据）；</li>
<li>通过浏览器的前进、后退、刷新页面均可以变；</li>
</ul>
</li>
<li>原理：
<ul>
<li>监听路由变化： 如果支持 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window/popstate_event" target="_blank" rel="noopener noreferrer">popstate 事件</a>，则监听 popstate 事件，否则监听 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window/hashchange_event" target="_blank" rel="noopener noreferrer">hashchange 事件</a>;</li>
<li>点击 <code>router-link</code> 路由切换：判断是否支持  <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window/history" target="_blank" rel="noopener noreferrer">HTML5 History API</a> （replaceState 和 pushState），支持则使用 History API，否则使用 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Location/hash" target="_blank" rel="noopener noreferrer">Location.hash API</a></li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>History 模式：</strong> 使用 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window/history" target="_blank" rel="noopener noreferrer">HTML5 History API</a> 和并需要服务器配置。一般用于多页应用。</p>
<ul>
<li>表现：
<ul>
<li>url 中没有 “#” 符号</li>
<li>可以通过浏览器前进、后退，但刷新页面会导致 404 (如果服务器中没有相应的响应或者资源)；</li>
</ul>
</li>
<li>原理：
<ul>
<li>监听路由变化：监听 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window/popstate_event" target="_blank" rel="noopener noreferrer">popstate 事件</a>；</li>
<li>点击 <code>router-link</code> 路由切换：使用  <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window/history" target="_blank" rel="noopener noreferrer">HTML5 History API</a> （replaceState 和 pushState）切换路由；</li>
</ul>
</li>
<li>缺点：
<ul>
<li>要在服务端增加一个覆盖所有情况的候选资源：如果 URL 匹配不到任何静态资源，则应该返回同一个 <code>index.html</code> 页面，这个页面就是你 app 依赖的页面。</li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>Abstract 模式：</strong> 支持所有javascript运行模式。如果发现没有浏览器的API，路由会自动强制进入这个模式。</p>
</li>
</ul>
</li>
<li>
<p>三种模式中类继承关系<img src="./../flow-graph/路由模式类关系.png" alt="" /></p>
</li>
<li>
<p>三种模式的路由初始化： 在 VueRouter 构造函数中根据配置项创建路由模式。</p>
<p><img src="./../images/路由模式初始化.png" alt="" /></p>
</li>
<li>
<p>三种模式路由切换的底层实现:</p>
<p><img src="./../images/不同路由模式切换原理.png" alt="" /></p>
</li>
</ul>
<h3 id="路由安装"> 路由安装</h3>
<p><img src="./../flow-graph/路由安装.png" alt="" /></p>
<ul>
<li>
<p>Vue-Router 的入口文件是 <code>src/index.js</code>，其中定义了 <code>VueRouter</code> 类，也实现了 <code>install</code> 的静态方法：<code>VueRouter.install = install</code>，它的定义在 <code>src/install.js</code> 中：</p>
<div><pre><code><span>export</span> <span>let</span> _Vue
<span>export</span> <span>function</span> <span>install</span> <span>(</span><span>Vue</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span>install<span>.</span>installed <span>&amp;&amp;</span> _Vue <span>===</span> Vue<span>)</span> <span>return</span>
  install<span>.</span>installed <span>=</span> <span>true</span>

  _Vue <span>=</span> Vue

  <span>const</span> <span>isDef</span> <span>=</span> <span>v</span> <span>=></span> v <span>!==</span> <span>undefined</span>

  <span>const</span> <span>registerInstance</span> <span>=</span> <span>(</span><span>vm<span>,</span> callVal</span><span>)</span> <span>=></span> <span>{</span>
    <span>let</span> i <span>=</span> vm<span>.</span>$options<span>.</span>_parentVnode
    <span>if</span> <span>(</span><span>isDef</span><span>(</span>i<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>i <span>=</span> i<span>.</span>data<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>i <span>=</span> i<span>.</span>registerRouteInstance<span>)</span><span>)</span> <span>{</span>
      <span>i</span><span>(</span>vm<span>,</span> callVal<span>)</span>
    <span>}</span>
  <span>}</span>

  Vue<span>.</span><span>mixin</span><span>(</span><span>{</span>
    <span>beforeCreate</span> <span>(</span><span>)</span> <span>{</span>
      <span>if</span> <span>(</span><span>isDef</span><span>(</span><span>this</span><span>.</span>$options<span>.</span>router<span>)</span><span>)</span> <span>{</span>
        <span>this</span><span>.</span>_routerRoot <span>=</span> <span>this</span>
        <span>this</span><span>.</span>_router <span>=</span> <span>this</span><span>.</span>$options<span>.</span>router
        <span>this</span><span>.</span>_router<span>.</span><span>init</span><span>(</span><span>this</span><span>)</span>
        Vue<span>.</span>util<span>.</span><span>defineReactive</span><span>(</span><span>this</span><span>,</span> <span>'_route'</span><span>,</span> <span>this</span><span>.</span>_router<span>.</span>history<span>.</span>current<span>)</span>
      <span>}</span> <span>else</span> <span>{</span>
        <span>this</span><span>.</span>_routerRoot <span>=</span> <span>(</span><span>this</span><span>.</span>$parent <span>&amp;&amp;</span> <span>this</span><span>.</span>$parent<span>.</span>_routerRoot<span>)</span> <span>||</span> <span>this</span>
      <span>}</span>
      <span>registerInstance</span><span>(</span><span>this</span><span>,</span> <span>this</span><span>)</span>
    <span>}</span><span>,</span>
    <span>destroyed</span> <span>(</span><span>)</span> <span>{</span>
      <span>registerInstance</span><span>(</span><span>this</span><span>)</span>
    <span>}</span>
  <span>}</span><span>)</span>

  Object<span>.</span><span>defineProperty</span><span>(</span><span>Vue</span><span>.</span>prototype<span>,</span> <span>'$router'</span><span>,</span> <span>{</span>
    <span>get</span> <span>(</span><span>)</span> <span>{</span> <span>return</span> <span>this</span><span>.</span>_routerRoot<span>.</span>_router <span>}</span>
  <span>}</span><span>)</span>

  Object<span>.</span><span>defineProperty</span><span>(</span><span>Vue</span><span>.</span>prototype<span>,</span> <span>'$route'</span><span>,</span> <span>{</span>
    <span>get</span> <span>(</span><span>)</span> <span>{</span> <span>return</span> <span>this</span><span>.</span>_routerRoot<span>.</span>_route <span>}</span>
  <span>}</span><span>)</span>

  Vue<span>.</span><span>component</span><span>(</span><span>'RouterView'</span><span>,</span> View<span>)</span>
  Vue<span>.</span><span>component</span><span>(</span><span>'RouterLink'</span><span>,</span> Link<span>)</span>

  <span>const</span> strats <span>=</span> Vue<span>.</span>config<span>.</span>optionMergeStrategies
  strats<span>.</span>beforeRouteEnter <span>=</span> strats<span>.</span>beforeRouteLeave <span>=</span> strats<span>.</span>beforeRouteUpdate <span>=</span> strats<span>.</span>created
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br></div></div></li>
</ul>
<h3 id="路由匹配"> 路由匹配</h3>
<ul>
<li>使用 math 方法匹配
<ul>
<li>总流程 <img src="./../images/路由匹配.png" alt="" /></li>
</ul>
</li>
</ul>
<h3 id="路由切换"> 路由切换</h3>
<ul>
<li>流程图
<ul>
<li><img src="./../images/路由切换.png" alt="" /></li>
</ul>
</li>
</ul>
<h3 id="内置组件"> 内置组件</h3>
<h4 id="router-link"> <a href="https://router.vuejs.org/zh/api/#router-link" target="_blank" rel="noopener noreferrer"><code>&lt;router-link&gt;</code> </a></h4>
<ul>
<li>
<p>作用：支持用户在具有路由功能的应用中 (点击) 导航。</p>
</li>
<li>
<p>通过 <code>to</code> 属性指定目标地址，默认渲染成带有正确链接的 <code>&lt;a&gt;</code> 标签，可以通过配置 <code>tag</code> 属性生成别的标签.。</p>
</li>
<li>
<p>当目标路由成功激活时，链接元素自动设置一个表示激活的 CSS 类名。</p>
</li>
<li>
<p><code>&lt;router-link&gt;</code> 比起写死的 <code>&lt;a href=&quot;...&quot;&gt;</code> 会好一些，理由如下：</p>
<ul>
<li>无论是 HTML5 history 模式还是 hash 模式，它的表现行为一致，所以，当你要切换路由模式，或者在 IE9 降级使用 hash 模式，无须作任何变动。</li>
<li>在 HTML5 history 模式下，<code>router-link</code> 会守卫点击事件，让浏览器不再重新加载页面。</li>
<li>当你在 HTML5 history 模式下使用 <code>base</code> 选项之后，所有的 <code>to</code> 属性都不需要写 (基路径) 了。</li>
</ul>
</li>
<li>
<p>源码分析：router-link 本质是内置的函数式组件，<a href="https://github.com/vuejs/vue-router/blob/v3.0.1/src/components/link.js" target="_blank" rel="noopener noreferrer"><code>src/components/link.js</code></a> 中。</p>
<div><pre><code><span>export</span> <span>default</span> <span>{</span>
  name<span>:</span> <span>'RouterLink'</span><span>,</span>
  props<span>:</span> <span>{</span>
    to<span>:</span> <span>{</span>
      type<span>:</span> toTypes<span>,</span>
      required<span>:</span> <span>true</span>
    <span>}</span><span>,</span>
    tag<span>:</span> <span>{</span>
      type<span>:</span> String<span>,</span>
      <span>default</span><span>:</span> <span>'a'</span>
    <span>}</span><span>,</span>
    exact<span>:</span> Boolean<span>,</span>
    append<span>:</span> Boolean<span>,</span>
    replace<span>:</span> Boolean<span>,</span>
    activeClass<span>:</span> String<span>,</span>
    exactActiveClass<span>:</span> String<span>,</span>
    event<span>:</span> <span>{</span>
      type<span>:</span> eventTypes<span>,</span>
      <span>default</span><span>:</span> <span>'click'</span>
    <span>}</span>
  <span>}</span><span>,</span>
  <span>render</span> <span>(</span><span>h<span>:</span> Function</span><span>)</span> <span>{</span> <span>//基于 render 函数渲染</span>
    <span>//路由解析</span>
    <span>const</span> router <span>=</span> <span>this</span><span>.</span>$router
    <span>const</span> current <span>=</span> <span>this</span><span>.</span>$route
    <span>//router.resolve 是 VueRouter 的实例方法，解析 router 获得目标 location、route、href。它的定义在  src/index.js 中</span>
    <span>const</span> <span>{</span> location<span>,</span> route<span>,</span> href <span>}</span> <span>=</span> router<span>.</span><span>resolve</span><span>(</span><span>this</span><span>.</span>to<span>,</span> current<span>,</span> <span>this</span><span>.</span>append<span>)</span>

    <span>//解析类名</span>
    <span>const</span> classes <span>=</span> <span>{</span><span>}</span>
    <span>const</span> globalActiveClass <span>=</span> router<span>.</span>options<span>.</span>linkActiveClass
    <span>const</span> globalExactActiveClass <span>=</span> router<span>.</span>options<span>.</span>linkExactActiveClass
    <span>const</span> activeClassFallback <span>=</span> globalActiveClass <span>==</span> <span>null</span>
            <span>?</span> <span>'router-link-active'</span>
            <span>:</span> globalActiveClass
    <span>const</span> exactActiveClassFallback <span>=</span> globalExactActiveClass <span>==</span> <span>null</span>
            <span>?</span> <span>'router-link-exact-active'</span>
            <span>:</span> globalExactActiveClass
    <span>const</span> activeClass <span>=</span> <span>this</span><span>.</span>activeClass <span>==</span> <span>null</span>
            <span>?</span> activeClassFallback
            <span>:</span> <span>this</span><span>.</span>activeClass
    <span>const</span> exactActiveClass <span>=</span> <span>this</span><span>.</span>exactActiveClass <span>==</span> <span>null</span>
            <span>?</span> exactActiveClassFallback
            <span>:</span> <span>this</span><span>.</span>exactActiveClass
    <span>const</span> compareTarget <span>=</span> location<span>.</span>path
      <span>?</span> <span>createRoute</span><span>(</span><span>null</span><span>,</span> location<span>,</span> <span>null</span><span>,</span> router<span>)</span>
      <span>:</span> route

    classes<span>[</span>exactActiveClass<span>]</span> <span>=</span> <span>isSameRoute</span><span>(</span>current<span>,</span> compareTarget<span>)</span>
    classes<span>[</span>activeClass<span>]</span> <span>=</span> <span>this</span><span>.</span>exact
      <span>?</span> classes<span>[</span>exactActiveClass<span>]</span>
      <span>:</span> <span>isIncludedRoute</span><span>(</span>current<span>,</span> compareTarget<span>)</span>
	<span>//创建了一个守卫函数</span>
    <span>const</span> <span>handler</span> <span>=</span> <span>e</span> <span>=></span> <span>{</span>
      <span>if</span> <span>(</span><span>guardEvent</span><span>(</span>e<span>)</span><span>)</span> <span>{</span>
        <span>if</span> <span>(</span><span>this</span><span>.</span>replace<span>)</span> <span>{</span>
          router<span>.</span><span>replace</span><span>(</span>location<span>)</span>
        <span>}</span> <span>else</span> <span>{</span>
          router<span>.</span><span>push</span><span>(</span>location<span>)</span>
        <span>}</span>
      <span>}</span>
    <span>}</span>
	<span>//监听点击事件或者其它可以通过 prop 传入的事件类型，执行 hanlder 函数</span>
    <span>const</span> on <span>=</span> <span>{</span> click<span>:</span> guardEvent <span>}</span>
    <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span><span>this</span><span>.</span>event<span>)</span><span>)</span> <span>{</span>
      <span>this</span><span>.</span>event<span>.</span><span>forEach</span><span>(</span><span>e</span> <span>=></span> <span>{</span> on<span>[</span>e<span>]</span> <span>=</span> handler <span>}</span><span>)</span>
    <span>}</span> <span>else</span> <span>{</span>
      on<span>[</span><span>this</span><span>.</span>event<span>]</span> <span>=</span> handler
    <span>}</span>

    <span>const</span> data<span>:</span> any <span>=</span> <span>{</span>
      <span>class</span><span>:</span> classes
    <span>}</span>
	<span>//判断当前 tag 是否是 &lt;a> 标签，&lt;router-link> 默认会渲染成 &lt;a> 标签</span>
    <span>if</span> <span>(</span><span>this</span><span>.</span>tag <span>===</span> <span>'a'</span><span>)</span> <span>{</span>
      data<span>.</span>on <span>=</span> on
      data<span>.</span>attrs <span>=</span> <span>{</span> href <span>}</span>
    <span>}</span> <span>else</span> <span>{</span>
       <span>//当修改 tag 的 prop 渲染成其他节点，这种情况下会尝试找它子元素的 &lt;a> 标签</span>
      <span>const</span> a <span>=</span> <span>findAnchor</span><span>(</span><span>this</span><span>.</span>$slots<span>.</span>default<span>)</span>
      <span>if</span> <span>(</span>a<span>)</span> <span>{</span>
        <span>//如果有则把事件绑定到 &lt;a> 标签上并添加 href 属性</span>
        a<span>.</span>isStatic <span>=</span> <span>false</span>
        <span>const</span> extend <span>=</span> _Vue<span>.</span>util<span>.</span>extend
        <span>const</span> aData <span>=</span> a<span>.</span>data <span>=</span> <span>extend</span><span>(</span><span>{</span><span>}</span><span>,</span> a<span>.</span>data<span>)</span>
        aData<span>.</span>on <span>=</span> on
        <span>const</span> aAttrs <span>=</span> a<span>.</span>data<span>.</span>attrs <span>=</span> <span>extend</span><span>(</span><span>{</span><span>}</span><span>,</span> a<span>.</span>data<span>.</span>attrs<span>)</span>
        aAttrs<span>.</span>href <span>=</span> href
      <span>}</span> <span>else</span> <span>{</span><span>//否则绑定到外层元素本身</span>
        data<span>.</span>on <span>=</span> on
      <span>}</span>
    <span>}</span>
	<span>//使用 createElement() 创建 &lt;a> 标签</span>
    <span>return</span> <span>h</span><span>(</span><span>this</span><span>.</span>tag<span>,</span> data<span>,</span> <span>this</span><span>.</span>$slots<span>.</span>default<span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br><span>93</span><br><span>94</span><br><span>95</span><br><span>96</span><br></div></div></li>
</ul>
<h4 id="router-view"> <a href="https://router.vuejs.org/zh/api/#aria-current-value" target="_blank" rel="noopener noreferrer"><code>&lt;router-view&gt;</code> </a></h4>
<ul>
<li>
<p><code>&lt;router-view&gt;</code> 组件是一个 functional 组件，渲染路径匹配到的视图组件。<code>&lt;router-view&gt;</code> 渲染的组件还可以内嵌自己的 <code>&lt;router-view&gt;</code>，根据嵌套路径，渲染嵌套组件。</p>
</li>
<li>
<p>其他属性 (非 router-view 使用的属性) 都直接传给渲染的组件， 很多时候，每个路由的数据都是包含在路由参数中。</p>
</li>
<li>
<p>源码分析：它的定义在<a href="https://github.com/vuejs/vue-router/blob/v3.0.1/src/components/view.js" target="_blank" rel="noopener noreferrer"> <code>src/components/view.js</code></a> 中。</p>
<div><pre><code><span>export</span> <span>default</span> <span>{</span>
  name<span>:</span> <span>'RouterView'</span><span>,</span>
  functional<span>:</span> <span>true</span><span>,</span>
  props<span>:</span> <span>{</span>
    name<span>:</span> <span>{</span>
      type<span>:</span> String<span>,</span>
      <span>default</span><span>:</span> <span>'default'</span>
    <span>}</span>
  <span>}</span><span>,</span>
  <span>render</span> <span>(</span><span>_<span>,</span> <span>{</span> props<span>,</span> children<span>,</span> parent<span>,</span> data <span>}</span></span><span>)</span> <span>{</span>
    data<span>.</span>routerView <span>=</span> <span>true</span>
   
    <span>const</span> h <span>=</span> parent<span>.</span>$createElement
    <span>const</span> name <span>=</span> props<span>.</span>name
    <span>const</span> route <span>=</span> parent<span>.</span>$route <span>//获取当前的路径</span>
    <span>const</span> cache <span>=</span> parent<span>.</span>_routerViewCache <span>||</span> <span>(</span>parent<span>.</span>_routerViewCache <span>=</span> <span>{</span><span>}</span><span>)</span>

    <span>let</span> depth <span>=</span> <span>0</span> <span>//&lt;router-view> 是支持嵌套的,表示 &lt;router-view> 嵌套的深度</span>
    <span>let</span> inactive <span>=</span> <span>false</span>
    <span>//parent._routerRoot 表示的是根 Vue 实例,这个循环就是从当前的 &lt;router-view> 的父节点向上找，一直找到根 Vue 实例，在这个过程，如果碰到了父节点也是 &lt;router-view> 的时候，说明 &lt;router-view> 有嵌套的情况，depth++。</span>
    <span>while</span> <span>(</span>parent <span>&amp;&amp;</span> parent<span>.</span>_routerRoot <span>!==</span> parent<span>)</span> <span>{</span>
      <span>if</span> <span>(</span>parent<span>.</span>$vnode <span>&amp;&amp;</span> parent<span>.</span>$vnode<span>.</span>data<span>.</span>routerView<span>)</span> <span>{</span>
        depth<span>++</span>
      <span>}</span>
      <span>if</span> <span>(</span>parent<span>.</span>_inactive<span>)</span> <span>{</span>
        inactive <span>=</span> <span>true</span>
      <span>}</span>
      parent <span>=</span> parent<span>.</span>$parent
    <span>}</span>
    data<span>.</span>routerViewDepth <span>=</span> depth

    <span>if</span> <span>(</span>inactive<span>)</span> <span>{</span>
      <span>return</span> <span>h</span><span>(</span>cache<span>[</span>name<span>]</span><span>,</span> data<span>,</span> children<span>)</span>
    <span>}</span>
    <span>//根据当前线路匹配的路径和 depth 找到对应的 RouteRecord，进而找到该渲染的组件</span>
    <span>const</span> matched <span>=</span> route<span>.</span>matched<span>[</span>depth<span>]</span>
    <span>if</span> <span>(</span><span>!</span>matched<span>)</span> <span>{</span>
      cache<span>[</span>name<span>]</span> <span>=</span> <span>null</span>
      <span>return</span> <span>h</span><span>(</span><span>)</span>
    <span>}</span>

    <span>const</span> component <span>=</span> cache<span>[</span>name<span>]</span> <span>=</span> matched<span>.</span>components<span>[</span>name<span>]</span>
   	<span>//注册路由实例的方法</span>
    data<span>.</span><span>registerRouteInstance</span> <span>=</span> <span>(</span><span>vm<span>,</span> val</span><span>)</span> <span>=></span> <span>{</span>  
      <span>//matched.instances[name] 赋值当前组件的 vm 实例:在混入的 beforeCreate 钩子函数中，会执行 registerInstance 方法，进而执行 render 函数中定义的 registerRouteInstance 方法</span>
      <span>const</span> current <span>=</span> matched<span>.</span>instances<span>[</span>name<span>]</span>
      <span>if</span> <span>(</span>
        <span>(</span>val <span>&amp;&amp;</span> current <span>!==</span> vm<span>)</span> <span>||</span>
        <span>(</span><span>!</span>val <span>&amp;&amp;</span> current <span>===</span> vm<span>)</span>
      <span>)</span> <span>{</span>
        matched<span>.</span>instances<span>[</span>name<span>]</span> <span>=</span> val
      <span>}</span>
    <span>}</span>
    
    <span>;</span><span>(</span>data<span>.</span>hook <span>||</span> <span>(</span>data<span>.</span>hook <span>=</span> <span>{</span><span>}</span><span>)</span><span>)</span><span>.</span><span>prepatch</span> <span>=</span> <span>(</span><span>_<span>,</span> vnode</span><span>)</span> <span>=></span> <span>{</span>
      matched<span>.</span>instances<span>[</span>name<span>]</span> <span>=</span> vnode<span>.</span>componentInstance
    <span>}</span>

    <span>let</span> propsToPass <span>=</span> data<span>.</span>props <span>=</span> <span>resolveProps</span><span>(</span>route<span>,</span> matched<span>.</span>props <span>&amp;&amp;</span> matched<span>.</span>props<span>[</span>name<span>]</span><span>)</span>
    <span>if</span> <span>(</span>propsToPass<span>)</span> <span>{</span>
      propsToPass <span>=</span> data<span>.</span>props <span>=</span> <span>extend</span><span>(</span><span>{</span><span>}</span><span>,</span> propsToPass<span>)</span>
      <span>const</span> attrs <span>=</span> data<span>.</span>attrs <span>=</span> data<span>.</span>attrs <span>||</span> <span>{</span><span>}</span>
      <span>for</span> <span>(</span><span>const</span> key <span>in</span> propsToPass<span>)</span> <span>{</span>
        <span>if</span> <span>(</span><span>!</span>component<span>.</span>props <span>||</span> <span>!</span><span>(</span>key <span>in</span> component<span>.</span>props<span>)</span><span>)</span> <span>{</span>
          attrs<span>[</span>key<span>]</span> <span>=</span> propsToPass<span>[</span>key<span>]</span>
          <span>delete</span> propsToPass<span>[</span>key<span>]</span>
        <span>}</span>
      <span>}</span>
    <span>}</span>
	<span>//根据 component 渲染出对应的组件 vond</span>
    <span>return</span> <span>h</span><span>(</span>component<span>,</span> data<span>,</span> children<span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br></div></div></li>
</ul>
<h3 id="相关问题"> 相关问题</h3>
<blockquote>
<p>路由匹配过程?</p>
</blockquote>
<ul>
<li>
<p>在 new Router(routes)时,根据传入用户路由配置生成三张路由映射表：pathList (存储所有路径) ，pathMap (路径 -&gt; RouterRecord映射表)，nameMap  (name -&gt; RouterRecord映射表)</p>
</li>
<li>
<p>路由切换触发 history.transitionTo() 进行路由过度, 回调执行 router.math() 进行路由匹配：</p>
<ul>
<li>
<p>当直接在浏览器输入路径时，math() 中先通过 normalizeLocation() 将 router 转换成 Location 对象：<img src="./../images/image-20200803222454635.png" alt="image-20200803222454635" /></p>
<p>当name 不存在时，遍历 pathList 表，获得 path，再通过 pathMap 获取 RoouterRecord 对象:<img src="./../images/image-20200803222804270.png" alt="image-20200803222804270" /></p>
<p>（RoouterRecord 对象的 regex 使用<a href="https://github.com/pillarjs/path-to-regexp/tree/v1.7.0" target="_blank" rel="noopener noreferrer">path-to-regexp</a> 作为路径匹配引擎，path-to-regexp 会将 路由配置中 path 转为如下结构的一个正则表达式，最终使用日 re 正则表达式匹配 &quot;输入浏览器的路径&quot; ）<img src="./../images/image-20200803223300946.png" alt="image-20200803223300946" /></p>
<p>然后获取RoouterRecord 对象 regex 与当前 Router 的 location 对象的 path 比较</p>
<div><pre><code><span>matchRoute</span><span>(</span>record<span>.</span>regex<span>,</span> location<span>.</span>path<span>,</span> location<span>.</span>params<span>)</span>
</code></pre>
<div><span>1</span><br></div></div><p>匹配成功通过 _createRoute(record, location, redirectedFrom) 创建路径</p>
</li>
</ul>
</li>
</ul>
<blockquote>
<p>路由匹配引擎?</p>
</blockquote>
<ul>
<li><code>vue-router</code> 使用 <a href="https://github.com/pillarjs/path-to-regexp/tree/v1.7.0" target="_blank" rel="noopener noreferrer">path-to-regexp</a> 作为路径匹配引擎，所以支持很多高级的匹配模式，例如：可选的动态路径参数、匹配零个或多个、一个或多个，甚至是自定义正则匹配</li>
</ul>
<blockquote>
<p>/test/:name 与 /test/:id 两个路由将如何匹配？  (假设 name 参数特点为非数字字符串，id 参数特点为数字)</p>
</blockquote>
<ul>
<li>
<div><pre><code><span>const</span> routes <span>=</span> <span>[</span>	
  <span>{</span> path<span>:</span> <span>'/test/:name'</span><span>,</span> component<span>:</span> Name <span>}</span><span>,</span>
  <span>{</span> path<span>:</span> <span>'/test/:id'</span><span>,</span> component<span>:</span> <span>ID</span> <span>}</span>
<span>]</span>
<span>const</span> router <span>=</span> <span>new</span> <span>VueRouter</span><span>(</span><span>{</span>
  routes
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
<li>
<p>因为 /test/:name 与 /test/:id 通过 <a href="https://github.com/pillarjs/path-to-regexp/tree/v1.7.0" target="_blank" rel="noopener noreferrer">path-to-regexp</a> 匹配引擎解析后的 regexp 相同  ( 解析结果都为 /^/foo/([^/]+?)/?$/i )，因此使用 /test/:name 与 /test/:id 作为路由配置的 path 参数是无法区分两个路由；在路由匹配时通过遍历 pathList 匹配 &quot;浏览器路径&quot;，因此当输入 /test/joker 或则 /test/1234 时将总是匹配到 /test/:name 路径</p>
</li>
<li>
<p>解决方案: /test/:name 与 /test/:id 是相同路径不同参数名称的路由，可以根据 name 和 id 两个不同参数的特征，使用正则表达式匹配，因此路由配置中路径可以写为 /test/:name([A-Za-z]+) 与 /test/:id(\d+)</p>
</li>
</ul>
<blockquote>
<p>路由之间是怎么跳转的？有哪些方式？</p>
</blockquote>
<p>组件导航: router-link</p>
<p>编程导航: router.push、 router.replace、 router.go</p>
<blockquote>
<p>active-class 是哪个组件的属性？</p>
</blockquote>
<p><a href="https://router.vuejs.org/zh/api/#active-class" target="_blank" rel="noopener noreferrer">active-class</a> 是 router-link 属性，设置链接激活时使用的 CSS 类名。默认值可以通过路由的构造选项 <code>linkActiveClass</code> 来全局配置。</p>
<p><strong>导航守卫相关:</strong></p>
<blockquote>
<p>如果目的地和当前路由相同，只有参数发生了改变 (比如从一个用户资料到另一个 <code>/users/1</code> -&gt; <code>/users/2</code>) 如何响应变化？</p>
</blockquote>
<ul>
<li>使用 <a href="https://router.vuejs.org/zh/guide/essentials/dynamic-matching.html#%E5%93%8D%E5%BA%94%E8%B7%AF%E7%94%B1%E5%8F%82%E6%95%B0%E7%9A%84%E5%8F%98%E5%8C%96" target="_blank" rel="noopener noreferrer"><code>beforeRouteUpdate</code></a> 来响应这个变化 (比如抓取用户信息)</li>
</ul>
<blockquote>
<p>beforeRouteEnter 能否拿到组件实例 this? 为什么?</p>
</blockquote>
<ul>
<li>
<p>不能，因为当守卫执行前，组件实例还没被创建，但是我们可以通过传一个回调给 next 来访问组件实例：</p>
<div><pre><code><span>beforeRouteEnter</span> <span>(</span><span>to<span>,</span> <span>from</span><span>,</span> next</span><span>)</span> <span>{</span>
	<span>next</span><span>(</span><span>vm</span> <span>=></span> <span>{</span>
		<span>// 通过 `vm` 访问组件实例</span>
	<span>}</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div></li>
</ul>
<blockquote>
<p>如何在路由守卫进行管道中的下一个钩子？</p>
</blockquote>
<ul>
<li>
<p>调用 next() 方法</p>
</li>
<li>
<p><code>router.beforeEach</code> 注册一个全局前置守卫：</p>
<div><pre><code><span>const</span> router <span>=</span> <span>new</span> <span>VueRouter</span><span>(</span><span>{</span> <span>...</span> <span>}</span><span>)</span>

router<span>.</span><span>beforeEach</span><span>(</span><span>(</span><span>to<span>,</span> <span>from</span><span>,</span> next</span><span>)</span> <span>=></span> <span>{</span>
  <span>// ...</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div><p>当一个导航触发时，全局前置守卫按照创建顺序调用。守卫是异步解析执行，此时导航在所有守卫 resolve 完之前一直处于 <strong>等待中</strong>。</p>
<p>每个守卫方法接收三个参数：</p>
<ul>
<li><strong><code>to: Route</code></strong>: 即将要进入的目标 <a href="https://router.vuejs.org/zh/api/#%E8%B7%AF%E7%94%B1%E5%AF%B9%E8%B1%A1" target="_blank" rel="noopener noreferrer">路由对象</a></li>
<li><strong><code>from: Route</code></strong>: 当前导航正要离开的路由</li>
<li><strong><code>next: Function</code></strong>: 一定要调用该方法来 <strong>resolve</strong> 这个钩子。执行效果依赖 <code>next</code> 方法的调用参数。
<ul>
<li><strong><code>next()</code></strong>: 进行管道中的下一个钩子。如果全部钩子执行完了，则导航的状态就是 <strong>confirmed</strong> (确认的)。</li>
<li><strong><code>next(false)</code></strong>: 中断当前的导航。如果浏览器的 URL 改变了 (可能是用户手动或者浏览器后退按钮)，那么 URL 地址会重置到 <code>from</code> 路由对应的地址。</li>
<li><strong><code>next('/')</code> 或者 <code>next({ path: '/' })</code></strong>: 跳转到一个不同的地址。当前的导航被中断，然后进行一个新的导航。你可以向 <code>next</code> 传递任意位置对象，且允许设置诸如 <code>replace: true</code>、<code>name: 'home'</code> 之类的选项以及任何用在 <a href="https://router.vuejs.org/zh/api/#to" target="_blank" rel="noopener noreferrer"><code>router-link</code> 的 <code>to</code> prop</a> 或 <a href="https://router.vuejs.org/zh/api/#router-push" target="_blank" rel="noopener noreferrer"><code>router.push</code></a> 中的选项。</li>
<li><strong><code>next(error)</code></strong>: (2.4.0+) 如果传入 <code>next</code> 的参数是一个 <code>Error</code> 实例，则导航会被终止且该错误会被传递给 <a href="https://router.vuejs.org/zh/api/#router-onerror" target="_blank" rel="noopener noreferrer"><code>router.onError()</code></a> 注册过的回调。</li>
</ul>
</li>
</ul>
</li>
</ul>
<blockquote>
<p>为什么路由路由守卫中必须调用 next()，才会前进到下一个导航守卫钩子函数中？</p>
</blockquote>
<p>Q：vue 路由中实现了一个 iterator 遍历器，在路由切换时，解析路由出守卫队列 queue ，然后使用遍历器遍历 路由守卫队列；</p>
<p><strong>路由应用：</strong></p>
<blockquote>
<p>切换路由时需要保存草稿的功能，怎么实现？</p>
</blockquote>
<p>使用 <code>&lt;keep-alive&gt;</code> 缓存组件：在组件切换过程中将状态保留在内存中，防止重复渲染DOM。</p>
<p>在router中设置router的元信息meta：</p>
<div><pre><code><span>//...router.js </span>
<span>export</span> <span>default</span> <span>new</span> <span>Router</span><span>(</span><span>{</span>
    routes<span>:</span> <span>[</span> 
      <span>{</span> path<span>:</span> <span>'/'</span><span>,</span>
        name<span>:</span> <span>'Hello'</span><span>,</span>
        component<span>:</span> Hello<span>,</span> 
        meta<span>:</span> <span>{</span>
          keepAlive<span>:</span> <span>false</span>    <span>// 不需要缓存 </span>
  <span>}</span> 
<span>}</span><span>,</span> 
       <span>{</span> path<span>:</span> <span>'/page1'</span><span>,</span> 
         name<span>:</span> <span>'Page1'</span><span>,</span> 
         component<span>:</span> Page1<span>,</span> 
         meta<span>:</span> <span>{</span>
           keepAlive<span>:</span> <span>true</span>    <span>// 需要被缓存 </span>
       <span>}</span> 
     <span>}</span> 
   <span>]</span> 
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div><p>组件中添加 meta 判断：</p>
<div><pre><code><span><span><span>&lt;</span>keep-alive</span><span>></span></span>
    <span><span><span>&lt;</span>router-view</span> <span>v-if</span><span><span>=</span><span>"</span>$route.meta.keepAlive<span>"</span></span><span>></span></span><span><span><span>&lt;/</span>router-view</span><span>></span></span>
<span><span><span>&lt;/</span>keep-alive</span><span>></span></span>
 <span><span><span>&lt;</span>router-view</span> <span>v-if</span><span><span>=</span><span>"</span>!$route.meta.keepAlive<span>"</span></span><span>></span></span><span><span><span>&lt;/</span>router-view</span><span>></span></span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br></div></div><h3 id="参考资料"> 参考资料</h3>
<ul>
<li><a href="https://router.vuejs.org/zh/" target="_blank" rel="noopener noreferrer">Vue Router</a></li>
</ul>
<hr>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-29T22:43:23.000Z</published>
  </entry>
  <entry>
    <title type="html">VUE-SSR</title>
    <id>https://sewar-x.github.io/vueSSR/</id>
    <link href="https://sewar-x.github.io/vueSSR/"/>
    <updated>2021-07-01T16:57:35.000Z</updated>
    <content type="html"><![CDATA[<h1 id="vue-ssr"> VUE-SSR</h1>
<h2 id="预备知识"> 预备知识</h2>
<h3 id="seo"> SEO</h3>
<ul>
<li>
<p>概念：SEO 是搜索引擎优化（Search Engine Optimization）。SEO是指通过对网站进行站内优化和修复(网站Web结构调整、网站内容建设、网站代码优化和编码等)和站外优化。</p>
</li>
<li>
<p>SEO规则：不同搜索引擎对于 SEO 有不同详细规则。</p>
</li>
<li>
<p>目的：</p>
<ul>
<li>提高网站的网站关键词排名以及公司产品的曝光度。通过搜索引擎查找信息是当今网民们寻找网上信息和资源的主要手段。</li>
<li>通过引流增加曝光度。</li>
</ul>
</li>
<li>
<p>SEO 标准：</p>
<p><img src="./../images/seo标准.png" alt="" /></p>
</li>
</ul>
<p><strong>前端 SEO 优化：</strong></p>
<ul>
<li>
<p>添加 TDK ：</p>
<ul>
<li>title：
<ul>
<li>浏览器上显示的那些内容，不仅用户能看到，也能被搜索引擎检索到（搜索引擎在抓取网页时，最先读取的就是网页标题，所以title是否正确设置极其重要。）</li>
<li>title一般不超过80个字符，而且词语间要用英文“-”隔开，因为计算机只对英语的敏感性较高，对汉语的敏感性不高。</li>
<li>重要关键词出现不要超过 2 次，而且要靠前，不同页面 title 要有所不同。</li>
<li>用法：<code>&lt;title&gt;网站标题&lt;/title&gt;</code></li>
</ul>
</li>
<li>description（内容摘要）:
<ul>
<li>description 是对于一个网页的简要内容概况。</li>
<li>description一般不超过150个字符，描述内容要和页面内容相关。</li>
<li>用法：<code>&lt;meta name=&quot;Description&quot; Content=&quot;你网页的简述&quot;&gt;</code></li>
</ul>
</li>
<li>keywords（关键词）:
<ul>
<li>主要作用是告诉搜索引擎本页内容是围绕哪些词展开的。</li>
<li>keywords 的每个词都要能在内容中找到相应匹配，才有利于排名。</li>
<li>keywords一般不超过3个，每个关键词不宜过长，而且词语间要用英文“,”隔开。</li>
<li>用法：<code>&lt;meta name=&quot;Keywords&quot; Content=&quot;关键词1,关键词2,关键词3,关键词4″&gt;</code></li>
</ul>
</li>
</ul>
</li>
<li>
<p>添加结构化数据：</p>
<ul>
<li>结构化数据（英文：Structured data）是一种标准化格式，用于提供网页的相关信息并对网页内容进行分类；</li>
<li>作用：增加展示信息，利于排名。结构化数据优化过的网站，在Google搜索中比单一的通过title，url，description三部分展示的信息更多更全，更加有助于排名，而且也更加吸引读者点击，也是Google比较推崇的。</li>
</ul>
</li>
<li>
<p>添加站点地图：</p>
<ul>
<li>站点地图是网站的蓝图，可帮助搜索引擎查找、爬取和索引网站的所有内容。它还会告诉搜索引擎您网站上的哪些页面最重要。</li>
<li>站点地图不是面向用户的，而是面向搜索引擎的。</li>
<li>主要作用是：主动帮助搜索引擎爬取网站内容，告诉搜索引擎哪个页面很重要。</li>
<li>网站地图主要有四种类型：
<ul>
<li>普通XML网站地图：这是迄今为止最常见的网站地图类型。通常以XML Sitemap的形式链接到您网站上的页面。</li>
<li>视频站点地图：专门用于帮助Google了解您页面上的视频内容。</li>
<li>新闻站点地图：帮助Google在“Google新闻”审核过的网站上查找内容。</li>
<li>图片站点地图：帮助Google 查找您网站上托管的所有图片。</li>
</ul>
</li>
</ul>
</li>
<li>
<p>代码优化：</p>
<ul>
<li>
<p>减少无效代码；</p>
</li>
<li>
<p>html 代码优化：</p>
<ul>
<li>重要内容 HTML 代码放在最前：搜索引擎抓取 HTML 顺序是从上到下，有的搜索引 擎对抓取长度有限制，保证重要内容肯定被抓取。</li>
<li><code>&lt;body&gt;</code> 的代码遵循语义化：
<ul>
<li>h1~h6标签多用于标题</li>
<li>ul标签多用于无序列表</li>
<li>ol标签用于有序列表</li>
<li>dl标签用于定义数据列表</li>
<li>em,strong表示强调</li>
<li>span标签是html里面最没有语义的</li>
</ul>
</li>
<li>标签优化：
<ul>
<li><code>&lt;a&gt;</code> 标签：加上链接说明title，另外对于指向其他网站的链接要加上&quot;rel=nofollow&quot;,表示不要爬取这个链接。</li>
<li><code>&lt;img&gt;</code>标签应使用alt说明，爬虫对图片的读取有限。</li>
</ul>
</li>
</ul>
</li>
<li>
<p>CSS 代码优化：</p>
<ul>
<li>
<p>减少 CSS 结构嵌套</p>
</li>
<li>
<p>隐藏内容元素对SEO影响：display:none 和 overflow:hidden 大量地被使用会被SEO认为作弊</p>
</li>
<li>
<p>使用 display: none 控制 tab 元素显示隐藏，但是  display: none 大量地被使用会被SEO认为作弊</p>
<p>解决方式： 使用 height: 0；控制不显示元素方式代替 display:none；</p>
</li>
</ul>
</li>
<li>
<p>JS 代码优化：</p>
<ul>
<li>重要内容不要用 js 输出：爬虫不会执行 js 获取内</li>
<li>避免内嵌 JS 代码；</li>
<li>少用 iframe：搜索引擎不会抓取 iframe 中的内容。</li>
</ul>
</li>
</ul>
</li>
<li>
<p>内容优化：增加页面有效内容</p>
</li>
</ul>
<p><strong>参考资料：</strong></p>
<p><a href="http://www.tianying888.com/gongsidongtai/70326.html" target="_blank" rel="noopener noreferrer">网站TDK对SEO有什么影响</a></p>
<p><a href="https://developers.google.com/search/docs/guides/search-gallery?hl=zh-cn" target="_blank" rel="noopener noreferrer">google 结构化数据介绍</a></p>
<p><a href="https://support.google.com/webmasters/answer/156184?hl=zh-Hans" target="_blank" rel="noopener noreferrer">了解站点地图</a></p>
<p><a href="https://zhuanlan.zhihu.com/p/144078265" target="_blank" rel="noopener noreferrer">什么是站点地图？如何优化站点地图？</a></p>
<p><a href="https://zhuanlan.zhihu.com/p/76258427" target="_blank" rel="noopener noreferrer">浅谈Google SEO里的结构化数据优化</a></p>
<h3 id="pwa-应用"> PWA 应用</h3>
<ul>
<li>
<p>概念：</p>
<ul>
<li>PWA 全称 Progressive Web App，即渐进式 WEB 应用。</li>
<li>PWA 应用是一个网页, 可以通过搜索引擎发现。</li>
<li>使用 Manifest 和 Service Worker 来实现 PWA 的安装和离线等功能。</li>
</ul>
</li>
<li>
<p>PWA 特性：</p>
<ul>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Introduction#Advantages_of_web_applications#discoverable" target="_blank" rel="noopener noreferrer">可发现（Discoverable）</a>, 可以通过搜索引擎发现。</li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Introduction#Advantages_of_web_applications#installable" target="_blank" rel="noopener noreferrer">可安装（Installable）</a>, 可以出现在设备的主屏幕。</li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Introduction#Advantages_of_web_applications#linkable" target="_blank" rel="noopener noreferrer">可链接（Linkable）</a>, 可以简单地通过 URL 分享。</li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Introduction#Advantages_of_web_applications#network_independent" target="_blank" rel="noopener noreferrer">独立于网络（Network independent）</a>, 可以在离线状态或者是在网速很差的情况下运行。</li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Introduction#Advantages_of_web_applications#progressive" target="_blank" rel="noopener noreferrer">渐进式（Progressive）</a>, 在老版本的浏览器仍旧可以使用，在新版本的浏览器上可以使用全部功能。</li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Introduction#Advantages_of_web_applications#re-engageable" target="_blank" rel="noopener noreferrer">可重入（Re-engageable）</a>, 无论何时有新的内容，都可以发送通知。</li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Introduction#Advantages_of_web_applications#responsive" target="_blank" rel="noopener noreferrer">响应式（Responsive）</a>, 在任何具有屏幕和浏览器的设备上可以正常使用——包括手机、平板电脑、笔记本、电视、冰箱等。</li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Introduction#Advantages_of_web_applications#safe" target="_blank" rel="noopener noreferrer">安全（Safe）</a>, 在用户、应用和服务器之间的连接是安全的，第三方无法访问你的敏感数据。</li>
</ul>
</li>
<li>
<p>主要结构：</p>
<ul>
<li>提供 <a href="https://developer.mozilla.org/zh-CN/docs/Web/Progressive_web_apps/App_structure#app_shell_%E6%A6%82%E5%BF%B5" target="_blank" rel="noopener noreferrer">App Shell</a> 基本外壳可以添加至主屏幕。使用 Manifest 实现。</li>
<li>提供离线缓存功能。使用 service worke 实现。</li>
<li>提供消息推送功能。使用 service worke 实现。</li>
</ul>
</li>
</ul>
<h4 id="app-shell"> App Shell</h4>
<ul>
<li>概念：提供一个 Web APP 的外壳，然后缓存它，以便在后续访问时可以离线使用，然后再加载应用程序的所有内容。这样，下次有人从设备访问应用程序时，UI 立即从缓存加载；如果缓存数据不可用的话，再从服务器请求新内容。
<ul>
<li>从 HTML 的角度，App Shell 就是 content 节之外的内容。再使用 JS 处理模板生成 content 中的内容。</li>
</ul>
</li>
<li>优点：
<ul>
<li>这种结构的页面很快，给用户的感觉也很快：用户会立即看到内容而不是加载动画或空白页。</li>
<li>如果网络连接不可用，允许离线访问网站。</li>
<li>网站感觉就像一个原生应用，交互及时、性能可靠。</li>
</ul>
</li>
</ul>
<h4 id="manifest"> Manifest</h4>
<p>可安装网站需要满足以下条件：</p>
<ul>
<li>一份网页清单，填好<a href="https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Add_to_home_screen#manifest" target="_blank" rel="noopener noreferrer">正确的字段</a></li>
<li>网站的协议必须是安全的（即使用 HTTPS 协议）</li>
<li>一个在设备上代表应用的图标</li>
<li>一个注册好的 Service Worker，可以让应用离线工作（这仅对于安卓设备上的 Chrome 浏览器是必需的）</li>
</ul>
<p>Manifest: 离线访问的关键在于一份网页清单，它通过 JSON 形式列举了网站的所有信息。</p>
<p>它通常位于网页应用的根目录，包含一些有用的信息，比如应用的标题、在移动设备操作系统上显示的代表该应用的不同大小的图标（例如主屏图标）的路径，和用于加载页或启动画面的背景颜色。浏览器需要这些信息来安装 web 应用并使其在主屏上显示。</p>
<p>参考资料：</p>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/Progressive_web_apps/Installable_PWAs#%E6%B8%85%E5%8D%95%E6%96%87%E4%BB%B6_(manifest)" target="_blank" rel="noopener noreferrer">清单文件 (Manifest)</a></p>
<h4 id="service-worker"> Service Worker</h4>
<ul>
<li>
<p>概念： Service Worker 是浏览器和网络之间的虚拟代理。</p>
</li>
<li>
<p>特点：</p>
<ul>
<li>
<p>运行在一个与页面 JavaScript 主线程独立的线程上，单独的作用域范围，单独的运行环境和执行线程。</p>
</li>
<li>
<p>不能操作页面 DOM。但可以通过事件机制来处理。</p>
</li>
<li>
<p>不受页面刷新的影响，可以监听和截拦作用域范围内所有页面的 HTTP 请求。</p>
</li>
<li>
<p>API 是异步非阻塞的，并且可以在不同的上下文之间发送和接收信息。</p>
</li>
<li>
<p>它设计为完全异步，同步API（如<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/XMLHttpRequest" target="_blank" rel="noopener noreferrer">XHR</a>和<a href="https://developer.mozilla.org/zh-CN/docs/conflicting/Web/API/Web_Storage_API" target="_blank" rel="noopener noreferrer">localStorage</a>）不能在service worker中使用。</p>
</li>
<li>
<p>事件驱动型服务线程。可分配给 Service Worker 一些任务，并通过基于 <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise" target="_blank" rel="noopener noreferrer">Promise</a> 的方法在任务完成时收到结果。</p>
</li>
<li>
<p>网站必须使用 HTTPS。除了使用本地开发环境调试时(如域名使用 localhost)。</p>
<ul>
<li>
<blockquote>
<p>为什么要求网站必须是HTTPS？</p>
<p>因为service worker权限太大能拦截所有页面的请求，使用 http 的网站安装 service worker 很容易被攻击,修改网络请求的能力暴露给中间人攻击会非常危险</p>
</blockquote>
</li>
</ul>
</li>
</ul>
</li>
<li>
<p>Service Worker 使用场景：</p>
<ul>
<li><a href="https://github.com/slightlyoff/BackgroundSync" target="_blank" rel="noopener noreferrer">后台同步</a>：启动一个service worker即使没有用户访问特定站点，也可以更新缓存</li>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Push_API" target="_blank" rel="noopener noreferrer">响应推送</a>：启动一个service worker向用户发送一条信息通知新的内容可用</li>
<li>后台数据同步</li>
<li>响应来自其它源的资源请求</li>
<li>集中接收计算成本高的数据更新，比如地理位置和陀螺仪信息，这样多个页面就可以利用同一组数据</li>
<li>在客户端进行 CoffeeScript，LESS，CJS/AMD等模块编译和依赖管理（用于开发目的）</li>
<li>后台服务钩子</li>
<li>自定义模板用于特定URL模式</li>
<li>性能增强，比如预取用户可能需要的资源，比如相册中的后面数张图片</li>
</ul>
</li>
<li>
<p>生命周期：注册、sw.js 文件下载、安装，激活。</p>
<ul>
<li>流程图<img src="./../images/service-work-lifrcycle.png" alt="" /></li>
</ul>
</li>
<li>
<p>安全性问题</p>
<ul>
<li>跨域请求支持：  SW 可以拦截它作用域内的所有请求，跨域资源也不例外，但是浏览器默认对跨域资源发起的是 no-cors 请求，得到的 response 是 opaque 的， 所以会导致我们无法判断跨域请求是否成功，以便进行缓存，因此我们需要修改 fetch 请求头部信息，添加 mode：’cors’ 标记。</li>
<li>no-cors：该模式允许来自 CDN 的脚本、其他域的图片和其他一些跨域资源，但是首先有个前提条件，就是请求的 method 只能是 HEAD 、GET 、POST 。此外，如果 ServiceWorkers 拦截了这些请求，它不能随意添加或者修改除这些之外 Header 属性。第三，JS 不能访问 Response 对象中的任何属性，这确保了跨域时 ServiceWorkers 的安全和隐私信息泄漏问题。</li>
<li>opaque：Response 对象中 type 属性的值 ， 在 ‘no-cors’ 模式下请求了跨域资源，依靠服务端来做限制。</li>
</ul>
</li>
</ul>
<h4 id="离线缓存"> 离线缓存</h4>
<ul>
<li>Service Workers  缓存：Service Workers  能够拦截 HTTP 请求，进入任何传入的 HTTP 请求，并决定想要如何响应。在你的 Service Worker 中，可以编写逻辑来决定想要缓存的资源，以及需要满足什么条件和资源需要缓存多久。
<ul>
<li>与 HTTP 缓存比较：Web 服务器可以使用 Expires 首部来通知 Web 客户端，它可以使用资源的当前副本，直到指定的“过期时间”。反过来，浏览器可以缓存此资源，并且只有在有效期满后才会再次检查新版本。使用 HTTP 缓存意味着你要依赖服务器来告诉你何时缓存资源和何时过期。</li>
</ul>
</li>
</ul>
<ul>
<li>
<p>**注册：**使用 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/ServiceWorkerContainer/register" target="_blank" rel="noopener noreferrer"><code>ServiceWorkerContainer.register()</code></a> 方法在该站点注册。其内容在 sw.js 文件中，可以在注册成功后执行。</p>
<div><pre><code><span>if</span><span>(</span><span>'serviceWorker'</span> <span>in</span> navigator<span>)</span> <span>{</span>
    navigator<span>.</span>serviceWorker<span>.</span><span>register</span><span>(</span><span>'/sw.js'</span><span>)</span><span>;</span>
<span>}</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
<li>
<p>**安装：**sw.js 监听 install 事件、<code>e.waitUntil()</code>等待 service worker 安装完成、Cache API 开启了一个缓存，并且将我们的应用所需要缓存的文件全部添加进去，当我们再次加载这些资源时，由请求 URL 确定的对应缓存就是可用的。</p>
<div><pre><code>self<span>.</span><span>addEventListener</span><span>(</span><span>'install'</span><span>,</span> <span>function</span><span>(</span><span>e</span><span>)</span> <span>{</span>
  console<span>.</span><span>log</span><span>(</span><span>'[Service Worker] Install'</span><span>)</span><span>;</span>
  e<span>.</span><span>waitUntil</span><span>(</span>
    caches<span>.</span><span>open</span><span>(</span>cacheName<span>)</span><span>.</span><span>then</span><span>(</span><span>function</span><span>(</span><span>cache</span><span>)</span> <span>{</span>
          console<span>.</span><span>log</span><span>(</span><span>'[Service Worker] Caching all: app shell and content'</span><span>)</span><span>;</span>
      <span>return</span> cache<span>.</span><span>addAll</span><span>(</span>contentToCache<span>)</span><span>;</span>
    <span>}</span><span>)</span>
  <span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div><ul>
<li>API 介绍：
<ul>
<li><code>e.waitUntil()</code>：等待 service worker 安装完成。<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/ExtendableEvent/waitUntil" target="_blank" rel="noopener noreferrer"><code>ExtendableEvent.waitUntil</code></a>方法告诉事件分发器该事件仍在进行。这个方法也可以用于检测进行的任务是否成功。在服务工作线程中，这个方法告诉浏览器事件一直进行，直至 promise 解决，浏览器不应该在事件中的异步操作完成之前终止服务工作线程。
<ul>
<li><code>caches</code> 是一个特殊的 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/CacheStorage" target="_blank" rel="noopener noreferrer"><code>CacheStorage</code></a> 对象，它能在 Service Worker 指定的范围内提供数据存储的能力。因为 Web Storage 的执行是同步的，在 Service Worker 中使用 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Web_Storage_API" target="_blank" rel="noopener noreferrer">Web Storage</a> 将不会有效果，所以我们使用 Cache API 作为替代。</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>
<p><strong>激活：</strong> 监听<code>activate</code> 事件。删除那些我们已经不需要的文件或者做一些清理工作。</p>
<div><pre><code>self<span>.</span><span>addEventListener</span><span>(</span><span>'activate'</span><span>,</span> <span>event</span> <span>=></span> <span>{</span>
  <span>const</span> currentCaches <span>=</span> <span>[</span><span>PRECACHE</span><span>]</span><span>;</span>
  <span>//遍历缓存,查找不存在于自定义缓存列表中的浏览器缓存，并清除</span>
  event<span>.</span><span>waitUntil</span><span>(</span>
    caches<span>.</span><span>keys</span><span>(</span><span>)</span><span>.</span><span>then</span><span>(</span><span>cacheNames</span> <span>=></span> <span>{</span><span>//查找缓存</span>
      <span>return</span> cacheNames<span>.</span><span>filter</span><span>(</span><span>cacheName</span> <span>=></span> <span>!</span>currentCaches<span>.</span><span>includes</span><span>(</span>cacheName<span>)</span><span>)</span><span>;</span>
    <span>}</span><span>)</span><span>.</span><span>then</span><span>(</span><span>cachesToDelete</span> <span>=></span> <span>{</span>
      <span>return</span> Promise<span>.</span><span>all</span><span>(</span>cachesToDelete<span>.</span><span>map</span><span>(</span><span>cacheToDelete</span> <span>=></span> <span>{</span>
        <span>return</span> caches<span>.</span><span>delete</span><span>(</span>cacheToDelete<span>)</span><span>;</span> <span>//清除缓存</span>
      <span>}</span><span>)</span><span>)</span><span>;</span>
    <span>}</span><span>)</span><span>.</span><span>then</span><span>(</span><span>(</span><span>)</span> <span>=></span> self<span>.</span>clients<span>.</span><span>claim</span><span>(</span><span>)</span><span>)</span>
  <span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br></div></div></li>
<li>
<p>**响应请求：**通过 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/FetchEvent" target="_blank" rel="noopener noreferrer"><code>FetchEvent</code></a> 事件去响应请求。通过使用 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/FetchEvent/respondWith" target="_blank" rel="noopener noreferrer"><code>FetchEvent.respondWith</code></a> 方法，你可以任意修改对于这些请求的响应。拦截页面请求，现在缓存中查找，未找到才去请求。</p>
<div><pre><code>self<span>.</span><span>addEventListener</span><span>(</span><span>'fetch'</span><span>,</span> <span>function</span><span>(</span><span>event</span><span>)</span> <span>{</span>
  console<span>.</span><span>log</span><span>(</span><span>'Handling fetch event for'</span><span>,</span> event<span>.</span>request<span>.</span>url<span>)</span><span>;</span>
 <span>//事件处理程序侦听 fetch 事件。当触发时返回一个promise给受控页面。</span>
  event<span>.</span><span>respondWith</span><span>(</span>
    <span>// promise在 Cache 对象中查询第一个匹配URL请求。如果没有发现匹配项，该代码将转而从网络获取响应。</span>
    caches<span>.</span><span>match</span><span>(</span>event<span>.</span>request<span>)</span><span>.</span><span>then</span><span>(</span><span>function</span><span>(</span><span>response</span><span>)</span> <span>{</span>
      <span>if</span> <span>(</span>response<span>)</span> <span>{</span>
        console<span>.</span><span>log</span><span>(</span><span>'Found response in cache:'</span><span>,</span> response<span>)</span><span>;</span>

        <span>return</span> response<span>;</span>
      <span>}</span>
      console<span>.</span><span>log</span><span>(</span><span>'No response found in cache. About to fetch from network...'</span><span>)</span><span>;</span>

      <span>return</span> <span>fetch</span><span>(</span>event<span>.</span>request<span>)</span><span>.</span><span>then</span><span>(</span><span>function</span><span>(</span><span>response</span><span>)</span> <span>{</span>
        console<span>.</span><span>log</span><span>(</span><span>'Response from network is:'</span><span>,</span> response<span>)</span><span>;</span>

        <span>return</span> response<span>;</span>
      <span>}</span><span>)</span><span>.</span><span>catch</span><span>(</span><span>function</span><span>(</span><span>error</span><span>)</span> <span>{</span>
        console<span>.</span><span>error</span><span>(</span><span>'Fetching failed:'</span><span>,</span> error<span>)</span><span>;</span>

        <span>throw</span> error<span>;</span>
      <span>}</span><span>)</span><span>;</span>
    <span>}</span><span>)</span>
  <span>)</span><span>;</span>
<span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br></div></div></li>
</ul>
<p>参考资料：</p>
<ul>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Service_Worker_API" target="_blank" rel="noopener noreferrer">Service Worker API MDN</a></li>
</ul>
<h4 id="消息推送"> 消息推送</h4>
<ul>
<li>
<p>在 Service Worker 使用 <a href="https://developer.mozilla.org/en-US/docs/Web/API/Push_API" target="_blank" rel="noopener noreferrer">推送 API</a>和<a href="https://developer.mozilla.org/en-US/docs/Web/API/Notifications_API" target="_blank" rel="noopener noreferrer">通知 API</a>实现。</p>
<ul>
<li>推送 API 可以用来从服务端推送新的内容而无需客户端介入，它是由应用的 Service Worker 来实现的；</li>
<li>通知功能则可以通过 Service Worker 来向用户展示一些新信息，或者至少提醒用户应用已经更新了某些功能。</li>
<li>这些工作是在浏览器外部实现的，所以即使应用被隐藏到后台甚至被关闭了，我们仍然能够推送更新或者通知给用户。</li>
</ul>
</li>
<li>
<p>通知实现：</p>
<ul>
<li>注册 Service Worker 成功后，为了能够显示通知，我们需要先请求用户的授权，一旦用户选择授权，这个授权结果对通知 API 和推送 API 两者都有效。</li>
<li>创建通知：<code>new Notification(notifTitle, options);</code></li>
</ul>
</li>
<li>
<p>消息推送实现：</p>
<ul>
<li>
<p>注册 Service Worker 成功后，立即开启该客户端的消息推送订阅功能</p>
<ul>
<li>
<p>使用 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/PushManager" target="_blank" rel="noopener noreferrer"><code>PushManager</code></a> 来订阅新用户。这个方法支持传递两个参数：</p>
<ul>
<li>第一个是 <code>userVisibleOnly: true</code>，意思是发送给用户的所有通知对他们都是可见的</li>
<li>第二个是 <code>applicationServerKey</code>，这个参数包含我们之前从服务端取得并转化的 VAPID key。</li>
</ul>
<div><pre><code><span>return</span> registration<span>.</span>pushManager<span>.</span><span>subscribe</span><span>(</span><span>{</span>
    userVisibleOnly<span>:</span> <span>true</span><span>,</span>
    applicationServerKey<span>:</span> convertedVapidKey
<span>}</span><span>)</span><span>;</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br></div></div></li>
</ul>
</li>
<li>
<p>为了方便之后的推送，为每个客户端简单生成一个标识</p>
</li>
<li>
<p>最后，将生成的客户端订阅信息存储在自己的服务器上：使用 fetch 将 subscription 以 JSON 的方式发送给服务器。</p>
</li>
</ul>
</li>
</ul>
<h4 id="参考资料"> 参考资料</h4>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/Progressive_web_apps" target="_blank" rel="noopener noreferrer">渐进式 Web 应用 MDN</a></p>
<p><a href="https://segmentfault.com/a/1190000012353473" target="_blank" rel="noopener noreferrer">PWA实现</a></p>
<p><a href="https://lavas-project.github.io/pwa-book/" target="_blank" rel="noopener noreferrer">《PWA 应用实战》</a></p>
<h3 id="前端渲染模式"> 前端渲染模式</h3>
<ul>
<li>前端渲染主要为从服务到浏览器，根据页面渲染方式划分。</li>
</ul>
<p><img src="./../images/front-end-render.png" alt="" /></p>
<h4 id="静态页面渲染-static-rendering"> 静态页面渲染（Static Rendering）</h4>
<ul>
<li>
<p>概念：静态渲染就是直接用已经成型的html文件进行渲染，将生成 HTML 页面的工作放到编译时，而不必在请求带来时动态完成。会有一些辅助的JS来增强页面交互；</p>
<p><img src="./../images/static-render.jpg" alt="" /></p>
</li>
<li>
<p>优点：</p>
<ul>
<li>性能参数都比较优异，TTFB 和 FP 和 FCP几乎相同；</li>
<li>适合CDN部署；</li>
<li>客户端与服务端压力都比较小；</li>
</ul>
</li>
<li>
<p>缺点：</p>
<ul>
<li>必须为每个可能的URL生成单独的HTML文件。 当无法提前预测这些URL的URL或具有大量唯一页面的网站时，这可能具有挑战性甚至不可行, 如果是纯手工开发，那开发效率相对也比较低。</li>
<li>只适用于偏静态内容：对于动态的、个性化的内容作用不大</li>
</ul>
</li>
</ul>
<h4 id="预渲染"> 预渲染</h4>
<ul>
<li>概念：
<ul>
<li>预渲染是通过启动一个无头浏览器(headless browser)，加载应用程序的路由并将结果保存到静态HTML文件中。</li>
<li>预渲染并不需要服务端支持，它只能生成在 templete 写好的 dom 结构,也就是只能生成静态的 html 文件。</li>
<li>预渲染必须经客户端渲染才真正可交互。</li>
<li>一般是每个路由对应一个静态的 html 文件，在服务器获取时就已经生成好了，而不需要 diff 再动态生成 dom。
<ul>
<li>补充知识：Headless Chrome 是 Chrome 浏览器的无界面形态，可以在不打开浏览器的前提下，使用所有 Chrome 支持的特性运行你的程序。相比于现代浏览器，Headless Chrome 更加方便测试 web 应用，获得网站的截图，爬虫抓取信息等。</li>
</ul>
</li>
</ul>
</li>
<li>应用场景：
<ul>
<li>针对特定路由的静态 HTML 文件。如：营销页面（例如<code>/</code>,<code>/about</code>,<code>/contact</code>等）。</li>
<li>配置骨架屏。当ajax获取到数据之后把骨架屏替换掉，可以减少白屏时间。</li>
</ul>
</li>
<li>不适用场景：
<ul>
<li>大量路由场景。 如果您的站点有成百上千个路由，则预渲染将非常缓慢。当然，每个更新只需要执行一次，但是可能要花一些时间。大多数人并没有最终获得数千个静态网页，而是以防万一。</li>
<li>动态内容。 如果您的渲染页面包含特定于用户查看其内容或其他动态源的内容，则应确保拥有可以显示的占位符组件，直到动态内容加载到客户端为止。否则可能有点怪异。</li>
</ul>
</li>
<li>与服务端渲染区别：
<ul>
<li>服务端渲染的过程为：服务端获取后端数据 -&gt; 解析执行JS -&gt; 构建HTML页面 -&gt; 输出完整的带数据 HTML 页面给浏览器。</li>
<li>预渲染：直接输出 HTML 结构的页面给浏览器，HTML 页面中只有页面结构或静态内容，没有后端数据内容和 不执行JS。</li>
</ul>
</li>
<li>常用插件： <a href="https://github.com/chrisvfritz/prerender-spa-plugin" target="_blank" rel="noopener noreferrer">prerender-spa-plugin</a></li>
<li>参考资料：
<ul>
<li><a href="https://tech.meituan.com/2018/11/15/first-contentful-paint-practice.html" target="_blank" rel="noopener noreferrer">构建时预渲染：网页首帧优化实践</a></li>
</ul>
</li>
</ul>
<h4 id="客户端渲染-csr"> 客户端渲染（CSR）</h4>
<ul>
<li>
<p>概念：Client-side rendering (CSR) ，一种纯在客户端(浏览器)利用JS操作Dom渲染页面的方式. 所有的生成逻辑, 数据获取, 模板 and 路由 都由浏览器而不是服务端来控制。</p>
<p><img src="./../images/csr.jpg" alt="" /></p>
<ul>
<li>
<p>客户端渲染是当前最流行的渲染模式，常以SPA单页面应用的方式存在；通常使用 VUE 开发的单页面应用一般为客户端渲染；</p>
</li>
<li>
<p>与后端的交互主要为API服务接口的数据调用，得到数据后在浏览器中通过 JS 进行模板编译、渲染、页面更新、路由等操作。</p>
</li>
<li>
<p>除了TTFB短，其他都会被延长，可以通过Http2的服务端推送与<code>&lt;link rel=preload&gt;</code>来提升FCP；</p>
</li>
<li>
<p>由于JS和CSS的大小会影响首屏的渲染，所以最好做好代码分割，只提供页面渲染必要的资源代码，应用懒加载的形式来提供其余的资源时非常有必要的；</p>
</li>
</ul>
</li>
<li>
<p>优点：</p>
<ul>
<li>允许在导航到不同页面时几乎立即在浏览器中更新网站，更新速度快。</li>
<li>但在开始时需要更多的初始下载和客户端上的额外渲染。 首次访问时网站速度较慢，但后续访问速度要快得多。</li>
<li>天然的浏览器分布式环境</li>
<li>组件复用度高，还可以通过懒加载等进行性能的进一步优化</li>
<li>除首屏外性能响应快速</li>
<li>WEB/MOBILE多端渲染</li>
<li>前后端分离开发</li>
</ul>
</li>
</ul>
<ul>
<li>缺点：
<ul>
<li>首屏性能差：由于页面渲染、三方包等逻辑都置于一个JS文件中，首屏加载会导致一定时间的白屏现象。</li>
<li>浏览器SEO性能差：由于现阶段大多搜索引擎采用的爬虫算法是直接抓取页面代码分析，而 SPA 应用只有一个入口文件而没实质内容，内容通过获取后端数据接口通过浏览器端 JS 解析渲染，而搜索引擎不能抓取异步解析的数据。</li>
</ul>
</li>
</ul>
<h4 id="服务端渲染-ssr"> 服务端渲染（SSR）</h4>
<ul>
<li>
<p>概念：Server-Side Rendering - 就是服务端渲染出HTML页面。</p>
<p><img src="./../images/ssr.jpg" alt="" /></p>
<ul>
<li>在不同页面之间导航需要下载新的HTML，首页会从缓存中获取已渲染页面，为查找到则服务端重新渲染。</li>
<li>服务器上渲染页面：后端不仅仅进行数据的获取处理操作，同时也负责页面的生成，最终传到客户端(浏览器)的是生成的包含数据的页面。</li>
<li>客户端所需要做的仅仅是html页面的展现和之后的DOM事件处理。</li>
<li>除了TTFB会延长（服务端需要去准备相应的页面数据），其他三个性能参数都比较客观；</li>
</ul>
</li>
<li>
<p>优点：</p>
<ul>
<li>SEO 友好：页面是在服务端渲染好的静态页面，便于爬虫抓取信息。</li>
<li>更好的性能数据，客户端压力更。</li>
<li>首屏加载快，跨浏览器兼容性良好，但代价是页间加载时间延长，也就是总体感知上的性能降低：每加载一个页面，都需要一个服务器请求往返的时间。</li>
</ul>
</li>
</ul>
<ul>
<li>缺点：
<ul>
<li>在不同页面之间导航都需要下载新的HTML内容导致页面切换无法渐进式加载，每个页面都需要重新渲染，页面切换时不能定义过渡动画(间隔有白屏，Chrome 在同域名页面跳转时，有一个<a href="https://developers.google.com/web/updates/2019/05/paint-holding" target="_blank" rel="noopener noreferrer">PLS优化</a>，即延迟到下一个页面FCP节点时开始下一个页面的渲染)；</li>
<li>服务器负载大：由于渲染任务都交由服务端进行，在高并发的情况下，对于服务端负载压力大，同时丧失了浏览器端作为一个天然分布式系统的优势。</li>
<li>复用性能差：因为返回的是整个页面，对于每个路由都要重新进行页面刷新，复用性能上不友好。</li>
<li>前后端耦合严重，前端开发依赖于后端，开发形式上不友好，属于前后端不分离模式。</li>
</ul>
</li>
</ul>
<h4 id="同构渲染"> 同构渲染</h4>
<ul>
<li>
<p>概念：同构是 SSR+CSR 的合体。首屏的html页面由服务端提供，然后加载 js，js 利用现有的 dom 树来接管渲染后页面的交互操作，跳转到新页面时就变成纯CSR渲染。</p>
<p><img src="./../images/rehydretion.jpg" alt="" /></p>
<ul>
<li>前后端共用一套 JS 代码，采用不同的构建方式，应用程序的大部分代码都可以在服务器和客户端上运行。</li>
<li>通过Node服务器进行页面的首屏生成，在服务器上渲染网站，缓存其内容，然后在客户端需要时更新渲染。</li>
<li>使用了 SSR，第一页加载很快；因为客户端可以仅使用已更改的部分重新渲染页面，所以页面之间的导航也是平滑的。</li>
<li>将同一个组件渲染为服务器端的 HTML 字符串和渲染数据，将它们直接发送到浏览器，最后将这些静态标记&quot;激活&quot;为客户端上完全可交互的应用程序。激活后在客户端交给客户端脚本处理。
<ul>
<li>服务端渲染后的内容包括 的 HTML 字符串和渲染数据：<img src="./../images/ssr2.png" alt="" /></li>
</ul>
</li>
<li><code>bundle.js</code>仍然是全量的 CSR 代码，这些代码执行完毕页面才真正可交互。因此，这种模式下，FP（First Paint）虽然有所提升，但 TTI（Time To Interactive）可能会变慢，因为在客户端二次渲染完成之前，页面无法响应用户输入（被 JS 代码执行阻塞了）</li>
<li>是一种比较有技术含量的渲染方式，当下比较流行的NextJs(React), NuxtJs(Vue)就是这种渲染技术的成熟框架。</li>
</ul>
</li>
<li>
<p>优点：</p>
<ul>
<li>利于SEO，由于搜索引擎爬虫抓取工具可以直接查看完全渲染的页面。</li>
<li>结合了SSR与CSR的特点，首屏之后的页面交互可实现渐进式加载，可控性高。</li>
<li>更快的内容到达时间 (time-to-content)，特别是对于缓慢的网络情况或运行缓慢的设备。
<ul>
<li>无需等待所有的 JavaScript 都完成下载并执行，才显示服务器渲染的标记，所以你的用户将会更快速地看到完整渲染的页面。</li>
<li>通常可以产生更好的用户体验，并且对于那些「内容到达时间(time-to-content) 与转化率直接相关」的应用程序而言，服务器端渲染 (SSR) 至关重要。</li>
</ul>
</li>
</ul>
</li>
<li>
<p>缺点：</p>
<ul>
<li>开发条件所限。浏览器特定的代码，只能在某些生命周期钩子函数 (lifecycle hook) 中使用；一些外部扩展库 (external library) 可能需要特殊处理，才能在服务器渲染应用程序中运行。</li>
<li>涉及构建设置和部署的更多要求。与可以部署在任何静态文件服务器上的完全静态单页面应用程序 (SPA) 不同，服务器渲染应用程序，需要处于 Node.js server 运行环境。</li>
<li>更多的服务器端负载。在 Node.js 中渲染完整的应用程序，显然会比仅仅提供静态文件的 server 更加大量占用 CPU 资源 (CPU-intensive - CPU 密集)，因此如果你预料在高流量环境 (high traffic) 下使用，请准备相应的服务器负载，并明智地采用缓存策略。</li>
<li>技术要求更高（包含代码处理），同时对服务器和客户端都有性能要求；</li>
<li>通过服务端 <code>html</code> 生成方法 <code>renderToString</code> 是同步的，这会阻塞 Node 服务主线程；使用 <code>renderToNodeStream</code> 是异步的，服务端压力相对而言就没那么大。</li>
</ul>
</li>
</ul>
<h4 id="trisomorphic-rendering"> Trisomorphic Rendering</h4>
<ul>
<li>
<p>概念：把Service Worker也考虑进来的话，还有一种<em>涉及三方的渲染模式</em>：</p>
<p><img src="./../images/Trisomorphic-Rendering.png" alt="" /></p>
<div><pre><code><span>SSR</span> <span>+</span> <span>CSR</span> <span>+</span> ServiceWorker rendering <span>=</span> Trisomorphic Rendering
</code></pre>
<div><span>1</span><br></div></div><ul>
<li>首先通过流式 SSR 渲染初始页面，接着由 Service Worker 根据路由规则，借助 SSR 渲染出目标 HTML 页面。</li>
</ul>
</li>
<li>
<p>优点：</p>
<ul>
<li>跨三方共享模板渲染和路由控制逻</li>
</ul>
</li>
</ul>
<h4 id="比较"> 比较</h4>
<p><img src="./../images/应用比较.png" alt="" /></p>
<p><strong>参考资料</strong></p>
<p><a href="https://developers.google.com/web/updates/2019/02/rendering-on-the-web#:~:text=%20Rendering%20on%20the%20Web%20%201%20Terminology.,Paint%2C%20First%20Contentful%20Paint%20and%20Time...%20More%20" target="_blank" rel="noopener noreferrer">Rendering on the Web</a></p>
<h2 id="vue-ssr-2"> VUE SSR</h2>
<p>基于官文demo：<a href="https://github.com/vuejs/vue-hackernews-2.0/" target="_blank" rel="noopener noreferrer">HackerNews Demo</a> 进行分析，项目结构等基础参考 <a href="https://ssr.vuejs.org/zh/#%E4%BB%80%E4%B9%88%E6%98%AF%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%AB%AF%E6%B8%B2%E6%9F%93-ssr-%EF%BC%9F" target="_blank" rel="noopener noreferrer">vue-ssr 文档</a>。</p>
<h3 id="vue-ssr-流程"> VUE SSR 流程</h3>
<ul>
<li>开发环境下执行流程</li>
</ul>
<p><img src="./../flow-graph/ssr过程.png" alt="" /></p>
<ul>
<li>SSR 流程主要分为：
<ul>
<li>服务端渲染</li>
<li>客户端渲染</li>
</ul>
</li>
<li>服务端渲染
<ul>
<li>渲染时机：
<ul>
<li>通过 express 服务渲染：浏览器输入 url 直接定向到某个页面，express 执行 <code>entry-server.js</code> 动态渲染 Html</li>
<li>静态化页面时，通过执行 <code>entry-client.js</code> 渲染出 html 页面</li>
</ul>
</li>
<li>总体流程：
<ul>
<li>浏览器输入 url -&gt;  express 服务接受请求 -&gt; 服务端获取数据 -&gt; 数据注入 store -&gt; SSR Renderer 渲染HTML页面 -&gt; 返回浏览器</li>
</ul>
</li>
</ul>
</li>
<li>客户端渲染
<ul>
<li>渲染时机： 通过页面中链接切换路由</li>
<li>总体流程：首次渲染时由服务端渲染，客户端激活后通过路由导航的所有页面又客户端渲染。
<ul>
<li>页面内点击链接 -&gt; 客户端获取数据 -&gt; 渲染页面</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="ssr-注意事项"> SSR 注意事项</h3>
<ul>
<li>服务端渲染时，将数据进行响应式的过程在服务器上是多余的，所以默认情况下禁用响应式数据。避免将「数据」转换为「响应式对象」的性能开销。</li>
<li>所有的生命周期钩子函数中，只有 <code>beforeCreate</code> 和 <code>created</code> 会在服务器端渲染 (SSR) 过程中被调用。任何其他生命周期钩子函数中的代码（例如 <code>beforeMount</code> 或 <code>mounted</code>），只会在客户端执行。
<ul>
<li>为什么只有<code>beforeCreate</code> 和 <code>created</code> 会在服务器端渲染 (SSR) 过程中被调用？
<ul>
<li>服务端渲染过程中不需要响应式数据，没有动态更新过程；</li>
<li>服务端渲染在VUE初始化时仅仅 new VUE 实例，不需要挂载（$mount）实例，因此不会执行 $mount 之后流程；</li>
<li>客户端动态请求的数据/内容 需要在 mounted 函数中执行（如：动态获取用户数据）</li>
</ul>
</li>
</ul>
</li>
<li>对于仅浏览器可用的 API，通常方式是，在「纯客户端 (client-only)」的生命周期钩子函数 <code>beforeMounte</code>、<code>mounted</code> 中惰性访问 (lazily access) 它们。</li>
<li>应该避免在 <code>beforeCreate</code> 和 <code>created</code> 生命周期时产生全局副作用的代码。
<ul>
<li>例如在其中使用 <code>setInterval</code> 设置 time，由于在 SSR 期间并不会调用销毁钩子函数，所以 timer 将永远保留下来</li>
</ul>
</li>
<li>服务端和浏览器通用代码不可接受特定平台的 API：
<ul>
<li>如果通用代码中，直接使用了仅浏览器可用的全局变量 <code>window</code> 或 <code>document</code>，则会在 Node.js 中执行时抛出错误；</li>
<li>通用代码使用 Node.js 平台特定 API，如<code>global</code>，则会在浏览器中执行报错；</li>
</ul>
</li>
<li>大多数自定义指令直接操作 DOM，因此会在服务器端渲染 (SSR) 过程中导致错误；</li>
<li>静态函数 <code>asyncData</code>无法访问 <code>this</code>，因为此函数会在组件实例化之前调用；</li>
<li>服务端渲染时不需要响应式数据，默认禁用响应式数据，可以避免将「数据」转换为「响应式对象」的性能开销；</li>
<li>模板中的<code>&lt;!--vue-ssr-outlet--&gt;</code> 注释 部分是 renderer 渲染 HTML 字符串插入的位置；</li>
</ul>
<h3 id="参考资料-2"> 参考资料</h3>
<p><a href="https://ssr.vuejs.org/zh/#%E4%BB%80%E4%B9%88%E6%98%AF%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%AB%AF%E6%B8%B2%E6%9F%93-ssr-%EF%BC%9F" target="_blank" rel="noopener noreferrer">vue-ssr 文档</a></p>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-29T22:43:23.000Z</published>
  </entry>
  <entry>
    <title type="html">VUEX</title>
    <id>https://sewar-x.github.io/vuex/</id>
    <link href="https://sewar-x.github.io/vuex/"/>
    <updated>2021-07-12T03:10:17.000Z</updated>
    <content type="html"><![CDATA[<h2 id="vuex"> VUEX</h2>
<h3 id="vuex-基础"> VUEX 基础</h3>
<ul>
<li>
<p>Vuex  应用开发的状态管理模式。采用集中式存储管理应用的所有组件的状态。</p>
</li>
<li>
<p>Vuex 和单纯的全局对象有以下两点不同：</p>
<ul>
<li><strong>Vuex 的状态存储是响应式的</strong>。当 Vue 组件从 store 中读取状态的时候，若 store 中的状态发生变化，那么相应的组件也会相应地得到高效更新。</li>
<li><strong>不能直接改变 store 中的状态。<strong>改变 store 中的状态的唯一途径就是显式地</strong>提交 (commit) mutation</strong>。这样使得我们可以方便地跟踪每一个状态的变化，从而让我们能够实现一些工具帮助我们更好地了解我们的应用。</li>
</ul>
</li>
<li>
<p>Vuex解决了什么问题？</p>
<ul>
<li>多个组件依赖于同一状态时，多层嵌套组件和兄弟组件状态传参问题。</li>
<li>来自不同组件的行为需要变更同一状态问题。以往采用父子组件直接引用或者通过事件来变更和同步状态的多份拷贝。以上的这些模式非常脆弱，通常会导致无法维护的代码。</li>
</ul>
</li>
<li>
<p>什么时候使用 Vuex?</p>
<ul>
<li>多个组件依赖于同一状态时。</li>
<li>来自不同组件的行为需要变更同一状态。</li>
</ul>
</li>
<li>
<p>核心属性：</p>
<ul>
<li>
<p><strong>State 状态：</strong> 存储状态数据，状态存储是响应式的。</p>
<ul>
<li>获取 state 数据: <code>this.$store.state</code> 获取， store 实例中读取状态最简单的方法就是在<a href="https://cn.vuejs.org/guide/computed.html" target="_blank" rel="noopener noreferrer">计算属性 (opens new window)</a>中返回某个状态 。</li>
<li>获取多个状态：<code>mapState</code> 辅助函数。<code>mapState</code> 返回的是一个对象，使用对象展开运算符<code>...</code> 将对象混入到外部对象中。</li>
</ul>
</li>
<li>
<p>**Getter：**用于获取 <code>state</code>。</p>
<ul>
<li>参数：Getter 接受 state 作为其第一个参数，其他 getter 作为第二个参数。</li>
<li>返回值：返回值会根据它的依赖被缓存起来，且只有当它的依赖值发生了改变才会被重新计算。</li>
<li>获取单个 getters：  <code>this.$store.getters</code> 对象。</li>
<li>获取多个 getters：<code>mapGetters</code> 辅助函数，返回多个 getters 对象。</li>
</ul>
</li>
<li>
<p>**Mutation：**提交 mutation 是更改状态的唯一方法。</p>
<ul>
<li>
<p>规则：</p>
<ul>
<li><strong>mutation 必须是同步函数</strong>（在回调函数中进行的状态的改变都是不可追踪）</li>
<li>使用常量替代 mutation 事件类型</li>
<li>在组件中使用 <code>this.$store.commit('xxx')</code> 提交 mutation，或者使用 <code>mapMutations</code> 辅助函。</li>
</ul>
</li>
<li>
<p>定义和提交：</p>
<div><pre><code><span>//定义：第一个参数 state，第二个参数提交数据</span>
mutations<span>:</span> <span>{</span>
  <span>increment</span> <span>(</span><span>state<span>,</span> payload</span><span>)</span> <span>{</span>
    state<span>.</span>count <span>+=</span> payload<span>.</span>amount
  <span>}</span>
<span>}</span>
<span>//提交：第一个参数 mutation 事件类型，第二个参数为提交数据</span>
store<span>.</span><span>commit</span><span>(</span><span>'increment'</span><span>,</span> <span>{</span>
  amount<span>:</span> <span>10</span>
<span>}</span><span>)</span>
<span>//对象风格提交</span>
store<span>.</span><span>commit</span><span>(</span><span>{</span>
  type<span>:</span> <span>'increment'</span><span>,</span>
  amount<span>:</span> <span>10</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div></li>
<li>
<p>实现: 从 _mutations 中取出对应的 mutation，循环执行其中的每一个 mutation。</p>
<div><pre><code><span>commit</span> <span>(</span><span>type<span>,</span> payload<span>,</span> _options</span><span>)</span> <span>{</span>
    <span>//_mutations 为用户定义的 mutation 的数组，type 为 namespace + path 拼接字符串</span>
    <span>const</span> entry <span>=</span> <span>this</span><span>.</span>_mutations<span>[</span>type<span>]</span><span>;</span> 
    entry<span>.</span><span>forEach</span><span>(</span><span>function</span> <span>commitIterator</span> <span>(</span><span>handler</span><span>)</span> <span>{</span>
        <span>handler</span><span>(</span>payload<span>)</span><span>;</span>    
    <span>}</span><span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
</ul>
</li>
<li>
<p><strong>Action：<strong>一般用于</strong>调用异步 API</strong> 和<strong>分发多重 mutation</strong></p>
<ul>
<li>
<p>Action 提交的是 mutation，而不是直接变更状态。</p>
</li>
<li>
<p>Action 可以包含任意异步操作。</p>
</li>
<li>
<p>使用：使用 <code>this.$store.dispatch('xxx')</code> 分发 action，或者使用 <code>mapActions</code> 辅助函数将组件的 methods 映射为 <code>store.dispatch</code> 调用（需要先在根节点注入 <code>store</code>）</p>
</li>
<li>
<p><code>store.dispatch</code> 可以处理被触发的 action 的处理函数返回的 Promise，并且 <code>store.dispatch</code> 仍旧返回 Promise，然后 then 提交 commit 修改 state。</p>
</li>
<li>
<p>多次提交同一个action： mapActions辅助函数。</p>
</li>
<li>
<div><pre><code><span>//定义</span>
actions<span>:</span> <span>{</span>
  <span>checkout</span> <span>(</span><span><span>{</span> commit<span>,</span> state <span>}</span><span>,</span> products</span><span>)</span> <span>{</span>
    <span>// 把当前购物车的物品备份起来</span>
    <span>const</span> savedCartItems <span>=</span> <span>[</span><span>...</span>state<span>.</span>cart<span>.</span>added<span>]</span>
    <span>// 发出结账请求，然后乐观地清空购物车</span>
    <span>commit</span><span>(</span>types<span>.</span><span>CHECKOUT_REQUEST</span><span>)</span>
    <span>// 购物 API 接受一个成功回调和一个失败回调</span>
    shop<span>.</span><span>buyProducts</span><span>(</span>
      products<span>,</span>
      <span>// 成功操作</span>
      <span>(</span><span>)</span> <span>=></span> <span>commit</span><span>(</span>types<span>.</span><span>CHECKOUT_SUCCESS</span><span>)</span><span>,</span>
      <span>// 失败操作</span>
      <span>(</span><span>)</span> <span>=></span> <span>commit</span><span>(</span>types<span>.</span><span>CHECKOUT_FAILURE</span><span>,</span> savedCartItems<span>)</span>
    <span>)</span>
  <span>}</span>
<span>}</span>
<span>//使用 promise</span>
store<span>.</span><span>dispatch</span><span>(</span><span>'checkout'</span><span>)</span><span>.</span><span>then</span><span>(</span><span>(</span><span>res</span><span>)</span> <span>=></span> <span>{</span>
   <span>commit</span><span>(</span><span>'checkout'</span><span>,</span> res<span>.</span>data<span>)</span>
<span>}</span><span>)</span>
<span>// 使用 async await </span>
 actions<span>:</span> <span>{</span>
  <span>async</span> <span>actionA</span> <span>(</span><span><span>{</span> commit <span>}</span></span><span>)</span> <span>{</span>
    <span>commit</span><span>(</span><span>'checkout'</span><span>,</span> <span>await</span> <span>getData</span><span>(</span><span>)</span><span>)</span>
  <span>}</span>
 <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br></div></div></li>
<li>
<p>实现：取出 _actions 中的所有对应 action，将其执行，如果有多个则用 Promise.all 进行包装。</p>
<div><pre><code><span>dispatch</span> <span>(</span><span>type<span>,</span> payload</span><span>)</span> <span>{</span>
     <span>//_actions 为用户定义的 action 的数组，type 为 namespace + path 拼接字符串</span>
    <span>const</span> entry <span>=</span> <span>this</span><span>.</span>_actions<span>[</span>type<span>]</span><span>;</span>
    <span>return</span> entry<span>.</span>length <span>></span> <span>1</span>
    <span>?</span> Promise<span>.</span><span>all</span><span>(</span>entry<span>.</span><span>map</span><span>(</span><span>handler</span> <span>=></span> <span>handler</span><span>(</span>payload<span>)</span><span>)</span><span>)</span>
    <span>:</span> entry<span>[</span><span>0</span><span>]</span><span>(</span>payload<span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
</ul>
</li>
<li>
<p><strong>Module：</strong></p>
<ul>
<li>产生：由于使用单一状态树，应用的所有状态会集中到一个比较大的对象，当应用变得非常复杂时，<code>store</code> 对象就有可能变得相当臃肿。</li>
<li>作用：将 store 分割成<strong>模块（module）</strong>。每个模块拥有自己的 state、mutation、action、getter、嵌套子模块。</li>
<li>数据结构：模块的设计就是一个树型结构，<code>store</code> 本身可以理解为一个 <code>root module</code>，它下面的 <code>modules</code> 就是子模块</li>
<li>命名空间：
<ul>
<li>全局命名空间：默认情况为全局命名空间，多个模块能够对同一 <code>mutation</code> 或 <code>action</code> 作出响应。</li>
<li>局部命名空间：<code>namespaced: true</code> 的方式使其成为带命名空间的模块，所有 <code>getter</code>、<code>action</code> 及 <code>mutation</code> 都会自动根据模块注册的路径调整命名。</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>
<p>规则：</p>
<ul>
<li>应用层级的状态应该集中到单个 store 对象中。</li>
<li>提交 <strong>mutation</strong> 是更改状态的唯一方法，并且这个过程是同步的。</li>
<li>异步逻辑都应该封装到 <strong>action</strong> 里面。</li>
</ul>
</li>
<li>
<p>流程：</p>
<ul>
<li>
<p>页面组件 dispatch 分发 action 获取数据；</p>
</li>
<li>
<p>Action 中异步获取数据，获取后 commit 同步提交 mutation;</p>
</li>
<li>
<p>mutation 同步修改 state 状态，state 通过 getters 通知变化；</p>
</li>
<li>
<p>在组件的计算属性中，通过 getters 来动态获取 state 中的值。</p>
<p><img src="./../flow-graph/vuex流程.png" alt="" /></p>
</li>
<li>
<p>Vuex 缺点：</p>
<ul>
<li>vuex中存储的数据不能持久化。vuex 的 store 中的数据是保存在运行内存中的，当页面刷新后，页面会重新加载 vue 实例，vuex 里面的数据就会被重新赋值，这样就会出现页面刷新vuex中的数据丢失的问题。需要监听处理来维持vuex存储的数据状态持久化。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="初始化"> 初始化</h3>
<ul>
<li>
<p>初始化过程：</p>
<ul>
<li>使用 Vue 插件机制状态 Vuex；</li>
<li>vuex 的 <code>install</code> 中 使用 <code>Vue.mixin</code>  全局混入 <code>beforeCreate</code> 钩子；</li>
<li><code>new Vue()</code> 创建 Vue 实例时在 init() 方法中调用  <code>beforeCreate</code> 钩子；</li>
<li><code>beforeCreate</code> 钩子中执行 <code>vuexInit()</code> ：把 <code>options.store</code> （<code>Store</code> 实例）保存在所有组件的 <code>this.$store</code> 中。</li>
</ul>
</li>
<li>
<p><img src="./../flow-graph/vuex初始化过程.jpg" alt="" /></p>
</li>
</ul>
<h3 id="实例化"> 实例化</h3>
<ul>
<li>
<p>实例化过程分为三个阶段</p>
<ul>
<li>
<p><code>new ModuleCollection(options)</code> 建立模块树：</p>
<ul>
<li>创建一个子模块；</li>
<li>模块 <code>path</code>中包含模块父子关系，遍历模块 <code>path</code> ，通过每个子模块通过路径找到它的父模块，然后通过父模块的 <code>addChild</code> 方法建立父子关系</li>
<li>递归执行这样的过程，最终就建立一颗完整的模块树</li>
</ul>
</li>
<li>
<p><code>installModule</code> 安装模块：</p>
<ul>
<li>完成模块下的 state、getters、actions、mutations 的初始化工作</li>
<li>并且通过递归遍历的方式，就完成所有子模块的安装工作</li>
</ul>
</li>
<li>
<p><code>resetStoreVM</code> 建立 <code>getters</code> 和 <code>state</code> 的联系: 使 getter 缓存 state。</p>
<ul>
<li>
<p>**vuex 存储响应式原理：**创建 vue 实例，将 state 作为 vue 实例的 data 数据， getters 作为 computed 数据，在 vue 实例化时,会进行 state 和 getter 依赖收集，在数据修改时候触发更新。</p>
</li>
<li>
<p>从设计上 <code>getters</code> 的获取就依赖了 <code>state</code> ，并且希望它的依赖能被缓存起来，且只有当它的依赖值发生了改变才会被重新计算。</p>
</li>
<li>
<p>因此这里利用了 Vue 中 <code>computed</code> 计算属性来实现 getters 缓存 state：</p>
<div><pre><code><span>forEachValue</span><span>(</span>wrappedGetters<span>,</span> <span>(</span><span>fn<span>,</span> key</span><span>)</span> <span>=></span> <span>{</span>
    <span>// use computed to leverage its lazy-caching mechanism</span>
    computed<span>[</span>key<span>]</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>fn</span><span>(</span>store<span>)</span>
    Object<span>.</span><span>defineProperty</span><span>(</span>store<span>.</span>getters<span>,</span> key<span>,</span> <span>{</span>
      <span>get</span><span>:</span> <span>(</span><span>)</span> <span>=></span> store<span>.</span>_vm<span>[</span>key<span>]</span><span>,</span>
      enumerable<span>:</span> <span>true</span> <span>// for local getters</span>
    <span>}</span><span>)</span>
  <span>}</span><span>)</span>

  <span>// use a Vue instance to store the state tree</span>
  <span>// suppress warnings just in case the user has added</span>
  <span>// some funky global mixins</span>
  <span>const</span> silent <span>=</span> Vue<span>.</span>config<span>.</span>silent
  Vue<span>.</span>config<span>.</span>silent <span>=</span> <span>true</span>
  store<span>.</span>_vm <span>=</span> <span>new</span> <span>Vue</span><span>(</span><span>{</span>
    data<span>:</span> <span>{</span>
      $$state<span>:</span> state
    <span>}</span><span>,</span>
    computed
  <span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
</ul>
<p><img src="./../flow-graph/vuex实例化过程.png" alt="" /></p>
<h3 id="相关问题"> 相关问题</h3>
<p><strong>基础题：</strong></p>
<blockquote>
<p>Vuex中状态是对象时，使用时要注意什么？</p>
</blockquote>
<ul>
<li>因为对象是引用类型，复制后改变属性还是会影响原始数据，这样会改变state里面的状态，是不允许，所以先用深度克隆复制对象，再修改。</li>
</ul>
<blockquote>
<p>Vuex中要从state派生一些状态出来，且多个组件使用它，该怎么做？</p>
</blockquote>
<ul>
<li>
<p>使用getter属性，相当Vue中的计算属性computed，只有原状态改变派生状态才会改变。</p>
</li>
<li>
<p>getter接收两个参数，第一个是state，第二个是getters(可以用来访问其他getter)。</p>
</li>
<li>
<p>然后在组件中可以用计算属性computed通过<code>this.$store.getters.total</code>这样来访问这些派生转态。</p>
</li>
</ul>
<blockquote>
<p>vuex中直接修改 state 与 commit 来修改 state 的区别?</p>
</blockquote>
<ul>
<li>可以通过 <code>this.$store.state.aaa = xxx</code> 进行 state 值的修改;</li>
<li>当<code>vuex</code>的模式改成严格模式下，在通过直接修改<code>state</code>的方式修改状态的时候，控制台会报错；</li>
<li>使用commit提交状态修改是为了配合开发者工具使用，使得状态更好的被最终，保存状态快照，实现时间漫游／回滚之类的操作;</li>
</ul>
<blockquote>
<p>在模块中，getter和mutation接收的第一个参数state，是全局的还是模块的?</p>
</blockquote>
<ul>
<li>
<p>对于模块内部的 mutation 和 getter，接收的第一个参数是<strong>模块的局部状态对象</strong>。</p>
<p>参考： <a href="https://vuex.vuejs.org/zh/guide/modules.html#%E6%A8%A1%E5%9D%97%E7%9A%84%E5%B1%80%E9%83%A8%E7%8A%B6%E6%80%81" target="_blank" rel="noopener noreferrer">模块的局部状态对象</a></p>
</li>
</ul>
<blockquote>
<p>在模块中，getter和mutation和action中怎么访问全局的state和getter？</p>
</blockquote>
<ul>
<li>
<p>在getter中可以通过第三个参数rootState访问到全局的state,可以通过第四个参数rootGetters访问到全局的getter。</p>
</li>
<li>
<p>在mutation中不可以访问全局的satat和getter，只能访问到局部的state。</p>
</li>
<li>
<p>在action中第一个参数context中的<code>context.rootState</code>访问到全局的state，<code>context.rootGetters</code>访问到全局的getter。</p>
</li>
</ul>
<p><strong>应用题：</strong></p>
<blockquote>
<p>vuex 数据表单可以使用 v-model 吗？</p>
</blockquote>
<ul>
<li>
<p>不能。</p>
</li>
<li>
<p>当在严格模式中使用 Vuex 时，在属于 Vuex 的 state 上使用 <code>v-model</code> 会比较棘手：</p>
<div><pre><code><span>&lt;</span>input v<span>-</span>model<span>=</span><span>"obj.message"</span><span>></span>
</code></pre>
<div><span>1</span><br></div></div><p>​		假设这里的 <code>obj</code> 是在计算属性中返回的一个属于 Vuex store 的对象，在用户输入时，<code>v-model</code> 会试图直接修改 <code>obj.message</code>。在严格模式中，由于这个修改不是在 mutation 函数中执行的, 这里会抛出一个错误。</p>
</li>
<li>
<p>解决方案一：通过computed计算属性来转换，computed 数据 get 方法返回 state，set 方法提交 mutation</p>
<div><pre><code><span>&lt;</span>input v<span>-</span>model<span>=</span><span>"message"</span><span>></span>
<span>// ...</span>
computed<span>:</span> <span>{</span>
    message<span>:</span> <span>{</span>
        <span>get</span> <span>(</span><span>)</span> <span>{</span>
            <span>return</span> <span>this</span><span>.</span>$store<span>.</span>state<span>.</span>message
        <span>}</span><span>,</span>
        <span>set</span> <span>(</span>value<span>)</span> <span>{</span>
            <span>this</span><span>.</span>$store<span>.</span><span>commit</span><span>(</span><span>'updateMessage'</span><span>,</span> value<span>)</span>
        <span>}</span>
    <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div></li>
<li>
<p>解决方案二：用“Vuex 的思维”去解决这个问题的方法是：给 <code>&lt;input&gt;</code> 中绑定 value，然后侦听 <code>input</code> 或者 <code>change</code> 事件，在事件回调中调用一个方法:</p>
<div><pre><code><span>&lt;</span>input <span>:</span>value<span>=</span><span>"message"</span> @input<span>=</span><span>"updateMessage"</span><span>></span>
<span>// ...</span>
computed<span>:</span> <span>{</span>
  <span>...</span><span>mapState</span><span>(</span><span>{</span>
    <span>message</span><span>:</span> <span>state</span> <span>=></span> state<span>.</span>obj<span>.</span>message
  <span>}</span><span>)</span>
<span>}</span><span>,</span>
methods<span>:</span> <span>{</span>
  <span>updateMessage</span> <span>(</span><span>e</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>$store<span>.</span><span>commit</span><span>(</span><span>'updateMessage'</span><span>,</span> e<span>.</span>target<span>.</span>value<span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div></li>
</ul>
<blockquote>
<p>vue 中 ajax 请求代码应该写在组件的 methods 中还是 vuex 的 actions 中？</p>
</blockquote>
<ul>
<li>
<p>如果请求来的数据不需要其他组件公用，仅仅在请求的组件内使用，就不需要放入vuex 的state里。</p>
</li>
<li>
<p>如果请求的数据被其他地方复用，则将请求放入action里。方便复用，并包装成promise返回，在调用处用async await处理返回的数据。</p>
</li>
</ul>
<blockquote>
<p>Vuex中有两个action，分别是actionA和actionB，其内都是异步操作，在actionB要提交actionA，需在actionA处理结束再处理其它操作，怎么实现？</p>
</blockquote>
<p>利用 async / await:</p>
<ul>
<li>
<div><pre><code>actions<span>:</span> <span>{</span>
 <span>async</span> <span>actionA</span> <span>(</span><span><span>{</span> commit <span>}</span></span><span>)</span> <span>{</span>
   <span>commit</span><span>(</span><span>'gotData'</span><span>,</span> <span>await</span> <span>getData</span><span>(</span><span>)</span><span>)</span>
 <span>}</span><span>,</span>
 <span>async</span> <span>actionB</span> <span>(</span><span><span>{</span> dispatch<span>,</span> commit <span>}</span></span><span>)</span> <span>{</span>
   <span>await</span> <span>dispatch</span><span>(</span><span>'actionA'</span><span>)</span> <span>// 等待 actionA 完成</span>
   <span>commit</span><span>(</span><span>'gotOtherData'</span><span>,</span> <span>await</span> <span>getOtherData</span><span>(</span><span>)</span><span>)</span>
 <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div></li>
</ul>
<h3 id="应用"> 应用</h3>
<h4 id="vuex和localstorage-比较"> Vuex和localStorage 比较</h4>
<p>共同点：</p>
<ul>
<li>用于存储数据</li>
<li>用于组件间 数据通信</li>
</ul>
<p>区别：</p>
<ul>
<li>
<p>存储位置不同：</p>
<ul>
<li>Vuex 存储在内存，</li>
<li>localstorage（本地存储）则以文件的方式存储在本地(浏览器),永久保存；</li>
</ul>
</li>
<li>
<p>存储空间来：</p>
<ul>
<li>Vuex取决于可用内存和浏览器的限制</li>
<li>localstorage 都有个默认的大小（至少5MB，由浏览器决定），超出大小则需要用户同意增加空间；</li>
</ul>
</li>
<li>
<p>共享：</p>
<ul>
<li>Vuex无法跨标签页、跨物理页面共享</li>
<li>localstorage 则可以在同一域名底下共享</li>
</ul>
</li>
<li>
<p>独有能力：</p>
<ul>
<li>vuex的状态存储是响应式的：当Vue组件从store中读取状态的时候，若store中的状态发生变化，那么相应的组件也会得到更新</li>
</ul>
</li>
<li>
<p>应用：</p>
<ul>
<li>当需要响应式共享数据时， 需要使用使用Vuex来实现
<ul>
<li>vuex与localStorage结合，实现组件间通信，同时刷新页面数据不丢失</li>
</ul>
</li>
<li>当需求是刷新页面仍然可以获取到数据，或者是同一域名下不同项目共享数据，需要使用 localStorage</li>
</ul>
</li>
</ul>
<h4 id="vuex数据持久化"> Vuex数据持久化</h4>
<blockquote>
<p>Vuex 实现数据持久化？ vuex 如何解决页面刷新后数据丢失的问题？</p>
</blockquote>
<ul>
<li>
<p>方案1：<code>Vuex + sessionStorage/localStorage</code></p>
<ul>
<li>
<p>实现：</p>
<ul>
<li>封装 Storage 对象（封装目的：sessionStorage/localStorage 保存的是字符串，避免每次获取 JSON.parse 和存入都使用 JSON.stringify 转换）</li>
<li>Vuex 中 state 读取 Storage 中数据， mutation 提交数据更改时候存入 Storage。</li>
</ul>
<div><pre><code><span>var</span> Storage <span>=</span> <span>{</span>
  <span>// ==================sessionStorage设置缓存================</span>
  <span>// 设置缓存</span>
  <span>sessionSet</span><span>:</span> <span>function</span> <span>(</span><span>name<span>,</span> data<span>,</span> expires</span><span>)</span> <span>{</span>
    sessionStorage<span>.</span><span>removeItem</span><span>(</span>name<span>)</span>
    sessionStorage<span>.</span><span>setItem</span><span>(</span>name<span>,</span> <span>JSON</span><span>.</span><span>stringify</span><span>(</span>data<span>)</span><span>)</span>
    <span>if</span> <span>(</span>expires <span>&amp;&amp;</span> expires <span>instanceof</span> <span>Date</span><span>)</span> <span>{</span>
      sessionStorage<span>.</span><span>removeItem</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span>__expires</span><span>`</span></span><span>)</span>
      sessionStorage<span>.</span><span>setItem</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span>__expires</span><span>`</span></span><span>,</span> <span>new</span> <span>Date</span><span>(</span>expires<span>)</span><span>.</span><span>getTime</span><span>(</span><span>)</span><span>)</span>
    <span>}</span>
  <span>}</span><span>,</span>
  <span>// 获取缓存</span>
  <span>sessionGet</span><span>:</span> <span>function</span> <span>(</span><span>name</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>sessionStorage<span>.</span><span>getItem</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span>__expires</span><span>`</span></span><span>)</span><span>)</span> <span>{</span>
      <span>const</span> now <span>=</span> Date<span>.</span><span>now</span><span>(</span><span>)</span>
      <span>const</span> expires <span>=</span> sessionStorage<span>.</span><span>getItem</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span>__expires</span><span>`</span></span><span>)</span>
      <span>if</span> <span>(</span>now <span>>=</span> expires<span>)</span> <span>{</span>
        sessionStorage<span>.</span><span>removeItem</span><span>(</span>name<span>)</span>
        sessionStorage<span>.</span><span>removeItem</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span>__expires</span><span>`</span></span><span>)</span>
        <span>return</span> <span>undefined</span>
      <span>}</span> <span>else</span> <span>{</span>
        <span>return</span> <span>JSON</span><span>.</span><span>parse</span><span>(</span>sessionStorage<span>.</span><span>getItem</span><span>(</span>name<span>)</span><span>)</span>
      <span>}</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>return</span> <span>JSON</span><span>.</span><span>parse</span><span>(</span>sessionStorage<span>.</span><span>getItem</span><span>(</span>name<span>)</span><span>)</span>
    <span>}</span>
  <span>}</span><span>,</span>
  <span>// 清除缓存</span>
  <span>sessionRemove</span><span>:</span> <span>function</span> <span>(</span><span>name</span><span>)</span> <span>{</span>
    sessionStorage<span>.</span><span>removeItem</span><span>(</span>name<span>)</span>
  <span>}</span><span>,</span>
  <span>// ==================localStorage设置缓存==================</span>
  <span>// 设置缓存</span>
  <span>localSet</span><span>:</span> <span>function</span> <span>(</span><span>name<span>,</span> data<span>,</span> expires</span><span>)</span> <span>{</span>
    localStorage<span>.</span><span>removeItem</span><span>(</span>name<span>)</span>
    localStorage<span>.</span><span>setItem</span><span>(</span>name<span>,</span> <span>JSON</span><span>.</span><span>stringify</span><span>(</span>data<span>)</span><span>)</span>
    <span>if</span> <span>(</span>expires <span>&amp;&amp;</span> expires <span>instanceof</span> <span>Date</span><span>)</span> <span>{</span>
      localStorage<span>.</span><span>removeItem</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span>__expires</span><span>`</span></span><span>)</span>
      localStorage<span>.</span><span>setItem</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span>__expires</span><span>`</span></span><span>,</span> <span>new</span> <span>Date</span><span>(</span>expires<span>)</span><span>.</span><span>getTime</span><span>(</span><span>)</span><span>)</span>
    <span>}</span>
  <span>}</span><span>,</span>
  <span>// 获取缓存</span>
  <span>localGet</span><span>:</span> <span>function</span> <span>(</span><span>name</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>localStorage<span>.</span><span>getItem</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span>__expires</span><span>`</span></span><span>)</span><span>)</span> <span>{</span>
      <span>const</span> now <span>=</span> Date<span>.</span><span>now</span><span>(</span><span>)</span>
      <span>const</span> expires <span>=</span> localStorage<span>.</span><span>getItem</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span>__expires</span><span>`</span></span><span>)</span>
      <span>if</span> <span>(</span>now <span>>=</span> expires<span>)</span> <span>{</span>
        localStorage<span>.</span><span>removeItem</span><span>(</span>name<span>)</span>
        localStorage<span>.</span><span>removeItem</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span>__expires</span><span>`</span></span><span>)</span>
        <span>return</span> <span>undefined</span>
      <span>}</span> <span>else</span> <span>{</span>
        <span>return</span> <span>JSON</span><span>.</span><span>parse</span><span>(</span>localStorage<span>.</span><span>getItem</span><span>(</span>name<span>)</span><span>)</span>
      <span>}</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>return</span> <span>JSON</span><span>.</span><span>parse</span><span>(</span>localStorage<span>.</span><span>getItem</span><span>(</span>name<span>)</span><span>)</span>
    <span>}</span>
  <span>}</span><span>,</span>
  <span>// 清除缓存</span>
  <span>localRemove</span><span>:</span> <span>function</span> <span>(</span><span>name</span><span>)</span> <span>{</span>
    localStorage<span>.</span><span>removeItem</span><span>(</span>name<span>)</span>
  <span>}</span>
<span>}</span>

<span>export</span> <span>default</span> Storage
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br></div></div></li>
</ul>
</li>
<li>
<p>方案二： 插件实现。</p>
<ul>
<li><a href="https://www.npmjs.com/package/vuex-persistedstate" target="_blank" rel="noopener noreferrer">vuex-persistedstate 插件</a>：<code>persistedState</code>内部默认使用<code>localStorage</code>存储。</li>
<li><a href="https://github.com/championswimmer/vuex-persist" target="_blank" rel="noopener noreferrer">vuex-persist 插件</a></li>
</ul>
</li>
</ul>
<h3 id="参考资料"> 参考资料</h3>
<p><a href="https://vuex.vuejs.org/zh/guide/" target="_blank" rel="noopener noreferrer">vuex3.x文档</a></p>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-29T22:43:23.000Z</published>
  </entry>
  <entry>
    <title type="html">VUE 2.X</title>
    <id>https://sewar-x.github.io/vue/</id>
    <link href="https://sewar-x.github.io/vue/"/>
    <updated>2021-06-29T22:43:23.000Z</updated>
    <content type="html"><![CDATA[<h1 id="vue-2-x"> VUE 2.X</h1>
<h2 id="框架设计"> 框架设计</h2>
<h3 id="mvc-mvvm"> MVC &amp; MVVM</h3>
<p><img src="./../images/mvvm1.png" alt="" /></p>
<p><img src="./../images/mvvm2.png" alt="" /></p>
<p><strong>vue 采用模型：</strong></p>
<ul>
<li>Vue.js 采用mvvm模型，一个核心思想是数据驱动。</li>
<li>所谓数据驱动，是指视图是由数据驱动生成的，我们对视图的修改，不会直接操作 DOM，而是通过修改数据。</li>
<li>它相比我们传统的前端开发，如使用 jQuery 等前端库直接修改 DOM，大大简化了代码量。特别是当交互复杂的时候，只关心数据的修改会让代码的逻辑变的非常清晰，因为 DOM 变成了数据的映射，我们所有的逻辑都是对数据的修改，而不用碰触 DOM，这样的代码非常利于维护。</li>
</ul>
<h3 id="单页面-多页面应用"> 单页面 &amp; 多页面应用</h3>
<p><img src="./../images/mvvm3.png" alt="" /></p>
<h2 id="原理分析"> 原理分析</h2>
<h3 id="vue-构建"> VUE 构建</h3>
<ul>
<li>
<p>通常在开发 vue 项目完成后会通过命令行 npm run build 进行 打包构建，vue 中的构建也是如此。</p>
</li>
<li>
<p>vue 构建流程：</p>
<p><img src="./../flow-graph/vue构建流程.png" alt="" /></p>
</li>
<li>
<p>Vue.js 源码是基于 <a href="https://github.com/rollup/rollup" target="_blank" rel="noopener noreferrer">Rollup</a> 构建的，它的构建相关配置都在 scripts 目录下</p>
<p><strong>源码分析：</strong></p>
</li>
<li>
<p>构建脚本</p>
<p>通常一个基于 NPM 托管的项目都会有一个 package.json 文件，它是对项目的描述文件，它的内容实际上是一个标准的 JSON 对象。</p>
<p>我们通常会配置 <code>script</code> 字段作为 NPM 的执行脚本，Vue.js 源码构建的脚本如下：</p>
<div><pre><code><span>{</span>
  <span>"script"</span><span>:</span> <span>{</span>
    <span>"build"</span><span>:</span> <span>"node scripts/build.js"</span><span>,</span>
    <span>"build:ssr"</span><span>:</span> <span>"npm run build -- web-runtime-cjs,web-server-renderer"</span><span>,</span>
    <span>"build:weex"</span><span>:</span> <span>"npm run build --weex"</span>
  <span>}</span>
<span>}</span>
 
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div><p>这里总共有 3 条命令，作用都是构建 Vue.js，后面 2 条是在第一条命令的基础上，添加一些环境参数。</p>
<p>当在命令行运行 <code>npm run build</code> 的时候，实际上就会执行 <code>node scripts/build.js</code>，接下来我们来看看它实际是怎么构建的。</p>
</li>
<li>
<p>构建过程</p>
<p>我们对于构建过程分析是基于源码的，先打开构建的入口 JS 文件，在 <code>scripts/build.js</code> 中：</p>
<div><pre><code><span>let</span> builds <span>=</span> <span>require</span><span>(</span><span>'./config'</span><span>)</span><span>.</span><span>getAllBuilds</span><span>(</span><span>)</span>

<span>// filter builds via command line arg</span>
<span>if</span> <span>(</span>process<span>.</span>argv<span>[</span><span>2</span><span>]</span><span>)</span> <span>{</span>
  <span>const</span> filters <span>=</span> process<span>.</span>argv<span>[</span><span>2</span><span>]</span><span>.</span><span>split</span><span>(</span><span>','</span><span>)</span>
  builds <span>=</span> builds<span>.</span><span>filter</span><span>(</span><span>b</span> <span>=></span> <span>{</span>
    <span>return</span> filters<span>.</span><span>some</span><span>(</span><span>f</span> <span>=></span> b<span>.</span>output<span>.</span>file<span>.</span><span>indexOf</span><span>(</span>f<span>)</span> <span>></span> <span>-</span><span>1</span> <span>||</span> b<span>.</span>_name<span>.</span><span>indexOf</span><span>(</span>f<span>)</span> <span>></span> <span>-</span><span>1</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span> <span>else</span> <span>{</span>
  <span>// filter out weex builds by default</span>
  builds <span>=</span> builds<span>.</span><span>filter</span><span>(</span><span>b</span> <span>=></span> <span>{</span>
    <span>return</span> b<span>.</span>output<span>.</span>file<span>.</span><span>indexOf</span><span>(</span><span>'weex'</span><span>)</span> <span>===</span> <span>-</span><span>1</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>build</span><span>(</span>builds<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div><p>这段代码逻辑非常简单，先从配置文件读取配置，再通过命令行参数对构建配置做过滤，这样就可以构建出不同用途的 Vue.js 了。接下来我们看一下配置文件，在 <code>scripts/config.js</code> 中：</p>
<div><pre><code><span>const</span> builds <span>=</span> <span>{</span>
  <span>// Runtime only (CommonJS). Used by bundlers e.g. Webpack &amp; Browserify</span>
  <span>'web-runtime-cjs'</span><span>:</span> <span>{</span>
    entry<span>:</span> <span>resolve</span><span>(</span><span>'web/entry-runtime.js'</span><span>)</span><span>,</span>
    dest<span>:</span> <span>resolve</span><span>(</span><span>'dist/vue.runtime.common.js'</span><span>)</span><span>,</span>
    format<span>:</span> <span>'cjs'</span><span>,</span>
    banner
  <span>}</span><span>,</span>
  <span>// Runtime+compiler CommonJS build (CommonJS)</span>
  <span>'web-full-cjs'</span><span>:</span> <span>{</span>
    entry<span>:</span> <span>resolve</span><span>(</span><span>'web/entry-runtime-with-compiler.js'</span><span>)</span><span>,</span>
    dest<span>:</span> <span>resolve</span><span>(</span><span>'dist/vue.common.js'</span><span>)</span><span>,</span>
    format<span>:</span> <span>'cjs'</span><span>,</span>
    alias<span>:</span> <span>{</span> he<span>:</span> <span>'./entity-decoder'</span> <span>}</span><span>,</span>
    banner
  <span>}</span><span>,</span>
  <span>// Runtime only (ES Modules). Used by bundlers that support ES Modules,</span>
  <span>// e.g. Rollup &amp; Webpack 2</span>
  <span>'web-runtime-esm'</span><span>:</span> <span>{</span>
    entry<span>:</span> <span>resolve</span><span>(</span><span>'web/entry-runtime.js'</span><span>)</span><span>,</span>
    dest<span>:</span> <span>resolve</span><span>(</span><span>'dist/vue.runtime.esm.js'</span><span>)</span><span>,</span>
    format<span>:</span> <span>'es'</span><span>,</span>
    banner
  <span>}</span><span>,</span>
  <span>// Runtime+compiler CommonJS build (ES Modules)</span>
  <span>'web-full-esm'</span><span>:</span> <span>{</span>
    entry<span>:</span> <span>resolve</span><span>(</span><span>'web/entry-runtime-with-compiler.js'</span><span>)</span><span>,</span>
    dest<span>:</span> <span>resolve</span><span>(</span><span>'dist/vue.esm.js'</span><span>)</span><span>,</span>
    format<span>:</span> <span>'es'</span><span>,</span>
    alias<span>:</span> <span>{</span> he<span>:</span> <span>'./entity-decoder'</span> <span>}</span><span>,</span>
    banner
  <span>}</span><span>,</span>
  <span>// runtime-only build (Browser)</span>
  <span>'web-runtime-dev'</span><span>:</span> <span>{</span>
    entry<span>:</span> <span>resolve</span><span>(</span><span>'web/entry-runtime.js'</span><span>)</span><span>,</span>
    dest<span>:</span> <span>resolve</span><span>(</span><span>'dist/vue.runtime.js'</span><span>)</span><span>,</span>
    format<span>:</span> <span>'umd'</span><span>,</span>
    env<span>:</span> <span>'development'</span><span>,</span>
    banner
  <span>}</span><span>,</span>
  <span>// runtime-only production build (Browser)</span>
  <span>'web-runtime-prod'</span><span>:</span> <span>{</span>
    entry<span>:</span> <span>resolve</span><span>(</span><span>'web/entry-runtime.js'</span><span>)</span><span>,</span>
    dest<span>:</span> <span>resolve</span><span>(</span><span>'dist/vue.runtime.min.js'</span><span>)</span><span>,</span>
    format<span>:</span> <span>'umd'</span><span>,</span>
    env<span>:</span> <span>'production'</span><span>,</span>
    banner
  <span>}</span><span>,</span>
  <span>// Runtime+compiler development build (Browser)</span>
  <span>'web-full-dev'</span><span>:</span> <span>{</span>
    entry<span>:</span> <span>resolve</span><span>(</span><span>'web/entry-runtime-with-compiler.js'</span><span>)</span><span>,</span>
    dest<span>:</span> <span>resolve</span><span>(</span><span>'dist/vue.js'</span><span>)</span><span>,</span>
    format<span>:</span> <span>'umd'</span><span>,</span>
    env<span>:</span> <span>'development'</span><span>,</span>
    alias<span>:</span> <span>{</span> he<span>:</span> <span>'./entity-decoder'</span> <span>}</span><span>,</span>
    banner
  <span>}</span><span>,</span>
  <span>// Runtime+compiler production build  (Browser)</span>
  <span>'web-full-prod'</span><span>:</span> <span>{</span>
    entry<span>:</span> <span>resolve</span><span>(</span><span>'web/entry-runtime-with-compiler.js'</span><span>)</span><span>,</span>
    dest<span>:</span> <span>resolve</span><span>(</span><span>'dist/vue.min.js'</span><span>)</span><span>,</span>
    format<span>:</span> <span>'umd'</span><span>,</span>
    env<span>:</span> <span>'production'</span><span>,</span>
    alias<span>:</span> <span>{</span> he<span>:</span> <span>'./entity-decoder'</span> <span>}</span><span>,</span>
    banner
  <span>}</span><span>,</span>
  <span>// ...</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br></div></div><p>这里列举了一些 Vue.js 构建的配置，关于还有一些服务端渲染 webpack 插件以及 weex 的打包配置就不列举了。</p>
<p>对于单个配置，它是遵循 Rollup 的构建规则的。其中 <code>entry</code> 属性表示构建的入口 JS 文件地址，<code>dest</code> 属性表示构建后的 JS 文件地址。<code>format</code> 属性表示构建的格式，<code>cjs</code> 表示构建出来的文件遵循 <a href="http://wiki.commonjs.org/wiki/Modules/1.1" target="_blank" rel="noopener noreferrer">CommonJS</a> 规范，<code>es</code> 表示构建出来的文件遵循 <a href="http://exploringjs.com/es6/ch_modules.html" target="_blank" rel="noopener noreferrer">ES Module</a> 规范。 <code>umd</code> 表示构建出来的文件遵循 <a href="https://github.com/umdjs/umd" target="_blank" rel="noopener noreferrer">UMD</a> 规范。</p>
<p>以 <code>web-runtime-cjs</code> 配置为例，它的 <code>entry</code> 是 <code>resolve('web/entry-runtime.js')</code>，先来看一下 <code>resolve</code> 函数的定义。</p>
<p>源码目录：<code>scripts/config.js</code></p>
<div><pre><code><span>const</span> aliases <span>=</span> <span>require</span><span>(</span><span>'./alias'</span><span>)</span>
<span>const</span> <span>resolve</span> <span>=</span> <span>p</span> <span>=></span> <span>{</span>
  <span>const</span> base <span>=</span> p<span>.</span><span>split</span><span>(</span><span>'/'</span><span>)</span><span>[</span><span>0</span><span>]</span>
  <span>if</span> <span>(</span>aliases<span>[</span>base<span>]</span><span>)</span> <span>{</span>
    <span>return</span> path<span>.</span><span>resolve</span><span>(</span>aliases<span>[</span>base<span>]</span><span>,</span> p<span>.</span><span>slice</span><span>(</span>base<span>.</span>length <span>+</span> <span>1</span><span>)</span><span>)</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>return</span> path<span>.</span><span>resolve</span><span>(</span>__dirname<span>,</span> <span>'../'</span><span>,</span> p<span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div><p>这里的 <code>resolve</code> 函数实现非常简单，它先把 <code>resolve</code> 函数传入的参数 <code>p</code> 通过 <code>/</code> 做了分割成数组，然后取数组第一个元素设置为 <code>base</code>。在我们这个例子中，参数 <code>p</code> 是 <code>web/entry-runtime.js</code>，那么 <code>base</code> 则为 <code>web</code>。<code>base</code> 并不是实际的路径，它的真实路径借助了别名的配置，我们来看一下别名配置的代码，在 <code>scripts/alias</code> 中：</p>
<div><pre><code><span>const</span> path <span>=</span> <span>require</span><span>(</span><span>'path'</span><span>)</span>

module<span>.</span>exports <span>=</span> <span>{</span>
  vue<span>:</span> path<span>.</span><span>resolve</span><span>(</span>__dirname<span>,</span> <span>'../src/platforms/web/entry-runtime-with-compiler'</span><span>)</span><span>,</span>
  compiler<span>:</span> path<span>.</span><span>resolve</span><span>(</span>__dirname<span>,</span> <span>'../src/compiler'</span><span>)</span><span>,</span>
  core<span>:</span> path<span>.</span><span>resolve</span><span>(</span>__dirname<span>,</span> <span>'../src/core'</span><span>)</span><span>,</span>
  shared<span>:</span> path<span>.</span><span>resolve</span><span>(</span>__dirname<span>,</span> <span>'../src/shared'</span><span>)</span><span>,</span>
  web<span>:</span> path<span>.</span><span>resolve</span><span>(</span>__dirname<span>,</span> <span>'../src/platforms/web'</span><span>)</span><span>,</span>
  weex<span>:</span> path<span>.</span><span>resolve</span><span>(</span>__dirname<span>,</span> <span>'../src/platforms/weex'</span><span>)</span><span>,</span>
  server<span>:</span> path<span>.</span><span>resolve</span><span>(</span>__dirname<span>,</span> <span>'../src/server'</span><span>)</span><span>,</span>
  entries<span>:</span> path<span>.</span><span>resolve</span><span>(</span>__dirname<span>,</span> <span>'../src/entries'</span><span>)</span><span>,</span>
  sfc<span>:</span> path<span>.</span><span>resolve</span><span>(</span>__dirname<span>,</span> <span>'../src/sfc'</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br></div></div><p>很显然，这里 <code>web</code> 对应的真实的路径是 <code>path.resolve(__dirname, '../src/platforms/web')</code>，这个路径就找到了 Vue.js 源码的 web 目录。然后 <code>resolve</code> 函数通过 <code>path.resolve(aliases[base], p.slice(base.length + 1))</code> 找到了最终路径，它就是 Vue.js 源码 web 目录下的 <code>entry-runtime.js</code>。因此，<code>web-runtime-cjs</code> 配置对应的入口文件就找到了。</p>
<p>它经过 Rollup 的构建打包后，最终会在 dist 目录下生成 <code>vue.runtime.common.js</code></p>
</li>
</ul>
<h3 id="总流程"> 总流程</h3>
<p><img src="./../flow-graph/vue生命周期.png" alt="vue生命周期" /></p>
<p><img src="./../flow-graph/响应式原理流程图.jpeg" alt="vue生命周期" /></p>
<ul>
<li>
<p>总体流程：初始化 -&gt;挂载组件 -&gt; 模板编译 -&gt;  依赖收集 -&gt; 数据更新 -&gt; 实例销毁</p>
<ul>
<li>初始化：
<ul>
<li>在 new Vue() 之后, 调用 _init 函数进行初始化: 生命周期、事件、初始化渲染、调用 beforeCreated 钩子；</li>
<li>初始化状态 initState： props、methods、data、computed 与 watch;</li>
<li>初始化 data、computed 和 watch 时，调用 <code>defineReactive</code> 定义响应式数据，使用 Object.defineProperty 设置  getter 函数，初始化「 响应式」数据。</li>
<li>调用 created 钩子。</li>
</ul>
</li>
<li>挂载组件：
<ul>
<li>调用 <code>$mount</code> 挂载组件，如果是运行时编译，即不存在  render function  但是存在 template  的情况，需要进行「 编译」步骤。</li>
</ul>
</li>
<li>模板编译:
<ul>
<li>将模板编译生成 渲染函数。</li>
<li><code>parse</code>:  用正则等方式解析  template  模板中的指令、class、style 等数据，形成 AST。</li>
<li><code>optimize</code>: 标记  static  静态节点。当 update更新界面时，会有一个 patch 的过程，diff  算法会直接跳过静态节点，从而减少了比较的过程，优化了 patch 的性能。</li>
<li><code>generate</code>:将  AST  转化成  render function  字符串的过程,得到 render 的字符串以及 staticRenderFns  字符串。</li>
</ul>
</li>
<li>依赖收集
<ul>
<li>当  render function  被渲染的时候，因为会读取所需对象的值，所以会触发数据的 getter 函数进行「 依赖收集」；</li>
<li>「 依赖收集」的目的是将观察者  Watcher  对象存放到当前闭包中的订阅者  Dep  的  subs  中。</li>
<li>调用 beforeMount 钩子。</li>
<li>通过 _render() 生成 VNode，通过 _update() 将 VNode 渲染成真实 DOM。</li>
<li>createElm() 先通过 VNode 创建真实 DOM 插入到父节点，直到递归完成完整 DOM  插入到 body，<strong>完成初次渲染</strong>。</li>
<li>调用 mounted 钩子。</li>
</ul>
</li>
<li>数据更新
<ul>
<li>在修改对象的值的时候，会触发对应的 setter， setter 通知之前「 依赖收集」得到的  Dep  中的每一个 Watcher，告诉它们自己的值改变了，需要重新渲染视图。</li>
<li>Watcher  就会开始调用 update 来更新视图。</li>
</ul>
</li>
<li>update时, 通过 patch (打补丁) 的过程以及使用队列来异步更新的策略更新视图。
<ul>
<li>patch 时，通过 patchVnode() 比较 新旧 VNode, 并 updateChild() 进行更新视图。</li>
</ul>
</li>
<li>实例销毁：
<ul>
<li>离开页面时，触发 beforeDestroy 钩子</li>
</ul>
</li>
</ul>
</li>
<li>
<p>总体流程图：</p>
<p><img src="./../flow-graph/vue创建流程.png" alt="" /></p>
</li>
</ul>
<p><strong>VUE 三大核心：</strong></p>
<ul>
<li>响应式模块：以下响应式部分讲解。</li>
<li>编译器模块：将模板编译成渲染函数，以下模板编译部分为编译器模块。</li>
<li>渲染模块：包含三个阶段。
<ul>
<li>渲染阶段：将 render function 渲染成 Virtual Dom Node; 对应以下 render -&gt; patch 阶段；</li>
<li>挂载阶段：将  Virtual Dom Node 调用 DOM API 创建页面；对应以下 render -&gt; patch  阶段;</li>
<li>patch阶段：进行 新旧虚拟节点比较并只更新网页变化部分；对应 派发更新阶段；</li>
</ul>
</li>
</ul>
<h3 id="生命周期"> 生命周期</h3>
<p><strong>基础知识：</strong></p>
<p>Vue 一共有 8 个生命阶段，分别是创建前、创建后、加载前、加载后、更新前、更新后、销毁 前和销毁后，每个阶段对应了一个生命周期的钩子函数：</p>
<ul>
<li>
<p>beforeCreate 钩子函数：在实例初始化之后，在数据监听和事件配置之前触发。</p>
<ul>
<li>在这个事件中获取不到 data 数据。</li>
</ul>
</li>
<li>
<p>created 钩子函数：在实例创建完成后触发，此时可以访问 data、methods 等属性。</p>
<ul>
<li>但这个时候组件还没有被挂载到页面中去，所以这个时候访问不到 $el 属性。</li>
<li>一般我们可以在这 个函数中进行一些页面初始化的工作。比如通过 ajax 请求数据来对页面进行初始化。</li>
</ul>
</li>
<li>
<p>beforeMount 钩子函数：在组件被挂载到页面之前触发。</p>
<ul>
<li>在 beforeMount 之前，会找到对应的 template，编译成 render 函数。</li>
</ul>
</li>
<li>
<p>mounted 钩子函数：在组件挂载到页面之后触发。已经完成页面首次渲染。</p>
<ul>
<li>此时可以通过 DOM API 获取到页面中 的 DOM 元素。</li>
<li>在 mounted 中请求数据，可能导致页面闪屏。
<ul>
<li>因为 mounted 中，已经完成页面首次渲染，因此在 mounted 获取异步数据，数据返回前首次渲染的页面是没有后台数据的，在获取数据后将进入数据派发过程。</li>
</ul>
</li>
</ul>
</li>
<li>
<p>beforeUpdate 钩子函数：在响应式数据更新时触发，发生在虚拟 DOM 重新渲染和 patch 打补 丁之前。</p>
<ul>
<li>这个时候我们可以对可能会被移除的元素做一些操作，比如移除事件监听器。</li>
</ul>
</li>
<li>
<p>updated 钩子函数：虚拟 DOM 重新渲染和打补丁之后调用。</p>
</li>
<li>
<p>beforeDestroy 钩子函数：在实例销毁之前调用。</p>
<ul>
<li>一般在这一步我们可以销毁定时器、 解绑全局事件、组件中创建的实例等。</li>
</ul>
</li>
<li>
<p>destroyed 钩子函数：在实例销毁之后调用，调用后，Vue 实例中的所有东西都会解除 绑定，所有的事件监听器会被移除，所有的子实例也会被销毁。</p>
</li>
</ul>
<p><code>&lt;keep-alive&gt;</code> 组件中，还有两个钩子函数，分别是 activated 和 deactivated 。</p>
<ul>
<li>用 keep-alive 包裹的组件在切换时不会进行销毁，而是缓存到内存中并执行 deactivated 钩子函数。</li>
<li>命中缓存渲染后会执行 actived 钩子函数。</li>
</ul>
<p><strong>原理分析：</strong></p>
<ul>
<li>执行生命周期的函数都是调用 <code>callHook</code> 方法。</li>
</ul>
<p><strong>callHook 方法</strong></p>
<p><code>callHook</code> 方法： 功能就是调用某个生命周期钩子注册的所有回调函数。</p>
<ul>
<li>
<p>它的定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/rc/core/instance/lifecycle" target="_blank" rel="noopener noreferrer">src/core/instance/lifecycle</a> 中：</p>
<div><pre><code><span>/*callhook 函数的功能就是调用某个生命周期钩子注册的所有回调函数,调用钩子函数并且触发钩子事件*/</span>
<span>export</span> <span>function</span> <span>callHook</span><span>(</span><span>vm<span>:</span> Component<span>,</span> hook<span>:</span> string</span><span>)</span> <span>{</span>
  <span>const</span> handlers <span>=</span> vm<span>.</span>$options<span>[</span>hook<span>]</span> <span>//获取hook对应的回调数组，在合并options时将生命周期合并进入options</span>
  <span>if</span> <span>(</span>handlers<span>)</span> <span>{</span>
    <span>//遍历执行，执行的时候把 vm 作为函数执行的上下文</span>
    <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>,</span> j <span>=</span> handlers<span>.</span>length<span>;</span> i <span>&lt;</span> j<span>;</span> i<span>++</span><span>)</span> <span>{</span>
      <span>try</span> <span>{</span>
        handlers<span>[</span>i<span>]</span><span>.</span><span>call</span><span>(</span>vm<span>)</span>
      <span>}</span> <span>catch</span> <span>(</span>e<span>)</span> <span>{</span>
        <span>handleError</span><span>(</span>e<span>,</span> vm<span>,</span> <span><span>`</span><span><span>${</span>hook<span>}</span></span><span> hook</span><span>`</span></span><span>)</span>
      <span>}</span>
    <span>}</span>
  <span>}</span>
  <span>if</span> <span>(</span>vm<span>.</span>_hasHookEvent<span>)</span> <span>{</span>
    vm<span>.</span><span>$emit</span><span>(</span><span>'hook:'</span> <span>+</span> hook<span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br></div></div></li>
</ul>
<p><strong>beforeCreate &amp; created</strong></p>
<ul>
<li>
<p><code>beforeCreate</code> 和 <code>created</code> 函数都是在实例化 <code>Vue</code> 的阶段，在 <code>_init</code> 方法中执行的，它的定义在 <code>src/core/instance/init.js</code> 中：</p>
<div><pre><code><span>Vue</span><span>.</span>prototype<span>.</span><span>_init</span> <span>=</span> <span>function</span> <span>(</span><span>options<span>?</span><span>:</span> Object</span><span>)</span> <span>{</span>
  <span>// ...</span>
  <span>initLifecycle</span><span>(</span>vm<span>)</span>
  <span>initEvents</span><span>(</span>vm<span>)</span>
  <span>initRender</span><span>(</span>vm<span>)</span>
  <span>callHook</span><span>(</span>vm<span>,</span> <span>'beforeCreate'</span><span>)</span>
  <span>initInjections</span><span>(</span>vm<span>)</span> <span>// resolve injections before data/props</span>
  <span>initState</span><span>(</span>vm<span>)</span>
  <span>initProvide</span><span>(</span>vm<span>)</span> <span>// resolve provide after data/props</span>
  <span>callHook</span><span>(</span>vm<span>,</span> <span>'created'</span><span>)</span>
  <span>// ...</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div><ul>
<li><code>beforeCreate</code> 的钩子函数中不能获取到 <code>props</code>、<code>data</code> 中定义的值和调用 <code>methods</code> 中定义的函数。
<ul>
<li><code>initState</code> 的作用是初始化 <code>props</code>、<code>data</code>、<code>methods</code>、<code>watch</code>、<code>computed</code> 等属性，<code>beforeCreate</code> 的钩子函数在 initState 之前调用，</li>
</ul>
</li>
<li><code>beforeCreate</code> 和 <code>created</code> 钩子函数执行的时候不能够访问 DOM。
<ul>
<li>因为钩子函数执行的时候，并没有渲染 DOM。</li>
</ul>
</li>
<li>访问 <code>props</code>、<code>data</code> 等数据的话，就需要使用 <code>created</code> 钩子函数。
<ul>
<li>created 在 props、data 初始化后调用。</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>beforeMount &amp; mounted</strong></p>
<ul>
<li>
<p><code>mounted</code> 钩子函数被调用时机有两种：</p>
<ul>
<li>外部 <code>new Vue</code> 初始化过程调用 <code>mounted</code> 钩子 ；</li>
<li>组件实例化调用 <code>mounted</code> 钩子 ；</li>
</ul>
</li>
<li>
<p>外部 <code>new Vue</code> 初始化过程调用钩子：</p>
<ul>
<li><code>beforeMount</code> 和 <code>mount</code>钩子函数发生在 <code>mount</code>方法，也就是 DOM 挂载之前，它的调用时机是在 <code>mountComponent</code> 函数中。</li>
<li>定义在 <code>src/core/instance/lifecycle.js</code> 中：</li>
</ul>
<div><pre><code>  vm<span>:</span> Component<span>,</span>
  el<span>:</span> <span>?</span>Element<span>,</span>
  hydrating<span>?</span><span>:</span> boolean
<span>)</span><span>:</span> Component <span>{</span>
  vm<span>.</span>$el <span>=</span> el
  <span>if</span> <span>(</span><span>!</span>vm<span>.</span>$options<span>.</span>render<span>)</span> <span>{</span> <span>//不存在render方法(vue实例中没有render方法并且mount没有转换render方法)</span>
    <span>/*render函数不存在的时候创建一个空的VNode节点*/</span>
    vm<span>.</span>$options<span>.</span>render <span>=</span> createEmptyVNode
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
      <span>/* istanbul ignore if */</span>
      <span>// 当用了 runtime only 版本没有render方法并且写添加了template，则警告(runtime only版本不能添加template)</span>
      <span>if</span> <span>(</span><span>(</span>vm<span>.</span>$options<span>.</span>template <span>&amp;&amp;</span> vm<span>.</span>$options<span>.</span>template<span>.</span><span>charAt</span><span>(</span><span>0</span><span>)</span> <span>!==</span> <span>'#'</span><span>)</span> <span>||</span>
        vm<span>.</span>$options<span>.</span>el <span>||</span> el<span>)</span> <span>{</span>
        <span>warn</span><span>(</span>
          <span>'You are using the runtime-only build of Vue where the template '</span> <span>+</span>
          <span>'compiler is not available. Either pre-compile the templates into '</span> <span>+</span>
          <span>'render functions, or use the compiler-included build.'</span><span>,</span>
          vm
        <span>)</span>
      <span>}</span> <span>else</span> <span>{</span> <span>// 没有写render函数或则template</span>
        <span>warn</span><span>(</span>
          <span>'Failed to mount component: template or render function not defined.'</span><span>,</span>
          vm
        <span>)</span>
      <span>}</span>
    <span>}</span>
  <span>}</span>
  <span>/*触发beforeMount钩子*/</span>
  <span>callHook</span><span>(</span>vm<span>,</span> <span>'beforeMount'</span><span>)</span>

  <span>/*updateComponent作为Watcher对象的getter函数，用来依赖收集*/</span>
  <span>let</span> updateComponent
  <span>/* istanbul ignore if */</span>
  <span>// 性能埋点相关</span>
  <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> config<span>.</span>performance <span>&amp;&amp;</span> mark<span>)</span> <span>{</span>
    <span>updateComponent</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
      <span>const</span> name <span>=</span> vm<span>.</span>_name
      <span>const</span> id <span>=</span> vm<span>.</span>_uid
      <span>const</span> startTag <span>=</span> <span><span>`</span><span>vue-perf-start:</span><span><span>${</span>id<span>}</span></span><span>`</span></span>
      <span>const</span> endTag <span>=</span> <span><span>`</span><span>vue-perf-end:</span><span><span>${</span>id<span>}</span></span><span>`</span></span>

      <span>mark</span><span>(</span>startTag<span>)</span>
      <span>const</span> vnode <span>=</span> vm<span>.</span><span>_render</span><span>(</span><span>)</span>
      <span>mark</span><span>(</span>endTag<span>)</span>
      <span>measure</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span> render</span><span>`</span></span><span>,</span> startTag<span>,</span> endTag<span>)</span>

      <span>mark</span><span>(</span>startTag<span>)</span>
      vm<span>.</span><span>_update</span><span>(</span>vnode<span>,</span> hydrating<span>)</span>
      <span>mark</span><span>(</span>endTag<span>)</span>
      <span>measure</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span> patch</span><span>`</span></span><span>,</span> startTag<span>,</span> endTag<span>)</span>
    <span>}</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>updateComponent</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
      <span>/*在此方法中调用 vm._render (该方法定义在./render) 方法先生成虚拟 VNode，
      最终调用 vm._update 更新 DOM，_update 是实例的一个私有方法，它被调用的时机有 2 个，
      一个是首次渲染，一个是数据更新的时候*/</span>
      vm<span>.</span><span>_update</span><span>(</span>vm<span>.</span><span>_render</span><span>(</span><span>)</span><span>,</span> hydrating<span>)</span>
    <span>}</span>
  <span>}</span>

  <span>/**
   * 这里对该vm注册一个渲染Watcher实例，Watcher的getter为updateComponent函数，
   * 用于触发所有渲染所需要用到的数据的getter，进行依赖收集，该Watcher实例会存在所有渲染所需数据的闭包Dep中
   * Watcher 在这里起到两个作用:
   * 一个是初始化的时候会执行回调函数（updateComponent），
   * 另一个是当 vm 实例中的监测的数据发生变化的时候执行回调函数（updateComponent）
   * @param {} vm vue实例对象
   * @param {Function} updateComponent 
   * @param {Funciont} noop 空函数
   */</span>
  vm<span>.</span>_watcher <span>=</span> <span>new</span> <span>Watcher</span><span>(</span>vm<span>,</span> updateComponent<span>,</span> noop<span>)</span> <span>//创建渲染watcher</span>
  hydrating <span>=</span> <span>false</span>

  <span>// manually mounted instance, call mounted on self</span>
  <span>// mounted is called for render-created child components in its inserted hook</span>
  <span>if</span> <span>(</span>vm<span>.</span>$vnode <span>==</span> <span>null</span><span>)</span> <span>{</span> <span>//vm.$vnode 表示 Vue 实例的父虚拟 Node，所以它为 Null 则表示当前是根 Vue 的实例</span>
    <span>/*标志位，代表该组件已经挂载*/</span>
    vm<span>.</span>_isMounted <span>=</span> <span>true</span>
    <span>/*调用mounted钩子*/</span>
    <span>callHook</span><span>(</span>vm<span>,</span> <span>'mounted'</span><span>)</span>
  <span>}</span>
  <span>return</span> vm
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br></div></div><ul>
<li>在执行 <code>vm._render()</code> 函数渲染 VNode 之前，执行了 <code>beforeMount</code> 钩子函数，在执行完 <code>vm._update()</code> 把 VNode patch 到真实 DOM 后，执行 <code>mouted</code> 钩子。</li>
<li>这里对 <code>mouted</code> 钩子函数执行有一个判断逻辑，<code>vm.$vnode</code> 如果为 <code>null</code>，则表明这不是一次组件的初始化过程，而是我们通过外部 <code>new Vue</code> 初始化过程。</li>
</ul>
</li>
<li>
<p>组件实例化调用 <code>mounted</code> 钩子 ：</p>
<ul>
<li>
<p>组件的 VNode patch 到 DOM 后，会执行 <code>invokeInsertHook</code> 函数，把 <code>insertedVnodeQueue</code> 里保存的钩子函数依次执行一遍，它的定义在 <code>src/core/vdom/patch.js</code> 中：</p>
<div><pre><code><span>function</span> <span>invokeInsertHook</span> <span>(</span><span>vnode<span>,</span> queue<span>,</span> initial</span><span>)</span> <span>{</span>
  <span>// delay insert hooks for component root nodes, invoke them after the</span>
  <span>// element is really inserted</span>
  <span>if</span> <span>(</span><span>isTrue</span><span>(</span>initial<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>vnode<span>.</span>parent<span>)</span><span>)</span> <span>{</span>
    vnode<span>.</span>parent<span>.</span>data<span>.</span>pendingInsert <span>=</span> queue
  <span>}</span> <span>else</span> <span>{</span>
    <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> queue<span>.</span>length<span>;</span> <span>++</span>i<span>)</span> <span>{</span>
      queue<span>[</span>i<span>]</span><span>.</span>data<span>.</span>hook<span>.</span><span>insert</span><span>(</span>queue<span>[</span>i<span>]</span><span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><ul>
<li>该函数会执行 <code>insert</code> 这个钩子函数，对于组件而言，<code>insert</code> 钩子函数的定义在 <code>src/core/vdom/create-component.js</code> 中的 <code>componentVNodeHooks</code> 中：</li>
</ul>
<div><pre><code><span>const</span> componentVNodeHooks <span>=</span> <span>{</span>
  <span>// ...</span>
  <span>insert</span> <span>(</span><span>vnode<span>:</span> MountedComponentVNode</span><span>)</span> <span>{</span>
    <span>const</span> <span>{</span> context<span>,</span> componentInstance <span>}</span> <span>=</span> vnode
    <span>if</span> <span>(</span><span>!</span>componentInstance<span>.</span>_isMounted<span>)</span> <span>{</span>
      componentInstance<span>.</span>_isMounted <span>=</span> <span>true</span>
      <span>callHook</span><span>(</span>componentInstance<span>,</span> <span>'mounted'</span><span>)</span>
    <span>}</span>
    <span>// ...</span>
  <span>}</span><span>,</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><ul>
<li>每个子组件都是在这个钩子函数中执行 <code>mouted</code> 钩子函数，并且我们之前分析过，<code>insertedVnodeQueue</code> 的添加顺序是先子后父，所以对于同步渲染的子组件而言，<code>mounted</code> 钩子函数的执行顺序也是先子后父。</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>beforeUpdate &amp; updated</strong></p>
<ul>
<li>
<p><code>beforeUpdate</code> 和 <code>updated</code> 的钩子函数执行时机都在数据更新的时候。</p>
</li>
<li>
<p><code>beforeUpdate</code> 的执行时机：</p>
<ul>
<li>
<p>在渲染 Watcher 的 <code>before</code> 函数中，在组件已经 mounted 之后，去调用这个钩子函数：</p>
<div><pre><code><span>export</span> <span>function</span> <span>mountComponent</span> <span>(</span>
  <span>vm<span>:</span> Component<span>,</span>
  el<span>:</span> <span>?</span>Element<span>,</span>
  hydrating<span>?</span><span>:</span> boolean</span>
<span>)</span><span>:</span> Component <span>{</span>
  <span>// ...</span>

  <span>// we set this to vm._watcher inside the watcher's constructor</span>
  <span>// since the watcher's initial patch may call $forceUpdate (e.g. inside child</span>
  <span>// component's mounted hook), which relies on vm._watcher being already defined</span>
  <span>new</span> <span>Watcher</span><span>(</span>vm<span>,</span> updateComponent<span>,</span> noop<span>,</span> <span>{</span>
    <span>before</span> <span>(</span><span>)</span> <span>{</span>
      <span>if</span> <span>(</span>vm<span>.</span>_isMounted<span>)</span> <span>{</span>
        <span>callHook</span><span>(</span>vm<span>,</span> <span>'beforeUpdate'</span><span>)</span>
      <span>}</span>
    <span>}</span>
  <span>}</span><span>,</span> <span>true</span> <span>/* isRenderWatcher */</span><span>)</span>
  <span>// ...</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div></li>
</ul>
</li>
<li>
<p><code>update</code> 的执行时机:</p>
<ul>
<li>
<p>在<code>flushSchedulerQueue</code> 函数调用的时候, 它的定义在 <code>src/core/observer/scheduler.js</code> 中：</p>
<div><pre><code><span>function</span> <span>flushSchedulerQueue</span> <span>(</span><span>)</span> <span>{</span>
  <span>// ...</span>
  <span>// 获取到 updatedQueue</span>
  <span>callUpdatedHooks</span><span>(</span>updatedQueue<span>)</span>
<span>}</span>
<span>/**updatedQueue 是 更新了的 wathcer 数组，那么在 callUpdatedHooks 函数中，它对这些数组做遍历，只有满足当前 watcher 为 vm._watcher 以及组件已经 mounted 这两个条件，才会执行 updated 钩子函数*/</span>
<span>function</span> <span>callUpdatedHooks</span> <span>(</span><span>queue</span><span>)</span> <span>{</span>
  <span>let</span> i <span>=</span> queue<span>.</span>length
  <span>while</span> <span>(</span>i<span>--</span><span>)</span> <span>{</span>
    <span>const</span> watcher <span>=</span> queue<span>[</span>i<span>]</span>
    <span>const</span> vm <span>=</span> watcher<span>.</span>vm
    <span>if</span> <span>(</span>vm<span>.</span>_watcher <span>===</span> watcher <span>&amp;&amp;</span> vm<span>.</span>_isMounted<span>)</span> <span>{</span>
      <span>callHook</span><span>(</span>vm<span>,</span> <span>'updated'</span><span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div><ul>
<li>
<p>callUpdatedHooks 函数中，只有 vm._watcher 的回调执行完毕后，才会执行 updated 钩子函数:</p>
<ul>
<li>
<p>在组件 mount 的过程中，会实例化一个渲染的 <code>Watcher</code> 去监听 <code>vm</code> 上的数据变化重新渲染，这断逻辑发生在 <code>mountComponent</code> 函数执行的时候：</p>
<div><pre><code><span>export</span> <span>function</span> <span>mountComponent</span> <span>(</span>
  <span>vm<span>:</span> Component<span>,</span>
  el<span>:</span> <span>?</span>Element<span>,</span>
  hydrating<span>?</span><span>:</span> boolean</span>
<span>)</span><span>:</span> Component <span>{</span>
  <span>// ...</span>
  <span>// 这里是简写</span>
  <span>let</span> <span>updateComponent</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
      vm<span>.</span><span>_update</span><span>(</span>vm<span>.</span><span>_render</span><span>(</span><span>)</span><span>,</span> hydrating<span>)</span>
  <span>}</span>
  <span>new</span> <span>Watcher</span><span>(</span>vm<span>,</span> updateComponent<span>,</span> noop<span>,</span> <span>{</span>
    <span>before</span> <span>(</span><span>)</span> <span>{</span>
      <span>if</span> <span>(</span>vm<span>.</span>_isMounted<span>)</span> <span>{</span>
        <span>callHook</span><span>(</span>vm<span>,</span> <span>'beforeUpdate'</span><span>)</span>
      <span>}</span>
    <span>}</span>
  <span>}</span><span>,</span> <span>true</span> <span>/* isRenderWatcher */</span><span>)</span>
  <span>// ...</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div><p>那么在实例化 <code>Watcher</code> 的过程中，在它的构造函数里会判断 <code>isRenderWatcher</code>，接着把当前 <code>watcher</code> 的实例赋值给 <code>vm._watcher</code>，定义在 <code>src/core/observer/watcher.js</code> 中：</p>
<div><pre><code><span>export</span> <span>default</span> <span>class</span> <span>Watcher</span> <span>{</span>
  <span>// ...</span>
  <span>constructor</span> <span>(</span>
    <span>vm<span>:</span> Component<span>,</span>
    expOrFn<span>:</span> string <span>|</span> Function<span>,</span>
    cb<span>:</span> Function<span>,</span>
    options<span>?</span><span>:</span> <span>?</span>Object<span>,</span>
    isRenderWatcher<span>?</span><span>:</span> boolean</span>
  <span>)</span> <span>{</span>
    <span>this</span><span>.</span>vm <span>=</span> vm
    <span>if</span> <span>(</span>isRenderWatcher<span>)</span> <span>{</span>
      vm<span>.</span>_watcher <span>=</span> <span>this</span>
    <span>}</span>
    vm<span>.</span>_watchers<span>.</span><span>push</span><span>(</span><span>this</span><span>)</span>
    <span>// ...</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br></div></div><p>同时，还把当前 <code>wathcer</code> 实例 push 到 <code>vm._watchers</code> 中，<code>vm._watcher</code> 是专门用来监听 <code>vm</code> 上数据变化然后重新渲染的，所以它是一个渲染相关的 <code>watcher</code>，因此在 <code>callUpdatedHooks</code> 函数中，只有 <code>vm._watcher</code> 的回调执行完毕后，才会执行 <code>updated</code> 钩子函数。</p>
</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>beforeDestroy &amp; destroyed</strong></p>
<ul>
<li>
<p><code>beforeDestroy</code> 和 <code>destroyed</code> 钩子函数的执行时机在组件销毁的阶段;</p>
</li>
<li>
<p>组件的销毁过程之后，最终会调用 <code>$destroy</code> 方法，它的定义在 <code>src/core/instance/lifecycle.js</code> 中：</p>
<div><pre><code>  <span>Vue</span><span>.</span>prototype<span>.</span><span>$destroy</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
    <span>const</span> vm<span>:</span> Component <span>=</span> <span>this</span>
    <span>if</span> <span>(</span>vm<span>.</span>_isBeingDestroyed<span>)</span> <span>{</span>
      <span>return</span>
    <span>}</span>
    <span>/* 调用beforeDestroy钩子 */</span>
    <span>callHook</span><span>(</span>vm<span>,</span> <span>'beforeDestroy'</span><span>)</span>
    <span>/* 标志位 */</span>
    vm<span>.</span>_isBeingDestroyed <span>=</span> <span>true</span>
    <span>// remove self from parent</span>
    <span>const</span> parent <span>=</span> vm<span>.</span>$parent
    <span>if</span> <span>(</span>parent <span>&amp;&amp;</span> <span>!</span>parent<span>.</span>_isBeingDestroyed <span>&amp;&amp;</span> <span>!</span>vm<span>.</span>$options<span>.</span>abstract<span>)</span> <span>{</span>
      <span>remove</span><span>(</span>parent<span>.</span>$children<span>,</span> vm<span>)</span>
    <span>}</span>
    <span>// teardown watchers</span>
    <span>/* 该组件下的所有Watcher从其所在的Dep中释放 */</span>
    <span>if</span> <span>(</span>vm<span>.</span>_watcher<span>)</span> <span>{</span>
      vm<span>.</span>_watcher<span>.</span><span>teardown</span><span>(</span><span>)</span>
    <span>}</span>
    <span>let</span> i <span>=</span> vm<span>.</span>_watchers<span>.</span>length
    <span>while</span> <span>(</span>i<span>--</span><span>)</span> <span>{</span>
      vm<span>.</span>_watchers<span>[</span>i<span>]</span><span>.</span><span>teardown</span><span>(</span><span>)</span>
    <span>}</span>
    <span>// remove reference from data ob</span>
    <span>// frozen object may not have observer.</span>
    <span>if</span> <span>(</span>vm<span>.</span>_data<span>.</span>__ob__<span>)</span> <span>{</span>
      vm<span>.</span>_data<span>.</span>__ob__<span>.</span>vmCount<span>--</span>
    <span>}</span>
    <span>// call the last hook...</span>
    vm<span>.</span>_isDestroyed <span>=</span> <span>true</span>
    <span>// invoke destroy hooks on current rendered tree</span>
    vm<span>.</span><span>__patch__</span><span>(</span>vm<span>.</span>_vnode<span>,</span> <span>null</span><span>)</span>
    <span>// fire destroyed hook</span>
    <span>/* 调用destroyed钩子 */</span>
    <span>callHook</span><span>(</span>vm<span>,</span> <span>'destroyed'</span><span>)</span>
    <span>// turn off all instance listeners.</span>
    <span>/* 移除所有事件监听 */</span>
    vm<span>.</span><span>$off</span><span>(</span><span>)</span>
    <span>// remove __vue__ reference</span>
    <span>if</span> <span>(</span>vm<span>.</span>$el<span>)</span> <span>{</span>
      vm<span>.</span>$el<span>.</span>__vue__ <span>=</span> <span>null</span>
    <span>}</span>
    <span>// remove reference to DOM nodes (prevents leak)</span>
    vm<span>.</span>$options<span>.</span>_parentElm <span>=</span> vm<span>.</span>$options<span>.</span>_refElm <span>=</span> <span>null</span>
  <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br></div></div><ul>
<li><code>beforeDestroy</code> 钩子函数的执行时机是在 <code>$destroy</code> 函数执行最开始的地方，接着执行了一系列的销毁动作，包括从 <code>parent</code> 的 <code>$children</code> 中删掉自身，删除 <code>watcher</code>，当前渲染的 VNode 执行销毁钩子函数等，执行完毕后再调用 <code>destroy</code> 钩子函数。</li>
<li>在 <code>$destroy</code> 的执行过程中，它又会执行 <code>vm.__patch__(vm._vnode, null)</code> 触发它子组件的销毁钩子函数，这样一层层的递归调用，所以 <code>destroy</code> 钩子函数执行顺序是先子后父，和 <code>mounted</code> 过程一样。</li>
</ul>
</li>
</ul>
<h3 id="初始化"> 初始化</h3>
<ul>
<li>
<p>初始化总体流程</p>
<p><img src="./../flow-graph/初始化过程.png" alt="" /></p>
</li>
</ul>
<p><strong>源码分析</strong></p>
<ul>
<li>
<p>从入口代码开始分析，我们先来分析 <code>new Vue</code> 背后发生了哪些事情。我们都知道，<code>new</code> 关键字在 Javascript 语言中代表实例化是一个对象，而 <code>Vue</code> 实际上是一个类，类在 Javascript 中是用 Function 来实现的，来看一下<a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/instance/index.js" target="_blank" rel="noopener noreferrer">源码</a>，在<code>src/core/instance/index.js</code> 中。</p>
<div><pre><code><span>//定义vue对象</span>
<span>function</span> <span>Vue</span> <span>(</span><span>options</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span>
    <span>!</span><span>(</span><span>this</span> <span>instanceof</span> <span>Vue</span><span>)</span><span>)</span> <span>{</span> <span>//必须通过new关键字创建vue实例(使用es5实现class的机制)</span>
    <span>warn</span><span>(</span><span>'Vue is a constructor and should be called with the `new` keyword'</span><span>)</span>
  <span>}</span>
  <span>/*初始化vue,该方法在 ./init.js 文件中定义，新建一个vue实例时会调用vue的init方法*/</span>
  <span>this</span><span>.</span><span>_init</span><span>(</span>options<span>)</span>
<span>}</span>

<span>// 在vue原型对象上进行扩展</span>
<span>initMixin</span><span>(</span>Vue<span>)</span> <span>// 在Vue的原型上增加_init方法，构造Vue实例的时候会调用这个_init方法来初始化Vue实例</span>
<span>stateMixin</span><span>(</span>Vue<span>)</span> <span>// 初始化props、methods、data、computed与watch</span>
<span>eventsMixin</span><span>(</span>Vue<span>)</span> <span>// 为Vue原型加入操作事件的方法</span>
<span>lifecycleMixin</span><span>(</span>Vue<span>)</span> <span>//为vue原型中添加声明周期钩子</span>
<span>renderMixin</span><span>(</span>Vue<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div><p>可以看到 <code>Vue</code> 只能通过 new 关键字初始化，然后会调用 <code>this._init</code> 方法， 该方法在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/instance/init.js" target="_blank" rel="noopener noreferrer">src/core/instance/init.js</a> 中定义。</p>
<div><pre><code><span>export</span> <span>function</span> <span>initMixin</span> <span>(</span><span>Vue<span>:</span> Class<span>&lt;</span>Component<span>></span></span><span>)</span> <span>{</span>
  <span>Vue</span><span>.</span>prototype<span>.</span><span>_init</span> <span>=</span> <span>function</span> <span>(</span><span>options<span>?</span><span>:</span> Object</span><span>)</span> <span>{</span>
    <span>const</span> vm<span>:</span> Component <span>=</span> <span>this</span>
    <span>// a uid</span>
    vm<span>.</span>_uid <span>=</span> uid<span>++</span>

    <span>let</span> startTag<span>,</span> endTag
    <span>/* istanbul ignore if */</span>
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> config<span>.</span>performance <span>&amp;&amp;</span> mark<span>)</span> <span>{</span>
      startTag <span>=</span> <span><span>`</span><span>vue-perf-init:</span><span><span>${</span>vm<span>.</span>_uid<span>}</span></span><span>`</span></span>
      endTag <span>=</span> <span><span>`</span><span>vue-perf-end:</span><span><span>${</span>vm<span>.</span>_uid<span>}</span></span><span>`</span></span>
      <span>mark</span><span>(</span>startTag<span>)</span>
    <span>}</span>

    <span>// a flag to avoid this being observed</span>
    <span>/*一个防止vm实例自身被观察的标志位*/</span>
    vm<span>.</span>_isVue <span>=</span> <span>true</span>
    <span>// 合并传入options</span>
    <span>if</span> <span>(</span>options <span>&amp;&amp;</span> options<span>.</span>_isComponent<span>)</span> <span>{</span> <span>//当传入参数为组件时,执行组件初始化时合并options</span>
      <span>// optimize internal component instantiation</span>
      <span>// since dynamic options merging is pretty slow, and none of the</span>
      <span>// internal component options needs special treatment.</span>
      <span>//优化内部组件实例化，因为动态选项合并非常慢，而且没有一个内部组件选项需要特殊处理。</span>
      <span>//把通过 createComponentInstanceForVnode 函数传入的几个参数合并到内部的选项 $options 里了</span>
      <span>initInternalComponent</span><span>(</span>vm<span>,</span> options<span>)</span>
    <span>}</span> <span>else</span> <span>{</span> <span>//通过new Vue(option)时，合并option</span>
      vm<span>.</span>$options <span>=</span> <span>mergeOptions</span><span>(</span> <span>//合并options，将new Vue(options)传入的options合并到vue实例的options</span>
        <span>resolveConstructorOptions</span><span>(</span>vm<span>.</span>constructor<span>)</span><span>,</span> <span>//指Vue.option，定义在src/core/global-api/index </span>
        options <span>||</span> <span>{</span><span>}</span><span>,</span> <span>//new Vue(option)定义Vue时候传入的option</span>
        vm
      <span>)</span>
    <span>}</span>
    <span>/* istanbul ignore else */</span>
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
      <span>initProxy</span><span>(</span>vm<span>)</span> <span>//开发环境下定义属性代理</span>
    <span>}</span> <span>else</span> <span>{</span>
      vm<span>.</span>_renderProxy <span>=</span> vm <span>//生产环境下保存vue实例</span>
    <span>}</span>
    <span>// 对外暴露实例对象</span>
    vm<span>.</span>_self <span>=</span> vm
    <span>/*初始化生命周期*/</span>
    <span>initLifecycle</span><span>(</span>vm<span>)</span>
    <span>/*初始化事件中心*/</span>
    <span>initEvents</span><span>(</span>vm<span>)</span>
    <span>/*初始化render*/</span>
    <span>initRender</span><span>(</span>vm<span>)</span>
    <span>/*调用beforeCreate钩子函数并且触发beforeCreate钩子事件*/</span>
    <span>callHook</span><span>(</span>vm<span>,</span> <span>'beforeCreate'</span><span>)</span>
    <span>initInjections</span><span>(</span>vm<span>)</span> <span>// resolve injections before data/props</span>
    <span>/*初始化props、methods、data、computed与watch*/</span>
    <span>initState</span><span>(</span>vm<span>)</span>
    <span>initProvide</span><span>(</span>vm<span>)</span> <span>// resolve provide after data/props</span>
    <span>/*调用created钩子函数并且触发created钩子事件*/</span>
    <span>callHook</span><span>(</span>vm<span>,</span> <span>'created'</span><span>)</span>

    <span>/* istanbul ignore if */</span>
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> config<span>.</span>performance <span>&amp;&amp;</span> mark<span>)</span> <span>{</span>
      <span>/*格式化组件名*/</span>
      vm<span>.</span>_name <span>=</span> <span>formatComponentName</span><span>(</span>vm<span>,</span> <span>false</span><span>)</span>
      <span>mark</span><span>(</span>endTag<span>)</span>
      <span>measure</span><span>(</span><span><span>`</span><span><span>${</span>vm<span>.</span>_name<span>}</span></span><span> init</span><span>`</span></span><span>,</span> startTag<span>,</span> endTag<span>)</span>
    <span>}</span>

    <span>if</span> <span>(</span>vm<span>.</span>$options<span>.</span>el<span>)</span> <span>{</span> <span>//new Vue(options)时，options如果包含$el选项，则将vue实例挂载到el指定的dom对象上</span>
      <span>/*挂载组件*/</span>
      <span>/**$mount 这个方法的实现是和平台、构建方式都相关的，不同版本vue对$mount()方法实现不同：
       * runtime+compiler版本的$mount()实现在src/platform/web/entry-runtime-with-compiler.js
       * 只有runtime版本的实现在src/platform/web/runtime/index.js
       * weex平台下实现在src/platform/weex/runtime/index.js
       */</span>
      vm<span>.</span><span>$mount</span><span>(</span>vm<span>.</span>$options<span>.</span>el<span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br></div></div><ul>
<li>Vue 初始化主要就干了几件事情: 合并配置，初始化生命周期，初始化事件中心，初始化渲染，初始化 data、props、computed、watcher 等等。</li>
</ul>
</li>
<li>
<p>总结</p>
<ul>
<li>
<p>Vue 的初始化逻辑写的非常清楚，把不同的功能逻辑拆成一些单独的函数执行，让主线逻辑一目了然，这样的编程思想是非常值得借鉴和学习的。</p>
</li>
<li>
<p>在初始化的最后，检测到如果有 <code>el</code> 属性，则调用 <code>vm.$mount</code> 方法挂载 <code>vm</code>，挂载的目标就是把模板渲染成最终的 DOM，那么接下来就是 Vue 的挂载过程。</p>
</li>
</ul>
</li>
</ul>
<h3 id="响应式"> 响应式</h3>
<p><strong>响应式系统：</strong></p>
<ul>
<li>
<p>Vue.js  是一款  MVVM  框架，数据模型仅仅是普通的  JavaScript  对象，但是对这些对象进行操作时，却能影响对应视图，它的核心实现就是「 响应式系统」。</p>
</li>
<li>
<p>响应式系统核心使用 ES5的 <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/defineProperty" target="_blank" rel="noopener noreferrer">Object.defineProperty()</a> API 实现 ：</p>
<ul>
<li>参数：第一个参数是需要定义属性的对象，第二个 参数是需要定义的属性，第三个是该属性描述符。</li>
<li>属性的描述符有四个属性：value 属性的值，writable 属性是否可写， enumerable 属性是否可枚举，configurable 属性是否可配置修改。</li>
<li>缺陷：有一些对属性的操作，使用这种方法无法拦截：比如说通过下标方式修改数组数据或者给对象新 增属性
<ul>
<li>vue 内部通过重写函数解决了这个问题：在 Vue3.0 中已经不使用这种方式了，而是 通过使用 Proxy 对对象进行代理，从而实现数据劫持。</li>
<li>使用 Proxy 的好处是它可以完美的监 听到任何方式的数据改变，唯一的缺点是兼容性的问题，因为这是 ES6 的语法。</li>
</ul>
</li>
</ul>
<p><img src="./../flow-graph/响应式流程.png" alt="" /></p>
</li>
<li>
<p>流程：</p>
<ul>
<li>**创建实例：**新建一个vue实例，在构造函数中调用 oserver() 传入 data 数据并创建一个观察者对象;</li>
<li>**定义响应式数据：**observer 函数遍历 data 的属性，调用 defineReactive，在方法内创建一个依赖收集对象并使用 Object.defineProperty 修改data 的属性</li>
<li>**依赖收集：**runder funtion 读取数据时，触发数据的 getter()，在属性的 getter 中往依赖收集对象中添加属性的 watcher对象；</li>
<li>**派发更新：**修改数据时，触发数据的 setter()，在 setter 中调用依赖收集对象的 notify 方法，notify 方法再调用对应的 watcher 对象的update 方法更新视图。</li>
</ul>
</li>
<li>
<p>简单实现：</p>
<div><pre><code><span>/**
 * 依赖收集对象
 */</span>
<span>class</span> <span>Dep</span> <span>{</span>
  <span>constructor</span><span>(</span><span>)</span> <span>{</span>
    <span>//存放watcher类的数组</span>
    <span>this</span><span>.</span>subs <span>=</span> <span>[</span><span>]</span>
  <span>}</span>
  <span>//添加watcher类</span>
  <span>addSub</span><span>(</span><span>sub</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>subs<span>.</span><span>push</span><span>(</span>sub<span>)</span>
  <span>}</span>
  <span>//通知更新视图方法</span>
  <span>notify</span><span>(</span><span>)</span> <span>{</span>
    <span>//遍历watcher类,调用update更新视图</span>
    <span>this</span><span>.</span>subs<span>.</span><span>forEach</span><span>(</span><span>(</span><span>sub</span><span>)</span> <span>=></span> <span>{</span>
      sub<span>.</span><span>update</span><span>(</span><span>)</span>
    <span>}</span><span>)</span>
  <span>}</span>
<span>}</span>

<span>/**
 * 观察者类
 */</span>
<span>class</span> <span>Watcher</span> <span>{</span>
  <span>constructor</span><span>(</span><span>)</span> <span>{</span>
    <span>//在 new 一个 Watcher 对象时将该对象赋值给 Dep.target，在 get 中会用到</span>
    Dep<span>.</span>target <span>=</span> <span>this</span>
  <span>}</span>
  <span>//模拟更新视图方法</span>
  <span>update</span><span>(</span><span>val</span><span>)</span> <span>{</span>

    <span>// 默认情况下都会进入else的分支，同步则直接调用watcher的run方法</span>
    <span>if</span> <span>(</span><span>this</span><span>.</span>lazy<span>)</span> <span>{</span>
      <span>this</span><span>.</span>dirty <span>=</span> <span>true</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>this</span><span>.</span>sync<span>)</span> <span>{</span>
      <span>this</span><span>.</span><span>run</span><span>(</span><span>)</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>queueWatcher</span><span>(</span><span>this</span><span>)</span> 
    <span>}</span>

  <span>}</span>
<span>}</span>

<span>/**
 * 异步更新队列
 * @param {Watcher} watcher 
 */</span>
<span>function</span> <span>queueWatcher</span><span>(</span><span>watcher</span><span>)</span> <span>{</span>
  <span>//将观察者对象添加进入队列等操作</span>

  <span>//...</span>

  <span>// 调用nextTick在此次事件循环结尾的时候刷新watcher队列检查的dom更新操作</span>
  <span>nextTick</span><span>(</span>flushSchedulerQueue<span>)</span>
<span>}</span>


<span>/**
 * 定义响应式方法
 * @param {*} obj 
 * @param {*} key 
 * @param {*} val 
 */</span>
<span>function</span> <span>defineReactive</span><span>(</span><span>obj<span>,</span> key<span>,</span> val</span><span>)</span> <span>{</span>
  <span>const</span> dep <span>=</span> <span>new</span> <span>Dep</span><span>(</span><span>)</span> <span>// 创建数据的依赖收集对象</span>
  Object<span>.</span><span>defineProperty</span><span>(</span>obj<span>,</span> key<span>,</span> <span>{</span>
    enumerable<span>:</span> <span>true</span><span>,</span>       <span>//属性可枚举 </span>
    configurable<span>:</span> <span>true</span><span>,</span>     <span>// 属性可被修改或删除 </span>
    <span>get</span><span>:</span> <span>function</span> <span>reactiveGetter</span><span>(</span><span>)</span> <span>{</span> <span>//渲染时触发getter依赖收集</span>
      dep<span>.</span><span>addSub</span><span>(</span>Dep<span>.</span>target<span>)</span> <span>//依赖收集:把 Watcher 实例存放到对应的 Dep 对象中去</span>
      <span>return</span> val
    <span>}</span><span>,</span>
    <span>set</span><span>:</span> <span>function</span> <span>reactiveSetter</span><span>(</span><span>newVal</span><span>)</span> <span>{</span> <span>/*设置数据时触发setter更新视图 */</span>
      <span>if</span> <span>(</span>newVal <span>===</span> val<span>)</span> <span>return</span>
      val <span>=</span> newVal <span>//更新数据</span>
      dep<span>.</span><span>notify</span><span>(</span><span>)</span> <span>//依赖收集对象通知视图更新</span>
    <span>}</span>
  <span>}</span><span>)</span>
<span>}</span>

<span>/**
 * 观察者方法
 * @param {*} value 
 */</span>
<span>function</span> <span>observer</span><span>(</span><span>value</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>!</span>value <span>||</span> <span>(</span><span>typeof</span> value <span>!==</span> <span>'object'</span><span>)</span><span>)</span> <span>{</span>
    <span>return</span>
  <span>}</span>
  <span>//遍历对象属性，给对象每一个值重写setter和getter</span>
  Object<span>.</span><span>keys</span><span>(</span>value<span>)</span><span>.</span><span>forEach</span><span>(</span><span>(</span><span>key</span><span>)</span> <span>=></span> <span>{</span>
    <span>defineReactive</span><span>(</span>value<span>,</span> key<span>,</span> value<span>[</span>key<span>]</span><span>)</span>
  <span>}</span><span>)</span>
<span>}</span>


<span>/**
 * vue类
 */</span>
<span>class</span> <span>Vue</span> <span>{</span>
  <span>/* Vue 构造类 */</span>
  <span>constructor</span><span>(</span><span>options</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>_data <span>=</span> options<span>.</span>data
    <span>observer</span><span>(</span><span>this</span><span>.</span>_data<span>)</span>
    <span>new</span> <span>Watcher</span><span>(</span><span>)</span> <span>// 新建一个Watcher观察者对象，这时候Dep.target会指向这个Watcher对象</span>
    console<span>.</span><span>log</span><span>(</span><span>'当前数据'</span><span>,</span> <span>this</span><span>.</span>_data<span>.</span>test<span>)</span>
  <span>}</span>
<span>}</span>


<span>let</span> testVue <span>=</span> <span>new</span> <span>Vue</span><span>(</span><span>{</span>
  data<span>:</span> <span>{</span>
    test<span>:</span> <span>"测试数据"</span>
  <span>}</span>
<span>}</span><span>)</span>
testVue<span>.</span>_data<span>.</span>test <span>=</span> <span>"hello,test."</span>

Dep<span>.</span>target <span>=</span> <span>null</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br><span>93</span><br><span>94</span><br><span>95</span><br><span>96</span><br><span>97</span><br><span>98</span><br><span>99</span><br><span>100</span><br><span>101</span><br><span>102</span><br><span>103</span><br><span>104</span><br><span>105</span><br><span>106</span><br><span>107</span><br><span>108</span><br><span>109</span><br><span>110</span><br><span>111</span><br><span>112</span><br><span>113</span><br><span>114</span><br><span>115</span><br><span>116</span><br><span>117</span><br><span>118</span><br></div></div></li>
<li>
<p>依赖收集对象 Dep 和 观察者对象 Watcher:</p>
<ul>
<li>
<p>对象结构：</p>
<p><img src="./../images/dep&watcher.png" alt="" /></p>
</li>
<li>
<p>对象关系:</p>
<p><img src="./../images/dep&watcher2.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
<p><strong>源码分析：</strong></p>
<ul>
<li>
<p>总体流程图：<img src="./../flow-graph/响应式初始化流程.png" alt="" /></p>
<ul>
<li>
<p><strong><code>initState</code></strong>： 在 Vue 的初始化阶段，<code>_init</code> 方法执行的时候，会执行 <code>initState(vm)</code> 方法: 主要是对 <code>props</code>、<code>methods</code>、<code>data</code>、<code>computed</code> 和 <code>wathcer</code> 等属性做了初始化操作。它的定义在 <code>src/core/instance/state.js</code></p>
<div><pre><code><span>/*初始化props、methods、data、computed与watch*/</span>
<span>export</span> <span>function</span> <span>initState</span> <span>(</span><span>vm<span>:</span> Component</span><span>)</span> <span>{</span>
  vm<span>.</span>_watchers <span>=</span> <span>[</span><span>]</span>
  <span>const</span> opts <span>=</span> vm<span>.</span>$options <span>//获取创建vue实例时传入的options参数</span>
  <span>/*初始化props*/</span>
  <span>if</span> <span>(</span>opts<span>.</span>props<span>)</span> <span>initProps</span><span>(</span>vm<span>,</span> opts<span>.</span>props<span>)</span>
  <span>/*初始化方法*/</span>
  <span>if</span> <span>(</span>opts<span>.</span>methods<span>)</span> <span>initMethods</span><span>(</span>vm<span>,</span> opts<span>.</span>methods<span>)</span>
  <span>/*初始化data*/</span>
  <span>if</span> <span>(</span>opts<span>.</span>data<span>)</span> <span>{</span>
    <span>initData</span><span>(</span>vm<span>)</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>/*该组件没有data的时候绑定一个空对象*/</span>
    <span>observe</span><span>(</span>vm<span>.</span>_data <span>=</span> <span>{</span><span>}</span><span>,</span> <span>true</span> <span>/* asRootData */</span><span>)</span>
  <span>}</span>
  <span>/*初始化computed*/</span>
  <span>if</span> <span>(</span>opts<span>.</span>computed<span>)</span> <span>initComputed</span><span>(</span>vm<span>,</span> opts<span>.</span>computed<span>)</span>
  <span>/*初始化watchers*/</span>
  <span>if</span> <span>(</span>opts<span>.</span>watch<span>)</span> <span>initWatch</span><span>(</span>vm<span>,</span> opts<span>.</span>watch<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br></div></div></li>
<li>
<p><strong>initProps 方法</strong>：遍历定义的 <code>props</code> 配置。遍历的过程主要做两件事情：</p>
<ul>
<li>
<p>一个是调用 <code>defineReactive</code> 方法把每个 <code>prop</code> 对应的值变成响应式，可以通过 <code>vm._props.xxx</code> 访问到定义 <code>props</code> 中对应的属性。</p>
</li>
<li>
<p>另一个是通过 <code>proxy</code> 把 <code>vm._props.xxx</code> 的访问代理到 <code>vm.xxx</code> 上</p>
<div><pre><code><span>/*初始化props*/</span>
<span>function</span> <span>initProps</span> <span>(</span><span>vm<span>:</span> Component<span>,</span> propsOptions<span>:</span> Object</span><span>)</span> <span>{</span>
  <span>const</span> propsData <span>=</span> vm<span>.</span>$options<span>.</span>propsData <span>||</span> <span>{</span><span>}</span> <span>//获取组件实例的props</span>
  <span>const</span> props <span>=</span> vm<span>.</span>_props <span>=</span> <span>{</span><span>}</span>
  <span>// cache prop keys so that future props updates can iterate using Array</span>
  <span>// instead of dynamic object key enumeration.</span>
  <span>/*缓存属性的key，使得将来能直接使用数组的索引值来更新props来替代动态地枚举对象*/</span>
  <span>const</span> keys <span>=</span> vm<span>.</span>$options<span>.</span>_propKeys <span>=</span> <span>[</span><span>]</span>
  <span>/*根据$parent是否存在来判断当前是否是根结点*/</span>
  <span>const</span> isRoot <span>=</span> <span>!</span>vm<span>.</span>$parent
  <span>// root instance props should be converted</span>
  <span>/*根结点会给shouldConvert赋true，根结点的props应该被转换*/</span>
  observerState<span>.</span>shouldConvert <span>=</span> isRoot
  <span>for</span> <span>(</span><span>const</span> key <span>in</span> propsOptions<span>)</span> <span>{</span>
    <span>/*props的key值存入keys（_propKeys）中*/</span>
    keys<span>.</span><span>push</span><span>(</span>key<span>)</span>
    <span>/*验证prop,不存在用默认值替换，类型为bool则声称true或false，当使用default中的默认值的时候会将默认值的副本进行observe*/</span>
    <span>const</span> value <span>=</span> <span>validateProp</span><span>(</span>key<span>,</span> propsOptions<span>,</span> propsData<span>,</span> vm<span>)</span>
    <span>/* istanbul ignore else */</span>
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
      <span>/*判断是否是保留字段，如果是则发出warning*/</span>
      <span>if</span> <span>(</span>isReservedProp<span>[</span>key<span>]</span> <span>||</span> config<span>.</span><span>isReservedAttr</span><span>(</span>key<span>)</span><span>)</span> <span>{</span>
        <span>warn</span><span>(</span>
          <span><span>`</span><span>"</span><span><span>${</span>key<span>}</span></span><span>" is a reserved attribute and cannot be used as component prop.</span><span>`</span></span><span>,</span>
          vm
        <span>)</span>
      <span>}</span>
      <span>defineReactive</span><span>(</span>props<span>,</span> key<span>,</span> value<span>,</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
        <span>/*
          由于父组件重新渲染的时候会重写prop的值，所以应该直接使用prop来作为一个data或者计算属性的依赖
          https://cn.vuejs.org/v2/guide/components.html#字面量语法-vs-动态语法
        */</span>
        <span>if</span> <span>(</span>vm<span>.</span>$parent <span>&amp;&amp;</span> <span>!</span>observerState<span>.</span>isSettingProps<span>)</span> <span>{</span>
          <span>warn</span><span>(</span>
            <span><span>`</span><span>Avoid mutating a prop directly since the value will be </span><span>`</span></span> <span>+</span>
            <span><span>`</span><span>overwritten whenever the parent component re-renders. </span><span>`</span></span> <span>+</span>
            <span><span>`</span><span>Instead, use a data or computed property based on the prop's </span><span>`</span></span> <span>+</span>
            <span><span>`</span><span>value. Prop being mutated: "</span><span><span>${</span>key<span>}</span></span><span>"</span><span>`</span></span><span>,</span>
            vm
          <span>)</span>
        <span>}</span>
      <span>}</span><span>)</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>//对属性进行定义响应式</span>
      <span>defineReactive</span><span>(</span>props<span>,</span> key<span>,</span> value<span>)</span>
    <span>}</span>
    <span>// static props are already proxied on the component's prototype</span>
    <span>// during Vue.extend(). We only need to proxy props defined at</span>
    <span>// instantiation here.</span>
    <span>/*Vue.extend()期间，静态prop已经在组件原型上代理了，我们只需要在这里进行代理prop
    通过 proxy 把 vm._props.xxx 的访问代理到 vm.xxx 上*/</span>
    <span>if</span> <span>(</span><span>!</span><span>(</span>key <span>in</span> vm<span>)</span><span>)</span> <span>{</span>
      <span>proxy</span><span>(</span>vm<span>,</span> <span><span>`</span><span>_props</span><span>`</span></span><span>,</span> key<span>)</span>
    <span>}</span>
  <span>}</span>
  observerState<span>.</span>shouldConvert <span>=</span> <span>true</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br></div></div></li>
</ul>
</li>
<li>
<p><strong>initData 方法</strong>： 初始化 data ，主要过程也是做两件事：</p>
<ul>
<li>
<p>一个是对定义 data 函数返回对象的遍历，通过 proxy 把每一个值 vm._data.xxx 都代理到 vm.xxx 上；</p>
</li>
<li>
<p>另一个是调用 observe 方法观测整个 data 的变化，把 data 也变成响应式，可以通过 vm._data.xxx 访问到定义 data 返回函数中对应的属性并检查data对象中是否与props对象中存在同名key</p>
<div><pre><code><span>function</span> <span>initData</span> <span>(</span><span>vm<span>:</span> Component</span><span>)</span> <span>{</span>

  <span>/*得到data数据*/</span>
  <span>let</span> data <span>=</span> vm<span>.</span>$options<span>.</span>data <span>//获取vue实例对象中设置的data对象</span>
  data <span>=</span> vm<span>.</span>_data <span>=</span> <span>typeof</span> data <span>===</span> <span>'function'</span> <span>//判断data对象是否为funtion,不是直接设置data</span>
    <span>?</span> <span>getData</span><span>(</span>data<span>,</span> vm<span>)</span>
    <span>:</span> data <span>||</span> <span>{</span><span>}</span>

  <span>/*对对象类型进行严格检查，只有当对象是纯javascript对象的时候返回true*/</span>
  <span>if</span> <span>(</span><span>!</span><span>isPlainObject</span><span>(</span>data<span>)</span><span>)</span> <span>{</span>
    data <span>=</span> <span>{</span><span>}</span>
    process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> <span>warn</span><span>(</span>
      <span>'data functions should return an object:\n'</span> <span>+</span>
      <span>'https://vuejs.org/v2/guide/components.html#data-Must-Be-a-Function'</span><span>,</span>
      vm
    <span>)</span>
  <span>}</span>
  <span>// proxy data on instance</span>
  <span>/*遍历data对象*/</span>
  <span>const</span> keys <span>=</span> Object<span>.</span><span>keys</span><span>(</span>data<span>)</span>
  <span>const</span> props <span>=</span> vm<span>.</span>$options<span>.</span>props
  <span>let</span> i <span>=</span> keys<span>.</span>length

  <span>//遍历data中的数据</span>
  <span>while</span> <span>(</span>i<span>--</span><span>)</span> <span>{</span>

    <span>/*保证data中的key不与props中的key重复，props优先，如果有冲突会产生warning*/</span>
    <span>if</span> <span>(</span>props <span>&amp;&amp;</span> <span>hasOwn</span><span>(</span>props<span>,</span> keys<span>[</span>i<span>]</span><span>)</span><span>)</span> <span>{</span> <span>//props对象存在并且props中存在与data同名Key</span>
      process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> <span>warn</span><span>(</span>
        <span><span>`</span><span>The data property "</span><span><span>${</span>keys<span>[</span>i<span>]</span><span>}</span></span><span>" is already declared as a prop. </span><span>`</span></span> <span>+</span>
        <span><span>`</span><span>Use prop default value instead.</span><span>`</span></span><span>,</span>
        vm
      <span>)</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>!</span><span>isReserved</span><span>(</span>keys<span>[</span>i<span>]</span><span>)</span><span>)</span> <span>{</span> <span>//data对象的key不为保存关键字</span>
      <span>/*判断是否是保留字段*/</span>

      <span>/*将data上面的属性代理到了vm实例上（_data保存了vue实例传入的data对象：132行）
      通过 proxy 把 vm._data.xxx 的访问代理到 vm.xxx 上
      */</span>
      <span>proxy</span><span>(</span>vm<span>,</span> <span><span>`</span><span>_data</span><span>`</span></span><span>,</span> keys<span>[</span>i<span>]</span><span>)</span>
    <span>}</span>
  <span>}</span>
  <span>// observe data</span>
  <span>/*从这里开始我们要observe了，开始对数据进行绑定，这里有尤大大的注释asRootData，这步作为根数据，下面会进行递归observe进行对深层对象的绑定。*/</span>
  <span>observe</span><span>(</span>data<span>,</span> <span>true</span> <span>/* asRootData */</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br></div></div></li>
</ul>
</li>
<li>
<p><strong>proxy 方法</strong>：把 <code>props</code> 和 <code>data</code> 上的属性代理到 <code>vm</code> 实例上。</p>
<ul>
<li>
<p>通过 proxy 当定义了如下 props，却可以通过 vm 实例访问到它。</p>
<div><pre><code><span>let</span> comP <span>=</span> <span>{</span>
  props<span>:</span> <span>{</span>
    msg<span>:</span> <span>'hello'</span>
  <span>}</span><span>,</span>
  methods<span>:</span> <span>{</span>
    <span>say</span><span>(</span><span>)</span> <span>{</span>
      console<span>.</span><span>log</span><span>(</span><span>this</span><span>.</span>msg<span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>j
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div></li>
<li>
<p>实现：通过 <code>Object.defineProperty</code> 把 <code>target[sourceKey][key]</code> 的读写变成了对 <code>target[key]</code> 的读写。所以对于 <code>props</code> 而言，对 <code>vm._props.xxx</code> 的读写变成了 <code>vm.xxx</code> 的读写，而对于 <code>vm._props.xxx</code> 我们可以访问到定义在 <code>props</code> 中的属性，所以我们就可以通过 <code>vm.xxx</code> 访问到定义在 <code>props</code> 中的 <code>xxx</code> 属性了。同理，对于 <code>data</code> 而言，对 <code>vm._data.xxxx</code> 的读写变成了对 <code>vm.xxxx</code> 的读写，而对于 <code>vm._data.xxxx</code> 我们可以访问到定义在 <code>data</code> 函数返回对象中的属性，所以我们就可以通过 <code>vm.xxxx</code> 访问到定义在 <code>data</code> 函数返回对象中的 <code>xxxx</code> 属性了。</p>
<div><pre><code><span>/*通过proxy函数将_data（或者_props等）上面的数据代理到vm上，这样就可以用app.text代替app._data.text了。*/</span>
<span>export</span> <span>function</span> <span>proxy</span> <span>(</span><span>target<span>:</span> Object<span>,</span> sourceKey<span>:</span> string<span>,</span> key<span>:</span> string</span><span>)</span> <span>{</span>
  sharedPropertyDefinition<span>.</span><span>get</span> <span>=</span> <span>function</span> <span>proxyGetter</span> <span>(</span><span>)</span> <span>{</span> <span>//定义getter</span>
    <span>return</span> <span>this</span><span>[</span>sourceKey<span>]</span><span>[</span>key<span>]</span>
  <span>}</span>
  sharedPropertyDefinition<span>.</span><span>set</span> <span>=</span> <span>function</span> <span>proxySetter</span> <span>(</span><span>val</span><span>)</span> <span>{</span> <span>//定义setter</span>
    <span>this</span><span>[</span>sourceKey<span>]</span><span>[</span>key<span>]</span> <span>=</span> val
  <span>}</span>
  <span>//对vm对象的key属性定义setter和getter方法，当访问vm.key时，既触发getter方法，返回vm.sourceKey.key</span>
  Object<span>.</span><span>defineProperty</span><span>(</span>target<span>,</span> key<span>,</span> sharedPropertyDefinition<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div></li>
</ul>
</li>
<li>
<p><strong><code>Observer</code> 类</strong>：它的作用是给对象的属性添加 getter 和 setter，用于依赖收集和派发更新：</p>
<div><pre><code><span>/*
  每个被观察到对象被附加上观察者实例，一旦被添加，观察者将为目标对象加上getter\setter属性，
  进行依赖收集以及调度更新。
*/</span>
<span>export</span> <span>class</span> <span>Observer</span> <span>{</span>
  value<span>:</span> any<span>;</span>
  dep<span>:</span> Dep<span>;</span>
  vmCount<span>:</span> number<span>;</span> <span>// number of vms that has this object as root $data</span>

  <span>constructor</span><span>(</span><span>value<span>:</span> any</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>value <span>=</span> value
    <span>this</span><span>.</span>dep <span>=</span> <span>new</span> <span>Dep</span><span>(</span><span>)</span>
    <span>this</span><span>.</span>vmCount <span>=</span> <span>0</span>
    <span>/* 将Observer实例绑定到data的__ob__属性上面去，data的__ob__属性保存数据对应的观察者对象
    observe的时候会先检测是否已经有__ob__对象存放Observer实例了*/</span>
    <span>def</span><span>(</span>value<span>,</span> <span>'__ob__'</span><span>,</span> <span>this</span><span>)</span>
    <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>value<span>)</span><span>)</span> <span>{</span>
 <span>/*如果是数组，将修改后可以截获响应的数组方法替换掉该数组的原型中的原生方法，达到监听数组数据变化响应的效果。
 这里如果当前浏览器支持__proto__属性，则直接覆盖当前数组对象原型上的原生数组方法，如果不支持该属性，则直接覆盖数组对象的原型*/</span>
      <span>const</span> augment <span>=</span> hasProto
        <span>?</span> protoAugment  <span>/*直接覆盖原型的方法来修改目标对象*/</span>
        <span>:</span> copyAugment   <span>/*定义（覆盖）目标对象或数组的某一个方法*/</span>
      <span>augment</span><span>(</span>value<span>,</span> arrayMethods<span>,</span> arrayKeys<span>)</span>
 
      <span>/*如果是数组则需要遍历数组的每一个成员进行observe*/</span>
      <span>this</span><span>.</span><span>observeArray</span><span>(</span>value<span>)</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>/*如果是对象则直接walk进行绑定*/</span>
      <span>this</span><span>.</span><span>walk</span><span>(</span>value<span>)</span>
    <span>}</span>
  <span>}</span>

  <span>/**
   * Walk through each property and convert them into
   * getter/setters. This method should only be called when
   * value type is Object.
   */</span>
  <span>/* 遍历每一个对象并且在它们上面绑定getter与setter,这个方法只有在value的类型是对象的时候才能被调用 */</span>
  <span>walk</span><span>(</span><span>obj<span>:</span> Object</span><span>)</span> <span>{</span>
    <span>const</span> keys <span>=</span> Object<span>.</span><span>keys</span><span>(</span>obj<span>)</span>
    <span>/*walk方法会遍历对象的每一个属性进行defineReactive绑定*/</span>
    <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> keys<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
      <span>defineReactive</span><span>(</span>obj<span>,</span> keys<span>[</span>i<span>]</span><span>,</span> obj<span>[</span>keys<span>[</span>i<span>]</span><span>]</span><span>)</span>
    <span>}</span>
  <span>}</span>

  <span>/**
   * Observe a list of Array items.
   */</span>
  <span>/*对一个数组的每一个成员进行observe*/</span>
  <span>observeArray</span><span>(</span><span>items<span>:</span> Array<span>&lt;</span>any<span>></span></span><span>)</span> <span>{</span>
    <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>,</span> l <span>=</span> items<span>.</span>length<span>;</span> i <span>&lt;</span> l<span>;</span> i<span>++</span><span>)</span> <span>{</span>
      <span>/*数组需要遍历每一个成员进行observe*/</span>
      <span>observe</span><span>(</span>items<span>[</span>i<span>]</span><span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br></div></div><ul>
<li><code>Observer</code> 的构造函数逻辑:</li>
<li>首先实例化 <code>Dep</code> 对象，接着通过执行 <code>def</code> 函数把自身实例添加到数据对象 <code>value</code> 的 <code>__ob__</code> 属性上; <code>def</code> 方法就是对 <code>Object.defineProperty</code> 的简单封装。</li>
<li>接着进行判断：</li>
<li>如果是数组则需要遍历数组的每一个成员进行observe。observe 会对对象/数组的子对象递归进行observe。
<ul>
<li>如果是对象则直接walk进行绑定：遍历对象 key 给每个属性定义响应式，同样会调用observe 会对对象/数组的子对象递归进行observe。</li>
</ul>
</li>
<li>由于<code>Observer</code>中需要对数组和对象的遍历观测，因此 Vue2.x 中响应式过程存在性能损耗。</li>
</ul>
</li>
<li>
<p><code>observe</code> 函数：给非 VNode 的对象类型数据添加一个 <code>Observer</code>，如果已经添加过则直接返回，否则在满足一定条件下去实例化一个 <code>Observer</code> 对象实例，它的定义在 <code>src/core/observer/index.js</code> 中：</p>
<div><pre><code>  <span>/*对象的子对象递归进行observe并返回子节点的Observer对象*/</span>
  <span>let</span> childOb <span>=</span> <span>observe</span><span>(</span>val<span>)</span>
  Object<span>.</span><span>defineProperty</span><span>(</span>obj<span>,</span> key<span>,</span> <span>{</span>
    enumerable<span>:</span> <span>true</span><span>,</span>
    configurable<span>:</span> <span>true</span><span>,</span>
    <span>get</span><span>:</span> <span>function</span> <span>reactiveGetter</span><span>(</span><span>)</span> <span>{</span>
      <span>/*如果原本对象拥有getter方法则执行*/</span>
      <span>const</span> value <span>=</span> getter <span>?</span> <span>getter</span><span>.</span><span>call</span><span>(</span>obj<span>)</span> <span>:</span> val
      <span>if</span> <span>(</span>Dep<span>.</span>target<span>)</span> <span>{</span>
        <span>/*进行依赖收集*/</span>
        dep<span>.</span><span>depend</span><span>(</span><span>)</span>
        <span>if</span> <span>(</span>childOb<span>)</span> <span>{</span>
          <span>/*子对象进行依赖收集，其实就是将同一个watcher观察者实例放进了两个depend中，
          一个是正在本身闭包中的depend，另一个是子元素的depend*/</span>
          childOb<span>.</span>dep<span>.</span><span>depend</span><span>(</span><span>)</span>
        <span>}</span>
        <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>value<span>)</span><span>)</span> <span>{</span>
          <span>/*是数组则需要对每一个成员都进行依赖收集，如果数组的成员还是数组，则递归。*/</span>
          <span>dependArray</span><span>(</span>value<span>)</span>
        <span>}</span>
      <span>}</span>
      <span>return</span> value
    <span>}</span><span>,</span>
    <span>set</span><span>:</span> <span>function</span> <span>reactiveSetter</span><span>(</span><span>newVal</span><span>)</span> <span>{</span>
      <span>/*通过getter方法获取当前值，与新值进行比较，一致则不需要执行下面的操作*/</span>
      <span>const</span> value <span>=</span> getter <span>?</span> <span>getter</span><span>.</span><span>call</span><span>(</span>obj<span>)</span> <span>:</span> val
      <span>/* eslint-disable no-self-compare */</span>
      <span>if</span> <span>(</span>newVal <span>===</span> value <span>||</span> <span>(</span>newVal <span>!==</span> newVal <span>&amp;&amp;</span> value <span>!==</span> value<span>)</span><span>)</span> <span>{</span>
        <span>return</span>
      <span>}</span>
      <span>/* eslint-enable no-self-compare */</span>
      <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> customSetter<span>)</span> <span>{</span>
        <span>customSetter</span><span>(</span><span>)</span>
      <span>}</span>
      <span>if</span> <span>(</span>setter<span>)</span> <span>{</span>
        <span>/*如果原本对象拥有setter方法则执行setter*/</span>
        <span>setter</span><span>.</span><span>call</span><span>(</span>obj<span>,</span> newVal<span>)</span>
      <span>}</span> <span>else</span> <span>{</span>
        val <span>=</span> newVal
      <span>}</span>
      <span>/*新的值需要重新进行observe，保证数据响应式*/</span>
      childOb <span>=</span> <span>observe</span><span>(</span>newVal<span>)</span>
      <span>/*dep对象通知所有的观察者*/</span>
    dep<span>.</span><span>notify</span><span>(</span><span>)</span>
    <span>}</span>
<span>}</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br></div></div></li>
<li>
<p><code>defineReactive</code> 函数： 定义一个响应式对象，给对象动态添加 getter 和 setter。</p>
</li>
<li>
<p>过程：</p>
<ul>
<li>初始化 <code>Dep</code> 对象的实例，接着拿到 <code>obj</code> 的属性描述符；</li>
<li>然后对子对象递归调用 <code>observe</code> 方法，这样就保证了无论 <code>obj</code> 的结构多复杂，它的所有子属性也能变成响应式的对象，这样我们访问或修改 <code>obj</code> 中一个嵌套较深的属性，也能触发 getter 和 setter；</li>
<li>最后利用 <code>Object.defineProperty</code> 去给 <code>obj</code> 的属性 <code>key</code> 添加 getter 和 setter。</li>
<li>它的定义在 <code>src/core/observer/index.js</code> 中：</li>
</ul>
<div><pre><code><span>/*为对象defineProperty上在变化时通知的属性*/</span>
<span>export</span> <span>function</span> <span>defineReactive</span><span>(</span>
  <span>obj<span>:</span> Object<span>,</span>
  key<span>:</span> string<span>,</span>
  val<span>:</span> any<span>,</span>
  customSetter<span>?</span><span>:</span> Function</span>
<span>)</span> <span>{</span>
  <span>/*在闭包中定义一个dep对象*/</span>
  <span>const</span> dep <span>=</span> <span>new</span> <span>Dep</span><span>(</span><span>)</span>

  <span>const</span> property <span>=</span> Object<span>.</span><span>getOwnPropertyDescriptor</span><span>(</span>obj<span>,</span> key<span>)</span>
  <span>if</span> <span>(</span>property <span>&amp;&amp;</span> property<span>.</span>configurable <span>===</span> <span>false</span><span>)</span> <span>{</span>
    <span>return</span>
  <span>}</span>

  <span>/*如果之前该对象已经预设了getter以及setter函数则将其取出来，新定义的getter/setter中会将其执行，
  保证不会覆盖之前已经定义的getter/setter。*/</span>
  <span>// cater for pre-defined getter/setters</span>
  <span>const</span> getter <span>=</span> property <span>&amp;&amp;</span> property<span>.</span>get
  <span>const</span> setter <span>=</span> property <span>&amp;&amp;</span> property<span>.</span>set

  <span>/*对象的子对象递归进行observe并返回子节点的Observer对象*/</span>
  <span>let</span> childOb <span>=</span> <span>observe</span><span>(</span>val<span>)</span>
  Object<span>.</span><span>defineProperty</span><span>(</span>obj<span>,</span> key<span>,</span> <span>{</span>
    enumerable<span>:</span> <span>true</span><span>,</span>
    configurable<span>:</span> <span>true</span><span>,</span>
    <span>get</span><span>:</span> <span>function</span> <span>reactiveGetter</span><span>(</span><span>)</span> <span>{</span>
      <span>/*如果原本对象拥有getter方法则执行*/</span>
      <span>const</span> value <span>=</span> getter <span>?</span> <span>getter</span><span>.</span><span>call</span><span>(</span>obj<span>)</span> <span>:</span> val
      <span>if</span> <span>(</span>Dep<span>.</span>target<span>)</span> <span>{</span>
        <span>/*进行依赖收集*/</span>
        dep<span>.</span><span>depend</span><span>(</span><span>)</span>
        <span>if</span> <span>(</span>childOb<span>)</span> <span>{</span>
          <span>/*子对象进行依赖收集，其实就是将同一个watcher观察者实例放进了两个depend中，
          一个是正在本身闭包中的depend，另一个是子元素的depend*/</span>
          childOb<span>.</span>dep<span>.</span><span>depend</span><span>(</span><span>)</span>
        <span>}</span>
        <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>value<span>)</span><span>)</span> <span>{</span>
          <span>/*是数组则需要对每一个成员都进行依赖收集，如果数组的成员还是数组，则递归。*/</span>
          <span>dependArray</span><span>(</span>value<span>)</span>
        <span>}</span>
      <span>}</span>
      <span>return</span> value
    <span>}</span><span>,</span>
    <span>set</span><span>:</span> <span>function</span> <span>reactiveSetter</span><span>(</span><span>newVal</span><span>)</span> <span>{</span>
      <span>/*通过getter方法获取当前值，与新值进行比较，一致则不需要执行下面的操作*/</span>
      <span>const</span> value <span>=</span> getter <span>?</span> <span>getter</span><span>.</span><span>call</span><span>(</span>obj<span>)</span> <span>:</span> val
      <span>/* eslint-disable no-self-compare */</span>
      <span>if</span> <span>(</span>newVal <span>===</span> value <span>||</span> <span>(</span>newVal <span>!==</span> newVal <span>&amp;&amp;</span> value <span>!==</span> value<span>)</span><span>)</span> <span>{</span>
        <span>return</span>
      <span>}</span>
      <span>/* eslint-enable no-self-compare */</span>
      <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> customSetter<span>)</span> <span>{</span>
        <span>customSetter</span><span>(</span><span>)</span>
      <span>}</span>
      <span>if</span> <span>(</span>setter<span>)</span> <span>{</span>
        <span>/*如果原本对象拥有setter方法则执行setter*/</span>
        <span>setter</span><span>.</span><span>call</span><span>(</span>obj<span>,</span> newVal<span>)</span>
      <span>}</span> <span>else</span> <span>{</span>
        val <span>=</span> newVal
      <span>}</span>
      <span>/*新的值需要重新进行observe，保证数据响应式*/</span>
      childOb <span>=</span> <span>observe</span><span>(</span>newVal<span>)</span>
      <span>/*dep对象通知所有的观察者*/</span>
      dep<span>.</span><span>notify</span><span>(</span><span>)</span>
    <span>}</span>
  <span>}</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br></div></div></li>
</ul>
</li>
</ul>
<h3 id="挂载组件"> 挂载组件</h3>
<p><strong>Runtime Only VS Runtime + Compiler</strong></p>
<p>通常我们利用 vue-cli 去初始化我们的 Vue.js 项目的时候会询问我们用 Runtime Only 版本的还是 Runtime + Compiler 版本。下面我们来对比这两个版本。</p>
<p><img src="./../images/mounted.png" alt="" /></p>
<ul>
<li>
<p>Runtime Only</p>
<p>我们在使用 Runtime Only 版本的 Vue.js 的时候，通常需要借助如 webpack 的 vue-loader 工具把 .vue 文件编译成 JavaScript，因为是在编译阶段做的，所以它只包含运行时的 Vue.js 代码，因此代码体积也会更轻量。</p>
<div><pre><code><span>//Runtime Only  不需要编译器的版本 </span>
<span>new</span> <span>Vue</span><span>(</span><span>{</span>
  <span>render</span> <span>(</span><span>h</span><span>)</span> <span>{</span>
    <span>return</span> <span>h</span><span>(</span><span>'div'</span><span>,</span> <span>this</span><span>.</span>hi<span>)</span>
  <span>}</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div></li>
<li>
<p>Runtime + Compiler</p>
<p>我们如果没有对代码做预编译，但又使用了 Vue 的 template 属性并传入一个字符串，则需要在客户端编译模板，如下所示：</p>
<div><pre><code><span>// 需要编译器的版本</span>
<span>new</span> <span>Vue</span><span>(</span><span>{</span>
  template<span>:</span> <span>'&lt;div>{{ hi }}&lt;/div>'</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br></div></div><p>因为在 Vue.js 2.0 中，最终渲染都是通过 <code>render</code> 函数，如果写 <code>template</code> 属性，则需要编译成 <code>render</code> 函数，那么这个编译过程会发生运行时，所以需要带有编译器的版本。</p>
<p>很显然，这个编译过程对性能会有一定损耗，所以通常我们更推荐使用 Runtime-Only 的 Vue.js。</p>
</li>
</ul>
<p><strong>mount() 挂载实现:</strong></p>
<ul>
<li>
<p>Vue 中我们是通过 <code>$mount</code> 实例方法去挂载 <code>vm</code> 的</p>
<ul>
<li><code>$mount</code> 方法在多个文件中都有定义，如:
<ul>
<li>原型上的定义：执行组件挂载，将 render function 渲染成 VNode，定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/platforms/web/runtime/index.js" target="_blank" rel="noopener noreferrer">src/platform/web/runtime/index.js</a> 、</li>
<li><code>compiler</code> 版本的定义 :  获取模板进行模板编译，编译后调用原型上 mount 方法进行组件挂载，定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/platforms/web/runtime-with-compiler.js" target="_blank" rel="noopener noreferrer">src/platform/web/entry-runtime-with-compiler.js</a></li>
<li>weex平台版本定义： <code>src/platform/weex/runtime/index.js</code>。</li>
</ul>
</li>
</ul>
</li>
<li>
<p><code>$mount</code> 这个方法的实现是和平台、构建方式都相关的。</p>
</li>
<li>
<p>不同版本之间关系： compiler 版本 和 weex 版本都是以 原型上mount() 定义进行重写，添加一些平台相关逻辑，原型版本主要执行 <code>mountComponent</code> 方法。</p>
</li>
<li>
<p>总体流程图：</p>
<p><img src="./../flow-graph/mount过程.png" alt="" /></p>
</li>
<li>
<p>原型上的 <code>$mount</code> 方法： 在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/platforms/web/runtime/index.js" target="_blank" rel="noopener noreferrer">src/platform/web/runtime/index.js</a> 中定义，它是可以被 <code>runtime only</code> 版本的 Vue 直接使用的。</p>
<div><pre><code><span>/**
 * runtime only版本
 * 组件挂载方法
 * 第一个是 el，它表示挂载的元素，可以是字符串，也可以是 DOM 对象，如果是字符串在浏览器环境下会调用 query 方法转换成 DOM 对象的
 * 第二个参数是和服务端渲染相关，在浏览器环境下我们不需要传第二个参数
 */</span>
<span>Vue</span><span>.</span>prototype<span>.</span><span>$mount</span> <span>=</span> <span>function</span> <span>(</span>
  <span>el<span>?</span><span>:</span> string <span>|</span> Element<span>,</span>
  hydrating<span>?</span><span>:</span> boolean</span>
<span>)</span><span>:</span> Component <span>{</span>
  <span>/*获取DOM实例对象*/</span>
  el <span>=</span> el <span>&amp;&amp;</span> inBrowser <span>?</span> <span>query</span><span>(</span>el<span>)</span> <span>:</span> <span>undefined</span>
  <span>/*挂载组件,在浏览器环境下会调用 query 方法转换成 DOM 对象的*/</span>
  <span>return</span> <span>mountComponent</span><span>(</span><span>this</span><span>,</span> el<span>,</span> hydrating<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div></li>
<li>
<p><code>mountComponent</code> 方法: 定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/%5Bsrc/core/instance/lifecycle.js%5D(https://github.com/vuejs/vue/blob/v2.3.0/src/platforms/web/runtime/index.js)" target="_blank" rel="noopener noreferrer">src/core/instance/lifecycle.js</a> 文件中：</p>
<div><pre><code><span>/*挂载组件*/</span>
<span>export</span> <span>function</span> <span>mountComponent</span><span>(</span>
  <span>vm<span>:</span> Component<span>,</span>
  el<span>:</span> <span>?</span>Element<span>,</span>
  hydrating<span>?</span><span>:</span> boolean</span>
<span>)</span><span>:</span> Component <span>{</span>
  vm<span>.</span>$el <span>=</span> el
  <span>if</span> <span>(</span><span>!</span>vm<span>.</span>$options<span>.</span>render<span>)</span> <span>{</span> <span>//不存在render方法(vue实例中没有render方法并且mount没有转换render方法)</span>
    <span>/*render函数不存在的时候创建一个空的VNode节点*/</span>
    vm<span>.</span>$options<span>.</span>render <span>=</span> createEmptyVNode
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
      <span>/* istanbul ignore if */</span>
      <span>// 当用了 runtime only 版本没有render方法并且写添加了template，则警告(runtime only版本不能添加template)</span>
      <span>if</span> <span>(</span><span>(</span>vm<span>.</span>$options<span>.</span>template <span>&amp;&amp;</span> vm<span>.</span>$options<span>.</span>template<span>.</span><span>charAt</span><span>(</span><span>0</span><span>)</span> <span>!==</span> <span>'#'</span><span>)</span> <span>||</span>
        vm<span>.</span>$options<span>.</span>el <span>||</span> el<span>)</span> <span>{</span>
        <span>warn</span><span>(</span>
          <span>'You are using the runtime-only build of Vue where the template '</span> <span>+</span>
          <span>'compiler is not available. Either pre-compile the templates into '</span> <span>+</span>
          <span>'render functions, or use the compiler-included build.'</span><span>,</span>
          vm
        <span>)</span>
      <span>}</span> <span>else</span> <span>{</span> <span>// 没有写render函数或则template</span>
        <span>warn</span><span>(</span>
          <span>'Failed to mount component: template or render function not defined.'</span><span>,</span>
          vm
        <span>)</span>
      <span>}</span>
    <span>}</span>
  <span>}</span>
  <span>/*触发beforeMount钩子*/</span>
  <span>callHook</span><span>(</span>vm<span>,</span> <span>'beforeMount'</span><span>)</span>

  <span>/*updateComponent作为Watcher对象的getter函数，用来依赖收集*/</span>
  <span>let</span> updateComponent
  <span>/* istanbul ignore if */</span>
  <span>// 性能埋点相关</span>
  <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> config<span>.</span>performance <span>&amp;&amp;</span> mark<span>)</span> <span>{</span>
    <span>updateComponent</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
      <span>const</span> name <span>=</span> vm<span>.</span>_name
      <span>const</span> id <span>=</span> vm<span>.</span>_uid
      <span>const</span> startTag <span>=</span> <span><span>`</span><span>vue-perf-start:</span><span><span>${</span>id<span>}</span></span><span>`</span></span>
      <span>const</span> endTag <span>=</span> <span><span>`</span><span>vue-perf-end:</span><span><span>${</span>id<span>}</span></span><span>`</span></span>

      <span>mark</span><span>(</span>startTag<span>)</span>
      <span>const</span> vnode <span>=</span> vm<span>.</span><span>_render</span><span>(</span><span>)</span>
      <span>mark</span><span>(</span>endTag<span>)</span>
      <span>measure</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span> render</span><span>`</span></span><span>,</span> startTag<span>,</span> endTag<span>)</span>

      <span>mark</span><span>(</span>startTag<span>)</span>
      vm<span>.</span><span>_update</span><span>(</span>vnode<span>,</span> hydrating<span>)</span>
      <span>mark</span><span>(</span>endTag<span>)</span>
      <span>measure</span><span>(</span><span><span>`</span><span><span>${</span>name<span>}</span></span><span> patch</span><span>`</span></span><span>,</span> startTag<span>,</span> endTag<span>)</span>
    <span>}</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>updateComponent</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
      <span>/*在此方法中调用 vm._render (该方法定义在./render) 方法先生成虚拟 VNode，
      最终调用 vm._update 更新 DOM，_update 是实例的一个私有方法，它被调用的时机有 2 个，
      一个是首次渲染，一个是数据更新的时候*/</span>
      vm<span>.</span><span>_update</span><span>(</span>vm<span>.</span><span>_render</span><span>(</span><span>)</span><span>,</span> hydrating<span>)</span>
    <span>}</span>
  <span>}</span>

  <span>/**
   * 这里对该vm注册一个渲染Watcher实例，Watcher的getter为updateComponent函数，
   * 用于触发所有渲染所需要用到的数据的getter，进行依赖收集，该Watcher实例会存在所有渲染所需数据的闭包Dep中
   * Watcher 在这里起到两个作用:
   * 一个是初始化的时候会执行回调函数（updateComponent），
   * 另一个是当 vm 实例中的监测的数据发生变化的时候执行回调函数（updateComponent）
   * @param {} vm vue实例对象
   * @param {Function} updateComponent 
   * @param {Funciont} noop 空函数
   */</span>
  vm<span>.</span>_watcher <span>=</span> <span>new</span> <span>Watcher</span><span>(</span>vm<span>,</span> updateComponent<span>,</span> noop<span>)</span> <span>//创建渲染watcher</span>
  hydrating <span>=</span> <span>false</span>

  <span>// manually mounted instance, call mounted on self</span>
  <span>// mounted is called for render-created child components in its inserted hook</span>
  <span>if</span> <span>(</span>vm<span>.</span>$vnode <span>==</span> <span>null</span><span>)</span> <span>{</span> <span>//vm.$vnode 表示 Vue 实例的父虚拟 Node，所以它为 Null 则表示当前是根 Vue 的实例</span>
    <span>/*标志位，设置 vm._isMounted 为 true 代表该组件已经挂载*/</span>
    vm<span>.</span>_isMounted <span>=</span> <span>true</span>
    <span>/*调用mounted钩子*/</span>
    <span>callHook</span><span>(</span>vm<span>,</span> <span>'mounted'</span><span>)</span>
  <span>}</span>
  <span>return</span> vm
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br></div></div><ul>
<li>
<p><code>mountComponent</code> 核心就是先调用 <code>vm._render</code> 方法先生成虚拟 Node，再实例化一个渲染<code>Watcher</code>，在它的回调函数中会调用 <code>updateComponent</code> 方法，最终调用 <code>vm._update</code> 更新 DOM。</p>
</li>
<li>
<p><code>mountComponent</code> 方法的逻辑也是非常清晰的，它会完成整个渲染工作，最核心的 2 个方法：<code>vm._render</code> 和 <code>vm._update</code>。</p>
</li>
</ul>
</li>
<li>
<p><code>compiler</code> 版本的 <code>$mount</code> 方法：在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/platforms/web/runtime-with-compiler.js" target="_blank" rel="noopener noreferrer">src/platform/web/entry-runtime-with-compiler.js</a>文件中定义：</p>
<div><pre><code><span>/*把原本不带编译的$mount方法保存下来，在最后会调用
 *Vue.prototype.$mount()方法定义在./runtime/index
 */</span>
<span>const</span> mount <span>=</span> <span>Vue</span><span>.</span>prototype<span>.</span>$mount
<span>/**
 * runtime + compiler版本
 * 在vue原型上重新定义了mount方法，挂载组件，带模板编译(将template换成成render)
 * Q: 为什么要保存Vue原型中的$mount方法，然后又在此处重写$mount方法？
 * A: 因为Vue原型中的$mount方法是不带编译方法的，在此处重写是为了往$mount中添加编译方法compileToFunctions方法
 * 此文件是带runtime和compilr的vue版本，Vue原型中的$mount方法是不带compiler的版本
 * (原先原型上的 $mount 方法在 src/platform/web/runtime/index.js 中定义)，这样做的好处是能复用$mount方法
 */</span>
<span>Vue</span><span>.</span>prototype<span>.</span><span>$mount</span> <span>=</span> <span>function</span> <span>(</span>
  <span>el<span>?</span><span>:</span> string <span>|</span> Element<span>,</span>
  hydrating<span>?</span><span>:</span> boolean</span>
<span>)</span><span>:</span> Component <span>{</span>
  el <span>=</span> el <span>&amp;&amp;</span> <span>query</span><span>(</span>el<span>)</span> <span>//获取dom对象</span>

  <span>/* istanbul ignore if */</span>
  <span>//限制 vue 不能挂载到 body 和 html 上</span>
  <span>if</span> <span>(</span>el <span>===</span> document<span>.</span>body <span>||</span> el <span>===</span> document<span>.</span>documentElement<span>)</span> <span>{</span>
    process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> <span>warn</span><span>(</span>
      <span><span>`</span><span>Do not mount Vue to &lt;html> or &lt;body> - mount to normal elements instead.</span><span>`</span></span>
    <span>)</span>
    <span>return</span> <span>this</span>
  <span>}</span>

  <span>const</span> options <span>=</span> <span>this</span><span>.</span>$options
  <span>// resolve template/el and convert to render function</span>
  <span>/*render不存在的时候编译template，编译成render函数，否则优先使用render*/</span>
  <span>if</span> <span>(</span><span>!</span>options<span>.</span>render<span>)</span> <span>{</span> 
    <span>let</span> template <span>=</span> options<span>.</span>template
    <span>/*template存在的时候取template，不存在的时候取el的outerHTML*/</span>
    <span>if</span> <span>(</span>template<span>)</span> <span>{</span>
      <span>/*当template是字符串的时候*/</span>
      <span>if</span> <span>(</span><span>typeof</span> template <span>===</span> <span>'string'</span><span>)</span> <span>{</span>
        <span>if</span> <span>(</span>template<span>.</span><span>charAt</span><span>(</span><span>0</span><span>)</span> <span>===</span> <span>'#'</span><span>)</span> <span>{</span> <span>//传入vue中的template字段的value值必须为dom的id</span>
          template <span>=</span> <span>idToTemplate</span><span>(</span>template<span>)</span> <span>//根据id获取templete，即获取id对应的DOM，然后访问其innerHTML</span>
          <span>/* istanbul ignore if */</span>
          <span>//id对应的dom为空，警告</span>
          <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> <span>!</span>template<span>)</span> <span>{</span>
            <span>warn</span><span>(</span>
              <span><span>`</span><span>Template element not found or is empty: </span><span><span>${</span>options<span>.</span>template<span>}</span></span><span>`</span></span><span>,</span>
              <span>this</span>
            <span>)</span>
          <span>}</span>
        <span>}</span>
      <span>}</span> <span>else</span> <span>if</span> <span>(</span>template<span>.</span>nodeType<span>)</span> <span>{</span>
        <span>/*当template为DOM节点的时候，直接获取dom节点对象*/</span>
        template <span>=</span> template<span>.</span>innerHTML
      <span>}</span> <span>else</span> <span>{</span>
        <span>/*报错*/</span>
        <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
          <span>warn</span><span>(</span><span>'invalid template option:'</span> <span>+</span> template<span>,</span> <span>this</span><span>)</span>
        <span>}</span>
        <span>return</span> <span>this</span>
      <span>}</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span>el<span>)</span> <span>{</span> <span>//template字段不存在的时候取el字段对应的dom对象</span>
      <span>/*获取element的outerHTML*/</span>
      template <span>=</span> <span>getOuterHTML</span><span>(</span>el<span>)</span>
    <span>}</span>
    <span>if</span> <span>(</span>template<span>)</span> <span>{</span>
      <span>/* istanbul ignore if */</span>
      <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> config<span>.</span>performance <span>&amp;&amp;</span> mark<span>)</span> <span>{</span>
        <span>mark</span><span>(</span><span>'compile'</span><span>)</span> <span>//vue性能埋点(vue中提供的性能分析)</span>
      <span>}</span>

      <span>/*
      将template编译成render函数，这里会有render以及staticRenderFns两个返回，这是vue的编译时优化，
      static静态不需要在VNode更新时进行patch，优化性能
      在 Vue 2.0 版本中，所有 Vue 的组件的渲染最终都需要 render 方法，无论我们是用单文件 .vue 方式开发组件，
      还是写了 el 或者 template 属性，最终都会转换成 render 方法，那么这个过程是 Vue 的一个“在线编译”的过程，
      它是调用 compileToFunctions 方法实现的*/</span>
      <span>const</span> <span>{</span> render<span>,</span> staticRenderFns <span>}</span> <span>=</span> <span>compileToFunctions</span><span>(</span>template<span>,</span> <span>{</span>
        shouldDecodeNewlines<span>,</span>
        delimiters<span>:</span> options<span>.</span>delimiters
      <span>}</span><span>,</span> <span>this</span><span>)</span>
      options<span>.</span>render <span>=</span> render
      options<span>.</span>staticRenderFns <span>=</span> staticRenderFns

      <span>/* istanbul ignore if */</span>
      <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> config<span>.</span>performance <span>&amp;&amp;</span> mark<span>)</span> <span>{</span>
        <span>mark</span><span>(</span><span>'compile end'</span><span>)</span>
        <span>measure</span><span>(</span><span><span>`</span><span><span>${</span><span>this</span><span>.</span>_name<span>}</span></span><span> compile</span><span>`</span></span><span>,</span> <span>'compile'</span><span>,</span> <span>'compile end'</span><span>)</span>
      <span>}</span>
    <span>}</span>
  <span>}</span>
  <span>/**
   * 如果vue实例参数中存在render方法，直接调用mount挂载el
   * 调用const mount = Vue.prototype.$mount保存下来的不带编译的mount
   * 原先原型上的 $mount 方法在 src/platform/web/runtime/index.js 中定义
   * 之所以这么设计完全是为了复用，因为它是可以被 runtime only 版本的 Vue 直接使用的
   */</span>
  <span>return</span> <span>mount</span><span>.</span><span>call</span><span>(</span><span>this</span><span>,</span> el<span>,</span> hydrating<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br><span>93</span><br><span>94</span><br><span>95</span><br></div></div><ul>
<li>
<p>流程分析：</p>
<ul>
<li>首先缓存了原型上的 <code>$mount</code> 方法，再重新定义该方法。</li>
<li>首先，它对 <code>el</code> 做了限制，Vue 不能挂载在 <code>body</code>、<code>html</code> 这样的根节点上。</li>
<li>如果没有定义 <code>render</code> 方法，则会把 <code>el</code> 或者 <code>template</code> 字符串转换成 <code>render</code> 方法。</li>
<li>在 Vue 2.0 版本中，所有 Vue 的组件的渲染最终都需要 <code>render</code> 方法，无论我们是用单文件 .vue 方式开发组件，还是写了 <code>el</code> 或者 <code>template</code> 属性，最终都会转换成 <code>render</code> 方法，那么这个过程是 Vue 的一个“在线编译”的过程，它是调用 <code>compileToFunctions</code> 方法实现的。</li>
<li>最后，调用原先原型上的 <code>$mount</code> 方法挂载。</li>
</ul>
</li>
<li>
<p>核心编译的入口：<code>compileToFunctions</code> 方法，把模板 <code>template</code> 编译生成 <code>render</code> 以及 <code>staticRenderFns</code>，它的定义在 <a href="">src/platforms/web/compiler/index.js</a> https://github.com/vuejs/vue/blob/v2.3.0/src/platforms/web/compiler/index.js中</p>
<div><pre><code><span>export</span> <span>const</span> baseOptions<span>:</span> CompilerOptions <span>=</span> <span>{</span>
  expectHTML<span>:</span> <span>true</span><span>,</span>
  modules<span>,</span>
  directives<span>,</span>
  isPreTag<span>,</span>
  isUnaryTag<span>,</span>
  mustUseProp<span>,</span>
  canBeLeftOpenTag<span>,</span>
  isReservedTag<span>,</span>
  getTagNamespace<span>,</span>
  staticKeys<span>:</span> <span>genStaticKeys</span><span>(</span>modules<span>)</span>
<span>}</span>
<span>/*这里会根据不同平台传递不同的baseOptions创建编译器*/</span>
<span>const</span> <span>{</span> compile<span>,</span> compileToFunctions <span>}</span> <span>=</span> <span>createCompiler</span><span>(</span>baseOptions<span>)</span>
<span>export</span> <span>{</span> compile<span>,</span> compileToFunctions <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div></li>
<li>
<p><code>createCompiler</code> 方法的定义，在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/compiler/index.js" target="_blank" rel="noopener noreferrer">src/compiler/index.js</a>  中：</p>
<div><pre><code><span>/*提供一个方法，根据传递的baseOptions（不同平台可以有不同的实现）创建相应的编译器*/</span>
<span>export</span> <span>function</span> <span>createCompiler</span> <span>(</span><span>baseOptions<span>:</span> CompilerOptions</span><span>)</span> <span>{</span>
  <span>/*
    作为缓存，防止每次都重新编译。
    模板的key为delimiters(https://cn.vuejs.org/v2/api/#delimiters)+template，value为编译结果
  */</span>
  <span>const</span> functionCompileCache<span>:</span> <span>{</span>
    <span>[</span>key<span>:</span> string<span>]</span><span>:</span> CompiledFunctionResult<span>;</span>
  <span>}</span> <span>=</span> Object<span>.</span><span>create</span><span>(</span><span>null</span><span>)</span>

  <span>/*编译，将模板template编译成AST树、render函数以及staticRenderFns函数*/</span>
  <span>//-------------------- compile function --------------------</span>
  <span>function</span> <span>compile</span> <span>(</span>
    <span>template<span>:</span> string<span>,</span>
    options<span>?</span><span>:</span> CompilerOptions</span>
  <span>)</span><span>:</span> CompiledResult <span>{</span>
    <span>const</span> finalOptions <span>=</span> Object<span>.</span><span>create</span><span>(</span>baseOptions<span>)</span>
    <span>const</span> errors <span>=</span> <span>[</span><span>]</span>
    <span>const</span> tips <span>=</span> <span>[</span><span>]</span>
    finalOptions<span>.</span><span>warn</span> <span>=</span> <span>(</span><span>msg<span>,</span> tip</span><span>)</span> <span>=></span> <span>{</span>
      <span>(</span>tip <span>?</span> tips <span>:</span> errors<span>)</span><span>.</span><span>push</span><span>(</span>msg<span>)</span>
    <span>}</span>

    <span>/*做下面这些merge的目的因为不同平台可以提供自己本身平台的一个baseOptions，内部封装了平台自己的实现，然后把共同的部分抽离开来放在这层compiler中，所以在这里需要merge一下*/</span>
    <span>if</span> <span>(</span>options<span>)</span> <span>{</span>
      <span>// merge custom modules</span>
      <span>/*合并modules*/</span>
      <span>if</span> <span>(</span>options<span>.</span>modules<span>)</span> <span>{</span>
        finalOptions<span>.</span>modules <span>=</span> <span>(</span>baseOptions<span>.</span>modules <span>||</span> <span>[</span><span>]</span><span>)</span><span>.</span><span>concat</span><span>(</span>options<span>.</span>modules<span>)</span>
      <span>}</span>
      <span>// merge custom directives</span>
      <span>if</span> <span>(</span>options<span>.</span>directives<span>)</span> <span>{</span>
        <span>/*合并directives*/</span>
        finalOptions<span>.</span>directives <span>=</span> <span>extend</span><span>(</span>
          Object<span>.</span><span>create</span><span>(</span>baseOptions<span>.</span>directives<span>)</span><span>,</span>
          options<span>.</span>directives
        <span>)</span>
      <span>}</span>
      <span>// copy other options</span>
      <span>for</span> <span>(</span><span>const</span> key <span>in</span> options<span>)</span> <span>{</span>
        <span>/*合并其余的options，modules与directives已经在上面做了特殊处理了*/</span>
        <span>if</span> <span>(</span>key <span>!==</span> <span>'modules'</span> <span>&amp;&amp;</span> key <span>!==</span> <span>'directives'</span><span>)</span> <span>{</span>
          finalOptions<span>[</span>key<span>]</span> <span>=</span> options<span>[</span>key<span>]</span>
        <span>}</span>
      <span>}</span>
    <span>}</span>

    <span>/*基础模板编译，得到编译结果*/</span>
    <span>const</span> compiled <span>=</span> <span>baseCompile</span><span>(</span>template<span>,</span> finalOptions<span>)</span>
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
      errors<span>.</span><span>push</span><span>.</span><span>apply</span><span>(</span>errors<span>,</span> <span>detectErrors</span><span>(</span>compiled<span>.</span>ast<span>)</span><span>)</span>
    <span>}</span>
    compiled<span>.</span>errors <span>=</span> errors
    compiled<span>.</span>tips <span>=</span> tips
    <span>return</span> compiled
  <span>}</span>
 <span>//-------------------- compileToFunctions function --------------------</span>
  <span>/*带缓存的编译器，同时staticRenderFns以及render函数会被转换成Funtion对象*/</span>
  <span>function</span> <span>compileToFunctions</span> <span>(</span>
    <span>template<span>:</span> string<span>,</span>
    options<span>?</span><span>:</span> CompilerOptions<span>,</span>
    vm<span>?</span><span>:</span> Component</span>
  <span>)</span><span>:</span> CompiledFunctionResult <span>{</span>
    options <span>=</span> options <span>||</span> <span>{</span><span>}</span>

    <span>/* istanbul ignore if */</span>
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
      <span>// detect possible CSP restriction</span>
      <span>try</span> <span>{</span>
        <span>new</span> <span>Function</span><span>(</span><span>'return 1'</span><span>)</span>
      <span>}</span> <span>catch</span> <span>(</span>e<span>)</span> <span>{</span>
        <span>if</span> <span>(</span>e<span>.</span><span>toString</span><span>(</span><span>)</span><span>.</span><span>match</span><span>(</span><span><span>/</span><span>unsafe-eval|CSP</span><span>/</span></span><span>)</span><span>)</span> <span>{</span>
          <span>warn</span><span>(</span>
            <span>'It seems you are using the standalone build of Vue.js in an '</span> <span>+</span>
            <span>'environment with Content Security Policy that prohibits unsafe-eval. '</span> <span>+</span>
            <span>'The template compiler cannot work in this environment. Consider '</span> <span>+</span>
            <span>'relaxing the policy to allow unsafe-eval or pre-compiling your '</span> <span>+</span>
            <span>'templates into render functions.'</span>
          <span>)</span>
        <span>}</span>
      <span>}</span>
    <span>}</span>

    <span>// check cache</span>
    <span>/*有缓存的时候直接取出缓存中的结果即可*/</span>
    <span>const</span> key <span>=</span> options<span>.</span>delimiters
      <span>?</span> <span>String</span><span>(</span>options<span>.</span>delimiters<span>)</span> <span>+</span> template
      <span>:</span> template
    <span>if</span> <span>(</span>functionCompileCache<span>[</span>key<span>]</span><span>)</span> <span>{</span>
      <span>return</span> functionCompileCache<span>[</span>key<span>]</span>
    <span>}</span>

    <span>// compile</span>
    <span>/*核心编译*/</span>
    <span>const</span> compiled <span>=</span> <span>compile</span><span>(</span>template<span>,</span> options<span>)</span>

    <span>// check compilation errors/tips</span>
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
      <span>if</span> <span>(</span>compiled<span>.</span>errors <span>&amp;&amp;</span> compiled<span>.</span>errors<span>.</span>length<span>)</span> <span>{</span>
        <span>warn</span><span>(</span>
          <span><span>`</span><span>Error compiling template:\n\n</span><span><span>${</span>template<span>}</span></span><span>\n\n</span><span>`</span></span> <span>+</span>
          compiled<span>.</span>errors<span>.</span><span>map</span><span>(</span><span>e</span> <span>=></span> <span><span>`</span><span>- </span><span><span>${</span>e<span>}</span></span><span>`</span></span><span>)</span><span>.</span><span>join</span><span>(</span><span>'\n'</span><span>)</span> <span>+</span> <span>'\n'</span><span>,</span>
          vm
        <span>)</span>
      <span>}</span>
      <span>if</span> <span>(</span>compiled<span>.</span>tips <span>&amp;&amp;</span> compiled<span>.</span>tips<span>.</span>length<span>)</span> <span>{</span>
        compiled<span>.</span>tips<span>.</span><span>forEach</span><span>(</span><span>msg</span> <span>=></span> <span>tip</span><span>(</span>msg<span>,</span> vm<span>)</span><span>)</span>
      <span>}</span>
    <span>}</span>

    <span>// turn code into functions</span>
    <span>const</span> res <span>=</span> <span>{</span><span>}</span>
    <span>const</span> fnGenErrors <span>=</span> <span>[</span><span>]</span>
    <span>/*将render转换成Funtion对象*/</span>
    res<span>.</span>render <span>=</span> <span>makeFunction</span><span>(</span>compiled<span>.</span>render<span>,</span> fnGenErrors<span>)</span>
    <span>/*将staticRenderFns全部转化成Funtion对象 */</span>
    <span>const</span> l <span>=</span> compiled<span>.</span>staticRenderFns<span>.</span>length
    res<span>.</span>staticRenderFns <span>=</span> <span>new</span> <span>Array</span><span>(</span>l<span>)</span>
    <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> l<span>;</span> i<span>++</span><span>)</span> <span>{</span>
      res<span>.</span>staticRenderFns<span>[</span>i<span>]</span> <span>=</span> <span>makeFunction</span><span>(</span>compiled<span>.</span>staticRenderFns<span>[</span>i<span>]</span><span>,</span> fnGenErrors<span>)</span>
    <span>}</span>

    <span>// check function generation errors.</span>
    <span>// this should only happen if there is a bug in the compiler itself.</span>
    <span>// mostly for codegen development use</span>
    <span>/* istanbul ignore if */</span>
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
      <span>if</span> <span>(</span><span>(</span><span>!</span>compiled<span>.</span>errors <span>||</span> <span>!</span>compiled<span>.</span>errors<span>.</span>length<span>)</span> <span>&amp;&amp;</span> fnGenErrors<span>.</span>length<span>)</span> <span>{</span>
        <span>warn</span><span>(</span>
          <span><span>`</span><span>Failed to generate render function:\n\n</span><span>`</span></span> <span>+</span>
          fnGenErrors<span>.</span><span>map</span><span>(</span><span>(</span><span><span>{</span> err<span>,</span> code <span>}</span></span><span>)</span> <span>=></span> <span><span>`</span><span><span>${</span>err<span>.</span><span>toString</span><span>(</span><span>)</span><span>}</span></span><span> in\n\n</span><span><span>${</span>code<span>}</span></span><span>\n</span><span>`</span></span><span>)</span><span>.</span><span>join</span><span>(</span><span>'\n'</span><span>)</span><span>,</span>
          vm
        <span>)</span>
      <span>}</span>
    <span>}</span>

    <span>/*存放在缓存中，以免每次都重新编译*/</span>
    <span>return</span> <span>(</span>functionCompileCache<span>[</span>key<span>]</span> <span>=</span> res<span>)</span> 
  <span>}</span>
 <span>//-----------------</span>
  <span>return</span> <span>{</span>
    compile<span>,</span>
    compileToFunctions
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br><span>93</span><br><span>94</span><br><span>95</span><br><span>96</span><br><span>97</span><br><span>98</span><br><span>99</span><br><span>100</span><br><span>101</span><br><span>102</span><br><span>103</span><br><span>104</span><br><span>105</span><br><span>106</span><br><span>107</span><br><span>108</span><br><span>109</span><br><span>110</span><br><span>111</span><br><span>112</span><br><span>113</span><br><span>114</span><br><span>115</span><br><span>116</span><br><span>117</span><br><span>118</span><br><span>119</span><br><span>120</span><br><span>121</span><br><span>122</span><br><span>123</span><br><span>124</span><br><span>125</span><br><span>126</span><br><span>127</span><br><span>128</span><br><span>129</span><br><span>130</span><br><span>131</span><br><span>132</span><br><span>133</span><br><span>134</span><br><span>135</span><br><span>136</span><br><span>137</span><br><span>138</span><br><span>139</span><br><span>140</span><br><span>141</span><br><span>142</span><br><span>143</span><br><span>144</span><br><span>145</span><br></div></div><ul>
<li>
<p><code>createCompiler</code> 方法主要 定义 <code>compile</code> 和 <code>compileToFunctions</code> 方法并返回。</p>
</li>
<li>
<p><code>mount</code> 方法中主要调用 <code>compileToFunctions</code> 方法，获得 <code>render</code>,  <code>staticRenderFns</code>。</p>
</li>
<li>
<p><code>compileToFunctions</code> 定义：</p>
<ul>
<li>
<p>它接收 3 个参数、编译模板 <code>template</code>，编译配置 <code>options</code> 和 Vue 实例 <code>vm</code>。</p>
</li>
<li>
<p>返回值： 模板编译结果。</p>
</li>
<li>
<p>处理流程：</p>
<ul>
<li>先在缓存中查找 模板编译结果，有则返回；否则调用 <code>compiled</code> 方法进行编译；</li>
<li>检查编译是否错误 -&gt; 将编译的 <code>render</code> 转换成 <code>Funtion</code> 对象 -&gt; 缓存并返回编译结果。</li>
</ul>
</li>
<li>
<p>核心的编译过程就一行代码：因此主要分析   <code>compile</code> 方法。</p>
<div><pre><code><span>const</span> compiled <span>=</span> <span>compile</span><span>(</span>template<span>,</span> options<span>)</span>
</code></pre>
<div><span>1</span><br></div></div></li>
</ul>
</li>
<li>
<p><code>compile</code> 方法：</p>
<ul>
<li>
<p>处理流程</p>
<ul>
<li>处理 options 参数配置；</li>
<li>调用  <code>baseCompile()</code> 进行基础编译，返回编译结果；</li>
</ul>
</li>
<li>
<p><code>compile</code> 函数真正执行编译过程就一行代码：因此主要分析 <code>baseCompile</code> 方法。</p>
<div><pre><code><span>const</span> compiled <span>=</span> <span>baseCompile</span><span>(</span>template<span>,</span> finalOptions<span>)</span>
</code></pre>
<div><span>1</span><br></div></div></li>
</ul>
</li>
<li>
<p><code>baseCompile</code> 方法： 定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/compiler/index.js" target="_blank" rel="noopener noreferrer">src/compiler/index.js</a></p>
<div><pre><code><span>function</span> <span>baseCompile</span> <span>(</span>
  <span>template<span>:</span> string<span>,</span>
  options<span>:</span> CompilerOptions</span>
<span>)</span><span>:</span> CompiledResult <span>{</span>
  <span>/*parse解析得到ast树*/</span>
  <span>const</span> ast <span>=</span> <span>parse</span><span>(</span>template<span>.</span><span>trim</span><span>(</span><span>)</span><span>,</span> options<span>)</span>
  <span>/*
    将AST树进行优化
    优化的目标：生成模板AST树，检测不需要进行DOM改变的静态子树。
    一旦检测到这些静态树，我们就能做以下这些事情：
    1.把它们变成常数，这样我们就再也不需要每次重新渲染时创建新的节点了。
    2.在patch的过程中直接跳过。
 */</span>
  <span>optimize</span><span>(</span>ast<span>,</span> options<span>)</span>
  <span>/*根据ast树生成所需的code（内部包含render与staticRenderFns）*/</span>
  <span>const</span> code <span>=</span> <span>generate</span><span>(</span>ast<span>,</span> options<span>)</span>
  <span>return</span> <span>{</span>
    ast<span>,</span>
    render<span>:</span> code<span>.</span>render<span>,</span>
    staticRenderFns<span>:</span> code<span>.</span>staticRenderFns
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br></div></div><ul>
<li>
<p><code>baseCompile</code> 方法为真正的编译入口，它主要就是执行了如下几个逻辑：</p>
<ul>
<li>解析模板字符串生成 AST</li>
</ul>
<div><pre><code><span>const</span> ast <span>=</span> <span>parse</span><span>(</span>template<span>.</span><span>trim</span><span>(</span><span>)</span><span>,</span> options<span>)</span>
</code></pre>
<div><span>1</span><br></div></div><ul>
<li>优化语法树</li>
</ul>
<div><pre><code><span>optimize</span><span>(</span>ast<span>,</span> options<span>)</span>
</code></pre>
<div><span>1</span><br></div></div><ul>
<li>生成代码</li>
</ul>
<div><pre><code><span>const</span> code <span>=</span> <span>generate</span><span>(</span>ast<span>,</span> options<span>)</span>
</code></pre>
<div><span>1</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="模板编译"> 模板编译</h3>
<ul>
<li>
<p>模板编译总体流程</p>
<p><img src="./../flow-graph/模板编译过程.png" alt="" /></p>
</li>
</ul>
<h4 id="parse"> Parse</h4>
<ul>
<li>
<p><strong>作用</strong>： 用正则等方式解析  template  模板中的指令、class、style 等数据，形成 AST。</p>
<div><pre><code><span>// 被解析的模板</span>
<span>&lt;</span>ul <span>:</span><span>class</span><span>=</span><span>"bindCls"</span> <span>class</span><span>=</span><span>"list"</span> v<span>-</span><span>if</span><span>=</span><span>"isShow"</span><span>></span>
    <span>&lt;</span>li v<span>-</span><span>for</span><span>=</span><span>"(item,index) in data"</span> @click<span>=</span><span>"clickItem(index)"</span><span>></span><span>{</span><span>{</span>item<span>}</span><span>}</span><span>:</span><span>{</span><span>{</span>index<span>}</span><span>}</span><span>&lt;</span><span>/</span>li<span>></span>
<span>&lt;</span><span>/</span>ul<span>></span>

<span>// 最终解析出的 AST </span>
ast <span>=</span> <span>{</span>
  <span>'type'</span><span>:</span> <span>1</span><span>,</span>
  <span>'tag'</span><span>:</span> <span>'ul'</span><span>,</span>
  <span>'attrsList'</span><span>:</span> <span>[</span><span>]</span><span>,</span>
  <span>'attrsMap'</span><span>:</span> <span>{</span>
    <span>':class'</span><span>:</span> <span>'bindCls'</span><span>,</span>
    <span>'class'</span><span>:</span> <span>'list'</span><span>,</span>
    <span>'v-if'</span><span>:</span> <span>'isShow'</span>
  <span>}</span><span>,</span>
  <span>'if'</span><span>:</span> <span>'isShow'</span><span>,</span>
  <span>'ifConditions'</span><span>:</span> <span>[</span><span>{</span>
    <span>'exp'</span><span>:</span> <span>'isShow'</span><span>,</span>
    <span>'block'</span><span>:</span> <span>// ul ast element</span>
  <span>}</span><span>]</span><span>,</span>
  <span>'parent'</span><span>:</span> <span>undefined</span><span>,</span>
  <span>'plain'</span><span>:</span> <span>false</span><span>,</span>
  <span>'staticClass'</span><span>:</span> <span>'list'</span><span>,</span>
  <span>'classBinding'</span><span>:</span> <span>'bindCls'</span><span>,</span>
  <span>'children'</span><span>:</span> <span>[</span><span>{</span>
    <span>'type'</span><span>:</span> <span>1</span><span>,</span>
    <span>'tag'</span><span>:</span> <span>'li'</span><span>,</span>
    <span>'attrsList'</span><span>:</span> <span>[</span><span>{</span>
      <span>'name'</span><span>:</span> <span>'@click'</span><span>,</span>
      <span>'value'</span><span>:</span> <span>'clickItem(index)'</span>
    <span>}</span><span>]</span><span>,</span>
    <span>'attrsMap'</span><span>:</span> <span>{</span>
      <span>'@click'</span><span>:</span> <span>'clickItem(index)'</span><span>,</span>
      <span>'v-for'</span><span>:</span> <span>'(item,index) in data'</span>
     <span>}</span><span>,</span>
    <span>'parent'</span><span>:</span> <span>// ul ast element</span>
    <span>'plain'</span><span>:</span> <span>false</span><span>,</span>
    <span>'events'</span><span>:</span> <span>{</span>
      <span>'click'</span><span>:</span> <span>{</span>
        <span>'value'</span><span>:</span> <span>'clickItem(index)'</span>
      <span>}</span>
    <span>}</span><span>,</span>
    <span>'hasBindings'</span><span>:</span> <span>true</span><span>,</span>
    <span>'for'</span><span>:</span> <span>'data'</span><span>,</span>
    <span>'alias'</span><span>:</span> <span>'item'</span><span>,</span>
    <span>'iterator1'</span><span>:</span> <span>'index'</span><span>,</span>
    <span>'children'</span><span>:</span> <span>[</span>
      <span>'type'</span><span>:</span> <span>2</span><span>,</span>
      <span>'expression'</span><span>:</span> <span>'_s(item)+":"+_s(index)'</span>
      <span>'text'</span><span>:</span> <span>'{{item}}:{{index}}'</span><span>,</span>
      <span>'tokens'</span><span>:</span> <span>[</span>
        <span>{</span><span>'@binding'</span><span>:</span><span>'item'</span><span>}</span><span>,</span>
        <span>':'</span><span>,</span>
        <span>{</span><span>'@binding'</span><span>:</span><span>'index'</span><span>}</span>
      <span>]</span>
    <span>]</span>
  <span>}</span><span>]</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br></div></div><ul>
<li>生成的 AST 是一个树状结构，每一个节点都是一个 <code>ast element</code>，除了它自身的一些属性，还维护了它的父子关系，如 <code>parent</code> 指向它的父节点，<code>children</code> 指向它的所有子节点.</li>
</ul>
</li>
<li>
<p><strong>parse算法</strong>：</p>
<ul>
<li>
<p>数据结构：</p>
<ul>
<li>
<p>使用栈保存标签头，栈中使用 root 指向根元素标签头，使用 currentParent 指向当前标签头，使用 currentParent.children 数组保存当前标签的子元素；</p>
</li>
<li>
<p>使用数组保存 dom 属性和对应值；</p>
<p><img src="./../images/parse-stack.png" alt="" /></p>
</li>
</ul>
</li>
<li>
<p>使用 <code>parseHTML</code> 函数，在里面我们循环解析  template  字符串:</p>
<ul>
<li>
<p>1.从左到右解析 template 字符串，</p>
</li>
<li>
<p>2.使用正则匹配标签的头部，标签头入栈；将匹配的标签头放入 currentParent.children 数组， 并将 currentParent 指向栈顶节点；</p>
</li>
<li>
<p>3.使用两个正则分别用来解析标签结束以及标签内的属性，循环一直匹配到结束标签为止，解析内部所有的属性，存入数组中；</p>
</li>
<li>
<p>4.匹配到 <code>v-for</code> 和 <code>v-if</code> 指令时，将指令存入  attrsMap 中 ；定义一个 getAndRemoveAttr 函数，用来从 el 的 attrsMap 属性或是 attrsList 属性中取出 name 对应值（比如解析<code>&lt;div v-for=&quot;item in test&quot;&gt;</code> 中的 item 和 test 值）；<code>v-for</code>会将指令解析成 for 属性以及 alias 属性，而 <code>v-if</code> 会将条件都存入 ifConditions 数组中。</p>
</li>
<li>
<p>5.解析下一个标签解析：</p>
<ul>
<li>如果还是头标签或者是文本的话，会成为当前栈顶节点的子节点；</li>
<li>如果是尾标签的话，从栈中取出最近的跟自己标签名一致的那个元素，将currentParent 指向那个元素，并将该元素之前的元素都从栈中出栈；（注意：这里为什么解析的尾元素不是对应栈的最上面的一个元素？因为栈中可能出现自闭合标签；如 <code>&lt;br/&gt;</code>标签，或者是写了<code>&lt;span&gt;</code>但是没有加上<code>&lt; /span&gt;</code>的情况，这时候就要找到栈中的第二个位置才能找到同名标签）</li>
</ul>
</li>
<li>
<p>6.最后是解析文本，这个比较简单，只需要将文本取出，然后有两种情况：</p>
<ul>
<li>一种是普通的文本，直接构建一个节点  push  进当前 currentParent 的  children  中即可；</li>
<li>还有一种情况是文本是如“{{item}}”这样的 Vue.js  的表达式，使用一个 tokens 数组来存放解析结果，通过 defaultTagRE 来循环匹配该文本，如果是普通文本直接 push 到 tokens 数组中去，如果是表达式（{{item}}），则转化成“_s(${exp})”的形式</li>
</ul>
</li>
<li>
<p>7.最终得到的  AST  通过一些特定的属性，能够比较清晰地描述出标签的属性以及依赖关系。</p>
</li>
</ul>
</li>
</ul>
</li>
<li>
<p>parse 算法示例：</p>
<ul>
<li>被解析模板： <img src="./../images/parse-demo.png" alt="" />
<ul>
<li>遇到 div 入栈；从左往右解析，遇到属性存入数组；遇到指令 存入 map;</li>
<li>遇到 span 标签头，入栈，并将 span 存入 div 元素对象的 children 数组，将 currentParent 指向 span；继续解析 span 中的指令；</li>
<li>遇到 Vue.js  的表达式，使用一个 tokens 数组来存放解析结果；</li>
<li>遇到 span 尾标签，从栈中取出最近的跟自己标签名一致的那个元素，将 currentParent 指向 span 元素，并将该元素之前的元素都从栈中出栈；栈中剩余 div 标签</li>
<li>遇到 div 尾标签，将栈中  div 标签取出。</li>
</ul>
</li>
</ul>
</li>
<li>
<p><a href="http://caibaojian.com/vue-analysis/compile/parse.html#parse" target="_blank" rel="noopener noreferrer">查看源码分析</a></p>
</li>
</ul>
<h4 id="optimize"> Optimize</h4>
<ul>
<li>**作用：**给每个节点会加上 static 属性，用来标记是否是静态的。
<ul>
<li>添加 static 属性原因：
<ul>
<li>Vue 是数据驱动，是响应式的，但是我们的模板并不是所有数据都是响应式的，也有很多数据是首次渲染后就永远不会变化的，那么这部分数据生成的 DOM 也不会变化，我们可以在 <code>patch</code> 的过程跳过对他们的比对;</li>
<li>patch 的过程实际上是将  VNode  节点进行一层一层的比对，然后将「差异」更新到视图上。那么一些静态节点是不会根据数据变化而产生变化的，这些节点我们没有比对的需求，因此可以添加标记在 patch 时跳过这些静态节点的比对，从而达到「优化」的目的。</li>
</ul>
</li>
</ul>
</li>
<li>算法：
<ul>
<li>遍历所有节点通过 isStatic 来判断当前节点是否是静态节点，此外，会遍历当前节点的所有子节点，如果子节点是非静态节点，那么当前节点也是非静态节点；</li>
<li>isStatic 函数，传入一个  node  判断该  node  是否是静态节点。判断的标准是当  type  为 2（表达式节点）则是非静态节点，当 type 为 3（文本节点）的时候则是静态节点，当然，如果存在 if 或者 for这样的条件的时候（表达式节点），也是非静态节点；</li>
<li>最后使用 markStaticRoots 函数，用来标记 staticRoot（静态根）。如果当前节点是静态节点，同时满足该节点并不是只有一个文本节点左右子节点时，标记 staticRoot 为  true，否则为  false。</li>
</ul>
</li>
</ul>
<h4 id="generate"> generate</h4>
<ul>
<li>
<p>**作用：**generate 主要 将AST 树转换成可执行的代码 (既 render funtion 字符串) ，最终得到  render  的字符串以及 staticRenderFns  字符串。</p>
</li>
<li>
<p><code>generate</code> 函数： 定义在  <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/compiler/codegen/index.js" target="_blank" rel="noopener noreferrer">src/compiler/codegen/index.js</a> 中：</p>
<div><pre><code></code></pre>
<div></div></div></li>
</ul>
<p>export function generate (
ast: ASTElement | void,
options: CompilerOptions
): CodegenResult {
const state = new CodegenState(options)
const code = ast ? genElement(ast, state) : '_c(&quot;div&quot;)'
return {
render: <code>with(this){return ${code}}</code>,
staticRenderFns: state.staticRenderFns
}
}</p>
<div><pre><code>
* `generate` 函数首先通过 `genElement(ast, state)` 生成 `code`，再把 `code` 用 `with(this){return ${code}}}` 包裹起来。这里的 `state` 是 `CodegenState` 的一个实例。

*  `genElement` 函数：判断当前 AST 元素节点的属性执行不同的代码生成函数，最后返回可执行代码 code。

```js
export function genElement (el: ASTElement, state: CodegenState): string {
  if (el.staticRoot &amp;&amp; !el.staticProcessed) {
    return genStatic(el, state)
  } else if (el.once &amp;&amp; !el.onceProcessed) {
    return genOnce(el, state)
  } else if (el.for &amp;&amp; !el.forProcessed) {
    return genFor(el, state)
  } else if (el.if &amp;&amp; !el.ifProcessed) {
    return genIf(el, state)
  } else if (el.tag === &#39;template&#39; &amp;&amp; !el.slotTarget) {
    return genChildren(el, state) || &#39;void 0&#39;
  } else if (el.tag === &#39;slot&#39;) {
    return genSlot(el, state)
  } else {
    // component or element
    let code
    if (el.component) {
      code = genComponent(el.component, el, state)
    } else {
      const data = el.plain ? undefined : genData(el, state)

      const children = el.inlineTemplate ? null : genChildren(el, state, true)
      code = `_c(&#39;${el.tag}&#39;${
        data ? `,${data}` : &#39;&#39; // data
      }${
        children ? `,${children}` : &#39;&#39; // children
      })`
    }
    // module transforms
    for (let i = 0; i &lt; state.transforms.length; i++) {
      code = state.transforms[i](el, code)
    }
    return code
  }
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br></div></div><ul>
<li>
<p>示例：</p>
<div><pre><code><span><span><span>&lt;</span>ul</span> <span>:class</span><span><span>=</span><span>"</span>bindCls<span>"</span></span> <span>class</span><span><span>=</span><span>"</span>list<span>"</span></span> <span>v-if</span><span><span>=</span><span>"</span>isShow<span>"</span></span><span>></span></span>
      <span><span><span>&lt;</span>li</span> <span>v-for</span><span><span>=</span><span>"</span>(item,index) in data<span>"</span></span> <span>@click</span><span><span>=</span><span>"</span>clickItem(index)<span>"</span></span><span>></span></span>{{item}}:{{index}}<span><span><span>&lt;/</span>li</span><span>></span></span>
  <span><span><span>&lt;/</span>ul</span><span>></span></span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><div><pre><code><span>//它经过编译，执行 `const code = generate(ast, options)`，生成的 `render` 代码串如下：</span>
<span>with</span><span>(</span><span>this</span><span>)</span><span>{</span>
  <span>return</span> <span>(</span>isShow<span>)</span> <span>?</span>
    <span>_c</span><span>(</span><span>'ul'</span><span>,</span> <span>{</span>
        staticClass<span>:</span> <span>"list"</span><span>,</span>
        <span>class</span><span>:</span> bindCls
      <span>}</span><span>,</span>
      <span>_l</span><span>(</span><span>(</span>data<span>)</span><span>,</span> <span>function</span><span>(</span><span>item<span>,</span> index</span><span>)</span> <span>{</span>
        <span>return</span> <span>_c</span><span>(</span><span>'li'</span><span>,</span> <span>{</span>
          on<span>:</span> <span>{</span>
            <span>"click"</span><span>:</span> <span>function</span><span>(</span><span>$event</span><span>)</span> <span>{</span>
              <span>clickItem</span><span>(</span>index<span>)</span>
            <span>}</span>
          <span>}</span>
        <span>}</span><span>,</span>
        <span>[</span><span>_v</span><span>(</span><span>_s</span><span>(</span>item<span>)</span> <span>+</span> <span>":"</span> <span>+</span> <span>_s</span><span>(</span>index<span>)</span><span>)</span><span>]</span><span>)</span>
      <span>}</span><span>)</span>
    <span>)</span> <span>:</span> <span>_e</span><span>(</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div><ul>
<li>
<p>在 <code>compileToFunctions</code> 中，通过 <code>makeFunction()</code>  把 <code>render</code> 代码串转换成函数，它的定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/compiler/index.js" target="_blank" rel="noopener noreferrer">src/compiler/index.js</a>中</p>
<div><pre><code></code></pre>
<div></div></div></li>
</ul>
<p>/<em>提供一个方法，根据传递的baseOptions（不同平台可以有不同的实现）创建相应的编译器</em>/
export function createCompiler (baseOptions: CompilerOptions) {
// 省略 compile 代码...</p>
<div><pre><code>/*带缓存的编译器，同时staticRenderFns以及render函数会被转换成Funtion对象*/
function compileToFunctions (
  template: string,
  options?: CompilerOptions,
  vm?: Component
): CompiledFunctionResult {
  // 省略其余代码....
  /*编译*/
  const compiled = compile(template, options)

  // turn code into functions
  const res = {}
  const fnGenErrors = []
  /*将 render 转换成 Funtion对象*/
  res.render = makeFunction(compiled.render, fnGenErrors)
  /*将 staticRenderFns 全部转化成 Funtion 对象 */
  const l = compiled.staticRenderFns.length
  res.staticRenderFns = new Array(l)
  for (let i = 0; i &lt; l; i++) {
    res.staticRenderFns[i] = makeFunction(compiled.staticRenderFns[i], fnGenErrors)
  }
}
</code></pre>
</div><p>}</p>
<div><pre><code>


* `makeFunction` 方法: 把 `render` 代码串通过 `new Function` 的方式转换成可执行的函数，赋值给 `vm.options.render`，这样当组件通过 `vm._render` 的时候，就会执行这个 `render` 函数。定义在 [src/compiler/index.js](https://github.com/vuejs/vue/blob/v2.3.0/src/compiler/index.js) 中：

```js
function makeFunction (code, errors) {
  try {
    return new Function(code)
  } catch (err) {
    errors.push({ err, code })
    return noop
  }
}
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div></li>
<li>
<p>接下来就会执行组件挂载，调用原型上的 <code>mount</code> 方法，<code>mount</code> 方法中调用 <code>mountComponent</code> 执行组件挂载，组件挂载主要将 render function 渲染成 VNode，在渲染过程中会触发数据依赖的收集。</p>
</li>
</ul>
<h4 id="依赖收集"> 依赖收集</h4>
<ul>
<li>
<p>依赖收集发生时间：模板编译完成 -&gt; 原型中的 <code>mount()</code>  -&gt; <code>mountComponent()</code> -&gt;  <code>new Watcher()</code> 创建渲染 watcher 的构造函数 -&gt; <code>watcher.get()</code>。</p>
</li>
<li>
<p>作用： 订阅数据变化的 watcher 的收集，数据更新时，触发数据 setter，通知到对应的 vm 实例订阅者进行视图的更新。</p>
<div><pre><code><span>//假设我们现在有一个全局的对象，我们可能会在多个  Vue  对象中用到它进行展示。</span>
<span>let</span> globalObj <span>=</span> <span>{</span>
    text1<span>:</span><span>'text1'</span>
<span>}</span><span>;</span>
<span>let</span> o1 <span>=</span> <span>new</span> <span>Vue</span><span>(</span><span>{</span>
    template<span>:</span><span><span>`</span><span>&lt;div>&lt;span>{{text1}}&lt;/span>&lt;div></span><span>`</span></span><span>,</span>
    data<span>:</span> globalObj
<span>}</span><span>)</span><span>;</span>
<span>let</span> o2 <span>=</span> <span>newVue</span><span>(</span><span>{</span>
    template<span>:</span><span><span>`</span><span>&lt;div>&lt;span>{{text1}}&lt;/span>&lt;div></span><span>`</span></span><span>,</span>
    data<span>:</span>globalObj
<span>}</span><span>)</span><span>;</span>
<span>//这个时候，我们执行了如下操作。 </span>
globalObj<span>.</span>text1<span>=</span><span>'hello,text1'</span><span>;</span>
<span>//我们应该需要通知 o1 以及 o2 两个 vm 实例进行视图的更新，「依赖收集」会让 text1 这个数据知道“哦～有两个地方依赖我的数据，我变化的时候需要通知它们～</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div></li>
<li>
<p>通过依赖收集最终会形成数据与视图的一种对应关系：</p>
<ul>
<li>一个数据 data 对应一个依赖对象，依赖对象用于收集 Watcher 对象；</li>
<li>Watcher 通常包括组件的 渲染 Watcher，用户自定义 Watcher，computed Watcher等。</li>
<li>当数据 data 发生变化时，会通过 遍历 Dep 对象，调用 watcher.run() 进行视图更新。</li>
</ul>
<p><img src="./../images/reactive.png" alt="" /></p>
</li>
<li>
<p>依赖收集流程</p>
<ul>
<li>
<p>一次依赖收集流程</p>
<p><img src="./../images/一次依赖收集.png" alt="" /></p>
</li>
<li>
<p>嵌套组件依赖收集流程： 依赖收集发生在组件渲染时，嵌套组件创建过程是深度遍历子组件和父组件，因此嵌套组件依赖收集也是先收集子组件依赖后收集父组件依赖。</p>
<p><img src="./../images/嵌套组件依赖收集.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
<p><strong>依赖对象Dep &amp; Watcher 对象</strong></p>
<ul>
<li>
<p>Dep 对象：<code>Dep</code> 是一个 Class，它定义了一些属性和方法，对 <code>Watcher</code> 的管理对象。定义在 <code>src/core/observer/dep.js</code> 中：</p>
<div><pre><code><span>/* @flow */</span>

<span>import</span> type Watcher <span>from</span> <span>'./watcher'</span>
<span>import</span> <span>{</span> remove <span>}</span> <span>from</span> <span>'../util/index'</span>

<span>let</span> uid <span>=</span> <span>0</span>
<span>/**
 * A dep is an observable that can have multiple
 * directives subscribing to it.
 * Dep 实际上就是对 Watcher 的一种管理，Dep 脱离 Watcher 单独存在是没有意义的
 */</span>
<span>export</span> <span>default</span> <span>class</span> <span>Dep</span> <span>{</span>
  <span>static</span> target<span>:</span> <span>?</span>Watcher<span>;</span><span>//静态属性 target，这是一个全局唯一 Watcher，在同一时间只能有一个全局的 Watcher 被计算</span>
  id<span>:</span> number<span>;</span>
  subs<span>:</span> Array<span>&lt;</span>Watcher<span>></span><span>;</span>

  <span>constructor</span> <span>(</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>id <span>=</span> uid<span>++</span>
    <span>this</span><span>.</span>subs <span>=</span> <span>[</span><span>]</span>
  <span>}</span>

  <span>/*添加一个观察者对象*/</span>
  <span>addSub</span> <span>(</span><span>sub<span>:</span> Watcher</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>subs<span>.</span><span>push</span><span>(</span>sub<span>)</span>
  <span>}</span>

  <span>/*移除一个观察者对象*/</span>
  <span>removeSub</span> <span>(</span><span>sub<span>:</span> Watcher</span><span>)</span> <span>{</span>
    <span>remove</span><span>(</span><span>this</span><span>.</span>subs<span>,</span> sub<span>)</span>
  <span>}</span>

  <span>/*依赖收集，当存在Dep.target的时候添加观察者对象*/</span>
  <span>depend</span> <span>(</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>Dep<span>.</span>target<span>)</span> <span>{</span>
      Dep<span>.</span>target<span>.</span><span>addDep</span><span>(</span><span>this</span><span>)</span>
    <span>}</span>
  <span>}</span>

  <span>/*通知所有订阅者*/</span>
  <span>notify</span> <span>(</span><span>)</span> <span>{</span>
    <span>// stabilize the subscriber list first</span>
    <span>const</span> subs <span>=</span> <span>this</span><span>.</span>subs<span>.</span><span>slice</span><span>(</span><span>)</span>
    <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>,</span> l <span>=</span> subs<span>.</span>length<span>;</span> i <span>&lt;</span> l<span>;</span> i<span>++</span><span>)</span> <span>{</span>
      subs<span>[</span>i<span>]</span><span>.</span><span>update</span><span>(</span><span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>

<span>// the current target watcher being evaluated.</span>
<span>// this is globally unique because there could be only one</span>
<span>// watcher being evaluated at any time.</span>
<span>/**静态属性 target，这是一个全局唯一 Watcher，这是一个非常巧妙的设计，因为在同一时间只能有一个全局的 Watcher 被计算，另外它的自身属性 subs 也是 Watcher 的数组 */</span>
<span>/*依赖收集完需要将Dep.target设为null，防止后面重复添加依赖。*/</span>
Dep<span>.</span>target <span>=</span> <span>null</span>
<span>const</span> targetStack <span>=</span> <span>[</span><span>]</span>

<span>/*将watcher观察者实例设置给Dep.target，用以依赖收集。同时将该实例存入target栈中*/</span>
<span>export</span> <span>function</span> <span>pushTarget</span> <span>(</span><span>_target<span>:</span> Watcher</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span>Dep<span>.</span>target<span>)</span> targetStack<span>.</span><span>push</span><span>(</span>Dep<span>.</span>target<span>)</span>
  Dep<span>.</span>target <span>=</span> _target
<span>}</span>

<span>/*将观察者实例从target栈中取出并设置给Dep.target*/</span>
<span>export</span> <span>function</span> <span>popTarget</span> <span>(</span><span>)</span> <span>{</span>
  Dep<span>.</span>target <span>=</span> targetStack<span>.</span><span>pop</span><span>(</span><span>)</span>
<span>}</span>

</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br></div></div></li>
<li>
<p>Watcher 对象：进行依赖收集的观察者，同时在数据变更时通知响应数据改变。</p>
</li>
</ul>
<p><strong>过程分析</strong></p>
<ul>
<li>Vue 的 mount 过程是通过 <code>mountComponent</code> 函数，其中有一段比较重要的逻辑，大致如下：</li>
</ul>
<div><pre><code><span>updateComponent</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
  vm<span>.</span><span>_update</span><span>(</span>vm<span>.</span><span>_render</span><span>(</span><span>)</span><span>,</span> hydrating<span>)</span>
<span>}</span>
<span>new</span> <span>Watcher</span><span>(</span>vm<span>,</span> updateComponent<span>,</span> noop<span>,</span> <span>{</span>
  <span>before</span> <span>(</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>vm<span>.</span>_isMounted<span>)</span> <span>{</span>
      <span>callHook</span><span>(</span>vm<span>,</span> <span>'beforeUpdate'</span><span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span><span>,</span> <span>true</span> <span>/* isRenderWatcher */</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div><p>当我们去实例化一个渲染 <code>watcher</code> 的时候，首先进入 <code>watcher</code> 的构造函数逻辑，然后会执行它的 <code>this.get()</code> 方法，进入 <code>get</code> 函数，首先会执行：</p>
<div><pre><code><span>pushTarget</span><span>(</span><span>this</span><span>)</span>
</code></pre>
<div><span>1</span><br></div></div><p><code>pushTarget</code> 的定义在 <code>src/core/observer/dep.js</code> 中：</p>
<div><pre><code><span>export</span> <span>function</span> <span>pushTarget</span> <span>(</span><span>_target<span>:</span> Watcher</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span>Dep<span>.</span>target<span>)</span> targetStack<span>.</span><span>push</span><span>(</span>Dep<span>.</span>target<span>)</span>
  Dep<span>.</span>target <span>=</span> _target
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br></div></div><p>实际上就是把 <code>Dep.target</code> 赋值为当前的渲染 <code>watcher</code> 并压栈（为了恢复用）。接着又执行了：</p>
<div><pre><code>value <span>=</span> <span>this</span><span>.</span><span>getter</span><span>.</span><span>call</span><span>(</span>vm<span>,</span> vm<span>)</span>
</code></pre>
<div><span>1</span><br></div></div><p><code>this.getter</code> 对应就是 <code>updateComponent</code> 函数，这实际上就是在执行：</p>
<div><pre><code>vm<span>.</span><span>_update</span><span>(</span>vm<span>.</span><span>_render</span><span>(</span><span>)</span><span>,</span> hydrating<span>)</span>
</code></pre>
<div><span>1</span><br></div></div><p>它会先执行 <code>vm._render()</code> 方法，因为之前分析过这个方法会生成 渲染 VNode，并且在这个过程中会对 <code>vm</code> 上的数据访问，这个时候就触发了数据对象的 getter。</p>
<p>那么每个对象值的 getter 都持有一个 <code>dep</code>，在触发 getter 的时候会调用 <code>dep.depend()</code> 方法，也就会执行 <code>Dep.target.addDep(this)</code>。</p>
<p>刚才我们提到这个时候 <code>Dep.target</code> 已经被赋值为渲染 <code>watcher</code>，那么就执行到 <code>addDep</code> 方法：</p>
<div><pre><code><span>addDep</span> <span>(</span><span>dep<span>:</span> Dep</span><span>)</span> <span>{</span>
  <span>const</span> id <span>=</span> dep<span>.</span>id
  <span>if</span> <span>(</span><span>!</span><span>this</span><span>.</span>newDepIds<span>.</span><span>has</span><span>(</span>id<span>)</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>newDepIds<span>.</span><span>add</span><span>(</span>id<span>)</span>
    <span>this</span><span>.</span>newDeps<span>.</span><span>push</span><span>(</span>dep<span>)</span>
    <span>if</span> <span>(</span><span>!</span><span>this</span><span>.</span>depIds<span>.</span><span>has</span><span>(</span>id<span>)</span><span>)</span> <span>{</span>
      dep<span>.</span><span>addSub</span><span>(</span><span>this</span><span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div><p>这时候会做一些逻辑判断（保证同一数据不会被添加多次）后执行 <code>dep.addSub(this)</code>，那么就会执行 <code>this.subs.push(sub)</code>，也就是说把当前的 <code>watcher</code> 订阅到这个数据持有的 <code>dep</code> 的 <code>subs</code> 中，这个目的是为后续数据变化时候能通知到哪些 <code>subs</code> 做准备。</p>
<p>所以在 <code>vm._render()</code> 过程中，会触发所有数据的 getter，这样实际上已经完成了一个依赖收集的过程。那么到这里就结束了么，其实并没有，再完成依赖收集后，还有几个逻辑要执行，首先是：</p>
<div><pre><code><span>if</span> <span>(</span><span>this</span><span>.</span>deep<span>)</span> <span>{</span>
  <span>traverse</span><span>(</span>value<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><p>这个是要递归去访问 <code>value</code>，触发它所有子项的 <code>getter</code>，这个之后会详细讲。接下来执行：</p>
<div><pre><code><span>popTarget</span><span>(</span><span>)</span>
</code></pre>
<div><span>1</span><br></div></div><p><code>popTarget</code> 的定义在 <code>src/core/observer/dep.js</code> 中：</p>
<div><pre><code>Dep<span>.</span>target <span>=</span> targetStack<span>.</span><span>pop</span><span>(</span><span>)</span>
</code></pre>
<div><span>1</span><br></div></div><p>实际上就是把 <code>Dep.target</code> 恢复成上一个状态，因为当前 vm 的数据依赖收集已经完成，那么对应的渲染<code>Dep.target</code> 也需要改变。最后执行：</p>
<div><pre><code><span>cleanupDeps</span> <span>(</span><span>)</span> <span>{</span>
  <span>let</span> i <span>=</span> <span>this</span><span>.</span>deps<span>.</span>length
<span>while</span> <span>(</span>i<span>--</span><span>)</span> <span>{</span>
    <span>const</span> dep <span>=</span> <span>this</span><span>.</span>deps<span>[</span>i<span>]</span>
  <span>if</span> <span>(</span><span>!</span><span>this</span><span>.</span>newDepIds<span>.</span><span>has</span><span>(</span>dep<span>.</span>id<span>)</span><span>)</span> <span>{</span>
      dep<span>.</span><span>removeSub</span><span>(</span><span>this</span><span>)</span>
    <span>}</span>
  <span>}</span>
  <span>let</span> tmp <span>=</span> <span>this</span><span>.</span>depIds
  <span>this</span><span>.</span>depIds <span>=</span> <span>this</span><span>.</span>newDepIds
  <span>this</span><span>.</span>newDepIds <span>=</span> tmp
  <span>this</span><span>.</span>newDepIds<span>.</span><span>clear</span><span>(</span><span>)</span>
  tmp <span>=</span> <span>this</span><span>.</span>deps
  <span>this</span><span>.</span>deps <span>=</span> <span>this</span><span>.</span>newDeps
  <span>this</span><span>.</span>newDeps <span>=</span> tmp
  <span>this</span><span>.</span>newDeps<span>.</span>length <span>=</span> <span>0</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br></div></div><p>考虑到 Vue 是数据驱动的，所以每次数据变化都会重新 render，那么 <code>vm._render()</code> 方法又会再次执行，并再次触发数据的 getters，所以 <code>Wathcer</code> 在构造函数中会初始化 2 个 <code>Dep</code> 实例数组，<code>newDeps</code> 表示新添加的 <code>Dep</code> 实例数组，而 <code>deps</code> 表示上一次添加的 <code>Dep</code> 实例数组。</p>
<p>在执行 <code>cleanupDeps</code> 函数的时候，会首先遍历 <code>deps</code>，移除对 <code>dep</code> 的订阅，然后把 <code>newDepIds</code> 和 <code>depIds</code> 交换，<code>newDeps</code> 和 <code>deps</code> 交换，并把 <code>newDepIds</code> 和 <code>newDeps</code> 清空。</p>
<p>那么为什么需要做 <code>deps</code> 订阅的移除呢，在添加 <code>deps</code> 的订阅过程，已经能通过 <code>id</code> 去重避免重复订阅了。</p>
<p>考虑到一种场景，我们的模板会根据 <code>v-if</code> 去渲染不同子模板 a 和 b，当我们满足某种条件的时候渲染 a 的时候，会访问到 a 中的数据，这时候我们对 a 使用的数据添加了 getter，做了依赖收集，那么当我们去修改 a 的数据的时候，理应通知到这些订阅者。那么如果我们一旦改变了条件渲染了 b 模板，又会对 b 使用的数据添加了 getter，如果我们没有依赖移除的过程，那么这时候我去修改 a 模板的数据，会通知 a 数据的订阅的回调，这显然是有浪费的。</p>
<h3 id="首次渲染"> 首次渲染</h3>
<ul>
<li>
<p>首次渲染为 vue 通过初始化数据渲染出视图的过程，未涉及数据修改后重新触发视图渲染流程。</p>
</li>
<li>
<p>首次渲染流程：通过 初始化 -&gt; 挂载组件 -&gt; 模板编译 -&gt; 依赖收集 -&gt; 触发 render() 渲染视图 -&gt; 完成 vue 视图的首次渲染。</p>
</li>
<li>
<p>首次渲染阶段：</p>
<ul>
<li>渲染阶段：将 render function 渲染成 Virtual Dom Node; 对应以下 render -&gt; patch 阶段；</li>
<li>挂载阶段：将  Virtual Dom Node 调用 DOM API 创建页面；对应以下 render -&gt; patch  阶段;</li>
</ul>
<p><img src="./../flow-graph/首次渲染总流程.png" alt="" /></p>
</li>
</ul>
<h4 id="virtual-dom"> Virtual DOM</h4>
<p><img src="./../images/vdom.png" alt="" /></p>
<ul>
<li>
<p>Vue 中 定义了 Virtual DOM 的数据结构，映射到真实的 DOM 实际上要经历 VNode 的 create、patch、diff 等过程：</p>
<ul>
<li>创建过程：在模板编译以后，通过 render() 渲染时，调用 createElement 创建 父Vnode 占位符；</li>
</ul>
</li>
<li>
<p>path 过程：把 VNode 渲染成真实的 DOM 的过程。</p>
<ul>
<li>首先，通过另一个 <code>createElem()</code> ，递归方式深度遍历创建子组件 VNode，通过 <code>insert()</code> 插入到真实 dom；</li>
<li>更新父占 VNode 位节点，最后创建了一个完整的 DOM 树并插入到 Body 上</li>
<li>当数据更新时：path 过程会执行 diff 比较，记录下两棵树的差异，最后将记录的有差异的地方应用到真正的 DOM 树中去，完成视图就更新。</li>
</ul>
</li>
<li>
<p>Virtual Dom 结构：</p>
</li>
</ul>
<div><pre><code><span>// Virtual DOM</span>
<span>{</span> 
  tag<span>:</span><span>'div'</span><span>,</span>	<span>/*说明这是一个div标签*/</span>
	children<span>:</span><span>[</span>	<span>/*存放该标签的子节点*/</span>
		<span>{</span>
			tag<span>:</span><span>'a'</span><span>,</span>	<span>/*说明这是一个a标签*/</span>
			text<span>:</span><span>'clickme'</span>	<span>/*标签的内容*/</span>
		<span>}</span>
	<span>]</span>
<span>}</span>
<span>//渲染后的 真实 DOM</span>
<span>&lt;</span>div<span>></span><span>&lt;</span>a<span>></span>clickme<span>&lt;</span><span>/</span>a<span>></span><span>&lt;</span><span>/</span>div<span>></span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div><ul>
<li>
<p>虚拟dom与真实dom映射过程:</p>
<ul>
<li>
<p>1.借助JSX编译器，将文件中的HTML转化成函数的形式</p>
<p>html 元素：</p>
<div><pre><code><span>function</span> <span>render</span><span>(</span><span>)</span> <span>{</span>
    <span>return</span> <span>(</span>
        <span>&lt;</span>ul<span>></span>
            <span>&lt;</span>li<span>></span><span>0</span><span>&lt;</span><span>/</span>li<span>></span>
            <span>{</span>
                <span>[</span><span>1</span><span>,</span> <span>2</span><span>,</span> <span>3</span><span>]</span><span>.</span><span>map</span><span>(</span> <span>i</span> <span>=></span> <span>(</span>
                    <span>&lt;</span>li<span>></span><span>{</span>i<span>}</span><span>&lt;</span><span>/</span>li<span>></span>
                <span>)</span><span>)</span>
            <span>}</span>
        <span>&lt;</span><span>/</span>ul<span>></span>
    <span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div><p>JSX编译后函数：(h()函数为createElement()函数别名)</p>
<div><pre><code><span>function</span> <span>render</span><span>(</span><span>)</span> <span>{</span>
    <span>return</span> <span>h</span><span>(</span>
        <span>'ul'</span><span>,</span>
        <span>null</span><span>,</span>
        <span>h</span><span>(</span>
            <span>'li'</span><span>,</span>
            <span>null</span><span>,</span>
            <span>'0'</span>
        <span>)</span><span>,</span>
        <span>/*
         * 需要将下面这个数组解构出来再放到children数组中
         */</span>
        <span>[</span><span>1</span><span>,</span> <span>2</span><span>,</span> <span>3</span><span>]</span><span>.</span><span>map</span><span>(</span><span>i</span> <span>=></span> <span>h</span><span>(</span>
            <span>'li'</span><span>,</span>
            <span>null</span><span>,</span>
            i
        <span>)</span><span>)</span>
    <span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div></li>
<li>
<p>2.利用这个函数生成虚拟dom对象: 函数转成虚拟dom对象(执行h函数后得到虚拟dom对象）</p>
<div><pre><code><span>{</span>
    tag<span>:</span> <span>"div"</span><span>,</span>
    props<span>:</span> <span>{</span><span>}</span><span>,</span>
    children<span>:</span> <span>[</span>
        <span>"Hello World"</span><span>,</span> 
        <span>{</span>
            tag<span>:</span> <span>"ul"</span><span>,</span>
            props<span>:</span> <span>{</span><span>}</span><span>,</span>
            children<span>:</span> <span>[</span><span>{</span>
                tag<span>:</span> <span>"li"</span><span>,</span>
                props<span>:</span> <span>{</span>
                    id<span>:</span> <span>1</span><span>,</span>
                    <span>class</span><span>:</span> <span>"li-1"</span>
                <span>}</span><span>,</span>
                children<span>:</span> <span>[</span><span>"第"</span><span>,</span> <span>1</span><span>]</span>
            <span>}</span><span>]</span>
        <span>}</span>
    <span>]</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div></li>
<li>
<p>3.通过遍历虚拟对象，生成真实的dom</p>
<div><pre><code><span>// 创建dom元素</span>
<span>function</span> <span>createElement</span><span>(</span><span>vdom</span><span>)</span> <span>{</span>
    <span>// 如果vdom是字符串或者数字类型，则创建文本节点，比如“Hello World”</span>
    <span>if</span> <span>(</span><span>typeof</span> vdom <span>===</span> <span>'string'</span> <span>||</span> <span>typeof</span> vdom <span>===</span> <span>'number'</span><span>)</span> <span>{</span>
        <span>return</span> doc<span>.</span><span>createTextNode</span><span>(</span>vdom<span>)</span><span>;</span>
    <span>}</span>

    <span>const</span> <span>{</span>tag<span>,</span> props<span>,</span> children<span>}</span> <span>=</span> vdom<span>;</span>

    <span>// 1. 创建元素</span>
    <span>const</span> element <span>=</span> doc<span>.</span><span>createElement</span><span>(</span>tag<span>)</span><span>;</span>

    <span>// 2. 属性赋值</span>
    <span>setProps</span><span>(</span>element<span>,</span> props<span>)</span><span>;</span>

    <span>// 3. 创建子元素</span>
    <span>// appendChild在执行的时候，会检查当前的this是不是dom对象，因此要bind一下</span>
    children<span>.</span><span>map</span><span>(</span>createElement<span>)</span>
            <span>.</span><span>forEach</span><span>(</span>element<span>.</span><span>appendChild</span><span>.</span><span>bind</span><span>(</span>element<span>)</span><span>)</span><span>;</span>

    <span>return</span> element<span>;</span>
<span>}</span>

<span>// 属性赋值</span>
<span>function</span> <span>setProps</span><span>(</span><span>element<span>,</span> props</span><span>)</span> <span>{</span>
    <span>for</span> <span>(</span><span>let</span> key <span>in</span> props<span>)</span> <span>{</span>
        element<span>.</span><span>setAttribute</span><span>(</span>key<span>,</span> props<span>[</span>key<span>]</span><span>)</span><span>;</span>
    <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br></div></div></li>
<li>
<p>生成dom时，vue.js 使用一层适配层了，将不同平台的  API  封装在内，以同样的接口对外提供</p>
<div><pre><code><span>//nodeOps 对象做适配，根据  platform  区分不同平台来执行当前平台对应的 API，而对外则是提供了一致的接口，供  Virtual DOM  来调用</span>
<span>const</span> nodeOps<span>=</span><span>{</span>
    <span>setTextContent</span><span>(</span><span>text</span><span>)</span><span>{</span>
        <span>if</span><span>(</span>platform<span>===</span><span>'weex'</span><span>)</span><span>{</span>
            node<span>.</span>parentNode<span>.</span><span>setAttr</span><span>(</span><span>'value'</span><span>,</span>
            text<span>)</span><span>;</span>
        <span>}</span><span>elseif</span><span>(</span>platform<span>===</span><span>'web'</span><span>)</span><span>{</span>
            node<span>.</span>textContent<span>=</span>text<span>;</span>
        <span>}</span>
    <span>}</span><span>,</span>
    <span>parentNode</span><span>(</span><span>)</span><span>{</span>
        <span>//......</span>
    <span>}</span><span>,</span>
    <span>removeChild</span><span>(</span><span>)</span><span>{</span>
        <span>//......</span>
    <span>}</span><span>,</span>
    <span>nextSibling</span><span>(</span><span>)</span><span>{</span>
        <span>//......</span>
    <span>}</span><span>,</span>
    <span>insertBefore</span><span>(</span><span>)</span><span>{</span>
        <span>//......</span>
    <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br></div></div></li>
</ul>
</li>
<li>
<p>Virtual DOM 源码中定义：定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/vdom/vnode.js" target="_blank" rel="noopener noreferrer">src/core/vdom/vnode.js</a> 中。</p>
</li>
</ul>
<div><pre><code><span>!</span><span>[</span>render方法<span>]</span><span>(</span><span>.</span><span>.</span><span>/</span>flow<span>-</span>graph<span>/</span>render方法<span>.</span>png<span>)</span><span>export</span> <span>default</span> <span>class</span> <span>VNode</span> <span>{</span>
  tag<span>:</span> string <span>|</span> <span>void</span><span>;</span>
  data<span>:</span> VNodeData <span>|</span> <span>void</span><span>;</span>
  children<span>:</span> <span>?</span>Array<span>&lt;</span>VNode<span>></span><span>;</span>
  text<span>:</span> string <span>|</span> <span>void</span><span>;</span>
  elm<span>:</span> Node <span>|</span> <span>void</span><span>;</span>
  ns<span>:</span> string <span>|</span> <span>void</span><span>;</span>
  context<span>:</span> Component <span>|</span> <span>void</span><span>;</span> <span>// rendered in this component's scope</span>
  key<span>:</span> string <span>|</span> number <span>|</span> <span>void</span><span>;</span>
  componentOptions<span>:</span> VNodeComponentOptions <span>|</span> <span>void</span><span>;</span>
  componentInstance<span>:</span> Component <span>|</span> <span>void</span><span>;</span> <span>// component instance</span>
  parent<span>:</span> VNode <span>|</span> <span>void</span><span>;</span> <span>// component placeholder node</span>

  <span>// strictly internal</span>
  raw<span>:</span> boolean<span>;</span> <span>// contains raw HTML? (server only)</span>
  isStatic<span>:</span> boolean<span>;</span> <span>// hoisted static node</span>
  isRootInsert<span>:</span> boolean<span>;</span> <span>// necessary for enter transition check</span>
  isComment<span>:</span> boolean<span>;</span> <span>// empty comment placeholder?</span>
  isCloned<span>:</span> boolean<span>;</span> <span>// is a cloned node?</span>
  isOnce<span>:</span> boolean<span>;</span> <span>// is a v-once node?</span>
  asyncFactory<span>:</span> Function <span>|</span> <span>void</span><span>;</span> <span>// async component factory function</span>
  asyncMeta<span>:</span> Object <span>|</span> <span>void</span><span>;</span>
  isAsyncPlaceholder<span>:</span> boolean<span>;</span>
  ssrContext<span>:</span> Object <span>|</span> <span>void</span><span>;</span>
  fnContext<span>:</span> Component <span>|</span> <span>void</span><span>;</span> <span>// real context vm for functional nodes</span>
  fnOptions<span>:</span> <span>?</span>ComponentOptions<span>;</span> <span>// for SSR caching</span>
  fnScopeId<span>:</span> <span>?</span>string<span>;</span> <span>// functional scope id support</span>

  <span>constructor</span> <span>(</span>
    <span>tag<span>?</span><span>:</span> string<span>,</span>
    data<span>?</span><span>:</span> VNodeData<span>,</span>
    children<span>?</span><span>:</span> <span>?</span>Array<span>&lt;</span>VNode<span>></span><span>,</span>
    text<span>?</span><span>:</span> string<span>,</span>
    elm<span>?</span><span>:</span> Node<span>,</span>
    context<span>?</span><span>:</span> Component<span>,</span>
    componentOptions<span>?</span><span>:</span> VNodeComponentOptions<span>,</span>
    asyncFactory<span>?</span><span>:</span> Function</span>
  <span>)</span> <span>{</span>
    <span>this</span><span>.</span>tag <span>=</span> tag
    <span>this</span><span>.</span>data <span>=</span> data
    <span>this</span><span>.</span>children <span>=</span> children
    <span>this</span><span>.</span>text <span>=</span> text
    <span>this</span><span>.</span>elm <span>=</span> elm
    <span>this</span><span>.</span>ns <span>=</span> <span>undefined</span>
    <span>this</span><span>.</span>context <span>=</span> context
    <span>this</span><span>.</span>fnContext <span>=</span> <span>undefined</span>
    <span>this</span><span>.</span>fnOptions <span>=</span> <span>undefined</span>
    <span>this</span><span>.</span>fnScopeId <span>=</span> <span>undefined</span>
    <span>this</span><span>.</span>key <span>=</span> data <span>&amp;&amp;</span> data<span>.</span>key
    <span>this</span><span>.</span>componentOptions <span>=</span> componentOptions
    <span>this</span><span>.</span>componentInstance <span>=</span> <span>undefined</span>
    <span>this</span><span>.</span>parent <span>=</span> <span>undefined</span>
    <span>this</span><span>.</span>raw <span>=</span> <span>false</span>
    <span>this</span><span>.</span>isStatic <span>=</span> <span>false</span>
    <span>this</span><span>.</span>isRootInsert <span>=</span> <span>true</span>
    <span>this</span><span>.</span>isComment <span>=</span> <span>false</span>
    <span>this</span><span>.</span>isCloned <span>=</span> <span>false</span>
    <span>this</span><span>.</span>isOnce <span>=</span> <span>false</span>
    <span>this</span><span>.</span>asyncFactory <span>=</span> asyncFactory
    <span>this</span><span>.</span>asyncMeta <span>=</span> <span>undefined</span>
    <span>this</span><span>.</span>isAsyncPlaceholder <span>=</span> <span>false</span>
  <span>}</span>

  <span>// DEPRECATED: alias for componentInstance for backwards compat.</span>
  <span>/* istanbul ignore next */</span>
  <span>get</span> <span>child</span> <span>(</span><span>)</span><span>:</span> Component <span>|</span> <span>void</span> <span>{</span>
    <span>return</span> <span>this</span><span>.</span>componentInstance
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br></div></div><ul>
<li>VNode 是对真实 DOM 的一种抽象描述，它的核心定义无非就几个关键属性，标签名、数据、子节点、键值等，其它属性都是都是用来扩展 VNode 的灵活性以及实现一些特殊 feature 的。由于 VNode 只是用来映射到真实 DOM 的渲染，不需要包含操作 DOM 的方法，因此它是非常轻量和简单的。</li>
</ul>
<h4 id="render"> render</h4>
<ul>
<li>
<p>作用： 把实例渲染成一个虚拟 Node，生成 Virtual DOM。</p>
</li>
<li>
<p>render 方法生成：通过模板编译将 ast 转换成 code，生成可执行的 render() 挂到 vue 实例 选项和 私有方法中。</p>
</li>
<li>
<p>render 调用： 在依赖收集阶段， <code>new Watcher(vm, updateComponent, noop)</code> 时回调的  <code>updateComponent</code> 中调用。</p>
</li>
<li>
<p>render 流程图：</p>
<p><img src="./../flow-graph/render方法.png" alt="" /></p>
</li>
<li>
<p>Vue 的 <code>_render</code> 方法：定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/instance/render.js" target="_blank" rel="noopener noreferrer">src/core/instance/render.js</a> 文件中：</p>
<div><pre><code><span>export</span> <span>function</span> <span>renderMixin</span> <span>(</span><span>Vue<span>:</span> Class<span>&lt;</span>Component<span>></span></span><span>)</span> <span>{</span>
  <span>//省略无关代码...</span>
  <span>/*vm._render 最终是通过执行 createElement 方法并返回 vnode，它是一个虚拟 Node*/</span>
  <span>Vue</span><span>.</span>prototype<span>.</span><span>_render</span> <span>=</span> <span>function</span> <span>(</span><span>)</span><span>:</span> VNode <span>{</span>
    <span>const</span> vm<span>:</span> Component <span>=</span> <span>this</span>
    <span>const</span> <span>{</span>
      render<span>,</span>
      staticRenderFns<span>,</span>
      _parentVnode
    <span>}</span> <span>=</span> vm<span>.</span>$options

    <span>if</span> <span>(</span>vm<span>.</span>_isMounted<span>)</span> <span>{</span>
      <span>// clone slot nodes on re-renders</span>
      <span>/*在重新渲染时会克隆槽位节点*/</span>
      <span>for</span> <span>(</span><span>const</span> key <span>in</span> vm<span>.</span>$slots<span>)</span> <span>{</span>
        vm<span>.</span>$slots<span>[</span>key<span>]</span> <span>=</span> <span>cloneVNodes</span><span>(</span>vm<span>.</span>$slots<span>[</span>key<span>]</span><span>)</span>
      <span>}</span>
    <span>}</span>

    <span>/*作用域slot*/</span>
    vm<span>.</span>$scopedSlots <span>=</span> <span>(</span>_parentVnode <span>&amp;&amp;</span> _parentVnode<span>.</span>data<span>.</span>scopedSlots<span>)</span> <span>||</span> emptyObject

    <span>if</span> <span>(</span>staticRenderFns <span>&amp;&amp;</span> <span>!</span>vm<span>.</span>_staticTrees<span>)</span> <span>{</span>
      <span>/*用来存放static节点，已经被渲染的并且不存在v-for中的static节点不需要重新渲染，只需要进行浅拷贝*/</span>
      vm<span>.</span>_staticTrees <span>=</span> <span>[</span><span>]</span>
    <span>}</span>
    <span>// set parent vnode. this allows render functions to have access</span>
    <span>// to the data on the placeholder node.</span>
    vm<span>.</span>$vnode <span>=</span> _parentVnode
    <span>// render self</span>
    <span>/*渲染*/</span>
    <span>let</span> vnode
    <span>try</span> <span>{</span>
      <span>/**
       * _render方法核心：调用render函数，返回一个VNode节点
       * @param {} vm._renderProxy 生产环境下为vue实例，开发环境为代理 （./init.js中初始化了_renderProxy）
       * @param {} vm.$createElement 内置创建VNode方法，render时候调用该方法创建VNode（定义在../vdom/create-element）
       */</span>
      vnode <span>=</span> <span>render</span><span>.</span><span>call</span><span>(</span>vm<span>.</span>_renderProxy<span>,</span> vm<span>.</span>$createElement<span>)</span>
    <span>}</span> <span>catch</span> <span>(</span>e<span>)</span> <span>{</span>
      <span>handleError</span><span>(</span>e<span>,</span> vm<span>,</span> <span><span>`</span><span>render function</span><span>`</span></span><span>)</span>
      <span>// return error render result,</span>
      <span>// or previous vnode to prevent render error causing blank component</span>
      <span>/* istanbul ignore else */</span>
      <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
        vnode <span>=</span> vm<span>.</span>$options<span>.</span>renderError
          <span>?</span> vm<span>.</span>$options<span>.</span><span>renderError</span><span>.</span><span>call</span><span>(</span>vm<span>.</span>_renderProxy<span>,</span> vm<span>.</span>$createElement<span>,</span> e<span>)</span>
          <span>:</span> vm<span>.</span>_vnode
      <span>}</span> <span>else</span> <span>{</span>
        vnode <span>=</span> vm<span>.</span>_vnode
      <span>}</span>
    <span>}</span>
    <span>// return empty vnode in case the render function errored out</span>
    <span>/*如果VNode节点没有创建成功则创建一个空节点*/</span>
    <span>if</span> <span>(</span><span>!</span><span>(</span>vnode <span>instanceof</span> <span>VNode</span><span>)</span><span>)</span> <span>{</span>
      <span>// VNode为数组，表示当前根节点不止一个节点，报错</span>
      <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> Array<span>.</span><span>isArray</span><span>(</span>vnode<span>)</span><span>)</span> <span>{</span>
        <span>warn</span><span>(</span>
          <span>'Multiple root nodes returned from render function. Render function '</span> <span>+</span>
          <span>'should return a single root node.'</span><span>,</span>
          vm
        <span>)</span>
      <span>}</span>
      vnode <span>=</span> <span>createEmptyVNode</span><span>(</span><span>)</span>
    <span>}</span>
    <span>// set parent</span>
    vnode<span>.</span>parent <span>=</span> _parentVnode
    <span>return</span> vnode
  <span>}</span>
<span>// 省略无关代码...</span>
<span>}</span>

</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br></div></div><ul>
<li>
<p><code>render</code> 函数核心代码： <code>vnode = render.call(vm._renderProxy, vm.$createElement)</code></p>
</li>
<li>
<p><code>render</code> 函数中的 参数 <code>vm.$createElement</code>  就是我们编写传入的 <code>render</code> 选项的<code>createElement</code> 方法。</p>
<div><pre><code><span><span><span>&lt;</span>div</span> <span>id</span><span><span>=</span><span>"</span>app<span>"</span></span><span>></span></span>
  {{ message }}
<span><span><span>&lt;/</span>div</span><span>></span></span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><p>相当于我们编写如下 <code>render</code> 函数：</p>
<div><pre><code><span>render</span><span>:</span> <span>function</span> <span>(</span><span>createElement</span><span>)</span> <span>{</span>
  <span>return</span> <span>createElement</span><span>(</span><span>'div'</span><span>,</span> <span>{</span>
     attrs<span>:</span> <span>{</span>
        id<span>:</span> <span>'app'</span>
      <span>}</span><span>,</span>
  <span>}</span><span>,</span> <span>this</span><span>.</span>message<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
<li>
<p><code>vm.$createElement</code> 方法：定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/instance/render.js" target="_blank" rel="noopener noreferrer">src/core/instance/render.js</a> 文件中。</p>
<div><pre><code><span>/*初始化render*/</span>
<span>export</span> <span>function</span> <span>initRender</span> <span>(</span><span>vm<span>:</span> Component</span><span>)</span> <span>{</span>
  vm<span>.</span>_vnode <span>=</span> <span>null</span> <span>// the root of the child tree</span>
  vm<span>.</span>_staticTrees <span>=</span> <span>null</span>
  <span>const</span> parentVnode <span>=</span> vm<span>.</span>$vnode <span>=</span> vm<span>.</span>$options<span>.</span>_parentVnode <span>// the placeholder node in parent tree  父树中的占位符节点</span>
  <span>const</span> renderContext <span>=</span> parentVnode <span>&amp;&amp;</span> parentVnode<span>.</span>context
  vm<span>.</span>$slots <span>=</span> <span>resolveSlots</span><span>(</span>vm<span>.</span>$options<span>.</span>_renderChildren<span>,</span> renderContext<span>)</span>
  vm<span>.</span>$scopedSlots <span>=</span> emptyObject
  <span>// bind the createElement fn to this instance</span>
  <span>// so that we get proper render context inside it.</span>
  <span>// args order: tag, data, children, normalizationType, alwaysNormalize</span>
  <span>// internal version is used by render functions compiled from templates</span>
  <span>/**
   * 被编译生成的render函数使用的方法
   * 将createElement函数绑定到该实例上，该vm存在闭包中，不可修改，vm实例则固定。这样我们就可以得到正确的上下文渲染
   */</span>
  vm<span>.</span><span>_c</span> <span>=</span> <span>(</span><span>a<span>,</span> b<span>,</span> c<span>,</span> d</span><span>)</span> <span>=></span> <span>createElement</span><span>(</span>vm<span>,</span> a<span>,</span> b<span>,</span> c<span>,</span> d<span>,</span> <span>false</span><span>)</span>
  <span>// normalization is always applied for the public version, used in</span>
  <span>// user-written render functions.</span>
  <span>/**
   * 给手写的render函数提供的创建 VNode 的方法
   * 常规方法被用于公共版本，被用来作为用户界面的渲染方法
   */</span>
  vm<span>.</span><span>$createElement</span> <span>=</span> <span>(</span><span>a<span>,</span> b<span>,</span> c<span>,</span> d</span><span>)</span> <span>=></span> <span>createElement</span><span>(</span>vm<span>,</span> a<span>,</span> b<span>,</span> c<span>,</span> d<span>,</span> <span>true</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br></div></div><ul>
<li><code>vm.$createElement</code> 方法定义是在执行 <code>initRender</code> 方法的时候，可以看到除了 <code>vm.$createElement</code> 方法，还有一个 <code>vm._c</code> 方法，它是被模板编译成的 <code>render</code> 函数使用，而 <code>vm.$createElement</code> 是用户手写 <code>render</code> 方法使用的， 这俩个方法支持的参数相同，并且内部都调用了 <code>createElement</code> 方法。</li>
</ul>
</li>
</ul>
</li>
<li>
<p><code>createElement()</code> 方法:对 _createElement 方法的封装，它允许传入的参数更加灵活。</p>
<div><pre><code><span>/**
 * createElement 方法实际上是对 _createElement 方法的封装，它允许传入的参数更加灵活，
 * 在处理这些参数后，调用真正创建 VNode 的函数 _createElement
 * @param {*} context 表示 VNode 的上下文环境，它是 Component 类型
 * @param {*} tag 表示标签，它可以是一个字符串，也可以是一个 Component
 * @param {*} data 表示 VNode 的数据，它是一个 VNodeData 类型，可以在 flow/vnode.js 中找到它的定义
 * @param {*} children 表示当前 VNode 的子节点，它是任意类型的，它接下来需要被规范为标准的 VNode 数组
 * @param {*} normalizationType 表示子节点规范的类型，类型不同规范的方法也就不一样，它主要是参考 render 函数是编译生成的还是用户手写的
 * @param {*} alwaysNormalize 
 */</span>
<span>export</span> <span>function</span> <span>createElement</span> <span>(</span>
  <span>context<span>:</span> Component<span>,</span>
  tag<span>:</span> any<span>,</span>
  data<span>:</span> any<span>,</span>
  children<span>:</span> any<span>,</span>
  normalizationType<span>:</span> any<span>,</span>
  alwaysNormalize<span>:</span> boolean</span>
<span>)</span><span>:</span> VNode <span>{</span>
  <span>/*兼容不传data的情况,将所有参数前移*/</span>
  <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>data<span>)</span> <span>||</span> <span>isPrimitive</span><span>(</span>data<span>)</span><span>)</span> <span>{</span>
    normalizationType <span>=</span> children
    children <span>=</span> data
    data <span>=</span> <span>undefined</span>
  <span>}</span>
  <span>/*如果alwaysNormalize为true，则normalizationType标记为ALWAYS_NORMALIZE*/</span>
  <span>if</span> <span>(</span><span>isTrue</span><span>(</span>alwaysNormalize<span>)</span><span>)</span> <span>{</span>
    normalizationType <span>=</span> <span>ALWAYS_NORMALIZE</span>
  <span>}</span>
  <span>/*创建虚拟节点*/</span>
  <span>return</span> <span>_createElement</span><span>(</span>context<span>,</span> tag<span>,</span> data<span>,</span> children<span>,</span> normalizationType<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br></div></div></li>
<li>
<p><code>_createElement()</code> 方法:  创建 VNode，它定义在  <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/vdom/create-elemenet.js" target="_blank" rel="noopener noreferrer">src/core/vdom/create-elemenet.js</a> 中:</p>
<div><pre><code><span>/*创建VNode节点*/</span>
<span>export</span> <span>function</span> <span>_createElement</span> <span>(</span>
  context<span>:</span> Component<span>,</span> <span>// 表示 VNode 的上下文环境</span>
  tag<span>?</span><span>:</span> string <span>|</span> Class<span>&lt;</span>Component<span>></span> <span>|</span> Function <span>|</span> Object<span>,</span> <span>//表示标签</span>
  data<span>?</span><span>:</span> VNodeData<span>,</span> <span>// VNode 的数据</span>
  children<span>?</span><span>:</span> any<span>,</span> <span>//当前 VNode 的子节点，它是任意类型的，它接下来需要被规范为标准的 VNode 数组；</span>
  normalizationType<span>?</span><span>:</span> number <span>//子节点规范的类型，类型不同规范的方法也就不一样，它主要是参考 render 函数是编译生成的还是用户手写的</span>
<span>)</span><span>:</span> VNode <span>{</span>
  <span>/*
    如果data未定义（undefined或者null）或者是data的__ob__已经定义（代表已经被observed，上面绑定了Oberver对象），
    https://cn.vuejs.org/v2/guide/render-function.html#约束，那么创建一个空节点
  */</span>
  <span>if</span> <span>(</span><span>isDef</span><span>(</span>data<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span><span>(</span>data<span>:</span> any<span>)</span><span>.</span>__ob__<span>)</span><span>)</span> <span>{</span>
    process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> <span>warn</span><span>(</span>
      <span><span>`</span><span>Avoid using observed data object as vnode data: </span><span><span>${</span><span>JSON</span><span>.</span><span>stringify</span><span>(</span>data<span>)</span><span>}</span></span><span>\n</span><span>`</span></span> <span>+</span>
      <span>'Always create fresh vnode data objects in each render!'</span><span>,</span>
      context
    <span>)</span>
    <span>//返回空节点</span>
    <span>return</span> <span>createEmptyVNode</span><span>(</span><span>)</span>
  <span>}</span>
  <span>/*如果tag不存在也是创建一个空节点*/</span>
  <span>if</span> <span>(</span><span>!</span>tag<span>)</span> <span>{</span>
    <span>// in case of component :is set to falsy value</span>
    <span>return</span> <span>createEmptyVNode</span><span>(</span><span>)</span>
  <span>}</span>
  <span>// support single function children as default scoped slot</span>
  <span>/*默认默认作用域插槽*/</span>
  <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>children<span>)</span> <span>&amp;&amp;</span>
      <span>typeof</span> children<span>[</span><span>0</span><span>]</span> <span>===</span> <span>'function'</span><span>)</span> <span>{</span>
    data <span>=</span> data <span>||</span> <span>{</span><span>}</span>
    data<span>.</span>scopedSlots <span>=</span> <span>{</span> <span>default</span><span>:</span> children<span>[</span><span>0</span><span>]</span> <span>}</span>
    children<span>.</span>length <span>=</span> <span>0</span>
  <span>}</span>
  <span>/**
   * 由于 Virtual DOM 实际上是一个树状结构，每一个 VNode 可能会有若干个子节点，这些子节点应该也是 VNode 的类型。
   * _createElement 接收的第 4 个参数 children 是任意类型的，因此我们需要把它们规范成 VNode 类型 
   * 根据 normalizationType 的不同，调用了 normalizeChildren(children) 和 simpleNormalizeChildren(children) 方法
   * 它们的定义都在 src/core/vdom/helpers/normalzie-children.js 中*/</span>
  <span>if</span> <span>(</span>normalizationType <span>===</span> <span>ALWAYS_NORMALIZE</span><span>)</span> <span>{</span>
  <span>//normalizeChildren 方法的调用场景有 2 种：</span>
  <span>//一个场景是 render 函数是用户手写的，当 children 只有一个节点的时候，Vue.js 从接口层面允许用户把 children 写成基础类型用来创建单个简单的文本节点，这种情况会调用 createTextVNode 创建一个文本节点的 VNode；</span>
<span>//另一个场景是当编译 slot、v-for 的时候会产生嵌套数组的情况，会调用 normalizeArrayChildren 方法</span>
    children <span>=</span> <span>normalizeChildren</span><span>(</span>children<span>)</span>
  <span>}</span> <span>else</span> <span>if</span> <span>(</span>normalizationType <span>===</span> <span>SIMPLE_NORMALIZE</span><span>)</span> <span>{</span>
  <span>//simpleNormalizeChildren 方法调用场景是 render 函数是编译生成的</span>
  <span>//理论上编译生成的 children 都已经是 VNode 类型的，但这里有一个例外，就是 functional component 函数式组件返回的是一个数组而不是一个根节点，所以会通过 Array.prototype.concat 方法把整个 children 数组打平，让它的深度只有一层</span>
    children <span>=</span> <span>simpleNormalizeChildren</span><span>(</span>children<span>)</span>
  <span>}</span>
  <span>let</span> vnode<span>,</span> ns
  <span>if</span> <span>(</span><span>typeof</span> tag <span>===</span> <span>'string'</span><span>)</span> <span>{</span> <span>//传入createElement()为Vnode对象时，tag为string</span>
    <span>let</span> Ctor
    <span>/*获取tag的名字空间*/</span>
    ns <span>=</span> config<span>.</span><span>getTagNamespace</span><span>(</span>tag<span>)</span>
    <span>/*判断是否是保留的标签*/</span>
    <span>if</span> <span>(</span>config<span>.</span><span>isReservedTag</span><span>(</span>tag<span>)</span><span>)</span> <span>{</span>
      <span>// platform built-in elements</span>
      <span>/*如果是保留的标签则创建一个相应节点*/</span>
      vnode <span>=</span> <span>new</span> <span>VNode</span><span>(</span>
        config<span>.</span><span>parsePlatformTagName</span><span>(</span>tag<span>)</span><span>,</span> data<span>,</span> children<span>,</span>
        <span>undefined</span><span>,</span> <span>undefined</span><span>,</span> context
      <span>)</span>
      <span>/*调用 resolveAsset(context.$options, 'components', tag)，
      即拿 vm.$options.components[tag]，这样我们就可以在 resolveAsset 的时候拿到这个组件的构造函数，
      并作为 createComponent 的钩子的参数*/</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>isDef</span><span>(</span>Ctor <span>=</span> <span>resolveAsset</span><span>(</span>context<span>.</span>$options<span>,</span> <span>'components'</span><span>,</span> tag<span>)</span><span>)</span><span>)</span> <span>{</span>
      <span>//如果是为已注册的组件名，则通过 createComponent 创建一个组件类型的 VNode</span>
      <span>/*从vm实例的option的components中寻找该tag，存在则就是一个组件，创建相应节点，Ctor为组件的构造类*/</span>
      vnode <span>=</span> <span>createComponent</span><span>(</span>Ctor<span>,</span> data<span>,</span> context<span>,</span> children<span>,</span> tag<span>)</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>// unknown or unlisted namespaced elements</span>
      <span>// check at runtime because it may get assigned a namespace when its</span>
      <span>// parent normalizes children</span>
      <span>/*未知的元素，在运行时检查，因为父组件可能在序列化子组件的时候分配一个名字空间*/</span>
      vnode <span>=</span> <span>new</span> <span>VNode</span><span>(</span>
        tag<span>,</span> data<span>,</span> children<span>,</span>
        <span>undefined</span><span>,</span> <span>undefined</span><span>,</span> context
      <span>)</span>
    <span>}</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>//直接传入 createElement() 组件对象</span>
    <span>// direct component options / constructor</span>
    <span>/*tag不是字符串的时候则是组件的构造类，调用createComponent创建组件*/</span>
    vnode <span>=</span> <span>createComponent</span><span>(</span>tag<span>,</span> data<span>,</span> context<span>,</span> children<span>)</span>
  <span>}</span>
  <span>if</span> <span>(</span><span>isDef</span><span>(</span>vnode<span>)</span><span>)</span> <span>{</span>
    <span>/*如果有名字空间，则递归所有子节点应用该名字空间*/</span>
    <span>if</span> <span>(</span>ns<span>)</span> <span>applyNS</span><span>(</span>vnode<span>,</span> ns<span>)</span>
    <span>return</span> vnode
  <span>}</span> <span>else</span> <span>{</span>
    <span>/*如果vnode没有成功创建则创建空节点*/</span>
    <span>return</span> <span>createEmptyVNode</span><span>(</span><span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br><span>93</span><br><span>94</span><br></div></div><ul>
<li>
<p>由于 Virtual DOM 实际上是一个树状结构，每一个 VNode 可能会有若干个子节点，这些子节点应该也是 VNode 的类型。<code>_createElement</code> 接收的第 4 个参数 children 是任意类型的，因此我们需要把它们规范成 VNode 类型。</p>
</li>
<li>
<p>这里根据 <code>normalizationType</code> 的不同，调用了 <code>normalizeChildren(children)</code> 和 <code>simpleNormalizeChildren(children)</code> 方法，它们的定义都在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/vdom/helpers/normalize-children.js" target="_blank" rel="noopener noreferrer">src/core/vdom/helpers/normalzie-children.js</a> 中：</p>
<div><pre><code><span>/**
 * simpleNormalizeChildren 方法调用场景是 render 函数是编译生成的。
 * 理论上编译生成的 children 都已经是 VNode 类型的，但这里有一个例外，
 * 就是 functional component 函数式组件返回的是一个数组而不是一个根节点，
 * 所以会通过 Array.prototype.concat 方法把整个 children 数组打平，让它的深度只有一层
 * 该方法仅考虑最多二维数组情况
 * @param {*} children 
 */</span>
<span>export</span> <span>function</span> <span>simpleNormalizeChildren</span> <span>(</span><span>children<span>:</span> any</span><span>)</span> <span>{</span>
  <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> children<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>children<span>[</span>i<span>]</span><span>)</span><span>)</span> <span>{</span>
      <span>return</span> <span>Array</span><span>.</span>prototype<span>.</span><span>concat</span><span>.</span><span>apply</span><span>(</span><span>[</span><span>]</span><span>,</span> children<span>)</span>
    <span>}</span>
  <span>}</span>
  <span>return</span> children
<span>}</span>

<span>/**
 * normalizeChildren 方法的调用场景有 2 种，一个场景是 render 函数是用户手写的，
 * 当 children 只有一个节点的时候，Vue.js 从接口层面允许用户把 children 写成基础类型用来创建单个简单的文本节点，
 * 这种情况会调用 createTextVNode 创建一个文本节点的 VNode；
 * 另一个场景是当编译 slot、v-for 的时候会产生嵌套数组的情况，会调用 normalizeArrayChildren 方法
 * @param {*} children 
 */</span>
<span>export</span> <span>function</span> <span>normalizeChildren</span> <span>(</span><span>children<span>:</span> any</span><span>)</span><span>:</span> <span>?</span>Array<span>&lt;</span>VNode<span>></span> <span>{</span>
  <span>return</span> <span>isPrimitive</span><span>(</span>children<span>)</span>
    <span>?</span> <span>[</span><span>createTextVNode</span><span>(</span>children<span>)</span><span>]</span> <span>//返回一维数组，数组中包含一个文本VNode</span>
    <span>:</span> Array<span>.</span><span>isArray</span><span>(</span>children<span>)</span>
      <span>?</span> <span>normalizeArrayChildren</span><span>(</span>children<span>)</span>
      <span>:</span> <span>undefined</span>
<span>}</span>

<span>/**
 * 作用：对 children 的规范化，children 变成了一个类型为 VNode 的 Array，将children多维数组转为一维数组
 * normalizeArrayChildren 主要的逻辑就是遍历 children，获得单个节点 c，然后对 c 的类型判断，
 * 如果是一个数组类型，则递归调用 normalizeArrayChildren; 
 * 如果是基础类型，则通过 createTextVNode 方法转换成 VNode 类型；
 * 否则就已经是 VNode 类型了，如果 children 是一个列表并且列表还存在嵌套的情况，
 * 则根据 nestedIndex 去更新它的 key。
 * 这里需要注意一点，在遍历的过程中，对这 3 种情况都做了如下处理：
 * 如果存在两个连续的 text 节点，会把它们合并成一个 text 节点
 * 经过对 children 的规范化，children 变成了一个类型为 VNode 的 Array
 * @param {*} children 表示要规范的子节点
 * @param {*} nestedIndex 表示嵌套的索引，因为单个 child 可能是一个数组类型
 */</span>
<span>function</span> <span>normalizeArrayChildren</span> <span>(</span><span>children<span>:</span> any<span>,</span> nestedIndex<span>?</span><span>:</span> string</span><span>)</span><span>:</span> Array<span>&lt;</span>VNode<span>></span> <span>{</span>
  <span>const</span> res <span>=</span> <span>[</span><span>]</span>
  <span>let</span> i<span>,</span> c<span>,</span> last
  <span>for</span> <span>(</span>i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> children<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span> <span>//遍历子节点</span>
    c <span>=</span> children<span>[</span>i<span>]</span> <span>//获得一个子节点</span>
    <span>if</span> <span>(</span><span>isUndef</span><span>(</span>c<span>)</span> <span>||</span> <span>typeof</span> c <span>===</span> <span>'boolean'</span><span>)</span> <span>continue</span>
    last <span>=</span> res<span>[</span>res<span>.</span>length <span>-</span> <span>1</span><span>]</span>
    <span>//  nested</span>
    <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>c<span>)</span><span>)</span> <span>{</span> <span>//如果是一个数组类型，则递归调用 normalizeArrayChildren</span>
      res<span>.</span><span>push</span><span>.</span><span>apply</span><span>(</span>res<span>,</span> <span>normalizeArrayChildren</span><span>(</span>c<span>,</span> <span><span>`</span><span><span>${</span>nestedIndex <span>||</span> <span>''</span><span>}</span></span><span>_</span><span><span>${</span>i<span>}</span></span><span>`</span></span><span>)</span><span>)</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>isPrimitive</span><span>(</span>c<span>)</span><span>)</span> <span>{</span> <span>//如果是基础类型</span>
      <span>if</span> <span>(</span><span>isDef</span><span>(</span>last<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>last<span>.</span>text<span>)</span><span>)</span> <span>{</span> <span>//为文本节点</span>
        last<span>.</span>text <span>+=</span> <span>String</span><span>(</span>c<span>)</span>
      <span>}</span> <span>else</span> <span>if</span> <span>(</span>c <span>!==</span> <span>''</span><span>)</span> <span>{</span>
        <span>// convert primitive to vnode</span>
        <span>//通过 createTextVNode 方法转换成 VNode 类型</span>
        res<span>.</span><span>push</span><span>(</span><span>createTextVNode</span><span>(</span>c<span>)</span><span>)</span>
      <span>}</span>
    <span>}</span> <span>else</span> <span>{</span> <span>//已经是 VNode 类型了</span>
      <span>if</span> <span>(</span><span>isDef</span><span>(</span>c<span>.</span>text<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>last<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>last<span>.</span>text<span>)</span><span>)</span> <span>{</span>
        res<span>[</span>res<span>.</span>length <span>-</span> <span>1</span><span>]</span> <span>=</span> <span>createTextVNode</span><span>(</span>last<span>.</span>text <span>+</span> c<span>.</span>text<span>)</span>
      <span>}</span> <span>else</span> <span>{</span>
        <span>// default key for nested array children (likely generated by v-for)</span>
        <span>if</span> <span>(</span><span>isDef</span><span>(</span>c<span>.</span>tag<span>)</span> <span>&amp;&amp;</span> <span>isUndef</span><span>(</span>c<span>.</span>key<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>nestedIndex<span>)</span><span>)</span> <span>{</span>
          c<span>.</span>key <span>=</span> <span><span>`</span><span>__vlist</span><span><span>${</span>nestedIndex<span>}</span></span><span>_</span><span><span>${</span>i<span>}</span></span><span>__</span><span>`</span></span>
        <span>}</span>
        res<span>.</span><span>push</span><span>(</span>c<span>)</span>
      <span>}</span>
    <span>}</span>
  <span>}</span>
  <span>return</span> res
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br></div></div></li>
</ul>
</li>
<li>
<p><code>vm._render</code> 是如何创建了一个 VNode，接下来就是要把这个 VNode 渲染成一个真实的 DOM 并渲染出来，这个过程是通过 <code>vm._update</code> 完成的。</p>
</li>
<li>
<p><code>update</code>:</p>
<ul>
<li>
<p>作用：对 patch 方法封装，把 VNode 渲染成真实的 DOM。</p>
</li>
<li>
<p>调用的时机：</p>
<ul>
<li>一个是首次渲染；</li>
<li>一个是数据更新的时候；</li>
</ul>
</li>
<li>
<p><code>_update</code> 函数：方法在初始化时 <code>lifecycleMixin</code>中添加到 Vue.prototype，定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/instance/lifecycle.js" target="_blank" rel="noopener noreferrer">src/core/instance/lifecycle.js</a> 中：</p>
<div><pre><code><span>export</span> <span>function</span> <span>lifecycleMixin</span><span>(</span><span>Vue<span>:</span> Class<span>&lt;</span>Component<span>></span></span><span>)</span> <span>{</span>
  <span>/*更新节点Vue 的 _update 是实例的一个私有方法，它被调用的时机有 2 个，一个是首次渲染，一个是数据更新的时候*/</span>
  <span>Vue</span><span>.</span>prototype<span>.</span><span>_update</span> <span>=</span> <span>function</span> <span>(</span><span>vnode<span>:</span> VNode<span>,</span> hydrating<span>?</span><span>:</span> boolean</span><span>)</span> <span>{</span>
    <span>const</span> vm<span>:</span> Component <span>=</span> <span>this</span>
    <span>/*如果该组件已经挂载过了则代表进入这个步骤是个更新的过程，触发beforeUpdate钩子*/</span>
    <span>if</span> <span>(</span>vm<span>.</span>_isMounted<span>)</span> <span>{</span>
      <span>callHook</span><span>(</span>vm<span>,</span> <span>'beforeUpdate'</span><span>)</span>
    <span>}</span>
    <span>const</span> prevEl <span>=</span> vm<span>.</span>$el
    <span>const</span> prevVnode <span>=</span> vm<span>.</span>_vnode <span>//首次渲染时 _vonde为空</span>
    <span>const</span> prevActiveInstance <span>=</span> activeInstance <span>//activeInstance 作用就是保持当前上下文的 Vue 实例，它是在 lifecycle 模块的全局变量</span>
    activeInstance <span>=</span> vm
    vm<span>.</span>_vnode <span>=</span> vnode<span>//这个 vnode 是通过 vm._render() 返回的组件渲染 VNode，vm._vnode 和 vm.$vnode 的关系就是一种父子关系，用代码表达就是 vm._vnode.parent === vm.$vnode</span>
    <span>// Vue.prototype.__patch__ is injected in entry points</span>
    <span>// based on the rendering backend used.</span>
    <span>/*基于后端渲染Vue.prototype.__patch__被用来作为一个入口*/</span>
    <span>if</span> <span>(</span><span>!</span>prevVnode<span>)</span> <span>{</span> <span>//首次渲染</span>
    <span>/* vm.__patch__ 方法实际上在不同的平台，比如 web 和 weex 上的定义是不一样的，
      因此在 web 平台中它的定义在 src/platforms/web/runtime/index.js 中
      首次渲染在执行 patch 函数的时候，传入的 vm.$el 对应的是例子中 options的el选项对应的 DOM 对象,这个也就是我们在 index.html 模板中写的 &lt;div id="app">
      vm.$el 的赋值是在之前 mountComponent 函数做的，vnode 对应的是调用 render 函数的返回值，hydrating 在非服务端渲染情况下为 false; removeOnly 为 false
    */</span>
      vm<span>.</span>$el <span>=</span> vm<span>.</span><span>__patch__</span><span>(</span>
        vm<span>.</span>$el<span>,</span> vnode<span>,</span> hydrating<span>,</span> <span>false</span> <span>/* removeOnly */</span><span>,</span>
        vm<span>.</span>$options<span>.</span>_parentElm<span>,</span>
        vm<span>.</span>$options<span>.</span>_refElm
      <span>)</span>
    <span>}</span> <span>else</span> <span>{</span> <span>//数据更新</span>
      <span>// updates</span>
      vm<span>.</span>$el <span>=</span> vm<span>.</span><span>__patch__</span><span>(</span>prevVnode<span>,</span> vnode<span>)</span>
    <span>}</span>
    activeInstance <span>=</span> prevActiveInstance
    <span>// update __vue__ reference</span>
    <span>/*更新新的实例对象的__vue__*/</span>
    <span>if</span> <span>(</span>prevEl<span>)</span> <span>{</span>
      prevEl<span>.</span>__vue__ <span>=</span> <span>null</span>
    <span>}</span>
    <span>if</span> <span>(</span>vm<span>.</span>$el<span>)</span> <span>{</span>
      vm<span>.</span>$el<span>.</span>__vue__ <span>=</span> vm
    <span>}</span>
    <span>// if parent is an HOC, update its $el as well</span>
    <span>if</span> <span>(</span>vm<span>.</span>$vnode <span>&amp;&amp;</span> vm<span>.</span>$parent <span>&amp;&amp;</span> vm<span>.</span>$vnode <span>===</span> vm<span>.</span>$parent<span>.</span>_vnode<span>)</span> <span>{</span>
      vm<span>.</span>$parent<span>.</span>$el <span>=</span> vm<span>.</span>$el
    <span>}</span>
    <span>// updated hook is called by the scheduler to ensure that children are</span>
    <span>// updated in a parent's updated hook.</span>
  <span>}</span>
<span>}</span>

</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br></div></div><ul>
<li>_update 方法主要根据变量  _isMounted   和 _vnode  判断是首次渲染还是数据更新，然后调用 patch 传入不同参数。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h4 id="patch"> patch</h4>
<ul>
<li>作用：深度遍历 VNode，递归创建 VNode 并把 VNode 渲染成真实的 DOM。</li>
<li>调用的时机：
<ul>
<li>一个是首次渲染；</li>
<li>一个是数据更新的时候；</li>
<li>patch 由 update 方法根据参数判断首次渲染还是数据更新，给 patch 传入不同参数。</li>
</ul>
</li>
<li>总体流程：</li>
</ul>
<p><img src="./../flow-graph/组件更新.png" alt="" /></p>
<ul>
<li>
<p><code>vm.__patch__</code> 方法：</p>
<ul>
<li>
<p>这个方法实际上在不同的平台。</p>
</li>
<li>
<p>在 web 平台中它的定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/platforms/web/runtime/index.js" target="_blank" rel="noopener noreferrer">src/platforms/web/runtime/index.js</a> 中：</p>
<div><pre><code><span>Vue</span><span>.</span>prototype<span>.</span>__patch__ <span>=</span> inBrowser <span>?</span> patch <span>:</span> noop
</code></pre>
<div><span>1</span><br></div></div><p>可以看到，甚至在 web 平台上，是否是服务端渲染也会对这个方法产生影响。因为在服务端渲染中，没有真实的浏览器 DOM 环境，所以不需要把 VNode 最终转换成 DOM，因此是一个空函数，而在浏览器端渲染中渲染中，它指向了 <code>patch</code> 方法。</p>
</li>
</ul>
</li>
<li>
<p><code>patch</code> 方法：它的定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/platforms/web/runtime/patch.js" target="_blank" rel="noopener noreferrer">src/platforms/web/runtime/patch.js</a>中:</p>
</li>
</ul>
<div><pre><code><span>import</span> <span>*</span> <span>as</span> nodeOps <span>from</span> <span>'web/runtime/node-ops'</span>
<span>import</span> <span>{</span> createPatchFunction <span>}</span> <span>from</span> <span>'core/vdom/patch'</span>
<span>import</span> baseModules <span>from</span> <span>'core/vdom/modules/index'</span>
<span>import</span> platformModules <span>from</span> <span>'web/runtime/modules/index'</span>

<span>// the directive module should be applied last, after all</span>
<span>// built-in modules have been applied.</span>
<span>const</span> modules <span>=</span> platformModules<span>.</span><span>concat</span><span>(</span>baseModules<span>)</span>
<span>//nodeOps 封装了一系列 DOM 操作的方法，modules 定义了一些模块的钩子函数的实现</span>
<span>export</span> <span>const</span> patch<span>:</span> Function <span>=</span> <span>createPatchFunction</span><span>(</span><span>{</span> nodeOps<span>,</span> modules <span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br></div></div><ul>
<li>
<p>方法的定义是调用 <code>createPatchFunction</code> 方法的返回值，这里传入了一个对象，包含 <code>nodeOps</code> 参数和 <code>modules</code> 参数。其中，<code>nodeOps</code> 封装了一系列 DOM 操作的方法，<code>modules</code> 定义了一些模块的钩子函数的实现。</p>
</li>
<li>
<p><code>createPatchFunction</code> 方法: 它的定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/vdom/patch.js" target="_blank" rel="noopener noreferrer">src/core/vdom/patch.js</a>中:</p>
<div><pre><code><span>//创建patch方法</span>
<span>export</span> <span>function</span> <span>createPatchFunction</span> <span>(</span><span>backend</span><span>)</span> <span>{</span>
  <span>let</span> i<span>,</span> j
  <span>const</span> cbs <span>=</span> <span>{</span><span>}</span>

  <span>/*
 	nodeOps见platforms/web(weex)/runtime/node-ops.js  实际上是操作节点（分平台，比如web上是Dom节点的操作）的方法集合的一个适配层，保证不同平台使用同样的对外接口操作节点。
    modules见/platforms/web(weex)/runtime/modules 表示平台的一些模块
  */</span>
  <span>const</span> <span>{</span> modules<span>,</span> nodeOps <span>}</span> <span>=</span> backend

  <span>/*构建cbs回调函数，web平台上见/platforms/web/runtime/modules*/</span>
  <span>for</span> <span>(</span>i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> hooks<span>.</span>length<span>;</span> <span>++</span>i<span>)</span> <span>{</span>
    cbs<span>[</span>hooks<span>[</span>i<span>]</span><span>]</span> <span>=</span> <span>[</span><span>]</span>
    <span>for</span> <span>(</span>j <span>=</span> <span>0</span><span>;</span> j <span>&lt;</span> modules<span>.</span>length<span>;</span> <span>++</span>j<span>)</span> <span>{</span>
      <span>if</span> <span>(</span><span>isDef</span><span>(</span>modules<span>[</span>j<span>]</span><span>[</span>hooks<span>[</span>i<span>]</span><span>]</span><span>)</span><span>)</span> <span>{</span>
        cbs<span>[</span>hooks<span>[</span>i<span>]</span><span>]</span><span>.</span><span>push</span><span>(</span>modules<span>[</span>j<span>]</span><span>[</span>hooks<span>[</span>i<span>]</span><span>]</span><span>)</span>
      <span>}</span>
    <span>}</span>
  <span>}</span>

  <span>//省略一系列 VNode 操作的辅助方法定义...</span>
  <span>//createPatchFunction的返回值，一个patch函数</span>
  <span>return</span> <span>function</span> <span>patch</span> <span>(</span><span>oldVnode<span>,</span> vnode<span>,</span> hydrating<span>,</span> removeOnly<span>,</span> parentElm<span>,</span> refElm</span><span>)</span> <span>{</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br></div></div><ul>
<li>createPatchFunction 内部定义了一系列的辅助方法，最终返回了一个 patch 方法，这个方法就赋值给了 vm._update 函数里调用的 vm.<strong>patch</strong>。</li>
<li>每个平台都有各自的 nodeOps 和 modules，它们的代码需要托管在 src/platforms 这个大目录下，而不同平台的 patch 的主要逻辑部分是相同的，所以这部分公共的部分托管在 core 这个大目录下，差异化部分只需要通过参数来区别，这里用到了一个函数柯里化的技巧，通过 createPatchFunction 把差异化参数提前固化，这样不用每次调用 patch 的时候都传递 nodeOps 和 modules 了。</li>
<li>nodeOps 表示对 “平台 DOM” 的一些操作方法，modules 表示平台的一些模块，它们会在整个 patch 过程的不同阶段执行相应的钩子函数</li>
</ul>
</li>
<li>
<p>示例分析：</p>
<ul>
<li>
<div><pre><code><span>var</span> app <span>=</span> <span>new</span> <span>Vue</span><span>(</span><span>{</span>
  el<span>:</span> <span>'#app'</span><span>,</span>
  <span>render</span><span>:</span> <span>function</span> <span>(</span><span>createElement</span><span>)</span> <span>{</span>
    <span>return</span> <span>createElement</span><span>(</span><span>'div'</span><span>,</span> <span>{</span>
      attrs<span>:</span> <span>{</span>
        id<span>:</span> <span>'app'</span>
      <span>}</span><span>,</span>
    <span>}</span><span>,</span> <span>this</span><span>.</span>message<span>)</span>
  <span>}</span><span>,</span>
  data<span>:</span> <span>{</span>
    message<span>:</span> <span>'Hello Vue!'</span>
  <span>}</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br></div></div><p>然后我们在 <code>vm._update</code> 的方法里是这么调用 <code>patch</code> 方法的：</p>
<div><pre><code><span>// initial render</span>
vm<span>.</span>$el <span>=</span> vm<span>.</span><span>__patch__</span><span>(</span>vm<span>.</span>$el<span>,</span> vnode<span>,</span> hydrating<span>,</span> <span>false</span> <span>/* removeOnly */</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br></div></div><p>结合我们的例子，我们的场景是首次渲染，所以在执行 <code>patch</code> 函数的时候，传入的 <code>vm.$el</code> 对应的是例子中 id 为 <code>app</code> 的 DOM 对象，这个也就是我们在 index.html 模板中写的 <code>&lt;div id=&quot;app&quot;&gt;</code>， <code>vm.$el</code> 的赋值是在之前 <code>mountComponent</code> 函数做的，<code>vnode</code> 对应的是调用 <code>render</code> 函数的返回值，<code>hydrating</code> 在非服务端渲染情况下为 false，<code>removeOnly</code> 为 false。</p>
</li>
</ul>
</li>
<li>
<p><code>pach</code> 方法中关键函数分析：<code>sameVnode</code> 、<code>createElm</code>、<code>patchVnode</code>。</p>
</li>
</ul>
<h5 id="samevnode"> sameVnode</h5>
<ul>
<li>
<p>作用 : 判断是否为相同节点。只有当 key、 tag、 isComment（是否为注释节点）、 data同时定义（或不定义），同时满足当标签类型为  input  的时候  type  相同（某些浏览器不支持动态修改<code>&lt;input&gt;</code>类型，所以他们被视为不同类型）即可。</p>
<div><pre><code><span>/*
  判断两个VNode节点是否是同一个节点，需要满足以下条件
  key相同
  tag（当前节点的标签名）相同
  isComment（是否为注释节点）相同
  是否data（当前节点对应的对象，包含了具体的一些数据信息，是一个VNodeData类型，可以参考VNodeData类型中的数据信息）都有定义
  当标签是&lt;input>的时候，type必须相同
*/</span>
<span>function</span> <span>sameVnode</span> <span>(</span><span>a<span>,</span> b</span><span>)</span> <span>{</span>
  <span>return</span> <span>(</span>
    a<span>.</span>key <span>===</span> b<span>.</span>key <span>&amp;&amp;</span>
    a<span>.</span>tag <span>===</span> b<span>.</span>tag <span>&amp;&amp;</span>
    a<span>.</span>isComment <span>===</span> b<span>.</span>isComment <span>&amp;&amp;</span>
    <span>isDef</span><span>(</span>a<span>.</span>data<span>)</span> <span>===</span> <span>isDef</span><span>(</span>b<span>.</span>data<span>)</span> <span>&amp;&amp;</span>
    <span>sameInputType</span><span>(</span>a<span>,</span> b<span>)</span>
  <span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br></div></div></li>
</ul>
<h5 id="createelem"> createElem</h5>
<ul>
<li>
<p>作用：通过虚拟节点创建真实的 DOM 并插入到它的父节点中。</p>
</li>
<li>
<p>过程：递归创建了一个完整的 DOM 树并插入到 Body 上。</p>
</li>
<li>
<p>createElem 流程：</p>
<p><img src="./../flow-graph/createElem流程.png" alt="" /></p>
<div><pre><code>  <span>/**
   * createElm 的作用是通过虚拟节点创建真实的 DOM 并插入到它的父节点中
   * @param {*} vnode 
   * @param {*} insertedVnodeQueue 
   * @param {*} parentElm 
   * @param {*} refElm 
   * @param {*} nested 
   */</span>
  <span>function</span> <span>createElm</span> <span>(</span><span>vnode<span>,</span> insertedVnodeQueue<span>,</span> parentElm<span>,</span> refElm<span>,</span> nested</span><span>)</span> <span>{</span>
    <span>/*insertedVnodeQueue为空数组[]的时候isRootInsert标志为true*/</span>
    vnode<span>.</span>isRootInsert <span>=</span> <span>!</span>nested <span>// for transition enter check</span>
    <span>/*createComponent 创建一个组件节点*/</span>
    <span>if</span> <span>(</span><span>createComponent</span><span>(</span>vnode<span>,</span> insertedVnodeQueue<span>,</span> parentElm<span>,</span> refElm<span>)</span><span>)</span> <span>{</span>
      <span>return</span>
    <span>}</span>

    <span>const</span> data <span>=</span> vnode<span>.</span>data
    <span>const</span> children <span>=</span> vnode<span>.</span>children
    <span>const</span> tag <span>=</span> vnode<span>.</span>tag
    <span>if</span> <span>(</span><span>isDef</span><span>(</span>tag<span>)</span><span>)</span> <span>{</span> <span>//判断 vnode 是否包含 tag</span>
      <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span> <span>//如果包含，先简单对 tag 的合法性在非生产环境下做校验，看是否是一个合法标签</span>
        <span>if</span> <span>(</span>data <span>&amp;&amp;</span> data<span>.</span>pre<span>)</span> <span>{</span>
          inPre<span>++</span>
        <span>}</span>
        <span>if</span> <span>(</span>
          <span>!</span>inPre <span>&amp;&amp;</span>
          <span>!</span>vnode<span>.</span>ns <span>&amp;&amp;</span>
          <span>!</span><span>(</span>config<span>.</span>ignoredElements<span>.</span>length <span>&amp;&amp;</span> config<span>.</span>ignoredElements<span>.</span><span>indexOf</span><span>(</span>tag<span>)</span> <span>></span> <span>-</span><span>1</span><span>)</span> <span>&amp;&amp;</span>
          config<span>.</span><span>isUnknownElement</span><span>(</span>tag<span>)</span>
        <span>)</span> <span>{</span>
          <span>warn</span><span>(</span>
            <span>'Unknown custom element: &lt;'</span> <span>+</span> tag <span>+</span> <span>'> - did you '</span> <span>+</span>
            <span>'register the component correctly? For recursive components, '</span> <span>+</span>
            <span>'make sure to provide the "name" option.'</span><span>,</span>
            vnode<span>.</span>context
          <span>)</span>
        <span>}</span>
      <span>}</span>
      <span>//去调用平台 DOM 的操作去创建一个占位符元素</span>
      vnode<span>.</span>elm <span>=</span> vnode<span>.</span>ns
        <span>?</span> nodeOps<span>.</span><span>createElementNS</span><span>(</span>vnode<span>.</span>ns<span>,</span> tag<span>)</span>
        <span>:</span> nodeOps<span>.</span><span>createElement</span><span>(</span>tag<span>,</span> vnode<span>)</span>
      <span>setScope</span><span>(</span>vnode<span>)</span>

      <span>/* istanbul ignore if */</span>
      <span>if</span> <span>(</span>__WEEX__<span>)</span> <span>{</span>
		<span>// 省略 weex 平台操作...</span>
      <span>}</span> <span>else</span> <span>{</span>
        <span>createChildren</span><span>(</span>vnode<span>,</span> children<span>,</span> insertedVnodeQueue<span>)</span> <span>//创建子元素</span>
        <span>if</span> <span>(</span><span>isDef</span><span>(</span>data<span>)</span><span>)</span> <span>{</span>
          <span>//执行所有的 create 的钩子并把 vnode push 到 insertedVnodeQueue 中</span>
          <span>invokeCreateHooks</span><span>(</span>vnode<span>,</span> insertedVnodeQueue<span>)</span>
        <span>}</span>
        <span>insert</span><span>(</span>parentElm<span>,</span> vnode<span>.</span>elm<span>,</span> refElm<span>)</span>
      <span>}</span>

      <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> data <span>&amp;&amp;</span> data<span>.</span>pre<span>)</span> <span>{</span>
        inPre<span>--</span>
      <span>}</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>isTrue</span><span>(</span>vnode<span>.</span>isComment<span>)</span><span>)</span> <span>{</span> <span>//如果 vnode 节点不包含 tag，则它有可能是一个注释或者纯文本节点</span>
      vnode<span>.</span>elm <span>=</span> nodeOps<span>.</span><span>createComment</span><span>(</span>vnode<span>.</span>text<span>)</span> <span>//创建注释节点</span>
      <span>insert</span><span>(</span>parentElm<span>,</span> vnode<span>.</span>elm<span>,</span> refElm<span>)</span> <span>//直接插入到父元素中</span>
    <span>}</span> <span>else</span> <span>{</span>
      vnode<span>.</span>elm <span>=</span> nodeOps<span>.</span><span>createTextNode</span><span>(</span>vnode<span>.</span>text<span>)</span>
      <span>insert</span><span>(</span>parentElm<span>,</span> vnode<span>.</span>elm<span>,</span> refElm<span>)</span>
    <span>}</span>
  <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br></div></div></li>
</ul>
<h5 id="patchvnode"> patchVnode</h5>
<ul>
<li>
<p><code>patchVnode</code>: 把新的 VNode path 到 旧VNode。</p>
<div><pre><code> <span>/*patch VNode节点*/</span>
 <span>function</span> <span>patchVnode</span> <span>(</span><span>oldVnode<span>,</span> vnode<span>,</span> insertedVnodeQueue<span>,</span> removeOnly</span><span>)</span> <span>{</span>
   <span>/*两个VNode节点相同则直接返回*/</span>
   <span>if</span> <span>(</span>oldVnode <span>===</span> vnode<span>)</span> <span>{</span>
     <span>return</span>
   <span>}</span>
   <span>// reuse element for static trees.</span>
   <span>// note we only do this if the vnode is cloned -</span>
   <span>// if the new node is not cloned it means the render functions have been</span>
   <span>// reset by the hot-reload-api and we need to do a proper re-render.</span>
   <span>/*
     如果新旧VNode都是静态的，同时它们的key相同（代表同一节点），
     并且新的VNode是clone或者是标记了once（标记v-once属性，只渲染一次），
     那么只需要替换elm以及componentInstance即可。
   */</span>
   <span>if</span> <span>(</span><span>isTrue</span><span>(</span>vnode<span>.</span>isStatic<span>)</span> <span>&amp;&amp;</span>
       <span>isTrue</span><span>(</span>oldVnode<span>.</span>isStatic<span>)</span> <span>&amp;&amp;</span>
       vnode<span>.</span>key <span>===</span> oldVnode<span>.</span>key <span>&amp;&amp;</span>
       <span>(</span><span>isTrue</span><span>(</span>vnode<span>.</span>isCloned<span>)</span> <span>||</span> <span>isTrue</span><span>(</span>vnode<span>.</span>isOnce<span>)</span><span>)</span><span>)</span> <span>{</span>
     vnode<span>.</span>elm <span>=</span> oldVnode<span>.</span>elm
     vnode<span>.</span>componentInstance <span>=</span> oldVnode<span>.</span>componentInstance
     <span>return</span>
   <span>}</span>
   <span>let</span> i
   <span>const</span> data <span>=</span> vnode<span>.</span>data<span>,</span>
   <span>//当更新的 vnode 是一个组件 vnode 的时候，会执行 prepatch 的方法,它的定义在 src/core/vdom/create-component.js 中</span>
   <span>if</span> <span>(</span><span>isDef</span><span>(</span>data<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>i <span>=</span> data<span>.</span>hook<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>i <span>=</span> i<span>.</span>prepatch<span>)</span><span>)</span> <span>{</span> <span>//判断并获取prepatch 的方法</span>
     <span>/*i = data.hook.prepatch，如果存在的话，见"./create-component componentVNodeHooks"。*/</span>
     <span>i</span><span>(</span>oldVnode<span>,</span> vnode<span>)</span> <span>//调用prepatch 的方法</span>
   <span>}</span>
   <span>const</span> elm <span>=</span> vnode<span>.</span>elm <span>=</span> oldVnode<span>.</span>elm
   <span>const</span> oldCh <span>=</span> oldVnode<span>.</span>children
   <span>const</span> ch <span>=</span> vnode<span>.</span>children
   <span>if</span> <span>(</span><span>isDef</span><span>(</span>data<span>)</span> <span>&amp;&amp;</span> <span>isPatchable</span><span>(</span>vnode<span>)</span><span>)</span> <span>{</span>
     <span>/*调用update回调以及update钩子*/</span>
     <span>for</span> <span>(</span>i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> cbs<span>.</span>update<span>.</span>length<span>;</span> <span>++</span>i<span>)</span> cbs<span>.</span>update<span>[</span>i<span>]</span><span>(</span>oldVnode<span>,</span> vnode<span>)</span>
     <span>if</span> <span>(</span><span>isDef</span><span>(</span>i <span>=</span> data<span>.</span>hook<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>i <span>=</span> i<span>.</span>update<span>)</span><span>)</span> <span>i</span><span>(</span>oldVnode<span>,</span> vnode<span>)</span>
   <span>}</span>
   <span>/*如果这个VNode节点没有text文本时*/</span>
   <span>if</span> <span>(</span><span>isUndef</span><span>(</span>vnode<span>.</span>text<span>)</span><span>)</span> <span>{</span>
     <span>if</span> <span>(</span><span>isDef</span><span>(</span>oldCh<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>ch<span>)</span><span>)</span> <span>{</span>
       <span>/*新老节点均有children子节点，则对子节点进行diff操作，调用updateChildren*/</span>
       <span>if</span> <span>(</span>oldCh <span>!==</span> ch<span>)</span> <span>updateChildren</span><span>(</span>elm<span>,</span> oldCh<span>,</span> ch<span>,</span> insertedVnodeQueue<span>,</span> removeOnly<span>)</span>
     <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>isDef</span><span>(</span>ch<span>)</span><span>)</span> <span>{</span>
       <span>/*如果老节点没有子节点而新节点存在子节点，表示执行了新增子节点操作，先清空elm的文本内容，然后为当前节点加入子节点*/</span>
       <span>if</span> <span>(</span><span>isDef</span><span>(</span>oldVnode<span>.</span>text<span>)</span><span>)</span> nodeOps<span>.</span><span>setTextContent</span><span>(</span>elm<span>,</span> <span>''</span><span>)</span>
       <span>addVnodes</span><span>(</span>elm<span>,</span> <span>null</span><span>,</span> ch<span>,</span> <span>0</span><span>,</span> ch<span>.</span>length <span>-</span> <span>1</span><span>,</span> insertedVnodeQueue<span>)</span>
     <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>isDef</span><span>(</span>oldCh<span>)</span><span>)</span> <span>{</span>
       <span>/*当新节点没有子节点而老节点有子节点的时候，表示进行了节点删除操作，则移除所有ele的子节点*/</span>
       <span>removeVnodes</span><span>(</span>elm<span>,</span> oldCh<span>,</span> <span>0</span><span>,</span> oldCh<span>.</span>length <span>-</span> <span>1</span><span>)</span>
     <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>isDef</span><span>(</span>oldVnode<span>.</span>text<span>)</span><span>)</span> <span>{</span>
       <span>/*当新老节点都无子节点的时候，只是文本的替换，因为这个逻辑中新节点text不存在，所以直接去除ele的文本*/</span>
       nodeOps<span>.</span><span>setTextContent</span><span>(</span>elm<span>,</span> <span>''</span><span>)</span>
     <span>}</span>
   <span>}</span> <span>else</span> <span>if</span> <span>(</span>oldVnode<span>.</span>text <span>!==</span> vnode<span>.</span>text<span>)</span> <span>{</span>
     <span>/*当新老节点text不一样时，直接替换这段文本*/</span>
     nodeOps<span>.</span><span>setTextContent</span><span>(</span>elm<span>,</span> vnode<span>.</span>text<span>)</span>
   <span>}</span>
   <span>/*调用postpatch钩子*/</span>
   <span>if</span> <span>(</span><span>isDef</span><span>(</span>data<span>)</span><span>)</span> <span>{</span>
     <span>if</span> <span>(</span><span>isDef</span><span>(</span>i <span>=</span> data<span>.</span>hook<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>i <span>=</span> i<span>.</span>postpatch<span>)</span><span>)</span> <span>i</span><span>(</span>oldVnode<span>,</span> vnode<span>)</span>
   <span>}</span>
 <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br></div></div></li>
<li>
<p><code>updateChildren</code>: 使用 diff 算法，比较对比两棵树的差异。</p>
</li>
</ul>
<h5 id="diff-算法"> diff 算法</h5>
<ul>
<li>
<p>作用：diff  算法可以比对出两颗树的「差异」</p>
</li>
<li>
<p>思想：</p>
<ul>
<li>diff  算法是通过同层的树节点进行比较而非对树进行逐层搜索遍历的方式，所以时间复杂度只有  O(n)。</li>
<li>比较的是新旧的虚拟dom节点，操作的是真实的dom节点，最终根据新旧虚拟dom节点的变化操作真实dom节点。</li>
</ul>
<p><img src="./../images/diff.png" alt="" /></p>
<p>假设我们现在有新老  VNode  节点两颗树，这时候到了 patch 的过程，我们需要将他们进行比对;这张图中的相同颜色的方块中的节点会进行比对，比对得到「差异」后将这些「差异」更新到视图上。因为只进行同层级的比对，所以十分高效。</p>
</li>
<li>
<p><strong>算法：</strong></p>
<ul>
<li>path 时，首先会对新旧两棵树进行一个深度优先的遍历，每遍历到一个节点，我们就将这个节点和新的树中的节点进行比较；</li>
<li>循环遍历两个 VNode 的子节点；</li>
<li>分别对两个 Vnode 的子节点的第一个和最后一个两两比较，因此有四次比较；</li>
<li>比较结果有三种：VNode 的替换、新增、删除；</li>
<li>根据比较结果进行 VNode 操作:
<ul>
<li>替换/新增：在 oldVnode（老  VNode  节点）不存在的时候，相当于新的  VNode  替代原本没有的节点；</li>
<li>删除:   在 vnode（新  VNode  节点）不存在的时候，相当于要把老的节点删除;</li>
</ul>
</li>
<li>四次比较匹配都不成功：根据是否设置 key 值执行不同操作。
<ul>
<li>没有设置 key 值，就会创建新 VNode;</li>
<li>设置 Key 值，会从旧 VNode 子节点中查找 Key 值相同 VNode 进行复用 VNode。</li>
</ul>
</li>
<li>两个 VNode 的子节点循环遍历完成：
<ul>
<li>如果旧 VNode 子节点遍历完成：则将未匹配的新 VNode 插入到真实 DOM；</li>
<li>如果新 VNode 子节点遍历完成：则将未匹配的旧 VNode 从真实 DOM 中删除。</li>
</ul>
</li>
<li>根据新的 Virtual DOM 继续下一层节点的比较。</li>
</ul>
</li>
<li>
<p><img src="./../images/diff1.png" alt="" /></p>
</li>
<li>
<p>示例：</p>
<ul>
<li>
<p>不设置 key 的 diff</p>
<p><img src="./../images/diff2.png" alt="" /></p>
<p><img src="./../images/diff5.png" alt="" /></p>
<p><img src="./../images/diff4.png" alt="" /></p>
</li>
<li>
<p>设置 key 的 diff:<img src="./../images/diff3.png" alt="" /></p>
</li>
</ul>
</li>
<li>
<p>源码：diff 算法 使用 updateChildren() 实现，位于<a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/vdom/patch.js" target="_blank" rel="noopener noreferrer">src/core/vdom/patch.js</a>，该算法来源于 <a href="https://github.com/snabbdom/snabbdom" target="_blank" rel="noopener noreferrer">snabbdom</a>。</p>
<div><pre><code>  <span>function</span> <span>updateChildren</span> <span>(</span><span>parentElm<span>,</span> oldCh<span>,</span> newCh<span>,</span> insertedVnodeQueue<span>,</span> removeOnly</span><span>)</span> <span>{</span>
    <span>let</span> oldStartIdx <span>=</span> <span>0</span>
    <span>let</span> newStartIdx <span>=</span> <span>0</span>
    <span>let</span> oldEndIdx <span>=</span> oldCh<span>.</span>length <span>-</span> <span>1</span>
    <span>let</span> oldStartVnode <span>=</span> oldCh<span>[</span><span>0</span><span>]</span>
    <span>let</span> oldEndVnode <span>=</span> oldCh<span>[</span>oldEndIdx<span>]</span>
    <span>let</span> newEndIdx <span>=</span> newCh<span>.</span>length <span>-</span> <span>1</span>
    <span>let</span> newStartVnode <span>=</span> newCh<span>[</span><span>0</span><span>]</span>
    <span>let</span> newEndVnode <span>=</span> newCh<span>[</span>newEndIdx<span>]</span>
    <span>let</span> oldKeyToIdx<span>,</span> idxInOld<span>,</span> elmToMove<span>,</span> refElm

    <span>// removeOnly is a special flag used only by &lt;transition-group></span>
    <span>// to ensure removed elements stay in correct relative positions</span>
    <span>// during leaving transitions</span>
    <span>const</span> canMove <span>=</span> <span>!</span>removeOnly

    <span>while</span> <span>(</span>oldStartIdx <span>&lt;=</span> oldEndIdx <span>&amp;&amp;</span> newStartIdx <span>&lt;=</span> newEndIdx<span>)</span> <span>{</span>
      <span>if</span> <span>(</span><span>isUndef</span><span>(</span>oldStartVnode<span>)</span><span>)</span> <span>{</span>
        oldStartVnode <span>=</span> oldCh<span>[</span><span>++</span>oldStartIdx<span>]</span> <span>// Vnode has been moved left</span>
      <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>isUndef</span><span>(</span>oldEndVnode<span>)</span><span>)</span> <span>{</span>
        oldEndVnode <span>=</span> oldCh<span>[</span><span>--</span>oldEndIdx<span>]</span>
      <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>sameVnode</span><span>(</span>oldStartVnode<span>,</span> newStartVnode<span>)</span><span>)</span> <span>{</span>
        <span>/*前四种情况其实是指定key的时候，判定为同一个VNode，则直接patchVnode即可，分别比较oldCh以及newCh的两头节点2*2=4种情况*/</span>
        <span>patchVnode</span><span>(</span>oldStartVnode<span>,</span> newStartVnode<span>,</span> insertedVnodeQueue<span>)</span>
        oldStartVnode <span>=</span> oldCh<span>[</span><span>++</span>oldStartIdx<span>]</span>
        newStartVnode <span>=</span> newCh<span>[</span><span>++</span>newStartIdx<span>]</span>
      <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>sameVnode</span><span>(</span>oldEndVnode<span>,</span> newEndVnode<span>)</span><span>)</span> <span>{</span>
        <span>patchVnode</span><span>(</span>oldEndVnode<span>,</span> newEndVnode<span>,</span> insertedVnodeQueue<span>)</span>
        oldEndVnode <span>=</span> oldCh<span>[</span><span>--</span>oldEndIdx<span>]</span>
        newEndVnode <span>=</span> newCh<span>[</span><span>--</span>newEndIdx<span>]</span>
      <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>sameVnode</span><span>(</span>oldStartVnode<span>,</span> newEndVnode<span>)</span><span>)</span> <span>{</span> <span>// Vnode moved right</span>
        <span>patchVnode</span><span>(</span>oldStartVnode<span>,</span> newEndVnode<span>,</span> insertedVnodeQueue<span>)</span>
        canMove <span>&amp;&amp;</span> nodeOps<span>.</span><span>insertBefore</span><span>(</span>parentElm<span>,</span> oldStartVnode<span>.</span>elm<span>,</span> nodeOps<span>.</span><span>nextSibling</span><span>(</span>oldEndVnode<span>.</span>elm<span>)</span><span>)</span>
        oldStartVnode <span>=</span> oldCh<span>[</span><span>++</span>oldStartIdx<span>]</span>
        newEndVnode <span>=</span> newCh<span>[</span><span>--</span>newEndIdx<span>]</span>
      <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>sameVnode</span><span>(</span>oldEndVnode<span>,</span> newStartVnode<span>)</span><span>)</span> <span>{</span> <span>// Vnode moved left</span>
        <span>patchVnode</span><span>(</span>oldEndVnode<span>,</span> newStartVnode<span>,</span> insertedVnodeQueue<span>)</span>
        canMove <span>&amp;&amp;</span> nodeOps<span>.</span><span>insertBefore</span><span>(</span>parentElm<span>,</span> oldEndVnode<span>.</span>elm<span>,</span> oldStartVnode<span>.</span>elm<span>)</span>
        oldEndVnode <span>=</span> oldCh<span>[</span><span>--</span>oldEndIdx<span>]</span>
        newStartVnode <span>=</span> newCh<span>[</span><span>++</span>newStartIdx<span>]</span>
      <span>}</span> <span>else</span> <span>{</span>
        <span>/*
          生成一个key与旧VNode的key对应的哈希表（只有第一次进来undefined的时候会生成，也为后面检测重复的key值做铺垫）
          比如childre是这样的 [{xx: xx, key: 'key0'}, {xx: xx, key: 'key1'}, {xx: xx, key: 'key2'}]  beginIdx = 0   endIdx = 2  
          结果生成{key0: 0, key1: 1, key2: 2}
        */</span>
        <span>if</span> <span>(</span><span>isUndef</span><span>(</span>oldKeyToIdx<span>)</span><span>)</span> oldKeyToIdx <span>=</span> <span>createKeyToOldIdx</span><span>(</span>oldCh<span>,</span> oldStartIdx<span>,</span> oldEndIdx<span>)</span>
        <span>/*如果newStartVnode新的VNode节点存在key并且这个key在oldVnode中能找到则返回这个节点的idxInOld（即第几个节点，下标）*/</span>
        idxInOld <span>=</span> <span>isDef</span><span>(</span>newStartVnode<span>.</span>key<span>)</span> <span>?</span> oldKeyToIdx<span>[</span>newStartVnode<span>.</span>key<span>]</span> <span>:</span> <span>null</span>
        <span>if</span> <span>(</span><span>isUndef</span><span>(</span>idxInOld<span>)</span><span>)</span> <span>{</span> <span>// New element</span>
          <span>/*newStartVnode没有key或者是该key没有在老节点中找到则创建一个新的节点*/</span>
          <span>createElm</span><span>(</span>newStartVnode<span>,</span> insertedVnodeQueue<span>,</span> parentElm<span>,</span> oldStartVnode<span>.</span>elm<span>)</span>
          newStartVnode <span>=</span> newCh<span>[</span><span>++</span>newStartIdx<span>]</span>
        <span>}</span> <span>else</span> <span>{</span>
          <span>/*获取同key的老节点*/</span>
          elmToMove <span>=</span> oldCh<span>[</span>idxInOld<span>]</span>
          <span>/* istanbul ignore if */</span>
          <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> <span>!</span>elmToMove<span>)</span> <span>{</span>
            <span>/*如果elmToMove不存在说明之前已经有新节点放入过这个key的Dom中，提示可能存在重复的key，确保v-for的时候item有唯一的key值*/</span>
            <span>warn</span><span>(</span>
              <span>'It seems there are duplicate keys that is causing an update error. '</span> <span>+</span>
              <span>'Make sure each v-for item has a unique key.'</span>
            <span>)</span>
          <span>}</span>
          <span>if</span> <span>(</span><span>sameVnode</span><span>(</span>elmToMove<span>,</span> newStartVnode<span>)</span><span>)</span> <span>{</span>
            <span>/*如果新VNode与得到的有相同key的节点是同一个VNode则进行patchVnode*/</span>
            <span>patchVnode</span><span>(</span>elmToMove<span>,</span> newStartVnode<span>,</span> insertedVnodeQueue<span>)</span>
            <span>/*因为已经patchVnode进去了，所以将这个老节点赋值undefined，之后如果还有新节点与该节点key相同可以检测出来提示已有重复的key*/</span>
            oldCh<span>[</span>idxInOld<span>]</span> <span>=</span> <span>undefined</span>
            <span>/*当有标识位canMove实可以直接插入oldStartVnode对应的真实Dom节点前面*/</span>
            canMove <span>&amp;&amp;</span> nodeOps<span>.</span><span>insertBefore</span><span>(</span>parentElm<span>,</span> newStartVnode<span>.</span>elm<span>,</span> oldStartVnode<span>.</span>elm<span>)</span>
            newStartVnode <span>=</span> newCh<span>[</span><span>++</span>newStartIdx<span>]</span>
          <span>}</span> <span>else</span> <span>{</span>
            <span>// same key but different element. treat as new element</span>
            <span>/*当新的VNode与找到的同样key的VNode不是sameVNode的时候（比如说tag不一样或者是有不一样type的input标签），创建一个新的节点*/</span>
            <span>createElm</span><span>(</span>newStartVnode<span>,</span> insertedVnodeQueue<span>,</span> parentElm<span>,</span> oldStartVnode<span>.</span>elm<span>)</span>
            newStartVnode <span>=</span> newCh<span>[</span><span>++</span>newStartIdx<span>]</span>
          <span>}</span>
        <span>}</span>
      <span>}</span>
    <span>}</span>
    <span>if</span> <span>(</span>oldStartIdx <span>></span> oldEndIdx<span>)</span> <span>{</span>
      <span>/*全部比较完成以后，发现oldStartIdx > oldEndIdx的话，说明老节点已经遍历完了，新节点比老节点多，所以这时候多出来的新节点需要一个一个创建出来加入到真实Dom中*/</span>
      refElm <span>=</span> <span>isUndef</span><span>(</span>newCh<span>[</span>newEndIdx <span>+</span> <span>1</span><span>]</span><span>)</span> <span>?</span> <span>null</span> <span>:</span> newCh<span>[</span>newEndIdx <span>+</span> <span>1</span><span>]</span><span>.</span>elm
      <span>addVnodes</span><span>(</span>parentElm<span>,</span> refElm<span>,</span> newCh<span>,</span> newStartIdx<span>,</span> newEndIdx<span>,</span> insertedVnodeQueue<span>)</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span>newStartIdx <span>></span> newEndIdx<span>)</span> <span>{</span>
      <span>/*如果全部比较完成以后发现newStartIdx > newEndIdx，则说明新节点已经遍历完了，老节点多余新节点，这个时候需要将多余的老节点从真实Dom中移除*/</span>
      <span>removeVnodes</span><span>(</span>parentElm<span>,</span> oldCh<span>,</span> oldStartIdx<span>,</span> oldEndIdx<span>)</span>
    <span>}</span>
  <span>}</span>

</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br></div></div></li>
</ul>
<h4 id="组件创建"> 组件创建</h4>
<ul>
<li>
<p>组件创建时机：</p>
<ul>
<li>render 方法调用的 _createElement() 方法中：当 tag 不是 string 时，调用 createComponent() 创建组件；</li>
<li>path 方法调用 createElem() 方法中： 调用 createComponent() 创建组件；</li>
</ul>
</li>
<li>
<p>根据不同的创建时机， createComponent() 实现不同，主要有两种实现：</p>
<ul>
<li>render 方法调用的 _createElement() 方法中的 createComponent() 的实现；
<ul>
<li>此处主要创建组件的占位符节点；</li>
</ul>
</li>
<li>path 方法调用 createElem() 方法中： 调用 createComponent() 的实现；
<ul>
<li>此处递归创建组件组件实例，通过 new Vue() 创建组件；</li>
</ul>
</li>
</ul>
</li>
<li>
<p>查看组件的模板被编译后的渲染函数方式：访问 <code>this.$options.render</code></p>
</li>
<li>
<p>组件创建总体流程图：</p>
<p><img src="./../flow-graph/createComponent.png" alt="" /></p>
</li>
<li>
<p>render 方法调用的 _createElement() 中的 <code>createComponent</code> 方法：</p>
<ul>
<li>
<p>流程图</p>
<p><img src="./../flow-graph/第二次createComponent流程.png" alt="" /></p>
</li>
<li>
<p>源码：定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/vdom/create-component.js" target="_blank" rel="noopener noreferrer">src/core/vdom/create-component.js</a> 文件中。</p>
</li>
</ul>
<div><pre><code><span>/*创建一个组件节点，返回Vnode节点*/</span>
<span>export</span> <span>function</span> <span>createComponent</span> <span>(</span>
  Ctor<span>:</span> Class<span>&lt;</span>Component<span>></span> <span>|</span> Function <span>|</span> Object <span>|</span> <span>void</span><span>,</span>
  data<span>?</span><span>:</span> VNodeData<span>,</span>
  context<span>:</span> Component<span>,</span> <span>//vm实例</span>
  children<span>:</span> <span>?</span>Array<span>&lt;</span>VNode<span>></span><span>,</span>
  tag<span>?</span><span>:</span> string
<span>)</span><span>:</span> VNode <span>|</span> <span>void</span> <span>{</span>
  <span>/*没有传组件构造类直接返回*/</span>
  <span>if</span> <span>(</span><span>isUndef</span><span>(</span>Ctor<span>)</span><span>)</span> <span>{</span>
    <span>return</span>
  <span>}</span>

  <span>/*获取vue实例,_base存放了Vue，作为基类，这个的定义是在最开始初始化 Vue 的阶段，
  在 src/core/global-api/index.js 中的 initGlobalAPI 函数中保存了vue实例*/</span>
  <span>const</span> baseCtor <span>=</span> context<span>.</span>$options<span>.</span>_base 

  <span>// plain options object: turn it into a constructor</span>
  <span>if</span> <span>(</span><span>isObject</span><span>(</span>Ctor<span>)</span><span>)</span> <span>{</span>
    <span>// baseCtor.extend()实际为 Vue.extend 函数，定义在 src/core/global-api/extend.js 中</span>
    <span>//子组件继承于vue实例</span>
    Ctor <span>=</span> baseCtor<span>.</span><span>extend</span><span>(</span>Ctor<span>)</span>
  <span>}</span>

  <span>// if at this stage it's not a constructor or an async component factory,</span>
  <span>// reject.</span>
  <span>/*如果在该阶段Ctor依然不是一个构造函数或者是一个异步组件工厂则直接返回*/</span>
  <span>if</span> <span>(</span><span>typeof</span> Ctor <span>!==</span> <span>'function'</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
      <span>warn</span><span>(</span><span><span>`</span><span>Invalid Component definition: </span><span><span>${</span><span>String</span><span>(</span>Ctor<span>)</span><span>}</span></span><span>`</span></span><span>,</span> context<span>)</span>
    <span>}</span>
    <span>return</span>
  <span>}</span>

  <span>// async component</span>
  <span>/*处理异步组件*/</span>
  <span>if</span> <span>(</span><span>isUndef</span><span>(</span>Ctor<span>.</span>cid<span>)</span><span>)</span> <span>{</span>
    Ctor <span>=</span> <span>resolveAsyncComponent</span><span>(</span>Ctor<span>,</span> baseCtor<span>,</span> context<span>)</span>
    <span>if</span> <span>(</span>Ctor <span>===</span> <span>undefined</span><span>)</span> <span>{</span>
      <span>// return nothing if this is indeed an async component</span>
      <span>// wait for the callback to trigger parent update.</span>
      <span>/*如果这是一个异步组件则会不会返回任何东西（undifiened），直接return掉，等待回调函数去触发父组件更新。s*/</span>
      <span>return</span>
    <span>}</span>
  <span>}</span>

  <span>// resolve constructor options in case global mixins are applied after</span>
  <span>// component constructor creation</span>
  <span>resolveConstructorOptions</span><span>(</span>Ctor<span>)</span>

  data <span>=</span> data <span>||</span> <span>{</span><span>}</span>

  <span>// transform component v-model data into props &amp; events</span>
  <span>if</span> <span>(</span><span>isDef</span><span>(</span>data<span>.</span>model<span>)</span><span>)</span> <span>{</span>
    <span>transformModel</span><span>(</span>Ctor<span>.</span>options<span>,</span> data<span>)</span>
  <span>}</span>

  <span>// extract props</span>
  <span>const</span> propsData <span>=</span> <span>extractPropsFromVNodeData</span><span>(</span>data<span>,</span> Ctor<span>,</span> tag<span>)</span>

  <span>// functional component</span>
  <span>if</span> <span>(</span><span>isTrue</span><span>(</span>Ctor<span>.</span>options<span>.</span>functional<span>)</span><span>)</span> <span>{</span>
    <span>return</span> <span>createFunctionalComponent</span><span>(</span>Ctor<span>,</span> propsData<span>,</span> data<span>,</span> context<span>,</span> children<span>)</span>
  <span>}</span>

  <span>// extract listeners, since these needs to be treated as</span>
  <span>// child component listeners instead of DOM listeners</span>
  <span>const</span> listeners <span>=</span> data<span>.</span>on
  <span>// replace with listeners with .native modifier</span>
  data<span>.</span>on <span>=</span> data<span>.</span>nativeOn

  <span>if</span> <span>(</span><span>isTrue</span><span>(</span>Ctor<span>.</span>options<span>.</span>abstract<span>)</span><span>)</span> <span>{</span>
    <span>// abstract components do not keep anything</span>
    <span>// other than props &amp; listeners</span>
    data <span>=</span> <span>{</span><span>}</span>
  <span>}</span>

  <span>// merge component management hooks onto the placeholder node</span>
  <span>//合并组件钩子和vue实例钩子</span>
 <span>//在 VNode 执行 patch 的过程中执行相关的钩子函数</span>
  <span>mergeHooks</span><span>(</span>data<span>)</span>

  <span>// return a placeholder vnode</span>
  <span>//返回一个占位符节点</span>
  <span>const</span> name <span>=</span> Ctor<span>.</span>options<span>.</span>name <span>||</span> tag
  <span>const</span> vnode <span>=</span> <span>new</span> <span>VNode</span><span>(</span>
    <span><span>`</span><span>vue-component-</span><span><span>${</span>Ctor<span>.</span>cid<span>}</span></span><span><span>${</span>name <span>?</span> <span><span>`</span><span>-</span><span><span>${</span>name<span>}</span></span><span>`</span></span> <span>:</span> <span>''</span><span>}</span></span><span>`</span></span><span>,</span> <span>//tag参数</span>
    data<span>,</span> <span>undefined</span><span>,</span> <span>undefined</span><span>,</span> <span>undefined</span><span>,</span> context<span>,</span> <span>//data、children、text、elm、context参数</span>
    <span>{</span> Ctor<span>,</span> propsData<span>,</span> listeners<span>,</span> tag<span>,</span> children <span>}</span> <span>//componentOptions参数</span>
  <span>)</span>
  <span>return</span> vnode
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br></div></div></li>
<li>
<p>path 方法调用 createElem() 方法中的 <code>createComponent()</code> 的实现:定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/vdom/patch.js" target="_blank" rel="noopener noreferrer">src/core/vdom/patch.js</a> 文件中。</p>
<div><pre><code>  <span>/*创建一个组件，创建成功返回true*/</span>
  <span>function</span> <span>createComponent</span> <span>(</span><span>vnode<span>,</span> insertedVnodeQueue<span>,</span> parentElm<span>,</span> refElm</span><span>)</span> <span>{</span>
    <span>let</span> i <span>=</span> vnode<span>.</span>data
    <span>if</span> <span>(</span><span>isDef</span><span>(</span>i<span>)</span><span>)</span> <span>{</span><span>//组件 VNode 包含 data 选项</span>
      <span>const</span> isReactivated <span>=</span> <span>isDef</span><span>(</span>vnode<span>.</span>componentInstance<span>)</span> <span>&amp;&amp;</span> i<span>.</span>keepAlive
      <span>//获取组件 init 钩子函数</span>
      <span>if</span> <span>(</span><span>isDef</span><span>(</span>i <span>=</span> i<span>.</span>hook<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>i <span>=</span> i<span>.</span>init<span>)</span><span>)</span> <span>{</span>
        <span>i</span><span>(</span>vnode<span>,</span> <span>false</span> <span>/* hydrating */</span><span>,</span> parentElm<span>,</span> refElm<span>)</span>
      <span>}</span>
      <span>// after calling the init hook, if the vnode is a child component</span>
      <span>// it should've created a child instance and mounted it. the child</span>
      <span>// component also has set the placeholder vnode's elm.</span>
      <span>// in that case we can just return the element and be done.</span>
      <span>/*
        在调用了init钩子以后，如果VNode是一个子组件，它应该已经创建了一个子组件实例并挂载它。
        子组件也应该设置了一个VNode占位符，我们直接返回组件实例即可。
        意思就是如果已经存在组件实例，则不需要重新创建一个新的，我们要做的就是初始化组件以及激活组件即可，还是用原来的组件实例。
      */</span>
      <span>if</span> <span>(</span><span>isDef</span><span>(</span>vnode<span>.</span>componentInstance<span>)</span><span>)</span> <span>{</span>
        <span>/*初始化组件*/</span>
        <span>initComponent</span><span>(</span>vnode<span>,</span> insertedVnodeQueue<span>)</span>
        <span>if</span> <span>(</span><span>isTrue</span><span>(</span>isReactivated<span>)</span><span>)</span> <span>{</span>
          <span>reactivateComponent</span><span>(</span>vnode<span>,</span> insertedVnodeQueue<span>,</span> parentElm<span>,</span> refElm<span>)</span>
        <span>}</span>
        <span>return</span> <span>true</span>
      <span>}</span>
    <span>}</span>
  <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br></div></div><ul>
<li>
<p><code>createComponent</code> 函数中，首先对 <code>vnode.data</code> 做了一些判断：</p>
<div><pre><code><span>let</span> i <span>=</span> vnode<span>.</span>data
<span>if</span> <span>(</span><span>isDef</span><span>(</span>i<span>)</span><span>)</span> <span>{</span>
  <span>// ...</span>
  <span>if</span> <span>(</span><span>isDef</span><span>(</span>i <span>=</span> i<span>.</span>hook<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>i <span>=</span> i<span>.</span>init<span>)</span><span>)</span> <span>{</span>
    <span>i</span><span>(</span>vnode<span>,</span> <span>false</span> <span>/* hydrating */</span><span>)</span>
    <span>// ...</span>
  <span>}</span>
  <span>// ..</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div><p>如果 <code>vnode</code> 是一个组件 VNode，那么条件会满足，并且得到 <code>i</code> 就是 <code>init</code> 钩子函数，回顾上节我们在创建组件 VNode 的时候合并钩子函数中就包含 <code>init</code> 钩子函数，定义在 <code>src/core/vdom/create-component.js</code> 中：</p>
<div><pre><code><span>init</span> <span>(</span>vnode<span>:</span> VNodeWithData<span>,</span> hydrating<span>:</span> boolean<span>)</span><span>:</span> <span>?</span>boolean <span>{</span>
  <span>if</span> <span>(</span>
    vnode<span>.</span>componentInstance <span>&amp;&amp;</span>
    <span>!</span>vnode<span>.</span>componentInstance<span>.</span>_isDestroyed <span>&amp;&amp;</span>
    vnode<span>.</span>data<span>.</span>keepAlive
  <span>)</span> <span>{</span>
    <span>// kept-alive components, treat as a patch</span>
    <span>const</span> mountedNode<span>:</span> any <span>=</span> vnode <span>// work around flow</span>
    componentVNodeHooks<span>.</span><span>prepatch</span><span>(</span>mountedNode<span>,</span> mountedNode<span>)</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>const</span> child <span>=</span> vnode<span>.</span>componentInstance <span>=</span> <span>createComponentInstanceForVnode</span><span>(</span>
      vnode<span>,</span>
      activeInstance
    <span>)</span>
    child<span>.</span><span>$mount</span><span>(</span>hydrating <span>?</span> vnode<span>.</span>elm <span>:</span> <span>undefined</span><span>,</span> hydrating<span>)</span>
  <span>}</span>
<span>}</span><span>,</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br></div></div><p><code>init</code> 钩子函数执行也很简单，我们先不考虑 <code>keepAlive</code> 的情况，它是通过 <code>createComponentInstanceForVnode</code> 创建一个 Vue 的实例，然后调用 <code>$mount</code> 方法挂载子组件， 先来看一下 <code>createComponentInstanceForVnode</code> 的实现：</p>
<div><pre><code><span>export</span> <span>function</span> <span>createComponentInstanceForVnode</span> <span>(</span>
  vnode<span>:</span> any<span>,</span> <span>// we know it's MountedComponentVNode but flow doesn't</span>
  parent<span>:</span> any<span>,</span> <span>// activeInstance in lifecycle state</span>
<span>)</span><span>:</span> Component <span>{</span>
  <span>const</span> options<span>:</span> InternalComponentOptions <span>=</span> <span>{</span>
    _isComponent<span>:</span> <span>true</span><span>,</span>
    _parentVnode<span>:</span> vnode<span>,</span>
    parent
  <span>}</span>
  <span>// check inline-template render functions</span>
  <span>const</span> inlineTemplate <span>=</span> vnode<span>.</span>data<span>.</span>inlineTemplate
  <span>if</span> <span>(</span><span>isDef</span><span>(</span>inlineTemplate<span>)</span><span>)</span> <span>{</span>
    options<span>.</span>render <span>=</span> inlineTemplate<span>.</span>render
    options<span>.</span>staticRenderFns <span>=</span> inlineTemplate<span>.</span>staticRenderFns
  <span>}</span>
  <span>return</span> <span>new</span> <span>vnode<span>.</span>componentOptions<span>.</span>Ctor</span><span>(</span>options<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br></div></div><p><code>createComponentInstanceForVnode</code> 函数构造的一个内部组件的参数，然后执行 <code>new vnode.componentOptions.Ctor(options)</code>。这里的 <code>vnode.componentOptions.Ctor</code> 对应的就是子组件的构造函数，我们上一节分析了它实际上是继承于 Vue 的一个构造器 <code>Sub</code>，相当于 <code>new Sub(options)</code> 这里有几个关键参数要注意几个点，<code>_isComponent</code> 为 <code>true</code> 表示它是一个组件，<code>parent</code> 表示当前激活的组件实例（注意，这里比较有意思的是如何拿到组件实例，后面会介绍。</p>
<p>所以子组件的实例化实际上就是在这个时机执行的，并且它会执行实例的 <code>_init</code> 方法，这个过程有一些和之前不同的地方需要挑出来说，代码在 <code>src/core/instance/init.js</code> 中：</p>
<div><pre><code><span>Vue</span><span>.</span>prototype<span>.</span><span>_init</span> <span>=</span> <span>function</span> <span>(</span><span>options<span>?</span><span>:</span> Object</span><span>)</span> <span>{</span>
  <span>const</span> vm<span>:</span> Component <span>=</span> <span>this</span>
  <span>// merge options</span>
  <span>if</span> <span>(</span>options <span>&amp;&amp;</span> options<span>.</span>_isComponent<span>)</span> <span>{</span>
    <span>// optimize internal component instantiation</span>
    <span>// since dynamic options merging is pretty slow, and none of the</span>
    <span>// internal component options needs special treatment.</span>
    <span>initInternalComponent</span><span>(</span>vm<span>,</span> options<span>)</span>
  <span>}</span> <span>else</span> <span>{</span>
    vm<span>.</span>$options <span>=</span> <span>mergeOptions</span><span>(</span>
      <span>resolveConstructorOptions</span><span>(</span>vm<span>.</span>constructor<span>)</span><span>,</span>
      options <span>||</span> <span>{</span><span>}</span><span>,</span>
      vm
    <span>)</span>
  <span>}</span>
  <span>// ...</span>
  <span>if</span> <span>(</span>vm<span>.</span>$options<span>.</span>el<span>)</span> <span>{</span>
    vm<span>.</span><span>$mount</span><span>(</span>vm<span>.</span>$options<span>.</span>el<span>)</span>
  <span>}</span> 
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br></div></div><p>这里首先是合并 <code>options</code> 的过程有变化，<code>_isComponent</code> 为 true，所以走到了 <code>initInternalComponent</code> 过程，这个函数的实现也简单看一下：</p>
<div><pre><code><span>export</span> <span>function</span> <span>initInternalComponent</span> <span>(</span><span>vm<span>:</span> Component<span>,</span> options<span>:</span> InternalComponentOptions</span><span>)</span> <span>{</span>
  <span>const</span> opts <span>=</span> vm<span>.</span>$options <span>=</span> Object<span>.</span><span>create</span><span>(</span>vm<span>.</span>constructor<span>.</span>options<span>)</span>
  <span>// doing this because it's faster than dynamic enumeration.</span>
  <span>const</span> parentVnode <span>=</span> options<span>.</span>_parentVnode
  opts<span>.</span>parent <span>=</span> options<span>.</span>parent
  opts<span>.</span>_parentVnode <span>=</span> parentVnode

  <span>const</span> vnodeComponentOptions <span>=</span> parentVnode<span>.</span>componentOptions
  opts<span>.</span>propsData <span>=</span> vnodeComponentOptions<span>.</span>propsData
  opts<span>.</span>_parentListeners <span>=</span> vnodeComponentOptions<span>.</span>listeners
  opts<span>.</span>_renderChildren <span>=</span> vnodeComponentOptions<span>.</span>children
  opts<span>.</span>_componentTag <span>=</span> vnodeComponentOptions<span>.</span>tag

  <span>if</span> <span>(</span>options<span>.</span>render<span>)</span> <span>{</span>
    opts<span>.</span>render <span>=</span> options<span>.</span>render
    opts<span>.</span>staticRenderFns <span>=</span> options<span>.</span>staticRenderFns
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br></div></div><p>这个过程我们重点记住以下几个点即可：<code>opts.parent = options.parent</code>、<code>opts._parentVnode = parentVnode</code>，它们是把之前我们通过 <code>createComponentInstanceForVnode</code> 函数传入的几个参数合并到内部的选项 <code>$options</code> 里了。</p>
<p>再来看一下 <code>_init</code> 函数最后执行的代码：</p>
<div><pre><code><span>if</span> <span>(</span>vm<span>.</span>$options<span>.</span>el<span>)</span> <span>{</span>
   vm<span>.</span><span>$mount</span><span>(</span>vm<span>.</span>$options<span>.</span>el<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><p>由于组件初始化的时候是不传 el 的，因此组件是自己接管了 <code>$mount</code> 的过程，这个过程的主要流程在上一章介绍过了，回到组件 <code>init</code> 的过程，<code>componentVNodeHooks</code> 的 <code>init</code> 钩子函数，在完成实例化的 <code>_init</code> 后，接着会执行 <code>child.$mount(hydrating ? vnode.elm : undefined, hydrating)</code> 。这里 <code>hydrating</code> 为 true 一般是服务端渲染的情况，我们只考虑客户端渲染，所以这里 <code>$mount</code> 相当于执行 <code>child.$mount(undefined, false)</code>，它最终会调用 <code>mountComponent</code> 方法，进而执行 <code>vm._render()</code> 方法：</p>
<div><pre><code><span>Vue</span><span>.</span>prototype<span>.</span><span>_render</span> <span>=</span> <span>function</span> <span>(</span><span>)</span><span>:</span> VNode <span>{</span>
  <span>const</span> vm<span>:</span> Component <span>=</span> <span>this</span>
  <span>const</span> <span>{</span> render<span>,</span> _parentVnode <span>}</span> <span>=</span> vm<span>.</span>$options

  
  <span>// set parent vnode. this allows render functions to have access</span>
  <span>// to the data on the placeholder node.</span>
  vm<span>.</span>$vnode <span>=</span> _parentVnode
  <span>// render self</span>
  <span>let</span> vnode
  <span>try</span> <span>{</span>
    vnode <span>=</span> <span>render</span><span>.</span><span>call</span><span>(</span>vm<span>.</span>_renderProxy<span>,</span> vm<span>.</span>$createElement<span>)</span>
  <span>}</span> <span>catch</span> <span>(</span>e<span>)</span> <span>{</span>
    <span>// ...</span>
  <span>}</span>
  <span>// set parent</span>
  vnode<span>.</span>parent <span>=</span> _parentVnode
  <span>return</span> vnode
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div><p>我们只保留关键部分的代码，这里的 <code>_parentVnode</code> 就是当前组件的父 VNode，而 <code>render</code> 函数生成的 <code>vnode</code> 当前组件的渲染 <code>vnode</code>，<code>vnode</code> 的 <code>parent</code> 指向了 <code>_parentVnode</code>，也就是 <code>vm.$vnode</code>，它们是一种父子的关系。</p>
<p>我们知道在执行完 <code>vm._render</code> 生成 VNode 后，接下来就要执行 <code>vm._update</code> 去渲染 VNode 了。来看一下组件渲染的过程中有哪些需要注意的，<code>vm._update</code> 的定义在 <code>src/core/instance/lifecycle.js</code> 中：</p>
<div><pre><code><span>export</span> <span>let</span> activeInstance<span>:</span> any <span>=</span> <span>null</span>
<span>Vue</span><span>.</span>prototype<span>.</span><span>_update</span> <span>=</span> <span>function</span> <span>(</span><span>vnode<span>:</span> VNode<span>,</span> hydrating<span>?</span><span>:</span> boolean</span><span>)</span> <span>{</span>
  <span>const</span> vm<span>:</span> Component <span>=</span> <span>this</span>
  <span>const</span> prevEl <span>=</span> vm<span>.</span>$el
  <span>const</span> prevVnode <span>=</span> vm<span>.</span>_vnode
  <span>const</span> prevActiveInstance <span>=</span> activeInstance
  activeInstance <span>=</span> vm
  vm<span>.</span>_vnode <span>=</span> vnode
  <span>// Vue.prototype.__patch__ is injected in entry points</span>
  <span>// based on the rendering backend used.</span>
  <span>if</span> <span>(</span><span>!</span>prevVnode<span>)</span> <span>{</span>
    <span>// initial render</span>
    vm<span>.</span>$el <span>=</span> vm<span>.</span><span>__patch__</span><span>(</span>vm<span>.</span>$el<span>,</span> vnode<span>,</span> hydrating<span>,</span> <span>false</span> <span>/* removeOnly */</span><span>)</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>// updates</span>
    vm<span>.</span>$el <span>=</span> vm<span>.</span><span>__patch__</span><span>(</span>prevVnode<span>,</span> vnode<span>)</span>
  <span>}</span>
  activeInstance <span>=</span> prevActiveInstance
  <span>// update __vue__ reference</span>
  <span>if</span> <span>(</span>prevEl<span>)</span> <span>{</span>
    prevEl<span>.</span>__vue__ <span>=</span> <span>null</span>
  <span>}</span>
  <span>if</span> <span>(</span>vm<span>.</span>$el<span>)</span> <span>{</span>
    vm<span>.</span>$el<span>.</span>__vue__ <span>=</span> vm
  <span>}</span>
  <span>// if parent is an HOC, update its $el as well</span>
  <span>if</span> <span>(</span>vm<span>.</span>$vnode <span>&amp;&amp;</span> vm<span>.</span>$parent <span>&amp;&amp;</span> vm<span>.</span>$vnode <span>===</span> vm<span>.</span>$parent<span>.</span>_vnode<span>)</span> <span>{</span>
    vm<span>.</span>$parent<span>.</span>$el <span>=</span> vm<span>.</span>$el
  <span>}</span>
  <span>// updated hook is called by the scheduler to ensure that children are</span>
  <span>// updated in a parent's updated hook.</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br></div></div><p><code>_update</code> 过程中有几个关键的代码，首先 <code>vm._vnode = vnode</code> 的逻辑，这个 <code>vnode</code> 是通过 <code>vm._render()</code> 返回的组件渲染 VNode，<code>vm._vnode</code> 和 <code>vm.$vnode</code> 的关系就是一种父子关系，用代码表达就是 <code>vm._vnode.parent === vm.$vnode</code>。还有一段比较有意思的代码：</p>
<div><pre><code><span>export</span> <span>let</span> activeInstance<span>:</span> any <span>=</span> <span>null</span>
<span>Vue</span><span>.</span>prototype<span>.</span><span>_update</span> <span>=</span> <span>function</span> <span>(</span><span>vnode<span>:</span> VNode<span>,</span> hydrating<span>?</span><span>:</span> boolean</span><span>)</span> <span>{</span>
    <span>// ...</span>
    <span>const</span> prevActiveInstance <span>=</span> activeInstance
    activeInstance <span>=</span> vm
    <span>if</span> <span>(</span><span>!</span>prevVnode<span>)</span> <span>{</span>
      <span>// initial render</span>
      vm<span>.</span>$el <span>=</span> vm<span>.</span><span>__patch__</span><span>(</span>vm<span>.</span>$el<span>,</span> vnode<span>,</span> hydrating<span>,</span> <span>false</span> <span>/* removeOnly */</span><span>)</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>// updates</span>
      vm<span>.</span>$el <span>=</span> vm<span>.</span><span>__patch__</span><span>(</span>prevVnode<span>,</span> vnode<span>)</span>
    <span>}</span>
    activeInstance <span>=</span> prevActiveInstance
  <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div><p>这个 <code>activeInstance</code> 作用就是保持当前上下文的 Vue 实例，它是在 <code>lifecycle</code> 模块的全局变量，定义是 <code>export let activeInstance: any = null</code>，并且在之前我们调用 <code>createComponentInstanceForVnode</code> 方法的时候从 <code>lifecycle</code> 模块获取，并且作为参数传入的。因为实际上 JavaScript 是一个单线程，Vue 整个初始化是一个深度遍历的过程，在实例化子组件的过程中，它需要知道当前上下文的 Vue 实例是什么，并把它作为子组件的父 Vue 实例。之前我们提到过对子组件的实例化过程先会调用 <code>initInternalComponent(vm, options)</code> 合并 <code>options</code>，把 <code>parent</code> 存储在 <code>vm.$options</code> 中，在 <code>$mount</code> 之前会调用 <code>initLifecycle(vm)</code> 方法：</p>
<div><pre><code><span>export</span> <span>function</span> <span>initLifecycle</span> <span>(</span><span>vm<span>:</span> Component</span><span>)</span> <span>{</span>
  <span>const</span> options <span>=</span> vm<span>.</span>$options

  <span>// locate first non-abstract parent</span>
  <span>let</span> parent <span>=</span> options<span>.</span>parent
  <span>if</span> <span>(</span>parent <span>&amp;&amp;</span> <span>!</span>options<span>.</span>abstract<span>)</span> <span>{</span>
    <span>while</span> <span>(</span>parent<span>.</span>$options<span>.</span>abstract <span>&amp;&amp;</span> parent<span>.</span>$parent<span>)</span> <span>{</span>
      parent <span>=</span> parent<span>.</span>$parent
    <span>}</span>
    parent<span>.</span>$children<span>.</span><span>push</span><span>(</span>vm<span>)</span>
  <span>}</span>

  vm<span>.</span>$parent <span>=</span> parent
  <span>// ...</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div><p>可以看到 <code>vm.$parent</code> 就是用来保留当前 <code>vm</code> 的父实例，并且通过 <code>parent.$children.push(vm)</code> 来把当前的 <code>vm</code> 存储到父实例的 <code>$children</code> 中。</p>
<p>在 <code>vm._update</code> 的过程中，把当前的 <code>vm</code> 赋值给 <code>activeInstance</code>，同时通过 <code>const prevActiveInstance = activeInstance</code> 用 <code>prevActiveInstance</code> 保留上一次的 <code>activeInstance</code>。实际上，<code>prevActiveInstance</code> 和当前的 <code>vm</code> 是一个父子关系，当一个 <code>vm</code> 实例完成它的所有子树的 patch 或者 update 过程后，<code>activeInstance</code> 会回到它的父实例，这样就完美地保证了 <code>createComponentInstanceForVnode</code> 整个深度遍历过程中，我们在实例化子组件的时候能传入当前子组件的父 Vue 实例，并在 <code>_init</code> 的过程中，通过 <code>vm.$parent</code> 把这个父子关系保留。</p>
<p>那么回到 <code>_update</code>，最后就是调用 <code>__patch__</code> 渲染 VNode 了。</p>
<div><pre><code>vm<span>.</span>$el <span>=</span> vm<span>.</span><span>__patch__</span><span>(</span>vm<span>.</span>$el<span>,</span> vnode<span>,</span> hydrating<span>,</span> <span>false</span> <span>/* removeOnly */</span><span>)</span>
 
<span>function</span> <span>patch</span> <span>(</span><span>oldVnode<span>,</span> vnode<span>,</span> hydrating<span>,</span> removeOnly</span><span>)</span> <span>{</span>
  <span>// ...</span>
  <span>let</span> isInitialPatch <span>=</span> <span>false</span>
  <span>const</span> insertedVnodeQueue <span>=</span> <span>[</span><span>]</span>

  <span>if</span> <span>(</span><span>isUndef</span><span>(</span>oldVnode<span>)</span><span>)</span> <span>{</span>
    <span>// empty mount (likely as component), create new root element</span>
    isInitialPatch <span>=</span> <span>true</span>
    <span>createElm</span><span>(</span>vnode<span>,</span> insertedVnodeQueue<span>)</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>// ...</span>
  <span>}</span>
  <span>// ...</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div><p>这里又回到了本节开始的过程，之前分析过负责渲染成 DOM 的函数是 <code>createElm</code>，注意这里我们只传了 2 个参数，所以对应的 <code>parentElm</code> 是 <code>undefined</code>。我们再来看看它的定义：</p>
<div><pre><code><span>function</span> <span>createElm</span> <span>(</span>
  <span>vnode<span>,</span>
  insertedVnodeQueue<span>,</span>
  parentElm<span>,</span>
  refElm<span>,</span>
  nested<span>,</span>
  ownerArray<span>,</span>
  index</span>
<span>)</span> <span>{</span>
  <span>// ...</span>
  <span>if</span> <span>(</span><span>createComponent</span><span>(</span>vnode<span>,</span> insertedVnodeQueue<span>,</span> parentElm<span>,</span> refElm<span>)</span><span>)</span> <span>{</span>
    <span>return</span>
  <span>}</span>

  <span>const</span> data <span>=</span> vnode<span>.</span>data
  <span>const</span> children <span>=</span> vnode<span>.</span>children
  <span>const</span> tag <span>=</span> vnode<span>.</span>tag
  <span>if</span> <span>(</span><span>isDef</span><span>(</span>tag<span>)</span><span>)</span> <span>{</span>
    <span>// ...</span>

    vnode<span>.</span>elm <span>=</span> vnode<span>.</span>ns
      <span>?</span> nodeOps<span>.</span><span>createElementNS</span><span>(</span>vnode<span>.</span>ns<span>,</span> tag<span>)</span>
      <span>:</span> nodeOps<span>.</span><span>createElement</span><span>(</span>tag<span>,</span> vnode<span>)</span>
    <span>setScope</span><span>(</span>vnode<span>)</span>

    <span>/* istanbul ignore if */</span>
    <span>if</span> <span>(</span>__WEEX__<span>)</span> <span>{</span>
      <span>// ...</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>createChildren</span><span>(</span>vnode<span>,</span> children<span>,</span> insertedVnodeQueue<span>)</span>
      <span>if</span> <span>(</span><span>isDef</span><span>(</span>data<span>)</span><span>)</span> <span>{</span>
        <span>invokeCreateHooks</span><span>(</span>vnode<span>,</span> insertedVnodeQueue<span>)</span>
      <span>}</span>
      <span>insert</span><span>(</span>parentElm<span>,</span> vnode<span>.</span>elm<span>,</span> refElm<span>)</span>
    <span>}</span>
    
    <span>// ...</span>
  <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>isTrue</span><span>(</span>vnode<span>.</span>isComment<span>)</span><span>)</span> <span>{</span>
    vnode<span>.</span>elm <span>=</span> nodeOps<span>.</span><span>createComment</span><span>(</span>vnode<span>.</span>text<span>)</span>
    <span>insert</span><span>(</span>parentElm<span>,</span> vnode<span>.</span>elm<span>,</span> refElm<span>)</span>
  <span>}</span> <span>else</span> <span>{</span>
    vnode<span>.</span>elm <span>=</span> nodeOps<span>.</span><span>createTextNode</span><span>(</span>vnode<span>.</span>text<span>)</span>
    <span>insert</span><span>(</span>parentElm<span>,</span> vnode<span>.</span>elm<span>,</span> refElm<span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br></div></div><p>注意，这里我们传入的 <code>vnode</code> 是组件渲染的 <code>vnode</code>，也就是我们之前说的 <code>vm._vnode</code>，如果组件的根节点是个普通元素，那么 <code>vm._vnode</code> 也是普通的 <code>vnode</code>，这里 <code>createComponent(vnode, insertedVnodeQueue, parentElm, refElm)</code> 的返回值是 false。接下来的过程就和我们上一章一样了，先创建一个父节点占位符，然后再遍历所有子 VNode 递归调用 <code>createElm</code>，在遍历的过程中，如果遇到子 VNode 是一个组件的 VNode，则重复本节开始的过程，这样通过一个递归的方式就可以完整地构建了整个组件树。</p>
<p>由于我们这个时候传入的 <code>parentElm</code> 是空，所以对组件的插入，在 <code>createComponent</code> 有这么一段逻辑：</p>
<div><pre><code><span>function</span> <span>createComponent</span> <span>(</span><span>vnode<span>,</span> insertedVnodeQueue<span>,</span> parentElm<span>,</span> refElm</span><span>)</span> <span>{</span>
  <span>let</span> i <span>=</span> vnode<span>.</span>data
  <span>if</span> <span>(</span><span>isDef</span><span>(</span>i<span>)</span><span>)</span> <span>{</span>
    <span>// ....</span>
    <span>if</span> <span>(</span><span>isDef</span><span>(</span>i <span>=</span> i<span>.</span>hook<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>i <span>=</span> i<span>.</span>init<span>)</span><span>)</span> <span>{</span>
      <span>i</span><span>(</span>vnode<span>,</span> <span>false</span> <span>/* hydrating */</span><span>)</span>
    <span>}</span>
    <span>// ...</span>
    <span>if</span> <span>(</span><span>isDef</span><span>(</span>vnode<span>.</span>componentInstance<span>)</span><span>)</span> <span>{</span>
      <span>initComponent</span><span>(</span>vnode<span>,</span> insertedVnodeQueue<span>)</span>
      <span>insert</span><span>(</span>parentElm<span>,</span> vnode<span>.</span>elm<span>,</span> refElm<span>)</span>
      <span>if</span> <span>(</span><span>isTrue</span><span>(</span>isReactivated<span>)</span><span>)</span> <span>{</span>
        <span>reactivateComponent</span><span>(</span>vnode<span>,</span> insertedVnodeQueue<span>,</span> parentElm<span>,</span> refElm<span>)</span>
      <span>}</span>
      <span>return</span> <span>true</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br></div></div><p>在完成组件的整个 <code>patch</code> 过程后，最后执行 <code>insert(parentElm, vnode.elm, refElm)</code> 完成组件的 DOM 插入，如果组件 <code>patch</code> 过程中又创建了子组件，那么DOM 的插入顺序是先子后父。</p>
</li>
</ul>
</li>
</ul>
<h3 id="派发更新"> 派发更新</h3>
<ul>
<li>
<p>总体更新流程</p>
<p><img src="./../flow-graph/派发更新.png" alt="" /></p>
</li>
</ul>
<h4 id="异步更新"> 异步更新</h4>
<ul>
<li>
<p>理解异步更新，先理解 <a href="https://gitee.com/jokerxw/myNotes/tree/master/javascript/javascript%E7%9F%A5%E8%AF%86%E7%82%B9%E6%95%B4%E5%90%88#%E4%BA%8B%E4%BB%B6%E5%BE%AA%E7%8E%AF" target="_blank" rel="noopener noreferrer">js 事件循环机制</a></p>
<p><img src="./../images/nextTick2.png" alt="" /></p>
<p><img src="./../images/nextTick3.png" alt="" /></p>
</li>
<li>
<p><strong>异步更新队列原理：</strong></p>
<ul>
<li>Vue中有一个watcher，用于观察数据的变化，然后更新dom。</li>
<li>Vue里面不是每一次数据改变都会触发更新dom，而是将这些操作都缓存在一个队列，在一个事件循环结束之后，刷新队列，统一执行dom更新操作</li>
</ul>
</li>
<li>
<p>核心流程：</p>
<ul>
<li>**定义响应式：**遍历 data 属性为其增加 get，set 方法，在 get 方法中会收集依赖 (dev.subs.push(watcher))，而 set 方法则会调用 dep 的notify 方法，此方法的作用是通知 subs 中的所有的 watcher 并调用 watcher 的 update 方法，我们可以将此理解为设计模式中的发布与订阅;</li>
<li>**修改数据：**当我们在组件中对响应的数据做了修改，就会触发 setter 的逻辑，最后调用 <code>dep.notify()</code> 方法，notify() 遍历所有的 <code>subs</code>，也就是 <code>Watcher</code> 的实例数组，然后调用每一个 <code>watcher</code> 的 <code>update</code> 方法;</li>
<li><strong>数据更新：</strong> update 方法中首先判断两个特殊标记（ <code>Watcher</code> 的状态标记）:
<ul>
<li>是否为 lazy 懒更新，则设置 dirty 为 true，以标记当前 <code>watcher</code> 为懒更新;</li>
<li>再判断是否有 sync 同步更新标记，直接执行 <code>watcher.run()</code>，Vue 官方不推荐使用，文档没有该属性;</li>
</ul>
</li>
<li><strong>Vue 默认异步更新</strong>， <code>watcher.async</code> 为true，因此 update 方法被调用后会触发 queueWatcher 函数，此函数的主要功能就是将 watcher 实例本身加入一个队列中(queue.push(watcher))；
<ul>
<li>queueWatcher 函数用 <code>has</code> 对象保证同一个 <code>Watcher</code> 只添加一次；</li>
<li>放入队列有两种方式，以 flushing 标志判断：
<ul>
<li>若无在刷新队列中，直接 push 进 queue 队列</li>
<li>若正在刷新队列中，按 watcher.id 进行升序排序，确保更新的顺序</li>
</ul>
</li>
<li>通过 <code>wating</code> 保证对 <code>nextTick(flushSchedulerQueue)</code> 的调用逻辑只有一次</li>
<li>这也是 Vue 在做派发更新的时候的一个优化的点，它并不会每次数据改变都触发 <code>watcher</code> 的回调，而是把这些 <code>watcher</code> 先添加到一个队列里，然后在 <code>nextTick</code> 后执行 <code>flushSchedulerQueue</code></li>
</ul>
</li>
<li>然后调用 nextTick(flushSchedulerQueue)，将 flushSchedulerQueue 放入 callbacks 数组中，若浏览器的任务队列中无 flushCallbacks 函数，则执行 timerFunc()（用 pending 来判断控制）。</li>
<li>timerFunc() 将 flushCallbacks 函数放入浏览器的异步任务队列中。</li>
<li>等待浏览器异步任务队列执行 callbacks 数组中的 flushSchedulerQueue 方法。</li>
<li>flushSchedulerQueue 是一个函数，目的是调用 queue 中所有 watcher 的 watcher.run 方法。
<ul>
<li>flushSchedulerQueue 首先进行队列排序： <code>queue.sort((a, b) =&gt; a.id - b.id)</code> 对队列做了从小到大的排序；</li>
<li>然后执行队列遍历：在对 <code>queue</code> 排序后，接着就是要对它做遍历，拿到对应的 <code>watcher</code>，执行 <code>watcher.run()</code></li>
</ul>
</li>
<li>flushSchedulerQueue 在被执行后调用 watcher.run()，而 run 方法被调用后接下来的操作就是通过新的虚拟 dom 与老的虚拟 dom 做 diff 算法后生成新的真实 dom于是你看到了一个新的页面</li>
</ul>
</li>
<li>
<p>实现异步更新流程：</p>
<div><pre><code><span>//============================简单实现异步更新流程==============================================</span>
<span>let</span> uid <span>=</span> <span>0</span><span>;</span>
<span>class</span> <span>Watcher</span> <span>{</span>
  <span>constructor</span><span>(</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>id <span>=</span> <span>++</span>uid<span>;</span> <span>//观察者对象的uid</span>
  <span>}</span>

  <span>update</span><span>(</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'watch'</span> <span>+</span> <span>this</span><span>.</span>id <span>+</span> <span>' update'</span><span>)</span><span>;</span>
    <span>queueWatcher</span><span>(</span><span>this</span><span>)</span><span>;</span> <span>//观察者队列</span>
  <span>}</span>

  <span>run</span><span>(</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'watch'</span> <span>+</span> <span>this</span><span>.</span>id <span>+</span> <span>'视图更新啦～'</span><span>)</span><span>;</span>
  <span>}</span>
<span>}</span>

<span>let</span> has <span>=</span> <span>{</span><span>}</span><span>;</span>
<span>let</span> queue <span>=</span> <span>[</span><span>]</span><span>;</span> <span>//watcher 队列</span>
<span>//waiting 是一个标记位，标记是否已经向 nextTick 传递了 flushSchedulerQueue 方法，在下一个 tick</span>
<span>//时候执行 flushSchedulerQueue 方法来 flush queue，执行它里面的所有 Watcher 对象的 run 方法</span>
<span>let</span> waiting <span>=</span> <span>false</span><span>;</span>

<span>/**
 * 缓存变更数据的观察者队列，如果队列中不存在该观察者对象，则缓存；否则忽略
 * @param {Watcher} watcher 变更数据对应的观察者对象
 */</span>
<span>function</span> <span>queueWatcher</span><span>(</span><span>watcher</span><span>)</span> <span>{</span>
  <span>const</span> id <span>=</span> watcher<span>.</span>id<span>;</span> <span>//获取观察者对象id</span>
  <span>if</span> <span>(</span>has<span>[</span>id<span>]</span> <span>==</span> <span>null</span><span>)</span> <span>{</span> <span>//观察者队列中不存在id的观察者对象,则添加进队列</span>
    has<span>[</span>id<span>]</span> <span>=</span> <span>true</span><span>;</span> <span>//设置存在标记</span>
    queue<span>.</span><span>push</span><span>(</span>watcher<span>)</span><span>;</span>
    <span>if</span> <span>(</span><span>!</span>waiting<span>)</span> <span>{</span> <span>//未向nextTick传入flushSchedulerQueue,则传入队列</span>
      waiting <span>=</span> <span>true</span><span>;</span> <span>//等待执行 watcher queue</span>
      <span>nextTick</span><span>(</span>flushSchedulerQueue<span>)</span><span>;</span>
    <span>}</span>
  <span>}</span>
<span>}</span>

<span>/**
 * 取出一次 tick 时所有 Watcher，执行它里面的所有 Watcher 对象的 run 方法
 */</span>
<span>function</span> <span>flushSchedulerQueue</span><span>(</span><span>)</span> <span>{</span>
  <span>let</span> watcher<span>,</span> id<span>;</span>
  queue<span>.</span><span>sort</span><span>(</span><span>(</span><span>a<span>,</span> b</span><span>)</span> <span>=></span> a<span>.</span>id <span>-</span> b<span>.</span>id<span>)</span> <span>//队列排序</span>
  <span>// 执行 watcher queue 中所有 run()</span>
  <span>for</span> <span>(</span>index <span>=</span> <span>0</span><span>;</span> index <span>&lt;</span> queue<span>.</span>length<span>;</span> index<span>++</span><span>)</span> <span>{</span>
    watcher <span>=</span> queue<span>[</span>index<span>]</span>
    id <span>=</span> watcher<span>.</span>id<span>;</span>
    has<span>[</span>id<span>]</span> <span>=</span> <span>null</span><span>;</span>
    watcher<span>.</span><span>run</span><span>(</span><span>)</span><span>;</span>
  <span>}</span>
  waiting <span>=</span> <span>false</span><span>;</span> <span>// 停止等待执行一次 tick 时所有 watcher queue</span>
<span>}</span>

<span>//callbacks 数组用来存储 nextTick，在下一个tick处理这些回调函数之前，所有的cb都会被存在这个callbacks数组中</span>
<span>let</span> callbacks <span>=</span> <span>[</span><span>]</span><span>;</span>
<span>let</span> pending <span>=</span> <span>false</span><span>;</span><span>// pending 是一个标记位，代表一个等待的状态</span>

<span>/**
 * 在下次 DOM 更新循环结束之后执行延迟回调，在修改数据之后立即使用这个方法，获取更新后的 DOM
 * @param {Function} cb 回调函数
 */</span>
<span>function</span> <span>nextTick</span><span>(</span><span>cb</span><span>)</span> <span>{</span>
  callbacks<span>.</span><span>push</span><span>(</span>cb<span>)</span><span>;</span>
  <span>if</span> <span>(</span><span>!</span>pending<span>)</span> <span>{</span>
    pending <span>=</span> <span>true</span><span>;</span>
    <span>//使用setTimeout模拟异步更新, setTimeout会在 task 中创建一个事件 flushCallbacks </span>
    <span>setTimeout</span><span>(</span>flushCallbacks<span>,</span> <span>0</span><span>)</span><span>;</span>
  <span>}</span>
<span>}</span>

<span>/**
 * 在执行时将 callbacks 中的所有 cb 依次执行,这里的 cb 就是 flushSchedulerQueue 方法
 */</span>
<span>function</span> <span>flushCallbacks</span><span>(</span><span>)</span> <span>{</span>
  pending <span>=</span> <span>false</span><span>;</span>
  <span>const</span> copies <span>=</span> callbacks<span>.</span><span>slice</span><span>(</span><span>0</span><span>)</span><span>;</span>
  callbacks<span>.</span>length <span>=</span> <span>0</span><span>;</span> <span>//清空回调队列</span>
  <span>// 执行回调队列中所有的回调函数</span>
  <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> copies<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
    copies<span>[</span>i<span>]</span><span>(</span><span>)</span><span>;</span>
  <span>}</span>
<span>}</span>

<span>//--------------------------------测试---------------------------------------------------</span>
<span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
  <span>let</span> watch1 <span>=</span> <span>new</span> <span>Watcher</span><span>(</span><span>)</span><span>;</span>
  <span>let</span> watch2 <span>=</span> <span>new</span> <span>Watcher</span><span>(</span><span>)</span><span>;</span>

  watch1<span>.</span><span>update</span><span>(</span><span>)</span><span>;</span>
  watch1<span>.</span><span>update</span><span>(</span><span>)</span><span>;</span>
  watch2<span>.</span><span>update</span><span>(</span><span>)</span><span>;</span>
<span>}</span><span>)</span><span>(</span><span>)</span><span>;</span>
<span>/**
  我们现在 new 了两个 Watcher 对象，因为修改了 data 的数据，所以我们模拟触发了两次 watch1的 update 以及 一次 watch2 的 update
  假设没有批量异步更新策略的话，理论上应该执行 Watcher 对象的 run，那么会打印：
  watch1 update
  watch1 视图更新啦～
  watch1 update
  watch1 视图更新啦～
  watch2 update
  watch2 视图更新啦～
  实际上则执行：
  watch1 update
  watch1 update
  watch2 update
  watch1 视图更新啦～
  watch2 视图更新啦～
  异步更新策略的效果把相同的 Watcher 对象会在这个过程中被剔除，在下一个 tick 的时候去更新视图
 */</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br><span>93</span><br><span>94</span><br><span>95</span><br><span>96</span><br><span>97</span><br><span>98</span><br><span>99</span><br><span>100</span><br><span>101</span><br><span>102</span><br><span>103</span><br><span>104</span><br><span>105</span><br><span>106</span><br><span>107</span><br><span>108</span><br><span>109</span><br><span>110</span><br><span>111</span><br></div></div></li>
<li>
<blockquote>
<p>为什么要为什么要异步更新数据 ？</p>
</blockquote>
<div><pre><code><span><span><span>&lt;</span>template</span><span>></span></span>
    <span><span><span>&lt;</span>div</span><span>></span></span><span><span><span>&lt;</span>div</span><span>></span></span>{{number}}<span><span><span>&lt;/</span>div</span><span>></span></span>
        &lt;div@click="handleClick">click<span><span><span>&lt;/</span>div</span><span>></span></span>
    <span><span><span>&lt;/</span>div</span><span>></span></span>
<span><span><span>&lt;/</span>template</span><span>></span></span>

export default {
	data(){ 
		return {
			number: 0
		};
	},
	methods: {
		handleClick(){
			for( let i=0; i &lt; 1000 ;i++) { 
            	this.number++;
            }
        }
    }
  }
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br></div></div><ul>
<li>当我们按下  click  按钮的时候， number 会被循环增加 1000 次。</li>
<li>那么按照之前的理解，每次 number 被  +1  的时候，都会触发 number 的 setter 方法，从而根据上面的流程一直跑下来最后修改真实  DOM。那么在这个过程中，DOM  会被更新  1000  次！ Vue.js  为了避免如此低效的处理方法。</li>
<li>Vue.js 在默认情况下，每次触发某个数据的 setter 方法后，对应的 Watcher 对象其实会被 push 进一个队列 queue 中，在下一个  tick  的时候将这个队列queue 全部拿出来 run（ Watcher 对象的一个方法，用来触发 patch 操作）  一遍。</li>
</ul>
</li>
</ul>
<h3 id="销毁实例"> 销毁实例</h3>
<hr>
<h2 id="组件"> 组件</h2>
<h3 id="组件基础"> <a href="https://cn.vuejs.org/v2/guide/components.html" target="_blank" rel="noopener noreferrer">组件基础</a></h3>
<p><img src="./../images/zujian.png" alt="" /></p>
<h3 id="组件注册"> 组件注册</h3>
<ul>
<li>组件注册有两种方式
<ul>
<li>全局注册：使用 <a href="https://cn.vuejs.org/v2/api/#Vue-component" target="_blank" rel="noopener noreferrer">Vue.component( id, [definition] )</a></li>
<li>局部注册</li>
</ul>
</li>
</ul>
<p><strong>全局注册</strong></p>
<ul>
<li>
<p>原理：将全局注册的组件扩展到 <code>Vue.options</code> 中，在所有组件创建的过程中，都会从全局的 <code>Vue.options.components</code> 扩展到当前组件的 <code>vm.$options.components</code> 下。</p>
</li>
<li>
<p>要注册一个全局组件，可以使用 <code>Vue.component(tagName, options)</code>。例如：</p>
<div><pre><code>Vue<span>.</span><span>component</span><span>(</span><span>'my-component'</span><span>,</span> <span>{</span>
  <span>// 选项</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
<li>
<p><code>Vue.component</code> 函数： 定义过程发生在最开始初始化 Vue 的全局函数的时候： <code>import Vue from vue</code> 引入 vue -&gt; <code>initGlobalAPI</code>在 Vue 上扩展的一些全局方法的定义 -&gt; <code>initAssetRegisters</code> 注册全局 api</p>
<ul>
<li>
<p><code>initAssetRegisters</code>代码在 <code>src/core/global-api/assets.js</code> 中：</p>
<div><pre><code><span>export</span> <span>function</span> <span>initAssetRegisters</span> <span>(</span><span>Vue<span>:</span> GlobalAPI</span><span>)</span> <span>{</span>
  <span>/**
   * Create asset registration methods.
   */</span>
  <span>ASSET_TYPES</span><span>.</span><span>forEach</span><span>(</span><span>type</span> <span>=></span> <span>{</span>
    <span>//遍历 ASSET_TYPES，得到 type 后挂载到 Vue 上</span>
    Vue<span>[</span>type<span>]</span> <span>=</span> <span>function</span> <span>(</span> 
      <span>id<span>:</span> string<span>,</span>
      definition<span>:</span> Function <span>|</span> Object</span>
    <span>)</span><span>:</span> Function <span>|</span> Object <span>|</span> <span>void</span> <span>{</span>
      <span>if</span> <span>(</span><span>!</span>definition<span>)</span> <span>{</span>
        <span>return</span> <span>this</span><span>.</span>options<span>[</span>type <span>+</span> <span>'s'</span><span>]</span><span>[</span>id<span>]</span>
      <span>}</span> <span>else</span> <span>{</span>
        <span>/* istanbul ignore if */</span>
        <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
          <span>if</span> <span>(</span>type <span>===</span> <span>'component'</span> <span>&amp;&amp;</span> config<span>.</span><span>isReservedTag</span><span>(</span>id<span>)</span><span>)</span> <span>{</span>
            <span>warn</span><span>(</span>
              <span>'Do not use built-in or reserved HTML elements as component '</span> <span>+</span>
              <span>'id: '</span> <span>+</span> id
            <span>)</span>
          <span>}</span>
        <span>}</span>

        <span>if</span> <span>(</span>type <span>===</span> <span>'component'</span> <span>&amp;&amp;</span> <span>isPlainObject</span><span>(</span>definition<span>)</span><span>)</span> <span>{</span>
          definition<span>.</span>name <span>=</span> definition<span>.</span>name <span>||</span> id
          <span>//把组件转换成一个继承于 Vue 的构造函数</span>
          definition <span>=</span> <span>this</span><span>.</span>options<span>.</span>_base<span>.</span><span>extend</span><span>(</span>definition<span>)</span> 
        <span>}</span>
        <span>if</span> <span>(</span>type <span>===</span> <span>'directive'</span> <span>&amp;&amp;</span> <span>typeof</span> definition <span>===</span> <span>'function'</span><span>)</span> <span>{</span>
          definition <span>=</span> <span>{</span> bind<span>:</span> definition<span>,</span> update<span>:</span> definition <span>}</span>
        <span>}</span>
        <span>//挂载到 Vue.options</span>
        <span>this</span><span>.</span>options<span>[</span>type <span>+</span> <span>'s'</span><span>]</span><span>[</span>id<span>]</span> <span>=</span> definition 
        <span>return</span> definition
      <span>}</span>
    <span>}</span>
  <span>}</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br></div></div></li>
</ul>
</li>
<li>
<p>全局组件执行过程：在创建 <code>vnode</code> 的过程中 <code>init()</code> -&gt; <code>render()</code> -&gt; <code>update()</code> -&gt;  <code>_createElement()</code>  创建Vnode -&gt; <code>resolveAsset()</code> 解析全局组件 -&gt; <code>createComponent()</code> 创建组件</p>
<ul>
<li><code>_createElement</code> 方法中 调用 <code>resolveAsset()</code> 解析全局组件：</li>
</ul>
<div><pre><code><span>export</span> <span>function</span> <span>_createElement</span> <span>(</span>
  <span>context<span>:</span> Component<span>,</span>
  tag<span>?</span><span>:</span> string <span>|</span> Class<span>&lt;</span>Component<span>></span> <span>|</span> Function <span>|</span> Object<span>,</span>
  data<span>?</span><span>:</span> VNodeData<span>,</span>
  children<span>?</span><span>:</span> any<span>,</span>
  normalizationType<span>?</span><span>:</span> number</span>
<span>)</span><span>:</span> VNode <span>|</span> Array<span>&lt;</span>VNode<span>></span> <span>{</span>
  <span>// ...</span>
  <span>let</span> vnode<span>,</span> ns
  <span>if</span> <span>(</span><span>typeof</span> tag <span>===</span> <span>'string'</span><span>)</span> <span>{</span>
    <span>let</span> Ctor
    ns <span>=</span> <span>(</span>context<span>.</span>$vnode <span>&amp;&amp;</span> context<span>.</span>$vnode<span>.</span>ns<span>)</span> <span>||</span> config<span>.</span><span>getTagNamespace</span><span>(</span>tag<span>)</span>
    <span>if</span> <span>(</span>config<span>.</span><span>isReservedTag</span><span>(</span>tag<span>)</span><span>)</span> <span>{</span>
      <span>// platform built-in elements</span>
      vnode <span>=</span> <span>new</span> <span>VNode</span><span>(</span>
        config<span>.</span><span>parsePlatformTagName</span><span>(</span>tag<span>)</span><span>,</span> data<span>,</span> children<span>,</span>
        <span>undefined</span><span>,</span> <span>undefined</span><span>,</span> context
      <span>)</span><span>//调用 `resolveAsset()` 解析全局组件</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>isDef</span><span>(</span>Ctor <span>=</span> <span>resolveAsset</span><span>(</span>context<span>.</span>$options<span>,</span> <span>'components'</span><span>,</span> tag<span>)</span><span>)</span><span>)</span> <span>{</span>
      <span>// component</span>
      vnode <span>=</span> <span>createComponent</span><span>(</span>Ctor<span>,</span> data<span>,</span> context<span>,</span> children<span>,</span> tag<span>)</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>// unknown or unlisted namespaced elements</span>
      <span>// check at runtime because it may get assigned a namespace when its</span>
      <span>// parent normalizes children</span>
      vnode <span>=</span> <span>new</span> <span>VNode</span><span>(</span>
        tag<span>,</span> data<span>,</span> children<span>,</span>
        <span>undefined</span><span>,</span> <span>undefined</span><span>,</span> context
      <span>)</span>
    <span>}</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>// direct component options / constructor</span>
    vnode <span>=</span> <span>createComponent</span><span>(</span>tag<span>,</span> data<span>,</span> context<span>,</span> children<span>)</span>
  <span>}</span>
  <span>// ...</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br></div></div><ul>
<li>
<p><code>resolveAsset</code> 解析全局组件，定义在 <code>src/core/utils/options.js</code> 中：</p>
<div><pre><code>
<span>/**
 * Resolve an asset.
 * This function is used because child instances need access
 * to assets defined in its ancestor chain.
 * 先通过 const assets = options[type] 拿到 assets，然后再尝试拿 assets[id]，
 * 这里有个顺序，先直接使用 id 拿，如果不存在，则把 id 变成驼峰的形式再拿，
 * 如果仍然不存在则在驼峰的基础上把首字母再变成大写的形式再拿，如果仍然拿不到则报错。
 * 这样说明了我们在使用 Vue.component(id, definition) 全局注册组件的时候，
 * id 可以是连字符、驼峰或首字母大写的形式
 */</span>
<span>export</span> <span>function</span> <span>resolveAsset</span> <span>(</span>
  <span>options<span>:</span> Object<span>,</span>
  type<span>:</span> string<span>,</span>
  id<span>:</span> string<span>,</span>
  warnMissing<span>?</span><span>:</span> boolean</span>
<span>)</span><span>:</span> any <span>{</span>
  <span>/* istanbul ignore if */</span>
  <span>if</span> <span>(</span><span>typeof</span> id <span>!==</span> <span>'string'</span><span>)</span> <span>{</span>
    <span>return</span>
  <span>}</span>
  <span>/*分别用id本身、驼峰以及大写开头驼峰寻找是否存在，存在则返回，不存在则打印*/</span>
  <span>const</span> assets <span>=</span> options<span>[</span>type<span>]</span>
  <span>// check local registration variations first</span>
  <span>if</span> <span>(</span><span>hasOwn</span><span>(</span>assets<span>,</span> id<span>)</span><span>)</span> <span>return</span> assets<span>[</span>id<span>]</span>
  <span>/*转化为驼峰命名*/</span>
  <span>const</span> camelizedId <span>=</span> <span>camelize</span><span>(</span>id<span>)</span>
  <span>if</span> <span>(</span><span>hasOwn</span><span>(</span>assets<span>,</span> camelizedId<span>)</span><span>)</span> <span>return</span> assets<span>[</span>camelizedId<span>]</span>
  <span>/*驼峰首字母大写*/</span>
  <span>const</span> PascalCaseId <span>=</span> <span>capitalize</span><span>(</span>camelizedId<span>)</span>
  <span>if</span> <span>(</span><span>hasOwn</span><span>(</span>assets<span>,</span> PascalCaseId<span>)</span><span>)</span> <span>return</span> assets<span>[</span>PascalCaseId<span>]</span>
  <span>// fallback to prototype chain</span>
  <span>//在原型链中查找</span>
  <span>const</span> res <span>=</span> assets<span>[</span>id<span>]</span> <span>||</span> assets<span>[</span>camelizedId<span>]</span> <span>||</span> assets<span>[</span>PascalCaseId<span>]</span>
  <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> warnMissing <span>&amp;&amp;</span> <span>!</span>res<span>)</span> <span>{</span>
    <span>warn</span><span>(</span>
      <span>'Failed to resolve '</span> <span>+</span> type<span>.</span><span>slice</span><span>(</span><span>0</span><span>,</span> <span>-</span><span>1</span><span>)</span> <span>+</span> <span>': '</span> <span>+</span> id<span>,</span>
      options
    <span>)</span>
  <span>}</span>
  <span>return</span> res
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br></div></div><ul>
<li>通过 <code>resolveAsset</code> 解析结果作为 <code>createComponent</code> 参数创建组件。</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>局部注册</strong></p>
<ul>
<li>
<p>原理： 局部注册组件扩展到当前组件的 <code>vm.$options.components</code> 下，只有当前组件可以访问。</p>
</li>
<li>
<p>注册方法：在一个组件内部使用 <code>components</code> 选项做组件的局部注册，例如：</p>
<div><pre><code><span>import</span> HelloWorld <span>from</span> <span>'./components/HelloWorld'</span>

<span>export</span> <span>default</span> <span>{</span>
  components<span>:</span> <span>{</span>
    HelloWorld
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
<li>
<p>过程：在组件的 Vue 的实例化阶段有一个合并 <code>option</code> 的逻辑，就把 <code>components</code> 合并到当前组件的 <code>vm.$options.components</code> 上，这样我们就可以在 <code>resolveAsset</code> 的时候拿到这个组件的构造函数，并作为 <code>createComponent</code> 的钩子的参数。</p>
</li>
</ul>
<h3 id="组件生命周期"> 组件生命周期</h3>
<ul>
<li>
<p><strong>流程分析</strong></p>
<ul>
<li>整体执行顺序:</li>
</ul>
<p><img src="./../flow-graph/父子组件生命周期.png" alt="" /></p>
<ul>
<li>加载渲染过程: 父 beforeCreate -&gt; 父 created -&gt; 父 beforeMount -&gt; 子 beforeCreate -&gt; 子 created -&gt; 子 beforeMount -&gt; 子 mounted -&gt; 父 mounted</li>
<li>组件更新过程:  父 beforeUpdate -&gt; 子 beforeUpdate -&gt; 子 updated -&gt; 父 updated</li>
<li>销毁过程: 父 beforeDestroy -&gt; 子 beforeDestroy -&gt; 子 destroyed -&gt; 父 destroyed</li>
</ul>
</li>
<li>
<p>父组件监听到子组件的生命周期方法</p>
<ul>
<li>
<p>方法1： 在父组件引用子组件时通过 @hook 来监听</p>
<div><pre><code><span>// 父组件 Parent.vue</span>
<span>&lt;</span>Child @hook<span>:</span>mounted<span>=</span><span>"doSomething"</span> <span>></span><span>&lt;</span><span>/</span>Child<span>></span>
<span>doSomething</span><span>(</span><span>)</span> <span>{</span>
   console<span>.</span><span>log</span><span>(</span><span>'父组件监听到 mounted 钩子函数 ...'</span><span>)</span><span>;</span>
<span>}</span><span>,</span>
			
<span>//子组件  Child.vue</span>
<span>mounted</span><span>(</span><span>)</span><span>{</span>
   console<span>.</span><span>log</span><span>(</span><span>'子组件触发 mounted 钩子函数 ...'</span><span>)</span><span>;</span>
<span>}</span><span>,</span>  

<span>//输出顺序</span>
<span>// 子组件触发 mounted 钩子函数 ...</span>
<span>// 父组件监听到 mounted 钩子函数 ...  </span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br></div></div></li>
<li>
<p>方法2: 父组件监听到子组件生命周期事件，子组件生命周期内emit事件</p>
<div><pre><code><span>// 父组件Parent.vue</span>
<span>&lt;</span>Child @mounted<span>=</span><span>"doSomething"</span><span>/</span><span>></span>
			
<span>// 子组件Child.vue</span>
<span>mounted</span><span>(</span><span>)</span> <span>{</span>
  <span>this</span><span>.</span><span>$emit</span><span>(</span><span>"mounted"</span><span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
</ul>
</li>
<li>
<p>常见问题:</p>
<ul>
<li>
<blockquote>
<p>在哪个生命周期内调用异步请求？</p>
</blockquote>
<p>在created、beforeMount、mounted中data 已经创建，可以将服务端端返回的数据进行赋值</p>
</li>
<li>
<blockquote>
<p>在什么阶段才能访问操作DOM？</p>
</blockquote>
<div><pre><code>	在钩子函数 mounted 被调用前，Vue 已经将编译好的模板挂载到页面上，所以在 mounted 中可以访问操作 DOM
</code></pre>
</div></li>
<li>
<blockquote>
<p>为什么beforeMount先父组件后子组件执行而mount先子组件父后组件执行？</p>
</blockquote>
<p>在组件挂载过程中调用mountComponent()，mountComponent中立即调用breforeMount钩子，后调用mount钩子；</p>
<p>在组件的 VNode patch 到 DOM 后，会执行 invokeInsertHook 函数，把 insertedVnodeQueue（在执行createElm函数中如果发现子组件包含insert方法，则将子组件添加到insertedVnodeQueue中；在createComponent中所有子组件path完毕以后执行initComponent,在initComponent中把子组件添加到insertedVnodeQueue） 里保存的钩子函数依次执行一遍，该函数会执行 insert 这个钩子函数，每个子组件都是在这个钩子函数中执行 mounted 钩子函数，insertedVnodeQueue 的添加顺序是先子后父，所以对于同步渲染的子组件而言，mounted 钩子函数的执行顺序也是先子后父</p>
</li>
</ul>
</li>
</ul>
<h3 id="组件分类"> 组件分类</h3>
<ul>
<li>
<p>按组件定义分类：</p>
<ul>
<li>内置的组件： <code>keep-alive</code>、<code>component</code>、<code>transition</code>、<code>transition-group</code> 等；</li>
<li>用户自定义组件：在使用前必须注册。</li>
</ul>
</li>
<li>
<p>按逻辑分类</p>
<ul>
<li>异步组件</li>
<li>递归组件<img src="./../images/zujian-felei.png" alt="" /></li>
</ul>
</li>
<li>
<p>按功能分类</p>
<ul>
<li>视图组件</li>
<li>逻辑组件</li>
</ul>
</li>
</ul>
<h4 id="异步组件"> <a href="https://cn.vuejs.org/v2/guide/components-dynamic-async.html" target="_blank" rel="noopener noreferrer">异步组件</a></h4>
<ul>
<li>
<p>异步组件注册方式</p>
<ul>
<li>
<p>全局注册</p>
<ul>
<li>
<p>使用工厂函数组件创建</p>
<div><pre><code>Vue<span>.</span><span>component</span><span>(</span><span>'async-webpack-example'</span><span>,</span> <span>function</span> <span>(</span><span>resolve</span><span>)</span> <span>{</span>
  <span>// 这个特殊的 `require` 语法将会告诉 webpack</span>
  <span>// 自动将你的构建代码切割成多个包，这些包</span>
  <span>// 会通过 Ajax 请求加载</span>
  <span>require</span><span>(</span><span>[</span><span>'./my-async-component'</span><span>]</span><span>,</span> resolve<span>)</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div></li>
<li>
<p>使用 Promise 创建</p>
<div><pre><code>Vue<span>.</span><span>component</span><span>(</span>
  <span>'async-webpack-example'</span><span>,</span>
  <span>// 这个动态导入会返回一个 `Promise` 对象。</span>
  <span>(</span><span>)</span> <span>=></span> <span>import</span><span>(</span><span>'./my-async-component'</span><span>)</span>
<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div></li>
<li>
<p>高级异步组件</p>
<div><pre><code><span>const</span> <span>AsyncComponent</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>(</span><span>{</span>
  <span>// 需要加载的组件 (应该是一个 `Promise` 对象)</span>
  component<span>:</span> <span>import</span><span>(</span><span>'./MyComponent.vue'</span><span>)</span><span>,</span>
  <span>// 异步组件加载时使用的组件</span>
  loading<span>:</span> LoadingComponent<span>,</span>
  <span>// 加载失败时使用的组件</span>
  error<span>:</span> ErrorComponent<span>,</span>
  <span>// 展示加载时组件的延时时间。默认值是 200 (毫秒)</span>
  delay<span>:</span> <span>200</span><span>,</span>
  <span>// 如果提供了超时时间且组件加载也超时了，</span>
  <span>// 则使用加载失败时使用的组件。默认值是：`Infinity`</span>
  timeout<span>:</span> <span>3000</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br></div></div></li>
</ul>
</li>
<li>
<p>局部注册</p>
<ul>
<li>
<p>使用 <code>Promise</code> 创建</p>
<div><pre><code><span>new</span> <span>Vue</span><span>(</span><span>{</span>
  <span>// ...</span>
  components<span>:</span> <span>{</span>
    <span>'my-component'</span><span>:</span> <span>(</span><span>)</span> <span>=></span> <span>import</span><span>(</span><span>'./my-async-component'</span><span>)</span>
  <span>}</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>源码分析</strong></p>
<ul>
<li>
<p>原理：异步组件实现的本质是 2 次渲染</p>
<ul>
<li>第一次渲染生成一个注释节点；</li>
<li>当异步获取组件成功后，再通过 <code>forceRender</code> 强制重新渲染，这样就能正确渲染出我们异步加载的组件了；</li>
</ul>
</li>
<li>
<p>0 delay 的高级异步组件第一次直接渲染成 loading 组件，只有一次渲染。</p>
</li>
<li>
<p>异步组件创建时机：</p>
<ul>
<li>
<p>在组件创建一节中提到，组件创建时两个过程：</p>
<ul>
<li>第一次：render 方法调用的 _createElement() 方法中的 createComponent() 的实现，此处主要创建组件的占位符节点；</li>
<li>第二次：path 方法调用 createElem() 方法中： 调用 createComponent() 的实现，此处递归创建组件组件实例，通过 new Vue() 创建组件；</li>
</ul>
</li>
<li>
<p>异步组件创建时机是在第一次 render 方法中调用 createComponents() 中 <code>resolveAsyncComponent</code> 时创建：</p>
<div><pre><code><span>export</span> <span>function</span> <span>createComponent</span> <span>(</span>
  <span>Ctor<span>:</span> Class<span>&lt;</span>Component<span>></span> <span>|</span> Function <span>|</span> Object <span>|</span> <span>void</span><span>,</span>
  data<span>:</span> <span>?</span>VNodeData<span>,</span>
  context<span>:</span> Component<span>,</span>
  children<span>:</span> <span>?</span>Array<span>&lt;</span>VNode<span>></span><span>,</span>
  tag<span>?</span><span>:</span> string</span>
<span>)</span><span>:</span> VNode <span>|</span> Array<span>&lt;</span>VNode<span>></span> <span>|</span> <span>void</span> <span>{</span>
  <span>if</span> <span>(</span><span>isUndef</span><span>(</span>Ctor<span>)</span><span>)</span> <span>{</span>
    <span>return</span>
  <span>}</span>

  <span>const</span> baseCtor <span>=</span> context<span>.</span>$options<span>.</span>_base

  <span>// plain options object: turn it into a constructor</span>
  <span>if</span> <span>(</span><span>isObject</span><span>(</span>Ctor<span>)</span><span>)</span> <span>{</span> <span>// 异步组件中传入的 Ctor 为函数，因此此处逻辑跳过</span>
    Ctor <span>=</span> baseCtor<span>.</span><span>extend</span><span>(</span>Ctor<span>)</span>
  <span>}</span>
  
  <span>// ...</span>

  <span>// async component</span>
  <span>let</span> asyncFactory
  <span>if</span> <span>(</span><span>isUndef</span><span>(</span>Ctor<span>.</span>cid<span>)</span><span>)</span> <span>{</span> <span>//异步组件未定义 cid</span>
    asyncFactory <span>=</span> Ctor
    Ctor <span>=</span> <span>resolveAsyncComponent</span><span>(</span>asyncFactory<span>,</span> baseCtor<span>,</span> context<span>)</span> <span>//解析异步组件</span>
    <span>if</span> <span>(</span>Ctor <span>===</span> <span>undefined</span><span>)</span> <span>{</span>
      <span>// return a placeholder node for async component, which is rendered</span>
      <span>// as a comment node but preserves all the raw information for the node.</span>
      <span>// the information will be used for async server-rendering and hydration.</span>
      <span>// 创建异步组件的占位符 VNode </span>
      <span>return</span> <span>createAsyncPlaceholder</span><span>(</span>
        asyncFactory<span>,</span>
        data<span>,</span>
        context<span>,</span>
        children<span>,</span>
        tag
      <span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br></div></div><ul>
<li>通过 <code>resolveAsyncComponent</code> 函数解析后返回 VNode 占位符节点。</li>
</ul>
</li>
<li>
<p><code>resolveAsyncComponent</code> 函数：</p>
<ul>
<li>加载异步组件，处理三种类型的异步组件，当组件加载完成后根据加载状态进行处理。</li>
<li>加载成功时调用 <code>forceRender</code> 强制重新渲染组件。函数定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/https://github.com/vuejs/vue/blob/v2.3.0/" target="_blank" rel="noopener noreferrer">src/core/vdom/helpers/resolve-async-component.js</a></li>
</ul>
<div><pre><code><span>/**
 * resolveAsyncComponent处理 3 种异步组件的创建方式：
 * 1.工厂函数方式；
 * 2.Promise 创建组件；
 * 3.高级异步组件
 * @param {*} factory 
 * @param {*} baseCtor 
 * @param {*} context 
 */</span>
<span>export</span> <span>function</span> <span>resolveAsyncComponent</span><span>(</span>
  <span>factory<span>:</span> Function<span>,</span>
  baseCtor<span>:</span> Class<span>&lt;</span>Component<span>></span><span>,</span>
  context<span>:</span> Component</span>
<span>)</span><span>:</span> Class<span>&lt;</span>Component<span>></span> <span>|</span> <span>void</span> <span>{</span>
  <span>/*配置错误选项工厂返回出错组件*/</span>
  <span>if</span> <span>(</span><span>isTrue</span><span>(</span>factory<span>.</span>error<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>factory<span>.</span>errorComp<span>)</span><span>)</span> <span>{</span>
    <span>return</span> factory<span>.</span>errorComp
  <span>}</span>

  <span>/*resoved时候返回resolved组件*/</span>
  <span>if</span> <span>(</span><span>isDef</span><span>(</span>factory<span>.</span>resolved<span>)</span><span>)</span> <span>{</span>
    <span>return</span> factory<span>.</span>resolved
  <span>}</span>

  <span>/*如果异步组件加载中并未返回，这时候会走到这个逻辑，执行 loading 组件*/</span>
  <span>if</span> <span>(</span><span>isTrue</span><span>(</span>factory<span>.</span>loading<span>)</span> <span>&amp;&amp;</span> <span>isDef</span><span>(</span>factory<span>.</span>loadingComp<span>)</span><span>)</span> <span>{</span>
    <span>return</span> factory<span>.</span>loadingComp <span>//返回 factory.loadingComp，渲染 loading 组件</span>
  <span>}</span>
  <span>/**多个地方同时初始化一个异步组件，那么它的实际加载应该只有一次 */</span>
  <span>if</span> <span>(</span><span>isDef</span><span>(</span>factory<span>.</span>contexts<span>)</span><span>)</span> <span>{</span>
    <span>// already pending</span>
    factory<span>.</span>contexts<span>.</span><span>push</span><span>(</span>context<span>)</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>const</span> contexts <span>=</span> factory<span>.</span>contexts <span>=</span> <span>[</span>context<span>]</span>
    <span>let</span> sync <span>=</span> <span>true</span>
	<span>// forceRender 函数定义</span>
    <span>const</span> <span>forceRender</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span> 
      <span>//遍历调用每一异步组件的forceUpadate方法</span>
      <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>,</span> l <span>=</span> contexts<span>.</span>length<span>;</span> i <span>&lt;</span> l<span>;</span> i<span>++</span><span>)</span> <span>{</span>
        <span>/**
         * 强制触发组件渲染，之所以这么做是因为 Vue 通常是数据驱动视图重新渲染，
         * 但是在整个异步组件加载过程中是没有数据发生变化的，所以通过执行 $forceUpdate 可以强制组件重新渲染一次
         */</span>
        contexts<span>[</span>i<span>]</span><span>.</span><span>$forceUpdate</span><span>(</span><span>)</span> <span>//定义在 src/core/instance/lifecycle.js 中</span>
      <span>}</span>
    <span>}</span>
    <span>//resolve函数定义： 异步组件加载成功时候执行，利用闭包和一个标志位保证了它包装的函数只会执行一次</span>
    <span>const</span> resolve <span>=</span> <span>once</span><span>(</span><span>(</span><span>res<span>:</span> Object <span>|</span> Class<span>&lt;</span>Component<span>></span></span><span>)</span> <span>=></span> <span>{</span>
      <span>// cache resolved</span>
      <span>//把加载结果缓存到 factory.resolved</span>
      factory<span>.</span>resolved <span>=</span> <span>ensureCtor</span><span>(</span>res<span>,</span> baseCtor<span>)</span>
      <span>// invoke callbacks only if this is not a synchronous resolve</span>
      <span>// (async resolves are shimmed as synchronous during SSR)</span>
      <span>if</span> <span>(</span><span>!</span>sync<span>)</span> <span>{</span> <span>//异步加载组件成功后sync设置为false</span>
        <span>forceRender</span><span>(</span><span>)</span>
      <span>}</span>
    <span>}</span><span>)</span>
    <span>//reject 方法方法定义：once 包装 reject 方法，异步组件加载失败时执行，加载失败和加载超时时执行</span>
    <span>const</span> reject <span>=</span> <span>once</span><span>(</span><span>reason</span> <span>=></span> <span>{</span>
      process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> <span>warn</span><span>(</span>
        <span><span>`</span><span>Failed to resolve async component: </span><span><span>${</span><span>String</span><span>(</span>factory<span>)</span><span>}</span></span><span>`</span></span> <span>+</span>
        <span>(</span>reason <span>?</span> <span><span>`</span><span>\nReason: </span><span><span>${</span>reason<span>}</span></span><span>`</span></span> <span>:</span> <span>''</span><span>)</span>
      <span>)</span>
      <span>if</span> <span>(</span><span>isDef</span><span>(</span>factory<span>.</span>errorComp<span>)</span><span>)</span> <span>{</span><span>//定义了加载错误组件，则强制渲染</span>
        factory<span>.</span>error <span>=</span> <span>true</span> <span>//设置错误标记为true，显示加载错误组件</span>
        <span>forceRender</span><span>(</span><span>)</span>
      <span>}</span>
    <span>}</span><span>)</span>
    <span>/*
    开始加载组件: 执行传入组件的工厂函数，同时把 resolve 和 reject 函数作为参数传入，组件的工厂函数通常会先发送请求去加载我们的异步组件的 JS 文件，拿到组件定义的对象 res 后，执行 resolve(res) 逻辑
     */</span>
    <span>const</span> res <span>=</span> <span>factory</span><span>(</span>resolve<span>,</span> reject<span>)</span>

    <span>if</span> <span>(</span><span>isObject</span><span>(</span>res<span>)</span><span>)</span> <span>{</span>
      <span>/*使用Promise 异步组件,执行完 res = factory(resolve, reject)，返回的值就是 import('./my-async-component') 的返回值，它是一个 Promise 对象
      */</span>
      <span>if</span> <span>(</span><span>typeof</span> res<span>.</span>then <span>===</span> <span>'function'</span><span>)</span> <span>{</span>
        <span>// () => Promise</span>
        <span>if</span> <span>(</span><span>isUndef</span><span>(</span>factory<span>.</span>resolved<span>)</span><span>)</span> <span>{</span>
          res<span>.</span><span>then</span><span>(</span>resolve<span>,</span> reject<span>)</span>
        <span>}</span><span>////高级异步组件执行逻辑</span>
      <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>isDef</span><span>(</span>res<span>.</span>component<span>)</span> <span>&amp;&amp;</span> <span>typeof</span> res<span>.</span>component<span>.</span>then <span>===</span> <span>'function'</span><span>)</span> <span>{</span> 
        res<span>.</span>component<span>.</span><span>then</span><span>(</span>resolve<span>,</span> reject<span>)</span>
        <span>/**判断 res.error 是否定义了 error 组件，如果有的话则赋值给 factory.errorComp */</span>
        <span>if</span> <span>(</span><span>isDef</span><span>(</span>res<span>.</span>error<span>)</span><span>)</span> <span>{</span>
          factory<span>.</span>errorComp <span>=</span> <span>ensureCtor</span><span>(</span>res<span>.</span>error<span>,</span> baseCtor<span>)</span>
        <span>}</span>

        <span>if</span> <span>(</span><span>isDef</span><span>(</span>res<span>.</span>loading<span>)</span><span>)</span> <span>{</span> <span>//判断组件中是否定义Loading组件</span>
          factory<span>.</span>loadingComp <span>=</span> <span>ensureCtor</span><span>(</span>res<span>.</span>loading<span>,</span> baseCtor<span>)</span>
          <span>if</span> <span>(</span>res<span>.</span>delay <span>===</span> <span>0</span><span>)</span> <span>{</span> <span>//没有定义延时</span>
            factory<span>.</span>loading <span>=</span> <span>true</span>
          <span>}</span> <span>else</span> <span>{</span>
            <span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span> <span>//延时执行</span>
              <span>if</span> <span>(</span><span>isUndef</span><span>(</span>factory<span>.</span>resolved<span>)</span> <span>&amp;&amp;</span> <span>isUndef</span><span>(</span>factory<span>.</span>error<span>)</span><span>)</span> <span>{</span>
                factory<span>.</span>loading <span>=</span> <span>true</span>
                <span>forceRender</span><span>(</span><span>)</span>
              <span>}</span>
            <span>}</span><span>,</span> res<span>.</span>delay <span>||</span> <span>200</span><span>)</span>
          <span>}</span>
        <span>}</span>

        <span>if</span> <span>(</span><span>isDef</span><span>(</span>res<span>.</span>timeout<span>)</span><span>)</span> <span>{</span> <span>//如果定义了timeout，并在指定时间内没有加载成功，执行reject</span>
          <span>setTimeout</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
            <span>reject</span><span>(</span>
              process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span>
                <span>?</span> <span><span>`</span><span>timeout (</span><span><span>${</span>res<span>.</span>timeout<span>}</span></span><span>ms)</span><span>`</span></span>
                <span>:</span> <span>null</span>
            <span>)</span>
          <span>}</span><span>,</span> res<span>.</span>timeout<span>)</span>
        <span>}</span>
      <span>}</span>
    <span>}</span>

    sync <span>=</span> <span>false</span>
    <span>// return in case resolved synchronously</span>
    <span>return</span> factory<span>.</span>loading
      <span>?</span> factory<span>.</span>loadingComp
      <span>:</span> factory<span>.</span>resolved
  <span>}</span>
<span>}</span>

<span>/**
 * 函数目的是为了保证能找到异步组件 JS 定义的组件对象，并且如果它是一个普通对象，
 * 则调用 Vue.extend 把它转换成一个组件的构造函数
 * @param {*} comp 
 * @param {*} base 
 */</span>
<span>function</span> <span>ensureCtor</span><span>(</span><span>comp<span>,</span> base</span><span>)</span> <span>{</span>
  <span>return</span> <span>isObject</span><span>(</span>comp<span>)</span>
    <span>?</span> base<span>.</span><span>extend</span><span>(</span>comp<span>)</span>
    <span>:</span> comp
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br><span>93</span><br><span>94</span><br><span>95</span><br><span>96</span><br><span>97</span><br><span>98</span><br><span>99</span><br><span>100</span><br><span>101</span><br><span>102</span><br><span>103</span><br><span>104</span><br><span>105</span><br><span>106</span><br><span>107</span><br><span>108</span><br><span>109</span><br><span>110</span><br><span>111</span><br><span>112</span><br><span>113</span><br><span>114</span><br><span>115</span><br><span>116</span><br><span>117</span><br><span>118</span><br><span>119</span><br><span>120</span><br><span>121</span><br><span>122</span><br><span>123</span><br><span>124</span><br><span>125</span><br><span>126</span><br><span>127</span><br><span>128</span><br><span>129</span><br><span>130</span><br><span>131</span><br><span>132</span><br><span>133</span><br></div></div></li>
</ul>
</li>
</ul>
<h3 id="组件通信"> 组件通信</h3>
<ul>
<li>
<p>组件通信方式分类</p>
<p><img src="./../images/zujian-zujiantongxin.png" alt="" /></p>
<p><img src="./../images/zujian-zujiantongxin2.png" alt="zujian-zujiantongxin2" /></p>
<p><img src="./../images/zujian-zujiantongxin3.png" alt="zujian-zujiantongxin" /></p>
</li>
<li>
<p><strong>$attrs 和 $listeners 通信:</strong></p>
<ul>
<li>
<p><a href="https://cn.vuejs.org/v2/api/#vm-attrs" target="_blank" rel="noopener noreferrer">vm.$attrs</a>: 继承所有的父组件属性（除了prop传递的属性、class 和 style ）， 一般用在子组件的子元素上，如第一个例子的 <code>&lt;input v-bind=&quot;$attrs&quot;/&gt;</code></p>
</li>
<li>
<p><a href="https://cn.vuejs.org/v2/api/#vm-listeners" target="_blank" rel="noopener noreferrer">vm.$listeners</a>: 属性，它是一个对象，里面包含了作用在这个组件上的所有监听器，你就可以配合<code>v-on=&quot;$listeners&quot;</code>将所有的事件监听器指向这个组件的某个特定的子元素。（相当于子组件继承父组件的事件）</p>
</li>
<li>
<p><a href="https://gitee.com/jokerxw/myNotes/blob/master/vue/vue%E6%A0%B8%E5%BF%83/%E7%BB%84%E4%BB%B6%E9%80%9A%E4%BF%A1/$attrs%E5%92%8C$listeners.vue" target="_blank" rel="noopener noreferrer">查看示例</a></p>
</li>
</ul>
</li>
<li>
<p><strong>props/$emit通信：</strong></p>
<ul>
<li>父组件通过props传递数据给子组件事件并通过监听子组件事件获取子组件数据</li>
<li>子组件通过props接受父组件数据并通过$emit()触发事件并传入数据给父组件</li>
<li><a href="https://gitee.com/jokerxw/myNotes/blob/master/vue/vue%E6%A0%B8%E5%BF%83/%E7%BB%84%E4%BB%B6%E9%80%9A%E4%BF%A1/props%E5%92%8C$emit.vue" target="_blank" rel="noopener noreferrer">查看示例</a></li>
</ul>
</li>
<li>
<p><strong>$emit/$on通信：</strong></p>
<ul>
<li>这种方法通过一个空的Vue实例作为中央事件总线（事件中心），用它来触发事件和监听事件,巧妙而轻量地实现了任何组件间的通信</li>
<li>包括父子、兄弟、跨级</li>
<li>具体实现方式：
<ul>
<li>1.创建一个空的vue实例：var Event=new Vue()</li>
<li>2.在发送数据实例中使用：Event.$emit(事件名,数据) 发送数据</li>
<li>3.接受数据实例中使用：Event.$on(事件名,data =&gt; {}) 接数据</li>
</ul>
</li>
<li><a href="https://gitee.com/jokerxw/myNotes/blob/master/vue/vue%E6%A0%B8%E5%BF%83/%E7%BB%84%E4%BB%B6%E9%80%9A%E4%BF%A1/$emit%E5%92%8C$on.vue" target="_blank" rel="noopener noreferrer">查看示例</a></li>
</ul>
</li>
<li>
<p><strong><a href="https://cn.vuejs.org/v2/api/#provide-inject" target="_blank" rel="noopener noreferrer">provide / inject</a>通信：</strong></p>
<ul>
<li><a href="https://gitee.com/jokerxw/myNotes/blob/master/vue/vue%E6%A0%B8%E5%BF%83/%E7%BB%84%E4%BB%B6%E9%80%9A%E4%BF%A1/provide%E5%92%8Cinject.js" target="_blank" rel="noopener noreferrer">查看示例</a></li>
</ul>
</li>
<li>
<p><strong>EventBus：</strong></p>
<ul>
<li><code>EventBus</code> 又称为事件总线。创建一个事件中心，相当于中转站，可以用它来传递事件和接收事 件。</li>
<li>本质是通过创建一个空的 Vue 实例来作为消息传递的 对象，通信的组件引入这个实例，通信的组件通过在这个实例上监听和触发事件，来实现消息的传递。</li>
<li>如果业务逻辑复杂，很多组件之间需要同时处理一些公共的数据，这个时候采用上面这一些方法 可能不利于项目的维护，因此才需要更完善的 Vuex 作为状态管理中心，将通知的概念上升到共享状态层次。</li>
<li>参考资料：
<ul>
<li><a href="https://zhuanlan.zhihu.com/p/72777951" target="_blank" rel="noopener noreferrer">Vue事件总线（EventBus）使用详细介绍</a></li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="扩展组件方式"> 扩展组件方式</h3>
<p>扩展 Vue 组件最流行的方法有以下四种：</p>
<ul>
<li><a href="https://vue3js.cn/docs/zh/guide/composition-api-introduction.html#%E4%BB%80%E4%B9%88%E6%98%AF%E7%BB%84%E5%90%88%E5%BC%8F-api" target="_blank" rel="noopener noreferrer">Composition API</a></li>
<li><a href="https://vuejsdevelopers.com/2017/06/11/vue-js-extending-components/#mixins" target="_blank" rel="noopener noreferrer">mixin</a></li>
<li><a href="https://vuejsdevelopers.com/2017/06/11/vue-js-extending-components/#higher-order-components" target="_blank" rel="noopener noreferrer">高阶组件（HOC）</a></li>
<li><a href="https://vuejsdevelopers.com/2017/06/11/vue-js-extending-components/#renderless-components" target="_blank" rel="noopener noreferrer">无渲染组件</a></li>
</ul>
<h3 id="高阶组件"> 高阶组件</h3>
<ul>
<li>
<p>概念：一个函数接受一个组件为参数，返回一个包装后的组件。在 Vue 的世界里，组件是一个对象，所以高阶组件就是一个函数接受一个对象，返回一个新的包装好的对象。Vue 中，高阶组件就是 <code>f(object) -&gt; 新的object</code>。</p>
</li>
<li>
<p>规则：</p>
<ul>
<li>高阶组件(<code>HOC</code>)应该是无副作用的纯函数，且不应该修改原组件；
<ul>
<li>高阶组件纯函数，它接收一个组件作为参数并返回了一个新的组件，在新组件的 <code>render</code> 函数中仅仅渲染了被包装的组件，并没有侵入式的修改它。</li>
</ul>
</li>
<li>高阶组件(<code>HOC</code>)不关心你传递的数据(<code>props</code>)是什么，并且被包装组件不关心数据来源；</li>
<li>高阶组件(<code>HOC</code>)接收到的 <code>props</code> 应该透传给被包装组件：高阶组件完全可以添加、删除、修改 <code>props</code>，但是除此之外，要将其余 <code>props</code> 透传，否则在层级较深的嵌套关系中(<code>这是高阶组件的常见问题</code>)将造成 <code>props</code> 阻塞。</li>
</ul>
</li>
<li>
<p>实现：</p>
<ul>
<li>
<p>高阶组件：用于封装组件的公共逻辑组件，给所有被封装的组件添加初始化 console 打印方法。</p>
<p><img src="./../images/高阶组件.png" alt="" /></p>
</li>
<li>
<p>测试：</p>
<ul>
<li>
<p>编写被包裹的组件：被封装的组件 <code>BaseCompoent</code>。</p>
<p><img src="./../images/高阶组件-测试组件.png" alt="" /></p>
</li>
<li>
<p>使用方式： 使用高阶组件包裹基础组件 <code>BaseComponent</code> 生成一个新的组。</p>
<p><img src="./../images/高阶组件-测试包裹组件.png" alt="" /></p>
</li>
<li>
<p>测试结果：<img src="./../images/高阶组件-结果.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>参考资料</strong></p>
<p><a href="http://hcysun.me/2018/01/05/%E6%8E%A2%E7%B4%A2Vue%E9%AB%98%E9%98%B6%E7%BB%84%E4%BB%B6/" target="_blank" rel="noopener noreferrer">探索Vue高阶组件</a></p>
<h3 id="内置组件"> 内置组件</h3>
<h4 id="keep-alive"> <a href="https://cn.vuejs.org/v2/api/#keep-alive" target="_blank" rel="noopener noreferrer"><code>&lt;keep-alive&gt;</code></a></h4>
<p><img src="./../images/keep-alive.png" alt="" /></p>
<ul>
<li>
<p>keep-alive 组件： 是一个抽象组件，它自身不会渲染一个 DOM 元素，也不会出现在组件的父组件链中</p>
</li>
<li>
<p>作用：</p>
<ul>
<li>主要用于保留组件状态或避免重新渲染，组件实例能够被在它们第一次被创建的时候缓存下来</li>
<li>当组件在 <code>&lt;keep-alive&gt;</code> 内被切换，它的 <code>activated</code> 和 <code>deactivated</code> 这两个生命周期钩子函数将会被对应执行</li>
</ul>
</li>
<li>
<p>缓存对象：</p>
<ul>
<li>缓存子组件的 <code>vnode</code> 对象</li>
</ul>
</li>
<li>
<p>Props：</p>
<ul>
<li><code>include</code> - 字符串或正则表达式。只有名称匹配的组件会被缓存。</li>
<li><code>exclude</code> - 字符串或正则表达式。任何名称匹配的组件都不会被缓存。</li>
<li><code>max</code> - 数字。定义缓存组件上限，超出上限使用<a href="https://baike.baidu.com/item/LRU" target="_blank" rel="noopener noreferrer">LRU的策略</a>置换缓存数据</li>
</ul>
</li>
<li>
<p>应用场景:</p>
<ul>
<li>用户在某个列表页面选择筛选条件过滤出一份数据列表，由列表页面进入数据详情页面，再返回该列表页面，我们希望：列表页面可以保留用户的筛选（或选中）状态</li>
<li>避免组件反复创建和渲染，有效提升系统性能</li>
</ul>
</li>
<li>
<p>注意：</p>
<ul>
<li><code>&lt;keep-alive&gt;</code>只处理第一个子元素，所以一般和它搭配使用的有 component 动态组件或者是 router-view</li>
<li>被 <code>&lt;keep-alive&gt;</code> 包裹的组件在有缓存的时候就不会在执行组件的 <code>created</code>、<code>mounted</code> 等钩子函数</li>
<li>activated 和 deactivated 钩子将会在<code>&lt;keep-alive&gt;</code>树内的所有嵌套组件中触发</li>
</ul>
</li>
<li>
<p>用法：</p>
<ul>
<li>
<p><a href="https://cn.vuejs.org/v2/guide/components-dynamic-async.html#%E5%9C%A8%E5%8A%A8%E6%80%81%E7%BB%84%E4%BB%B6%E4%B8%8A%E4%BD%BF%E7%94%A8-keep-alive" target="_blank" rel="noopener noreferrer">动态组件中使用</a></p>
<ul>
<li>
<div><pre><code><span><span><span>&lt;</span>keep-alive</span> <span>:include</span><span><span>=</span><span>"</span>whiteList<span>"</span></span> <span>:exclude</span><span><span>=</span><span>"</span>blackList<span>"</span></span> <span>:max</span><span><span>=</span><span>"</span>amount<span>"</span></span><span>></span></span>
  <span><span><span>&lt;</span>component</span> <span>:is</span><span><span>=</span><span>"</span>currentComponent<span>"</span></span><span>></span></span><span><span><span>&lt;/</span>component</span><span>></span></span>
<span><span><span>&lt;/</span>keep-alive</span><span>></span></span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
</ul>
</li>
<li>
<p>在vue-router中的应用</p>
<ul>
<li>在需要缓存的组件中的路由配置中 meta 自定义字段 keepAlive 配置为 true</li>
<li><img src="./../images/vue_keepalive.png" alt="" /></li>
<li>在路由组件中添加 keep-alive 组件，通过 this.$route.meta.keepAlive 获取当前路由中 meta 的 keepAlive 字段,通过该字段判断页面是否需要缓存</li>
<li><img src="./../images/vue_keepalive2.png" alt="" /></li>
</ul>
</li>
</ul>
</li>
<li>
<p>源码分析</p>
<ul>
<li>
<p><a href="https://github.com/vuejs/vue/blob/dev/src/core/components/keep-alive.js" target="_blank" rel="noopener noreferrer">源码</a></p>
</li>
<li>
<p><a href="http://caibaojian.com/vue-analysis/extend/keep-alive.html#%E5%86%85%E7%BD%AE%E7%BB%84%E4%BB%B6" target="_blank" rel="noopener noreferrer">源码分析</a></p>
</li>
<li>
<div><pre><code><span>export</span> <span>default</span> <span>{</span>
  name<span>:</span> 'keep<span>-</span>alive<span>,</span>
  abstract<span>:</span> <span>true</span><span>,</span>

  props<span>:</span> <span>{</span>
    include<span>:</span> patternTypes<span>,</span>
    exclude<span>:</span> patternTypes<span>,</span>
    max<span>:</span> <span>[</span>String<span>,</span> Number<span>]</span>
  <span>}</span><span>,</span>

  <span>created</span> <span>(</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>cache <span>=</span> Object<span>.</span><span>create</span><span>(</span><span>null</span><span>)</span>
    <span>this</span><span>.</span>keys <span>=</span> <span>[</span><span>]</span>
  <span>}</span><span>,</span>

  <span>destroyed</span> <span>(</span><span>)</span> <span>{</span>
    <span>for</span> <span>(</span><span>const</span> key <span>in</span> <span>this</span><span>.</span>cache<span>)</span> <span>{</span>
      <span>pruneCacheEntry</span><span>(</span><span>this</span><span>.</span>cache<span>,</span> key<span>,</span> <span>this</span><span>.</span>keys<span>)</span> <span>//删除缓存</span>
    <span>}</span>
  <span>}</span><span>,</span>

  <span>mounted</span> <span>(</span><span>)</span> <span>{</span> 
 <span>//观测 include 和 exclude 变化，当有新的组件名称加入这两个数组，则通过 pruneCache() 遍历所有缓存组件，查找缓存组件中是否包含新增加到这两个数组的组件，如果存在则在缓存剔除该组件</span>
    <span>this</span><span>.</span><span>$watch</span><span>(</span><span>'include'</span><span>,</span> <span>val</span> <span>=></span> <span>{</span>
     <span>//新增组件不在 include 中，从缓存中剔除新增组件</span>
      <span>pruneCache</span><span>(</span><span>this</span><span>,</span> <span>name</span> <span>=></span> <span>matches</span><span>(</span>val<span>,</span> name<span>)</span><span>)</span>
    <span>}</span><span>)</span>
    <span>this</span><span>.</span><span>$watch</span><span>(</span><span>'exclude'</span><span>,</span> <span>val</span> <span>=></span> <span>{</span>
    <span>// 新增组件在 exclude 中，从缓存中剔除新增组件</span>
      <span>pruneCache</span><span>(</span><span>this</span><span>,</span> <span>name</span> <span>=></span> <span>!</span><span>matches</span><span>(</span>val<span>,</span> name<span>)</span><span>)</span>
    <span>}</span><span>)</span>
  <span>}</span><span>,</span>

  <span>render</span> <span>(</span><span>)</span> <span>{</span>
  <span>//注意：&lt;keep-alive> 是用在其一个直属的子组件被开关的情形，所以&lt;keep-alive> 只处理第一个子元素</span>
 <span>//一般和它搭配使用的有 component 动态组件或者是 router-view</span>
 <span>//由于我们也是在 &lt;keep-alive> 标签内部写 DOM，所以可以先获取到它的默认插槽，然后再获取到它的第一个子节点</span>
    <span>const</span> slot <span>=</span> <span>this</span><span>.</span>$slots<span>.</span>default <span>// 获取组件内默认插槽</span>
    <span>const</span> vnode<span>:</span> VNode <span>=</span> <span>getFirstComponentChild</span><span>(</span>slot<span>)</span> <span>//获取插槽第一个子节点</span>
    <span>const</span> componentOptions<span>:</span> <span>?</span>VNodeComponentOptions <span>=</span> vnode <span>&amp;&amp;</span> vnode<span>.</span>componentOptions
    <span>if</span> <span>(</span>componentOptions<span>)</span> <span>{</span>
      <span>// check pattern</span>
      <span>const</span> name<span>:</span> <span>?</span>string <span>=</span> <span>getComponentName</span><span>(</span>componentOptions<span>)</span>
      <span>const</span> <span>{</span> include<span>,</span> exclude <span>}</span> <span>=</span> <span>this</span>
      <span>//组件名如果配置了include 且不存在于 include 或者是配置了 exclude 且匹配，那么就直接返回这个组件的 vnode</span>
      <span>if</span> <span>(</span>
        <span>// not included</span>
        <span>(</span>include <span>&amp;&amp;</span> <span>(</span><span>!</span>name <span>||</span> <span>!</span><span>matches</span><span>(</span>include<span>,</span> name<span>)</span><span>)</span><span>)</span> <span>||</span>
        <span>// excluded</span>
        <span>(</span>exclude <span>&amp;&amp;</span> name <span>&amp;&amp;</span> <span>matches</span><span>(</span>exclude<span>,</span> name<span>)</span><span>)</span>
      <span>)</span> <span>{</span>
        <span>return</span> vnode
      <span>}</span>

      <span>const</span> <span>{</span> cache<span>,</span> keys <span>}</span> <span>=</span> <span>this</span>
      <span>const</span> key<span>:</span> <span>?</span>string <span>=</span> vnode<span>.</span>key <span>==</span> <span>null</span>
        <span>// same constructor may get registered as different local components</span>
        <span>// so cid alone is not enough (#3269)</span>
        <span>?</span> componentOptions<span>.</span>Ctor<span>.</span>cid <span>+</span> <span>(</span>componentOptions<span>.</span>tag <span>?</span> <span><span>`</span><span>::</span><span><span>${</span>componentOptions<span>.</span>tag<span>}</span></span><span>`</span></span> <span>:</span> <span>''</span><span>)</span>
        <span>:</span> vnode<span>.</span>key
      <span>// 以下缓存使用 LRU 最近最久未使用算法</span>
      <span>if</span> <span>(</span>cache<span>[</span>key<span>]</span><span>)</span> <span>{</span> <span>//命中缓存</span>
        vnode<span>.</span>componentInstance <span>=</span> cache<span>[</span>key<span>]</span><span>.</span>componentInstance
        <span>// make current key freshest</span>
        <span>//重新调整了 key 的顺序放在了最后一个</span>
        <span>remove</span><span>(</span>keys<span>,</span> key<span>)</span> 
        keys<span>.</span><span>push</span><span>(</span>key<span>)</span>
      <span>}</span> <span>else</span> <span>{</span> <span>// 未命中缓存直接加入缓存</span>
        cache<span>[</span>key<span>]</span> <span>=</span> vnode
        keys<span>.</span><span>push</span><span>(</span>key<span>)</span>
        <span>// prune oldest entry</span>
        <span>//如果配置了 max 并且缓存的长度超过了 this.max，还要从缓存中删除第一个</span>
        <span>if</span> <span>(</span><span>this</span><span>.</span>max <span>&amp;&amp;</span> keys<span>.</span>length <span>></span> <span>parseInt</span><span>(</span><span>this</span><span>.</span>max<span>)</span><span>)</span> <span>{</span>
          <span>pruneCacheEntry</span><span>(</span>cache<span>,</span> keys<span>[</span><span>0</span><span>]</span><span>,</span> keys<span>,</span> <span>this</span><span>.</span>_vnode<span>)</span> <span>//删除缓存</span>
        <span>}</span>
      <span>}</span>

      vnode<span>.</span>data<span>.</span>keepAlive <span>=</span> <span>true</span>
    <span>}</span>
		<span>return</span> vnode <span>||</span> <span>(</span>slot <span>&amp;&amp;</span> slot<span>[</span><span>0</span><span>]</span><span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br></div></div></li>
</ul>
</li>
<li>
<p>缓存流程分析</p>
<p><img src="./../flow-graph/keep-alive渲染流程.png" alt="" /></p>
</li>
<li>
<p>相关问题</p>
<ul>
<li>缓存中缓存组件的什么数据？
<ul>
<li>缓存中主要通过数组缓存子组件Vnode</li>
</ul>
</li>
</ul>
</li>
</ul>
<h4 id="transition"> <a href="https://cn.vuejs.org/v2/api/#transition" target="_blank" rel="noopener noreferrer"><code>&lt;transition&gt;</code></a></h4>
<ul>
<li>
<p><code>&lt;transition&gt;</code> 元素作为<strong>单个</strong>元素/组件的过渡效果。</p>
</li>
<li>
<p><code>&lt;transition&gt;</code> 是抽象组件：只会把过渡效果应用到其包裹的内容上，而不会额外渲染 DOM 元素，也不会出现在可被检查的组件层级中。</p>
<ul>
<li>不能在抽象组件中使用 <code>v-if</code> 和 <code>v-for</code> 指令：因为抽象组件不会渲染成 DOM 元素。</li>
<li>内置抽象组件包括：<code>&lt;keep-alive&gt;</code>、<code>&lt;transition&gt;</code></li>
</ul>
</li>
<li>
<p><code>&lt;transition&gt;</code> 组件是只能包裹一个子节点。</p>
</li>
<li>
<p>Vue 的过渡实现分为以下几个步骤：</p>
<ul>
<li>自动嗅探目标元素是否应用了 CSS 过渡或动画，如果是，在恰当的时机添加/删除 CSS 类名。</li>
<li>如果过渡组件提供了 JavaScript 钩子函数，这些钩子函数将在恰当的时机被调用。</li>
<li>如果没有找到 JavaScript 钩子并且也没有检测到 CSS 过渡/动画，DOM 操作 (插入/删除) 在下一帧中立即执行。</li>
</ul>
<p>所以真正执行动画的是我们写的 CSS 或者是 JavaScript 钩子函数，而 Vue 的 <code>&lt;transition&gt;</code> 只是帮我们很好地管理了这些 CSS 的添加/删除，以及钩子函数的执行时机。</p>
</li>
<li>
<p><code>&lt;transition&gt;</code> 的定义在 <code>src/platforms/web/runtime/component/transtion.js</code> 中，之所以在这里定义，是因为 <code>&lt;transition&gt;</code> 组件是 web 平台独有的：</p>
<div><pre><code><span>export</span> <span>default</span> <span>{</span>
  name<span>:</span> <span>'transition'</span><span>,</span>
  props<span>:</span> transitionProps<span>,</span>
  abstract<span>:</span> <span>true</span><span>,</span>

  <span>render</span> <span>(</span><span>h<span>:</span> Function</span><span>)</span> <span>{</span>
     <span>//从默认插槽中获取 &lt;transition> 包裹的子节点</span>
    <span>let</span> children<span>:</span> any <span>=</span> <span>this</span><span>.</span>$slots<span>.</span>default
    <span>if</span> <span>(</span><span>!</span>children<span>)</span> <span>{</span><span>//子节点的长度，如果长度为 0，则直接返回</span>
      <span>return</span>
    <span>}</span>

    <span>// filter out text nodes (possible whitespaces)</span>
    children <span>=</span> children<span>.</span><span>filter</span><span>(</span><span>(</span><span>c<span>:</span> VNode</span><span>)</span> <span>=></span> c<span>.</span>tag <span>||</span> <span>isAsyncPlaceholder</span><span>(</span>c<span>)</span><span>)</span>
    <span>/* istanbul ignore if */</span>
    <span>if</span> <span>(</span><span>!</span>children<span>.</span>length<span>)</span> <span>{</span><span>//子节点的长度，如果长度为 0，则直接返回</span>
      <span>return</span>
    <span>}</span>

    <span>// warn multiple elements</span>
    <span>//如果大于 1，也会在开发环境报警告，因为 &lt;transition> 组件是只能包裹一个子节点的。</span>
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> children<span>.</span>length <span>></span> <span>1</span><span>)</span> <span>{</span>
      <span>warn</span><span>(</span>
        <span>'&lt;transition> can only be used on a single element. Use '</span> <span>+</span>
        <span>'&lt;transition-group> for lists.'</span><span>,</span>
        <span>this</span><span>.</span>$parent
      <span>)</span>
    <span>}</span>
	
    <span>const</span> mode<span>:</span> string <span>=</span> <span>this</span><span>.</span>mode

    <span>// warn invalid mode</span>
    <span>//过渡组件的对 mode 的支持只有 2 种，in-out 或者是 out-in</span>
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span>
      mode <span>&amp;&amp;</span> mode <span>!==</span> <span>'in-out'</span> <span>&amp;&amp;</span> mode <span>!==</span> <span>'out-in'</span>
    <span>)</span> <span>{</span>
      <span>warn</span><span>(</span>
        <span>'invalid &lt;transition> mode: '</span> <span>+</span> mode<span>,</span>
        <span>this</span><span>.</span>$parent
      <span>)</span>
    <span>}</span>
	<span>//rawChild 就是第一个子节点 vnode</span>
    <span>const</span> rawChild<span>:</span> VNode <span>=</span> children<span>[</span><span>0</span><span>]</span>

    <span>// if this is a component root node and the component's</span>
    <span>// parent container node also has transition, skip.</span>
    <span>//当前 &lt;transition> 如果是组件根节点并且外面包裹该组件的容器也是 &lt;transition> 的时候要跳过</span>
    <span>if</span> <span>(</span><span>hasParentTransition</span><span>(</span><span>this</span><span>.</span>$vnode<span>)</span><span>)</span> <span>{</span>
      <span>return</span> rawChild
    <span>}</span>

    <span>// apply transition data to child</span>
    <span>// use getRealChild() to ignore abstract components e.g. keep-alive</span>
    <span>//getRealChild 的目的是获取组件的非抽象子节点，因为 &lt;transition> 很可能会包裹一个 keep-alive</span>
    <span>//getRealChild 会递归找到第一个非抽象组件的 vnode 并返回  </span>
    <span>const</span> child<span>:</span> <span>?</span>VNode <span>=</span> <span>getRealChild</span><span>(</span>rawChild<span>)</span>
    <span>/* istanbul ignore if */</span>
    <span>if</span> <span>(</span><span>!</span>child<span>)</span> <span>{</span>
      <span>return</span> rawChild
    <span>}</span>

    <span>if</span> <span>(</span><span>this</span><span>.</span>_leaving<span>)</span> <span>{</span>
      <span>return</span> <span>placeholder</span><span>(</span>h<span>,</span> rawChild<span>)</span>
    <span>}</span>

    <span>// ensure a key that is unique to the vnode type and to this transition</span>
    <span>// component instance. This key will be used to remove pending leaving nodes</span>
    <span>// during entering.</span>
    <span>const</span> id<span>:</span> string <span>=</span> <span><span>`</span><span>__transition-</span><span><span>${</span><span>this</span><span>.</span>_uid<span>}</span></span><span>-</span><span>`</span></span>
    child<span>.</span>key <span>=</span> child<span>.</span>key <span>==</span> <span>null</span>
      <span>?</span> child<span>.</span>isComment
        <span>?</span> id <span>+</span> <span>'comment'</span>
        <span>:</span> id <span>+</span> child<span>.</span>tag
      <span>:</span> <span>isPrimitive</span><span>(</span>child<span>.</span>key<span>)</span>
        <span>?</span> <span>(</span><span>String</span><span>(</span>child<span>.</span>key<span>)</span><span>.</span><span>indexOf</span><span>(</span>id<span>)</span> <span>===</span> <span>0</span> <span>?</span> child<span>.</span>key <span>:</span> id <span>+</span> child<span>.</span>key<span>)</span>
        <span>:</span> child<span>.</span>key
	<span>//从当前通过 extractTransitionData 组件实例上提取出过渡所需要的数据</span>
    <span>const</span> data<span>:</span> Object <span>=</span> <span>(</span>child<span>.</span>data <span>||</span> <span>(</span>child<span>.</span>data <span>=</span> <span>{</span><span>}</span><span>)</span><span>)</span><span>.</span>transition <span>=</span> <span>extractTransitionData</span><span>(</span><span>this</span><span>)</span>
    <span>const</span> oldRawChild<span>:</span> VNode <span>=</span> <span>this</span><span>.</span>_vnode
    <span>const</span> oldChild<span>:</span> VNode <span>=</span> <span>getRealChild</span><span>(</span>oldRawChild<span>)</span>

    <span>// mark v-show</span>
    <span>// so that the transition module can hand over the control to the directive</span>
    <span>if</span> <span>(</span>child<span>.</span>data<span>.</span>directives <span>&amp;&amp;</span> child<span>.</span>data<span>.</span>directives<span>.</span><span>some</span><span>(</span><span>d</span> <span>=></span> d<span>.</span>name <span>===</span> <span>'show'</span><span>)</span><span>)</span> <span>{</span>
      child<span>.</span>data<span>.</span>show <span>=</span> <span>true</span>
    <span>}</span>

    <span>if</span> <span>(</span>
      oldChild <span>&amp;&amp;</span>
      oldChild<span>.</span>data <span>&amp;&amp;</span>
      <span>!</span><span>isSameChild</span><span>(</span>child<span>,</span> oldChild<span>)</span> <span>&amp;&amp;</span>
      <span>!</span><span>isAsyncPlaceholder</span><span>(</span>oldChild<span>)</span> <span>&amp;&amp;</span>
      <span>// #6687 component root is a comment node</span>
      <span>!</span><span>(</span>oldChild<span>.</span>componentInstance <span>&amp;&amp;</span> oldChild<span>.</span>componentInstance<span>.</span>_vnode<span>.</span>isComment<span>)</span>
    <span>)</span> <span>{</span>
      <span>// replace old child transition data with fresh one</span>
      <span>// important for dynamic transitions!</span>
      <span>const</span> oldData<span>:</span> Object <span>=</span> oldChild<span>.</span>data<span>.</span>transition <span>=</span> <span>extend</span><span>(</span><span>{</span><span>}</span><span>,</span> data<span>)</span>
      <span>// handle transition mode</span>
      <span>if</span> <span>(</span>mode <span>===</span> <span>'out-in'</span><span>)</span> <span>{</span>
        <span>// return placeholder node and queue update when leave finishes</span>
        <span>this</span><span>.</span>_leaving <span>=</span> <span>true</span>
        <span>mergeVNodeHook</span><span>(</span>oldData<span>,</span> <span>'afterLeave'</span><span>,</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
          <span>this</span><span>.</span>_leaving <span>=</span> <span>false</span>
          <span>this</span><span>.</span><span>$forceUpdate</span><span>(</span><span>)</span>
        <span>}</span><span>)</span>
        <span>return</span> <span>placeholder</span><span>(</span>h<span>,</span> rawChild<span>)</span>
      <span>}</span> <span>else</span> <span>if</span> <span>(</span>mode <span>===</span> <span>'in-out'</span><span>)</span> <span>{</span>
        <span>if</span> <span>(</span><span>isAsyncPlaceholder</span><span>(</span>child<span>)</span><span>)</span> <span>{</span>
          <span>return</span> oldRawChild
        <span>}</span>
        <span>let</span> delayedLeave
        <span>const</span> <span>performLeave</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span> <span>delayedLeave</span><span>(</span><span>)</span> <span>}</span>
        <span>mergeVNodeHook</span><span>(</span>data<span>,</span> <span>'afterEnter'</span><span>,</span> performLeave<span>)</span>
        <span>mergeVNodeHook</span><span>(</span>data<span>,</span> <span>'enterCancelled'</span><span>,</span> performLeave<span>)</span>
        <span>mergeVNodeHook</span><span>(</span>oldData<span>,</span> <span>'delayLeave'</span><span>,</span> <span>leave</span> <span>=></span> <span>{</span> delayedLeave <span>=</span> leave <span>}</span><span>)</span>
      <span>}</span>
    <span>}</span>

    <span>return</span> rawChild
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br><span>93</span><br><span>94</span><br><span>95</span><br><span>96</span><br><span>97</span><br><span>98</span><br><span>99</span><br><span>100</span><br><span>101</span><br><span>102</span><br><span>103</span><br><span>104</span><br><span>105</span><br><span>106</span><br><span>107</span><br><span>108</span><br><span>109</span><br><span>110</span><br><span>111</span><br><span>112</span><br><span>113</span><br><span>114</span><br><span>115</span><br><span>116</span><br><span>117</span><br><span>118</span><br><span>119</span><br><span>120</span><br><span>121</span><br><span>122</span><br></div></div></li>
</ul>
<hr>
<h2 id="全局api"> 全局API</h2>
<h3 id="vue-extend-options"> <a href="https://cn.vuejs.org/v2/api/#Vue-extend" target="_blank" rel="noopener noreferrer">Vue.extend(options)</a></h3>
<ul>
<li>
<p>原理：</p>
<ul>
<li>使用基础 Vue 构造器，创建一个“子类”。</li>
<li>其实就是扩展了基础构造器，形成了一个可复用的有指定选项功能的子构造器</li>
<li>使用<code>原型继承</code>的方式把一个纯对象转换一个继承于 Vue 的构造器 Sub 并返回，然后对 Sub 这个对象本身扩展了一些属性，如扩展 options、添加全局 API 等；并且对配置中的 props 和 computed 做了初始化工作；</li>
<li>最后对于这个 Sub 构造函数做了缓存，避免多次执行 Vue.extend 的时候对同一个子组件重复构造</li>
</ul>
</li>
<li>
<p><code>Vue.extend</code> 函数的定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/global-api/extend.js" target="_blank" rel="noopener noreferrer">src/core/global-api/extend.js</a> 中。</p>
<div><pre><code>  <span>/*
  参数是一个包含组件option的对象。  https://cn.vuejs.org/v2/api/#Vue-extend-options
  */</span>
  Vue<span>.</span><span>extend</span> <span>=</span> <span>function</span> <span>(</span><span>extendOptions<span>:</span> Object</span><span>)</span><span>:</span> Function <span>{</span>
    extendOptions <span>=</span> extendOptions <span>||</span> <span>{</span><span>}</span> <span>//new Vue()时传入的组件对象</span>
    <span>/*父类的构造*/</span>
    <span>const</span> Super <span>=</span> <span>this</span> <span>//this指向Vue</span>
    <span>/*父类的cid，cid构造器唯一标识*/</span>
    <span>const</span> SuperId <span>=</span> Super<span>.</span>cid
    <span>/*cachedCtors用于缓存组件对象
    缓存组件对象目的：当该组件对象被多个组件引用时，该组件构造器逻辑只执行一次，下次再引用该组件时直接返回缓存中的构造器
    */</span>
    <span>const</span> cachedCtors <span>=</span> extendOptions<span>.</span>_Ctor <span>||</span> <span>(</span>extendOptions<span>.</span>_Ctor <span>=</span> <span>{</span><span>}</span><span>)</span>
    <span>/*如果构造函数中已经存在了该组件cid，则代表已经该组件通过extend进行实例化了，直接返回*/</span>
    <span>if</span> <span>(</span>cachedCtors<span>[</span>SuperId<span>]</span><span>)</span> <span>{</span>
      <span>return</span> cachedCtors<span>[</span>SuperId<span>]</span>
    <span>}</span>
    <span>//获取组件名称</span>
    <span>const</span> name <span>=</span> extendOptions<span>.</span>name <span>||</span> Super<span>.</span>options<span>.</span>name
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span> <span>//非生产环境下进行组件名称校验</span>
      <span>/*name只能包含字母与连字符*/</span>
      <span>if</span> <span>(</span><span>!</span><span><span>/</span><span>^[a-zA-Z][\w-]*$</span><span>/</span></span><span>.</span><span>test</span><span>(</span>name<span>)</span><span>)</span> <span>{</span>
        <span>warn</span><span>(</span>
          <span>'Invalid component name: "'</span> <span>+</span> name <span>+</span> <span>'". Component names '</span> <span>+</span>
          <span>'can only contain alphanumeric characters and the hyphen, '</span> <span>+</span>
          <span>'and must start with a letter.'</span>
        <span>)</span>
      <span>}</span>
    <span>}</span>

    <span>/*
      Sub构造函数其实就一个_init方法，这跟Vue的构造方法是一致的，在_init中处理各种数据初始化、生命周期等。
      因为Sub作为一个Vue的扩展构造器，所以基础的功能还是需要保持一致，跟Vue构造器一样在构造函数中初始化_init。
    */</span>
    <span>const</span> <span>Sub</span> <span>=</span> <span>function</span> <span>VueComponent</span><span>(</span><span>options</span><span>)</span> <span>{</span>
      <span>this</span><span>.</span><span>_init</span><span>(</span>options<span>)</span>
    <span>}</span>
    <span>/*继承父类，使用原型式继承，使子类构原型指向父类实例对象*/</span>
    <span>Sub</span><span>.</span>prototype <span>=</span> Object<span>.</span><span>create</span><span>(</span><span>Super</span><span>.</span>prototype<span>)</span>
    <span>/*构造函数*/</span>
    <span>Sub</span><span>.</span>prototype<span>.</span>constructor <span>=</span> Sub
    <span>/*创建一个新的cid*/</span>
    Sub<span>.</span>cid <span>=</span> cid<span>++</span>
    <span>/*将父组件的option与子组件的合并到一起(Vue有一个cid为0的基类，即Vue本身，会将一些默认初始化的option合并)*/</span>
    Sub<span>.</span>options <span>=</span> <span>mergeOptions</span><span>(</span>
      Super<span>.</span>options<span>,</span>
      extendOptions
    <span>)</span>
    <span>/*es6语法，super为父类构造*/</span>
    Sub<span>[</span><span>'super'</span><span>]</span> <span>=</span> Super

    <span>// For props and computed properties, we define the proxy getters on</span>
    <span>// the Vue instances at extension time, on the extended prototype. This</span>
    <span>// avoids Object.defineProperty calls for each instance created.</span>
    <span>/*在扩展时，我们将计算属性以及props通过代理绑定在Vue实例上（也就是vm），这也避免了Object.defineProperty被每一个实例调用*/</span>
    <span>if</span> <span>(</span>Sub<span>.</span>options<span>.</span>props<span>)</span> <span>{</span>
      <span>/*初始化props，将option中的_props代理到vm上*/</span>
      <span>initProps</span><span>(</span>Sub<span>)</span>
    <span>}</span>
    <span>if</span> <span>(</span>Sub<span>.</span>options<span>.</span>computed<span>)</span> <span>{</span>
      <span>/*处理计算属性，给计算属性设置defineProperty并绑定在vm上*/</span>
      <span>initComputed</span><span>(</span>Sub<span>)</span>
    <span>}</span>

    <span>// allow further extension/mixin/plugin usage</span>
    <span>/*加入extend、mixin以及use方法，允许将来继续为该组件提供扩展、混合或者插件*/</span>
    Sub<span>.</span>extend <span>=</span> Super<span>.</span>extend
    Sub<span>.</span>mixin <span>=</span> Super<span>.</span>mixin
    Sub<span>.</span>use <span>=</span> Super<span>.</span>use

    <span>// create asset registers, so extended classes</span>
    <span>// can have their private assets too.</span>
    <span>/*使得Sub也会拥有父类的私有选项（directives、filters、components）*/</span>
    <span>ASSET_TYPES</span><span>.</span><span>forEach</span><span>(</span><span>function</span> <span>(</span><span>type</span><span>)</span> <span>{</span>
      Sub<span>[</span>type<span>]</span> <span>=</span> Super<span>[</span>type<span>]</span>
    <span>}</span><span>)</span>
    <span>// enable recursive self-lookup</span>
    <span>/*把组件自身也加入components中，为递归自身提供可能（递归组件也会查找components是否存在当前组件，也就是自身）*/</span>
    <span>if</span> <span>(</span>name<span>)</span> <span>{</span>
      Sub<span>.</span>options<span>.</span>components<span>[</span>name<span>]</span> <span>=</span> Sub
    <span>}</span>

    <span>// keep a reference to the super options at extension time.</span>
    <span>// later at instantiation we can check if Super's options have</span>
    <span>// been updated.</span>
    <span>/*保存一个父类的options，此后我们可以用来检测父类的options是否已经被更新*/</span>
    Sub<span>.</span>superOptions <span>=</span> Super<span>.</span>options
    <span>/*extendOptions存储起来*/</span>
    Sub<span>.</span>extendOptions <span>=</span> extendOptions
    <span>/*保存一份option，extend的作用是将Sub.options中的所有属性放入{}中*/</span>
    Sub<span>.</span>sealedOptions <span>=</span> <span>extend</span><span>(</span><span>{</span><span>}</span><span>,</span> Sub<span>.</span>options<span>)</span>

    <span>// cache constructor</span>
    <span>/*缓存构造函数（用cid），防止重复extend*/</span>
    cachedCtors<span>[</span>SuperId<span>]</span> <span>=</span> Sub
    <span>return</span> Sub
  <span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br><span>93</span><br><span>94</span><br><span>95</span><br><span>96</span><br><span>97</span><br></div></div></li>
</ul>
<h3 id="vue-nexttick"> <a href="https://cn.vuejs.org/v2/api/#Vue-nextTick" target="_blank" rel="noopener noreferrer">Vue.nextTick</a></h3>
<ul>
<li>
<p><a href="https://cn.vuejs.org/v2/guide/reactivity.html#%E5%BC%82%E6%AD%A5%E6%9B%B4%E6%96%B0%E9%98%9F%E5%88%97" target="_blank" rel="noopener noreferrer">异步更新队列</a>:</p>
<ul>
<li>Vue 在更新 DOM 时是<strong>异步</strong>执行的。只要侦听到数据变化，Vue 将开启一个队列，并缓冲在同一事件循环中发生的所有数据变更。</li>
<li>如果同一个 watcher 被多次触发，只会被推入到队列中一次。</li>
<li>这种在缓冲时去除重复数据对于避免不必要的计算和 DOM 操作是非常重要的。</li>
<li>然后，在下一个的事件循环“tick”中，Vue 刷新队列并执行实际 (已去重的) 工作。</li>
</ul>
</li>
<li>
<p>vue 在异步更新数据时使用 nextTick 进行异步更新：参考 异步更新 一节</p>
</li>
<li>
<p>实现原理：</p>
<ul>
<li>Vue 在内部对异步队列尝试使用原生的 <code>Promise.then</code>、<code>MutationObserver</code> 和 <code>setImmediate</code>，如果执行环境不支持，则会采用 <code>setTimeout(fn, 0)</code> 代替。</li>
</ul>
</li>
<li>
<p>应用：</p>
<ul>
<li>
<p>数据的变化到 DOM 的重新渲染是一个异步过程，发生在下一个 tick。这就是我们平时在开发的过程中，比如从服务端接口去获取数据的时候，数据做了修改，如果我们的某些方法去依赖了数据修改后的 DOM 变化，我们就必须在 <code>nextTick</code> 后执行。</p>
<div><pre><code><span>getData</span><span>(</span>res<span>)</span><span>.</span><span>then</span><span>(</span><span>(</span><span>)</span><span>=></span><span>{</span>
  <span>this</span><span>.</span>xxx <span>=</span> res<span>.</span>data
  <span>this</span><span>.</span><span>$nextTick</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
    <span>// 这里我们可以获取变化后的 DOM</span>
  <span>}</span><span>)</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br></div></div></li>
</ul>
</li>
<li>
<p>流程图<img src="./../images/nextTick.png" alt="" /></p>
</li>
<li>
<p>源码分析：</p>
<ul>
<li>
<p>在 Vue  源码 2.3.0 中，<code>nextTick</code> 定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/util/env.js" target="_blank" rel="noopener noreferrer">src/core/util/env.js</a></p>
<div><pre><code><span>/**
 * Defer a task to execute it asynchronously.
 */</span>
 <span>/*
    延迟一个任务使其异步执行，在下一个tick时执行，一个立即执行函数，返回一个function
    这个函数的作用是在task或者microtask中推入一个timerFunc，在当前调用栈执行完以后以此执行直到执行到timerFunc
    目的是延迟到当前调用栈执行完以后执行
*/</span>
<span>export</span> <span>const</span> nextTick <span>=</span> <span>(</span><span>function</span> <span>(</span><span>)</span> <span>{</span>
  <span>/*异步任务队列，存放异步执行的回调*/</span>
  <span>const</span> callbacks <span>=</span> <span>[</span><span>]</span>
  <span>/*一个标记位，如果已经有timerFunc被推送到任务队列中去则不需要重复推送*/</span>
  <span>let</span> pending <span>=</span> <span>false</span>
  <span>/*一个函数指针，指向函数将被推送到任务队列中，等到主线程任务执行完时，任务队列中的timerFunc被调用*/</span>
  <span>let</span> timerFunc

  <span>/*下一个tick时的回调*/</span>
  <span>function</span> <span>nextTickHandler</span> <span>(</span><span>)</span> <span>{</span>
    <span>/*一个标记位，标记等待状态（即函数已经被推入任务队列或者主线程，已经在等待当前栈执行完毕去执行），这样就不需要在push多个回调到callbacks时将timerFunc多次推入任务队列或者主线程*/</span>
    pending <span>=</span> <span>false</span>
    <span>/*执行所有callback*/</span>
    <span>const</span> copies <span>=</span> callbacks<span>.</span><span>slice</span><span>(</span><span>0</span><span>)</span>
    callbacks<span>.</span>length <span>=</span> <span>0</span>
    <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> copies<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
      copies<span>[</span>i<span>]</span><span>(</span><span>)</span>
    <span>}</span>
  <span>}</span>

  <span>/*
    这里解释一下，一共有Promise、MutationObserver以及setTimeout三种尝试得到timerFunc的方法。
    优先使用Promise，在Promise不存在的情况下使用MutationObserver，这两个方法的回调函数都会在microtask中执行，它们会比setTimeout更早执行，所以优先使用。
    如果上述两种方法都不支持的环境则会使用setTimeout，在task尾部推入这个函数，等待调用执行。
    为啥要用 microtask？
    根据 HTML Standard，在每个 task 运行完以后，UI 都会重渲染，那么在 microtask 中就完成数据更新，
    当前 task 结束就可以得到最新的 UI 了。反之如果新建一个 task 来做数据更新，那么渲染就会进行两次。
    参考：https://www.zhihu.com/question/55364497/answer/144215284
  */</span>
  <span>if</span> <span>(</span><span>typeof</span> Promise <span>!==</span> <span>'undefined'</span> <span>&amp;&amp;</span> <span>isNative</span><span>(</span>Promise<span>)</span><span>)</span> <span>{</span>
    <span>/*使用Promise*/</span>
    <span>var</span> p <span>=</span> Promise<span>.</span><span>resolve</span><span>(</span><span>)</span>
    <span>var</span> <span>logError</span> <span>=</span> <span>err</span> <span>=></span> <span>{</span> console<span>.</span><span>error</span><span>(</span>err<span>)</span> <span>}</span>
    <span>timerFunc</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
      p<span>.</span><span>then</span><span>(</span>nextTickHandler<span>)</span><span>.</span><span>catch</span><span>(</span>logError<span>)</span>
      <span>// in problematic UIWebViews, Promise.then doesn't completely break, but</span>
      <span>// it can get stuck in a weird state where callbacks are pushed into the</span>
      <span>// microtask queue but the queue isn't being flushed, until the browser</span>
      <span>// needs to do some other work, e.g. handle a timer. Therefore we can</span>
      <span>// "force" the microtask queue to be flushed by adding an empty timer.</span>
      <span>if</span> <span>(</span>isIOS<span>)</span> <span>setTimeout</span><span>(</span>noop<span>)</span>
    <span>}</span>
  <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>typeof</span> MutationObserver <span>!==</span> <span>'undefined'</span> <span>&amp;&amp;</span> <span>(</span>
    <span>isNative</span><span>(</span>MutationObserver<span>)</span> <span>||</span>
    <span>// PhantomJS and iOS 7.x</span>
    MutationObserver<span>.</span><span>toString</span><span>(</span><span>)</span> <span>===</span> <span>'[object MutationObserverConstructor]'</span>
  <span>)</span><span>)</span> <span>{</span>
    <span>// use MutationObserver where native Promise is not available,</span>
    <span>// e.g. PhantomJS IE11, iOS7, Android 4.4</span>
    <span>/*新建一个textNode的DOM对象，用MutationObserver绑定该DOM并指定回调函数，
    在DOM变化的时候则会触发回调,该回调会进入主线程（比任务队列优先执行），
    即textNode.data = String(counter)时便会加入该回调*/</span>
    <span>var</span> counter <span>=</span> <span>1</span>
    <span>var</span> observer <span>=</span> <span>new</span> <span>MutationObserver</span><span>(</span>nextTickHandler<span>)</span>
    <span>var</span> textNode <span>=</span> document<span>.</span><span>createTextNode</span><span>(</span><span>String</span><span>(</span>counter<span>)</span><span>)</span>
    observer<span>.</span><span>observe</span><span>(</span>textNode<span>,</span> <span>{</span>
      characterData<span>:</span> <span>true</span>
    <span>}</span><span>)</span>
    <span>timerFunc</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
      counter <span>=</span> <span>(</span>counter <span>+</span> <span>1</span><span>)</span> <span>%</span> <span>2</span>
      textNode<span>.</span>data <span>=</span> <span>String</span><span>(</span>counter<span>)</span>
    <span>}</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>// fallback to setTimeout</span>
    <span>/* istanbul ignore next */</span>
    <span>/*使用setTimeout将回调推入任务队列尾部*/</span>
    <span>timerFunc</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
      <span>setTimeout</span><span>(</span>nextTickHandler<span>,</span> <span>0</span><span>)</span>
    <span>}</span>
  <span>}</span>

  <span>/*
    推送到队列中下一个tick时执行
    cb 回调函数
    ctx 上下文
  */</span>
  <span>return</span> <span>function</span> <span>queueNextTick</span> <span>(</span><span>cb<span>?</span><span>:</span> Function<span>,</span> ctx<span>?</span><span>:</span> Object</span><span>)</span> <span>{</span>
    <span>let</span> _resolve
    <span>/*cb存到callbacks中*/</span>
    callbacks<span>.</span><span>push</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
      <span>if</span> <span>(</span>cb<span>)</span> <span>{</span>
        <span>try</span> <span>{</span>
          <span>cb</span><span>.</span><span>call</span><span>(</span>ctx<span>)</span>
        <span>}</span> <span>catch</span> <span>(</span>e<span>)</span> <span>{</span>
          <span>handleError</span><span>(</span>e<span>,</span> ctx<span>,</span> <span>'nextTick'</span><span>)</span>
        <span>}</span>
      <span>}</span> <span>else</span> <span>if</span> <span>(</span>_resolve<span>)</span> <span>{</span>
        <span>_resolve</span><span>(</span>ctx<span>)</span>
      <span>}</span>
    <span>}</span><span>)</span>
    <span>if</span> <span>(</span><span>!</span>pending<span>)</span> <span>{</span>
      pending <span>=</span> <span>true</span>
      <span>timerFunc</span><span>(</span><span>)</span>
    <span>}</span>
    <span>if</span> <span>(</span><span>!</span>cb <span>&amp;&amp;</span> <span>typeof</span> Promise <span>!==</span> <span>'undefined'</span><span>)</span> <span>{</span>
      <span>return</span> <span>new</span> <span>Promise</span><span>(</span><span>(</span><span>resolve<span>,</span> reject</span><span>)</span> <span>=></span> <span>{</span>
        _resolve <span>=</span> resolve
      <span>}</span><span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span><span>)</span><span>(</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br><span>93</span><br><span>94</span><br><span>95</span><br><span>96</span><br><span>97</span><br><span>98</span><br><span>99</span><br><span>100</span><br><span>101</span><br><span>102</span><br><span>103</span><br><span>104</span><br><span>105</span><br><span>106</span><br><span>107</span><br><span>108</span><br><span>109</span><br></div></div></li>
<li>
<p>在 Vue 源码 2.5+ 后，<code>nextTick</code> 的实现单独有一个 JS 文件来维护它。在 <a href="https://github.com/vuejs/vue/blob/2.6/src/core/util/next-tick.js" target="_blank" rel="noopener noreferrer">src/core/util/next-tick.js</a> 中：</p>
<ul>
<li><code>next-tick.js</code> 申明了 <code>microTimerFunc</code> 和 <code>macroTimerFunc</code> 2 个变量，它们分别对应的是 micro task 的函数和 macro task 的函数。对于 macro task 的实现，优先检测是否支持原生 <code>setImmediate</code>，这是一个高版本 IE 和 Edge 才支持的特性，不支持的话再去检测是否支持原生的 <code>MessageChannel</code>，如果也不支持的话就会降级为 <code>setTimeout 0</code>；而对于 micro task 的实现，则检测浏览器是否原生支持 Promise，不支持的话直接指向 macro task 的实现。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="vue-set"> <a href="https://cn.vuejs.org/v2/api/#Vue-set" target="_blank" rel="noopener noreferrer">Vue.set </a></h3>
<ul>
<li>
<p>为什么要引入 <code>Vue.set</code> ?</p>
<ul>
<li>
<p>解决数组和对象检测更新问题。</p>
</li>
<li>
<p>对于使用 <code>Object.defineProperty</code> 实现响应式的对象，当我们去给这个对象添加一个新的属性的时候，是不能够触发它的 setter 的，比如：</p>
<div><pre><code><span>var</span> vm <span>=</span> <span>new</span> <span>Vue</span><span>(</span><span>{</span>
  data<span>:</span><span>{</span>
    a<span>:</span><span>1</span>
  <span>}</span>
<span>}</span><span>)</span>
<span>// vm.b 是非响应的</span>
vm<span>.</span>b <span>=</span> <span>2</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div><p>但是添加对象新属性的场景我们在平时开发中会经常遇到，那么 Vue 为了解决这个问题，定义了一个全局 API <code>Vue.set</code> 方法</p>
</li>
</ul>
</li>
<li>
<p>原理:</p>
<ul>
<li>如果目标是数组,使用 vue 实现的变异方法 splice 实现响应式</li>
<li>如果目标是对象,判断属性存在,即为响应式,直接赋值</li>
<li>如果 target 本身就不是响应式,直接赋值并返回</li>
<li>如果属性不是响应式,则调用 defineReactive 方法进行响应式处理</li>
<li>最后手动的触发依赖通知（这就是为什么执行 <code>Vue.set</code> 的时候可以让添加新的属性到对象也可以检测到变化）</li>
</ul>
</li>
<li>
<p>源码分析：</p>
<ul>
<li>
<p>初始化set过程: 在 <a href="https://github.com/vuejs/vue" target="_blank" rel="noopener noreferrer">vue</a>/<a href="https://github.com/vuejs/vue/tree/dev/src" target="_blank" rel="noopener noreferrer">src</a>/<a href="https://github.com/vuejs/vue/tree/dev/src/core" target="_blank" rel="noopener noreferrer">core</a>/<a href="https://github.com/vuejs/vue/tree/dev/src/core/instance" target="_blank" rel="noopener noreferrer">instance</a>/<a href="https://github.com/vuejs/vue/blob/dev/src/core/instance/index.js" target="_blank" rel="noopener noreferrer">index.js</a> 中，在 new Vue() 前 stateMixin(Vue) 中  <code>Vue.prototype.$set = set</code>。</p>
</li>
<li>
<p>set 源码： 定义在 <a href="(https://github.com/vuejs/vue/blob/v2.3.0/src/core/observer/index.js)">vue/src/core/observer/index.js</a></p>
<div><pre><code><span>export</span> <span>function</span> <span>set</span> <span>(</span><span>target<span>:</span> Array<span>&lt;</span>any<span>></span> <span>|</span> Object<span>,</span> key<span>:</span> any<span>,</span> val<span>:</span> any</span><span>)</span><span>:</span> any <span>{</span>
  <span>// set用于设置Array/Object, 如果 set 函数的第一个参数是 undefined 或 null 或者是原始类型值，那么在非生产环境下会打   //印警告信息</span>
  <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span>
    <span>(</span><span>isUndef</span><span>(</span>target<span>)</span> <span>||</span> <span>isPrimitive</span><span>(</span>target<span>)</span><span>)</span>
  <span>)</span> <span>{</span>
    <span>warn</span><span>(</span><span><span>`</span><span>Cannot set reactive property on undefined, null, or primitive value: </span><span><span>${</span><span>(</span>target<span>:</span> any<span>)</span><span>}</span></span><span>`</span></span><span>)</span>
  <span>}</span>
  <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>target<span>)</span> <span>&amp;&amp;</span> <span>isValidArrayIndex</span><span>(</span>key<span>)</span><span>)</span> <span>{</span>
    <span>// 修改数组的长度, 避免 索引 > 数组长度 导致splcie()执行有误</span>
    target<span>.</span>length <span>=</span> Math<span>.</span><span>max</span><span>(</span>target<span>.</span>length<span>,</span> key<span>)</span>
    <span>// 利用数组的 splice 变异方法触发响应式</span>
    target<span>.</span><span>splice</span><span>(</span>key<span>,</span> <span>1</span><span>,</span> val<span>)</span>
    <span>return</span> val
  <span>}</span>
  <span>// target为对象, key在target或者target.prototype上</span>
  <span>// 同时必须不能在 Object.prototype 上 issue: https://github.com/vuejs/vue/issues/6845</span>
  <span>if</span> <span>(</span>key <span>in</span> target <span>&amp;&amp;</span> <span>!</span><span>(</span>key <span>in</span> <span>Object</span><span>.</span>prototype<span>)</span><span>)</span> <span>{</span>
    target<span>[</span>key<span>]</span> <span>=</span> val
    <span>return</span> val
  <span>}</span>
  <span>// 给target创建一个新的属性,获取Observer实例(data的__ob__属性保存数据对应的观察者对象,Observe类中定义_ob_)</span>
  <span>const</span> ob <span>=</span> <span>(</span>target<span>:</span> any<span>)</span><span>.</span>__ob__
  <span>// Vue 实例对象拥有 _isVue 属性,一个防止vm实例自身被观察的标志位</span>
  <span>// 也不允许Vue.set/$set 函数为根数据对象(vm.$data)添加属性</span>
  <span>//当vue已经被Observer了,并且是根数据对象, vmCount才大于零</span>
  <span>if</span> <span>(</span>target<span>.</span>_isVue <span>||</span> <span>(</span>ob <span>&amp;&amp;</span> ob<span>.</span>vmCount<span>)</span><span>)</span> <span>{</span>
    process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> <span>warn</span><span>(</span>
      <span>'Avoid adding reactive properties to a Vue instance or its root $data '</span> <span>+</span>
      <span>'at runtime - declare it upfront in the data option.'</span>
    <span>)</span>
    <span>return</span> val
  <span>}</span>
  <span>// target本身就不是响应式数据, 直接赋值</span>
  <span>if</span> <span>(</span><span>!</span>ob<span>)</span> <span>{</span>
    target<span>[</span>key<span>]</span> <span>=</span> val
    <span>return</span> val
  <span>}</span>
  <span>// 进行响应式处理</span>
  <span>defineReactive</span><span>(</span>ob<span>.</span>value<span>,</span> key<span>,</span> val<span>)</span>
  ob<span>.</span>dep<span>.</span><span>notify</span><span>(</span><span>)</span> <span>//手动的触发依赖通知</span>
  <span>return</span> val
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br></div></div></li>
</ul>
</li>
<li>
<p>应用：</p>
<ul>
<li>错误示例：</li>
<li>先给对象添加属性，后使用Vue.set定义响应式内容<img src="./../images/vue.set_err.png" alt="" />
<ul>
<li>错误原因: 直接给对象添加 replayList 属性时, 该属性不是响应式，并且不能触发视图更新，当后面使用 Vue.set 添加 replayList[0] 时，replayList[0] 式响应式,但 replayList 依然不是响应式对象,所以依然无法触发视图更新</li>
<li>更正：
<ul>
<li>方式1：给对象添加 replayList 响应式属性后再添加数据<img src="./../images/fe57f01feb56c361931bbc6825085f3.png" alt="" /></li>
<li>方式2：直接给对象添加响应式属性数据 <img src="./../images/1b3384d6a9799879571ed3dc66edb5f.png" alt="" /></li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="vue-use-plugin"> <a href="https://cn.vuejs.org/v2/api/#Vue-use" target="_blank" rel="noopener noreferrer">Vue.use( plugin )</a></h3>
<ul>
<li>
<p><strong>参数</strong>：</p>
<ul>
<li><code>{Object | Function} plugin</code></li>
</ul>
</li>
<li>
<p><strong>用法</strong>：</p>
<ul>
<li>
<p>安装 Vue.js 插件。</p>
<ul>
<li>如果插件是一个对象，必须提供 <code>install</code> 方法。</li>
<li>如果插件是一个函数，它会被作为 install 方法。install 方法调用时，<strong>会将 Vue 作为参数传入</strong>。
<ul>
<li>插件中传入 Vue 原因：作为 Vue 的插件对 <code>Vue</code> 对象是有依赖的，但又不能去单独去 <code>import Vue</code>，因为那样会增加包体积，所以就通过这种方式拿到 <code>Vue</code> 对象。</li>
</ul>
</li>
</ul>
</li>
<li>
<p>该方法需要在调用 <code>new Vue()</code> 之前被调用。</p>
</li>
<li>
<p>当 install 方法被同一个插件多次调用，插件将只会被安装一次。</p>
</li>
</ul>
</li>
<li>
<p>原理：</p>
<ul>
<li>维护了一个 <code>_installedPlugins</code> 缓存数组，它存储所有注册过的 <code>plugin</code>；</li>
<li>先在缓存查找  <code>plugin</code>，找到直接返回；</li>
<li>否则，判断 <code>plugin</code> 有没有定义 <code>install</code> 方法，如果有的话则调用该方法，并且该方法执行的第一个参数是 <code>Vue</code>；</li>
<li>最后把 <code>plugin</code> 存储到 <code>installedPlugins</code> 中；</li>
</ul>
</li>
<li>
<p>源码：定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/global-api/use.js" target="_blank" rel="noopener noreferrer">vue/src/core/global-api/use.js</a>中。</p>
<div><pre><code><span>/*初始化use*/</span>
<span>export</span> <span>function</span> <span>initUse</span> <span>(</span><span>Vue<span>:</span> GlobalAPI</span><span>)</span> <span>{</span>
  <span>/*https://cn.vuejs.org/v2/api/#Vue-use*/</span>
  Vue<span>.</span><span>use</span> <span>=</span> <span>function</span> <span>(</span><span>plugin<span>:</span> Function <span>|</span> Object</span><span>)</span> <span>{</span>
    <span>/* istanbul ignore if */</span>
    <span>/*标识位检测该插件是否已经被安装*/</span>
    <span>if</span> <span>(</span>plugin<span>.</span>installed<span>)</span> <span>{</span>
      <span>return</span>
    <span>}</span>
    <span>// additional parameters</span>
    <span>const</span> args <span>=</span> <span>toArray</span><span>(</span>arguments<span>,</span> <span>1</span><span>)</span>
    <span>/*a*/</span>
    args<span>.</span><span>unshift</span><span>(</span><span>this</span><span>)</span>
    <span>if</span> <span>(</span><span>typeof</span> plugin<span>.</span>install <span>===</span> <span>'function'</span><span>)</span> <span>{</span>
      <span>/*install执行插件安装*/</span>
      plugin<span>.</span><span>install</span><span>.</span><span>apply</span><span>(</span>plugin<span>,</span> args<span>)</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>typeof</span> plugin <span>===</span> <span>'function'</span><span>)</span> <span>{</span>
      <span>plugin</span><span>.</span><span>apply</span><span>(</span><span>null</span><span>,</span> args<span>)</span>
    <span>}</span>
    plugin<span>.</span>installed <span>=</span> <span>true</span>
    <span>return</span> <span>this</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br></div></div></li>
<li>
<p>调用时机：引入<code>import Vue from vue</code> -&gt; <code>initGlobalAPI(Vue)</code> -&gt; <code>initUse(Vue)</code></p>
</li>
<li>
<p><strong>参考</strong>：<a href="https://cn.vuejs.org/v2/guide/plugins.html" target="_blank" rel="noopener noreferrer">插件</a></p>
</li>
</ul>
<h3 id="vue-mixin-mixin"> <a href="https://cn.vuejs.org/v2/api/#Vue-mixin" target="_blank" rel="noopener noreferrer">Vue.mixin( mixin )</a></h3>
<ul>
<li>
<p><strong>参数</strong>：</p>
<ul>
<li><code>{Object} mixin</code></li>
</ul>
</li>
<li>
<p><strong>用法</strong>：</p>
<ul>
<li>全局注册一个混入，影响注册之后所有创建的每个 Vue 实例。插件作者可以使用混入，向组件注入自定义的行为。<strong>不推荐在应用代码中使用</strong>。</li>
</ul>
</li>
<li>
<p>原理：把要混入的对象通过 <code>mergeOption</code> 合并到 <code>Vue</code> 的 <code>options</code> 中，由于每个组件的构造函数都会在 <code>extend</code> 阶段合并 <code>Vue.options</code> 到自身的 <code>options</code> 中，所以也就相当于每个组件都定义了 <code>mixin</code> 定义的选项。</p>
</li>
<li>
<p>源码：定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/global-api/mixin.js" target="_blank" rel="noopener noreferrer">src/core/global-api/mixin.js</a></p>
<div><pre><code><span>/*初始化mixin*/</span>
<span>export</span> <span>function</span> <span>initMixin</span> <span>(</span><span>Vue<span>:</span> GlobalAPI</span><span>)</span> <span>{</span>
    <span>/*https://cn.vuejs.org/v2/api/#Vue-mixin*/</span>
  Vue<span>.</span><span>mixin</span> <span>=</span> <span>function</span> <span>(</span><span>mixin<span>:</span> Object</span><span>)</span> <span>{</span>
    <span>/*mergeOptions合并optiuons*/</span>
    <span>this</span><span>.</span>options <span>=</span> <span>mergeOptions</span><span>(</span><span>this</span><span>.</span>options<span>,</span> mixin<span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div></li>
<li>
<p>执行时机： 定义 Vue 构造函数 -&gt; <code>initMixin(Vue)</code> -&gt; <code>new Vue()</code></p>
</li>
<li>
<p>优点：mixin可以根据逻辑关注点进行组织代码，复用组件代码。</p>
</li>
<li>
<p>缺点：</p>
<ul>
<li><code>mixins</code> 带来了隐式依赖</li>
<li><code>mixins</code> 与 <code>mixins</code> 之间，<code>mixins</code> 与组件之间容易导致命名冲突</li>
<li>由于 <code>mixins</code> 是侵入式的，它改变了原组件，所以修改 <code>mixins</code> 等于修改原组件，随着需求的增长 <code>mixins</code> 将变得复杂，导致滚雪球的复杂性。</li>
</ul>
</li>
<li>
<p><strong>参考</strong>：<a href="https://cn.vuejs.org/v2/guide/mixins.html#%E5%85%A8%E5%B1%80%E6%B7%B7%E5%85%A5" target="_blank" rel="noopener noreferrer">全局混入</a></p>
</li>
</ul>
<h3 id="选项合并策略"> <a href="https://cn.vuejs.org/v2/guide/mixins.html#%E8%87%AA%E5%AE%9A%E4%B9%89%E9%80%89%E9%A1%B9%E5%90%88%E5%B9%B6%E7%AD%96%E7%95%A5" target="_blank" rel="noopener noreferrer">选项合并策略</a></h3>
<p><strong>混入基础：</strong></p>
<ul>
<li>
<p><a href="https://cn.vuejs.org/v2/guide/mixins.html#%E8%87%AA%E5%AE%9A%E4%B9%89%E9%80%89%E9%A1%B9%E5%90%88%E5%B9%B6%E7%AD%96%E7%95%A5" target="_blank" rel="noopener noreferrer">混入</a>(mixin) : 提供了一种非常灵活的方式，来分发 Vue 组件中的可复用功能。</p>
</li>
<li>
<p>混入 API：<a href="https://cn.vuejs.org/v2/api/#Vue-mixin" target="_blank" rel="noopener noreferrer">Vue.mixin( mixin )</a></p>
</li>
<li>
<p>一个混入对象可以包含任意组件选项。当组件使用混入对象时，所有混入对象的选项将被“混合”进入该组件本身的选项。</p>
</li>
<li>
<p>默认混入(合并)策略：</p>
<ul>
<li>数据对象 data： 进行递归合并，并在发生冲突时以组件数据优先 ；</li>
<li>值为对象的选项（例如 <code>methods</code>、<code>components</code> 和 <code>directives</code>）：合并为同一个对象。两个对象键名冲突时，取组件对象的键值对；</li>
<li>钩子函数： 合并为一个数组，因此都将被调用，调用顺序：混入对象的钩子 -&gt; 组件自身钩子;</li>
</ul>
</li>
<li>
<p>混入类型：</p>
<ul>
<li>全局混入：一旦使用全局混入，它将影响每一个之后创建的 Vue 实例。使用恰当时，这可以用来为自定义选项注入处理逻辑。</li>
<li>局部混入：使用 minxis 属性添加对象。</li>
</ul>
</li>
<li>
<p>Vue 中提供自定义选项合并策略API:</p>
<ul>
<li>
<p><a href="https://cn.vuejs.org/v2/api/#optionMergeStrategies" target="_blank" rel="noopener noreferrer">Vue.config.optionMergeStrategies</a> 添加一个函数：</p>
<div><pre><code>Vue<span>.</span>config<span>.</span>optionMergeStrategies<span>.</span><span>myOption</span> <span>=</span> <span>function</span> <span>(</span><span>toVal<span>,</span> fromVal</span><span>)</span> <span>{</span>
  <span>// 返回合并后的值</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div></li>
</ul>
</li>
</ul>
<p><strong>选项合并策略分析：</strong></p>
<ul>
<li>
<p>混入即使用 vue 的默认选项合并策略。</p>
</li>
<li>
<p>对于 <code>options</code> 的合并有 2 个过程：</p>
<ul>
<li>外部初始化 Vue 通过 <code>mergeOptions</code> 合并选项；</li>
</ul>
</li>
<li>
<p>子组件初始化过程通过 <code>initInternalComponent</code> 过程中通过 <code>mergeOptions</code> 合并选项 ；</p>
</li>
<li>
<p>选项合并结果：合并后 vue实例 vm.$options 的值最终包含：vue默认option、用户传入 option、 组件实例 option。</p>
</li>
<li>
<p>选项合并流程：</p>
<p><img src="./../flow-graph/选项合并流程.png" alt="" /></p>
</li>
<li>
<p>合并流程分析：</p>
<ul>
<li>
<p>为了更直观，我们可以举个简单的示例：</p>
<div><pre><code><span>import</span> Vue <span>from</span> <span>'vue'</span>

<span>let</span> childComp <span>=</span> <span>{</span>
  template<span>:</span> <span>'&lt;div>{{msg}}&lt;/div>'</span><span>,</span>
  <span>created</span><span>(</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'child created'</span><span>)</span>
  <span>}</span><span>,</span>
  <span>mounted</span><span>(</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'child mounted'</span><span>)</span>
  <span>}</span><span>,</span>
  <span>data</span><span>(</span><span>)</span> <span>{</span>
    <span>return</span> <span>{</span>
      msg<span>:</span> <span>'Hello Vue'</span>
    <span>}</span>
  <span>}</span>
<span>}</span>

Vue<span>.</span><span>mixin</span><span>(</span><span>{</span>
  <span>created</span><span>(</span><span>)</span> <span>{</span>
    console<span>.</span><span>log</span><span>(</span><span>'parent created'</span><span>)</span>
  <span>}</span>
<span>}</span><span>)</span>

<span>let</span> app <span>=</span> <span>new</span> <span>Vue</span><span>(</span><span>{</span>
  el<span>:</span> <span>'#app'</span><span>,</span>
  <span>render</span><span>:</span> <span>h</span> <span>=></span> <span>h</span><span>(</span>childComp<span>)</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br></div></div></li>
<li>
<p><strong>外部调用合并过程：</strong></p>
<ul>
<li>
<p>外部引入 Vue 实例时，在 <code>initGlobalAPI(Vue)</code> 的时候初始化 Vue 实例默认 option。代码<a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/global-api/index.js" target="_blank" rel="noopener noreferrer">src/core/global-api/index.js</a></p>
<div><pre><code><span>//在 Vue 上扩展的一些全局方法的定义</span>
<span>export</span> <span>function</span> <span>initGlobalAPI</span><span>(</span><span>Vue<span>:</span> GlobalAPI</span><span>)</span> <span>{</span>
  <span>// config</span>
  <span>const</span> configDef <span>=</span> <span>{</span><span>}</span>
  configDef<span>.</span><span>get</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> config
  <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
    configDef<span>.</span><span>set</span> <span>=</span> <span>(</span><span>)</span> <span>=></span> <span>{</span>
      <span>warn</span><span>(</span>
        <span>'Do not replace the Vue.config object, set individual fields instead.'</span>
      <span>)</span>
    <span>}</span>
  <span>}</span>
  Object<span>.</span><span>defineProperty</span><span>(</span>Vue<span>,</span> <span>'config'</span><span>,</span> configDef<span>)</span>

  <span>// exposed util methods.</span>
  <span>// NOTE: these are not considered part of the public API - avoid relying on</span>
  <span>// them unless you are aware of the risk.</span>
  Vue<span>.</span>util <span>=</span> <span>{</span>
    warn<span>,</span>
    extend<span>,</span>
    mergeOptions<span>,</span>
    defineReactive
  <span>}</span>

  Vue<span>.</span>set <span>=</span> <span>set</span>
  Vue<span>.</span>delete <span>=</span> del
  Vue<span>.</span>nextTick <span>=</span> nextTick

  Vue<span>.</span>options <span>=</span> Object<span>.</span><span>create</span><span>(</span><span>null</span><span>)</span>
  <span>/** 
  `ASSET_TYPES` 的定义在 `src/shared/constants.js` 中：
   ASSET_TYPES = [
      'component',
      'directive',
      'filter'
    ]
    遍历结果为
    Vue.options.components = {}
    Vue.options.directives = {}
    Vue.options.filters = {}
  */</span>
  <span>ASSET_TYPES</span><span>.</span><span>forEach</span><span>(</span><span>type</span> <span>=></span> <span>{</span>
    Vue<span>.</span>options<span>[</span>type <span>+</span> <span>'s'</span><span>]</span> <span>=</span> Object<span>.</span><span>create</span><span>(</span><span>null</span><span>)</span>
  <span>}</span><span>)</span>

  <span>// this is used to identify the "base" constructor to extend all plain-object</span>
  <span>// components with in Weex's multi-instance scenarios.</span>
  <span>/*_base被用来标识基本构造函数（也就是Vue），以便在多场景下添加组件扩展*/</span>
  Vue<span>.</span>options<span>.</span>_base <span>=</span> Vue
  <span>/* 把一些内置组件扩展到 Vue.options.components*/</span>
  <span>extend</span><span>(</span>Vue<span>.</span>options<span>.</span>components<span>,</span> builtInComponents<span>)</span>

  <span>initUse</span><span>(</span>Vue<span>)</span>
  <span>initMixin</span><span>(</span>Vue<span>)</span>
  <span>initExtend</span><span>(</span>Vue<span>)</span>
  <span>initAssetRegisters</span><span>(</span>Vue<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br></div></div></li>
<li>
<p>当执行 <code>new Vue</code> 的时候，在执行 <code>this._init(options)</code> 的时候，就会执行如下逻辑去合并 <code>options</code>：</p>
<div><pre><code>vm<span>.</span>$options <span>=</span> <span>mergeOptions</span><span>(</span>
  <span>resolveConstructorOptions</span><span>(</span>vm<span>.</span>constructor<span>)</span><span>,</span>
  options <span>||</span> <span>{</span><span>}</span><span>,</span>
  vm
<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br></div></div></li>
<li>
<p><code>mergeOptions</code> 函数:  主要功能就是把 <code>parent</code> 和 <code>child</code> 这两个对象根据一些合并策略，合并成一个新对象并返回。它的定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/util/options.js" target="_blank" rel="noopener noreferrer">src/core/util/options.js</a> 中：</p>
<div><pre><code> <span>/**
  * mergeOptions 主要功能就是把 parent 和 child 这两个对象根据一些合并策略，合并成一个新对象并返回
  * @param {*} parent 
  * @param {*} child 
  * @param {*} vm 
  */</span>
<span>export</span> <span>function</span> <span>mergeOptions</span> <span>(</span>
  <span>parent<span>:</span> Object<span>,</span>
  child<span>:</span> Object<span>,</span>
  vm<span>?</span><span>:</span> Component</span>
<span>)</span><span>:</span> Object <span>{</span>
  <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span>
    <span>/*检查是否是有效的组件名*/</span>
    <span>checkComponents</span><span>(</span>child<span>)</span>
  <span>}</span>

  <span>if</span> <span>(</span><span>typeof</span> child <span>===</span> <span>'function'</span><span>)</span> <span>{</span>
    child <span>=</span> child<span>.</span>options
  <span>}</span>

  <span>/*确保所有props option序列化成正确的格式*/</span>
  <span>normalizeProps</span><span>(</span>child<span>)</span>
  <span>/*将函数指令序列化后加入对象*/</span>
  <span>normalizeDirectives</span><span>(</span>child<span>)</span>
  <span>/*
    https://cn.vuejs.org/v2/api/#extends
    允许声明扩展另一个组件(可以是一个简单的选项对象或构造函数),而无需使用 
    将child的extends也加入parent扩展
  */</span>
  <span>const</span> extendsFrom <span>=</span> child<span>.</span>extends
  <span>if</span> <span>(</span>extendsFrom<span>)</span> <span>{</span> <span>//递归把子组件的extends合并到parant</span>
    parent <span>=</span> <span>mergeOptions</span><span>(</span>parent<span>,</span> extendsFrom<span>,</span> vm<span>)</span>
  <span>}</span>
  <span>/*child的mixins加入parent中*/</span>
  <span>if</span> <span>(</span>child<span>.</span>mixins<span>)</span> <span>{</span> <span>//递归把子组件的mixins合并到parent</span>
    <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>,</span> l <span>=</span> child<span>.</span>mixins<span>.</span>length<span>;</span> i <span>&lt;</span> l<span>;</span> i<span>++</span><span>)</span> <span>{</span>
      parent <span>=</span> <span>mergeOptions</span><span>(</span>parent<span>,</span> child<span>.</span>mixins<span>[</span>i<span>]</span><span>,</span> vm<span>)</span>
    <span>}</span>
  <span>}</span>
  <span>const</span> options <span>=</span> <span>{</span><span>}</span>
  <span>let</span> key
  <span>for</span> <span>(</span>key <span>in</span> parent<span>)</span> <span>{</span>
    <span>mergeField</span><span>(</span>key<span>)</span>
  <span>}</span>
  <span>/*合并parent与child*/</span>
  <span>for</span> <span>(</span>key <span>in</span> child<span>)</span> <span>{</span>
    <span>if</span> <span>(</span><span>!</span><span>hasOwn</span><span>(</span>parent<span>,</span> key<span>)</span><span>)</span> <span>{</span> <span>//如果 key 不在 parent 的自身属性上</span>
      <span>mergeField</span><span>(</span>key<span>)</span> <span>//合并</span>
    <span>}</span>
  <span>}</span>
  <span>//mergeField 函数，它对不同的 key 有着不同的合并策略</span>
  <span>function</span> <span>mergeField</span> <span>(</span><span>key</span><span>)</span> <span>{</span>
    <span>/*strats里面保存了 用户自定义合并策略optionMergeStrategies、el、data、props、watch、computed、
    生命周期钩子函数、component、directive、filter 的合并方法，在这里取出合并方法*/</span>
    <span>const</span> strat <span>=</span> strats<span>[</span>key<span>]</span> <span>||</span> defaultStrat
    <span>/*根据合并方法来合并两个option*/</span>
    options<span>[</span>key<span>]</span> <span>=</span> <span>strat</span><span>(</span>parent<span>[</span>key<span>]</span><span>,</span> child<span>[</span>key<span>]</span><span>,</span> vm<span>,</span> key<span>)</span>
  <span>}</span>
  <span>return</span> options
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br></div></div><ul>
<li>先递归把 <code>extends</code> 和 <code>mixixns</code> 合并到 <code>parent</code> 上</li>
<li>然后遍历 <code>parent</code>，调用 <code>mergeField</code>，然后再遍历 <code>child</code>，如果 <code>key</code> 不在 <code>perent</code> 的自身属性上，则调用 <code>mergeField</code></li>
</ul>
</li>
<li>
<p><code>mergeField</code>函数：选项合并策略函数，它对不同的 <code>key</code> 有着不同的合并策略。 strat 是函数数组，数组包含 optionMergeStrategies 的 propsData、data、Hook、Assets、Watch、Computed、Components、Directives 等选项的合并策略方法。strat 定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/util/options.js" target="_blank" rel="noopener noreferrer">src/core/util/options.js</a> 中：</p>
<div><pre><code> <span>/*
  这个strats的作用就是，当要合并两个option（比如父组件的option与子组件的option）合并的时候，
  这里写了如何合并两个数据（或者function等）得到最终结果的方法
 */</span>
<span>const</span> strats <span>=</span> config<span>.</span>optionMergeStrategies <span>//全局api</span>


<span>/**
*  定义默认合并策略
 * Default strategy.
 */</span>
<span>const</span> <span>defaultStrat</span> <span>=</span> <span>function</span> <span>(</span><span>parentVal<span>:</span> any<span>,</span> childVal<span>:</span> any</span><span>)</span><span>:</span> any <span>{</span>
  <span>return</span> childVal <span>===</span> <span>undefined</span>
    <span>?</span> parentVal
    <span>:</span> childVal
<span>}</span>

<span>// 以 data 合并策略为例，其他合并策略代码省略...</span>
<span>/**
 * Data
 */</span>
strats<span>.</span><span>data</span> <span>=</span> <span>function</span> <span>(</span>
  <span>parentVal<span>:</span> any<span>,</span>
  childVal<span>:</span> any<span>,</span>
  vm<span>?</span><span>:</span> Component</span>
<span>)</span><span>:</span> <span>?</span>Function <span>{</span>
  <span>if</span> <span>(</span><span>!</span>vm<span>)</span> <span>{</span>
    <span>// in a Vue.extend merge, both should be functions</span>
    <span>if</span> <span>(</span><span>!</span>childVal<span>)</span> <span>{</span>
      <span>return</span> parentVal
    <span>}</span>
    <span>if</span> <span>(</span><span>typeof</span> childVal <span>!==</span> <span>'function'</span><span>)</span> <span>{</span>
      process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> <span>warn</span><span>(</span>
        <span>'The "data" option should be a function '</span> <span>+</span>
        <span>'that returns a per-instance value in component '</span> <span>+</span>
        <span>'definitions.'</span><span>,</span>
        vm
      <span>)</span>
      <span>return</span> parentVal
    <span>}</span>
    <span>if</span> <span>(</span><span>!</span>parentVal<span>)</span> <span>{</span>
      <span>return</span> childVal
    <span>}</span>
    <span>// when parentVal &amp; childVal are both present,</span>
    <span>// we need to return a function that returns the</span>
    <span>// merged result of both functions... no need to</span>
    <span>// check if parentVal is a function here because</span>
    <span>// it has to be a function to pass previous merges.</span>
    <span>return</span> <span>function</span> <span>mergedDataFn</span> <span>(</span><span>)</span> <span>{</span>
      <span>return</span> <span>mergeData</span><span>(</span>
        <span>childVal</span><span>.</span><span>call</span><span>(</span><span>this</span><span>)</span><span>,</span>
        <span>parentVal</span><span>.</span><span>call</span><span>(</span><span>this</span><span>)</span>
      <span>)</span>
    <span>}</span>
  <span>}</span> <span>else</span> <span>if</span> <span>(</span>parentVal <span>||</span> childVal<span>)</span> <span>{</span>
    <span>return</span> <span>function</span> <span>mergedInstanceDataFn</span> <span>(</span><span>)</span> <span>{</span>
      <span>// instance merge</span>
      <span>const</span> instanceData <span>=</span> <span>typeof</span> childVal <span>===</span> <span>'function'</span>
        <span>?</span> <span>childVal</span><span>.</span><span>call</span><span>(</span>vm<span>)</span>
        <span>:</span> childVal
      <span>const</span> defaultData <span>=</span> <span>typeof</span> parentVal <span>===</span> <span>'function'</span>
        <span>?</span> <span>parentVal</span><span>.</span><span>call</span><span>(</span>vm<span>)</span>
        <span>:</span> <span>undefined</span>
      <span>if</span> <span>(</span>instanceData<span>)</span> <span>{</span>
        <span>return</span> <span>mergeData</span><span>(</span>instanceData<span>,</span> defaultData<span>)</span>
      <span>}</span> <span>else</span> <span>{</span>
        <span>return</span> defaultData
      <span>}</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br></div></div></li>
<li>
<p>执行完 mergeOptions 方法后，<code>vm.$options</code> 中包含 Vue 默认 option 和 用户传入 option：</p>
<div><pre><code> vm<span>.</span>$options <span>=</span> <span>{</span>
  components<span>:</span> <span>{</span> <span>}</span><span>,</span> <span>// vue 默认option</span>
  created<span>:</span> <span>[</span>
    <span>function</span> <span>created</span><span>(</span><span>)</span> <span>{</span> <span>// 用户传入 option</span>
      console<span>.</span><span>log</span><span>(</span><span>'parent created'</span><span>)</span>
    <span>}</span>
  <span>]</span><span>,</span>
  directives<span>:</span> <span>{</span> <span>}</span><span>,</span> <span>// vue 默认option</span>
  filters<span>:</span> <span>{</span> <span>}</span><span>,</span> <span>// vue 默认option</span>
  <span>_base</span><span>:</span> <span>function</span> <span>Vue</span><span>(</span><span>options</span><span>)</span> <span>{</span>  <span>// vue 默认option</span>
    <span>// ...</span>
  <span>}</span><span>,</span>
  el<span>:</span> <span>"#app"</span><span>,</span>
  <span>render</span><span>:</span> <span>function</span> <span>(</span><span>h</span><span>)</span> <span>{</span>
    <span>//...</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br></div></div></li>
</ul>
</li>
<li>
<p><strong>组件实例化合并过程：</strong></p>
<ul>
<li>
<p>在创建组件一节中组件实例化时有两处：</p>
<ul>
<li>render 方法调用的 _createElement() 方法中：当 tag 不是 string 时，调用 createComponent() 创建组件；</li>
<li>path 方法调用 createElem() 方法中： 调用 createComponent() 创建组件；</li>
</ul>
</li>
<li>
<p>只有path 方法 createElem() 方法中调用 createComponent() 创建组件时通过 new Vue() 实例创建；代码定义在 <code>src/core/vdom/create-component.js</code> 中：</p>
<div><pre><code><span>export</span> <span>function</span> <span>createComponentInstanceForVnode</span> <span>(</span>
  vnode<span>:</span> any<span>,</span> <span>// we know it's MountedComponentVNode but flow doesn't</span>
  parent<span>:</span> any<span>,</span> <span>// activeInstance in lifecycle state</span>
<span>)</span><span>:</span> Component <span>{</span>
  <span>const</span> options<span>:</span> InternalComponentOptions <span>=</span> <span>{</span>
    _isComponent<span>:</span> <span>true</span><span>,</span>
    _parentVnode<span>:</span> vnode<span>,</span>
    parent
  <span>}</span>
  <span>// ...</span>
  <span>return</span> <span>new</span> <span>vnode<span>.</span>componentOptions<span>.</span>Ctor</span><span>(</span>options<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div></li>
<li>
<p><code>new vnode.componentOptions.Ctor(options)</code> 时，组件的构造函数是通过 <code>Vue.extend</code> 继承自 <code>Vue</code> 的：代码定义在 <code>src/core/global-api/extend.js</code> 中。</p>
<div><pre><code><span>/**
 * Class inheritance
 */</span>
Vue<span>.</span><span>extend</span> <span>=</span> <span>function</span> <span>(</span><span>extendOptions<span>:</span> Object</span><span>)</span><span>:</span> Function <span>{</span>
  <span>// ...</span>
  Sub<span>.</span>options <span>=</span> <span>mergeOptions</span><span>(</span>
    Super<span>.</span>options<span>,</span>
    extendOptions
  <span>)</span>

  <span>// ...</span>
  <span>// keep a reference to the super options at extension time.</span>
  <span>// later at instantiation we can check if Super's options have</span>
  <span>// been updated.</span>
  Sub<span>.</span>superOptions <span>=</span> Super<span>.</span>options
  Sub<span>.</span>extendOptions <span>=</span> extendOptions
  Sub<span>.</span>sealedOptions <span>=</span> <span>extend</span><span>(</span><span>{</span><span>}</span><span>,</span> Sub<span>.</span>options<span>)</span>

  <span>// ...</span>
  <span>return</span> Sub
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br></div></div><ul>
<li>在组件构造函数中，主要通过 mergeOptions 将 vue 实例默认 option 和 <code>extendOptions</code> 组件对象的  option 进行合并，作为组件 Sub.options。</li>
</ul>
</li>
<li>
<p>执行 <code>new vnode.componentOptions.Ctor(options)</code> 接着执行 <code>this._init(options)</code>，因为 <code>options._isComponent</code> 为 true，那么合并 <code>options</code> 的过程走到了 <code>initInternalComponent(vm, options)</code> 逻辑。</p>
</li>
<li>
<p><code>initInternalComponent</code> 函数：将组件 option 保存到 vue 实例的 option 中。</p>
<ul>
<li>
<p>因为实例化 vue 时,将 用户传入 option 和 vue 默认 option 已经合并，并且在实例化组件时，组件构造函数将组件option 和 vue 实例 option 合并作为组件 option，因此此时组件 option 包含：vue默认option、用户传入 option、 组件实例 option。</p>
<div><pre><code><span>export</span> <span>function</span> <span>initInternalComponent</span> <span>(</span><span>vm<span>:</span> Component<span>,</span> options<span>:</span> InternalComponentOptions</span><span>)</span> <span>{</span>
  <span>const</span> opts <span>=</span> vm<span>.</span>$options <span>=</span> Object<span>.</span><span>create</span><span>(</span>vm<span>.</span>constructor<span>.</span>options<span>)</span>
  <span>// doing this because it's faster than dynamic enumeration.</span>
  <span>const</span> parentVnode <span>=</span> options<span>.</span>_parentVnode
  opts<span>.</span>parent <span>=</span> options<span>.</span>parent
  opts<span>.</span>_parentVnode <span>=</span> parentVnode

  <span>const</span> vnodeComponentOptions <span>=</span> parentVnode<span>.</span>componentOptions
  opts<span>.</span>propsData <span>=</span> vnodeComponentOptions<span>.</span>propsData
  opts<span>.</span>_parentListeners <span>=</span> vnodeComponentOptions<span>.</span>listeners
  opts<span>.</span>_renderChildren <span>=</span> vnodeComponentOptions<span>.</span>children
  opts<span>.</span>_componentTag <span>=</span> vnodeComponentOptions<span>.</span>tag

  <span>if</span> <span>(</span>options<span>.</span>render<span>)</span> <span>{</span>
    opts<span>.</span>render <span>=</span> options<span>.</span>render
    opts<span>.</span>staticRenderFns <span>=</span> options<span>.</span>staticRenderFns
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br></div></div></li>
</ul>
</li>
<li>
<p><strong>执行完合并后<code>vm.$options</code> 的值最终包含：vue默认option、用户传入 option、 组件实例 option</strong>。</p>
<div><pre><code>vm<span>.</span>$options <span>=</span> <span>{</span>
  parent<span>:</span> Vue <span>/*父Vue实例*/</span><span>,</span>
  propsData<span>:</span> <span>undefined</span><span>,</span> <span>//vue默认option</span>
  _componentTag<span>:</span> <span>undefined</span><span>,</span>
  _parentVnode<span>:</span> VNode <span>/*父VNode实例*/</span><span>,</span>
  _renderChildren<span>:</span><span>undefined</span><span>,</span>
  __proto__<span>:</span> <span>{</span>
    components<span>:</span> <span>{</span> <span>}</span><span>,</span> <span>//vue默认option</span>
    directives<span>:</span> <span>{</span> <span>}</span><span>,</span> <span>//vue默认option</span>
    filters<span>:</span> <span>{</span> <span>}</span><span>,</span><span>//vue默认option</span>
    <span>_base</span><span>:</span> <span>function</span> <span>Vue</span><span>(</span><span>options</span><span>)</span> <span>{</span> <span>//vue默认option</span>
        <span>//...</span>
    <span>}</span><span>,</span>
    _Ctor<span>:</span> <span>{</span><span>}</span><span>,</span>
    created<span>:</span> <span>[</span>
      <span>function</span> <span>created</span><span>(</span><span>)</span> <span>{</span>
        console<span>.</span><span>log</span><span>(</span><span>'parent created'</span><span>)</span> <span>//用户传入 option</span>
      <span>}</span><span>,</span> <span>function</span> <span>created</span><span>(</span><span>)</span> <span>{</span>
        console<span>.</span><span>log</span><span>(</span><span>'child created'</span><span>)</span> <span>//组件实例 option</span>
      <span>}</span>
    <span>]</span><span>,</span>
    mounted<span>:</span> <span>[</span>
      <span>function</span> <span>mounted</span><span>(</span><span>)</span> <span>{</span>
        console<span>.</span><span>log</span><span>(</span><span>'child mounted'</span><span>)</span> <span>//组件实例 option</span>
      <span>}</span>
    <span>]</span><span>,</span>
    <span>data</span><span>(</span><span>)</span> <span>{</span>
       <span>return</span> <span>{</span>
         msg<span>:</span> <span>'Hello Vue'</span>
       <span>}</span>
    <span>}</span><span>,</span>
    template<span>:</span> <span>'&lt;div>{{msg}}&lt;/div>'</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br></div></div></li>
</ul>
</li>
</ul>
</li>
<li>
<p>选项合并设计思路：自身定义了一些默认配置，同时又可以在初始化阶段传入一些定义配置，然后去 merge 默认配置，来达到定制化不同需求的目的。</p>
</li>
</ul>
<h2 id="选项-数据"> 选项/数据</h2>
<h3 id="data"> <a href="https://cn.vuejs.org/v2/api/#data" target="_blank" rel="noopener noreferrer">data</a></h3>
<ul>
<li>
<p><strong>类型</strong>：<code>Object | Function</code></p>
</li>
<li>
<p><strong>限制</strong>：组件的定义只接受 <code>function</code>。</p>
</li>
<li>
<p><strong>详细</strong>：</p>
<ul>
<li>
<p>Vue 实例的数据对象。Vue 会递归地把 data 的 property 转换为 getter/setter，从而让 data 的 property 能够响应数据变化。<strong>对象必须是纯粹的对象 (含有零个或多个的 key/value 对)</strong>：浏览器 API 创建的原生对象，原型上的 property 会被忽略。大概来说，data 应该只能是数据 - 不推荐观察拥有状态行为的对象。</p>
</li>
<li>
<p>一旦观察过，你就无法在根数据对象上添加响应式 property。因此推荐在创建实例之前，就声明所有的根级响应式 property。</p>
</li>
<li>
<p>实例创建之后，可以通过 <code>vm.$data</code> 访问原始数据对象。Vue 实例也代理了 data 对象上所有的 property，因此访问 <code>vm.a</code> 等价于访问 <code>vm.$data.a</code>。</p>
</li>
<li>
<p>以 <code>_</code> 或 <code>$</code> 开头的 property <strong>不会</strong>被 Vue 实例代理，因为它们可能和 Vue 内置的 property、API 方法冲突。你可以使用例如 <code>vm.$data._property</code> 的方式访问这些 property。</p>
</li>
<li>
<p>当一个组件被定义，<strong><code>data</code> 必须声明为返回一个初始数据对象的函数</strong>，因为组件可能被用来创建多个实例。如果 <code>data</code> 仍然是一个纯粹的对象，则所有的实例将<strong>共享引用</strong>同一个数据对象！通过提供 <code>data</code> 函数，每次创建一个新实例后，我们能够调用 <code>data</code> 函数，从而返回初始数据的一个全新副本数据对象。</p>
</li>
<li>
<p>如果需要，可以通过将 <code>vm.$data</code> 传入 <code>JSON.parse(JSON.stringify(...))</code> 得到深拷贝的原始数据对象。</p>
</li>
</ul>
</li>
</ul>
<h3 id="computed-计算属性"> <a href="https://cn.vuejs.org/v2/api/#computed" target="_blank" rel="noopener noreferrer">computed 计算属性</a></h3>
<p><img src="./../images/computed.png" alt="" /></p>
<p><strong>computed &amp; watch 比较</strong></p>
<ul>
<li>
<p>相同点：数据变化时，会执行回调方法，进行依赖数据收集和重新计算。</p>
</li>
<li>
<p>区别：<img src="./../images/computed2.png" alt="computed2" /></p>
</li>
<li>
<p>流程图：</p>
<ul>
<li>
<p>初次渲染comouted 流程</p>
<p><img src="./../images/computed-yuanli1.png" alt="" /></p>
</li>
<li>
<p>修改 computed 重新渲染流程</p>
<p><img src="./../images/computed-yuanli2.png" alt="" /></p>
</li>
</ul>
</li>
<li>
<p>源码分析：</p>
<p>计算属性的初始化是发生在 Vue 实例初始化阶段的 <code>initState</code> 函数中，执行了 <code>if (opts.computed) initComputed(vm, opts.computed)</code>，<code>initComputed</code> 的定义在 <a href="https://github.com/vuejs/vue/blob/v2.3.0/src/core/instance/state.js" target="_blank" rel="noopener noreferrer">src/core/instance/state.js</a> 中：</p>
<div><pre><code><span>const</span> computedWatcherOptions <span>=</span> <span>{</span> lazy<span>:</span> <span>true</span> <span>}</span> <span>// lazy 标志是否重新求值</span>

<span>/*初始化computed*/</span>
<span>function</span> <span>initComputed</span> <span>(</span><span>vm<span>:</span> Component<span>,</span> computed<span>:</span> Object</span><span>)</span> <span>{</span>
  <span>const</span> watchers <span>=</span> vm<span>.</span>_computedWatchers <span>=</span> Object<span>.</span><span>create</span><span>(</span><span>null</span><span>)</span> <span>// 创建 watcher 对象</span>

  <span>for</span> <span>(</span><span>const</span> key <span>in</span> computed<span>)</span> <span>{</span> <span>// 遍历计算属性对象</span>
    <span>const</span> userDef <span>=</span> computed<span>[</span>key<span>]</span>
    <span>/*
      计算属性可能是一个function，也有可能设置了get以及set的对象。
      可以参考 https://cn.vuejs.org/v2/guide/computed.html#计算-setter
    */</span>
    <span>let</span> getter <span>=</span> <span>typeof</span> userDef <span>===</span> <span>'function'</span> <span>?</span> userDef <span>:</span> userDef<span>.</span>get
    <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span> <span>// 非生产环境下提示警告</span>
      <span>/*getter不存在的时候抛出warning并且给getter赋空函数*/</span>
      <span>if</span> <span>(</span>getter <span>===</span> <span>undefined</span><span>)</span> <span>{</span>
        <span>warn</span><span>(</span>
          <span><span>`</span><span>No getter function has been defined for computed property "</span><span><span>${</span>key<span>}</span></span><span>".</span><span>`</span></span><span>,</span>
          vm
        <span>)</span>
        getter <span>=</span> noop
      <span>}</span>
    <span>}</span>
    <span>// create internal watcher for the computed property.</span>
    <span>/*
      为每一个计算属性创建一个对应的内部的监视器 Watcher，保存在vm实例的_computedWatchers中
      这里的computedWatcherOptions参数传递了一个lazy为true，会使得watch实例的dirty为true
    */</span>
    watchers<span>[</span>key<span>]</span> <span>=</span> <span>new</span> <span>Watcher</span><span>(</span>vm<span>,</span> getter<span>,</span> noop<span>,</span> computedWatcherOptions<span>)</span>

    <span>// component-defined computed properties are already defined on the</span>
    <span>// component prototype. We only need to define computed properties defined</span>
    <span>// at instantiation here.</span>
    <span>/*组件正在定义的计算属性已经定义在现有组件的原型上则不会进行重复定义*/</span>
    <span>if</span> <span>(</span><span>!</span><span>(</span>key <span>in</span> vm<span>)</span><span>)</span> <span>{</span>
      <span>/*定义计算属性*/</span>
      <span>defineComputed</span><span>(</span>vm<span>,</span> key<span>,</span> userDef<span>)</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span>process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span><span>)</span> <span>{</span> <span>// 非生产环境下警告</span>
      <span>/*如果计算属性与已定义的data或者props中的名称冲突则发出warning*/</span>
      <span>if</span> <span>(</span>key <span>in</span> vm<span>.</span>$data<span>)</span> <span>{</span>
        <span>warn</span><span>(</span><span><span>`</span><span>The computed property "</span><span><span>${</span>key<span>}</span></span><span>" is already defined in data.</span><span>`</span></span><span>,</span> vm<span>)</span>
      <span>}</span> <span>else</span> <span>if</span> <span>(</span>vm<span>.</span>$options<span>.</span>props <span>&amp;&amp;</span> key <span>in</span> vm<span>.</span>$options<span>.</span>props<span>)</span> <span>{</span>
        <span>warn</span><span>(</span><span><span>`</span><span>The computed property "</span><span><span>${</span>key<span>}</span></span><span>" is already defined as a prop.</span><span>`</span></span><span>,</span> vm<span>)</span>
      <span>}</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br></div></div><ul>
<li>函数首先创建 <code>vm._computedWatchers</code> 为一个空对象，接着对 <code>computed</code> 对象做遍历，拿到计算属性的每一个 <code>userDef</code>，然后尝试获取这个 <code>userDef</code> 对应的 <code>getter</code> 函数，拿不到则在开发环境下报警告。</li>
<li>接下来为每一个 <code>getter</code> 创建一个 <code>watcher</code>，这个 <code>watcher</code> 和渲染 <code>watcher</code> 有一点很大的不同，它是一个 <code>computed watcher</code>，因为 <code>const computedWatcherOptions = { computed: true }</code>。</li>
<li>最后对判断如果 <code>key</code> 不是 <code>vm</code> 的属性，则调用 <code>defineComputed(vm, key, userDef)</code>，否则判断计算属性对于的 <code>key</code> 是否已经被 <code>data</code> 或者 <code>prop</code> 所占用，如果是的话则在开发环境报相应的警告。</li>
</ul>
<p>那么接下来需要重点关注 <code>defineComputed</code> 的实现：</p>
<div><pre><code><span>/*定义计算属性*/</span>
<span>export</span> <span>function</span> <span>defineComputed</span> <span>(</span><span>target<span>:</span> any<span>,</span> key<span>:</span> string<span>,</span> userDef<span>:</span> Object <span>|</span> Function</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>typeof</span> userDef <span>===</span> <span>'function'</span><span>)</span> <span>{</span>
    <span>/*创建计算属性的getter*/</span>
    sharedPropertyDefinition<span>.</span>get <span>=</span> <span>createComputedGetter</span><span>(</span>key<span>)</span>
    <span>/*
      当userDef是一个function的时候是不需要setter的，所以这边给它设置成了空函数。
      因为计算属性默认是一个function，只设置getter。
      当需要设置setter的时候，会将计算属性设置成一个对象。参考：https://cn.vuejs.org/v2/guide/computed.html#计算-setter
    */</span>
    sharedPropertyDefinition<span>.</span>set <span>=</span> noop
  <span>}</span> <span>else</span> <span>{</span>
    <span>/*get不存在则直接给空函数，如果存在则查看是否有缓存cache，没有依旧赋值get，有的话使用createComputedGetter创建*/</span>
    sharedPropertyDefinition<span>.</span>get <span>=</span> userDef<span>.</span>get
      <span>?</span> userDef<span>.</span>cache <span>!==</span> <span>false</span>
        <span>?</span> <span>createComputedGetter</span><span>(</span>key<span>)</span>
        <span>:</span> userDef<span>.</span>get
      <span>:</span> noop
    <span>/*如果有设置set方法则直接使用，否则赋值空函数*/</span>
    sharedPropertyDefinition<span>.</span>set <span>=</span> userDef<span>.</span>set
      <span>?</span> userDef<span>.</span>set
      <span>:</span> noop
  <span>}</span>
  <span>/*defineProperty上getter与setter*/</span>
  Object<span>.</span><span>defineProperty</span><span>(</span>target<span>,</span> key<span>,</span> sharedPropertyDefinition<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br></div></div><p>这段逻辑很简单，其实就是利用 <code>Object.defineProperty</code> 给计算属性对应的 <code>key</code> 值添加 getter 和 setter，setter 通常是计算属性是一个对象，并且拥有 <code>set</code> 方法的时候才有，否则是一个空函数。在平时的开发场景中，计算属性有 setter 的情况比较少，我们重点关注一下 getter 部分，缓存的配置也先忽略，最终 getter 对应的是 <code>createComputedGetter(key)</code> 的返回值，来看一下它的定义：</p>
<div><pre><code><span>/*
* 创建计算属性的 getter 函数
* 主要执行两个步骤： 
*  1. 获取 computed 对应的 watcher 对象判断依赖数据是否改变，改变了 重新计算computed 
*  2. 重新依赖收集
*/</span>
<span>function</span> <span>createComputedGetter</span> <span>(</span><span>key</span><span>)</span> <span>{</span>
  <span>return</span> <span>function</span> <span>computedGetter</span> <span>(</span><span>)</span> <span>{</span>
    <span>// 获取 computed 对应的 watcher 对象</span>
    <span>const</span> watcher <span>=</span> <span>this</span><span>.</span>_computedWatchers <span>&amp;&amp;</span> <span>this</span><span>.</span>_computedWatchers<span>[</span>key<span>]</span>
    <span>if</span> <span>(</span>watcher<span>)</span> <span>{</span>
      <span>/*实际是脏检查，在计算属性中的依赖发生改变的时候dirty会变成true，在get的时候重新计算计算属性的输出值*/</span>
      <span>if</span> <span>(</span>watcher<span>.</span>dirty<span>)</span> <span>{</span>
        watcher<span>.</span><span>evaluate</span><span>(</span><span>)</span>
      <span>}</span>
      <span>/*依赖收集*/</span>
      <span>if</span> <span>(</span>Dep<span>.</span>target<span>)</span> <span>{</span>
        watcher<span>.</span><span>depend</span><span>(</span><span>)</span>
      <span>}</span>
      <span>return</span> watcher<span>.</span>value
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br></div></div><p><code>createComputedGetter</code> 返回一个函数 <code>computedGetter</code>，它就是计算属性对应的 getter。</p>
<p>整个计算属性的初始化过程到此结束，我们知道计算属性是一个 <code>computed watcher</code>，它和普通的 <code>watcher</code> 有什么区别呢，为了更加直观，接下来来我们来通过一个例子来分析 <code>computed watcher</code> 的实现。</p>
<div><pre><code><span>var</span> vm <span>=</span> <span>new</span> <span>Vue</span><span>(</span><span>{</span>
  data<span>:</span> <span>{</span>
    firstName<span>:</span> <span>'Foo'</span><span>,</span>
    lastName<span>:</span> <span>'Bar'</span>
  <span>}</span><span>,</span>
  computed<span>:</span> <span>{</span>
    <span>fullName</span><span>:</span> <span>function</span> <span>(</span><span>)</span> <span>{</span>
      <span>return</span> <span>this</span><span>.</span>firstName <span>+</span> <span>' '</span> <span>+</span> <span>this</span><span>.</span>lastName
    <span>}</span>
  <span>}</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><p>当初始化这个 <code>computed watcher</code> 实例的时候，构造函数部分逻辑稍有不同：</p>
<div><pre><code><span>constructor</span> <span>(</span>
  <span>vm<span>:</span> Component<span>,</span>
  expOrFn<span>:</span> string <span>|</span> Function<span>,</span>
  cb<span>:</span> Function<span>,</span>
  options<span>?</span><span>:</span> <span>?</span>Object<span>,</span>
  isRenderWatcher<span>?</span><span>:</span> boolean</span>
<span>)</span> <span>{</span>
  <span>// ...</span>
  <span>if</span> <span>(</span><span>this</span><span>.</span>computed<span>)</span> <span>{</span>
    <span>this</span><span>.</span>value <span>=</span> <span>undefined</span>
    <span>this</span><span>.</span>dep <span>=</span> <span>new</span> <span>Dep</span><span>(</span><span>)</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>this</span><span>.</span>value <span>=</span> <span>this</span><span>.</span><span>get</span><span>(</span><span>)</span>
  <span>}</span>
<span>}</span>  
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div><p>可以发现 <code>computed watcher</code> 会并不会立刻求值，同时持有一个 <code>dep</code> 实例。</p>
<p>然后当我们的 <code>render</code> 函数执行访问到 <code>this.fullName</code> 的时候，就触发了计算属性的 <code>getter</code>，它会拿到计算属性对应的 <code>watcher</code>，然后执行 <code>watcher.depend()</code>，来看一下它的定义：</p>
<div><pre><code><span>/**
  * Depend on this watcher. Only for computed property watchers.
  */</span>
<span>depend</span> <span>(</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>this</span><span>.</span>dep <span>&amp;&amp;</span> Dep<span>.</span>target<span>)</span> <span>{</span>
    <span>this</span><span>.</span>dep<span>.</span><span>depend</span><span>(</span><span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div><p>注意，这时候的 <code>Dep.target</code> 是渲染 <code>watcher</code>，所以 <code>this.dep.depend()</code> 相当于渲染 <code>watcher</code> 订阅了这个 <code>computed watcher</code> 的变化。</p>
<p>然后再执行 <code>watcher.evaluate()</code> 去求值，来看一下它的定义：</p>
<div><pre><code><span>/**
  * Evaluate and return the value of the watcher.
  * This only gets called for computed property watchers.
  */</span>
<span>evaluate</span> <span>(</span><span>)</span> <span>{</span> <span>// 进行脏检查，在计算属性中的依赖发生改变的时候dirty会变成true，重新计算</span>
  <span>if</span> <span>(</span><span>this</span><span>.</span>dirty<span>)</span> <span>{</span>
    <span>this</span><span>.</span>value <span>=</span> <span>this</span><span>.</span><span>get</span><span>(</span><span>)</span>
    <span>this</span><span>.</span>dirty <span>=</span> <span>false</span>
  <span>}</span>
  <span>return</span> <span>this</span><span>.</span>value
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br></div></div><p><code>evaluate</code> 的逻辑非常简单，判断 <code>this.dirty</code>，如果为 <code>true</code> 则通过 <code>this.get()</code> 求值，然后把 <code>this.dirty</code> 设置为 false。在求值过程中，会执行 <code>value = this.getter.call(vm, vm)</code>，这实际上就是执行了计算属性定义的 <code>getter</code> 函数，在我们这个例子就是执行了 <code>return this.firstName + ' ' + this.lastName</code>。</p>
<p>这里需要特别注意的是，由于 <code>this.firstName</code> 和 <code>this.lastName</code> 都是响应式对象，这里会触发它们的 getter，根据我们之前的分析，它们会把自身持有的 <code>dep</code> 添加到当前正在计算的 <code>watcher</code> 中，这个时候 <code>Dep.target</code> 就是这个 <code>computed watcher</code>。</p>
<p>最后通过 <code>return this.value</code> 拿到计算属性对应的值。我们知道了计算属性的求值过程，那么接下来看一下它依赖的数据变化后的逻辑。</p>
<p>一旦我们对计算属性依赖的数据做修改，则会触发 setter 过程，通知所有订阅它变化的 <code>watcher</code> 更新，执行 <code>watcher.update()</code> 方法：</p>
<div><pre><code><span>/* istanbul ignore else */</span>
<span>if</span> <span>(</span><span>this</span><span>.</span>computed<span>)</span> <span>{</span>
  <span>// A computed property watcher has two modes: lazy and activated.</span>
  <span>// It initializes as lazy by default, and only becomes activated when</span>
  <span>// it is depended on by at least one subscriber, which is typically</span>
  <span>// another computed property or a component's render function.</span>
  <span>if</span> <span>(</span><span>this</span><span>.</span>dep<span>.</span>subs<span>.</span>length <span>===</span> <span>0</span><span>)</span> <span>{</span>
    <span>// In lazy mode, we don't want to perform computations until necessary,</span>
    <span>// so we simply mark the watcher as dirty. The actual computation is</span>
    <span>// performed just-in-time in this.evaluate() when the computed property</span>
    <span>// is accessed.</span>
    <span>this</span><span>.</span>dirty <span>=</span> <span>true</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>// In activated mode, we want to proactively perform the computation</span>
    <span>// but only notify our subscribers when the value has indeed changed.</span>
    <span>this</span><span>.</span><span>getAndInvoke</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
      <span>this</span><span>.</span>dep<span>.</span><span>notify</span><span>(</span><span>)</span>
    <span>}</span><span>)</span>
  <span>}</span>
<span>}</span> <span>else</span> <span>if</span> <span>(</span><span>this</span><span>.</span>sync<span>)</span> <span>{</span>
  <span>this</span><span>.</span><span>run</span><span>(</span><span>)</span>
<span>}</span> <span>else</span> <span>{</span>
  <span>queueWatcher</span><span>(</span><span>this</span><span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br></div></div><p>那么对于计算属性这样的 <code>computed watcher</code>，它实际上是有 2 种模式，lazy 和 active。如果 <code>this.dep.subs.length === 0</code> 成立，则说明没有人去订阅这个 <code>computed watcher</code> 的变化，仅仅把 <code>this.dirty = true</code>，只有当下次再访问这个计算属性的时候才会重新求值。在我们的场景下，渲染 <code>watcher</code> 订阅了这个 <code>computed watcher</code> 的变化，那么它会执行：</p>
<div><pre><code><span>this</span><span>.</span><span>getAndInvoke</span><span>(</span><span>(</span><span>)</span> <span>=></span> <span>{</span>
  <span>this</span><span>.</span>dep<span>.</span><span>notify</span><span>(</span><span>)</span>
<span>}</span><span>)</span>

<span>getAndInvoke</span> <span>(</span><span>cb<span>:</span> Function</span><span>)</span> <span>{</span>
  <span>const</span> value <span>=</span> <span>this</span><span>.</span><span>get</span><span>(</span><span>)</span>
  <span>if</span> <span>(</span>
    value <span>!==</span> <span>this</span><span>.</span>value <span>||</span>
    <span>// Deep watchers and watchers on Object/Arrays should fire even</span>
    <span>// when the value is the same, because the value may</span>
    <span>// have mutated.</span>
    <span>isObject</span><span>(</span>value<span>)</span> <span>||</span>
    <span>this</span><span>.</span>deep
  <span>)</span> <span>{</span>
    <span>// set new value</span>
    <span>const</span> oldValue <span>=</span> <span>this</span><span>.</span>value
    <span>this</span><span>.</span>value <span>=</span> value
    <span>this</span><span>.</span>dirty <span>=</span> <span>false</span>
    <span>if</span> <span>(</span><span>this</span><span>.</span>user<span>)</span> <span>{</span>
      <span>try</span> <span>{</span>
        <span>cb</span><span>.</span><span>call</span><span>(</span><span>this</span><span>.</span>vm<span>,</span> value<span>,</span> oldValue<span>)</span>
      <span>}</span> <span>catch</span> <span>(</span>e<span>)</span> <span>{</span>
        <span>handleError</span><span>(</span>e<span>,</span> <span>this</span><span>.</span>vm<span>,</span> <span><span>`</span><span>callback for watcher "</span><span><span>${</span><span>this</span><span>.</span>expression<span>}</span></span><span>"</span><span>`</span></span><span>)</span>
      <span>}</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>cb</span><span>.</span><span>call</span><span>(</span><span>this</span><span>.</span>vm<span>,</span> value<span>,</span> oldValue<span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br></div></div><p><code>getAndInvoke</code> 函数会重新计算，然后对比新旧值，如果变化了则执行回调函数，那么这里这个回调函数是 <code>this.dep.notify()</code>，在我们这个场景下就是触发了渲染 <code>watcher</code> 重新渲染。</p>
<p>通过以上的分析，我们知道计算属性本质上就是一个 <code>computed watcher</code>，也了解了它的创建过程和被访问触发 getter 以及依赖更新的过程，其实这是最新的计算属性的实现，之所以这么设计是因为 Vue 想确保不仅仅是计算属性依赖的值发生变化，而是当计算属性最终计算的值发生变化才会触发渲染 <code>watcher</code> 重新渲染，本质上是一种优化。</p>
<p>接下来我们来分析一下侦听属性 <code>watch</code> 是怎么实现的。</p>
</li>
</ul>
<h3 id="watch-侦听器"> <a href="https://cn.vuejs.org/v2/api/#watch" target="_blank" rel="noopener noreferrer">watch 侦听器</a></h3>
<ul>
<li>
<p>作用：用于数据变化时执行异步或开销较大的操作。</p>
</li>
<li>
<p>本质上侦听属性也是基于 <code>Watcher</code> 实现的，它是一个 <code>user watcher</code></p>
</li>
<li>
<p>watcher 初始化流程：</p>
<p><img src="./../flow-graph/watcher初始化.png" alt="" /></p>
</li>
<li>
<p>源码分析：</p>
<ul>
<li>
<p>侦听属性的初始化也是发生在 Vue 的实例初始化阶段的 <code>initState</code> 函数中，在 <code>computed</code> 初始化之后，执行了：</p>
<div><pre><code><span>if</span> <span>(</span>opts<span>.</span>watch <span>&amp;&amp;</span> opts<span>.</span>watch <span>!==</span> nativeWatch<span>)</span> <span>{</span>
  <span>initWatch</span><span>(</span>vm<span>,</span> opts<span>.</span>watch<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><p><code>initWatch</code> 函数：它的定义在 <code>src/core/instance/state.js</code> 中：</p>
<div><pre><code><span>function</span> <span>initWatch</span> <span>(</span><span>vm<span>:</span> Component<span>,</span> watch<span>:</span> Object</span><span>)</span> <span>{</span>
  <span>for</span> <span>(</span><span>const</span> key <span>in</span> watch<span>)</span> <span>{</span>
    <span>const</span> handler <span>=</span> watch<span>[</span>key<span>]</span>
    <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>handler<span>)</span><span>)</span> <span>{</span>
      <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>;</span> i <span>&lt;</span> handler<span>.</span>length<span>;</span> i<span>++</span><span>)</span> <span>{</span>
        <span>createWatcher</span><span>(</span>vm<span>,</span> key<span>,</span> handler<span>[</span>i<span>]</span><span>)</span>
      <span>}</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>createWatcher</span><span>(</span>vm<span>,</span> key<span>,</span> handler<span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br></div></div><p>这里就是对 <code>watch</code> 对象做遍历，拿到每一个 <code>handler</code>，因为 Vue 是支持 <code>watch</code> 的同一个 <code>key</code> 对应多个 <code>handler</code>，所以如果 <code>handler</code> 是一个数组，则遍历这个数组，调用 <code>createWatcher</code> 方法，否则直接调用 <code>createWatcher</code>：</p>
<div><pre><code><span>function</span> <span>createWatcher</span> <span>(</span>
  <span>vm<span>:</span> Component<span>,</span>
  expOrFn<span>:</span> string <span>|</span> Function<span>,</span>
  handler<span>:</span> any<span>,</span>
  options<span>?</span><span>:</span> Object</span>
<span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>isPlainObject</span><span>(</span>handler<span>)</span><span>)</span> <span>{</span>
    options <span>=</span> handler
    handler <span>=</span> handler<span>.</span>handler
  <span>}</span>
  <span>if</span> <span>(</span><span>typeof</span> handler <span>===</span> <span>'string'</span><span>)</span> <span>{</span>
    handler <span>=</span> vm<span>[</span>handler<span>]</span>
  <span>}</span>
  <span>return</span> vm<span>.</span><span>$watch</span><span>(</span>expOrFn<span>,</span> handler<span>,</span> options<span>)</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div><p>这里的逻辑也很简单，首先对 <code>hanlder</code> 的类型做判断，拿到它最终的回调函数，最后调用 <code>vm.$watch(keyOrFn, handler, options)</code> 函数，<code>$watch</code> 是 Vue 原型上的方法，它是在执行 <code>stateMixin</code> 的时候定义的：</p>
<div><pre><code><span>Vue</span><span>.</span>prototype<span>.</span><span>$watch</span> <span>=</span> <span>function</span> <span>(</span>
  <span>expOrFn<span>:</span> string <span>|</span> Function<span>,</span>
  cb<span>:</span> any<span>,</span>
  options<span>?</span><span>:</span> Object</span>
<span>)</span><span>:</span> Function <span>{</span>
  <span>const</span> vm<span>:</span> Component <span>=</span> <span>this</span>
  <span>if</span> <span>(</span><span>isPlainObject</span><span>(</span>cb<span>)</span><span>)</span> <span>{</span>
    <span>return</span> <span>createWatcher</span><span>(</span>vm<span>,</span> expOrFn<span>,</span> cb<span>,</span> options<span>)</span>
  <span>}</span>
  options <span>=</span> options <span>||</span> <span>{</span><span>}</span>
  options<span>.</span>user <span>=</span> <span>true</span>
  <span>const</span> watcher <span>=</span> <span>new</span> <span>Watcher</span><span>(</span>vm<span>,</span> expOrFn<span>,</span> cb<span>,</span> options<span>)</span>
  <span>if</span> <span>(</span>options<span>.</span>immediate<span>)</span> <span>{</span>
    <span>cb</span><span>.</span><span>call</span><span>(</span>vm<span>,</span> watcher<span>.</span>value<span>)</span>
  <span>}</span>
  <span>return</span> <span>function</span> <span>unwatchFn</span> <span>(</span><span>)</span> <span>{</span>
    watcher<span>.</span><span>teardown</span><span>(</span><span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div><ul>
<li>侦听属性 <code>watch</code> 最终会调用 <code>$watch</code> 方法，这个方法首先判断 <code>cb</code> 如果是一个对象，则调用 <code>createWatcher</code> 方法，这是因为 <code>$watch</code> 方法是用户可以直接调用的，它可以传递一个对象，也可以传递函数。</li>
<li>接着执行 <code>const watcher = new Watcher(vm, expOrFn, cb, options)</code> 实例化了一个 <code>watcher</code>，这里需要注意一点这是一个 <code>user watcher</code>，因为 <code>options.user = true</code>。通过实例化 <code>watcher</code> 的方式，一旦我们 <code>watch</code> 的数据发送变化，它最终会执行 <code>watcher</code> 的 <code>run</code> 方法，执行回调函数 <code>cb</code>，并且如果我们设置了 <code>immediate</code> 为 true，则直接会执行回调函数 <code>cb</code>。</li>
<li>最后返回了一个 <code>unwatchFn</code> 方法，它会调用 <code>teardown</code> 方法去移除这个 <code>watcher</code>。</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>watcher 对象</strong></p>
<ul>
<li>
<p>Watcher 对象：进行依赖收集的观察者，同时在数据变更时通知响应数据改变。</p>
</li>
<li>
<div><pre><code><span>/* A watcher parses an expression, collects dependencies,
 * and fires callback when the expression value changes.
 * This is used for both the $watch() api and directives.
  一个解析表达式，进行依赖收集的观察者，同时在表达式数据变更时触发回调函数。它被用于$watch api以及指令
 */</span>
<span>export</span> <span>default</span> <span>class</span> <span>Watcher</span> <span>{</span>
  vm<span>:</span> Component<span>;</span>
  expression<span>:</span> string<span>;</span>
  cb<span>:</span> Function<span>;</span>
  id<span>:</span> number<span>;</span>
  deep<span>:</span> boolean<span>;</span>
  user<span>:</span> boolean<span>;</span>
  lazy<span>:</span> boolean<span>;</span>
  sync<span>:</span> boolean<span>;</span>
  dirty<span>:</span> boolean<span>;</span><span>//表示数据是否被读取</span>
  active<span>:</span> boolean<span>;</span>
  deps<span>:</span> Array<span>&lt;</span>Dep<span>></span><span>;</span>
  newDeps<span>:</span> Array<span>&lt;</span>Dep<span>></span><span>;</span>
  depIds<span>:</span> ISet<span>;</span>
  newDepIds<span>:</span> ISet<span>;</span>
  getter<span>:</span> Function<span>;</span>
  value<span>:</span> any<span>;</span>

  <span>constructor</span> <span>(</span>
    vm<span>:</span> Component<span>,</span>
    expOrFn<span>:</span> string <span>|</span> Function<span>,</span> <span>//在new Watcher(vm, updateComponent, noop, {}) 中对应 updateComponent() 方法</span>
    cb<span>:</span> Function<span>,</span>
    options<span>?</span><span>:</span> Object
  <span>)</span> <span>{</span>
    <span>this</span><span>.</span>vm <span>=</span> vm
    <span>/*_watchers存放观察者实例*/</span>
    vm<span>.</span>_watchers<span>.</span><span>push</span><span>(</span><span>this</span><span>)</span>
    <span>// options</span>
    <span>if</span> <span>(</span>options<span>)</span> <span>{</span> <span>//Watcher 类型选项</span>
      <span>this</span><span>.</span>deep <span>=</span> <span>!</span><span>!</span>options<span>.</span>deep <span>// deep Watcher 标记</span>
      <span>this</span><span>.</span>user <span>=</span> <span>!</span><span>!</span>options<span>.</span>user  <span>// user Watcher 标记</span>
      <span>this</span><span>.</span>lazy <span>=</span> <span>!</span><span>!</span>options<span>.</span>lazy <span>// computed watcher 标记</span>
      <span>this</span><span>.</span>sync <span>=</span> <span>!</span><span>!</span>options<span>.</span>sync <span>// sync watcher 标记 （vue数据更新是异步的，当响应式数据发送变化后，触发了 watcher.update()，只是把这个 watcher 推送到一个队列中，在 nextTick 后才会真正执行 watcher 的回调函数。而一旦我们设置了 sync，就可以在当前 Tick 中同步执行 watcher 的回调函数）</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>this</span><span>.</span>deep <span>=</span> <span>this</span><span>.</span>user <span>=</span> <span>this</span><span>.</span>lazy <span>=</span> <span>this</span><span>.</span>sync <span>=</span> <span>false</span>
    <span>}</span>
    <span>this</span><span>.</span>cb <span>=</span> cb
    <span>this</span><span>.</span>id <span>=</span> <span>++</span>uid <span>// uid for batching</span>
    <span>this</span><span>.</span>active <span>=</span> <span>true</span>
    <span>this</span><span>.</span>dirty <span>=</span> <span>this</span><span>.</span>lazy <span>// for lazy watchers</span>
    <span>this</span><span>.</span>deps <span>=</span> <span>[</span><span>]</span> <span>//当数据发生更新时候的旧数据的依赖watcher</span>
    <span>this</span><span>.</span>newDeps <span>=</span> <span>[</span><span>]</span> <span>//当数据发生更新时候的新数据的依赖watcher</span>
    <span>this</span><span>.</span>depIds <span>=</span> <span>new</span> <span>Set</span><span>(</span><span>)</span>
    <span>this</span><span>.</span>newDepIds <span>=</span> <span>new</span> <span>Set</span><span>(</span><span>)</span>
    <span>this</span><span>.</span>expression <span>=</span> process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span>
      <span>?</span> expOrFn<span>.</span><span>toString</span><span>(</span><span>)</span>
      <span>:</span> <span>''</span>
    <span>// parse expression for getter</span>
    <span>/*把表达式expOrFn参数解析成getter*/</span>
    <span>if</span> <span>(</span><span>typeof</span> expOrFn <span>===</span> <span>'function'</span><span>)</span> <span>{</span>
      <span>this</span><span>.</span>getter <span>=</span> expOrFn
    <span>}</span> <span>else</span> <span>{</span>
      <span>this</span><span>.</span>getter <span>=</span> <span>parsePath</span><span>(</span>expOrFn<span>)</span>
      <span>if</span> <span>(</span><span>!</span><span>this</span><span>.</span>getter<span>)</span> <span>{</span>
        <span>this</span><span>.</span><span>getter</span> <span>=</span> <span>function</span> <span>(</span><span>)</span> <span>{</span><span>}</span>
        process<span>.</span>env<span>.</span><span>NODE_ENV</span> <span>!==</span> <span>'production'</span> <span>&amp;&amp;</span> <span>warn</span><span>(</span>
          <span><span>`</span><span>Failed watching path: "</span><span><span>${</span>expOrFn<span>}</span></span><span>" </span><span>`</span></span> <span>+</span>
          <span>'Watcher only accepts simple dot-delimited paths. '</span> <span>+</span>
          <span>'For full control, use a function instead.'</span><span>,</span>
          vm
        <span>)</span>
      <span>}</span>
    <span>}</span>
    <span>this</span><span>.</span>value <span>=</span> <span>this</span><span>.</span>lazy
      <span>?</span> <span>undefined</span>
      <span>:</span> <span>this</span><span>.</span><span>get</span><span>(</span><span>)</span>
  <span>}</span>

  <span>/**
   * Evaluate the getter, and re-collect dependencies.
   */</span>
  <span>/*获得getter的值并且重新进行依赖收集*/</span>
  <span>get</span> <span>(</span><span>)</span> <span>{</span>
    <span>/*将自身watcher观察者实例设置给Dep.target，用以依赖收集*/</span>
    <span>pushTarget</span><span>(</span><span>this</span><span>)</span>
    <span>let</span> value
    <span>const</span> vm <span>=</span> <span>this</span><span>.</span>vm

    <span>/*
      执行了getter操作，看似执行了渲染操作，其实是执行了依赖收集。
      在将Dep.target设置为自生观察者实例以后，执行getter操作。
      譬如说现在的的data中可能有a、b、c三个数据，getter渲染需要依赖a跟c，
      那么在执行getter的时候就会触发a跟c两个数据的getter函数，
      在getter函数中即可判断Dep.target是否存在然后完成依赖收集，
      将该观察者对象放入闭包中的Dep的subs中去。
    */</span>
    <span>if</span> <span>(</span><span>this</span><span>.</span>user<span>)</span> <span>{</span>
      <span>try</span> <span>{</span>
        value <span>=</span> <span>this</span><span>.</span><span>getter</span><span>.</span><span>call</span><span>(</span>vm<span>,</span> vm<span>)</span> <span>//this.getter 对应就是 updateComponent 函数()</span>
      <span>}</span> <span>catch</span> <span>(</span>e<span>)</span> <span>{</span>
        <span>handleError</span><span>(</span>e<span>,</span> vm<span>,</span> <span><span>`</span><span>getter for watcher "</span><span><span>${</span><span>this</span><span>.</span>expression<span>}</span></span><span>"</span><span>`</span></span><span>)</span>
      <span>}</span>
    <span>}</span> <span>else</span> <span>{</span>
      value <span>=</span> <span>this</span><span>.</span><span>getter</span><span>.</span><span>call</span><span>(</span>vm<span>,</span> vm<span>)</span>
    <span>}</span>
    <span>// "touch" every property so they are all tracked as</span>
    <span>// dependencies for deep watching</span>
    <span>/*如果存在deep，则触发每个深层对象的依赖，追踪其变化*/</span>
    <span>if</span> <span>(</span><span>this</span><span>.</span>deep<span>)</span> <span>{</span>
      <span>/*递归每一个对象或者数组，触发它们的getter，使得对象或数组的每一个成员都被依赖收集，形成一个“深（deep）”依赖关系*/</span>
      <span>traverse</span><span>(</span>value<span>)</span>
    <span>}</span>

    <span>/*将观察者实例从target栈中取出并设置给Dep.target*/</span>
    <span>popTarget</span><span>(</span><span>)</span>
    <span>this</span><span>.</span><span>cleanupDeps</span><span>(</span><span>)</span>
    <span>return</span> value
  <span>}</span>

  <span>/**
   * Add a dependency to this directive.
   */</span>
   <span>/*添加一个依赖关系到Deps集合中*/</span>
  <span>addDep</span> <span>(</span><span>dep<span>:</span> Dep</span><span>)</span> <span>{</span>
    <span>const</span> id <span>=</span> dep<span>.</span>id
    <span>if</span> <span>(</span><span>!</span><span>this</span><span>.</span>newDepIds<span>.</span><span>has</span><span>(</span>id<span>)</span><span>)</span> <span>{</span>
      <span>this</span><span>.</span>newDepIds<span>.</span><span>add</span><span>(</span>id<span>)</span>
      <span>this</span><span>.</span>newDeps<span>.</span><span>push</span><span>(</span>dep<span>)</span>
      <span>if</span> <span>(</span><span>!</span><span>this</span><span>.</span>depIds<span>.</span><span>has</span><span>(</span>id<span>)</span><span>)</span> <span>{</span>
        dep<span>.</span><span>addSub</span><span>(</span><span>this</span><span>)</span>
      <span>}</span>
    <span>}</span>
  <span>}</span>

  <span>/**
   * Clean up for dependency collection.
   */</span>
  <span>/*清理依赖收集
    目的：在重新渲染时候清除更新后视图没有依赖的Watcher对象
  */</span>
  <span>cleanupDeps</span> <span>(</span><span>)</span> <span>{</span>
    <span>/*移除所有观察者对象*/</span>
    <span>let</span> i <span>=</span> <span>this</span><span>.</span>deps<span>.</span>length
    <span>while</span> <span>(</span>i<span>--</span><span>)</span> <span>{</span>
      <span>const</span> dep <span>=</span> <span>this</span><span>.</span>deps<span>[</span>i<span>]</span>
      <span>/*移除更新后没有订阅的数据*/</span>
      <span>if</span> <span>(</span><span>!</span><span>this</span><span>.</span>newDepIds<span>.</span><span>has</span><span>(</span>dep<span>.</span>id<span>)</span><span>)</span> <span>{</span>
        dep<span>.</span><span>removeSub</span><span>(</span><span>this</span><span>)</span>
      <span>}</span>
    <span>}</span>
    <span>let</span> tmp <span>=</span> <span>this</span><span>.</span>depIds
    <span>this</span><span>.</span>depIds <span>=</span> <span>this</span><span>.</span>newDepIds
    <span>this</span><span>.</span>newDepIds <span>=</span> tmp
    <span>this</span><span>.</span>newDepIds<span>.</span><span>clear</span><span>(</span><span>)</span>
    tmp <span>=</span> <span>this</span><span>.</span>deps
    <span>this</span><span>.</span>deps <span>=</span> <span>this</span><span>.</span>newDeps
    <span>this</span><span>.</span>newDeps <span>=</span> tmp
    <span>this</span><span>.</span>newDeps<span>.</span>length <span>=</span> <span>0</span>
  <span>}</span>

  <span>/**
   * Subscriber interface.
   * Will be called when a dependency changes.
   */</span>
  <span>/*
    订阅者接口，当依赖发生改变的时候进行回调
   */</span>
  <span>update</span> <span>(</span><span>)</span> <span>{</span>
    <span>/* istanbul ignore else */</span>
    <span>if</span> <span>(</span><span>this</span><span>.</span>lazy<span>)</span> <span>{</span>
      <span>this</span><span>.</span>dirty <span>=</span> <span>true</span>
    <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>this</span><span>.</span>sync<span>)</span> <span>{</span> <span>//sync Watcher</span>
      <span>/*同步则执行run直接渲染视图*/</span>
      <span>this</span><span>.</span><span>run</span><span>(</span><span>)</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>/*异步推送到观察者队列中，下一个tick时调用。*/</span>
      <span>queueWatcher</span><span>(</span><span>this</span><span>)</span>
    <span>}</span>
  <span>}</span>

  <span>/**
   * Scheduler job interface.
   * Will be called by the scheduler.
   */</span>
  <span>/*
      调度者工作接口，将被调度者回调。
    */</span>
  <span>run</span> <span>(</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span><span>this</span><span>.</span>active<span>)</span> <span>{</span>
      <span>/* 对于渲染 watcher 而言，它在执行 this.get() 方法求值的时候，会执行 getter 方法
      (getter方法实际为updateComponents方法，该方法首先调用_render()方法渲染Vnode,然后调用_update()方法更新dom)
      所以这就是当我们去修改组件相关的响应式数据的时候，会触发组件重新渲染的原因，
      接着就会重新执行 patch 的过程，但它和首次渲染有所不同 */</span>
      <span>const</span> value <span>=</span> <span>this</span><span>.</span><span>get</span><span>(</span><span>)</span><span>//获取当前的值</span>
      <span>if</span> <span>(</span>
        value <span>!==</span> <span>this</span><span>.</span>value <span>||</span>
        <span>// Deep watchers and watchers on Object/Arrays should fire even</span>
        <span>// when the value is the same, because the value may</span>
        <span>// have mutated.</span>
        <span>/*
        如果满足新旧值不等、新值是对象类型、deep 模式任何一个条件
        即便值相同，拥有Deep属性的观察者以及在对象／数组上的观察者应该被触发更新，因为它们的值可能发生改变。
        */</span>
        <span>isObject</span><span>(</span>value<span>)</span> <span>||</span>
        <span>this</span><span>.</span>deep
      <span>)</span> <span>{</span>
        <span>// set new value</span>
        <span>const</span> oldValue <span>=</span> <span>this</span><span>.</span>value
        <span>/*设置新的值*/</span>
        <span>this</span><span>.</span>value <span>=</span> value

        <span>/*触发回调*/</span>
        <span>if</span> <span>(</span><span>this</span><span>.</span>user<span>)</span> <span>{</span>
          <span>try</span> <span>{</span>
            <span>//该回调就是定义watch对向时的回调</span>
            <span>this</span><span>.</span><span>cb</span><span>.</span><span>call</span><span>(</span><span>this</span><span>.</span>vm<span>,</span> value<span>,</span> oldValue<span>)</span>
          <span>}</span> <span>catch</span> <span>(</span>e<span>)</span> <span>{</span>
            <span>handleError</span><span>(</span>e<span>,</span> <span>this</span><span>.</span>vm<span>,</span> <span><span>`</span><span>callback for watcher "</span><span><span>${</span><span>this</span><span>.</span>expression<span>}</span></span><span>"</span><span>`</span></span><span>)</span>
          <span>}</span>
        <span>}</span> <span>else</span> <span>{</span>
          <span>this</span><span>.</span><span>cb</span><span>.</span><span>call</span><span>(</span><span>this</span><span>.</span>vm<span>,</span> value<span>,</span> oldValue<span>)</span>
        <span>}</span>
      <span>}</span>
    <span>}</span>
  <span>}</span>

  <span>/**
   * Evaluate the value of the watcher.
   * This only gets called for lazy watchers.
   */</span>
<span>/*获取观察者的值*/</span>
  <span>evaluate</span> <span>(</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>value <span>=</span> <span>this</span><span>.</span><span>get</span><span>(</span><span>)</span>
    <span>this</span><span>.</span>dirty <span>=</span> <span>false</span>
  <span>}</span>

  <span>/**
   * Depend on all deps collected by this watcher.
   */</span>
   <span>/*收集该watcher的所有deps依赖*/</span>
  <span>depend</span> <span>(</span><span>)</span> <span>{</span>
    <span>let</span> i <span>=</span> <span>this</span><span>.</span>deps<span>.</span>length
    <span>while</span> <span>(</span>i<span>--</span><span>)</span> <span>{</span>
      <span>this</span><span>.</span>deps<span>[</span>i<span>]</span><span>.</span><span>depend</span><span>(</span><span>)</span>
    <span>}</span>
  <span>}</span>

  <span>/**
   * Remove self from all dependencies' subscriber list.
   */</span>
   <span>/*将自身从所有依赖收集订阅列表删除*/</span>
  <span>teardown</span> <span>(</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span><span>this</span><span>.</span>active<span>)</span> <span>{</span>
      <span>// remove self from vm's watcher list</span>
      <span>// this is a somewhat expensive operation so we skip it</span>
      <span>// if the vm is being destroyed.</span>
      <span>/*从vm实例的观察者列表中将自身移除，由于该操作比较耗费资源，所以如果vm实例正在被销毁则跳过该步骤。*/</span>
      <span>if</span> <span>(</span><span>!</span><span>this</span><span>.</span>vm<span>.</span>_isBeingDestroyed<span>)</span> <span>{</span>
        <span>remove</span><span>(</span><span>this</span><span>.</span>vm<span>.</span>_watchers<span>,</span> <span>this</span><span>)</span>
      <span>}</span>
      <span>let</span> i <span>=</span> <span>this</span><span>.</span>deps<span>.</span>length
      <span>while</span> <span>(</span>i<span>--</span><span>)</span> <span>{</span>
        <span>this</span><span>.</span>deps<span>[</span>i<span>]</span><span>.</span><span>removeSub</span><span>(</span><span>this</span><span>)</span>
      <span>}</span>
      <span>this</span><span>.</span>active <span>=</span> <span>false</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br><span>71</span><br><span>72</span><br><span>73</span><br><span>74</span><br><span>75</span><br><span>76</span><br><span>77</span><br><span>78</span><br><span>79</span><br><span>80</span><br><span>81</span><br><span>82</span><br><span>83</span><br><span>84</span><br><span>85</span><br><span>86</span><br><span>87</span><br><span>88</span><br><span>89</span><br><span>90</span><br><span>91</span><br><span>92</span><br><span>93</span><br><span>94</span><br><span>95</span><br><span>96</span><br><span>97</span><br><span>98</span><br><span>99</span><br><span>100</span><br><span>101</span><br><span>102</span><br><span>103</span><br><span>104</span><br><span>105</span><br><span>106</span><br><span>107</span><br><span>108</span><br><span>109</span><br><span>110</span><br><span>111</span><br><span>112</span><br><span>113</span><br><span>114</span><br><span>115</span><br><span>116</span><br><span>117</span><br><span>118</span><br><span>119</span><br><span>120</span><br><span>121</span><br><span>122</span><br><span>123</span><br><span>124</span><br><span>125</span><br><span>126</span><br><span>127</span><br><span>128</span><br><span>129</span><br><span>130</span><br><span>131</span><br><span>132</span><br><span>133</span><br><span>134</span><br><span>135</span><br><span>136</span><br><span>137</span><br><span>138</span><br><span>139</span><br><span>140</span><br><span>141</span><br><span>142</span><br><span>143</span><br><span>144</span><br><span>145</span><br><span>146</span><br><span>147</span><br><span>148</span><br><span>149</span><br><span>150</span><br><span>151</span><br><span>152</span><br><span>153</span><br><span>154</span><br><span>155</span><br><span>156</span><br><span>157</span><br><span>158</span><br><span>159</span><br><span>160</span><br><span>161</span><br><span>162</span><br><span>163</span><br><span>164</span><br><span>165</span><br><span>166</span><br><span>167</span><br><span>168</span><br><span>169</span><br><span>170</span><br><span>171</span><br><span>172</span><br><span>173</span><br><span>174</span><br><span>175</span><br><span>176</span><br><span>177</span><br><span>178</span><br><span>179</span><br><span>180</span><br><span>181</span><br><span>182</span><br><span>183</span><br><span>184</span><br><span>185</span><br><span>186</span><br><span>187</span><br><span>188</span><br><span>189</span><br><span>190</span><br><span>191</span><br><span>192</span><br><span>193</span><br><span>194</span><br><span>195</span><br><span>196</span><br><span>197</span><br><span>198</span><br><span>199</span><br><span>200</span><br><span>201</span><br><span>202</span><br><span>203</span><br><span>204</span><br><span>205</span><br><span>206</span><br><span>207</span><br><span>208</span><br><span>209</span><br><span>210</span><br><span>211</span><br><span>212</span><br><span>213</span><br><span>214</span><br><span>215</span><br><span>216</span><br><span>217</span><br><span>218</span><br><span>219</span><br><span>220</span><br><span>221</span><br><span>222</span><br><span>223</span><br><span>224</span><br><span>225</span><br><span>226</span><br><span>227</span><br><span>228</span><br><span>229</span><br><span>230</span><br><span>231</span><br><span>232</span><br><span>233</span><br><span>234</span><br><span>235</span><br><span>236</span><br><span>237</span><br><span>238</span><br><span>239</span><br><span>240</span><br><span>241</span><br><span>242</span><br><span>243</span><br><span>244</span><br><span>245</span><br><span>246</span><br><span>247</span><br><span>248</span><br><span>249</span><br><span>250</span><br><span>251</span><br><span>252</span><br><span>253</span><br><span>254</span><br><span>255</span><br><span>256</span><br><span>257</span><br><span>258</span><br><span>259</span><br><span>260</span><br><span>261</span><br><span>262</span><br><span>263</span><br></div></div></li>
<li>
<p>根据 options 传入选项，Watcher 对象有四中类型。</p>
<ul>
<li><code>deep watcher</code>： 对象做深度观测；</li>
<li><code>user watcher</code>: 通过 <code>vm.$watch</code> 创建的 <code>watcher</code> ;</li>
<li><code>computed watcher</code> : watch 计算属性;</li>
<li><code>sync watcher</code> : 同步执行 <code>watcher</code></li>
</ul>
</li>
<li>
<p><strong>deep watcher</strong></p>
<p>通常，如果我们想对一下对象做深度观测的时候，需要设置这个属性为 true，考虑到这种情况：</p>
<div><pre><code><span>var</span> vm <span>=</span> <span>new</span> <span>Vue</span><span>(</span><span>{</span>
  <span>data</span><span>(</span><span>)</span> <span>{</span>
    a<span>:</span> <span>{</span>
      b<span>:</span> <span>1</span>
    <span>}</span>
  <span>}</span><span>,</span>
  watch<span>:</span> <span>{</span>
    a<span>:</span> <span>{</span>
      <span>handler</span><span>(</span><span>newVal</span><span>)</span> <span>{</span>
        console<span>.</span><span>log</span><span>(</span>newVal<span>)</span>
      <span>}</span>
    <span>}</span>
  <span>}</span>
<span>}</span><span>)</span>
vm<span>.</span>a<span>.</span>b <span>=</span> <span>2</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div><p>这个时候是不会 log 任何数据的，因为我们是 watch 了 <code>a</code> 对象，只触发了 <code>a</code> 的 getter，并没有触发 <code>a.b</code> 的 getter，所以并没有订阅它的变化，导致我们对 <code>vm.a.b = 2</code> 赋值的时候，虽然触发了 setter，但没有可通知的对象，所以也并不会触发 watch 的回调函数了。</p>
<p>而我们只需要对代码做稍稍修改，就可以观测到这个变化了</p>
<div><pre><code>watch<span>:</span> <span>{</span>
  a<span>:</span> <span>{</span>
    deep<span>:</span> <span>true</span><span>,</span>
    <span>handler</span><span>(</span><span>newVal</span><span>)</span> <span>{</span>
      console<span>.</span><span>log</span><span>(</span>newVal<span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br></div></div><p>这样就创建了一个 <code>deep watcher</code> 了，在 <code>watcher</code> 执行 <code>get</code> 求值的过程中有一段逻辑：</p>
<div><pre><code><span>get</span><span>(</span><span>)</span> <span>{</span>
  <span>let</span> value <span>=</span> <span>this</span><span>.</span><span>getter</span><span>.</span><span>call</span><span>(</span>vm<span>,</span> vm<span>)</span>
  <span>// ...</span>
  <span>if</span> <span>(</span><span>this</span><span>.</span>deep<span>)</span> <span>{</span>
    <span>traverse</span><span>(</span>value<span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div><p>在对 watch 的表达式或者函数求值后，会调用 <code>traverse</code> 函数，它的定义在 <code>src/core/observer/traverse.js</code> 中：</p>
<div><pre><code> <span>/*递归每一个对象或者数组，触发它们的getter，使得对象或数组的每一个成员都被依赖收集，形成一个“深（deep）”依赖关系*/</span>

 <span>/*用来存放Oberser实例等id，避免重复读取*/</span>
<span>const</span> seenObjects <span>=</span> <span>new</span> <span>Set</span><span>(</span><span>)</span>
<span>function</span> <span>traverse</span> <span>(</span><span>val<span>:</span> any</span><span>)</span> <span>{</span>
  seenObjects<span>.</span><span>clear</span><span>(</span><span>)</span>
  <span>_traverse</span><span>(</span>val<span>,</span> seenObjects<span>)</span>
<span>}</span>

<span>function</span> <span>_traverse</span> <span>(</span><span>val<span>:</span> any<span>,</span> seen<span>:</span> ISet</span><span>)</span> <span>{</span>
  <span>let</span> i<span>,</span> keys
  <span>const</span> isA <span>=</span> Array<span>.</span><span>isArray</span><span>(</span>val<span>)</span>
  <span>/*非对象或数组或是不可扩展对象直接return，不需要收集深层依赖关系。*/</span>
  <span>if</span> <span>(</span><span>(</span><span>!</span>isA <span>&amp;&amp;</span> <span>!</span><span>isObject</span><span>(</span>val<span>)</span><span>)</span> <span>||</span> <span>!</span>Object<span>.</span><span>isExtensible</span><span>(</span>val<span>)</span><span>)</span> <span>{</span>
    <span>return</span>
  <span>}</span>
  <span>if</span> <span>(</span>val<span>.</span>__ob__<span>)</span> <span>{</span>
    <span>/*把子响应式对象通过它们的 dep id 记录到 seenObjects，避免以后重复访问*/</span>
    <span>const</span> depId <span>=</span> val<span>.</span>__ob__<span>.</span>dep<span>.</span>id
    <span>if</span> <span>(</span>seen<span>.</span><span>has</span><span>(</span>depId<span>)</span><span>)</span> <span>{</span>
      <span>return</span>
    <span>}</span>
    seen<span>.</span><span>add</span><span>(</span>depId<span>)</span>
  <span>}</span>

  <span>/*递归对象及数组*/</span>
  <span>if</span> <span>(</span>isA<span>)</span> <span>{</span>
    i <span>=</span> val<span>.</span>length
    <span>while</span> <span>(</span>i<span>--</span><span>)</span> <span>_traverse</span><span>(</span>val<span>[</span>i<span>]</span><span>,</span> seen<span>)</span>
  <span>}</span> <span>else</span> <span>{</span>
    keys <span>=</span> Object<span>.</span><span>keys</span><span>(</span>val<span>)</span>
    i <span>=</span> keys<span>.</span>length
    <span>while</span> <span>(</span>i<span>--</span><span>)</span> <span>_traverse</span><span>(</span>val<span>[</span>keys<span>[</span>i<span>]</span><span>]</span><span>,</span> seen<span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br></div></div><p><code>traverse</code> 的逻辑也很简单，它实际上就是对一个对象做深层递归遍历，因为遍历过程中就是对一个子对象的访问，会触发它们的 getter 过程，这样就可以收集到依赖，也就是订阅它们变化的 <code>watcher</code>，这个函数实现还有一个小的优化，遍历过程中会把子响应式对象通过它们的 <code>dep id</code> 记录到 <code>seenObjects</code>，避免以后重复访问。</p>
<p>那么在执行了 <code>traverse</code> 后，我们再对 watch 的对象内部任何一个值做修改，也会调用 <code>watcher</code> 的回调函数了。</p>
<p>对 <code>deep watcher</code> 的理解非常重要，今后工作中如果大家观测了一个复杂对象，并且会改变对象内部深层某个值的时候也希望触发回调，一定要设置 <code>deep</code> 为 true，但是因为设置了 <code>deep</code> 后会执行 <code>traverse</code> 函数，会有一定的性能开销，所以一定要根据应用场景权衡是否要开启这个配置。</p>
</li>
<li>
<p><strong>user watcher</strong></p>
<p>通过 <code>vm.$watch</code> 创建的 <code>watcher</code> 是一个 <code>user watcher</code>，其实它的功能很简单，在对 <code>watcher</code> 求值以及在执行回调函数的时候，会处理一下错误，如下：</p>
<div><pre><code><span>get</span><span>(</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>this</span><span>.</span>user<span>)</span> <span>{</span>
    <span>handleError</span><span>(</span>e<span>,</span> vm<span>,</span> <span><span>`</span><span>getter for watcher "</span><span><span>${</span><span>this</span><span>.</span>expression<span>}</span></span><span>"</span><span>`</span></span><span>)</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>throw</span> e
  <span>}</span>
<span>}</span><span>,</span>
<span>getAndInvoke</span><span>(</span><span>)</span> <span>{</span>
  <span>// ...</span>
  <span>if</span> <span>(</span><span>this</span><span>.</span>user<span>)</span> <span>{</span>
    <span>try</span> <span>{</span>
      <span>this</span><span>.</span><span>cb</span><span>.</span><span>call</span><span>(</span><span>this</span><span>.</span>vm<span>,</span> value<span>,</span> oldValue<span>)</span>
    <span>}</span> <span>catch</span> <span>(</span>e<span>)</span> <span>{</span>
      <span>handleError</span><span>(</span>e<span>,</span> <span>this</span><span>.</span>vm<span>,</span> <span><span>`</span><span>callback for watcher "</span><span><span>${</span><span>this</span><span>.</span>expression<span>}</span></span><span>"</span><span>`</span></span><span>)</span>
    <span>}</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>this</span><span>.</span><span>cb</span><span>.</span><span>call</span><span>(</span><span>this</span><span>.</span>vm<span>,</span> value<span>,</span> oldValue<span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br></div></div><p><code>handleError</code> 在 Vue 中是一个错误捕获并且暴露给用户的一个利器</p>
</li>
<li>
<p><strong>sync watcher</strong></p>
<p>当响应式数据发送变化后，触发了 <code>watcher.update()</code>，只是把这个 <code>watcher</code> 推送到一个队列中，在 <code>nextTick</code> 后才会真正执行 <code>watcher</code> 的回调函数。而一旦我们设置了 <code>sync</code>，就可以在当前 <code>Tick</code> 中同步执行 <code>watcher</code> 的回调函数。</p>
<div><pre><code><span>update</span> <span>(</span><span>)</span> <span>{</span>
  <span>if</span> <span>(</span><span>this</span><span>.</span>computed<span>)</span> <span>{</span>
    <span>// ...</span>
  <span>}</span> <span>else</span> <span>if</span> <span>(</span><span>this</span><span>.</span>sync<span>)</span> <span>{</span>
    <span>this</span><span>.</span><span>run</span><span>(</span><span>)</span>
  <span>}</span> <span>else</span> <span>{</span>
    <span>queueWatcher</span><span>(</span><span>this</span><span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br></div></div><p>只有当我们需要 watch 的值的变化到执行 <code>watcher</code> 的回调函数是一个同步过程的时候才会去设置该属性为 true</p>
</li>
</ul>
<h2 id="指令"> 指令</h2>
<h3 id="v-if-v-show"> <a href="https://cn.vuejs.org/v2/api/#v-show" target="_blank" rel="noopener noreferrer">v-if &amp; v-show</a></h3>
<p><img src="./../images/if-show.png" alt="" /></p>
<ul>
<li><strong>共同点：</strong> 都能控制元素的显示和隐藏；</li>
<li><strong>不同点：</strong> 实现本质方法不同，<code>v-show</code> 本质就是通过控制 css 中的 <code>display</code> 设置为 <code>none</code>，控制隐藏，只会编译一次；<code>v-if</code> 是动态的向 DOM 树内添加或者删除 DOM 元素，若初始值为 <code>false</code> ，就不会编译了。而且 <code>v-if</code> 不停的销毁和创建比较消耗性能。</li>
</ul>
<p><strong>v-if &amp; v-for</strong></p>
<ul>
<li>2.x 版本中在一个元素上同时使用 <code>v-if</code> 和 <code>v-for</code> 时，<code>v-for</code> 会优先作用。</li>
<li>3.x 版本中 <code>v-if</code> 总是优先于 <code>v-for</code> 生效。</li>
</ul>
<h3 id="v-model-双向绑定"> <a href="https://cn.vuejs.org/v2/api/#v-model" target="_blank" rel="noopener noreferrer">v-model 双向绑定</a></h3>
<p><img src="./../images/v-model2.png" alt="" /></p>
<ul>
<li>
<p>双向绑定：数据变化更新视图，视图变化更新数据</p>
<ul>
<li>
<p>输入框内容变化时，Data 中的数据同步变化。即 View =&gt; Data 的变化</p>
</li>
<li>
<p>Data 中的数据变化时，文本节点的内容同步变化。即 Data =&gt; View 的变化</p>
<p><img src="./../images/v-model.png" alt="" /></p>
</li>
</ul>
</li>
<li>
<p>本质上就是一种语法糖，它即可以支持原生表单元素，也可以支持自定义组件。</p>
<ul>
<li>
<p>原生的v-model，会根据标签的不同生成不同的事件和属性。</p>
</li>
<li>
<p>在组件的实现中，我们是可以配置子组件接收的 <code>prop</code> 名称，以及派发的 <code>event</code>属性来进行自定义</p>
<div><pre><code><span>let</span> Child <span>=</span> <span>{</span>
  template<span>:</span> <span>'&lt;div>'</span>
  <span>+</span> <span>'&lt;input :value="msg" @input="updateValue" placeholder="edit me">'</span> <span>+</span>
  <span>'&lt;/div>'</span><span>,</span>
  props<span>:</span> <span>[</span><span>'msg'</span><span>]</span><span>,</span>
  model<span>:</span> <span>{</span>
    prop<span>:</span> <span>'msg'</span><span>,</span> <span>//子组件 v-model 绑定的属性</span>
    event<span>:</span> <span>'change'</span> <span>// 子组件 v-model 触发的事件</span>
  <span>}</span><span>,</span>
  methods<span>:</span> <span>{</span>
    <span>updateValue</span><span>(</span><span>e</span><span>)</span> <span>{</span>
      <span>this</span><span>.</span><span>$emit</span><span>(</span><span>'change'</span><span>,</span> e<span>.</span>target<span>.</span>value<span>)</span>
    <span>}</span>
  <span>}</span>
<span>}</span>

<span>let</span> vm <span>=</span> <span>new</span> <span>Vue</span><span>(</span><span>{</span>
  el<span>:</span> <span>'#app'</span><span>,</span>
  template<span>:</span> <span>'&lt;div>'</span> <span>+</span>
  <span>'&lt;child v-model="message">&lt;/child>'</span> <span>+</span>
  <span>'&lt;p>Message is: {{ message }}&lt;/p>'</span> <span>+</span>
  <span>'&lt;/div>'</span><span>,</span>
  <span>data</span><span>(</span><span>)</span> <span>{</span>
    <span>return</span> <span>{</span>
      message<span>:</span> <span>''</span>
    <span>}</span>
  <span>}</span><span>,</span>
  components<span>:</span> <span>{</span>
    Child
  <span>}</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br></div></div></li>
</ul>
</li>
<li>
<p><a href="http://caibaojian.com/vue-analysis/extend/v-model.html#v-model" target="_blank" rel="noopener noreferrer">源码分析</a></p>
</li>
</ul>
<h2 id="实例方法-事件"> <a href="https://cn.vuejs.org/v2/api/#%E5%AE%9E%E4%BE%8B%E6%96%B9%E6%B3%95-%E4%BA%8B%E4%BB%B6" target="_blank" rel="noopener noreferrer">实例方法 / 事件</a></h2>
<h3 id="vue-事件机制"> VUE 事件机制</h3>
<ul>
<li>Vue 事件机制 本质上就是 一个 发布-订阅 模式的实现</li>
</ul>
<h3 id="修饰符"> 修饰符</h3>
<p><img src="./../images/修饰符.png" alt="" /></p>
<h3 id="手写-on-off-emit-once"> 手写$on,$off,$emit,$once</h3>
<div><pre><code><span>class</span> <span>Vue</span> <span>{</span>

  <span>constructor</span><span>(</span><span>)</span> <span>{</span>
    <span>//  事件通道调度中心</span>
    <span>this</span><span>.</span>_events <span>=</span> Object<span>.</span><span>create</span><span>(</span><span>null</span><span>)</span>
  <span>}</span>

  <span>$on</span><span>(</span><span>event<span>,</span> fn</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>event<span>)</span><span>)</span> <span>{</span> <span>//如果事件名为数组，递归添加事件</span>
      event<span>.</span><span>map</span><span>(</span><span>item</span> <span>=></span> <span>{</span>
        <span>this</span><span>.</span><span>$on</span><span>(</span>item<span>,</span> fn<span>)</span>
      <span>}</span><span>)</span>
    <span>}</span> <span>else</span> <span>{</span> <span>//向事件对象添加事件回调</span>
      <span>(</span><span>this</span><span>.</span>_events<span>[</span>event<span>]</span> <span>||</span> <span>(</span><span>this</span><span>.</span>_events<span>[</span>event<span>]</span> <span>=</span> <span>[</span><span>]</span><span>)</span><span>)</span><span>.</span><span>push</span><span>(</span>fn<span>)</span>
    <span>}</span>
    <span>return</span> <span>this</span>
  <span>}</span>

  <span>$once</span><span>(</span><span>event<span>,</span> fn</span><span>)</span> <span>{</span>
    <span>function</span> <span>on</span><span>(</span><span>)</span> <span>{</span>
      <span>this</span><span>.</span><span>$off</span><span>(</span>event<span>,</span> on<span>)</span>
      <span>fn</span><span>.</span><span>apply</span><span>(</span><span>this</span><span>,</span> arguments<span>)</span>
    <span>}</span>
    on<span>.</span>fn <span>=</span> fn
    <span>this</span><span>.</span><span>$on</span><span>(</span>event<span>,</span> on<span>)</span>
    <span>return</span> <span>this</span>
  <span>}</span>

  <span>$off</span><span>(</span><span>event<span>,</span> fn</span><span>)</span> <span>{</span>
    <span>if</span> <span>(</span><span>!</span>arguments<span>.</span>length<span>)</span> <span>{</span>
      <span>this</span><span>.</span>_events <span>=</span> Object<span>.</span><span>create</span><span>(</span><span>null</span><span>)</span>
      <span>return</span> <span>this</span>
    <span>}</span>
    <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>event<span>)</span><span>)</span> <span>{</span>
      event<span>.</span><span>map</span><span>(</span><span>item</span> <span>=></span> <span>{</span>
        <span>this</span><span>.</span><span>$off</span><span>(</span>item<span>,</span> fn<span>)</span>
      <span>}</span><span>)</span>
      <span>return</span> <span>this</span>
    <span>}</span>
    <span>const</span> cbs <span>=</span> <span>this</span><span>.</span>_events<span>[</span>event<span>]</span>
    <span>if</span> <span>(</span><span>!</span>cbs<span>)</span> <span>{</span>
      <span>return</span> <span>this</span>
    <span>}</span>
    <span>if</span> <span>(</span><span>!</span>fn<span>)</span> <span>{</span>
      <span>this</span><span>.</span>_events<span>[</span>event<span>]</span> <span>=</span> <span>null</span>
      <span>return</span> <span>this</span>
    <span>}</span>
    <span>let</span> cb
    <span>let</span> i <span>=</span> cbs<span>.</span>length
    <span>while</span> <span>(</span>i<span>--</span><span>)</span> <span>{</span>
      cb <span>=</span> cbs<span>[</span>i<span>]</span>
      <span>if</span> <span>(</span>cb <span>===</span> fn <span>||</span> cb<span>.</span>fn <span>===</span> fn<span>)</span> <span>{</span>
        cbs<span>.</span><span>splice</span><span>(</span>i<span>,</span> <span>1</span><span>)</span>
        <span>break</span>
      <span>}</span>
    <span>}</span>
    <span>return</span> <span>this</span>
  <span>}</span>
  
  <span>$emit</span><span>(</span><span>event</span><span>)</span> <span>{</span>
    <span>let</span> cbs <span>=</span> <span>this</span><span>.</span>_events<span>[</span>event<span>]</span>
    <span>if</span> <span>(</span>cbs<span>)</span> <span>{</span>
      <span>const</span> args <span>=</span> <span>[</span><span>]</span><span>.</span><span>slice</span><span>.</span><span>call</span><span>(</span>arguments<span>,</span> <span>1</span><span>)</span>
      cbs<span>.</span><span>map</span><span>(</span><span>item</span> <span>=></span> <span>{</span>
        args <span>?</span> <span>item</span><span>.</span><span>apply</span><span>(</span><span>this</span><span>,</span> args<span>)</span> <span>:</span> <span>item</span><span>.</span><span>call</span><span>(</span><span>this</span><span>)</span>
      <span>}</span><span>)</span>
    <span>}</span>
    <span>return</span> <span>this</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br><span>45</span><br><span>46</span><br><span>47</span><br><span>48</span><br><span>49</span><br><span>50</span><br><span>51</span><br><span>52</span><br><span>53</span><br><span>54</span><br><span>55</span><br><span>56</span><br><span>57</span><br><span>58</span><br><span>59</span><br><span>60</span><br><span>61</span><br><span>62</span><br><span>63</span><br><span>64</span><br><span>65</span><br><span>66</span><br><span>67</span><br><span>68</span><br><span>69</span><br><span>70</span><br></div></div><h2 id="特殊-attribute"> <a href="https://cn.vuejs.org/v2/api/#%E7%89%B9%E6%AE%8A-attribute" target="_blank" rel="noopener noreferrer">特殊 attribute</a></h2>
<h3 id="key"> <a href="https://cn.vuejs.org/v2/api/#key" target="_blank" rel="noopener noreferrer">key</a></h3>
<ul>
<li>
<p>作用:</p>
<ul>
<li>
<p>key 是给每一个 vnode 的唯一 id,依靠 key,我们的 diff 操作可以更准确、更快速。</p>
<ul>
<li>diff 算法的过程中,先会进行新旧节点的首尾交叉对比,当无法匹配的时候会通过 key 查找 VNode。</li>
</ul>
</li>
<li>
<p>更准确 : 因为带 key 就不是就地复用了 VNode,在 sameNode 函数 a.key === b.key 对比中可以避免就地复用的情况。所以会更加准确,如果不加 key,会导致之前节点的状态被保留下来,会产生一系列的 bug。</p>
</li>
<li>
<p>更快速 : key 的唯一性可以被 Map 数据结构充分利用,相比于遍历查找的时间复杂度 O(n),Map 的时间复杂度仅仅为 O(1)。</p>
</li>
<li>
<p>在v-if条件渲染中，用 key 避免组件复用:</p>
<ul>
<li>当我们使用 v-if 来实现元素切换的时候，如果切换前后含有相同类型的元素，那么这个元素就会被复用。</li>
<li>如果是相同的 input 元素，那么切换前后用户的输入不会被清除掉，这样是不符合需求的。</li>
<li>因此我们可以通过使用 key 来唯一的标识一个元素，这个情况下，使用 key 的元素不会被复用。</li>
<li>这个时候 key 的作用是用来标识一个独立的元素。</li>
</ul>
<p><img src="./../images/key4.png" alt="" /></p>
</li>
<li>
<p>在v-for列表渲染中,用key维护维护列表状态：</p>
<ul>
<li>
<p>当 Vue 正在更新使用 v-for 渲染的元素列表时，它默认使用“就地更新”的策略；</p>
</li>
<li>
<p>如果数据项的顺序被改变，Vue 将不会移动 DOM 元素来匹配数据项的顺序，而是简单复用此处的每个元素。</p>
</li>
<li>
<p>因此通过为每个列表项提供一个 key 值，来以便 Vue 跟踪元 素的身份，从而高效的实现复用。</p>
</li>
<li>
<p>这个时候 key 的作用是为了高效的更新渲染虚拟 DOM。</p>
</li>
<li>
<p>原理：diff 比较同层列表时，未加 Key 则会继续比较该层的子 dom 变化，导致子 dom 树没有变化的节点被复用；添加 key 值就会直接根据 Key 比较，查找同层列表中 Key 值相同的节点，然后复用列表组件，因此复用整层的 dom 树。</p>
<p><img src="./../images/key5.png" alt="" /></p>
</li>
</ul>
</li>
</ul>
</li>
<li>
<p>原理:<img src="./../images/key2.png" alt="key2" /></p>
</li>
</ul>
<p><img src="./../images/key3.png" alt="key3" /></p>
<ul>
<li>参考资料
<ul>
<li><a href="https://www.zhihu.com/question/61064119" target="_blank" rel="noopener noreferrer">Vue2.0 v-for 中 :key 到底有什么用？</a></li>
</ul>
</li>
</ul>
<h2 id="vue-2-x-缺陷"> VUE 2.X 缺陷</h2>
<h3 id="数组和对象的检测缺陷"> <a href="https://vue3js.cn/docs/zh/guide/change-detection.html#%E5%A3%B0%E6%98%8E%E5%93%8D%E5%BA%94%E5%BC%8F-property" target="_blank" rel="noopener noreferrer">数组和对象的检测缺陷</a></h3>
<ul>
<li>
<p>缺陷原因：</p>
<ul>
<li>vue2.x使用 <code>Object.defineProperty()</code>实现数据响应式</li>
<li><code>Object.defineProperty()</code>存在以下缺陷
<ul>
<li>不能监听数组的变化</li>
<li>无法检测到对象属性的新增或删除</li>
</ul>
</li>
</ul>
</li>
<li>
<p>缺陷检测</p>
<p><img src="./../images/quexian.png" alt="" /></p>
</li>
</ul>
<h3 id="vue2-x数组变更方法实现"> Vue2.x数组变更方法实现</h3>
<ul>
<li>
<p>Vue 也是不能检测到以下变动的数组：</p>
<ul>
<li>当你利用索引直接设置一个项时，例如：<code>vm.items[indexOfItem] = newValue</code>；使用：<code>Vue.set(example1.items, indexOfItem, newValue)</code>；</li>
<li>当你修改数组的长度时，例如：<code>vm.items.length = newLength</code>；使用 <code>vm.items.splice(newLength)</code>。</li>
</ul>
</li>
<li>
<p><a href="https://cn.vuejs.org/v2/guide/list.html#%E5%8F%98%E6%9B%B4%E6%96%B9%E6%B3%95" target="_blank" rel="noopener noreferrer">变更方法</a>：Vue 将被侦听的数组的变更方法进行了包裹，所以它们也将会触发视图更新。</p>
<ul>
<li>这些被包裹过的方法包括：
<ul>
<li><code>push()</code></li>
<li><code>pop()</code></li>
<li><code>shift()</code></li>
<li><code>unshift()</code></li>
<li><code>splice()</code></li>
<li><code>sort()</code></li>
<li><code>reverse()</code></li>
</ul>
</li>
</ul>
</li>
<li>
<p>方法包裹实现思路：</p>
<ul>
<li>
<p>首先，进行代理原型：</p>
<ul>
<li>
<p>原数组对象原型关系： <code>ArrayInstance.__proto__</code> =  <code>Array.prototype</code>;</p>
<p><img src="./../images/vue-proto.png" alt="" /></p>
</li>
<li>
<p>以数组对象的原型 <code>Array.prototype</code> 创建新的对象 <code>arrayMethods</code> 作为代理原型 ；</p>
</li>
<li>
<p>创建新数组对象 <code>methodsToPatch</code>，将新数组对象的 <code>methodsToPatch.__proto__</code> 属性指向了  <code>arrayMethods</code>；</p>
</li>
<li>
<p>将  <code>arrayMethods__proto__</code> 属性指向 <code>Array.prototype</code>;</p>
</li>
<li>
<p>因此经过代理原型后，原型关系为：<code>ArrayInstance.__proto__</code> =  <code>methodsToPatch</code> , <code>methodsToPatch.__proto__</code>  =  <code>Array.prototype</code>;</p>
<p><img src="./../images/proto2.png" alt="" /></p>
</li>
</ul>
</li>
<li>
<p>然后，通过重写数组变更方法，调用缓存的数组变更方法；</p>
</li>
<li>
<p>最后对新插入数据进行数据观测和通知视图变更。</p>
</li>
</ul>
</li>
<li>
<p>源码分析：</p>
<ul>
<li>
<p>在 new Vue() 初始化时候，进入到定义响应式数据，调用 observer()</p>
<div><pre><code><span>export</span> <span>class</span> <span>Observer</span> <span>{</span>
  <span>constructor</span> <span>(</span><span>value<span>:</span> any</span><span>)</span> <span>{</span>
    <span>this</span><span>.</span>value <span>=</span> value
    <span>this</span><span>.</span>dep <span>=</span> <span>new</span> <span>Dep</span><span>(</span><span>)</span>
    <span>this</span><span>.</span>vmCount <span>=</span> <span>0</span>
    <span>def</span><span>(</span>value<span>,</span> <span>'__ob__'</span><span>,</span> <span>this</span><span>)</span>
    <span>if</span> <span>(</span>Array<span>.</span><span>isArray</span><span>(</span>value<span>)</span><span>)</span> <span>{</span>
      <span>const</span> augment <span>=</span> hasProto
        <span>?</span> protoAugment
        <span>:</span> copyAugment
      <span>augment</span><span>(</span>value<span>,</span> arrayMethods<span>,</span> arrayKeys<span>)</span>
      <span>this</span><span>.</span><span>observeArray</span><span>(</span>value<span>)</span>
    <span>}</span> <span>else</span> <span>{</span>
      <span>// ...</span>
    <span>}</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br></div></div><p>这里我们只需要关注 <code>value</code> 是 Array 的情况，首先获取 <code>augment</code>，这里的 <code>hasProto</code> 实际上就是判断对象中是否存在 <code>__proto__</code>，如果存在则 <code>augment</code> 指向 <code>protoAugment</code>， 否则指向 <code>copyAugment</code>，来看一下这两个函数的定义：</p>
<div><pre><code><span>/**
 * Augment an target Object or Array by intercepting
 * the prototype chain using __proto__
 `protoAugment` 方法是直接把 `target.__proto__` 原型直接修改为 `src`
 */</span>
<span>function</span> <span>protoAugment</span> <span>(</span><span>target<span>,</span> src<span>:</span> Object<span>,</span> keys<span>:</span> any</span><span>)</span> <span>{</span>
  <span>/* eslint-disable no-proto */</span>
  target<span>.</span>__proto__ <span>=</span> src
  <span>/* eslint-enable no-proto */</span>
<span>}</span>

<span>/**
 * Augment an target Object or Array by defining
 * hidden properties.
 `copyAugment` 方法是遍历 keys，通过 `def`，也就是 `Object.defineProperty` 去定义它自身的属性值。
 */</span>
<span>/* istanbul ignore next */</span>
<span>function</span> <span>copyAugment</span> <span>(</span><span>target<span>:</span> Object<span>,</span> src<span>:</span> Object<span>,</span> keys<span>:</span> Array<span>&lt;</span>string<span>></span></span><span>)</span> <span>{</span>
  <span>for</span> <span>(</span><span>let</span> i <span>=</span> <span>0</span><span>,</span> l <span>=</span> keys<span>.</span>length<span>;</span> i <span>&lt;</span> l<span>;</span> i<span>++</span><span>)</span> <span>{</span>
    <span>const</span> key <span>=</span> keys<span>[</span>i<span>]</span>
    <span>def</span><span>(</span>target<span>,</span> key<span>,</span> src<span>[</span>key<span>]</span><span>)</span>
  <span>}</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br></div></div><p>对于大部分现代浏览器都会走到 <code>protoAugment</code>，那么它实际上就把 <code>value</code> 的原型指向了 <code>arrayMethods</code></p>
</li>
<li>
<p><code>arrayMethods</code> 的定义在 <code>src/core/observer/array.js</code> 中：</p>
<div><pre><code><span>import</span> <span>{</span> def <span>}</span> <span>from</span> <span>'../util/index'</span>

<span>const</span> arrayProto <span>=</span> <span>Array</span><span>.</span>prototype
<span>//`arrayMethods` 首先继承了 `Array`</span>
<span>export</span> <span>const</span> arrayMethods <span>=</span> Object<span>.</span><span>create</span><span>(</span>arrayProto<span>)</span>

<span>const</span> methodsToPatch <span>=</span> <span>[</span>
  <span>'push'</span><span>,</span>
  <span>'pop'</span><span>,</span>
  <span>'shift'</span><span>,</span>
  <span>'unshift'</span><span>,</span>
  <span>'splice'</span><span>,</span>
  <span>'sort'</span><span>,</span>
  <span>'reverse'</span>
<span>]</span>

<span>/**
 * Intercept mutating methods and emit events
 方法进行重写
 */</span>
methodsToPatch<span>.</span><span>forEach</span><span>(</span><span>function</span> <span>(</span><span>method</span><span>)</span> <span>{</span>
  <span>// cache original method</span>
  <span>const</span> original <span>=</span> arrayProto<span>[</span>method<span>]</span>
  <span>// def 为 defineReactive 方法</span>
  <span>def</span><span>(</span>arrayMethods<span>,</span> method<span>,</span> <span>function</span> <span>mutator</span> <span>(</span><span><span>...</span>args</span><span>)</span> <span>{</span>
    <span>const</span> result <span>=</span> <span>original</span><span>.</span><span>apply</span><span>(</span><span>this</span><span>,</span> args<span>)</span>
    <span>const</span> ob <span>=</span> <span>this</span><span>.</span>__ob__
    <span>let</span> inserted
    <span>switch</span> <span>(</span>method<span>)</span> <span>{</span>
      <span>case</span> <span>'push'</span><span>:</span>
      <span>case</span> <span>'unshift'</span><span>:</span>
        inserted <span>=</span> args
        <span>break</span>
      <span>case</span> <span>'splice'</span><span>:</span>
        inserted <span>=</span> args<span>.</span><span>slice</span><span>(</span><span>2</span><span>)</span>
        <span>break</span>
    <span>}</span>
     <span>//获取到插入的值，然后把新添加的值变成一个响应式对象</span>
    <span>if</span> <span>(</span>inserted<span>)</span> ob<span>.</span><span>observeArray</span><span>(</span>inserted<span>)</span>
    <span>// notify change</span>
    ob<span>.</span>dep<span>.</span><span>notify</span><span>(</span><span>)</span>
    <span>return</span> result
<span>}</span><span>)</span>
<span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br><span>17</span><br><span>18</span><br><span>19</span><br><span>20</span><br><span>21</span><br><span>22</span><br><span>23</span><br><span>24</span><br><span>25</span><br><span>26</span><br><span>27</span><br><span>28</span><br><span>29</span><br><span>30</span><br><span>31</span><br><span>32</span><br><span>33</span><br><span>34</span><br><span>35</span><br><span>36</span><br><span>37</span><br><span>38</span><br><span>39</span><br><span>40</span><br><span>41</span><br><span>42</span><br><span>43</span><br><span>44</span><br></div></div><ul>
<li>对原来数字方法进行重写，重写的方法会先执行它们本身原有的逻辑，并对能增加数组长度的 3 个方法 <code>push、unshift、splice</code> 方法做了判断，获取到插入的值，然后把新添加的值变成一个响应式对象</li>
<li>再调用 <code>ob.dep.notify()</code> 手动触发依赖通知</li>
</ul>
</li>
</ul>
</li>
</ul>
<hr>
<h2 id="vue2-x问题"> VUE2.x问题</h2>
<p><strong>基础题</strong></p>
<blockquote>
<p><strong>如何让CSS只在当前组件中起作用</strong>？</p>
</blockquote>
<p>Q：在组件中的 style 标签上加上 scoped。</p>
<blockquote>
<p><strong>如何获取dom?</strong></p>
</blockquote>
<p>Q：ref=&quot;domName&quot; 用法：this.$refs.domName</p>
<blockquote>
<p><strong>vue获取数据在哪个周期函数?</strong></p>
</blockquote>
<p>Q：普通单页应用项目一般在 created 钩子以后，如： created、before Mount、mounted。</p>
<p>SSR 项目中： 由于没有动态更新，所有的生命周期钩子函数中，只有 <code>beforeCreate</code> 和 <code>created</code> 会在服务器端渲染 (SSR) 过程中被调用。这就是说任何其他生命周期钩子函数中的代码（例如 <code>beforeMount</code> 或 <code>mounted</code>），只会在客户端执行。所以静态化的数据一般只在 <code>beforeCreate</code> 和 <code>created</code> 中服务端获取渲染，动态获取的数据在 <code>beforeMount</code> 或 <code>mounted</code> 中客户端获取。</p>
<blockquote>
<p><strong>第一次页面加载会触发哪几个钩子？</strong></p>
</blockquote>
<p>Q：第一次页面加载即为首次渲染过程，首次渲染不涉及数据更新和页面销毁过程，所以只触发 beforeCreate, created, beforeMount, mounted ，并在 mounted 的时候DOM渲染完成。</p>
<blockquote>
<p><strong>vue中数据渲染的时候如何保证将数据原样输出？</strong></p>
</blockquote>
<ul>
<li>v-text：将数据输出到元素内部，如果输出的数据有HTML代码，会作为普通文本输出</li>
<li>v-html：将数据输出到元素内部，如果输出的数据有HTML代码，会被渲染</li>
<li>{{}}：插值表达式，可以直接获取Vue实例中定义的数据或函数，使用插值表达式的时候，值可能闪烁；而使用v-html、v-text不会闪烁，有值就显示，没值就隐藏</li>
</ul>
<blockquote>
<p>props 和 $attr 区别？</p>
</blockquote>
<ul>
<li>
<p><a href="https://cn.vuejs.org/v2/api/#props" target="_blank" rel="noopener noreferrer">props</a>:props 可以是数组或对象，用于接收来自父组件的数据。</p>
</li>
<li>
<p><a href="https://cn.vuejs.org/v2/api/#vm-attrs" target="_blank" rel="noopener noreferrer">vm.$attrs</a>:包含了父作用域中不作为 prop 被识别 (且获取) 的 attribute 绑定 (<code>class</code> 和 <code>style</code> 除外)。当一个组件没有声明任何 prop 时，这里会包含所有父作用域的绑定 (<code>class</code> 和 <code>style</code> 除外)，并且可以通过 <code>v-bind=&quot;$attrs&quot;</code> 传入内部组件——在创建高级别的组件时非常有用。</p>
</li>
</ul>
<p><strong>应用题</strong></p>
<h2 id="vue2-x源码问题"> VUE2.x源码问题</h2>
<ul>
<li><strong>初始化过程相关问题</strong></li>
</ul>
<blockquote>
<p><code>beforeCreate</code> 钩子函数前完成了什么？</p>
</blockquote>
<p>Q：beforeCreate 之前，主要是在处理 vm 实例上的各种属性配置和自定义事件属性，也就是<strong>将 Vue 的壳初始化完成</strong>。
首先合并了组件的配置项挂载到全局 vm.$options 上。初始化组件实例关系属性，如：$parent、$children、$root、$refs 等等，然后初始化自定义的事件监听，最后初始化组件的插槽 slot 和作用域插槽scopedSlots，createElement（即 render 函数，同时定义了组件 attrs 和 $listeners属性。）</p>
<blockquote>
<p>父子组件中，子组件调用执行本身注册的自定义事件 A()，那么父子组件中，谁监听事件 A() 的执行调用？</p>
</blockquote>
<p>Q：谁注册了自定义事件，则谁监听自定义事件。故是子组件监听事件。</p>
<blockquote>
<p><code>created</code> 钩子函数前完成了什么？</p>
</blockquote>
<p>Q：created 钩子函数是在 Vue 壳构建完成后，开始初始化实例的响应式数据和方法。
首先初始化好 inject 配置项，再初始化各种响应式数据和方法如：props、methods、data、computed、watch，最后初始化 vm._provided 属性。</p>
<blockquote>
<p><code>initInjections(vm)</code>、<code>initState(vm)</code>、<code>initProvide(vm)</code> 三者的执行顺序可否变化？</p>
</blockquote>
<p>不可以，源码中有官方注释。
inject 配置项是注入数据，在后续的 computed 和 data 中均可以或需要使用注入数据，故解析 injections 需要在 data/props 前。
解析 provide 实际上只是将 vm.$options.provide 挂载到 vm._providedinject 上，需要等响应式数据和方法初始化完毕后再执行。inject 和 provide 是成对出现的，一个注入，一个接收。</p>
<div><pre><code><span>initInjections</span><span>(</span>vm<span>)</span> <span>// resolve injections before data/props</span>
<span>initState</span><span>(</span>vm<span>)</span>
<span>initProvide</span><span>(</span>vm<span>)</span> <span>// resolve provide after data/props</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br></div></div><blockquote>
<p>Vue 的初始化过程？</p>
</blockquote>
<p>Q：Vue 初始化过程其实就是 beforeCreate 钩子函数和 created 钩子函数前执行的内容。</p>
<ul>
<li>在 beforeCreate 前，主要先初始化搭建了 Vue 实例的壳，如组件的 options 配置项，组件实例的关系属性，处理了自定义事件。</li>
<li>在 created 前，主要是初始化实例的响应式数据和方法，首先初始化 inject 配置项，再初始化数据响应式和方法，最后解析组件配置项上的 provide 对象。总结来说构建初始化 Vue 实例对象 vm。</li>
</ul>
<blockquote>
<p>vue 的类使用 ES5 function定义的，为什么不用 ES6 class 定义？</p>
</blockquote>
<p>源码中有很多 <code>xxxMixin</code> 的函数调用，并把 <code>Vue</code> 当参数传入，它们的功能都是给 Vue 的 prototype 上扩展一些方法，Vue 按功能把这些扩展分散到多个模块中去实现，而不是在一个模块里实现所有，这种方式是用 Class 难以实现的。</p>
<p>这么做的好处是非常方便代码的维护和管理</p>
<blockquote>
<p>怎么实现 this._test 改变而不是 this._data.test 改变触发更新?</p>
</blockquote>
<p>这中间有一个 代理的过程: 本质就是通过 Object.defineProperty 使在访问 this 上的某属性时从 this._data 中读取（写入）。</p>
<div><pre><code><span>function</span> <span>_proxy</span> <span>(</span><span>data</span><span>)</span> <span>{</span>
    <span>const</span> that <span>=</span> <span>this</span><span>;</span>
    Object<span>.</span><span>keys</span><span>(</span>data<span>)</span><span>.</span><span>forEach</span><span>(</span><span>key</span> <span>=></span> <span>{</span>
        Object<span>.</span><span>defineProperty</span><span>(</span>that<span>,</span> key<span>,</span> <span>{</span>
            configurable<span>:</span> <span>true</span><span>,</span>
            enumerable<span>:</span> <span>true</span><span>,</span>
            <span>get</span><span>:</span> <span>function</span> <span>proxyGetter</span> <span>(</span><span>)</span> <span>{</span>
                <span>return</span> that<span>.</span>_data<span>[</span>key<span>]</span><span>;</span>
            <span>}</span><span>,</span>
            <span>set</span><span>:</span> <span>function</span> <span>proxySetter</span> <span>(</span><span>val</span><span>)</span> <span>{</span>
                that<span>.</span>_data<span>[</span>key<span>]</span> <span>=</span> val<span>;</span>
            <span>}</span>
        <span>}</span><span>)</span>
    <span>}</span><span>)</span><span>;</span>
<span>}</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br></div></div><blockquote>
<p>Q:为什么vue组件中data对象、props、methods的key值不能重复(为什么data和props的key相同时会冲突)？</p>
</blockquote>
<p>A:因为data、props、methods最终都会挂载到vue实例上(vm)，vue将这些属性代理到 vue 实例上，最终可以通过this.xxx访问属性</p>
<blockquote>
<p>Q:如果组件中props对象和data对象中的Key重复,谁优先级高？</p>
</blockquote>
<p>A:props优先级高</p>
<blockquote>
<p>Q: 为什么通过this可以直接访问data对象中的数据？（比如：data对象中包含message，然后在钩子函数或函数中通过this.message可以访问该数据）</p>
</blockquote>
<p>A: 在vue初始化时，将data对象挂载到vue实例上的vm._data属性中(将data设置为vue实例的私有变量)，</p>
<p>然后通过Proxy代理(使用Object.defineProperty)设置data的key的getter和setter,在获取data时,代理到vm._data中(源码：src/core/instance/state.js中165行)</p>
<blockquote>
<p>Q: 能不能将vue实例mount()挂载到 html 或则 body 元素上？</p>
</blockquote>
<p>A: 不能。因为在vue生成的dom中会替换mount的节点，如果挂载到 html 或 body 上，会导致 html 页面结构改变</p>
<blockquote>
<p>Q: 为什么我们在其它组件中使用 <code>&lt;keep-alive&gt;</code> 组件不需要注册?</p>
</blockquote>
<p>A: vue通过 extend(Vue.options.components, builtInComponents) 把一些内置组件扩展到 Vue.options.components 上</p>
<blockquote>
<p>Q: options 的合并方式？</p>
</blockquote>
<p>A: options 的合并有 2 种方式:第一种，外部初始化 Vue 通过 mergeOptions；第二种，子组件初始化过程通过 initInternalComponent 方式</p>
<blockquote>
<p>Q: vue中钩子函数的合并策略函数？</p>
</blockquote>
<p>A: mergeHook()函数中定义合并策略(src/core/utils/options)</p>
<p><strong>全局API</strong></p>
<blockquote>
<p>Vue 初始化全局 API 时，做了什么？</p>
</blockquote>
<ul>
<li>1.Vue 初始化了全局的 config 配置并设为响应式。</li>
<li>2.暴露一些工具方法，如日志、选项扩展、选项合并、设置对象响应式</li>
<li>3.暴露全局初始化方法，如 Vue.set、Vue.delete、Vue.nextTick、Vue.observable</li>
<li>4.暴露组件配置注册方法，如  Vue.options.components、Vue.options.directives、Vue.options.filters、Vue.options._base</li>
<li>5.暴露全局方法，如 Vue.use、Vue.mixin、Vue.extend、Vue.initAssetRegisters()</li>
</ul>
<blockquote>
<p>Vue 全局 API 有什么作用？</p>
</blockquote>
<ul>
<li>
<p>Vue.use()： 用来安装 plugin 插件，对插件进行缓存优化，并执行 install() 安装。</p>
</li>
<li>
<p>Vue.mixin()：用来在 Vue 的全局配置上合并 options 配置。并且每个组件生成 vnode 时会合并全局配置和组件配置，因此可以作为抽离公共的业务逻辑，实现公共的业务逻辑，也就是类的继承。</p>
</li>
<li>
<p>Vue.extend()：用来在 Vue 实例扩展子类，可以用于一些公共组件化配置上。与 Vue.mixin() 区别，我认为 extend 更多的是公众的组件化，也就是类的多态，外观模式。</p>
</li>
<li>
<p>Vue.initAssetRegisters()：用来将实例上的 component、directive、filter 对象配置到全局的 Vue.options 上。</p>
</li>
<li>
<p><strong>响应式原理相关问题</strong></p>
</li>
</ul>
<blockquote>
<p>Vue 如何处理响应式数据？</p>
</blockquote>
<p>Q：响应式的数据主要分为两类：Object 和 Array</p>
<ul>
<li>
<p>Object 对象则利用 defineReactive()，来循环遍历整个对象，通过 Object.defineProperty 设置 getter 和 setter 的拦截，再通过观察者模式双向绑定来实现对象响应式原理</p>
</li>
<li>
<p>Array 数组则利用 <code>Array.split()</code> 重写数组的那 7 个可以更改自身的原型方法来实现响应式。</p>
</li>
<li>
<p><strong>数据更新相关问题</strong></p>
</li>
</ul>
<blockquote>
<p>Vue 默认更新是同步的还是异步的？</p>
</blockquote>
<p>Q：Vue 默认异步更新，通过 <code>watcher.async</code>。Vue 源码还设置了开启同步更新的操作，可以通过设置 <code>watcher.sync</code> 的属性，在 watcher.update() 方法时并直接执行 watcher.run() 方法进行更新操作。<strong>但 Vue 官方不推荐使用该属性，因同步更新机制将阻塞后续任务的执行，整个组件更新将大打折扣。</strong></p>
<blockquote>
<p>Vue 是如何避免重复执行同一次异步更新？</p>
</blockquote>
<p>Q：通过三个标识符的操作来进行避免重复执行同一次的异步更新：</p>
<ul>
<li>
<p>在将 watcher 放入 watcher 队列时，进行了 id 的缓存，避免重复 watcher 添加到 queue 数组。</p>
</li>
<li>
<p>通过 waiting 判断是否正在刷新 queue 队列，避免重复执行刷新 queue 队列。</p>
</li>
<li>
<p>通过 pending 判断浏览器的异步任务队列中是否有刷新 callbacks（放的是刷新 queue 队列的任务） 数组的任务，避免浏览器异步任务队列重复执行刷新 callbacks 数组的任务。</p>
</li>
</ul>
<blockquote>
<p>Vue 是如何将刷新 callbacks 数组的函数放入浏览器任务队列进行异步更新的？</p>
</blockquote>
<p>Q：根据浏览器任务队列异步执行的效率来选择放入方法的优先级，分别为：</p>
<ol>
<li>Promise.resolve().then(flushCallbacks)</li>
<li>new MutationObserver(flushCallbacks)
<ul>
<li>提供了监视对DOM树所做更改的能力（HTML5 中的新特性）</li>
</ul>
</li>
<li>setImmediate(flushCallbacks)</li>
<li>setTimeout(flushCallbacks, 0)</li>
</ol>
<hr>
<h2 id="vue-3-x"> VUE 3.x</h2>
<h3 id="vue2-与-vue3-比较"> VUE2 与 VUE3 比较</h3>
<h4 id="语法差异"> 语法差异</h4>
<p><a href="https://vue3js.cn/docs/zh/guide/migration/introduction.html#%E4%BB%8B%E7%BB%8D" target="_blank" rel="noopener noreferrer">vue3.x 文档-vue2迁移</a>章节中介绍了 vue3.x 新增特性和 vue2 差异。</p>
<p><strong>补充说明</strong></p>
<p>Vue 3 中需要关注的一些新功能包括：</p>
<ul>
<li>
<p><a href="https://vue3js.cn/docs/zh/guide/composition-api-introduction.html" target="_blank" rel="noopener noreferrer">组合式 API</a>：</p>
<ul>
<li>提供 <code>setup</code> 组件选项：在<strong>创建组件之前</strong>执行，一旦 <code>props</code> 被解析，并充当合成 API 的入口点。</li>
<li><code>setup</code> 选项应该是一个接受 <code>props</code> 和 <code>context</code> 的函数。<code>setup</code> 返回的所有内容都将暴露给组件的其余部分 (计算属性、方法、生命周期钩子等等) 以及组件的模板。</li>
<li>作用：在多个组件中复用相同的代码逻辑。把组件逻辑模块化复用，增加代码可读性。
<ul>
<li>vue2.x 中提供组件代码复用的 API：<code>mixin</code>、<code>slot</code>。</li>
</ul>
</li>
</ul>
</li>
<li>
<p><a href="https://vue3js.cn/docs/zh/guide/teleport.html" target="_blank" rel="noopener noreferrer">Teleport</a>：</p>
<ul>
<li>新的内置组件，将组件内的模板移动到 <code>to</code> 属性指定的 DOM 节点之下。</li>
<li>应用场景：用于类似 <code>modals</code>,<code>toast</code> 这类和 <code>Vue</code> 应用的 <code>DOM</code> 完全剥离的组件，这类组件如果嵌套在 <code>Vue</code> 的某个组件内部，那么处理嵌套组件的定位、<code>z-index</code> 和样式就会变得很困难，因此通过 <code>Teleport</code> 可以解决。</li>
</ul>
</li>
<li>
<p><a href="https://vue3js.cn/docs/zh/guide/migration/fragments.html" target="_blank" rel="noopener noreferrer">片段</a>：支持多根节点组件，即片段。</p>
</li>
<li>
<p><a href="https://vue3js.cn/docs/zh/guide/component-custom-events.html" target="_blank" rel="noopener noreferrer">组件选项 emits</a></p>
</li>
<li>
<p><a href="https://github.com/vuejs/vue-next/tree/master/packages/runtime-core" target="_blank" rel="noopener noreferrer"><code>createRenderer</code> API 来自 <code>@vue/runtime-core</code></a> 创建自定义渲染器</p>
</li>
</ul>
<p><strong>创建实例方式</strong></p>
<ul>
<li>
<p>vue2: 使用 <code>new Vue()</code> 创建实例。</p>
<ul>
<li>缺点：通过 <code>new Vue()</code> 创建的根 Vue 实例。从同一个 Vue 构造函数<strong>创建的每个根实例共享相同的全局配置</strong>，因此全局 API 影响所有 vue 实例。</li>
</ul>
<div><pre><code><span>// 这会影响两个根实例</span>
Vue<span>.</span><span>mixin</span><span>(</span><span>{</span>
  <span>/* ... */</span>
<span>}</span><span>)</span>

<span>const</span> app1 <span>=</span> <span>new</span> <span>Vue</span><span>(</span><span>{</span> el<span>:</span> <span>'#app-1'</span> <span>}</span><span>)</span>
<span>const</span> app2 <span>=</span> <span>new</span> <span>Vue</span><span>(</span><span>{</span> el<span>:</span> <span>'#app-2'</span> <span>}</span><span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br></div></div></li>
<li>
<p>vue3：使用 <a href="https://vue3js.cn/docs/zh/api/global-api.html#createapp" target="_blank" rel="noopener noreferrer"><code>createApp()</code></a> 创建实例。</p>
<ul>
<li>解决 vue2 全局 api 缺点</li>
</ul>
<div><pre><code>Vue<span>.</span><span>createApp</span><span>(</span><span>{</span>
  <span>data</span><span>(</span><span>)</span> <span>{</span>
    <span>return</span> <span>{</span>
      a<span>:</span> <span>1</span>
    <span>}</span>
  <span>}</span><span>,</span>
  <span>created</span><span>(</span><span>)</span> <span>{</span>
    <span>// `this` 指向 vm 实例</span>
    console<span>.</span><span>log</span><span>(</span><span>'a is: '</span> <span>+</span> <span>this</span><span>.</span>a<span>)</span> <span>// => "a is: 1"</span>
  <span>}</span>
<span>}</span><span>)</span>
<span>//应用实例暴露的大多数方法都会返回该同一实例，允许链式：</span>
Vue<span>.</span><span>createApp</span><span>(</span><span>{</span><span>}</span><span>)</span>
  <span>.</span><span>component</span><span>(</span><span>'SearchInput'</span><span>,</span> SearchInputComponent<span>)</span>
  <span>.</span><span>directive</span><span>(</span><span>'focus'</span><span>,</span> FocusDirective<span>)</span>
  <span>.</span><span>use</span><span>(</span>LocalePlugin<span>)</span>
</code></pre>
<div><span>1</span><br><span>2</span><br><span>3</span><br><span>4</span><br><span>5</span><br><span>6</span><br><span>7</span><br><span>8</span><br><span>9</span><br><span>10</span><br><span>11</span><br><span>12</span><br><span>13</span><br><span>14</span><br><span>15</span><br><span>16</span><br></div></div></li>
</ul>
<p><strong>生命周期</strong></p>
<ul>
<li>
<p>vue 3.x 生命周期：</p>
<p><img src="./../flow-graph/vue3-lifecycle.png" alt="" /></p>
</li>
</ul>
<p><img src="./../images/vue2-vue3-lifecycle.png" alt="" /></p>
<h4 id="性能差异"> 性能差异</h4>
<ul>
<li>
<p>Vue 3.x 响应式系统使用  <a href="https://es6.ruanyifeng.com/#docs/proxy" target="_blank" rel="noopener noreferrer">ES6 Proxy</a>，Vue2.x 使用 <code>Object.defineProperty()</code> 实现。</p>
</li>
<li>
<p>使用 Proxy 重构原因：</p>
<ul>
<li>
<p>原因1：</p>
<ul>
<li>
<p><code>Object.defineProperty()</code> 存在以下缺陷 (参考本章 vue2.x 缺陷):</p>
<ul>
<li>不能监听数组的变化。因此 vue2.x 针对数组是多做了一层处理，代理了数组的基本方法。</li>
<li>无法检测到对象属性的新增或删除;</li>
</ul>
</li>
<li>
<p><a href="https://es6.ruanyifeng.com/#docs/proxy" target="_blank" rel="noopener noreferrer">ES6 Proxy</a>优化：能响应新增的属性。当新增一个属性的时候，可以响应到get中，对当前对象/数组进行代理。</p>
</li>
</ul>
</li>
<li>
<p>原因2：</p>
<ul>
<li><code>Object.defineProperty()</code> 只能劫持对象的单个属性,因此需要对每个对象的每个属性进行遍历。
<ul>
<li>data/computed/props  中的属性进行响应式初始化时 (<code>initData</code>-&gt; <code>observer()</code>)是通过 递归 + 遍历  对象来实现对数据的监控的，如果属性值也是对象那么需要深度遍历，如果 vue 对象的 data/computed/props 中的数据规模庞大，那么遍历起来就会慢很多。（详细过程参考本章 响应式 源码分析过程）</li>
</ul>
</li>
<li>Vue3.x 使用 Proxy 的监听是针对一个对象的，那么对这个对象的所有操作会进入监听操作，因此不需要遍历 + 递归。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="参考资料"> 参考资料</h3>
<p><a href="https://juejin.cn/post/6858558735695937544#heading-5" target="_blank" rel="noopener noreferrer">超全的Vue3文档【Vue2迁移Vue3】</a></p>
<hr>
<h2 id="参考资料-2"> 参考资料</h2>
<h3 id="源码相关"> 源码相关</h3>
<p><a href="http://caibaojian.com/vue-analysis/" target="_blank" rel="noopener noreferrer">Vue.js 技术揭秘</a></p>
]]></content>
    <contributor>
      <name/>
    </contributor>
    <published>2021-06-25T19:47:29.000Z</published>
  </entry>
</feed>